# 149.1 Invoice logs offline product

## Scope and decision

Implement the frozen offline-only special `api_invoice_logs_list`. This node is
a leaf (`max_depth`, `max_children`, and `max_descendants` are all zero), so no
delegation is possible or needed. The cited shared contract is
`wiki/wave_fivesa_invoice_logs_list_research.md`; it fixes the path, required
query, sort values, response root, and all excluded surfaces.

## Deliverables

1. Add `src/billy_mcp/api/invoice_log_reads.py` with a strict request model for
   nonempty `invoiceId` and `organizationId`, Literal-only `eventTime`/`DESC`
   sort controls, an opaque extra-preserving entry model, typed success model,
   and a list-only handler over `BillyHttpClient`.
2. Add a transport-only contract suite covering the complete query, opaque
   sample and unknown fields, validation rejection, malformed root/element
   `BILLY_ERROR`s, typed auth propagation, and the exact one-tool FastMCP
   schema/result.
3. Wire only `register_invoice_log_read_tools` into the real server and extend
   its registry assertion to exactly 265 API tools, including the one new name.
4. Add only `api.special.invoice_logs` to
   `OFFLINE_API_IMPLEMENTATION_EVIDENCE`, regenerate the coverage artifacts,
   and confirm 180 implemented/contract-tested API rows with every live,
   vision, UI, and bulk state unchanged and `complete: false`.
5. Run the formatter check, Ruff, Pyright, focused API and server tests,
   coverage generation/checker, and the configured non-live/non-vision suite.
   Commit only owned product, test, generator, and generated coverage files;
   report the commit and offline-only non-claims through radio.

## Constraints and feasibility

The parent baseline is already current at `97f7e12`; there are no child
branches. The node reports 1709 seconds remaining and no cost cap. All
validation will use in-process `httpx.MockTransport`, never a credential,
browser, or live Billy request.

## Post-Mortem

### Accomplishments

- Delivered exactly one list-only `api_invoice_logs_list` tool, its strict
  input/output models, real-server registration, root registry assertion, and
  transport-only contract suite.
- Regenerated coverage from the source-controlled evidence map; only
  `api.special.invoice_logs` changed to implemented and contract-tested.
- Verified 265 API tools, 180 offline implemented/contract-tested rows, zero
  live and vision rows, and `complete: false`.

### Deviations

- None. A strict-type narrowing issue found during the first lint pass was
  corrected before the final lint and test runs.

### Next-iteration notes

- No product work remains in this leaf; the parent needs the commit for its
  integration and separate Grok product review.
