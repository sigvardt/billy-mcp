# 177.1 Repair auth submit-label validation

1. Replace the stale production submit-label allowlist with the exact trimmed
   values `Log in` and `Log ind`, retaining the fixed `data-cy=login-button`
   signature and all pre-submit safety checks.
2. Update the fake page default and happy-path parametrizations to the two
   allowed labels; add direct coverage that `Login` returns `UI_CHANGED` before
   credential resolution, fill, or click.
3. Run the focused browser unit tests, then the node lint and project test
   scripts. Inspect the scoped diff, commit only the production and unit-test
   repair, and report the result to the parent.

## Post-Mortem

### Accomplishments

- Replaced the stale production `Login` allowance with the observed `Log in`
  value while retaining `Log ind`.
- Updated fake defaults and happy paths; the named legacy-page-title case proves
  `Login` is rejected before resolution, fill, or click.
- Focused browser tests, node scripts, Ruff, and Pyright passed.

### Deviations

None. The repair remained limited to the two owned source and test files.

### Next-iteration notes

No additional implementation is needed; commit the two scoped files and hand
the hash and verification result to the parent.
