# 12.1 Contacts API read tools

Leaf execution: all child limits are zero, so this focused two-file change stays
local.

1. Add `src/billy_mcp/api/contact_reads.py` with strict Pydantic inputs,
   typed success envelopes, optional documented paging, and the two pure
   read handlers. Keep contact payloads opaque JSON objects, map only the
   documented `contact`/`contacts` roots and `meta.paging`, and return the
   shared `ToolError` from `BillyHttpClient` unchanged.
2. Register only `api_contacts_get` and `api_contacts_list` through an
   area-level registration function. Construct relative `/contacts` paths and
   pass only `page`, `pageSize`, `include`, `sortProperty`, and
   `sortDirection`; do not touch root server wiring or coverage state.
3. Add `tests/api/test_contact_reads.py` using `httpx.MockTransport` for
   request shape, singular/list root mapping, paging minimum/maximum and
   absent-paging behavior, typed authentication errors, Pydantic validation,
   and both registrations.
4. Format and run focused pytest, Ruff, Pyright, then the node lint and test
   scripts from the committed bytes. Report coverage IDs `api.contacts.get`
   and `api.contacts.list`, exact test path, and `live_tested: false` to the
   parent without editing the manifest or server.

## Post-Mortem

Implemented the two typed contacts reads and their isolated FastMCP
registration. Mock transport tests cover the documented query surface,
singular/list response roots, absent and boundary paging, authentication
translation, and the registered schema/result. No shared-interface change was
needed; server wiring and coverage state remain intentionally with the parent.
