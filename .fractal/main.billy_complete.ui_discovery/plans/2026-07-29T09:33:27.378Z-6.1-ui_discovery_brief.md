# 6.1 Billy UI discovery brief (read-only mit.billy.dk)

## Goal

Produce the first **cited, non-sensitive** Billy interface-discovery brief under
`wiki/` for the parent. Scope is **wiki only**. No source, tests, coverage
manifests, scripts, package metadata, or seed edits. No coverage row, UI tool,
or `not_applicable` assertion turns green from this pass.

## Delegation decision

`max_child_depth=0`, `max_children=0`, `max_descendants=0` → **leaf / solo**.
No spawn triggers fire; do not create children.

## Contracts and inputs

| Source | Path / URL | Use |
| --- | --- | --- |
| Design | `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` (§4.6 headless, §7.2 browser auth, §10 interface lane) | Rules and discovery discipline |
| Parent research | `/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete/.fractal/main.billy_complete/tmp/grok-research.md` | Hosts, blockers, UI red-row posture (read-only) |
| Official API docs | `https://www.billy.dk/api/` | API parity **context only** — never via browser lane |
| Browser runtime | `src/billy_mcp/browser.py` (`BrowserRuntime` + `BrowserEgressPolicy`) | Headless, deny-by-default egress |
| Default profile | `/Users/user/.local/share/billy-mcp/chrome-profile` (exists) | MCP-owned persistent profile |
| Config | `src/billy_mcp/config.py` (`BILLY_BROWSER_PROFILE`, token env names) | Session/credential paths outside git |

## Observed orientation (pre-execute)

- Clean tree at `a870594`; no backstop `(failed on …)` commit; node `tmp/` empty.
- Shared foundation present (includes `BrowserRuntime`); **no** `coverage/` tree
  on this branch yet — do not invent or green any coverage rows.
- Project wiki has `billy_api_v2_research_seed`, `phase_zero_contract` only.
- Node memory empty.
- Shell: no `BILLY_*` env vars set at orient time; profile directory **exists**.
- Parent research: browser host `mit.billy.dk`; API exclusive
  `api.billysbilling.com`; deny `api.billy.dk` for browser; credentials historically
  missing on host for live qualification.

## Safety envelope (hard stop)

1. Browser: Chromium **headless only** via `BrowserRuntime` (or equivalent
   Playwright with the same constraints). Profile under
   `/Users/user/.local/share/billy-mcp/chrome-profile` unless
   `BILLY_BROWSER_PROFILE` overrides.
2. Egress: `BrowserEgressPolicy(["mit.billy.dk"])` only — HTTPS default port.
   No arbitrary URL, no API hosts through the browser, no generic helper.
3. Read-only: no create/update/delete, no settings changes, no token create,
   no form submits that mutate Billy data. Login submit only if stored creds
   already configured **outside git** and the flow is pure auth (no write of
   business records). Prefer reuse of persisted session cookies first.
4. Stop and record blocker on: missing credentials, CAPTCHA, passkey, push
   approval, unsupported MFA, paywall/plan gate, or any non-automatable step.
5. Frames: if vision needed, write under node
   `.fractal/main.billy_complete.ui_discovery/tmp/` only, inspect, **purge**
   before commit; durable brief records purge result without paths/secrets.
6. Never mark coverage green or write `not_applicable` from this discovery.

## Execute sequence

### 1. Auth surface check (no browser yet)

- Resolve profile path from env or default; confirm directory exists.
- Check for stored session signals (profile cookies/storage present — names
  only, no secret dump).
- Check env for credential-related keys (`BILLY_API_TOKEN`, any documented
  UI/email/password/TOTP names in design/config) — record **present/absent**,
  never values.
- If neither live session nor configured credentials: still attempt one
  headless open of `https://mit.billy.dk/` to record unauthenticated landing
  screen, then publish **blocker brief** (do not invent route inventory).

### 2. Bounded headless session

- Launch `BrowserRuntime(profile, BrowserEgressPolicy(["mit.billy.dk"]))`.
- Open only `https://mit.billy.dk/` (and in-app navigations that stay on that
  host if already authenticated).
- Capture non-sensitive observations: final URL, title, auth state
  (logged-out form vs app shell vs MFA challenge), safe DOM selectors
  (stable labels/roles/ids without PII).
- If authenticated: record only routes/screens **actually reached** in this
  pass (bounded — auth + a few primary nav landings if available without
  writes). For each: DOM assertion refs + possible independent read-back route
  (API resource name as **possibility only**, not a green claim).
- If MFA/CAPTCHA/passkey/push: stop; screenshot to scratch only if needed for
  classification; purge after note.
- Close runtime cleanly; purge any frames.

### 3. Durable wiki brief

Write `wiki/billy_ui_discovery_brief.md` (snake_case) with:

- Observed host and policy (`mit.billy.dk` only)
- Session state (authenticated / unauthenticated / blocked)
- Routes/screens **actually reached** (observed vs blocked vs unverified)
- Safe selectors / DOM assertions
- Read-back possibility (unverified)
- Plan/MFA/credential blockers
- No-write confirmation
- Evidence-purge result
- Explicit: no coverage greened; distinguishes observed facts from speculation

Cite design + parent research path + that official API docs were context-only
(no browser traffic to `www.billy.dk/api` or API hosts).

### 4. Wiki hygiene and node gates

- `wiki update --path=…/wiki` and `wiki lint --path=…/wiki`
- Seed memory `state` page (present-tense working state only) if useful for
  remainder of iteration
- Run node `scripts/lint.sh` and `scripts/test.sh` when REVIEW/COMMIT require
  (plan assumes EXECUTE does discovery + wiki; REVIEW runs hooks)

### 5. Radio delivery

Post parent-visible outbox (or completion-path `send --parent` at finish) with:

- Wiki path of the brief
- Verdict: `observed routes` **or** `blocked before auth` + precise blocker
- Confirmation: headless + allowlist + no writes + purge + no greens

## Out of scope this iteration

- Editing `src/`, tests, `coverage/*`, scripts, package metadata
- Greening any API/UI coverage row
- Headed browser, desktop control, multi-host browsing
- Creating test records or changing Billy settings
- Spawning children

## Success criteria (maps to Completion Requirements)

1. Committed wiki brief with observed vs blocked/unverified.
2. Any browser use proven headless, MCP profile, `mit.billy.dk` only.
3. Raw evidence outside repo, purged; brief records purge without sensitive content.
4. No coverage/tool/`not_applicable` green claims.
5. `wiki update`/`wiki lint` pass; node lint/test hooks pass; parent radio
   delivery with path + verdict + blockers.

## Budget / feasibility

- Time remaining at plan: ~1186s step budget class; cost unlimited.
- Solo leaf; one bounded Playwright session + one wiki page is in budget.
- Primary risk: auth blocked → deliver **blocker brief** (still success).
- Profile exists; env tokens unset at orient — expect session-reuse or
  credential/MFA blocker as most likely outcomes.

## Post-mortem

_(fill at REVIEW)_

## Post-Mortem

### Accomplishments

- Headless discovery via `BrowserRuntime` + `BrowserEgressPolicy(["mit.billy.dk"])`
  with MCP default profile; no headed window.
- Observed only `/login` (`Log ind`); recorded safe field names and labels.
- Published cited brief at `wiki/billy_ui_discovery_brief.md` with observed vs
  blocked vs unverified; credentials/keyring absence as precise blocker.
- Classification screenshot purged from node scratch; durable brief keeps no
  frame paths or secrets.
- Radio delivery to outbox and parent inbox; no coverage greens; no source edits
  outside `wiki/`.
- Node lint/test hooks passed during execute.

### Deviations

- No authenticated navigation (plan allowed blocker brief): env credentials and
  keyring default were empty; profile storage did not restore an app session.
- Did not load official API docs in any browser lane (context-only by design).
- No vision re-inspection beyond capture-and-purge; classification used DOM text.

### Next-iteration notes

- Unblock requires UI credentials or a live session outside git (operator).
- After credentials: re-run headless allowlisted session; still no greens without
  dedicated non-production workflow + read-back + durable review.
- Parent must merge this branch wiki page; coverage inventory remains other
  nodes' ownership.
