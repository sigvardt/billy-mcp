# 16.1 Wave-3 document-line reads

1. Create `line_reads.py` following the unfiltered catalogue service pattern:
   shared forbid-extra get/list inputs, opaque records, optional paging, encoded
   item paths, and typed root mapping for invoice, bill, and daybook-transaction
   lines. Keep registration module-local.
2. Create focused contract tests for all six tool names, the three singular and
   plural camel-case roots, relative paths/query serialization, encoded IDs,
   paging preservation, forbid-extra input schemas, and 401 authentication
   translation.
3. Run the focused test file, Ruff format/check, and Pyright; correct any
   contract or typing issue without changing server wiring, coverage, redaction,
   or the frozen wiki.
4. Commit only the owned product/test files, report the commit and offline
   verification to the parent, then complete the node after the required
   terminal-state cleanup.

## Post-Mortem

- Completed the six typed get/list tools in `line_reads.py` and the focused
  contract suite in `test_line_reads.py` without touching server wiring,
  redaction, coverage evidence, or the frozen wiki.
- Independent review found that an empty `include` was accepted. Both request
  models and FastMCP wrappers now require a non-empty value when it is present.
  The same pass added tests for empty metadata serialization and flat FastMCP
  get/list invocation.
- Verification: focused pytest passes 19 tests; Ruff and Pyright pass; the
  commit-mode repository suite passes 147 tests. The memory wiki is clean after
  its generated separator repair.
- Cleanup state: scoped product/test files remain the only delivery artifacts;
  server registration, coverage evidence, and live qualification were not
  changed. Parent-owned registration and evidence integration are the remaining
  coverage slice.
