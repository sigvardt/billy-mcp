# 23.1 Wave 4 Geo Reads

Implement the frozen eight-tool geo read slice in the two owned paths only.
No child delegation is possible because depth, child, and descendant limits are
all zero.

1. Add `geo_reads.py` by adapting the Wave-3 `account_reads.py` pattern: strict
   flat request models, encoded identifiers, opaque response records, optional
   `meta.paging`, locked-client GET handlers, typed upstream errors, and one
   flat FastMCP registration hook for country groups, cities, states, and
   zipcodes.
2. Make cities, states, and zipcodes list models require a non-empty
   `countryId`; keep country groups on the default list surface and keep all
   singular reads free of that parameter. Reject `offset`, parent identifiers,
   and undeclared keys locally.
3. Add focused mock-transport tests for every documented path/root, query
   serialisation, encoded identifiers, optional paging metadata, local
   validation, typed 401 mapping, and the flat registered schemas.
4. Run the required formatter check, Ruff, Pyright, and focused pytest suite;
   repair only the owned files if any check fails. Review the final diff for
   scope, no live/UI/coverage claims, and no credentials before committing.

## Post-Mortem

- Completed `geo_reads.py` and `test_geo_reads.py`: eight typed GET tools,
  flat registration, strict request allowlists, encoded IDs, opaque roots,
  optional `meta.paging`, and typed 401 mapping.
- Stayed within the frozen evidence and two-file product scope; no documents or
  live endpoints were queried during implementation, and no coverage, UI,
  write, bulk, or completeness state changed.
- Independent review passed with no required fixes. It noted optional empty
  `sortProperty` and whitespace-only `countryId` hardening only; the frozen
  contract specifies a free-form sort field and non-empty country IDs by
  `min_length=1`, so neither advisory warrants an out-of-contract change.
- Verification passed: focused Ruff format/check, Pyright, and 25 Geo tests;
  the node suite and policy checks also pass. No credentials, browser evidence,
  or disposable records were created.
- Full mode runs the same 249 tests successfully and then fails only at the
  designed repository-wide completeness gate for red coverage and ambiguous
  bulk rows; that expected failure is not attributable to this leaf.
- Final evidence is ready for the scoped commit. Root-owned follow-up remains
  registration and coverage inventory integration; this leaf leaves those
  states unchanged.
