# 72.1 Wave-5h attachment fallback contract

## Decision

This is a leaf node with exactly one owned project file, so no child can own a
separable deliverable without adding coordination overhead and violating the
file-ownership boundary. Work stays local.

## Evidence baseline

- The direct, credential-free official-doc fetch matches the manifest and the
  parent research lead: ETag `hsisik4g9p3603`, 147934 bytes, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`.
- The scratch brief limits the candidate to the three documented singular
  attachment JSON CUD rows. The parent has reiterated that this fallback has no
  independent-review or gate authority.

## Execution

1. Create only `wiki/wave_fiveh_ticketed_writes_contract.md` with valid wiki
   frontmatter and explicit Codex Power fallback-candidate authority.
2. Freeze exactly the create, update, and delete attachment rows and their six
   preview/execute names; distinguish external `/v2` documentation paths from
   runtime-relative paths under `https://api.billysbilling.com/v2`.
3. State strict outer/opaque inner input rules, `priority` as the sole clear
   mutable candidate, body/route ID equality, no-write preview, ticket-only
   execute, server-owned binding, one root protocol/store, no retry, errors,
   sensitivity, cleanup, response limits, and required offline tests.
4. Exclude owner serialization, unobserved roots, binary file upload,
   `api.files.create`, `api.special.files_upload`, bulk attachment operations,
   webhooks, UI/live/vision work, coverage changes, and completeness claims.
5. Review the exact diff, run `git diff --check`, wiki lint, and the inherited
   test and lint scripts. Commit the sole project deliverable; report the
   non-authoritative candidate and verification results to the parent; drain
   radio, keep memory terminal, then finish this node.

## Post-Mortem

Completed the cited Wave-5h attachment JSON CUD fallback candidate with exactly
three inventory rows and six preview/execute tools. The direct official-doc
refresh matched the recorded manifest fingerprint, so no source or inventory
drift required a contract change.

The independent-review step was limited to a static, non-authoritative fallback
audit because an independent Grok verdict is expressly reserved for later. Its
only finding was the generated project-wiki index link. The parent authorised
that sole derived edit; no other existing project page, source, test, coverage,
or manifest file changed.

`git diff --check`, project and memory wiki lint, the inherited lint script,
and the ordinary test suite passed; the suite reported 892 passing tests. The
full-qualification test was run and correctly rejected the intentionally
incomplete repository (`complete: false`, 92 ambiguous-bulk API rows, and
incomplete API/UI qualification). No credential, browser, Billy mutation,
interface evidence, or coverage update was introduced.
