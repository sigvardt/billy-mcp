# 17.1 Contact-person read tools

Implement directly because this leaf has no child capacity and the frozen Grok
brief is already available at `.fractal/main.billy_complete.wave3_contact_persons/tmp/grok-research.md`.

1. Add `contact_person_reads.py`, mirroring the typed contact read pattern with
   strict/forbid request models, opaque payload models, the documented
   `contactPerson`/`contactPersons` roots, optional paging serialization, safe
   ID quoting, and a module-local two-tool registration function.
2. Add a focused contract suite using mock HTTP responses that contains no email
   fields. Cover encoded get paths, the exact list query surface and paging
   mapping, omitted metadata, invalid and unknown inputs (including `offset` and
   `contactId`), typed 401 errors, and FastMCP registration signatures.
3. Run focused pytest, Ruff format/check, Pyright, and the node test script;
   correct only the two owned files. Commit the scoped delivery and report it to
   the parent without claiming root registration, coverage, live testing, or
   project completeness.

## Post-Mortem

- Completed the typed `api_contact_persons_get` and `api_contact_persons_list`
  module-local tools with quoted IDs, opaque contact-person roots, optional
  paging, exact list inputs, and typed upstream authentication errors.
- Added 14 focused contract tests with no email fields. Independent review
  passed; it found no required code correction.
- Focused pytest, Ruff format/check, and Pyright pass. The node commit-mode
  suite passes 142 tests. Full mode also passes 142 tests before correctly
  failing the project-wide, fail-closed coverage gate: 92 ambiguous bulk rows,
  305 incomplete API rows, and 339 incomplete UI rows remain.
- Cleanup state is limited to the two owned source/test files and node-local
  planning and memory records. The unresolved slice is root-owned server
  registration and evidence for this wave; no coverage or live claim is made
  by this leaf.
