# 145.1 Wave-5q users product independent review

## Goal

Authoritative offline Grok independent product review of the Wave-5q
users-update ticketed-write product merged on parent baseline `e47da25`.
Sole deliverable: `wiki/wave_fiveq_users_product_independent_review.md` with
unambiguous **ACCEPT** or **REJECT**. Review only; never repair source.

## Constraints (hard)

- Leaf: max children/depth/descendants = 0 → solo work; no spawn.
- Own only `wiki/wave_fiveq_users_product_independent_review.md`.
- Do not edit `wiki/_index.md`, source, tests, manifests, coverage/status,
  generator mappings, root memory, or other wiki pages.
- Do not run `wiki update` after adding the page (parent owns derived index).
- No credentials, live mutations, headed browser, raw evidence retention.
- No live/UI/vision/cleanup/completeness claim; ACCEPT is offline-scoped only.
- REJECT must name exact root finding for later Codex Power repair.

## Reviewed baseline and product surface

| Item | Value |
| --- | --- |
| Parent baseline | `e47da25` (merge-base = parent tip; leaf at `da9dd47` init only) |
| Product merge | `5d81a0f` merge `wave5q_users_product` |
| Product impl | `a3641fe` adds `user_writes.py` + tests + inventory greening |
| Module | `src/billy_mcp/api/user_writes.py` |
| Registration | `src/billy_mcp/server.py` (`register_user_write_tools`) |
| Tests | `tests/api/test_user_writes.py`, `tests/unit/test_coverage_server.py` |
| Freeze | `wiki/wave_fiveq_ticketed_writes_contract.md` (freeze IR ACCEPT) |
| Expected tools | exactly `api_users_update_preview` + `api_users_update_execute` |
| Expected coverage | 178 offline contract-tested rows; 262 API tools; complete false; live 0; vision 0 |

## Delegation decision

No children. Caps are 0. Work is a single cited review page with external doc
re-fetch plus local inspection; not separable into parallel owners.

## EXECUTE steps (ordered)

### 1. External evidence (Grok ownership)

1. Re-fetch `https://www.billy.dk/api/` via curl only (no browser).
   Record HTTP status, ETag, body MD5, byte size. Extract Users Supports and
   property notes (create/update/delete/bulk wording).
2. Unauthenticated method gates against locked base
   `https://api.billysbilling.com/v2` (no token, no mutation success claim):
   - `POST /users` with JSON object body → expect **405**
   - `PUT /users/{id}` with JSON object body → expect **401**
   - `DELETE /users/{id}` → expect **405**
   Record status + error codes only; do not store raw response bodies in
   tracked files.

### 2. Source and contract inspection (read-only)

Against freeze page + freeze IR ACCEPT, verify:

| Check | Pass criteria |
| --- | --- |
| Two-tool surface | Only update preview+execute; no create/delete/bulk/generic tools |
| Outer input | Strict model: non-empty `id` + `user` map; no undeclared outer fields |
| Inner map | Opaque `dict`; optional `user.id` must equal path `id` if present |
| Path / root | Client-relative `PUT /users/:id`; success root only `users` |
| Tickets | Execute ticket-only; exact binding; single-use; expiry; no-retry |
| Redaction | PII/privilege sensitivity path uses shared redaction infrastructure |
| Registration | `register_user_write_tools` wired in `server.py` |
| Mapping | `api.users.update` greened offline; create absent; bulk still red; UI parity red |
| Honesty | status: complete false; contract_tested 178; live 0; vision 0; no false greens |

Also confirm absence of create/delete user write tools in registry/tests.

### 3. Focused offline verification

Run non-live focused tests (worktree venv / uv as available):

- `tests/api/test_user_writes.py`
- registry/tool-list assertions covering `api_users_update_*` in
  `tests/unit/test_coverage_server.py` (or full unit file if cheap)
- coverage policy / inventory checks as appropriate
  (`tests/coverage/test_coverage_inventory.py` slice if needed)

Also `git diff --check` on the review file after writing.

Do not run live mode or full qualification suite as a completeness claim.

### 4. Write the review page

Create `wiki/wave_fiveq_users_product_independent_review.md` modeled on
`wiki/wave_fivep_product_independent_review.md` / `wave_fiveo_*` structure:

- Frontmatter: name, title, desc, tags, sources, created/updated
- Verdict table + bold ACCEPT or REJECT
- Reviewed baseline (exact SHA `e47da25` and product ancestry)
- Evidence classes table (docs / unauth gates / offline tests / not-live)
- Official docs citation with this-review fingerprints
- Method-gate results (status/codes only)
- Checklist rows for two-tool contract, typed boundaries, tickets, redaction,
  registration/mapping, coverage honesty
- Explicit non-claims
- No secrets, no raw evidence dumps

If REJECT: name the single root defect (file + rule) for Codex Power repair.

### 5. Commit, handoff, finish discipline (later steps)

- COMMIT: stage only the review page; use `fractal commit` (or git commit in
  step); tree clean; no prohibited paths.
- Outbox: one-line verdict handoff to parent.
- Before `node finish`: run node `scripts/test.sh` if completion requires it;
  drain memory; empty saved radio; do not `wiki update`.

## Memory layout (leaf)

Keep one present-tense `state` page in node memory: baseline, evidence
fingerprints, interim checklist results, final verdict. No iteration labels.

## Done when

1. Cited review page exists at the sole path with ACCEPT or REJECT.
2. Baseline named; evidence + source + offline verification support verdict.
3. Frozen two-tool contract, types, tickets, mapping, redaction, coverage
   honesty all explicitly checked.
4. No live/UI/vision/cleanup/credential/completeness claim; no sensitive data.
5. Focused checks + `git diff --check` pass; review committed; outbox reports
   verdict.

## Feasibility

Budget: no time/cost limit. Scope is one leaf review page. No blockers from
memory (empty). Parent already at product baseline. Risk: method-gate network
failure → record attempt and fail closed on that evidence class rather than
guess; if gates cannot be rechecked, REJECT for missing independent gate
evidence rather than inherit prior research numbers uncritically.

## Post-Mortem

### Accomplishments

- Independent Grok product review delivered at
  `wiki/wave_fiveq_users_product_independent_review.md` with unambiguous
  **ACCEPT** for offline Wave-5q users update-only tools on baseline `e47da25`.
- Re-fetched official docs (ETag `wcw4x9hqvu3603`, MD5
  `8b94b0135c91fd15fe54ea33e088a4be`, 147934 bytes) and independently confirmed
  Supports: get by id, list, update, bulk save, bulk delete (no create, no
  singular delete).
- Independently confirmed unauth method gates: POST 405, PUT 401, DELETE 405.
- Source inspection confirmed exactly two tools, strict outer models, opaque
  user map, optional path/body id equality, client-relative PUT `/users/:id`,
  `users` success root, ticket/no-retry protocol, registration, and coverage
  honesty (178 offline / 262 tools / live 0 / vision 0 / complete false).
- Focused offline suite: 32 pytest passed; coverage and repository policy
  checks passed.

### Deviations

- Node `scripts/test.sh` remains a no-op; product proof used explicit focused
  `uv run pytest` plus `check_coverage.py` / `check_repository_policy.py`
  (documented on the review page).
- Privilege-flag redaction keys were treated as a residual non-blocking note
  rather than REJECT (email/phone redacted; no log leakage observed).

### Next-iteration notes

- COMMIT only the review wiki page (and allowed node memory/plan if in scope).
- Do not run project `wiki update`; parent owns `_index.md`.
- Outbox handoff after commit: ACCEPT offline product at `e47da25`.
