# 19.1 Wave-3 chart-of-accounts reads

1. Implement `account_reads.py` by adapting the existing unfiltered contacts-read
   pattern: strict request models, opaque payloads, optional paging metadata, safe
   response-root validation, and six module-local MCP registrations.
2. Add focused mock-transport tests for all three documented roots, encoded IDs,
   documented query allowlists, missing/present paging, local input rejection,
   typed authentication errors, and exact tool schemas without exposing bank data.
3. Run the focused suite, Ruff format/check, and Pyright; repair any regressions
   while keeping changes in the two owned files.
4. Commit the scoped deliverable, update private state for handoff, notify the
   parent without claiming coverage, live verification, or completeness, and
   finish after the node test script confirms the committed work.

## Post-Mortem

- Completed `account_reads.py` and its focused suite: six typed account,
  account-group, and account-nature reads with strict query validation, encoded
  IDs, opaque payloads, optional paging, and typed authentication errors.
- Followed the established unfiltered contacts-read pattern; no writes, live
  requests, UI work, coverage changes, or shared-redaction changes were made.
- Independent review passed the product contract. Its optional test gaps were
  closed by covering list 401 mapping, all registered list invocations, and
  default paging query construction.
- Final local evidence: 22 focused tests pass; Ruff and Pyright pass; the
  offline node suite passes 150 tests. Full qualification fails closed because
  `coverage/status.json` is incomplete, 92 bulk rows remain ambiguous, and the
  repository-wide API/UI evidence is intentionally red.
- Cleanup state: no live records, browser artifacts, secrets, or bank-field
  fixture values were created. The cited contract remains the shared record;
  this node contributes no new project-wiki fact.
