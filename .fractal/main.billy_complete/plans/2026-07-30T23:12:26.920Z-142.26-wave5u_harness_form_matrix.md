# 142.26 Wave-5u bulk delete form-matrix harness

## Objective

Encode the Grok research95 bulk-delete form matrix as fail-closed harness
infrastructure only. Preserve the current honest coverage posture: no residual
or bulk tool, no live/UI flag change, and no network activity in offline CI.

## Evidence and constraints

- Research95 is the cited authority in
  `.fractal/main.billy_complete/tmp/grok-research.md`. Its official docs
  re-fetch is byte-identical to research92--94 (ETag
  `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`).
- The frozen candidate query key is exactly `ids[]`. Empty query/body array
  variants return `INVALID_DELETE_ID_ARRAY`; they are never a safe no-op.
  An unauthenticated synthetic non-empty id returning metadata-only 200 is not
  non-persistence, authenticated-wire, cleanup, or live-test evidence.
- The existing Wave-5u contract blocks every residual/bulk real method before
  network unless reviewed non-persistence evidence and dual organisation gates
  exist. OPTIONS is infrastructure only and must not imply method support.

## Delegation decision

The form-matrix encode is a bounded, independent repository implementation with
clear file ownership, so a focused `codex-power` leaf will own only
`src/billy_mcp/live_probe.py` and `tests/unit/test_live_probe.py`. A parallel,
read-only `grok` leaf will independently review research95 and own only the new
durable record `wiki/wave_fives_research95_independent_review.md`. Neither leaf
may touch coverage files, FastMCP tool registration, browser/auth product files,
or the active auth-review child’s scope. Research95 is already a cited Grok
brief, so it satisfies the research-before-implementation rule; the parallel
review independently verifies its promotion to the shared record.

## Execution

1. Initialise and configure `wave5u_form_matrix_harness` with
   `--agent=codex-power`, and `wave5u_research95_review` with `--agent=grok`.
   Give both leaf depth/child limits and disjoint NODE contracts, then start
   them after their seeds are committed.
2. Require explicit unit proof that candidate construction retains `ids[]`,
   empty bulk-delete forms cannot be treated as a harmless probe, and no token
   or organisation gate can reach a residual/bulk real method. Keep the
   existing options-only observation path unchanged unless a fail-closed
   correction is necessary.
3. Require the Grok review record to state whether the research95 sources,
   citations, form-matrix conclusions, and zero-greening boundary are accepted
   or rejected. It must not reclassify unauthenticated results as live proof.
4. Monitor the active auth credential independent-review child. Merge only a
   material, review-backed finding; otherwise intentionally close it as the
   already-accepted offline slice is duplicated.
5. Review both leaf diffs from clean archives, run the focused harness tests,
   merge only the bounded infrastructure and review-record work, then run the
   repository verification suite required by the implementation and inspect
   coverage for unchanged red rows.

## Acceptance criteria

- No `api_*` residual/bulk tool, no preview/execute operation, no webhook, and
  no browser or live credential interaction is introduced.
- Offline tests prove block-before-network, exact `ids[]` construction, and
  that validation/meta-only unauth results never qualify a candidate.
- Coverage remains incomplete and retains zero new live or vision claims.
- Auth review child is assessed without merging stale or duplicate work.
