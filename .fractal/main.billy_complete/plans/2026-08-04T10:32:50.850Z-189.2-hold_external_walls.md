# 189.2 HOLD product external walls bulk92 residual29

## Goal

Record an honest **product HOLD** after research re-check: no bulk or residual
tool greening, no docs-fingerprint-only churn package, no annual re-open.
Preserve walls until Billy publishes bulk schema, live API scope reopens, or
owner answers residual/bulk `out_of_scope_by_user` (parent radio **B5C1EBE3**).

Authority: `.fractal/main.billy_complete/tmp/grok-research.md` (2026-08-04).

## Non-goals

- Invent `api_*_bulk_*` or residual ticketed write tools
- Live API probes / re-open annual dual browser work
- Restart research195–200 style MD5 re-lock as sole product deliverable
- Wiki-only stop-churn packages (STEER **8612433E**)
- No-op docs HOLD loops (STEER **7A4311C2**)
- Claim `complete: true`
- Child nodes (nothing separable to implement)

## Baseline (HEAD `6fe4ac9`)

| Metric | Value |
| --- | --- |
| `complete` | false |
| implemented / contract | 528 / 528 |
| UI live / vision | 344 / 344 |
| Walls | bulk **92** + residual **29** |
| Annual | `out_of_scope_by_user` `ANNUAL_REPORTS_OWNER_SKIP` (not a blocker) |
| Docs lock | MD5 `8b94b0135c91fd15fe54ea33e088a4be` ETag `wcw4x9hqvu3603` (research: unchanged) |

## Design choices

1. **HOLD product tree.** Empty EXECUTE against `src/`, `coverage/` greening,
   inventory flag flips, and new tools. Residual and bulk stay red + toolless.
2. **Steers bind.** No wiki-only churn; no fingerprint re-lock theater. Memory +
   plan (+ optional private note) are the package, not false product progress.
3. **Parent owner-scope ask already sent** (`B5C1EBE3`, P6). EXECUTE does **not**
   invent owner skip for residual/bulk without a reply. If a parent decision
   lands mid-loop in a later SYNC, supersede this plan then.
4. **Completeness remains false** until external unlock. Do not finish the node
   as complete.

## Execute steps (root, Grok only)

### 1. Confirm product tree clean vs HEAD

```bash
git status -sb
git diff --stat HEAD -- src/ tests/ coverage/ wiki/ scripts/
```

Expect: no product diffs (memory/plan/tmp only allowed).

### 2. Confirm coverage invariants (read-only)

```bash
python3 -c "import json; d=json.load(open('coverage/status.json')); print(d['complete'], d['qualification'])"
```

Expect: `complete` false; implemented/contract 528; live/vision 344; blocker
still bulk freeze (+ residual honesty implicit).

### 3. No children

Do not spawn. Ownership stays on root for a zero-file product HOLD.

### 4. Memory / plan only for COMMIT package

- Append EXECUTE result to this plan post-mortem after EXECUTE/IR/FIX-VERIFY.
- Update `memory/state.md` Current state.
- Do **not** regenerate coverage or edit generator for residual/bulk owner-scope
  unless parent replies yes.

### 5. Independent review + FIX-VERIFY + COMMIT

- IR: package PASS if product tree empty and no greening; node complete FAIL.
- FIX-VERIFY: lint + default `test.sh` if anything dirty beyond plan/memory; if
  product tree clean, still run lint/test only if required by commit hooks /
  FIX-VERIFY step rules (prefer run offline suite once if COMMIT includes more
  than fractal metadata — for plan+memory only, follow prior HOLD practice).
- COMMIT: plan + memory HOLD record; push via `fractal commit`.
- **Not** `fractal node finish` as qualification complete.

## Success criteria

| Check | Pass |
| --- | --- |
| Product tree vs HEAD empty for src/tests/coverage/wiki tools | yes |
| No new bulk/residual tools | yes |
| `complete` still false | yes |
| Walls still bulk92 + residual29 | yes |
| Annual remains owner-scoped | yes |
| Steers 8612433E / 7A4311C2 honored | yes |
| Node finish as complete | **no** |

## Unlock paths (outside this plan)

1. Billy publishes field-level bulk schema or OpenAPI → bulk product freeze.
2. User reopens live API qualification → residual method map can be proven.
3. Owner replies yes on residual and/or bulk `out_of_scope_by_user` → new plan
   slice (generator + tests + wiki), parallel to 189.1 annual skip.

## Children

None.

## EXECUTE post-mortem (189.2)

Date: 2026-08-04. Agent: grok.

| Check | Result |
| --- | --- |
| Product tree vs HEAD (`src/` `tests/` `coverage/` `wiki/` `scripts/`) | **empty** |
| Untracked product paths | **none** |
| `complete` | **false** |
| implemented / contract | **528** / **528** |
| UI live / vision | **344** / **344** |
| bulk92 empty tools | **true** |
| residual29 empty tools | **true** |
| annual owner-scope | `out_of_scope_by_user` / `ANNUAL_REPORTS_OWNER_SKIP` |
| Docs lock | MD5 `8b94b0135c91fd15fe54ea33e088a4be` ETag `wcw4x9hqvu3603` |
| Greening / new tools | **none** |
| Children spawned | **none** |
| Parent B5C1EBE3 residual/bulk owner-scope | **no reply at EXECUTE** (HOLD still correct) |

HOLD product honored. Dirty tree is only memory + this plan. Not finish.

## Post-Mortem

### Work completed

- RESEARCH: docs lock re-check unchanged (MD5 `8b94b013…` / ETag `wcw4x9hqvu3603`); OpenAPI 404; no product unlock.
- PLAN 189.2: HOLD product (no greening, no children).
- EXECUTE: product tree empty vs HEAD `6fe4ac9`; walls bulk92 + residual29; annual still owner-scoped.
- Parent P6 ask `B5C1EBE3` residual/bulk owner-scope: unanswered at FIX-VERIFY.
- IR: package **PASS**, node complete **FAIL** (expected); zero required fixes.
- FIX-VERIFY: no IR fixes to apply; product tree still clean.

### Deviations

None. HOLD honored under steers 8612433E / 7A4311C2.

### Review findings

- Package PASS; no greening; no false API live green; bulk/residual remain toolless red.
- Report: `.fractal/main.billy_complete/tmp/grok-review.md`

### Test results

| Check | Result |
| --- | --- |
| lint.sh | pass |
| test.sh | **1671 passed**, 54 deselected (~9m51s) |
| wiki lint (project) | clean |
| wiki lint (memory) | clean |
| BILLY_TEST_MODE=full / ui-full | not run (complete still false; not a completeness claim) |

### Cleanup state

No disposable UI records or browser frames created this package.

### Coverage snapshot

- `complete: false`
- implemented/contract **528**; UI live/vision **344**
- Walls: bulk **92** + residual **29**
- Annual: `out_of_scope_by_user` / `ANNUAL_REPORTS_OWNER_SKIP`

### Next unresolved coverage slice

External walls only. Unlock when Billy publishes bulk schema, live API scope reopens, or owner answers residual/bulk `out_of_scope_by_user` (radio **B5C1EBE3**). Do not invent tools. Do not claim complete. COMMIT plan+memory HOLD record next.
