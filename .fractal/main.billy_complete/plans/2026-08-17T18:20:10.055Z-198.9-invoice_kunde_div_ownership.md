# 198.9 Invoice Kunde unnamed DIV ownership

## Goal

Prove ownership of the unnamed right-edge `DIV` on the invoice
**Kunde** field (`9F777B8F`). First a failing unit fixture that the
current live opener misses the DIV-ownership keys. Then the smallest
helpers that record them. Then one live read-only recapture at rest.
Click once, as a normal Playwright position action at the dumped
page point, only if that evidence proves the DIV belongs to the same
Kunde control. Wait for a visible option before type. If ownership
is not proved, do not click. Record `UI_CHANGED`. Stay red.

Do not repeat the 198.8 chevron-hit dump. Do not guess a selector
for a classless DIV. Do not remap invoice CUD rows.

Authority: `9F777B8F`, `96908DC6`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Prior dumps `51E18E60` and `A3AB03C3` stay
in force as constraints. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Repeating the 198.8 chevron-hit keys as the new fixture
- Clicking the nameless DIV without the ownership gate
- Another center `field.click()` plus type
- Guessed sibling, caret, or classless-DIV selector
- Force click, `evaluate` click, Escape, Tab, Enter, ArrowDown
- Products, ledger, files, org, or bills
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Copying the bills vendor wrapper onto this field
- Treating a hidden `contactId` as a bind
- Persisting screenshots, frames, HAR, or traces in git
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `31039b5`. Memory edits from this continue are uncommitted.
Not a backstop. `complete=false`. Honesty 16 stay red. Contacts and
bills CUD rows already name preview tools. Invoice CUD rows still
name `ui_invoices_*_open`.

`A3AB03C3` dump is complete. Live opener box is `250x40` at
`(65,121)`. Center `elementFromPoint` is `INPUT` name `contact`.
Right-edge offset `{dx:242, dy:20}`. Right-edge hit is a `DIV`
with empty `class_tokens`, `name=null`, `testid=null`.
`right_edge_same_input=false`. No click. `named_opener=null`.
After prior type: `option_role_count=0`, GET `/v2/contacts` count 0.

Current dump does **not** record:

- `right_edge_elements_from_point_stack`
- `hit_box`
- `hit_pointer_events`
- `hit_role`
- `hit_name_present`
- `hit_testid`
- `hit_class_tokens`
- `hit_direct_parent`
- `hit_contained_by_input`
- `hit_contains_input`
- `hit_shares_smallest_wrapper`
- `smallest_wrapper`
- `nearest_clickable_ancestor`

`pointer_events` on the opener is the **input**, not the hit DIV.
`_OWNERSHIP_JS` uses topmost `elementFromPoint` only.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/invoices_kunde.py`
- `src/billy_mcp/ui_writes/invoices_form_observe.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

Parked write-family children stay parked. Unlimited budget is not
a reason to spawn.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is a failing unit
   fixture. The current live opener fails
   `div_ownership_missing_keys`. Run it. Capture the failure. Then
   implement. Do not reuse `chevron_hit_missing_keys` as the new
   fixture.
2. **Honesty-16 stays red.** Confirm the coverage gate still
   length 16 before any other edit.
3. **Keys are flags, counts, allowlisted tokens, and integer
   boxes.** Never store tag text, ids, accessible names, CSS urls,
   or pixels in git.
4. **Keep the 198.8 samples.** Center and right-edge
   `elementFromPoint` stay. Add `elementsFromPoint` at the same
   right-edge point, max 8 stack rows.
5. **Ownership gate.** `div_belongs_to_kunde_control` is true only
   when `hit_shares_smallest_wrapper` is true,
   `smallest_wrapper.contact_input_count == 1`, and at least one of:
   `hit_contains_input`, the stack includes `INPUT` name `contact`,
   or `nearest_clickable_ancestor.is_contact_input`.
6. **One allowed action if proved.** One normal
   `page.mouse.click` at the dumped page point (`box.x + dx`,
   `box.y + dy`). That is a position action, not a CSS guess. Wait
   for a visible option. Do not type until an option is visible.
   If no option, return `UI_CHANGED` and stop.
7. **If ownership is not proved, do not click.** Record
   `UI_CHANGED`. Stay red. Next slice is a different read-only
   capture.
8. **Read-only first.** Open `/:org_slug/invoices/new`. Recapture
   at rest before any click. Do not save a draft.
9. **Do not remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.
10. **Vision.** Live test may write `author=live_test` and
    `reviewer_verdict=pending_review` only. Field shot lives under
    `~/.local/share/billy-mcp/` and is purged after review.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_DIV_OWNERSHIP_KEYS` and `div_ownership_missing_keys`
in `invoices_kunde.py` **only after** a unit fixture fails because
those names do not exist, or add the fixture first against a local
helper stub that is not yet imported from product code. Preferred
shape:

- Fixture copies the current live opener from
  `~/.local/share/billy-mcp/inspect-live-invoices-kunde-opener.json`
  (flags only; no tag text).
- Helper returns the missing key list in the Goal table above.
- Current opener must fail with that exact set.

Do not edit observe/bind product files until that fixture fails.

Keep `chevron_hit_missing_keys`, `widget_contract_missing_keys`,
`test_typed_contact_dump_is_not_a_kunde_bind`,
`after_type_is_existing_customer_observation`, and
`test_bind_does_not_press_extra_keys_on_unbound_contact`.

### 2. Smallest helper, then live recapture

- Implement the helper in `invoices_kunde.py`.
- `div_belongs_to_kunde_control` implements the ownership gate.
- Extend `_OWNERSHIP_JS` / `observe_kunde_opener` / `observe_kunde`
  with the new flags. Isolate unit dumps so they cannot overwrite
  the owner files.
- `hit_pointer_events`: `auto` / `none` / `other`.
- Stack rows use the existing class-token allowlist
  (`ds-`, `ember-`, `Dropdown`, `input`, `form`).
- Recapture a redacted field shot outside git (`present` / `bytes` /
  `box` only in the dump).
- Live path: `create_server` login, open
  `/:org_slug/invoices/new`, wait for networkidle plus a short
  field wait, dump at rest. Do not type. Do not click **Gem som
  kladde**.
- After the dump is complete, apply the ownership gate. If the
  click runs, wait for a visible option. Do not type. If no
  option, `UI_CHANGED` and stop. If ownership is not proved, do
  not click.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the ownership gate into memory `state.md` / `todo.md`. No
  iteration labels.

### 3. Do not write a draft

This slice is dump plus at most one evidenced position click.
Do not click **Gem som kladde**. Do not persist an invoice. Do not
add PUT egress.

If observer login fails, recapture from the primary FastMCP
session and stop fail-closed.

### 4. Do not green

Honesty 16 stays red. Invoice `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. Do not call
`apply_ui_invoices_cud_preview_tool_names`.

Run honesty + new unit tests, then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline) after
the helper lands. Live recapture is the product proof, not the
offline suite.

## Done when

- Failing DIV-ownership fixture existed, then the helper lists the
  missing keys on the current live opener.
- Observe records those keys. Live recapture writes a complete
  DIV-ownership dump.
- Either one evidenced position click ran and waited for a
  visible option, or no click because ownership was not proved
  (`UI_CHANGED`).
- Honesty 16 still red. `complete=false`.
- Zero leftover tagged invoices or contacts. Field shots stay
  outside git and are purged after review if any were written.

## Budget

Unlimited. No children. One slice. Do not expand into other CUD
families this iteration.

## Post-Mortem

Work completed: failing DIV-ownership fixture first, then
`REQUIRED_DIV_OWNERSHIP_KEYS` / `div_ownership_missing_keys` /
`div_belongs_to_kunde_control` in `invoices_kunde_div.py`, then
observe stack and wrapper dump, then live recapture. Ownership
proved: smallest wrapper `ember-view` `DIV` with
`contact_input_count=1`, stack includes `INPUT` name `contact`.
One position click at `{x:307, y:141}`. After click:
`option_role_count=0`, GET `/v2/contacts` count 0. `UI_CHANGED`.
Honesty 16 stay red. No remap.

Deviations: helpers live in `invoices_kunde_div.py` instead of
growing `invoices_kunde.py`. No child spawn. No type. No **Gem som
kladde**.

Review findings: FAIL. Dump slice met `9F777B8F`. Invoice CUD still
unbound. Field shot showed **Vælg kunde** plus a visible chevron.
No draft, no persist, no read-back, no vision accept. Field shot
purged after review.

Tests: honesty-16 pass. DIV-ownership unit tests pass. Live
ownership dump pass. Offline `BILLY_TEST_MODE=commit` `test.sh`
1908 passed, 63 deselected on EXECUTE and again on FIX-VERIFY.
Did not run `BILLY_TEST_MODE=full` or `ui-full`. `complete=false`.

Cleanup: field shot `inspect-live-invoices-kunde-field.png` purged
after review. Zero `billy-live-invoices-*` profiles. Shared chrome
profiles untouched. No invoice or contact created this slice.

Next unresolved coverage slice: a different read-only capture. Do
not repeat the proved overlay click. Stay red until a later
observation names a real bind, then one FastMCP draft CUD with
independent read-back.

