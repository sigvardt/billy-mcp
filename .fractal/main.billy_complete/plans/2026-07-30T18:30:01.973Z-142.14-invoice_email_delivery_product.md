# 142.14 Ticketed invoice email and e-invoice delivery specials

## Gate

Do not initialize or start this product leaf until the repaired Wave-5s-B
upload slice has a fresh Grok product ACCEPT. The cited product-ready handoff
and its independent research review already satisfy the research prerequisite;
they do not waive this sequencing gate or provide a product acceptance.

## Authority and scope

- Current cited brief: `.fractal/main.billy_complete/tmp/grok-research.md`
  (research83), preserved by the durable pages
  `wiki/wave_fivesc_invoice_email_delivery_research.md`,
  `wiki/wave_fivesc_invoice_email_delivery_product_ready_research.md`, and its
  independent review.
- Implement exactly four tools: `api_invoices_send_email_preview`,
  `api_invoices_send_email_execute`, `api_invoice_deliveries_create_preview`,
  and `api_invoice_deliveries_create_execute`.
- Email uses bound `POST /invoices/{invoiceId}/emails` with an `email` object;
  no raw recipient addresses, multi-recipient fan-out, or destination URL.
- Delivery uses `POST /invoiceDeliveries` with `invoiceDelivery`; bind both
  `invoiceId` target and `organizationId`. `receiverIdType` is `gln` or `cvr`;
  GLN needs exactly 13 digits and CVR mode must not invent `receiverCvr`.
- Response roots are provisional and must fail closed. No get/list/update/delete
  invoiceDeliveries tools, webhooks, live sends, UI work, or coverage greening
  beyond implemented plus contract-tested rows are in scope.

## Delegated execution after the gate

1. Create a Codex Power leaf `wave5sc_invoice_email_delivery_product` with
   `--agent=codex-power`, assigning it
   `src/billy_mcp/api/invoice_email_delivery_writes.py`, the two focused API
   test files, `src/billy_mcp/server.py`,
   `scripts/generate_coverage_report.py`, and the two special rows in
   `coverage/api_v2_manifest.yaml` / `coverage/status.json` only as generated
   evidence permits.
2. Require custom `ConfirmationStore` services rather than
   `WriteProtocolService`, strict Pydantic/FastMCP boundaries, ticket-only
   executes, canonical path/request binding, no HTTP during preview, redaction
   coverage for email subject/body, and exact base URL locking.
3. Require tests for replay, expiry, wrong tool, changed invoice target,
   changed organization/payload, invalid GLN, valid CVR without invented data,
   omitted `copyToUserId`, nested path rejection, and the exact registry names.
   It must leave `live_tested` and every UI/vision row false.
4. After a labelled root merge and root verification, run a separate Grok
   product review before accepting the Wave-5s-C product slice. Any docs change
   or live observation is a new research gate, never an inference.

## Post-Mortem

- Completed: research83 was independently accepted as a cited implementation
  handoff only; no email or delivery coverage was greened.
- Gate status: Wave-5s-C remains intentionally unstarted while Wave-5s-B has a
  confirmed exact-byte upload race and lacks a fresh authoritative Grok product
  audit.
- Next unresolved slice: wait for the repaired Wave-5s-B merge and Grok product
  acceptance; then delegate the four-tool custom-service implementation under
  this plan.
