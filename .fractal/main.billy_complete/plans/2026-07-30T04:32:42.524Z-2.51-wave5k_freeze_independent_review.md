# 2.51 Wave-5k freeze independent review

## Decision

The root branch now contains the cited, offline-only Wave-5k bank-payment
freeze contract and a clean generated wiki index. The current Grok brief
reconfirmed the official documentation fingerprint and the unauthenticated
method boundary: `POST` and `PUT /bankPayments` are authentication-gated,
whereas singular `DELETE /bankPayments/:id` is refused with 405. An independent
Grok reviewer is therefore the only authorised next increment. Product code
remains blocked on that review's explicit ACCEPT.

This is a separable clean-context task: the reviewer owns one new wiki page and
independently verifies the official contract, root page, method probes, and
fail-closed coverage boundary. No parallel Codex Power product child is
permitted, because it would bypass the freeze gate.

## Grok route result and required fallback

The Grok reviewer route was attempted first and failed before reviewer work
began: every PREPARE attempt reported that Grok was not signed in, and its
branch contains only the configured seed commit. The routing policy requires a
full retry with the other allowed agent after an authentication failure before
edits. Launch a separate `codex-power` fallback leaf that performs the same
offline inspection and writes a clearly labelled, non-authoritative fallback
review. Its result does not replace the missing Grok acceptance, so product work
remains blocked even if the fallback finds the contract acceptable.

## Execution plan

1. Create a leaf reviewer on the committed root baseline:

   ```bash
   fractal node init wave5k_freeze_review \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --agent=grok --max-cost=320 --max-iter-cost=80 --max-iters=8 \
     --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
   ```

2. Configure its `NODE.md` before launch. Its only project deliverable is
   `wiki/wave_fivek_freeze_independent_review.md`. It must independently fetch
   the current official Billy documentation, inspect the committed root freeze
   page, validate `POST`/`PUT` 401 and singular `DELETE` 405 at the locked base
   without credentials, and issue a cited ACCEPT or REJECT. It may not edit
   source, tests, inventories, coverage flags, UI evidence, or the supplemental
   fallback review. It must state that live, UI, vision, bulk, delete, and
   completeness rows remain red.

3. Commit the child configuration, verify its limits and `grok` route, then
   launch it. Monitor its radio and status; merge only its reviewed page after
   it completes. A REJECT is a valid completed deliverable and blocks product
   work pending the identified correction.

4. If and only if the merged independent review is an ACCEPT, plan a separate
   Codex Power leaf for exactly four bank-payment preview/execute tools, their
   contract tests, server registration, and only the two corresponding offline
   coverage rows. Singular delete and both bulk rows stay red.

## Evidence and constraints

- Root freeze page: `wiki/wave_fivek_ticketed_writes_contract.md`.
- Current cited research brief: `.fractal/main.billy_complete/tmp/grok-research.md`.
- Locked API base: `https://api.billysbilling.com/v2`.
- Official documentation fingerprint: ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes.
- Current fail-closed baseline: 238 `api_*` tools, 166 offline API rows, zero
  live/vision rows, 92 ambiguous bulk rows, and `complete: false`.

## Post-Mortem

The root freeze page and generated wiki index were confirmed merged before this
review was launched. The dedicated Grok reviewer exhausted its run without
editing: every PREPARE attempt failed at Grok authentication, so no Grok
acceptance page exists. The user-specified routing rule therefore required a
fresh Codex Power fallback retry; it is deliberately non-authoritative and
cannot permit product implementation. The research-handoff review found no
required correction; it reported only cosmetic backticks around the quoted 405
message, deferred to preserve the fallback review baseline. Root lint and
non-live tests pass on the freeze baseline. The next unresolved coverage slice
is the mandatory Grok freeze acceptance, followed only then by the four-tool
bank-payment create/update product slice.
