# 198.71 residual audit census

## Pivot

`88C4B0A9` is binding under `9B979A05`. HEAD `e995411` is a clean
readonly-map remap, not a `(failed on <step>)` backstop.
Uncommitted files are node memory only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch this slice:
`billy-api-docs-research-88c4b0a9.html` and
`billy-api-docs-chunk-research-88c4b0a9.js`. Census JSON:
`tmp/research-88c4b0a9-residual-census.json`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical (MD5 `7902955e4d1a56ecea2d85551f17b49c`).
billy.dk OpenAPI paths are 404. Do not probe
`https://api.billysbilling.com/v2`.

Research stop condition is met. Remaining unimplemented API
rows are **exactly** bulk92 plus the six readonly-map
create/update rows. Remaining unimplemented UI rows are
**exactly** six owner-scoped `out_of_scope_by_user` rows.
Do not repeat bulk or field-map exhaust. Do not ship tools.
Do not invent empty payloads. Do not green. Do not finish.

Do not spawn. Do not remake UI dumps. No live API.

## Goal

Land the residual audit record. Keep both external-contract
blockers red and toolless. Then owner input.

| Remaining red | Count | Kind / blocker | Tools |
| --- | ---: | --- | --- |
| API bulk | 92 | `ambiguous_bulk` / `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` | none |
| API readonly-map | 6 | `readonly_field_map_insufficient` / `READONLY_PROPERTY_TABLE` | none |
| API method-closed | 0 | — | — |
| API meta-delete | 0 | — | — |
| UI owner-scope | 6 | `out_of_scope_by_user` (non-blocking) | none |

The six readonly-map ids stay:

- `api.contactBalancePostings.create`
- `api.contactBalancePostings.update`
- `api.postings.create`
- `api.postings.update`
- `api.transactions.create`
- `api.transactions.update`

The six UI owner-scope ids stay:

- `ui.discovery.annual_reports` (`ANNUAL_REPORTS_OWNER_SKIP`)
- `ui.parity.daybooks.create` / `.delete` (`DAYBOOKS_UNIQUE_PERSIST_ABSENT`)
- `ui.parity.daybookTransactions.create` / `ui.parity.transactions.create` (`GODKEND_HIGH_IMPACT_PROHIBITED`)
- `ui.parity.files.create` (`FILES_NO_UI_DELETE`)

Generated snapshot stays implemented **546**, contract **551**,
live/vision **339**, `complete=false`. Docs lock unchanged.

## Why no child

Work is one census lock plus wiki. File ownership would collide
on `scripts/generate_coverage_report.py`,
`coverage/api_v2_manifest.yaml`,
`tests/coverage/test_coverage_inventory.py`, and
`wiki/residual_clear_method_closed_inventory_honesty.md`.
Seven write children stay completed and merged. Leftover
retired descendants stay unmerged.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. In `tests/coverage/test_coverage_inventory.py`, add
   `test_residual_audit_unimplemented_api_set_is_bulk_plus_readonly_map`:
   the set of API ids with `implemented` false or
   `contract_tested` false equals `ambiguous_bulk` union
   `RESIDUAL_READONLY_MAP_IDS`. Size is **98**. No other
   unimplemented API id. Method-closed and meta-delete stay
   empty. `complete is False`. Counts stay 546 / 551 / 339.
2. Same file: residual `qualification.evidence_ref` must
   contain `research-88c4b0a9-residual-audit`. Keep the
   existing `research186` / `research191` / `research192` /
   `research-a485f530-readonly-map` tokens.
3. In `tests/coverage/test_ui_residual_write_owner_scope.py`
   (or the coverage inventory test), add
   `test_unimplemented_ui_rows_are_exactly_owner_scoped`:
   every UI row with `implemented` false is
   `is_owner_out_of_scope`. The id set is exactly the six
   owner-scope ids above, including annual reports. UI
   unqualified count is 0.
4. Keep `test_bulk_and_residual_stay_red` and residual
   honesty toolless asserts. Do not green any flag.

Run the new evidence-ref assert. Expect fail on HEAD because
`research-88c4b0a9-residual-audit` is absent. Capture that
failure. Then change the generator.

## Implement

1. `scripts/generate_coverage_report.py`:
   - Append `+research-88c4b0a9-residual-audit` to
     `RESIDUAL_EVIDENCE_REF_CHAIN`.
   - Do not add tools. Do not move ids. Do not change bulk
     freeze. Do not change owner-scope maps. Do not bump
     `complete`.
2. `uv run python scripts/generate_coverage_report.py`. Do not
   edit `coverage/status.json` or manifests by hand.
3. Wiki:
   - `wiki/residual_clear_method_closed_inventory_honesty.md`:
     completeness walls are bulk92 plus residual 6 plus
     owner-scoped UI 6. Cite `88C4B0A9`. Snapshot 546 / 551 /
     339. No greening.
   - Do not rewrite `wiki/offline_write_probe_rules.md` unless
     a sentence still claims method-closed remaining or
     residual 20.
4. Memory `state.md` / `todo.md` / `decisions.md`: `88C4B0A9`
   audit recorded; remaining red is bulk92 plus six
   readonly-map; owner input required; `complete` false.

Out of scope this slice: any new `api_*` / `ui_*` module, bulk
schema inference, empty payloads, live API, OpenAPI re-probe
on the API host, **Godkend**, annual-report tools, coverage
greening, `node finish`.

## Verify

Commit-mode coverage tests plus lint/type on the touched
files. No `ui-full`. No live API. Independent review after
EXECUTE.

## Stop line

If review finds any unimplemented API id outside bulk92 plus
the six readonly-map rows, stop and fix that row instead of
recording the census. If a writable official field appears,
do not invent a tool in this slice. Keep `complete=false`.

After independent review PASS and COMMIT, one outbox report:
both external-contract blockers remain (bulk schema
unspecified; readonly property tables). Genuine owner input
required. Unsave `88C4B0A9` only after that report lands.
Keep `96908DC6` and `9B979A05`. Do not finish.

## Post-Mortem

Work completed: residual audit census for `88C4B0A9`. Remaining
unimplemented API rows are exactly bulk92 plus six readonly-map
create/update rows. Remaining unimplemented UI rows are exactly
six owner-scoped rows. Evidence ref appends
`research-88c4b0a9-residual-audit`. Coverage regenerated:
implemented 546, contract 551, live/vision 339, complete false.
No tools. No empty payload. No greening.

Deviations: none from the stop condition. Did not invent bulk
or readonly-map tools. Did not relock official docs.

Independent review: PASS. Required fixes none. Live docs HTML
and chunk fingerprints drifted (`pi4s9u10j037qn` /
`d805f3d2bb8e339f7635d6834b4011bd`, chunk
`api-docs-4eba988e0c6b3605.js`). `/v2/` path set still 49.
The three residual Supports lines and Notes tokens match the
research chunk. Bulk still 46/46 with no body schema. That
drift is advisory, not a this-slice contract change. Do not
relock here.

TDD first failed `research-88c4b0a9-residual-audit` missing
from residual `evidence_ref`. Census tests passed on HEAD
before the generator append. Lint passed. Commit-mode
`test.sh` `2431 passed, 83 deselected`. No live UI records.
No `ui-full` or live API. No production edits in FIX-VERIFY.

Cleanup: dedicated org untouched. No frames.

Next unresolved slice after COMMIT: owner input on both
external-contract blockers (bulk schema unspecified; readonly
property tables). Do not repeat research. Do not guess tools.
`complete` stays false. Do not finish.

