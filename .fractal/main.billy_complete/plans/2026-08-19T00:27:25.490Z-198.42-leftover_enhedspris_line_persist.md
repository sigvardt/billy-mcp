# 198.42 leftover Enhedspris line persist then reverse cleanup

## Pivot

Owner `DECEA79B` / `B9C4FA7C` / `59A3A933`: leftover
`MCP-UI-INV-6CDB396B` still shows **Enhedspris** 1,00 after a
browser PUT. Official https://www.billy.dk/api/ (2026-08-19,
ETag `"121myuqjdm53603"`): a `PUT /v2/invoices/:id` updates only
fields in the hash, and `lines` must not be set on an existing
invoice. Line price is `PUT /v2/invoiceLines/:id` `unitPrice`.
Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Do not re-lock docs (`728BD2E4`). Do not create a seventh
invoice. Do not remake dumps.

HEAD `8b2144d` already opens the list row, types 2,00, prefers
**Opdater**, and requires a fresh **Enhedspris** read. The fail
is `persist_hit` treating any `PUT 200 /v2/invoices/:id` as
line-price persist.

## Goal

Prove one FastMCP update of leftover `MCP-UI-INV-6CDB396B` to
**Enhedspris** 2,00 on a fresh row-open session, then delete all
6 drafts, 6 products, and 6 customers.

1. Offline failing fixture: `persist_hit` / `wait_persist` must
   not treat `PUT 200 /v2/invoices/:id` as line-price persist.
   Today's test still asserts that URL is enough.
2. Offline failing fixture: update watch must name
   `/v2/invoiceLines` or a redacted `unit_price_is_ticket` flag.
   Today's `watch_invoice_response` only records `/v2/invoices`.
3. Offline failing fixture: update fill types Danish `2,00`
   once. Today's `_price_text(2.0) == "2"` first fill is the
   fail.
4. Smallest fix: keep list-row open and `prove_fresh_unit_price`.
   Type `2,00` once. Count persist only when the browser write
   carries ticket `unitPrice`. Header-only PUT is `UI_CHANGED`.
5. One live FastMCP update of `MCP-UI-INV-6CDB396B` to
   `unit_price=2`. Fresh-session **Enhedspris** 2,00. Then
   reverse-delete all 6 triples. Third session empty.
6. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `src/billy_mcp/ui_writes/invoices_form_page.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `src/billy_mcp/ui_writes/invoices_form_price.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child and forbids `ui_invoice_lines_*` tools.

## TDD

`728BD2E4` / `DECEA79B`: failing fixture first, then the
smallest fix.

1. Change `test_persist_hit_rejects_put_request_without_status`
   so `PUT 200 /v2/invoices/:id` is **not** line-price persist.
   Add a passing shape: `PUT 200 /v2/invoiceLines/:id` or a
   redacted ticket-price flag. Run it. Capture the fail.
2. Add an offline source test that `watch_invoice_response`
   records `/v2/invoiceLines` (or the redacted flag helper).
   Today's `/v2/invoices`-only watch is the fail.
3. Add an offline source test that `_price_text` / update fill
   uses `danish_price` (`2,00`) and does not write integer `2`
   first. Today's `_price_text(2.0) == "2"` is the fail.
4. Smallest watch fix: persist for update is a 2xx browser
   `PUT /v2/invoiceLines/:id`, or a redacted body check that
   `unitPrice` matches the ticket. Never store raw bodies.
   Header-only invoice PUT stays non-persist.
5. Smallest fill fix: type **Enhedspris** `2,00` once. Tab is
   allowed. Extra `dispatch_event("change")` is not a substitute
   for the line PUT.
6. Keep list-row open, `contact_name`, `unit_price > 0`,
   **Opdater** then **Gem som kladde**, and
   `prove_fresh_unit_price`. Missing line write is `UI_CHANGED`.
   Never **Godkend**, send, or email.
7. Live FastMCP update only `MCP-UI-INV-6CDB396B` to
   `unit_price=2`. Fresh row-open **Enhedspris** must be 2,00
   before delete. Then delete all 6 drafts, 6 products, 6
   customers. Third session: no `MCP-UI-INV` / `MCP-UI-PRD`.
8. Run `tests/unit/test_ui_invoices_writes.py` and the
   invoice-adjacent commit-mode unit set. Then one live
   FastMCP update plus cleanup. If the row is gone, record
   `UI_CHANGED` and stop. Do not create another invoice.
9. Update `wiki/ui_invoices_writes.md` and
   `wiki/ui_write_residual_table.md` in place. Stay red.

## Out of scope

- Coverage remaps or greening
- Official-docs re-lock
- A new invoice create
- `ui_invoice_lines_*` tools
- Files or daybook writes
- New inspect dumps or archive probes
- New child nodes
- `node finish`
- Send, email, **Godkend**

## Done when

- Offline tests prove header invoice PUT is not line-price
  persist, invoiceLines (or redacted `unitPrice`) is watched,
  and fill types `2,00` once
- Live FastMCP updates leftover `MCP-UI-INV-6CDB396B` so a
  fresh session reads **Enhedspris** 2,00, then restores an
  empty org, or returns `UI_CHANGED` without creating new
  records
- Honesty-16 stay red
- No headed browser, no live API, no new dumps

## Post-Mortem

Work completed: `persist_hit` / `watch_invoice_response` now treat
header `PUT /v2/invoices/:id` as non-persist. Update persist is
`PUT /v2/invoiceLines/:id` 2xx. Line selectors no longer match a
bare `unitPrice` product field. Fill reads the value before Tab.
Independent review FAIL confirmed live fill.

Deviations: live execute never reached the line PUT. Four live
attempts returned `UI_CHANGED` with `shown_len=3`. Danish `2,00`
and integer `2` both failed to make `price_is(..., 2.0)` true.
No seventh invoice. No coverage remap.

Review findings: all independent-review fails confirmed. Docs
re-lock rejected (`728BD2E4`). Redacted `unitPrice` body flag
deferred, not rejected.

Test results: lint.sh passed. Commit-mode test.sh passed
(2092 passed, 82 deselected). Live leftover update failed
before save. Coverage stays red. Did not run ui-full.

Cleanup state: six leftover drafts, products, and customers
remain.

Next unresolved slice: the live line field still shows a
3-character value that is not ticket `2`. Identify that host
without a new classify dump, type the ticket price once, wait
for `PUT /v2/invoiceLines/:id` 2xx, prove fresh **Enhedspris**
2,00, then reverse-delete all six triples. Stay red.

