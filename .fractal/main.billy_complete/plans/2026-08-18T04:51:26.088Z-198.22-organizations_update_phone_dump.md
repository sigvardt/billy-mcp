# 198.22 Organizations update phone-only dump

## Goal

Prove whether the dedicated test organisation still has a
company-phone field and a unique **Gem ændringer** on
`/:org_slug/settings`. `AD8966F2` wants a reversible phone update
through existing `ui_organizations_update_preview` plus exact
execute, independent read-back, then a second ticket that restores
the original value.

TDD first. Honesty-16 stays red. `ui.parity.organizations.update`
still names `ui_settings_company_open`. Persist **only**:

- `settings_path_class` (`settings` / `other`)
- `heading_token` (`indstillinger` / `other` / `none`)
- `company_panel_markers_present` (bool)
- `phone_field_visible` (bool)
- `phone_input_name_class` (`phone` / `other` / `none`)
- `phone_value_len` (int)
- `save_cta_token` (`gem_aendringer` / `other` / `none`)
- `save_cta_count` (int)
- `forbidden_surface_token` (`users` / `access` / `tokens` /
  `subscription` / `vat` / `none`)
- `proved_phone_only` (bool)
- `missing_keys`

`proved_phone_only` is true only when path is `settings`, heading
is `indstillinger`, company markers are present,
`phone_input_name_class` is `phone`, `save_cta_token` is
`gem_aendringer`, `save_cta_count` is 1, and
`forbidden_surface_token` is `none`.

Read the current phone into owner-local runtime state only. Never
persist, radio, or log the value, organisation id, URLs, tokens,
or screenshots.

Do not click **Gem ændringer** until `proved_phone_only` is true.

If the dump is not phone-only: `UI_CHANGED`, no submit, close this
recapture. Stay red.

If `proved_phone_only` is true in the same EXECUTE: one unique
test phone (`+4500` plus 8 hex) through FastMCP preview plus
execute, independent fresh-session input match, second separately
previewed restore, third-session `restored=true`. Persist class,
`value_len`, and booleans only.

Authority: `96908DC6`, `AD8966F2`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Closed: invoice picker, Ember, fiber,
listener, structure, `C0721A14`, product delete-chrome
(`D326FFB3`). Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.organizations.update` to
  `ui_organizations_update_preview`
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or org
  `parity_status` in the dump-only half
- Writing `author=independent_review` / `accept` / `purge_verified`
- Inventing `ui_organizations_create_*`, `ui_annual_*`, or
  `api_settings_*`
- Calling `api.billysbilling.com` or using a live API token
- Clicking **Gem ændringer** before `proved_phone_only`
- Filling name, CVR, address, email, owners, users, tokens,
  subscription, or VAT
- Guessed URLs, guessed selectors, generic text sweeps, force
  clicks, `evaluate` clicks, raw phone values in git
- Remaking invoice or product inspects, or altering those rows
- Persisting source, locators, URLs, slugs, phone values, or
  screenshots
- Ledger, files, invoices, products, or a new contacts/bills CUD
- Docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `63b2528`. Memory edits from SYNC are uncommitted. Not a
backstop. `complete=false`. Honesty 16 stay red. Contacts and
bills CUD rows already name preview tools. Invoice CUD stays
`ui_invoices_*_open` and frozen. Products create stays
`ui_products_create_open` and frozen. Org update preview/execute
already registered. Manifest still points the honesty row at
`ui_settings_company_open`.

`tests/live/test_ui_organizations_writes.py` still skips on stale
`D5136C2E` and omits `organization_id`. `AD8966F2` supersedes that
skip. Do not submit from that file this pass.

`BrowserOrganizationSubmitter` fills `input[name='phone']`, clicks
substring **Gem ændringer**, and proves page text. That is not
live proof until this dump names the field and the exact save
control. Independent read-back must read the phone input, not
page-wide text.

Wiki `ui_organizations_writes.md` still says contacts hold the
slot. That line is stale. Do not treat it as a blocker.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

Browser egress allows GET `/v2/organizations/` only. If a later
persist XHR is PUT and egress denies it, add a failing egress
test first, then PUT prefix `/v2/organizations/` only. Not in the
dump-only half.

## Delegation

No children. Root owns this slice. `728BD2E4` child-split already
landed the seven write-family modules. Those children stay merged
and parked. Unlimited budget is not a reason to spawn.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/organizations.py` (ticket pair; no remap)
- `src/billy_mcp/ui_writes/page_flow.py`
- `src/billy_mcp/browser.py` `_has_settings_company_signature`
- `src/billy_mcp/ui_writes/products_delete_chrome.py` (dump-key
  pattern only; do not copy product labels)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `tests/live/test_ui_settings_company_open.py` (shell contrast)
- `wiki/ui_organizations_writes.md`
- `wiki/ui_settings_company_open_shell.md`

New files:

- `src/billy_mcp/ui_writes/organizations_phone_dump.py`
- `tests/unit/test_ui_organizations_phone_dump.py`
- `tests/live/test_ui_organizations_phone_dump.py`

Do not grow `tests/live/test_ui_organizations_writes.py` this pass.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-organizations-phone.json`

## Design choices

1. **TDD first (`728BD2E4` / `AD8966F2`).** Confirm honesty-16
   still length 16 and red. First org edit is a failing unit
   fixture. An empty dump must fail `missing_keys` with the exact
   key set above. Run it. Capture the failure. Then implement the
   helper.
2. **Honesty-16 stays red.** Do not remap org tools.
3. **Read-only first.** Open `/:org_slug/settings` only. Classify
   path, heading, panel markers, phone input name class, exact
   **Gem ændringer** count, and current-surface forbidden token.
   No fill. No click.
4. **Exact labels only.** Count **Gem ændringer**, **Navn og
   adresse**, **Kontaktinformation**, **Indstillinger**. No
   substring. No body sweep. Forbidden surface is the current
   path or heading class, not the presence of nav links.
5. **Default stop is `UI_CHANGED`.** If `proved_phone_only` is
   false: do not submit, close the recapture.
6. **Persist CUD is a second half of the same EXECUTE.** Only if
   `proved_phone_only` is true. FastMCP preview plus execute with
   a unique test phone. Independent input match. Second ticket
   restores the original. Third-session `restored=true`. Fail
   `CLEANUP_FAILED` if restore does not land.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
8. **No remap** until independent accept + purge of a real update
   with restored state.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.organizations.update` still names
`ui_settings_company_open`.

Add `REQUIRED_ORGANIZATIONS_PHONE_DUMP_KEYS` and `missing_keys` in
`organizations_phone_dump.py` **only after** a unit fixture fails
because those names do not exist, or add the fixture first against
a local helper stub. Preferred shape:

- Fixture feeds `{}` or a flags-only stub.
- Helper returns the missing key list from this plan.
- Current empty dump must fail with that exact set.

Do not edit `generate_coverage_report.py` or remap coverage.

### 2. Smallest helper, then one live inspect

- Implement the helper in `organizations_phone_dump.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  phone values. Persist `phone_value_len` only.
- Live test: READY via existing credential refs. One path:
  `/:org_slug/settings`. No **Gem ændringer**. Write the dump
  outside git.
- Live test verifies the dump keys without repeating a click if
  the dump already exists from this EXECUTE. Prefer one capture.
- If `proved_phone_only` is false, stop. Do not call
  `ui_organizations_update_execute`.

### 3. Persist half (only if phone-only is proved)

- Failing persist fixture first if the dump is green on
  `proved_phone_only`.
- One FastMCP tagged phone update. Independent fresh-session
  input match (`matches_test_phone`, `value_len` only).
- Second preview plus execute restores the original from runtime
  state. Third-session `restored=true`.
- Fail closed on any other field or panel.
- Still no remap.

### 4. Stay red

Do not finish. Do not claim complete.

## Success criteria

- Honesty-16 still length 16 and red
- Failing dump-key fixture existed, then passed
- Live dump has every required key
- No **Gem ændringer** unless `proved_phone_only` is true
- Company phone restored if a submit happened
- Invoice and product coverage untouched
- No children spawned
- Node stays red

## Post-Mortem

Completed: TDD empty-dump fixture, phone-dump helper, sibling
live inspect. Live session opened `/:org_slug/settings`. Path
`settings`, heading `indstillinger`, company markers present,
phone field visible, input name `phone`, exact **Gem ændringer**
count 1, forbidden `none`, `proved_phone_only=true`.
`phone_value_len=0`. No **Gem ændringer** click. No company field
changed. Honesty 16 still red. `ui.parity.organizations.update`
still names `ui_settings_company_open`. `complete=false`.

Dump has no URL, source, or phone value.

Deviations: no persist CUD half. Original phone is empty or
unreadable, so a restore ticket cannot bind a blank value
(`min_length=1`). Persist stayed fail-closed.

Review: dump contract met. Organizations update still unbound.
Required: commit helpers, no remap, no remake, stay red. Do not
change the company phone.

Tests: honesty-16 2 passed. Phone-dump unit tests 7 passed. Live
`test_ui_organizations_phone_dump` passed. Lint passed.

Cleanup: no company field changed.

FIX-VERIFY: IR FAIL stands. Dump contract met. No remap. Wiki
`ui_organizations_writes.md` no longer claims the contacts slot.
Helpers ready to commit.

Next unresolved slice: not another invoice or product recapture,
and not a remake of this dump. Persist stays blocked until a
restorable original exists or the owner names a different
reversible company field. Not ledger.

