# 198.19 Invoice Kunde versus bills Leverandør structure compare

## Goal

Compare only the sanitized invoice Kunde control structure to the
live-proved bills Leverandør picker. Listener inspect is closed:
nine rows, no named invoke, `unique_normal_action=false`,
`UI_CHANGED`. That dump is not a bind. Parent `28C8FBC8` said the
next slice is this compare, not another identity-only inspector.

TDD first. Derive the compare from dumps already on disk. Persist
**only**:

- `kunde_input_name_token` (`contact` / `other`)
- `bills_input_name_token` (`vendor` / `other`)
- `kunde_wrapper_family` (`pickerfield` / `input_wrapper` / `other`)
- `bills_wrapper_family` (`pickerfield` / `input_wrapper` / `other`)
- `kunde_wrapper_data_attr_names` (names only, sorted)
- `kunde_search_trigger` (bool)
- `bills_search_trigger` (bool)
- `kunde_overlay_present` (bool)
- `kunde_list_host` (`ds_dropdown_portal` / `ds_dropdown` /
  `role_listbox` / `none`)
- `bills_list_host` (same tokens)
- `kunde_option_role_count` (int)
- `bills_option_role_count` (int)
- `same_family` (bool)
- `transferable_action` (`search_open` / `portal_option` / `none`)
- `unique_normal_action` (bool)
- `proved_kunde_bind` (`scoped_existing_option` / `none`)
- `proved_bills_bind` (`scoped_existing_option` / `none`)
- `kunde_structure_missing_keys`

`same_family` is true only when both sides share
`wrapper_family=input_wrapper` and `search_trigger=true` and
`list_host=ds_dropdown_portal`. `transferable_action` is non-`none`
only when the invoice rest page has an unused bills-family host on
the contact field. Existing dumps make both false.

Do not remake Ember, fiber, listener, descendant, wrapper-center,
or overlay dumps. Do not click. Do not type. Do not create a
customer. Do not copy the bills wrapper onto this field.

If `same_family` is false or `transferable_action` is `none`,
record `UI_CHANGED` and stop. Stay red. Do not remap invoice CUD
rows. Do not do official-doc work.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`,
`C7DBE974`. Closed: `28C8FBC8`, `51E18E60`, `A3AB03C3`,
`9F777B8F`, `8EFD0EAD`, `4A5CD1E7`, `F12B607E`, `452E0773`,
`07600147`, `E87B6AEF`, `31B0C7A6`, fiber inspect. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Remaking Ember, fiber, listener, descendant, or post-click dumps
- Repeating overlay, chevron, widget, typed-only, event-count,
  route, or wrapper-center captures
- Clicking wrapper center or the overlay `DIV` at `dx=242`
- Copying `[data-testid=input-wrapper]`, `[data-testid=search]`,
  or `.ds-dropdown-list` onto the invoice field
- Calling CDP `getEventListeners` or `Debugger.getScriptSource`
- `Runtime.callFunctionOn`, `evaluate` click, dispatch, type,
  Tab, Enter, Escape, or ArrowDown
- Fetching a script URL or issuing a `/v2/` URL
- Persisting source, locators, URLs, slugs, or screenshots in git
- Products, ledger, files, org, or a new bills CUD
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `c52d1ae`. Memory edits from SYNC/RESEARCH are uncommitted.
Not a backstop. `complete=false`. Honesty 16 stay red. Contacts
and bills CUD rows already name preview tools. Invoice CUD rows
still name `ui_invoices_*_open`.

Listener dump
(`~/.local/share/billy-mcp/inspect-live-invoices-kunde-listeners.json`):

- nine rows, all bubble, empty property categories
- `accepted_key_category=none`, `invoked_action_token=none`
- `unique_normal_action=false`

Control dump (cite only): wrapper tokens include `ember-view`
`super-field` `pickerfield`. Data names: `data-cy` only. That
`click_open` inference is closed.

Descendant dump (cite only): two visible descendants, overlay at
`dx=242`, `unique_target=false`.

Bills dumps (cite only): `input_name=vendor`,
`search_trigger=true`, portal
`.ds-dropdown-list.ds-moved-with-portal` after click,
`chosen=scoped:existing_option`, `role=option` count 0.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. Existing files (read, do not
reuse their key sets as the product):

- `src/billy_mcp/ui_writes/invoices_kunde_control.py`
- `src/billy_mcp/ui_writes/invoices_kunde_descendants.py`
- `src/billy_mcp/ui_writes/invoices_kunde_listeners.py`
- `src/billy_mcp/ui_writes/invoices_kunde_post_click.py`
- `src/billy_mcp/ui_writes/bills_vendor.py`
- `src/billy_mcp/ui_writes/bills_form.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `tests/unit/test_ui_invoices_kunde_listeners.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

New files:

- `src/billy_mcp/ui_writes/invoices_kunde_structure.py`
- `tests/unit/test_ui_invoices_kunde_structure.py`

Live hook goes in `tests/live/test_ui_invoices_writes.py` beside
the listener test, or a sibling file if that file is already too
large. Parked write-family children stay parked. Unlimited budget
is not a reason to spawn. `728BD2E4` child-split applies after
this field is bound, not to a dump compare.

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing unit fixture. Current
   listener dump flags fail `kunde_structure_missing_keys`. Run
   it. Capture the failure. Then implement. Do not reuse
   `REQUIRED_KUNDE_LISTENER_KEYS` as this fixture.
2. **Honesty-16 stays red.** Do not remap invoice tools.
3. **Map existing dumps.** The helper reads the already delivered
   Kunde control / descendant / listener / post-click dumps and
   the bills vendor / wrapper dumps. It does not call CDP. It
   does not remake those dumps.
4. **Same family is strict.** True only per the Goal rule. A
   shared official `contactId` is not the same chrome family.
5. **Transferable action is strict.** Non-`none` only when
   invoice rest counts prove unused `input-wrapper` + `search` +
   portal list host on the contact field. Existing dumps say no.
   `kunde_wrapper()` looking for `[data-testid=input-wrapper]` is
   a false family copy, not a bind.
6. **Live is a token count, not an inspector.** Open
   `/:org_slug/invoices/new` only if the structure dump is not
   yet delivered. Count allowlisted tokens. No click. No type.
   No customer. After the dump is delivered, later `ui-full`
   verifies the file and returns.
7. **Default stop is `UI_CHANGED`.** Do not start a bind slice
   unless live counts surprise the dumps.
8. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
9. **No remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_KUNDE_STRUCTURE_KEYS` and
`kunde_structure_missing_keys` in `invoices_kunde_structure.py`
**only after** a unit fixture fails because those names do not
exist, or add the fixture first against a local helper stub.
Preferred shape:

- Fixture copies the current live listener dump flags only
  (nine rows, `unique_normal_action=false`, no compare keys).
- Helper returns the missing key list from research section 5.
- Current listener dump must fail with that exact set.

Do not edit bind/observe product files until that fixture fails.

Keep `REQUIRED_KUNDE_LISTENER_KEYS` and the closed fiber / Ember /
descendant / post-click / control fixtures.

### 2. Smallest helper, then optional live count

- Implement the helper in `invoices_kunde_structure.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, or URLs.
- Derive `same_family` and `transferable_action` from the mapped
  fields. If either fails the Goal rule, set
  `unique_normal_action=false`, `code=UI_CHANGED`.
- Isolate unit dumps so they cannot overwrite owner files.
- Live path only if
  `inspect-live-invoices-kunde-structure.json` is not delivered:
  1. Login through `create_server` `call_tool`.
  2. Fresh observer page. Open `/:org_slug/invoices/new`. Wait
     until `input[name=contact]` and the `pickerfield` wrapper
     are present.
  3. Count allowlisted tokens only: `input[name=contact]`,
     `input[name=vendor]`, `.pickerfield`,
     `[data-testid=input-wrapper]`, `[data-testid=search]`,
     `.ds-dropdown-list`, `.ds-dropdown-list.ds-moved-with-portal`,
     `[role=option]`.
  4. Merge those counts with the already delivered dump map.
  5. No click. No type. No Ember. No fiber. No listener remake.
  6. Write allowlisted keys to
     `~/.local/share/billy-mcp/inspect-live-invoices-kunde-structure.json`.
- Add `test_ui_invoices_kunde_structure_compare` beside the
  listener live test. After the dump is delivered, later runs
  verify the file and return.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the structure gate into memory `state.md` / `todo.md`. No
  iteration labels.

### 3. Do not write a draft

Default stop is the allowlisted compare dump. Do not click
**Gem som kladde**.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus the honesty-16
gate. Do not run live API tests. Do not claim `complete: true`.

## Done when

- Honesty-16 still length 16 and red
- Failing structure fixture existed, then the helper lists
  missing keys on the current listener dump
- Compare dump written from existing dumps (plus a rest-page
  token count only if the dump was missing)
- `same_family` and `transferable_action` follow the Goal rule
- Expected from current dumps: `same_family=false`,
  `transferable_action=none`, `UI_CHANGED`, no click, no customer
- No remake of Ember, fiber, listener, descendant, or post-click
- No credentials, names, source, locators, or frames in git
- Invoice CUD rows still name `ui_invoices_*_open`
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice fits. No spawn.
Research section 5 is the contract. Stay red.

## Post-Mortem

Completed: TDD fixture, structure helper, dump map from existing
owner files, live FastMCP verify of the delivered dump. Honesty 16
still red. Invoice rows still name `ui_invoices_*_open`.
`complete=false`.

Live dump: Kunde `pickerfield` plus `data-cy` and overlay. Bills
`input_wrapper` plus search plus `ds_dropdown_portal`.
`same_family=false`. `transferable_action=none`.
`proved_kunde_bind=none`. `proved_bills_bind=scoped_existing_option`.
`unique_normal_action=false`. `UI_CHANGED`. No click. No customer.
Dump has no URL, source, or customer text.

Deviations: no live rest-page recount was needed. Existing dumps
already filled every compare key. Independent review said do not
remake, do not copy the bills wrapper, and do not start parent
`C0721A14` here.

Review: FAIL. Dump contract met. Invoice CUD still unbound.
Required: commit helpers, no remap, no remake, stay red.

Tests: honesty-16 2 passed. Structure unit tests 10 passed. Live
`test_ui_invoices_kunde_structure_compare` passed. Lint passed.
Offline `test.sh` 1970 passed, 73 deselected during EXECUTE.

Cleanup: no disposable customer created. Test org unchanged.

Next unresolved slice: parent `C0721A14` after COMMIT. Prove
whether customer detail has an exact **Opret faktura** action that
opens a prebound invoice form. TDD first. One tagged disposable
customer through FastMCP. If absent, `UI_CHANGED`, delete, prove
absence, close that route. Do not start another identity-only
inspector. Do not remake this structure compare.

