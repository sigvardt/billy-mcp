# 186.28 ui settings accounting open Regnskab panel


## Goal

Ship the next **read-only accounting settings open-shell** FastMCP tool for
Billy's Indstillinger **Regnskab** panel, dual-confirmed in research127 as
final path `/:org_slug/settings` (opened via seed `settings/accounting`) with
h1 **`Indstillinger`** and accounting panel markers (`Regnskab`, `Køb`,
`Kontoplan`; also `Bankafstemning`, `Momsopgørelse`), distinct from the
company default panel, with offline contract tests, dual-session live
interface tests (no API token), and a non-sensitive vision record that is
purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research127).  
Discovery: `.fractal/main.billy_complete/tmp/discovery127/summary.json` +
`summary_dual_accounting.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; `dual_accounting_seed_ok: true`;
`dual_click_ok: true`; frames purged; PII scrubbed in brief.  
Prior product tip: settings company 186.27 (`affbd3b`) `ui_settings_company_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family (accounting seed).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–127).

**Research127 decision:** accounting is a **SPA panel on the same hub path** as
company. Request `/:org_slug/settings/accounting` rewrites to bare
`/:org_slug/settings` with Regnskab panel content. Left-rail click on exact
text `Regnskab` also opens the same panel (observe-only dual OK). Product
should prefer the **deterministic seed** `settings/accounting` for open.
Success requires accounting panel markers and **must reject** the company
default panel (bare hub open) and the Faktura panel (`settings/invoicing`).
Soft nested rejects include `settings/company`, `settings/bogfoering`,
`settings/vat`, `settings/users`, etc. No official settings API — do **not**
invent `api_settings_*`. This product is **open-shell classification only**.
Write CTAs (`Gem ændringer`, lock-date copy) are **observe-only** — never click.

## Why this slice

- UI live/vision is 29 (through settings company).
- Research127 dual headless sessions:
  - Seed `settings/accounting` → path `/:org_slug/settings`, h1
    `Indstillinger`, dual path/h1/title match true.
  - Panel h2 dual-matched: `Regnskab`, `Køb`, `Bankafstemning`,
    `Momsopgørelse`, `Kontoplan`.
  - `accounting_panel_success` true both sessions; `company_panel_success`
    false; `distinct_from_company` true.
  - Click Regnskab from hub also dual-success (product optional recovery only).
  - Soft aliases soft-empty or wrong panel (reject for this tool).
  - Bare `settings` is company default — **reject** for accounting tool.
  - `settings/invoicing` is Faktura panel — **reject** for accounting tool
    (separate later product).
  - annual_reports remains non-success — **do not green**.
  - Other settings_* (user/vat/users/subscription/access_token/beta) deferred.
- No official Billy API v2 settings resource. Closest offline APIs
  (`api_organizations_*`, daybooks/accounts/tax already producted) are **not**
  greened by this UI open tool and **not** invented here.
- Target still red: `ui.discovery.settings_accounting` only for this product.
- Hard ban: never click Gem / Sæt låsedato / Opret* / Tilføj* / Upload / Opgrader.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening other `settings_*` rows (company re-green, user, vat, invoicing,
  users, subscription, access_token, beta) from this panel open.
- Producting settings write / ticketed Gem / lock-date change.
- Inventing any `api_settings_*` or webhook tools.
- Treating bare hub open (company default) as accounting success.
- Treating `settings/invoicing` Faktura panel as accounting success.
- Producting annual_reports.
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings company open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_accounting_open.py` (new; pattern from company open; accounting panel success; hard-ban write CTAs; reject company default + soft aliases + invoicing panel) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_accounting` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_accounting_open_shell_evidence` for
  **`ui.discovery.settings_accounting`** only; wire family `settings_accounting`
  in discovery apply path; ensure `build_browser_egress` retains prior lives
  (including settings company) + appends accounting open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings company, inventory,
  integrations, addons, saft_exports, exports, vat_declarations, reports,
  transactions, daybooks, financing, bank recon, receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_accounting_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, or children this
iteration.

## Contract freeze (research127 accounting / Regnskab open shell)

### Tool

- **Name:** `ui_settings_accounting_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, or write payload.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf after SPA rewrite).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_accounting`.
  - `accounting_panel_markers_present`: bool true when required markers are
    visible — require **`Regnskab`**, **`Køb`**, and **`Kontoplan`** (h2 or
    visible body). Optional observation of `Bankafstemning` / `Momsopgørelse`
    without logging fiscal values or PII.
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** both be present
    as the active panel signature (rail label `Regnskab` alone is insufficient —
    it appears on company rail too).
  - Optional observe-only: `save_cta_markers_present` when `Gem ændringer` text
    is visible (**no page dump**, no org names, no fiscal field values).
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path (`settings/company`,
    `settings/vat`, `settings/users`, `settings/bogfoering`, `settings/list`,
    `indstillinger`, …)
  - hub open but **wrong panel** (company default; Faktura invoicing panel;
    empty/other panels)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
- **Never:** click Gem ændringer / Sæt låsedato / Opret* / Tilføj* / Upload /
  Opgrader; invent `api_settings_*`; treat company open as this tool's success.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Prefer direct
   `https://mit.billy.dk/{org_slug}/settings/accounting`
   (SPA rewrites to bare `/settings` with Regnskab panel).
3. Optional recovery only: bare `/settings` then exact-text click `Regnskab`
   (never click write CTAs). Prefer seed path for primary product path.
4. Do **not** accept bare `/settings` alone as success (company default).
5. Independent second session (fresh profile) re-opens the same seed path and
   asserts path / h1 Indstillinger / accounting panel markers. **No** API
   read-back.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers |
| Soft nested paths | `settings/company`, `settings/bogfoering`, `settings/list`, `settings/vat`, `settings/users`, `settings/access-token`, `settings/beta`, … |
| Other hub panels | Faktura (`settings/invoicing` rewrite); Profil/etc. until frozen |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, settings company tool success |

### CTA hard ban (tests must prove no click)

- Gem ændringer / Gem / Save  
- Sæt låsedato (if control is clickable)  
- Opret* / Tilføj* / Upload / file input activate  
- Opgrader / Upgrade / Slet / Delete  

Observation of CTA **text** is allowed (and expected on accounting panel).
**Click count for write CTAs must stay 0.** Observe-only Regnskab tab click is
allowed only if used as recovery navigation inside the tool (prefer seed).

## EXECUTE steps (root)

1. **Models** — `UiSettingsAccountingOpenInput` (empty, forbid extra),
   `UiSettingsAccountingOpenSuccess` (`path_class`, `heading=Indstillinger`,
   `shell_kind=settings_accounting`, `accounting_panel_markers_present`).
2. **Browser** — `UiSettingsAccountingOpenService` protocol +
   `BrowserRuntime.ui_settings_accounting_open` implementing seed open +
   accounting panel classify; soft-alias / company default / invoicing panel /
   error → `UI_CHANGED`; write CTAs never clicked.
3. **Server** — register `ui_settings_accounting_open` with typed input/output.
4. **Offline tests** — model validation, browser unit (mock page states for
   accounting success / company default reject / soft alias / invoicing panel /
   login / error), server registration, coverage inventory map.
5. **Coverage generator** — `apply_ui_settings_accounting_open_shell_evidence`
   for `ui.discovery.settings_accounting` only; wire family; egress append
   without drop; regenerate manifests/status (**complete remains false**).
6. **Live tests** — dual-session open via seed; DOM assert path + h1
   Indstillinger + accounting markers; independent read-back; reject company
   default if asserted via negative unit or live soft checks as feasible;
   write CTA click ban; no API token.
7. **Vision** — capture non-sensitive Indstillinger Regnskab chrome frame(s)
   outside repo; Grok vision record; purge frames; store only non-sensitive
   review record with `purge_verified`.
8. **Wiki** — `wiki/ui_settings_accounting_open_shell.md` (contract + non-claims).
9. **Lint/type/offline suite** before REVIEW; live dual + vision before
   completeness claims for this row.

## FIX-VERIFY / COMMIT gates

- Independent Grok review ACCEPT for product slice (expected overall
  completeness FAIL).
- Offline non-live suite green; live settings accounting dual + vision
  purge_verified.
- Egress test_references retain all prior UI live paths + settings accounting
  open (and still include settings company).
- Coverage: only `ui.discovery.settings_accounting` newly greened for
  live/vision; complete false; API live_tested stays false with
  out_of_scope_by_user.
- No secrets, frames, HAR, traces in repo.
- `fractal commit` product + memory tip; **not** `node finish`.

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Mis-green all settings_* from hub open | Evidence helper only touches `ui.discovery.settings_accounting` |
| Company default classified as accounting | Require Regnskab+Køb+Kontoplan; reject company h2 pair |
| Invoicing panel false success | Reject Faktura panel markers as accounting success |
| Soft alias false success | Require final path bare settings leaf + accounting markers |
| Accidental Gem click | Live test asserts write_cta clicks 0; browser never clicks those labels |
| Egress drop of prior lives | Generator append-only; coverage unit asserts prior refs present (incl. settings company) |
| annual accidental green | Not in evidence helper target list |
| Inventing API tools | Explicit non-goal; no `api_settings_*` registration |
| PII / fiscal values in vision/logs | Scrub org/person names and field values; non-sensitive review record only |

## Done when (this product only)

- [x] `ui_settings_accounting_open` registered with typed models
- [x] Offline unit + coverage map green
- [x] Live dual-session + vision purge_verified green for accounting shell
- [x] `ui.discovery.settings_accounting` live_tested + vision_verified true via generator
- [x] No other discovery rows newly greened
- [x] Wiki shell page written
- [x] Independent review ACCEPT; commit pending

## Post-mortem (fill after EXECUTE/REVIEW)

- Filled in Post-Mortem below after FIX-VERIFY.

## Post-Mortem

### Work completed
- Producted `ui_settings_accounting_open` (models, browser, server, offline unit,
  live dual, coverage generator, egress append, wiki shell page).
- Greened only `ui.discovery.settings_accounting` (Indstillinger Regnskab panel
  via seed `settings/accounting` → bare `/settings`; markers Regnskab/Køb/Kontoplan).
- Live dual-session + vision record with `purge_verified: true`.
- No `api_settings_*` invented; write CTAs observe-only (never clicked).

### Deviations
- Signature uses `.first` on multi-match labels (Regnskab/Køb count > 1 live);
  required for Playwright multi-match `is_visible` strictness.
- Optional IR nits N1–N3 left as-is (constant True after signature; no unit for
  stuck nested URL; no live click-count instrumentation).

### Review findings
- Independent review **ACCEPT** product (`tmp/grok-review.md`).
- Required fixes: none.
- Overall completeness **FAIL** (expected).

### Test results (FIX-VERIFY)
- `bash scripts/lint.sh`: pass
- `bash scripts/test.sh` (offline): **1504** passed, 28 deselected
- Live: `tests/live/test_ui_settings_accounting_open.py`: **1 passed** (reconfirm)
- Vision: `tmp/vision-records/ui_settings_accounting_open.json` purge_verified true
- Coverage: live/vision **30**; complete **false**
- Egress: accounting live ref on mit + api hosts; company + prior lives retained

### Cleanup
- No disposable org records created (read-only open).
- Raw frames purged after vision review.

### Next unresolved coverage slice
- Remaining settings_* (invoicing SPA seed next; click-nav for
  user/vat/users/subscription/access_token/beta).
- `ui.discovery.annual_reports` stays red until non-error shell.
- Residual bulk offline API reds; API live remains `out_of_scope_by_user`.
