# 186.91 Walls reconfirm and annual reports dual gate


## Goal

One **root-only** iteration after research191: (1) dual-session reconfirm of the sole UI red row `ui.discovery.annual_reports`, producting an open/list tool **only** if the shell is non-Upsedasse; (2) optional inventory hygiene so residual/bulk/annual qualifications cite research191 unauth + docs reconfirm. No residual API tools. No bulk tools. No live API. `complete` stays **false** unless annual unlocks **and** residual+bulk walls somehow clear (they will not this package).

| Track | Rows | Outcome if blocked | Outcome if unlocked |
| --- | ---: | --- | --- |
| A. Annual dual gate | **1** UI `ui.discovery.annual_reports` | Stay red `discovery_required` + `ANNUAL_REPORTS_ORG_INACCESSIBLE`; evidence_ref→research191 dual; **no** `not_applicable` | Typed headless open (and list if present) with DOM + second-interface read-back + vision; green only that row |
| B. Walls evidence hygiene | **121** API red (92 bulk + 29 residual) stay red | Bump docs fingerprint / evidence_ref to research191 where tests already require current freeze; **never** flip implemented/contract/live | N/A — external walls |
| C. Product-plane UI bulk | **0** honesty remaining | No work (186.88–186.90 closed) | N/A |

### Baseline (HEAD `3f61ef9` / 186.90)

| Metric | Value |
| --- | ---: |
| implemented / contract_tested | **528** |
| live_tested / vision_verified | **344** |
| complete | **false** |
| API incomplete | **121** = 92 bulk + 29 residual |
| UI incomplete | **1** = annual_reports |
| Docs lock | MD5 `8b94b0135c91fd15fe54ea33e088a4be` ETag `wcw4x9hqvu3603` (research191 match) |

### Authority

| Source | Role |
| --- | --- |
| `.fractal/main.billy_complete/tmp/grok-research.md` (research191) | This iteration contract brief |
| `.fractal/main.billy_complete/tmp/research191_unauth_reconfirm.json` | Unauth residual/bulk reconfirm |
| `.fractal/main.billy_complete/tmp/billy-api-docs-research191.html` | Docs body fingerprint |
| wiki `ui_annual_reports_inaccessible` | Prior dual Upsedasse freeze; NA rejected |
| wiki `residual_clear_method_closed_inventory_honesty` | Residual tools_allowed=false |
| wiki `offline_write_probe_rules` | Supports alone ≠ tools |
| design §10.2 | UI NA only when no equivalent workflow; error shell ≠ absence |
| User scope 2026-07-31 | No live API qualification; API `live_tested` stays false + `out_of_scope_by_user` |

## Why this slice (research191)

- Product-plane UI bulk honesty is **closed** (strong + soft + empty-list). Next greenable UI candidate is **only** annual_reports.
- Official docs unchanged; OpenAPI still 404; bulk still Supports-only → **cannot** product API bulk.
- Residual unauth reconfirm = research186 (405 / 401+readonly / meta-200) → **cannot** product residual ticketed writes offline under fail-closed rules.
- Keyring has opaque accounts `browser-primary` / `browser-secondary` (present). AppConfig env refs may be null; EXECUTE must pass  
  `BILLY_BROWSER_PRIMARY_REFERENCE=browser-primary`  
  `BILLY_BROWSER_SECONDARY_REFERENCE=browser-secondary`  
  (values never logged). No `BILLY_API_TOKEN`.

## Track A — annual dual gate (primary)

### Method (EXECUTE)

1. Two independent headless Playwright sessions (ephemeral profiles preferred; MCP profile only if required by harness). Headless only; never headed window.
2. Login each to READY via stored credential refs + TOTP if configured. Abort with documented AUTH_INTERACTION_REQUIRED only for CAPTCHA/passkey/push.
3. Resolve dedicated test org (same non-production org as prior live UI suite). No production data.
4. Navigate nav **Årsrapporter** and direct `/:org_slug/annual_reports` (plus one nonsense control path).
5. Record non-sensitive dual observations only: path class, h1, error_upsedasse bool, plan-gate bool, product chrome flags (tables/rows/create), recovery CTAs classes. **No screenshots/HAR/traces in repo.**
6. Branch:

#### A1. Dual Upsedasse (expected)

| Action | Detail |
| --- | --- |
| Inventory | Keep all five flags false; `parity_status=discovery_required`; `blocker_code=ANNUAL_REPORTS_ORG_INACCESSIBLE` |
| Evidence | Append research191 dual summary path under tmp; update wiki `ui_annual_reports_inaccessible` `updated` + evidence_ref |
| Tools | **None** — do not invent `ui_annual_*` / `api_annual_*` |
| Tests | Keep/adjust `test_annual_reports_inaccessible_decision_rejects_not_applicable` still rejecting NA |
| Coverage counts | live/vision **344** unchanged (or +0) |
| Vision | Optional durable non-sensitive review record that shell is still error page — **not** sole pass; DOM dual agreement required |

#### A2. Dual non-Upsedasse product shell (unlock)

| Action | Detail |
| --- | --- |
| Product | Typed `ui_annual_reports_open` (and list-only sibling only if real list chrome exists). Pydantic I/O. No generic browser tool. |
| Tests | Live headless: DOM assert, independent second-session read-back, vision record, purge frames outside repo |
| Inventory | Green only `ui.discovery.annual_reports` (and any new discovery siblings if evidence-bound) |
| Counts | live/vision **345** if single row; complete still **false** (API walls) |
| Cleanup | No durable annual records; restore any accidental mutation |

### Explicit annual REJECT triggers

- Marking annual `not_applicable` while nav/route exist
- Greening from single session or vision-only
- Live API read-back
- Inventing API annual tools without official API resource (docs have **0** “annual” matches)
- Leaving credentials, frames, HAR, or org dumps in git

## Track B — walls evidence hygiene (secondary, always safe)

Keep residual 29 + bulk 92 **red** and toolless. Optional, only if needed for test honesty after research191:

| Item | Allowed | Forbidden |
| --- | --- | --- |
| evidence / qualification `evidence_ref` → research191 | yes | flipping `implemented` / `contract_tested` / `live_tested` |
| docs_md5 / docs_etag still lock values | yes | inventing bulk schema |
| status.json blocker text | may note research191 reconfirm; product-plane UI bulk honesty already closed | claiming complete |
| residual tools_allowed | stay false | ticketed residual tools |
| bulk tools_allowed | stay false | any bulk FastMCP tool |

If generator/tests already satisfy research186 freezes without drift, **skip code edits** for Track B and only ensure wiki/status narrative matches research191.

## Non-goals (REJECT)

- Any `api_*_bulk_*` or residual clear write tools
- Greening API bulk or residual `contract_tested`
- Setting any API `live_tested=true`
- Live API token traffic / credentialed API probes
- Product-plane UI bulk rework (already closed)
- Spawning children (single root ownership: browser probe + inventory + wiki)
- `complete:true`, `fractal node finish`, or ui-full as finish claim
- Headed browser; secrets in logs/commits

## Target coverage math

| Scenario | implemented/contract | live/vision | complete |
| --- | ---: | ---: | --- |
| A1 Upsedasse + optional hygiene | **528** | **344** | **false** |
| A2 annual unlock only | **528** (+0 API) | **345** | **false** |

Completeness walls after either scenario:

| Wall | Rows | Unlock |
| --- | ---: | --- |
| `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` | 92 API | Official bulk schema or user allows live API |
| Residual method/field/meta honesty | 29 API | Official method correction or user allows live API |
| Annual (only if A1) | 1 UI | Non-Upsedasse org/platform |

## EXECUTE steps (ordered)

1. **Reconfirm** research191 brief + docs MD5/ETag still match lock (regenerate if tmp stale).
2. **Annual dual probe** with keyring refs; write non-sensitive dual JSON under `tmp/research191_annual_dual.json` (or research192 if new pass id preferred; keep research191 family).
3. **Branch A1 or A2** as above.
4. **Track B** only if inventory strings/tests need research191 citation.
5. **Wiki:** update `ui_annual_reports_inaccessible` (A1) or add open-shell wiki (A2); scrub secrets.
6. **Tests:** inventory coverage tests + any new live annual tests; run lint + commit-mode `scripts/test.sh` (not ui-full finish).
7. **IR / FIX / COMMIT** via later steps; push via `fractal commit`.
8. **Never** node finish while walls remain.

## Files likely touched

| Path | When |
| --- | --- |
| `coverage/ui_workflows_manifest.yaml` / generator `scripts/generate_coverage_report.py` | A1 evidence or A2 green |
| `coverage/status.json` | blocker/counts after generate |
| `coverage/api_v2_manifest.yaml` | only if Track B evidence_ref hygiene |
| `src/billy_mcp/browser.py` / `server.py` / `models.py` | **only A2** |
| `tests/coverage/test_coverage_inventory.py` | A1/A2 assertions |
| `tests/live/test_ui_annual_reports_*.py` | **only A2** (or A1 reconfirm harness if adding non-green live gate) |
| `wiki/ui_annual_reports_inaccessible.md` (+ index) | always for dual result |
| `.fractal/.../tmp/*` | dual JSON, frames outside git |

## Children

**None.** Root owns probe + inventory. Do not spawn.

## Feasibility

- Budget: no limit.
- Blockers: residual/bulk external (accepted stay red). Annual may stay red (A1).
- Creds: keyring accounts present; wire via env refs at runtime.
- Risk: login friction → document AUTH path; do not false-green.

## Done when

- Dual annual observation recorded (A1 or A2) with independent second session.
- Inventory matches branch rules; no false greens; no bulk/residual tools.
- Lint + commit-mode tests pass for landed product/hygiene.
- Branch clean after COMMIT; `complete` false unless walls somehow cleared (not expected).
- Outbox notes result; not node finish.

## Post-mortem (EXECUTE)

- Dual annual result: **A1_UPSEDASSE_STAY_RED** (READY/READY; path `/:org_slug/annual_reports`; h1 Upsedasse! dual; productable false)
- Product landed (Y/N): **N** (stay red; evidence_ref → research191_annual_dual)
- Coverage counts: implemented/contract **528**, live/vision **344**, complete **false**
- Residual/bulk hygiene (Y/N): **Y** (evidence_ref + research191 unauth reconfirm citations; stay red/toolless)
- Secrets/frames purged (Y/N): **Y** (ephemeral profiles purged; no creds in repo; dual JSON non-sensitive only)
- Tests: inventory 23 passed; lint pass; commit-mode **1671** passed

## Post-Mortem (FIX-VERIFY)

- IR required fixes: **none**
- Optional IR nit landed: bulk evidence/qualification now cites research191 reconfirm (`reconfirm_ref=research191_unauth_reconfirm`); still red/toolless
- Lint: pass after format
- Tests: inventory 23 pass; commit-mode **1671** pass
- Wiki lint: project + memory clean
- complete: **false** (bulk92 + residual29 + annual1) — did **not** run ui-full/full
- Cleanup: dual profiles already purged; no secrets in tree
- Next unresolved slice: external walls (bulk schema / residual method / annual non-Upsedasse org) — no offline product available
