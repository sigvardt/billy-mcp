# 198.21 Products create delete-chrome dump

## Goal

Prove whether Billy still has no product delete chrome on the
dedicated test organisation. `D326FFB3` wants a tagged product
create through existing `ui_products_create_preview` plus exact
execute, then UI delete and restored state. research176 and
`wiki/ui_products_writes.md` say **Slet** / **Slet produkt** are
absent. Creating without a proved delete path leaves leftover
data. That is not allowed.

TDD first. Honesty-16 stays red. `ui.parity.products.create` still
names `ui_products_create_open`. Persist **only**:

- `products_path_class` (`products` / `other`)
- `products_heading_token` (`produkter` / `other` / `none`)
- `products_mere_open` (bool)
- `products_mere_slet_count` (int)
- `products_mere_slet_produkt_count` (int)
- `inventory_path_class` (`inventory` / `other`)
- `inventory_heading_token` (`lagermodul` / `other` / `none`)
- `inventory_slet_count` (int)
- `inventory_slet_produkt_count` (int)
- `save_cta_token` (`gem` / `other` / `none`)
- `create_form_open` (bool)
- `name_field_visible` (bool)
- `proved_delete_path` (`products_mere_slet` /
  `products_mere_slet_produkt` / `inventory_slet` /
  `inventory_slet_produkt` / `none`)
- `missing_keys`

`proved_delete_path` is not `none` only when the matching exact
count is 1. A count of 0 or greater than 1 is `none`. Open
**Opret produkt** is safe. Do not click **Gem** until
`proved_delete_path` is not `none`.

If delete counts stay 0: `UI_CHANGED`, no create, no leftover,
close this recapture. Do not invent `ui_products_delete_*`. Do
not SPA-DELETE `/v2/products`. Stay red.

If a genuine new delete control appears in the same EXECUTE: add
the failing persist fixture, then one tagged
`MCP-TEST-PRODUCT-` + 8 hex through FastMCP preview plus execute,
independent exact-name read-back, UI delete, third-session
absence. Never pay, email, VAT, users, or tokens.

Authority: `96908DC6`, `D326FFB3`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Closed: invoice picker, Ember, fiber,
listener, structure, `C0721A14`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.products.create` to
  `ui_products_create_preview`
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or product
  `parity_status` in the dump-only half
- Writing `author=independent_review` / `accept` / `purge_verified`
- Inventing `ui_products_delete_*` or `api_inventory_*`
- Using `DELETE /v2/products` or any `api.billysbilling.com` call
  as cleanup
- Clicking **Gem** before `proved_delete_path` is proved
- Guessed URLs, guessed selectors, generic text sweeps, force
  clicks, `evaluate` clicks, raw DOM values in git
- Remaking invoice Kunde inspects or altering invoice coverage
- Copying the bills vendor wrapper onto products
- Persisting source, locators, URLs, slugs, names, or screenshots
- Ledger, files, org, invoices, or a new contacts/bills CUD
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `5fa106c`. Memory edits from SYNC are uncommitted. Not a
backstop. `complete=false`. Honesty 16 stay red. Contacts and
bills CUD rows already name preview tools. Invoice CUD stays
`ui_invoices_*_open` and frozen. Product create preview/execute
already registered. Manifest still points the honesty row at
`ui_products_create_open`.

`tests/live/test_ui_products_writes.py` still skips on stale
`72C01DF9`. `D326FFB3` supersedes that skip. Do not submit from
that file this pass.

`BrowserProductSubmitter` fills only `name`, clicks **Gem**, and
reads back `inventory`. That is not live proof. List heading is
**Produkter** on `/:org_slug/products`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. `728BD2E4` child-split already
landed the seven write-family modules. Those children stay merged
and parked. Unlimited budget is not a reason to spawn.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/products.py` (ticket pair; no remap)
- `src/billy_mcp/ui_writes/page_flow.py`
- `src/billy_mcp/browser.py` research163 form flags
  (`_products_create_form_field_flags`, **Opret produkt** CTA)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `tests/live/test_ui_products_create_open.py` (form-open contrast)
- `wiki/ui_products_writes.md`
- `wiki/ui_products_create_open_shell.md`
- `wiki/ui_products_get_update_delete_not_applicable.md`

New files:

- `src/billy_mcp/ui_writes/products_delete_chrome.py`
- `tests/unit/test_ui_products_delete_chrome.py`
- `tests/live/test_ui_products_delete_chrome.py`

Do not grow `tests/live/test_ui_products_writes.py` this pass.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-products-delete-chrome.json`

## Design choices

1. **TDD first (`728BD2E4` / `D326FFB3`).** Confirm honesty-16
   still length 16 and red. First product edit is a failing unit
   fixture. An empty dump must fail `missing_keys` with the exact
   key set above. Run it. Capture the failure. Then implement the
   helper.
2. **Honesty-16 stays red.** Do not remap product tools.
3. **Read-only first.** Recapture list **Mere** and inventory
   delete labels. Open **Opret produkt** only to classify
   `save_cta_token` and `name_field_visible`. Escape. No **Gem**.
4. **Exact labels only.** Count **Slet**, **Slet produkt**,
   **Mere**, **Gem**, **Opret produkt** via exact role or exact
   text. No substring. No body sweep.
5. **Default stop is `UI_CHANGED`.** If
   `proved_delete_path=none`: do not create, do not submit, close
   the recapture.
6. **Persist CUD is a second half of the same EXECUTE.** Only if
   `proved_delete_path` is not `none`. Smallest route means reuse
   the proved exact delete label. Independent read-back is
   `/:org_slug/products` exact-name search unless the dump proves
   inventory lists the name.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
8. **No remap** until independent accept + purge of a real CUD
   with restored state.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.products.create` still names `ui_products_create_open`.

Add `REQUIRED_PRODUCTS_DELETE_CHROME_KEYS` and `missing_keys` in
`products_delete_chrome.py` **only after** a unit fixture fails
because those names do not exist, or add the fixture first against
a local helper stub. Preferred shape:

- Fixture feeds `{}` or a flags-only stub.
- Helper returns the missing key list from this plan.
- Current empty dump must fail with that exact set.

Do not edit `generate_coverage_report.py` or remap coverage.

### 2. Smallest helper, then one live inspect

- Implement the helper in `products_delete_chrome.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  names.
- Live test: READY via existing credential refs. Two paths:
  `/:org_slug/products` then **Mere**, then
  `/:org_slug/inventory` then optional **Opret produkt** for save
  CTA only. Escape. Write the dump outside git.
- Live test verifies the dump keys without repeating a click if
  the dump already exists from this EXECUTE. Prefer one capture.
- If `proved_delete_path=none`, stop. Do not call
  `ui_products_create_execute`.

### 3. Persist half (only if delete chrome is new)

- Failing persist fixture first.
- One FastMCP tagged create. Independent fresh-session read-back.
- Exact UI delete. Third-session absence.
- Fail closed on any other action.
- Still no remap.

### 4. Stay red

Do not finish. Do not claim complete.

## Success criteria

- Honesty-16 still length 16 and red
- Failing dump-key fixture existed, then passed
- Live dump has every required key
- No **Gem** unless `proved_delete_path` is not `none`
- No leftover tagged product if delete was not proved
- Invoice coverage untouched
- No children spawned
- Node stays red

## Post-Mortem

Completed: TDD empty-dump fixture, delete-chrome helper, sibling
live inspect. Live session opened products **Mere** and inventory
**Opret produkt**. Exact **Slet** / **Slet produkt** counts 0.
`proved_delete_path=none`. `UI_CHANGED`. No **Gem**. No product
created. Honesty 16 still red. `ui.parity.products.create` still
names `ui_products_create_open`. `complete=false`.

Live dump: path tokens `products` / `inventory`, headings
`produkter` / `lagermodul`, Mere opened, create form open, name
visible, save token `other`. Dump has no URL, source, or product
name.

Deviations: no persist CUD half. Delete chrome was absent, so
create stayed fail-closed per the plan.

Review: FAIL. Dump contract met. Products create still unbound.
Required: commit helpers, no remap, no remake, stay red. Do not
create a leftover product.

Tests: honesty-16 2 passed. Delete-chrome unit tests 6 passed.
Live `test_ui_products_delete_chrome` passed. Lint passed.
Offline `test.sh` 1983 passed, 75 deselected.

Cleanup: no tagged product created.

Next unresolved slice: not another products or invoice recapture.
Not ledger. Remaining safe honesty families are files create
(path plus digest) and org company-field update with restore.

