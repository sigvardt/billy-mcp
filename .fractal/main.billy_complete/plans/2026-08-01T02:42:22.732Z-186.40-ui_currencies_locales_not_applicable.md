# 186.40 ui currencies locales not_applicable dual-session freeze

## Goal

Product research139 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved reference UI
   parity rows for **`currencies`** and **`locales`** (all six ops each:
   get, list, create, update, bulk_save, bulk_delete) — **12 rows total**.
2. Extends the existing geo NA applicator path in
   `scripts/generate_coverage_report.py` so these prefixes are included
   (research138 deferred families now dual-proved with path contrast).
3. Updates inventory tests for NA count 30→**42** and live/vision baseline
   68→**80**.
4. Adds a scrubbed wiki page for the freeze; updates geo wiki deferred note
   if present.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** `ui_currencies_*` /
   `ui_locales_*` tools.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research139).  
Dual evidence (tmp, non-git): `tmp/research139_currencies_locales_dual.json`,
`tmp/research139_currencies_locales_contrast.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–138).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior package: 186.39 geo NA (`wiki/ui_geo_cities_not_applicable.md`);
`check_coverage.not_applicable_ui_errors` already evidence-gated.

## Why this slice

1. research138 deferred currencies/locales for missing path contrast; research139
   closed that gap with dual soft-empty == nonsense.
2. Dual independent sessions: no currency/valuta/locale/sprog/language nav
   labels/hrefs; candidate paths body_len **127**, h1_count **0** identical to
   nonsense; known shells (invoices/products/transactions/vat-declarations/
   settings) have h1 + larger body.
3. Soft URL landing is not a productable shell — do not invent UI tools.
4. NA gate in `check_coverage` already accepts evidence-backed NA; reuse it.
5. API currency/locale reads stay offline-green; API live remains
   `out_of_scope_by_user`; bulk stays red external-contract.

## Non-goals

- Shipping any `ui_currencies_*` / `ui_locales_*` FastMCP tools.
- Greening soft-empty paths as list shells.
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) or residual 405 currency/locale API writes.
- Live API traffic / `BILLY_API_TOKEN`.
- Dual-count `salesTaxReturns.list` / `files.list` / `users.list` (next slice;
  vat-declarations shell already real — inventory-only dual-count residual).
- Changing geo NA evidence_code semantics beyond adding prefixes/count.
- `complete: true`, headed browser, children (tight root ownership).
- Node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Implementation agent for EXECUTE: **codex-power** per
routing policy when spawning; if EXECUTE stays on this root process, apply the
same file edits here. Independent review remains **grok**.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.currencies.` and
  `api.locales.`; set `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 42` (7 families × 6).
  Keep evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class) **or**
  add sibling code only if tests need separation — prefer **one code, extended
  prefixes** for minimal diff. |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  update evidence_ref to include research139 dual+contrast for currency/locale
  rows (or pass research id by family); remove `deferred_families:
  ["currencies","locales"]` (empty or omit). Mention soft-empty==nonsense and
  contrast shells including vat-declarations/settings as optional controls. |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefixes alone should wire the
  12 new rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape as geo NA: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing `not_applicable_ui_errors`
  already validates required fields. Touch only if new evidence_ref patterns
  fail validation. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count 42; prefixes include currencies + locales.
  - Remove/update assertions that currencies/locales remain
    `discovery_required`.
  - live/vision count `38 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → still valid if
    ROW_COUNT is 42 (38+42=80).
  - annual still NA-rejected red; bulk 92 red; no currency/locale UI tools. |
| Wiki | New `wiki/ui_currencies_locales_not_applicable.md` (scrubbed) +
  sorted `_index` link. Optionally one-line update on
  `wiki/ui_geo_cities_not_applicable.md` that currencies/locales are no longer
  deferred. |
| Coverage regenerate | Run generator → refresh
  `coverage/ui_workflows_manifest.yaml`, `coverage/status.json`,
  `coverage/report.md`. |

## Product decision matrix

| Family | API parity ops | Dual evidence | Product |
| --- | ---: | --- | --- |
| currencies | 6 | nav empty + soft-empty == nonsense (research139) | **not_applicable** |
| locales | 6 | same | **not_applicable** |
| (prior) cities/countries/countryGroups/states/zipcodes | 30 | research138 | keep NA |
| salesTaxReturns.list | 1 | vat-declarations real shell | **defer** dual-count |
| annual_reports | discovery | nav exists, Upsedasse | stay red |

## Work sequence (EXECUTE)

1. Extend `GEO_UI_NOT_APPLICABLE_API_PREFIXES` + `ROW_COUNT=42`.
2. Refresh applicator evidence strings / evidence_ref for research139 families;
   clear deferred_families.
3. Confirm `build_ui_manifest` applies to new prefixes.
4. Update inventory tests (counts, no longer discovery_required for
   currencies/locales).
5. Wiki page + `_index`.
6. Regenerate coverage; assert live/vision 80; complete false.
7. Run `scripts/lint.sh` + offline `scripts/test.sh`.
8. Ready IR (grok) → FIX-VERIFY → COMMIT.

## Acceptance criteria

- [ ] All 12 currencies/locales UI parity rows: `parity_status=not_applicable`,
      empty `tool_name`, five flags true, `not_applicable_decision=accepted`,
      dual-session evidence refs (research139).
- [ ] Total geo/reference NA rows **42**; live/vision **80**.
- [ ] No `ui_currencies_*` / `ui_locales_*` tools registered.
- [ ] annual still red NA-rejected; bulk 92 external-contract red.
- [ ] Offline test suite green; complete false; no live API.
- [ ] Independent Grok IR of product vs research139 (ACCEPT/FAIL).

## Risks

| Risk | Mitigation |
| --- | --- |
| Settings hub mistaken for currency panel | research139 nested settings/* soft-empty; hub-only is not enough |
| Over-greening API bulk UI rows as NA while API bulk red | Intended: UI NA ≠ API bulk green; API bulk stays external-contract |
| Check_coverage rejects new evidence_ref format | Reuse same qualification shape as geo |
| Live/vision count formula drift | Keep `38 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` |

## Feasibility

- Budget: no limit. Scope is inventory-only, ~few files, same as 186.39.
- No children. No live UI suite required for NA inventory freeze.
- Blockers unchanged after product: bulk 92, annual org_inaccessible, residual
  parity (salesTaxReturns dual-count, etc.).

## Review / commit gates

- Independent Grok review of product diff vs research139 (ACCEPT/FAIL).
- Before commit: lint + offline test.sh + check_coverage (not require-complete).
- Push via `fractal commit`. Not node finish.

## Post-Mortem

### Work completed

- Producted research139 dual-session currencies/locales UI absence into
  evidence-backed `not_applicable` for **12** UI parity rows (currencies +
  locales × 6 operations).
- Extended `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.currencies.` and
  `api.locales.`; `GEO_UI_NOT_APPLICABLE_ROW_COUNT` 30→**42**; research139
  resource set for evidence_ref routing in `apply_ui_geo_reference_not_applicable_evidence`.
- Cleared `deferred_families` for NA qualification objects.
- Inventory tests: currency/locale rows must be NA (no longer discovery_required);
  live/vision formula remains `38 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **80**.
- Wiki: `wiki/ui_currencies_locales_not_applicable.md` + `_index` link; geo wiki
  deferral closed with cross-link.
- Regenerated coverage manifests/status/report. complete remains **false**.
- No currency/locale FastMCP UI tools. No live API traffic.

### Deviations

- None material. Optional dual-session live pytest not added (inventory freeze
  per plan, same as 186.39). Optional N1–N3 from IR deferred.

### Review findings

- Independent review **ACCEPT** (`tmp/grok-review.md`); required fixes: **none**.
- Optional N1–N3 non-blocking (live dual pytest; tmp-only dual JSON; salesTaxReturns
  dual-count next).

### Test results (FIX-VERIFY)

- `bash .fractal/main.billy_complete/scripts/lint.sh` — pass
- `uv run python scripts/check_coverage.py` — pass (not `--require-complete`)
- `bash .fractal/main.billy_complete/scripts/test.sh` — **1565 passed**, 35 deselected (reconfirm in progress / recorded)
- `wiki lint` project + memory — clean
- `coverage/status.json` `complete: false` (generator-derived; not hand-set)
- BILLY_TEST_MODE=full / ui-full **not** run (completion requirements not met)

### Cleanup state

- No disposable Billy records created this package (inventory-only).
- No raw frames retained in repo; `vision_evidence` null on NA rows.
- research139 dual/contrast JSON remains owner tmp only (not committed).

### Next unresolved coverage slice

1. Dual-count `ui.parity.salesTaxReturns.list` via existing
   `ui_vat_declarations_list` (research139 alternate).
2. annual_reports unlock (non-Upsedasse non-prod org) — stays red.
3. 92 bulk external-contract red (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`).
4. Residual offline clear writes still blocked (405 / metadata-only).
5. Not node finish until complete=true under scoped policy + ui-full suite.
