# 198.47 Product DELETE 2xx watch then reverse-clean

## Pivot

Owner `A0633A17`: product delete clicks scoped
`data-cy=delete-icon` and **Ja, slet**, then one fresh
read-back. It records no response. That cannot tell a missing
or failed `DELETE` from a stale list. Watch before the confirm
click. Require exactly one `DELETE /v2/products/:id` with 2xx.
Then poll a fresh `/products` page until the tagged
`table-item` is absent. Never blind-retry.

Owner `01FCBA13` / `3377FB3C`: seven leftover triples still
live. Reverse-clean invoices first with **Ja, slet faktura**
(already bound on `4af77cb`), then products with the new
DELETE watch, then customers. Prove settled zero. Do not claim
clean-org.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`):
`DELETE /v2/products/:id`, path id, no body. Bulk
`DELETE /v2/products?ids[]=` stays ambiguous. Do not re-lock
(`728BD2E4`). Do not spawn a child. Do not remake dumps.

HEAD `4af77cb` is clean. Last commit bound invoice confirm.
No interrupted work.

Current fail: `delete_tagged_row` returns True after **Ja,
slet** and `_wait_row_gone`. `BrowserProductDeleter` then
`prove_unfiltered_row_absent`. No `page.on("response"`.

## Goal

Failing fixture, then watch singular product DELETE 2xx, then
FastMCP reverse-clean the seven leftover triples. Stay red.

1. Offline failing fixture: product delete must watch
   `DELETE /v2/products/:id` 2xx before independent absence.
2. Smallest fix in the existing product-delete modules. No new
   helper.
3. Point leftover live lists at the seven `01FCBA13` triples.
4. Live FastMCP reverse-clean. Settled zero. Stay red.

## Why no child

One family on files the root already owns:

- `src/billy_mcp/ui_writes/products_delete_row.py`
- `src/billy_mcp/ui_writes/products_delete.py`
- `tests/unit/test_ui_products_writes.py`
- `tests/unit/test_ui_products_unfiltered_readback.py`
- `tests/live/test_ui_products_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `tests/live/product_leftover_sweep.py`
- `wiki/ui_products_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child. `D68E402A` forbids a new helper.

## TDD

`728BD2E4` / `A0633A17`: failing fixture first, then the
smallest fix.

1. Add an offline fixture that source-locks the watcher:
   - `delete_tagged_row` attaches `page.on("response"` before
     the **Ja, slet** click.
   - Persist hit is `DELETE` plus path `/v2/products/` with an
     id segment and status 200 / 201 / 204.
   - Events with no status do not count (`B9C4FA7C`).
   - Collection `/v2/products` and `?ids[]=` do not count.
   - Missing or non-2xx is fail-closed. No second click.
   - Independent absence still uses
     `prove_unfiltered_row_absent` on `/products`.
   Run it. Capture the fail.
2. Smallest fix:
   - Reuse the invoice persist-hit shape, scoped to products.
   - Watch before confirm. Wait for exactly one 2xx singular
     DELETE. Then `prove_unfiltered_row_absent`.
   - Poll or reload the fresh `/products` list until the tagged
     row is gone. First paint is not proof (`3377FB3C`).
   - Keep **Ja, slet** for products. Do not use **Ja, slet
     faktura**.
   - Keep scoped `table-item` then row `delete-icon`.
   - No `force`. No `evaluate`. No page-wide `delete-icon`.first.
3. Update leftover names to the seven `01FCBA13` triples
   (`MCP-UI-INV-*` / `MCP-UI-PRD-*`):
   `47BFE4A3/C0082FCA`, `9FDD7B04/2A0968A4`,
   `8DA1053F/773A4D76`, `05D2C589/0116A3D1`,
   `A9152C51/9A032275`, `2574A9D2/22679129`,
   `2AB12C2E/2647389E`.
   Drop the six `59A3A933` names and the stale
   `OWNER_LEFTOVERS` set as the cleanup target.
4. Offline unit/contract/safety for the touched files. Then live
   FastMCP reverse-clean through
   `ui_invoices_delete_{preview,execute}`,
   `ui_products_delete_{preview,execute}`,
   `ui_clients_delete_{preview,execute}`. Invoices first,
   products second, customers third. Require DELETE 2xx and a
   later settled empty list. Do not create a new record.
5. Vision: `author=live_test`,
   `reviewer_verdict=pending_review`. Do not self-accept. Do
   not purge in the live test.

If live SPA fires only bulk `DELETE /v2/products?ids[]=`: fail
closed, record sanitized method/path/status, stay red. Do not
green bulk92.

## Out of slice

Files, daybook, **Godkend**, emails, bulk, docs re-lock, dump
remakes, child spawn, live API, product create, invoice update,
archive dumps, Kunde dumps, honesty-16 remap, `node finish`.

## Success

Product delete watches one `DELETE /v2/products/:id` 2xx, then
a fresh unfiltered `/products` row is gone. The seven leftover
triples are deleted in reverse dependency order, or the run
fails closed with evidence. Honesty-16 stay red. Do not finish.

## Budget

No cost or time cap. This is one TDD slice plus one live
leftover cleanup. Do not expand.

## Post-Mortem

Completed:

- Product delete watches `page.on("response"` before **Ja, slet**
  and requires exactly one `DELETE /v2/products/:id` 2xx.
- Collection `/v2/products` and `ids[]` bulk do not count.
- Persist fail is ToolError. Inventory is not a retry.
- Independent `/products` row-absence polls until settled.
- Live reverse-clean of the seven leftover triples passed.
- Independent review FAIL (`D71E5B82`): the live invoice CUD
  test had become delete-only while still writing
  `workflow_ref=ui.parity.invoices.update`.
- Fix: CUD now creates a disposable customer, product, and
  draft, updates **Enhedspris** to `2,00`, then deletes that
  triple. Leftover names are isolated. Honesty-16 stay red.

Deviations:

- Leftover reverse-clean ran before the CUD isolation fix.
  Owner `D71E5B82` forbids mapping that delete-only run as
  invoice update.

Review: `tmp/grok-review.md` FAIL on delete-only invoice
update mapping. Product DELETE 2xx watch accepted.

Tests: lint.sh pass. `BILLY_TEST_MODE=commit` test.sh 2103
passed after the CUD isolation edit. Live disposable invoice CUD
passed. Isolated leftover sweep hit `UI_CHANGED` on a second
login; it now reuses the same profile. Did not run `ui-full`.

Cleanup: leftover triples were deleted. Do not treat the org as
qualified-complete. Next slice is reusable invoice CUD on a
disposable draft after settled zero, then independent accept.
Stay red. Do not finish.

