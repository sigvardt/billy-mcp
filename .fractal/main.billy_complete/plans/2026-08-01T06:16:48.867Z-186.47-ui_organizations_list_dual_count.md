# 186.47 ui organizations list dual-count via settings company shell

## Goal

Dual-count green **`ui.parity.organizations.list`** by mapping the already
qualified read-only **`ui_settings_company_open`** Indstillinger **Virksomhed /
company** panel shell to **`api.organizations.list`**, using the same
empty-input `shell_open_only` contract already live for
`ui.discovery.settings_company`.

No new browser workflow. No new API/UI tools. No get/create/update/bulk
greening. No annual product. No bulk external-contract greening. No taxRates /
files / bankLines / postings / productPrices dual-counts this slice.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research146;
also `tmp/grok-research.md`).  
Dual evidence (tmp, non-git): `tmp/research146_residual_parity_dual.json`.  
Discovery product already green: `ui_settings_company_open` (research126 /
prior 186.27); wiki `wiki/ui_settings_company_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_company_open.py`,
vision record class `tmp/vision-records/ui_settings_company_open.json`.  
API offline list already green: `api.organizations.list` → bootstrap reads
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–146).  
Prior dual-count precedents: invoices/products/contacts/bills/transactions/
salesTaxReturns/users/accounts `.list` via `parity_of_api_list=True` in
`scripts/generate_coverage_report.py` (accounts/users settings-panel templates
are the closest).

## Why this slice (research146)

1. Dual headless sessions research146:
   - Typed `ui_settings_company_open` dual READY + success
     (`shell_kind=settings_company`, `company_panel_markers_present=true`).
   - Soft seeds `/organizations`, `/organization`, `/company` dual soft-empty
     body_len **127** (= nonsense) — no dedicated multi-org list route in app.
   - All greened residual tools dual-ok; no API token; no write CTA clicks;
     ephemeral profiles deleted.
2. Same inventory dual-count pattern as `ui.parity.accounts.list` /
   `ui.parity.users.list`, with **parity_status remaining `shell_open_only`**
   (settings multi-section panel for active-org identity; not a dedicated
   `/:org_slug/organizations` list shell).
3. get/create/update/bulk UI parity remain red (no dual-proved detail/write
   isolation; bulk external-contract on API lane).
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research146: taxRates Momssatser multi-resource;
   files↔Bilag weak map; bankLines match isolation; postings no-steal from
   transactions; productPrices nested form; contactBalancePayments NA secondary.

## Non-goals

- Inventing `api_settings_*` or a second UI organizations/company tool.
- Greening `ui.parity.organizations.get|create|update|bulk_save|bulk_delete`.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting taxRates, files, bankLines, postings, productPrices.
- NA packages (productPrices / contactBalancePayments / bank* / salesTax*).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research126 live
  qualification + research146 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_company_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror accounting/users dual-count); when
  True: keep `api_row_id` from parity builder (`api.organizations.list`), append
  research146 evidence mapping, keep `parity_status=shell_open_only`; discovery
  path keeps default False and `api_row_id=None`; wire
  `if api_row["id"] == "api.organizations.list": apply_...(parity_of_api_list=True)`
  in the parity builder loop (alongside accounts/users/salesTaxReturns) |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.organizations.list` → `ui_settings_company_open`; qualified shell
  count **41→42**; live/vision total **121→122** (`ui_shell_green + geo_na` =
  `42 + GEO_UI_NOT_APPLICABLE_ROW_COUNT`); assert evidence mentions
  `api.organizations.list` + research146; assert other organizations ops stay
  red `discovery_required`; hard-code live total where present |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_company_open_shell.md` — dual-count note for
  `api.organizations.list` / shell open only; non-claims get/create/update/bulk;
  soft `/organizations` empty observation; API offline non-live; update desc /
  `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_company_open`
- models / server registration
- `tests/live/test_ui_settings_company_open.py`
- unit browser tests for the company settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_company_open` (existing) |
| Open sequence | Indstillinger company / Virksomhed panel (research126 product) |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_company` |
| Panel markers | Navn og adresse; Kontaktinformation (`company_panel_markers_present`) |
| Input | empty strict model |
| Soft seeds | `/organizations`, `/organization`, `/company` dual soft-empty = nonsense |
| CTA / write | never Gem / Tilføj ejer / Opret* / Tilføj* / Upload |
| Parity row | `ui.parity.organizations.list` / `api_row_id=api.organizations.list` |
| parity_status | **`shell_open_only`** (not `list_shell_open_only`) |
| API invent | never invent `api_settings_*` |
| Distinct shells | accounting / vat / invoicing / users settings stay separate |
| get/create/update/bulk UI | stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.organizations.list` wiring on
   `apply_ui_settings_company_open_shell_evidence`.
2. Update inventory tests counts and assertions (42 qualified shells; 122
   live/vision with geo NA = 42 + 80).
3. Run coverage generator; confirm status live/vision **122**, complete **false**.
4. Wiki dual-count note on settings company shell page + index desc if needed.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (no live API; no
   `BILLY_TEST_MODE=ui-full` completeness claim).
6. Memory EXECUTE notes; leave dual JSON under tmp (non-git) for IR.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.organizations.list` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_settings_company_open` |
| parity_status | `shell_open_only` |
| api_row_id | `api.organizations.list` |
| Evidence | research146 + maps api.organizations.list; no PII |
| Other organizations ops | stay red / discovery_required |
| Deferred families | taxRates, files, bankLines, postings, productPrices remain incomplete |
| Tools | No new tool registrations |
| API live | All API live_tested remain false with out_of_scope_by_user |
| Complete | `complete: false` |
| Offline tests | Inventory + non-live suite green |

## Independent review focus

- Confirm research146 dual proves company tool + company panel markers + soft
  `/organizations` empty (not inventing a multi-org list path).
- Confirm only `.list` is greened; get/create/update/bulk stay red.
- Confirm `shell_open_only` honesty (active-org identity panel; multi-section
  settings; not claiming a dedicated organizations list route).
- Confirm live/vision 122 math: 42 shells + 80 GEO NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects company form as organizations list | Same bar as accounts Kontoplan / users Brugere dual-count; shell_open_only; soft path empty dual documented; only list greened |
| Accidentally greening all organizations ops | Explicit per-op id gate `api.organizations.list` only |
| Count drift (41→42 shells) | Update every hard-coded 41 / 121 assertion in inventory tests |
| Wiki still claims “does not green organizations” | Edit dual-count note carefully; keep non-claims for other ops |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **122**; complete **false**.
- Ready independent review of dual-count package 186.47.
- Not node finish.

## Post-Mortem

### Work completed

- Extended `apply_ui_settings_company_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror accounting/users dual-count).
- Wired `api.organizations.list` → company applicator with `parity_of_api_list=True`.
- Inventory tests: qualified shells 41→42; live/vision 121→122; organizations.list
  dual-count asserts; get/create/update/bulk organizations stay red;
  discovery company keeps `api_row_id=None`.
- Regenerated coverage: live/vision 122, implemented/contract 306, complete false.
- Wiki dual-count note on `ui_settings_company_open_shell` + `_index` desc.
- No new UI tools; no src/ browser or server changes.

### Deviations

- None material vs plan. Optional IR N1/N2 left as-is (tmp dual JSON non-git;
  live suite not re-run this slice).

### Review findings

- IR 186.47: ACCEPT, required fixes none.
- Overall completeness: FAIL (expected: bulk 92, annual_reports red,
  residual UI parity, API live out of scope).

### Test results

- lint.sh: pass (wiki + ruff + pyright + coverage check + repo policy).
- offline test.sh: 1565 passed, 35 deselected (EXECUTE and FIX-VERIFY).
- Full BILLY_TEST_MODE=ui-full / completeness suite: not run (complete still false).
- wiki lint: wiki + memory clean.

### Cleanup state

- Research dual used ephemeral profiles (purged). No disposable UI writes.
- No frames/HAR in repo. No credentials in product files.

### Next unresolved coverage slice

- Residual UI parity (taxRates Momssatser multi-resource; files weak Bilag map;
  bank recon match isolation; postings no-steal; productPrices nested;
  contactBalancePayments NA secondary). Bulk 92 external-contract and
  annual_reports org_inaccessible remain red.
