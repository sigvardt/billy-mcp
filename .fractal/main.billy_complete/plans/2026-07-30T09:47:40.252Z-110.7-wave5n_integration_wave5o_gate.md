# 110.7 Wave-5n product integration and Wave-5o freeze gate

## Objective

Integrate only the completed, freeze-authorised Wave-5n invoice-late-fee
product after verifying its committed bytes, then obtain an independent Grok
product verdict. Preserve the cited Wave-5o invoice-reminder research as a
gated future freeze package; it authorises neither a freeze page nor product
code in this iteration.

## Evidence and current boundaries

- Research62 and `wiki/wave_fiven_ticketed_writes_contract.md` freeze exactly
  invoiceLateFees create and update. `wiki/wave_fiven_freeze_independent_review.md`
  is the authoritative ACCEPT for that freeze.
- The completed `codex-power` child reports four offline-tested tools, a child
  count of 254 `api_*` tools, and 174 implemented/contract-tested rows. Those
  claims are not root authority until root independently verifies and merges
  its archived committed bytes.
- Research63 in `.fractal/main.billy_complete/tmp/grok-research.md` is a cited
  current official-doc brief: invoiceReminders POST reaches 401 with a JSON
  body; PUT, singular DELETE, and bulk DELETE return 405. It permits only a
  later create-only Wave-5o freeze after Wave-5n product ACCEPT.
- Root remains 250 tools, 172 offline-green rows, zero live/vision rows, 92
  ambiguous bulk rows, and `complete: false` until a valid Wave-5n merge.

## Delegation decision

The only implementation child has completed and owns the Wave-5n module, tests,
registration, and two inventory rows. No new child starts now because the only
remaining work is causally dependent on inspecting and merging that committed
result.
After a clean root merge, spawn an independent `grok` reviewer; it has a
separate documentation-and-audit concern and may not edit product code. Wave-5o
freeze authoring remains blocked on this review's ACCEPT, so it is not spawned
early.

## Execution plan

1. Inspect `main.billy_complete.wave5n_invoice_late_fee_product` from clean
   committed bytes using
   `git archive` plus `git diff main.billy_complete...<child>`. Verify its scope
   is limited to the frozen four tools, registration, specified tests, and the
   two inventory rows/generated coverage state; confirm create cleanup says
   `live non-production cleanup strategy unqualified; singular DELETE is unsupported`.
2. Run the focused invoice-late-fee, coverage, registry, formatting, Ruff,
   Pyright, and repository-policy checks against root-ready bytes. Confirm the
   exact root target is 254 `api_*` tools, 174 implemented/contract-tested,
   zero live/vision, 92 bulk rows red, and `complete: false`.
3. Only if those checks pass, merge with
   `git merge --no-ff -m "merge main.billy_complete.wave5n_invoice_late_fee_product" main.billy_complete.wave5n_invoice_late_fee_product`, reconcile the
   wiki index if needed, and publish a terse integration notice. A failed check
   returns a precise repair request to a new Codex Power leaf rather than
   weakening coverage.
4. After root merge, configure and launch the independent review leaf with the
   explicit route:

   ```bash
   fractal node init wave5n_product_review_grok \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --agent=grok --max-cost=320 --max-iter-cost=80 --max-iters=8 \
     --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Its committed brief must cite current official docs and inspect only the
   root merge, with a durable `wiki/wave_fiven_product_independent_review.md`
   ACCEPT or REJECT. It must expressly preserve red live, UI, vision, bulk,
   delete, and completeness states. If Grok fails before edits for
   authentication, quota, or rate limit, retry the complete review with a
   `codex-power` fallback leaf; never switch after a review record is edited.
5. Do not author `wiki/wave_fiveo_ticketed_writes_contract.md`, green an
   invoiceReminders row, or start an invoice-reminder product child unless the
   Wave-5n independent product verdict is ACCEPT on root. Preserve the current
   Research63 plan and state change for the commit; its create-only scope and
   401/405 evidence are durable planning evidence, not delivery.

## Acceptance checks for this iteration

- A root merge occurs only after archive inspection and all non-live checks
  pass; otherwise root remains at its honest 250/172 baseline.
- The reviewer is independent, explicitly routed to Grok, and starts only from
  merged root bytes.
- No live request, browser action, credential material, bulk tool, delete tool,
  webhook, or premature coverage claim is introduced.
- The worktree closes with the Research63 records and this plan committed; the
  completed child is either merged after verification or deliberately retained
  with a documented rejection.
