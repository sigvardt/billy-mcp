# 186.56 UI special files_upload dual-count Bilag


## Goal

Product research155 ACCEPT:

1. Strengthen existing read-only FastMCP tool **`ui_uploads_list`** so the
   Bilag shell contract dual-proves the **special files upload surface**:
   path `/:org_slug/uploads`, h1 **Bilag**, CTA **Upload filer**, and
   **`file_input_present`** (`input[type=file]` count ≥ 1). Never set file
   inputs, never submit upload.
2. Dual-count **exactly** `ui.parity.special.files_upload` (maps
   `api.special.files_upload` / binary `POST /v2/files`) onto that tool with
   five UI flags true + vision.
3. Keep discovery **`ui.discovery.uploads`** green on the same tool (refresh
   evidence to research155 + file-input contract). Do **not** invent a second
   uploads tool.

Target math (from tip `e933e8f`): live/vision **149 → 150** (parity +1 only;
discovery already green); GEO NA **100** unchanged; implemented/contract
**333 → 334**; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research155)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. Special upload: `POST /v2/files` binary + `X-Filename` / optional attachment headers (API tools offline-green; live false / `out_of_scope_by_user`). |
| Soft `/files` | soft-empty body **127** dual == nonsense — not the UI surface. |
| Bilag shell | greened `ui_uploads_list`: path `/uploads`, h1 **Bilag**, `upload_action_visible` true dual. |
| File inputs dual | research155 dialog probe: `input[type=file]` count class **2** both sessions **before and after** observe-only click **Upload filer**; never `set_input_files`. |
| Isolation | soft `/files` ≠ Bilag; receipt inbox `/vouchers` already separate (`file_control_present`); soft attachments empty; special.invoice_email deferred (0 invoice detail links dual). |
| Deferred / reject | invoice_email dual/NA; products/contacts create dual-count; pure NA files_upload; postings→transactions steal; files.* / attachments.* full family. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md` (worktree copy
`tmp/grok-research.md`). Dual: `tmp/research155_residual_dual.json`,
`tmp/research155_dialog_probe.json`.

## Non-goals

- Greening `special.invoice_email`, `files.*` CRUD parity, `attachments.*`,
  `postings.*`, products.create, contacts.create, tax*, bank*, bills/invoices
  get/update/delete/bulk
- Pure NA for `special.files_upload`
- Dual-count special upload onto receipt_inbox or soft `/files`
- Setting `input[type=file]`, choosing a local file, multipart upload, or any
  write/submit
- New tool name (reuse `ui_uploads_list`)
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Models + browser signature + server (if schema changes) | Root Grok EXECUTE |
| Live dual-session test + vision record refresh | Root Grok EXECUTE |
| Coverage generator dual-count special.files_upload | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki uploads shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research155)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_uploads_list` (existing) |
| lane | ui |
| kind | list_shell_open_only + upload-surface observe (read-only) |
| success model | `UiUploadsListSuccess` (extend) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools (special files upload already offline-green) |

### Success model fields (non-PII)

`extra=forbid`; no org_slug/url/selector secrets. Mirror receipt-inbox
`file_control_present` naming or use research155 name `file_input_present` —
pick **one** stable field and use it everywhere (prefer
`file_input_present` to match research155 wording, or
`file_control_present` for consistency with `UiReceiptInboxListSuccess`;
EXECUTE chooses one and keeps tests/schema aligned).

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/uploads` |
| `heading` | Literal `Bilag` |
| `upload_action_visible` | bool — CTA **Upload filer** present (must not require click for success; may click observe-only if needed for visibility only) |
| `file_input_present` (or `file_control_present`) | bool — `input[type=file]` count ≥ 1; **must be true** for success (research155 dual count class 2) |
| `shell_markers_present` | bool — shell nav markers as today |

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open `https://mit.billy.dk/{slug}/uploads` (existing path).
4. Success when:
   - path class list uploads
   - h1 **Bilag**
   - **Upload filer** visible
   - at least one `input[type=file]` present (reuse `_has_file_input_present`
     already used by receipt inbox)
5. **Never** `set_input_files`, file chooser accept, drag-drop submit, or any
   upload confirm. Optional observe-only click **Upload filer** is allowed only
   if it does not set files and does not create records (research155: click
   left inputs present, dialog count 0).

### Reject as success

| Failure | Code class |
| --- | --- |
| Login page | AUTH_REQUIRED |
| CAPTCHA / passkey / push | AUTH_INTERACTION_REQUIRED |
| Missing Bilag h1 / wrong path / no Upload filer | UI_CHANGED |
| No file input | UI_CHANGED (new hard fail for upload-surface contract) |
| Egress deny | EGRESS_DENIED |
| Soft empty SPA chrome | not success |

### Hard ban

Never set file inputs; never upload bytes; never green invoice_email;
never dual-count soft `/files` or `/vouchers` as this special.

## Coverage generator contract

Mirror special dual-count pattern (`parity_of_special_user_get`):

1. Extend `apply_ui_uploads_list_shell_evidence(row, *, parity_of_special_files_upload: bool = False)`:
   - response_fields include new file-input bool
   - discovery path: flag false (api_row_id None)
   - when `parity_of_special_files_upload=True`: map
     `api.special.files_upload` → tool `ui_uploads_list`; five flags true;
     evidence cites research155 dual file-input + live test + vision record;
     `parity_status` stay `list_shell_open_only` or use
     `shell_open_only` / `upload_surface_open_only` if inventory already has a
     stable enum — prefer keeping **`list_shell_open_only`** unless generator
     already uses a better label for specials
2. In `build_ui_manifest` api_parity loop:
   - `if api_row["id"] == "api.special.files_upload": apply_...(row, parity_of_special_files_upload=True)`
3. Discovery `uploads` family: still call apply without special flag (or with
   false); refresh evidence to note research155 file-input requirement.
4. **Must not** green: `api.files.*`, `api.attachments.*`,
   `api.special.invoice_email`, receipt_inbox re-scope, soft `/files`.
5. After regenerate: live/vision **150**; implemented/contract **334**;
   complete **false**; GEO NA **100**; API live_tested all false with
   `out_of_scope_by_user`.
6. Inventory tests: special.files_upload green; files.*/attachments.*/invoice_email
   red; discovery uploads still green; math 149→150.

## EXECUTE steps (root)

1. **Models** (`src/billy_mcp/models.py` + `tests/test_models.py`): add
   file-input bool to `UiUploadsListSuccess`; schema forbids secrets; update
   forbid/extra tests.
2. **Browser** (`src/billy_mcp/browser.py`): on successful Bilag signature,
   measure `_has_file_input_present(page)` and require true for success (or
   return measured bool and fail closed when false). Unit tests in
   `tests/unit/test_browser.py` — success with file input; UI_CHANGED without.
3. **Server / registry mocks** (`tests/unit/test_coverage_server.py` etc.):
   return new field on success mocks.
4. **Live test** `tests/live/test_ui_uploads_list.py`: assert
   `file_input_present` dual both sessions; second-session independent
   read-back; refresh vision record + purge; never set files; skip if
   credentials missing.
5. **Coverage generator** dual-count exact `api.special.files_upload` +
   inventory tests for math and residuals.
6. **Regenerate** coverage manifests.
7. **Wiki** update `wiki/ui_uploads_list_shell.md` (+ `_index` if needed):
   research155 file-input dual; maps special.files_upload; isolation vs
   soft `/files`, receipt inbox, attachments, invoice_email.
8. **Offline gate**: `scripts/lint.sh`; offline `scripts/test.sh`.
9. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Still only `ui_uploads_list`; typed success includes file-input bool |
| Discovery | `ui.discovery.uploads` remains five flags true |
| Parity | `ui.parity.special.files_upload` five flags true; tool `ui_uploads_list` |
| Exact id | Only `api.special.files_upload` dual-counted (not files.* / attachments.*) |
| Isolation | invoice_email red; files.* red; attachments.* red; receipt_inbox unchanged |
| CTA / files | No set_input_files; no upload bytes |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **150**; GEO NA **100**; complete **false** |
| Suite | Offline lint + test.sh green; live dual when credentials configured |

## Expected coverage math

| Metric | Before (tip e933e8f) | After product |
| --- | --- | --- |
| live/vision | 149 | **150** |
| NA rows | 100 | 100 unchanged |
| discovery uploads | green | green (evidence refresh) |
| implemented/contract | 333 | **334** |
| complete | false | false |

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| File inputs hidden until CTA click | research155: present **before** click (count 2); measure after settle; optional observe-only click without set files |
| Dual-count files.list / attachments by mistake | Generator exact `api.special.files_upload` only; inventory asserts residuals red |
| Confuse receipt inbox vouchers file control | Different path/h1/tool; no re-scope |
| Breaking existing uploads list callers | Additive required field: update all unit/live mocks and schema tests in same EXECUTE |
| Accidental file upload in tests | Ban set_input_files; live test assert no upload side effects |
| Live credentials missing in CI | Existing skip pattern on live uploads list test |
| Over-claim complete | complete stays false; bulk 92 + residual UI remain |

## Post-mortem

### Delivered

- Strengthened `ui_uploads_list` with `file_input_present` (research155).
- Dual-count exact `ui.parity.special.files_upload` → `ui_uploads_list`.
- Coverage regenerated: live/vision **150**, implemented/contract **334**,
  complete **false**.
- IR **ACCEPT** (`tmp/grok-review.md`): no required fixes; optional N1–N2 deferred.
- FIX-VERIFY: lint.sh pass; wiki lint memory+project clean; offline test.sh
  **1586 passed** / 38 deselected; special.files_upload + discovery uploads green;
  invoice_email + files*/attachments* residual red; bulk 92 external-contract;
  annual stay red.
- Vision record purge_verified true (node tmp, outside repo frames).

### Deferred

- Optional N1: measure `upload_action_visible` / `file_input_present` bools after
  signature instead of hardcoding True (same pattern as sibling shells).
- Optional N2: restore explicit wiki CTA ban list strings (Udfyld alle / Kom i gang).
- special.invoice_email (empty list + Levering / create-send isolation).
- files.* / attachments.* family dual-count or NA packages.
- products/contacts create form open; postings nested.
- bulk 92 external-contract; annual_reports org_inaccessible.
- API live remains out_of_scope_by_user.

### Notes

- Prefer residual dual-count/NA next: special.invoice_email only with disposable
  draft dual; else multi-resource packages with dual proof. Complete false; do
  not node finish.

### Test results

| Gate | Result |
| --- | --- |
| lint.sh | pass |
| wiki lint (memory + project) | clean |
| offline test.sh | 1586 passed, 38 deselected |
| live uploads list dual | 1 passed (EXECUTE) |
| coverage | live/vision 150; contract/impl 334; complete false |

### Cleanup

- No file uploads performed.
- Live profiles ephemeral; vision frames purged (purge_verified true).
