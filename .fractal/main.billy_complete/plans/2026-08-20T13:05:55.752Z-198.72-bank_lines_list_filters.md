# 198.72 bank lines list filters

## Pivot

HEAD `d7b4319` is a clean residual-audit commit, not a
`(failed on <step>)` backstop. Worktree is clean. Research
brief: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official scratch this slice:
`billy-api-docs-research-iter72.html` and
`billy-api-docs-chunk-iter72.js`.

Live https://www.billy.dk/api/ drifted from the generator lock.
Lock is ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
Live is ETag `pi4s9u10j037qn` / MD5 `d805f3d2bb8e339f7635d6834b4011bd`
(150194 bytes). Chunk
`/_next/static/chunks/pages/api-docs-4eba988e0c6b3605.js`
(MD5 `572a5fb67063b4d5aaa5c7f18f6fe9e7`). Collection `/v2/`
path set still 49. Supports lines identical. Bulk still 46/46
with no body schema. Residual property tables unchanged.
The only contract addition is an official List filters table
on `GET /v2/bankLines`.

Current `api_bank_lines_list` uses shared `BankListRequest`
(page, pageSize, include, unconstrained sort). Inventory
`api.bankLines.list` has `filters: {}`. Official table
requires `accountId` and documents the rest. That is the
slice.

Keep bulk92 and six readonly-map rows toolless. Keep
`complete=false`. Do not finish. No live API. No UI dumps.

## Goal

Land typed offline `api_bank_lines_list` filters from the
live official table, and relock docs fingerprints to the
live page.

| Filter | Type | Tool rule |
| --- | --- | --- |
| `accountId` | string | required, min_length 1 |
| `sortProperty` | enum `entryDate`, `amount` | optional |
| `sortDirection` | enum `ASC`, `DESC` | optional |
| `isReconciled` | boolean | optional |
| `status` | enum `pending`, `booked` | optional; omit when unset (Billy defaults to booked) |
| `side` | enum `debit`, `credit` | optional |
| `externalId` | string | optional, min_length 1 |
| `receiptState` | string | optional, min_length 1; not an exclusive enum |
| `minAmount` / `maxAmount` | number | optional |
| `minEntryDate` / `maxEntryDate` | `YYYY-MM-DD` string | optional |
| `entryDatePeriod` | string | optional; documented period formats |
| `q` | string | optional, min_length 1 |
| `page` / `pageSize` / `include` | existing | keep |

Do not add `isBankAccount` to `api_accounts_list`. That
sentence is a finder hint inside the bankLines `accountId`
description. Accounts still have no List filters table.

## Why no child

File ownership would collide on
`src/billy_mcp/api/bank_reads.py`,
`tests/api/test_bank_reads.py`,
`scripts/generate_coverage_report.py`, and generated
coverage. Seven write children stay completed and merged.
Retired descendants stay unmerged.

## TDD

On HEAD, add failing tests first. Capture the expected
failure. Then implement.

1. `tests/api/test_bank_reads.py`:
   - `api_bank_lines_list` tool properties include required
     `accountId` plus the documented optional filters.
     Other nine bank/balance tools stay on the old surface.
   - Missing `accountId` fails at the model/tool boundary.
   - Undeclared fields still fail (`offset`, `organizationId`,
     `matchId`).
   - `sortProperty` other than `entryDate`/`amount` fails.
   - `status` other than `pending`/`booked` fails.
   - `side` other than `debit`/`credit` fails.
   - Shared `BankListRequest` still rejects `accountId` and
     `q` (other collections must not grow this table).
   - `bank_lines_list` GET `/v2/bankLines` query always
     includes `accountId` and omits unset optionals. No
     `/v2/accounts` request.
   - Existing shared-list query test keeps using
     `BankListRequest` for the four non-bankLines
     collections; bankLines uses `BankLinesListRequest`.
2. `tests/coverage/test_official_docs_lock.py`:
   - generator and generated `official_docs` match live
     `pi4s9u10j037qn` / `d805f3d2bb8e339f7635d6834b4011bd`.
   - Expect fail on HEAD because lock is still
     `tmhc6wpdc835zt` / `053f755f52e3926b028e29325e3670d4`.
3. Coverage: `api.bankLines.list` `filters` is the new map,
   not `{}`. `complete is False`. Bulk92 and six
   readonly-map ids stay toolless.

Run the missing-`accountId` and docs-lock asserts. Expect
fail on HEAD. Then change production code.

## Implement

1. `src/billy_mcp/api/bank_reads.py`:
   - Add `BankLinesListRequest` with `extra=forbid`. Do not
     widen `BankListRequest`.
   - `BankReadService.bank_lines_list` and the FastMCP
     `api_bank_lines_list` wrapper take the new request.
   - Serialize by alias, exclude unset optionals. GET
     `/bankLines` only.
   - Pattern: `invoice_reads.py` list request and
     `geo_reads.py` required `countryId`.
2. `scripts/generate_coverage_report.py`:
   - `DOCS_ETAG` / `DOCS_MD5` and observed pair to
     `pi4s9u10j037qn` / `d805f3d2bb8e339f7635d6834b4011bd`.
   - Add `BANK_LINE_FILTERS` and `LIST_FILTERS["bankLines"]`.
   - Do not bump `complete`. Do not touch bulk freeze or
     residual honesty ids.
3. `uv run python scripts/generate_coverage_report.py`. Do
   not edit `coverage/status.json` or manifests by hand.
4. Wiki `residual_clear_method_closed_inventory_honesty.md`:
   update the official fingerprint sentence only. Residual
   remaining stays 6. Bulk stays 92. No greening.
5. Memory `state.md` / `todo.md` / `decisions.md`: this
   slice is bankLines list filters plus relock; owner input
   still required for bulk92 plus six readonly-map rows;
   `complete` false.

Out of scope this slice: bulk tools, readonly-map
create/update tools, empty payloads, `api_accounts_list`
`isBankAccount`, UI dumps, **Godkend**, annual-report
tools, live API, OpenAPI re-probe on the API host,
`node finish`.

## Verify

Commit-mode tests plus lint/type on the touched files.
No `ui-full`. No live API. Independent review after
EXECUTE.

## Stop line

If a writable official field appears on the six
readonly-map rows, or a bulk body schema appears, stop
and keep those rows red. Do not invent tools for them
in this slice. Keep `complete=false`.

If `accountId` is no longer required on a re-fetch of
the live chunk, stop and re-read the table before
coding. Do not guess.

Keep `96908DC6` and `9B979A05`. Do not finish.

## Post-Mortem

Work completed: typed `api_bank_lines_list` filters with
required `accountId`, docs relock to ETag `pi4s9u10j037qn`
/ MD5 `d805f3d2bb8e339f7635d6834b4011bd`, landed on
`df2f475`. Independent review later PASSed. Residual bulk92
and six readonly-map rows stayed toolless.

Deviations: commit was an auto save before independent
review. Review and owner-input stop ran on the following
slice.

Review findings: PASS. Required fixes none.

Cleanup: no live UI, no frames.

Next unresolved coverage slice: owner scope on bulk92 plus
six `READONLY_PROPERTY_TABLE` rows. `complete` stays false.
