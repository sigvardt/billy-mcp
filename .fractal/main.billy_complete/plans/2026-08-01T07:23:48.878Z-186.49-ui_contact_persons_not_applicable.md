# 186.49 ui contactPersons not_applicable freeze


## Goal

Product research148 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved UI parity rows for
   **contactPersons only** (**7 rows total**):
   - get, list, create, update, delete, bulk_save, bulk_delete
   - (matches inventory shape for full CRUD + bulk; singular delete present)
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so `api.contactPersons.` is included.
3. Updates inventory tests for NA count 86→**93** and live/vision baseline
   128→**135** (`42` shell/parity dual-counts + `93` NA).
4. Adds a scrubbed wiki page for the freeze; one-line cross-link from peer NA
   wiki pages (contactBalancePayments / research144 join NA / geo).
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   this family.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research148).  
Dual evidence (tmp, non-git): `tmp/research148_residual_dual.json`,
`tmp/research148_shell_markers_dual.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–147).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–40 geo/currencies; 186.43–44 account natures/groups;
186.45 contactBalancePostings + invoiceLateFees + invoiceReminderAssociations;
186.48 contactBalancePayments; 186.46–47 dual-count accounts.list /
organizations.list (shells stay at 42).

## Why this slice

1. research147 greened contactBalancePayments NA only; deferred multi-surface
   dual-counts and residual soft-empty families with related greened shells.
2. research148 dual-probed residual soft seeds after 186.48: soft
   `/contactPersons` (and kebab/DA seeds) body_len **127**, empty h1, dual-agree
   identical to nonsense `zz-research148-no-such-route`.
3. Contrast: dual `ui_clients_list` READY as **contacts/Kunder** shell
   (`/:org_slug/clients`, h1 Kunder); shell-marker dual on `/clients` shows
   **Kontaktperson / person markers false** (kunde/faktura true). That shell is
   not a contactPersons API workflow.
4. Soft URL landing is not a productable person list/create shell — do not invent
   UI tools.
5. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class as
   research144–147 join/meta packages).
6. Explicit rejections/deferrals stay red this slice:
   - invoiceReminders (secondary NA ready; **out of this bounded package**)
   - specials delivery/logs/user_orgs (settings Levering ambiguity)
   - attachments pure NA (Bilag greened, bilag/upload markers dual true)
   - taxRateDeductionComponents / taxRates (Momssatser multi-resource)
   - productPrices (`/products/new` non-empty dual)
   - bankLine*/bankPayments (bank shells greened)
   - daybook* (daybooks editor greened; list dual-count weak)
   - postings (no steal from transactions.list)
   - files (Bilag weak map)

## Non-goals

- Shipping any new FastMCP tools (`ui_contact_persons_*`).
- Greening soft-empty paths as list shells.
- Dual-counting contactPersons onto `ui_clients_list` (different resource;
   person markers dual false → NA, not dual-count).
- Greening invoiceReminders / specials / attachments / taxRate* this slice.
- Dual-counting daybooks.list / taxRates / files / bankLines / postings /
  productPrices this slice.
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Changing offline API contactPersons tools (API lane separate; live_tested
  stays false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.contactPersons.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 93` (prior 86 + **7** ops). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH148_RESOURCES = frozenset({"contactPersons"})`
  (mirror research147 set). Prefer **same evidence code**
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. Update header
  comment that research148 adds contactPersons (7 ops). |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research148 family: evidence_ref
  `research148_contact_persons_dual`; nonsense path
  `zz-research148-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_contactPersons`; contrast controls include
  `ui_clients_list` (Kunder), and other greened dual-ok tools from research148
  (invoices, uploads, settings). Keep research138–147 branches intact. Order
  branch checks so research148 is evaluated before older catch-alls
  (same pattern as 147→144). |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefix alone should wire all
  **7** parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count uses `GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **93**.
  - research148 evidence_ref branch assertions (mirror research147).
  - live/vision `42 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **135** when
    ROW_COUNT=93.
  - Assert seven `api.contactPersons.*` UI parity rows are NA green.
  - annual still NA-rejected red; bulk 92 red; no new tools for this family;
    invoiceReminders/attachments/productPrices/bankLine*/bankPayments/
    salesTax*/postings/daybooks/specials parity still red where intended
    (not greened by this package).
  - Keep contactBalancePayments research147 asserts intact. |
| Wiki | New `wiki/ui_contact_persons_not_applicable.md` (scrubbed) + sorted
  `_index` link. Cross-link from
  `wiki/ui_contact_balance_payments_not_applicable.md` and/or
  `wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`
  (peer join/meta NA). |
| Coverage regenerate | Run generator → refresh `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md`. implemented/contract may move
  with UI NA greening (historical pattern: implemented/contract rose with NA
  packages). complete=false; API live_tested false. |

## EXECUTE steps (ordered)

1. Patch `scripts/generate_coverage_report.py` constants + research148 branch in
   applicator (prefix, ROW_COUNT 93, RESEARCH148 set, evidence_ref, contrast).
2. Patch `tests/coverage/test_coverage_inventory.py` for 93 / 135 / seven-row
   contactPersons asserts + research148 evidence_ref; keep older package tests.
3. Add wiki freeze page + `_index` + peer cross-link(s).
4. Regenerate coverage artifacts via project generator/check scripts as used in
   186.48 (do not hand-edit YAML status).
5. Run lint + offline `test.sh` (no live API; live UI suite not required for this
   inventory-only freeze before IR, matching 186.48 pattern).
6. Leave residual dual JSON in owner tmp only (not commit).

## Acceptance

- Seven `ui.parity.contactPersons.*` rows: discovered/implemented/contract_tested/
  live_tested/vision_verified all true; `parity_status=not_applicable`; empty
  tool_name; evidence_code GEO_UI_NO_EQUIVALENT_WORKFLOW; evidence_ref contains
  research148; not_applicable_decision accepted.
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 93`; live/vision status 135.
- No new ui_* tools; no contactPersons dual-count onto clients.
- invoiceReminders / specials / attachments / bank* / productPrices / daybooks /
  taxRates residual red as intended.
- complete=false; bulk 92 red; annual red; API live_tested false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Risks

| Risk | Mitigation |
| --- | --- |
| Dual-count onto clients by mistake | Assert empty tool_name + not_applicable; clients shell stays contacts.list only |
| Greening invoiceReminders with contactPersons | Explicit non-goal; assert invoiceReminders still red |
| ROW_COUNT drift | Single constant drives tests + status formula |
| Shell soft-empty alone without marker isolation | research148 shell-markers dual already proves person markers false on /clients |

## Post-mortem

- **Outcome:** Producted research148 contactPersons GEO NA freeze (7 ops).
  IR **ACCEPT** with no required fixes. Offline suite and lint green.
- **Actual live/vision:** 128 → **135** (42 shells + 93 NA).
- **Actual NA count:** 86 → **93**.
- **Actual implemented/contract:** 312 → **319**.
- **complete:** still **false** (expected).
- **Tests:** lint.sh pass; test.sh offline **1565 passed**, 35 deselected.
- **Deviations:** none vs plan. Optional IR N1/N2 deferred (probe shell_paths
  TargetClosedError vs soft+shell dual authority; tmp dual JSON not committed).
- **Cleanup:** no disposable Billy records (observe-only research). Profiles
  purged during research. No credentials/frames in repo.
- **Next unresolved coverage slice:** residual UI parity — prefer
  invoiceReminders secondary NA (research148 ready) or dual-count/isolation
  work on taxRates/files/bankLines/postings/productPrices/daybooks; bulk 92
  external-contract and annual_reports org_inaccessible remain red.
- **Not done:** node finish; complete true; live API; invent UI tools.

