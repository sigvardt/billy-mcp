# 190.14 Bind bill vendor typeahead

## Goal

Make one tagged draft-bill create stick through `create_server`
`call_tool` by binding the live vendor typeahead. Then complete
update/delete with independent second-session read-back and a third
session for absence. Coverage stays red.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`,
`31F6E753`, `F3F318A2`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Live API token calls, SPA `POST /v2/contacts` seed, docs re-lock
- Seeding vendor via `ui_clients_create` (that is a customer)
- Repeating fill-and-click on `input[name='vendor']` alone
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `652a1dc`. Last commit is 190.13 IR contract fixes. Not a
backstop. Memory is dirty from RESEARCH. `complete=false`. Honesty 16
stay red. Bills list is empty. Login reaches READY.

Live blocker:
`UI_CHANGED` field `vendor` selector `input[name='vendor']`.

Cited contract (research):

- Official bill belongs-to is `contact` / `contactId`
- UI label is **Leverandør**
- Invoice typeahead uses `input[name='contact']`
- Open-shell never asserted `input[name='vendor']`
- Create-supplier chrome is **Opret leverandør**, not only `Opret "{tag}"`
- `ui_clients_create` makes a Kunde, not a Leverandør

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/bills_form.py`
- `tests/unit/test_ui_bills_writes.py`
- `tests/live/test_ui_bills_writes.py`
- `wiki/ui_bills_writes.md`

Parked write-family children stay parked.

`728BD2E4` child split was for family implementation. Those seven
families are already merged. A vendor bind is not a new family.

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty 16 still passes. First
   product edit is failing unit tests in `test_ui_bills_writes.py`.
2. **Vendor control is observed, not guessed.** Candidates: `contact`,
   `contactId`, combobox next to **Leverandør**. The first live isolate
   dumps non-PII input names to the owner-only inspect file, then binds
   that control.
3. **Supplier via typeahead.** Click exact tag if listed, else
   **Opret leverandør** and/or `Opret "{tag}"`, then modal **Gem**.
   Never Tab off an unbound vendor. Never pre-create a customer.
4. **`31F6E753` dump before click.** Unique tag, bound vendor, date,
   line amount, **Gem som kladde**. No submit until that dump exists.
5. **Cleanup reverse.** Delete the draft bill, then the supplier
   created through the typeahead. Third session proves both gone.
6. **Vision.** `author=live_test`, `reviewer_verdict=pending_review`
   only.

## Execute

1. Run honesty 16. It must stay length 16 and red.
2. Add failing unit tests:
   - `_fill_vendor` / selector helper accepts `contact` / `contactId` /
     label **Leverandør**, not `vendor` alone.
   - Create-option matcher accepts **Opret leverandør** as well as
     `Opret "{tag}"`.
   - Live test no longer calls `ui_clients_create_*`.
3. Run those tests. Capture the expected failure.
4. Smallest product change in `bills_form.py` so those tests pass.
5. Strip the customer seed from `tests/live/test_ui_bills_writes.py`.
   Before retry, delete leftover `MCP-UI-B-*` customers through the UI
   if a fresh session still shows them.
6. Run the one live FastMCP bills CUD. First isolate: dump vendor
   chrome, bind it, dump `31F6E753` fields, then **Gem som kladde**.
   Prove POST persist, tagged list read-back, `{tag}-U` PUT, delete,
   third-session absence, supplier gone.
7. If date or amount then returns `UI_CHANGED`, bind the named field
   the same way. Do not guess `billDate` / `inclVatAmount` blindly.
8. Offline `test.sh`. Keep coverage red.

## Done when

- Honesty 16 still red, length 16
- Unit tests for vendor selectors and **Opret leverandør** pass
- Live FastMCP create/update/delete of one tagged draft bill passes
  with independent read-back
- Pre-submit dump has tag, vendor, date, amount, draft CTA
- No leftover tagged bill or supplier
- Vision record is `live_test` / `pending_review` only
- `complete=false`. Bills `tool_name` still `ui_bills_*_open`

## After this slice (not now)

Independent vision accept + purge, then remap only the three bills CUD
rows.

## Post-Mortem

Live FastMCP draft bills CUD passed once through `create_server`
`call_tool`. Vendor binds via the **Leverandør** label. IR **FAIL**:
create pre-submit dump was one file and got overwritten; vendor bind
accepted a typed string; vision `run_id` was not the frame folder.

FIX-VERIFY: create dump is now a separate owner-only file; typed-only
vendor bind is refused; live vision `run_id` is the frame-dir suffix;
create-form frame path is set; cleanup also checks the suppliers list.
Honesty 16 stay red. `complete=false`. No remap. No accept. No finish.

Offline `test.sh` 1855 passed, 60 deselected. Next unresolved slice:
re-run live CUD under the stricter bind, then independent accept +
purge, then remap only the three bills rows.

