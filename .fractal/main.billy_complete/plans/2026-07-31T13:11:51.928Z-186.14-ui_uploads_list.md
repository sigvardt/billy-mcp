# 186.14 UI uploads list shell

## Goal

Ship the next **read-only list-shell-open** business UI FastMCP tool for Billy
uploads (Bilag / attachments inbox shell), frozen in research113, with offline
contract tests, dual-session live interface tests (no API token), and a
non-sensitive vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research113).  
Discovery: `.fractal/main.billy_complete/tmp/discovery113/summary.json` dual
session seed `uploads`; frames purged; summary PII scrubbed;
`api_token_used: false`; `writes_attempted: false`.  
Prior product patterns: tip `ca651f5` (`ui_creditor_balances_list`), products
import (file-control presence without activate).  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
(Uploads and receipt inbox stay red until qualified; file upload roots outside
git).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–113).

## Why this slice

- Auth lane plus list/import shells through creditor balances are producted
  (UI live/vision 15).
- Research113 dual headless sessions both reached the uploads shell:
  path class `/:org_slug/uploads?drawerMode&type` (product path class
  `/:org_slug/uploads` with query allowed), h1 `Bilag`, CTA `Upload filer`
  present (presence only); A↔B path/h1/title/CTA match; `file_input_count` 2
  observed both sessions; no error shell; no plan-gate heuristic.
- Official docs have **no** `/v2/uploads` resource. Related offline API
  families (`files`, special binary `files_upload`, `attachments`) stay
  offline-contract only; do **not** invent `api_uploads_*` / `api_bilag_*`.
- Inventory target still red: `ui.discovery.uploads`.
- This product greens **shell open only** for that discovery row (bank_accounts /
  debtor_balances / creditor_balances pattern: discovery-only, no forced false
  API-parity list row). Do not green files/attachments parity or receipt_inbox.
- Alias seeds (`upload`, `bilag`, `files`, `inbox`, `attachments`, `receipts`,
  …) resolve to empty h1 or `/:org_slug/:id` — product navigates only
  `/uploads`.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Inventing `api_uploads_*` / `api_bilag_*` or greening bulk residual API rows.
- File pick, multipart submit, or any write via `input[type=file]`.
- Clicking `Upload filer`, `Udfyld alle`, or `Kom i gang`.
- Greening `ui.parity.files.*`, `ui.parity.attachments.*`,
  `ui.parity.special.files_upload`.
- Greening `ui.discovery.receipt_inbox` (aliases failed in research113; separate
  freeze).
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; new login-only `ui_*` tools.
- Setting `complete: true` or greening all 339 UI rows.
- Headed browsers, generic browser MCP tools, credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).

## Ownership

**Root Grok only. No children.** File cluster mirrors creditor balances list:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_uploads_list.py` (new; pattern from creditor + products import hard-ban on file activate) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for discovery.uploads |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_uploads_list_shell_evidence` for **`ui.discovery.uploads`**
  only (`list_shell_open_only`) |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — append live test path to existing
  `mit.billy.dk` `test_references` only; do not widen hosts |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_uploads_list_shell.md` after product |

Do **not** spawn residual/bulk, receipt_inbox, files-parity, or settings
children this iteration.

## Contract freeze (research113)

### Tool

- **Name:** `ui_uploads_list`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  filter, credential, local file path, digest, or destination fields.
- **Success:** non-PII only:
  - `path_class`: literal `/:org_slug/uploads`
  - `heading`: literal `Bilag`
  - `upload_action_visible`: bool for exact control label **`Upload filer`**
    (presence only; never click; never set file inputs)
  - `shell_markers_present`: bool for ≥1 shared shell marker
    (Overblik / Fakturering / Fakturaer / Udgifter / Menu / Bank / Køb /
    Leverandører / Bilag) without dumping page text
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` (error shell, wrong path, missing h1 /
  required control), `EGRESS_DENIED`, `BILLY_ERROR`.
  Messages must not include org slug, org name, email, password, tokens,
  filenames, or attachment content.
- **Navigation:**
  1. Open MCP profile under fixed egress (`coverage/browser_egress.yaml`).
  2. If login signature present → `AUTH_REQUIRED` (callers use auth tools first).
  3. Resolve org slug from session URL or outside-git identity file only.
  4. Goto `https://mit.billy.dk/{slug}/uploads` (same wait policy as
     creditor/debtor list shells).
  5. Classify with path regex `^/[^/]+/uploads$` (query params allowed on real
     URL; path class remains `/:org_slug/uploads`). Billy may append
     `drawerMode` / `type` query keys (values ignored).
  6. Signature: h1 text `Bilag` and visible `Upload filer`.
  7. Reject alias paths (`upload`, `bilag`, `files`, `inbox`, `attachments`,
     `receipts`, `receipt-inbox`, `receipt_inbox`, `documents`) as
     `UI_CHANGED` if somehow navigated for this list tool.
- **Hard bans:** never click Upload filer / Udfyld alle / Kom i gang; never set
  `input[type=file]`; never call API upload tools from this observation.
- **Session:** no API token; no screenshot persistence in production tool path;
  live test captures frames only outside git for vision then purges.

### Inventory rows

Primary only: `ui.discovery.uploads`  
No shell-open API parity row (no official `/v2/uploads`; bank_accounts /
debtor_balances / creditor_balances pattern).

| Flag | Target after product |
| --- | --- |
| discovered | true |
| implemented | true |
| contract_tested | true |
| live_tested | true |
| vision_verified | true |
| tool_name | `ui_uploads_list` |
| method_or_route | `mit.billy.dk /:org_slug/uploads` (list shell open only) |
| parity_status | `list_shell_open_only` |
| response_fields | path_class, heading, upload_action_visible, shell_markers_present |
| cleanup | not_applicable; read-only observation creates no records |
| side_effects | none |
| evidence | research113 dual-session + product; no invent upload API; no file pick |

Do not set `vision_evidence` to embed frame bytes. Durable vision record lives
under `tmp/vision-records/ui_uploads_list.json` (gitignored/tmp) with
`purge_verified: true`.

Do **not** green `ui.discovery.receipt_inbox` or any
`ui.parity.files*` / `attachments*` / `special.files_upload` rows. Do not
re-touch already-green discovery rows beyond incidental shared helpers if
required.

## Work sequence (EXECUTE)

1. **Models** — `UiUploadsListInput`, `UiUploadsListSuccess` in `models.py`
   (mirror `UiCreditorBalancesList*`; field `upload_action_visible` not
   `create_action_visible`).
2. **Browser** — path helper (`^/[^/]+/uploads$` + query allow), signature
   helper (h1 `Bilag` + visible `Upload filer`), `UI_CHANGED` factory,
   `UiUploadsListService` protocol, `BrowserRuntime.ui_uploads_list`. Hard-ban
   file input set and write CTAs (products-import discipline).
3. **Server** — register `ui_uploads_list` with typed empty input binding like
   other UI tools.
4. **Offline tests** — model contract (no extra fields; success literals;
   reject file_path/digest inputs); browser unit tests for path accept/reject
   (including alias reject), AUTH_REQUIRED, UI_CHANGED, success mapping with
   fakes; server registration name and call path.
5. **Coverage** — extend generator apply helper + inventory gate map for
   discovery.uploads only; regenerate report; assert UI live/vision 15→16;
   API live stays 0; `complete` stays false.
6. **Egress** — append `tests/live/test_ui_uploads_list.py` to mit.billy.dk
   test_references only.
7. **Live test** — dual ephemeral profiles; login READY; dual `ui_uploads_list`;
   assert path/heading/upload_action flags; capture frames outside repo; write
   vision record; purge frames; mark purge_verified; never set BILLY_API_TOKEN;
   never set file inputs.
8. **Wiki** — `wiki/ui_uploads_list_shell.md` (contract + non-claims).
9. **Lint/type/unit/contract/safety** before claiming FIX-VERIFY ready;
   live+vision before completeness of this row.

## Independent review criteria (REVIEW)

ACCEPT product only if all hold:

- Typed tool exists; no generic browser/HTTP exposure.
- Empty input; success fields match freeze; no PII in models.
- Unit/contract coverage for path signature and error codes (including alias
  reject for upload/bilag/files/inbox/receipts variants).
- Live dual session without API token; independent second profile read-back.
- Vision record non-sensitive with purge_verified; no frames in repo.
- Only `ui.discovery.uploads` greened for this shell; no invent upload API; no
  greening of receipt_inbox, files/attachments parity, or re-scope prior shells.
- `coverage/status.json` complete remains false; API live_tested remain false.
- No credentials, tokens, org slugs, filenames, or attachment content in
  committed files.
- Hard-ban evidence: product path never sets `input[type=file]` or clicks
  upload/fill/start CTAs.

## Feasibility

- Budget: no limit (uncapped run). Slice is one list shell matching creditor.
- Blockers: none known; credentials via keyring outside git; discovery113 READY.
- Children: none (clear single-owner file cluster).
- Risks:
  - Query `drawerMode`/`type` may appear on real URL — path class without query;
    accept query like products import.
  - `file_input_count == 2` is observation only; do not require count in success
    model unless needed for signature harden (prefer button + h1 only).
  - Shared CTAs / empty list (no HTML table) — do not require table for shell
    pass (research113: table_count 0).
  - Multi-h1 possible — use first matching h1 text if multiple.
  - English locale would change heading/CTA; treat as UI_CHANGED until re-frozen
    (current test org Danish).
  - Org/user menu button text can leak PII in raw DOM — product returns no
    button samples.

## Done when (this iteration product only)

- `ui_uploads_list` implemented and registered.
- Offline suite green for new unit/contract paths.
- Live dual + vision purge_verified for this row.
- `ui.discovery.uploads` all five flags true; status live/vision 15→16;
  complete false.
- Wiki shell page present.
- Ready for COMMIT after FIX-VERIFY / REVIEW ACCEPT.

## Post-Mortem

### Work completed

- Research113 dual headless freeze for uploads (Bilag) list shell; plan 186.14.
- Implemented `ui_uploads_list` (models, browser, server, offline + live tests,
  coverage generator evidence for `ui.discovery.uploads` only, wiki shell page).
- IR ACCEPT product; completeness FAIL as expected.
- FIX-VERIFY: restored `coverage/browser_egress.yaml` test_references for
  `tests/live/test_ui_uploads_list.py` on `mit.billy.dk` and
  `api.billysbilling.com` (path-scoped auth XHR), matching prior list shells.

### Deviations

- Root-only execution (no children), matching plan ownership.
- Discovery-only green (no forced files/attachments/files_upload parity),
  matching bank_accounts/debtor pattern and research113 non-claims.
- Egress live-test reference was missing after EXECUTE and was fixed in
  FIX-VERIFY (IR required housekeeping).

### Review findings

- Product ACCEPT; required egress test_references fix applied.
- Optional non-blocking: EN locale would need re-freeze.

### Test results

- `bash .fractal/main.billy_complete/scripts/lint.sh`: pass (pre-COMMIT).
- `bash .fractal/main.billy_complete/scripts/test.sh` (offline): **1400 passed**
  class at EXECUTE; reconfirm in FIX-VERIFY.
- Live: `tests/live/test_ui_uploads_list.py` **1 passed** (EXECUTE + reconfirm).
- Vision `purge_verified: true`.
- Did not run `BILLY_TEST_MODE=full` / `ui-full` completeness gate: node not complete.

### Cleanup state

- No business records created (read-only shell open).
- Ephemeral live profiles removed by test teardown.
- Frames purged; durable vision record non-sensitive only.

### Next unresolved coverage slice

- `ui.discovery.receipt_inbox` (aliases failed in research113; menu/deep-link freeze).
- Then remaining discovery families; residual bulk API offline reds.
