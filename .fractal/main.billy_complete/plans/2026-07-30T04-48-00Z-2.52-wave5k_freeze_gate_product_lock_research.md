# Wave-5k freeze-gate + product lock research (research52)

## Goal

Reconfirm official Billy API v2 contract stability and hand the accepted
Wave-5k bankPayments product recipe to the Codex Power implementation leaf.

## Findings (authoritative for next implementer)

- Docs unchanged: ETag `hsisik4g9p3603`, MD5 `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes.
- bankPayments POST/PUT unauth **401**; DELETE **405** with message that singular delete is unsupported (API body uses backticks around `bankPayments`).
- Root freeze page is content-match and independently **ACCEPTED** at
  `wiki/wave_fivek_freeze_independent_review.md`.
- Product module absent; next offline greens are create+update only (**166 → 168**, tools **238 → 242**).
- Full cited brief: `.fractal/main.billy_complete/tmp/grok-research.md`.

## Next steps (not this research step)

1. Codex Power product leaf: four tools in `bank_payment_writes.py` only.
2. Independent product review after the product merge; then Wave-5l
   (`salesTaxPayments`) research.

## Explicit non-claims

No coverage greens, no live/UI claims, no product code, no freeze ACCEPT.

## Execution plan

1. Preserve the cited research52 handoff and its fail-closed coverage state;
   no additional contract research is needed while the official fingerprint is
   unchanged.
2. In the next PREPARE pass, review and merge the completed Codex fallback
   review commit `be6976d` as a supplemental wiki record only. Verify the wiki
   index after the merge. It must not alter coverage, register tools, or unlock
   implementation.
3. The independent Grok freeze ACCEPT is now present. Spawn a focused Codex
   Power leaf owning `src/billy_mcp/api/bank_payment_writes.py`, its tests, and
   the corresponding `server.py` registration. It may green only create and
   update after real implementation and contract tests; it must leave delete
   and bulk red.
4. After the product leaf is merged and independently reviewed, continue with
   the next cited freeze research. Live, UI, vision, bulk, and completeness
   remain out of scope for this product slice.
