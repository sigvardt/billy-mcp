# 198.53 Promote 11 accepted honesty rows

## Pivot

Owner `58D7D0E1`: honesty-16 is a false-green stop, not a
permanent-red rule. Invoice and product are accept+purged, with
contacts, bills, and organization. HEAD `16b0c8c` is a clean
198.52 close, not a `(failed on <step>)` backstop. Memory edits
from SYNC are the only dirty files.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn. Do not remake dumps
or live CUD (`D68E402A`, `A337A622`). Do not finish.

`EE0A0F1B` still binds: open-only `parity_status` or `*_open` /
`*_list` tool can never green. The 11 already name preview tools
and have qualifying vision records. Honesty still forces the
three flags false and requires open-only status.

## Goal

Turn true only these 11 rows, from evidence, not by hand-editing
`coverage/status.json`:

- contacts C/U/D
- bills C/U/D
- organizations update
- invoices C/U/D
- products create

Keep red: files create, daybooks create/delete,
daybookTransactions create, transactions create.

`complete` stays false.

## Why no child

Generator, coverage fixtures, `organizations.py` literals, wiki
residual table, and memory are one root-owned slice. A child
would fork overlapping files. The seven write children stay
completed and merged.

## TDD

1. New fixture
   `tests/coverage/test_ui_cud_proved_honesty_promote.py`.
   On HEAD it must fail. Assert:
   - the 11 have preview `tool_name`, registered `_execute`
     twin, `parity_status == preview_execute`, and
     `implemented=live_tested=vision_verified=true`
   - each mapped `tmp/vision-records/*.json` qualifies
     (`author=independent_review`, accept, `purge_verified`)
   - the five stay open-only and red
   - `complete` is false
   Run it. Capture the fail.

2. Org registrar bug (same fail if left):
   `src/billy_mcp/ui_writes/organizations.py` uses
   `name=PREVIEW_TOOL_NAME`. Change `server.tool(name=...)` to
   string literals `ui_organizations_update_preview` and
   `ui_organizations_update_execute` so
   `registered_domain_tools` sees the twin.

3. Smallest generator change after the existing remaps in
   `generate_ui_inventory`:
   - Map the 11 ids to family vision-record paths and preview
     tools (contacts/bills/invoices family files cover CUD
     siblings).
   - Load each record. Require
     `qualifies_for_coverage_vision`.
   - Set `parity_status` to `preview_execute`.
   - Set the three flags true. Cite the record path in
     `evidence`. Leave `vision_evidence` null.
   - Shrink `apply_ui_cud_parity_open_only_honesty` to the
     remaining 5. Keep requiring open-only status on those 5.
   - Do not change discovery/get-open tool names.

4. Update assertions that still require the 11 to stay red:
   `test_coverage_inventory.py` (328/512/honesty branch,
   products.create `live_tested is False` /
   `form_open_only`),
   `test_products_create_honesty_row_names_preview_and_stays_red`.
   Grep for the other 10 ids. Keep
   `test_ui_open_only_cannot_complete_cud_parity.py`.
   Expect `live_tested_rows` and `vision_verified_rows` **339**,
   `implemented_rows` **523**.

5. Regenerate with
   `python scripts/generate_coverage_report.py`.
   Do not hand-edit `coverage/status.json`.

6. Wiki residual table: move the 11 to a proved category.
   Five stay fail-closed / prohibited. `complete` false.

7. Offline unit/contract/coverage on the touched files.
   Do not run live UI. Do not set `BILLY_API_TOKEN`.

## Out of scope

- Live write or UI dump
- Files, daybook, **Godkend**
- Product update tools
- Official-docs re-lock
- `complete: true` or `fractal node finish`

## Done when

- The 11 rows are `preview_execute` with the three flags true
- The five honesty rows stay red and open-only
- Org preview/execute names are string literals
- Coverage is regenerated. `complete` is false
- Offline coverage fixtures pass. Stay incomplete. Not finish

## Post-Mortem

Work completed: failing fixture
`tests/coverage/test_ui_cud_proved_honesty_promote.py`, then
generator promote of the 11 accepted CUD rows to
`preview_execute`. Honesty apply-set is the five files and
ledger rows. Org preview/execute names are string literals.
Coverage regenerated. Residual table updated.

Deviations: none. Independent review required no fix. Stale
remap comments that still said honesty forces red were corrected
in FIX-VERIFY.

Review: IR PASS. `C7DBE974` lock still includes products. Frame
dirs gone. `complete=false`.

Tests: lint passed. Commit-mode 2117 passed, 83 deselected.
Did not run `ui-full` or live API.

Cleanup: no new live records. Tagged product and invoice CUD
already deleted. Org phone restore already proved.

Next unresolved: files create (`UI_CHANGED`, no UI delete),
daybooks create/delete (`unique_persist_token=none`),
daybookTransactions create and transactions create (**Godkend**,
prohibited). Bulk92 and API live stay out of scope. Do not
finish.

