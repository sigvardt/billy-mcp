# 186.62 ui suppliers create open form_open discovery

## Goal

Product research161 ACCEPT:

1. New read-only FastMCP UI tool **`ui_suppliers_create_open`** that opens the
   Billy **supplier contact create form** as form open only.
2. Green discovery row **`ui.discovery.suppliers_create`** (new
   `UI_DISCOVERY_FAMILIES` entry `suppliers_create`; do **not** repurpose existing
   `ui.discovery.suppliers` list shell on `ui_suppliers_list`).
3. **Do not** re-green or re-dual-count `ui.parity.contacts.create` /
   `api.contacts.create` (already mapped via `ui_clients_create_open` in 186.61).
   Suppliers are contacts with isSupplier in the same create dialog family.

Target math (from tip `0b28bb1` / product 186.61): live/vision **156 → 157**
(discovery only); implemented/contract **340 → 341**; GEO NA **100** unchanged;
complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research161)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. |
| Soft paths | `emails` / productPrices / bankLines / postings / files / salesTax* soft-empty body **127** dual. |
| Suppliers create dual | List `/:org_slug/suppliers` + text CTA **Opret kontakt** opens dialog form dual (`research161_focus_dual.json`): `name`, `registrationNo`, `person_firstName`, `person_lastName`, **17** visible inputs both sessions; `form_open_ready` true. |
| Soft `/suppliers/new` / soft contacts paths | Not required; dialog-on-list is the proved path (mirror clients create). |
| Tool gap | `ui_suppliers_list` is list-only (h1 Leverandører, create_action_visible, never opens form). |
| Isolation | Create form on suppliers list ≠ clients create tool; ≠ special invoice_email; ≠ contacts.get/update/delete. Same API create already greened — discovery only. |
| Deferred | special.invoice_email (no client list marker / no invoice rows); products.create (no CTA/fields; soft /new 0 fields); quotes create (dialog 0 inputs); nested salesTaxRules/payments steal rejected; bulk 92 + annual red. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research161_residual_dual.json`, `tmp/research161_focus_dual.json`.

## Non-goals

- Re-greening `ui.parity.contacts.create` or any second dual-count of
  `api.contacts.create`
- Greening `special.invoice_email`, products.create, quotes create,
  contacts.get/update/delete/bulk_*, salesTaxRules.*, salesTaxPayments.*,
  postings.*, bankLines.*, productPrices.*, annual_reports, bulk 92
- Dual-counting create onto `ui_suppliers_list`
- Submitting the form (Gem / Opret / Save / Create) or filling disposable data in
  the product runtime path
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery only; no parity re-count) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research161)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_suppliers_create_open` |
| lane | ui |
| kind | form_open_only (read-only observe) |
| shell_kind | `suppliers_create` |
| success model | `UiSuppliersCreateOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools; no re-map of contacts.create |

### Success model fields (non-PII)

Mirror clients create-open strictness (`extra=forbid`, no org_slug/url/selector secrets):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/suppliers` (form is a **dialog on list path**) |
| `heading` | Literal `Leverandører` (list h1 retained under dialog) |
| `shell_kind` | Literal `suppliers_create` |
| `create_dialog_open` | bool — create dialog/form surface present after CTA |
| `name_field_visible` | bool — input `name` visible in form scope |
| `registration_no_field_present` | bool — input `registrationNo` present |
| `address_or_person_fields_present` | bool — at least one of street / person_email / person_firstName / person_lastName present |
| `shell_markers_present` | bool — aggregate true when dialog form signature holds |

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. **Required path** (research161 focus dual-proved): open
   `https://mit.billy.dk/{slug}/suppliers`, then observe-only click text CTA
   **Opret kontakt** (prefer exact text match; do not rely on bare nav "Opret"
   alone if ambiguous).
4. Success when:
   - path class remains list `/:org_slug/suppliers` (dialog overlay)
   - create dialog open with form signature:
     - `name` field visible
     - `registrationNo` present
     - address or person field class present (street and/or person_*)
   - shell_markers_present true
5. Close with Escape only after classify (no submit).
6. Never click Gem / Opret (submit) / Save / Create / Slet after form open;
   never fill and save disposable contacts in product path.

### Reject as success

| Failure | Code class |
| --- | --- |
| Soft empty / wrong list / no dialog form signature | `UI_CHANGED` (or project equivalent) |
| Auth not READY / interaction required | existing auth codes |
| Egress violation | `EGRESS_DENIED` |
| Soft `/suppliers/new` chrome-only without form signature | not success |

### Coverage generator

1. Add `suppliers_create` to `UI_DISCOVERY_FAMILIES`.
2. Apply shell evidence for `ui.discovery.suppliers_create`:
   - five flags true (discovered, implemented, contract_tested, live_tested,
     vision_verified)
   - `tool_name=ui_suppliers_create_open`
   - `parity_status` / status = `form_open_only`
   - evidence cites research161 focus dual + live test reference
3. **Do not** call any `parity_of_api_create` path for contacts.create.
4. Assert isolation in inventory tests:
   - `ui_suppliers_list` remains list_shell_open_only
   - `ui.parity.contacts.create` remains on `ui_clients_create_open`
   - `ui.parity.special.invoice_email` stays red
   - products.create residual red
   - bulk 92 external-contract; annual org_inaccessible
5. Math after regen: live/vision **157**; implemented/contract **341**;
   complete **false**; API live_tested false + out_of_scope_by_user.

### Files (expected touch set)

| Path | Change |
| --- | --- |
| `src/billy_mcp/models.py` | `UiSuppliersCreateOpenSuccess` (+ empty input if pattern requires) |
| `src/billy_mcp/browser.py` | `ui_suppliers_create_open` + CTA/form helpers (mirror clients create) |
| `src/billy_mcp/server.py` | Register tool |
| `scripts/generate_coverage_report.py` | discovery family + apply evidence; inventory asserts |
| `tests/unit/test_browser.py` (or existing unit homes) | path/CTA/form/never-submit |
| `tests/test_models.py` | model shape |
| `tests/live/test_ui_suppliers_create_open.py` | dual profile live + vision purge pattern |
| inventory/coverage tests | discovery green; math 157/341; residuals red |
| `wiki/ui_suppliers_create_open_shell.md` + `_index` | non-sensitive shell note |
| regenerate `coverage/*` | status + manifests |

### Live test contract

- Dual ephemeral profiles; independent second-interface read-back = full second
  login session.
- DOM asserts: path class suppliers, heading Leverandører, dialog form flags.
- Vision record outside repo; purge_verified true; no frames in git.
- Skip if browser credential refs missing; never use `BILLY_API_TOKEN`.
- Never submit create form.

### Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Registered `ui_suppliers_create_open` only; typed success/error |
| Discovery | `ui.discovery.suppliers_create` five flags true |
| Parity | `ui.parity.contacts.create` still clients tool only (no suppliers re-count) |
| Isolation | list stays on `ui_suppliers_list`; invoice_email + products.create red |
| CTA | No Gem/Opret-submit/Save after open |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **157**; implemented/contract **341**; GEO NA **100**; complete **false** |
| Docs | Official docs fingerprint unchanged; no bulk greening |

## Residual after this product (stay red)

- `ui.parity.special.invoice_email`
- contacts.get/update/delete (+ bulk)
- products.create / productPrices.*
- quotes create (interface-only; no dual form fields yet)
- nested salesTaxRules / taxRateDeductionComponents / residual salesTaxRulesets ops
- salesTaxPayments / salesTaxAccounts / salesTaxMetaFields (no steal)
- bankLines / postings steal candidates
- bulk 92 external-contract
- `ui.discovery.annual_reports` org_inaccessible

## Risk notes

- Dialog may not use `role=dialog` alone; inventory by named inputs (`name`,
  `registrationNo`, person_*) after **Opret kontakt** click, matching
  research161 focus dual.
- Bare "Opret" can hit nav chrome; prefer text **Opret kontakt** as research161.
- Product runtime must never submit.
- Do not claim a second API dual-count for contacts.create.

## EXECUTE steps (ordered)

1. Models: `UiSuppliersCreateOpenSuccess` (+ input empty model if peers use one).
2. Browser: mirror `ui_clients_create_open` with suppliers path/heading/shell_kind.
3. Server registration + unit tests (path, CTA, form signature, never-submit).
4. Live test `tests/live/test_ui_suppliers_create_open.py` dual ephemeral profiles.
5. Coverage generator: `suppliers_create` discovery family + apply evidence
   (parity_of_api_create **False**).
6. Inventory tests: discovery green; live/vision 156→157; residuals red;
   contacts.create still clients.
7. Regenerate coverage manifests.
8. Wiki `ui_suppliers_create_open_shell` + `_index`; non-sensitive; cite
   research161 markers class only; isolation note vs clients create.
9. Offline gate: `scripts/lint.sh`; offline `scripts/test.sh`.
10. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.



## Post-Mortem

- Outcome: **DELIVERED** product `ui_suppliers_create_open` form_open_only + discovery
  `suppliers_create` (research161 / IR ACCEPT). No `contacts.create` re-count.
- live/vision: **156 → 157**
- implemented/contract: **340 → 341**
- GEO NA: 100 unchanged
- complete: **false** (expected)
- Deviations: none material. IR optional N1–N3 deferred (vision workflow_ref still
  `ui.contacts.create`; Escape not required for form_open_only; plan/live/wiki
  untracked ride COMMIT).
- Review findings: IR **ACCEPT** (`tmp/grok-review.md`); no required product fixes.
- Tests: lint.sh pass; offline test.sh **1598 passed**, 40 deselected; live dual
  `test_ui_suppliers_create_open` **1 passed** (EXECUTE); vision purge_verified true.
- Cleanup: product path never submits; no disposable supplier/client records from
  product; frames outside repo purged after vision.
- Next residual slice: prefer special.invoice_email with disposable client save
  success + draft dual + send dialog dual (still blocked on client save); else
  products.create form dual if CTA stable; nested salesTaxRules isolation; no
  postings/bankLines steal; no bulk greening without official schema; annual stays
  org_inaccessible red.
