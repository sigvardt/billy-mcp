# 110.3 Wave-5m integration and independent review

## Objective

Integrate the completed Wave-5m `contactBalancePayments` ticketed-write leaf
only after verifying its committed bytes, then obtain an independent Grok
product review before opening the already researched Wave-5n `invoiceLateFees`
freeze.

## Evidence and constraints

- `tmp/grok-research.md` (research59) is the current cited official-documents
  brief: docs ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, and the 401/405 method gates.
- The accepted Wave-5m freeze is `wiki/wave_fivem_ticketed_writes_contract.md`
  (MD5 `fcb0e58742c8abc8ca9078859bb74eb8`) and
  `wiki/wave_fivem_freeze_independent_review.md`.
- Product tip `adc16eb` is committed and its child reports locally passing
  focused tests. It must be revalidated from committed bytes; no local child
  state is evidence.
- The root remains fail-closed for live, UI, vision, and the 92 ambiguous bulk
  rows. No completeness claim is in scope.
- The research59 edits to `memory/state.md` and
  `wiki/offline_write_probe_rules.md` are current evidence updates; preserve
  and validate them rather than overwriting them.

## Delegation decision

No node is spawned during this step. The only separable task is the mandatory
independent product review, but it depends on the Wave-5m merge landing on the
root branch. Once that integration is committed, the next plan must create a
focused `--agent=grok` review child with ownership limited to its review record
under `wiki/`; it must not alter product code or coverage status. Wave-5n
freeze authoring remains gated on that review's ACCEPT verdict.

## Execution sequence

1. Confirm the Wave-5m child has reached `completed`, inspect
   `HEAD...main.billy_complete.wave5m_contact_balance_payment_product`, and
   validate its executable artifacts from a clean archive. Check the four
   preview/execute tools, strict Pydantic outer models, exact ticket binding,
   one-write/no-retry behavior, required response root, absence of delete/bulk
   registration, corrected create cleanup text, and generated counts of 250
   `api_*` tools and 172 implemented/contract-tested rows.
2. In the next Prepare integration pass, merge only that completed child with a
   labelled no-fast-forward merge, reconcile wiki indexes, and rerun the
   relevant focused tests plus coverage and policy checks on the root merge.
3. Spawn the independent Grok product reviewer only after the root merge. Its
   cited brief must compare the current official docs and inventory with the
   merged implementation, reproduce the committed-byte checks, and write an
   explicit ACCEPT or REJECT record. Its scope excludes live/UI/vision/bulk
   qualification and any code changes.
4. If the review ACCEPTs, retain research59 as the evidence package for the
   subsequent Codex Power Wave-5n freeze-only child. If it rejects, fix only
   the cited defects, rerun verification, and obtain a fresh review before
   authoring the freeze.
5. Validate the research59 memory/wiki updates with `wiki lint`; keep every
   bulk row red and do not represent 401/405 probes as live qualification.

## Success criteria for this iteration

- Wave-5m is ready for a clean root integration and a separately owned Grok
  product review.
- Research59 evidence is preserved, indexed, and internally consistent.
- `coverage/status.json` remains `complete: false`; live/UI/vision/bulk are
  still red until their dedicated qualification work occurs.

## Post-Mortem

### Completed work

- Validated child commit `adc16eb` from a clean `git archive`; its focused
  product suite passed and verified the four ticketed tools, strict inputs,
  approval binding, no-retry behaviour, response-root mapping, and the absence
  of delete/bulk registration.
- Merged the product as root merge `8297713` and re-ran focused root checks.
  The root holds 250 `api_*` tools and 172 implemented/contract-tested API
  rows; `complete` remains false.
- Preserved research59's documented offline 401/405 probes, including the
  invoice-late-fee and invoice-reminder bulk-delete refusal, in the shared
  evidence rules.
- Independent Grok review59 accepts the Wave-5m offline product and records
  Wave-5n `invoiceLateFees` as research-ready only. The durable review record
  is `wiki/wave_fivem_product_independent_review.md`.

### Deviations and review findings

- The product child completed during execution, so the labelled root merge and
  reviewer launch occurred in this iteration instead of a later Prepare pass.
- Review59 found no product correction. It notes only the inherited process
  pattern of merged child seed files; it does not affect coverage honesty.

### Verification and cleanup

- Clean-archive validation passed 33 focused product/coverage/server tests.
- Root pre-merge non-live suite passed 1056 tests. Root focused post-merge
  Ruff, Pyright, coverage generation, coverage-policy, and repository-policy
  checks passed.
- No credentials, screenshots, frames, HAR files, or traces were added to the
  repository. Scratch review evidence remains outside tracked project files.

### Next unresolved coverage slice

Wave-5n may now author the `invoiceLateFees` create/update freeze contract from
research59. It still needs an independent freeze ACCEPT before product work.
The 92 ambiguous bulk rows, all live qualification, and the complete UI/vision
lane remain red.
