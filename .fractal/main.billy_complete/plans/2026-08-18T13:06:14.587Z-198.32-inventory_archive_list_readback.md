# 198.32 classify Lagermodul list archive filter

## Pivot

`113F1E05` closed further invoice entry-CTA dumps.
`unique_normal_action=false`. `next_slice=product_create`.
`FD39FFE7` still binds invoice draft CUD and product create.
Stay red. Do not finish.

The official dialog dump is closed on `b56ee43`.
`proved_submit=gem_produkt`. `unique_cleanup_path=archive_checkbox`.
Do not remake it. The Mere/Slet dump is closed.
`proved_delete_path=none`. Do not remake it. The products-list
archive dump is closed on `a87177f`.
`unique_restore_readback=none`. Do not remake it. Persist stays
fail-closed. `67CBACB6` is unanswered.

This slice classifies Lagermodul list archive chrome only. Create
lives on `/:org_slug/inventory`. Catalog `/products` has no create
CTA. Last research already named this follow-up once the products
list had no unique filter. That condition is now true.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.

## Goal

Open the Lagermodul inventory shell and classify archive-filter
chrome. Read-only. TDD first. Do not create a product. Do not
persist. Do not open **Opret produkt**.

Persist **only**:

- `inventory_path_class` (`inventory` / `other`)
- `inventory_heading_token` (`lagermodul` / `other` / `none`)
- `vis_arkiverede_count` (int)
- `arkiverede_count` (int)
- `skjul_arkiverede_count` (int)
- `archived_filter_token` (`vis_arkiverede` / `arkiverede` /
  `skjul_arkiverede` / `none`)
- `unique_restore_readback` (`show_archived_filter` / `none`)
- `create_form_open` (bool, must stay `false`)
- `proved_bind` (`none`)
- `missing_keys`

`archived_filter_token` is the one named count that equals 1.
`unique_restore_readback` is `show_archived_filter` only when
exactly one of the three named counts is 1 and the others are 0.
Otherwise both stay `none`. A search box or create CTA is not that
filter. `proved_bind` stays `none`. `create_form_open` stays
`false`. Do not click the filter unless the count is exactly one
and a click is required to prove it is a toggle. Prefer count-only.

Do not open **Opret produkt**. Do not click **Gem produkt**. Do
not tick **Arkiveret**. Do not open products **Mere**. Do not
reopen `ui_products_list`. Do not change
`product_persist_allowed`. Do not call
`ui_products_create_execute`.

Authority: `96908DC6`, `FD39FFE7`, `113F1E05`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Closed: form-contract,
Mere/Slet, products-list archive, invoice picker, Ember, fiber,
listener, structure, list CTA, Tilgodehavender CTA. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock
stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.products.create` to
  `ui_products_create_preview`
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or product
  `parity_status`
- Changing `product_persist_allowed` or wiring it to archive
- Calling `ui_products_create_execute` or clicking any save
- Overwriting form-contract, delete-chrome, or products-list
  archive dumps
- Inventing `ui_products_delete_*` or `api_inventory_*`
- Using `DELETE /v2/products` as cleanup
- Remaking invoice dumps or adding invoice helpers
- Recapturing catalog create CTA (research163)
- Opening Lagermodul **Opret produkt**
- Reopening `/:org_slug/products` for another archive count
- Picture upload, force clicks, `evaluate` clicks, guessed
  selectors
- Persisting source, locators, URLs, slugs, names, or screenshots
- Ledger, files, org, or a new contacts/bills CUD
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `a87177f`. Not a backstop. `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations CUD rows already name
preview tools. Invoice CUD stays `ui_invoices_*_open` and unbound.
Product create preview/execute already registered. Manifest still
points the honesty row at `ui_products_create_open`.
`BrowserProductSubmitter` fail-closes when
`product_persist_allowed` is false.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

`ui_inventory_open` already opens `/:org_slug/inventory` and fails
closed if Billy routes that path onto the products list. It never
clicks **Opret produkt**. Research125 classified path, heading, and
create-CTA markers only.

## Delegation

No children. Root owns this slice. `728BD2E4` child-split already
landed the seven write-family modules. Those children stay merged
and parked. Unlimited budget is not a reason to spawn.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/products.py` (ticket pair; no remap;
  persist stays fail-closed)
- `src/billy_mcp/ui_writes/products_form_contract.py` (closed)
- `src/billy_mcp/ui_writes/products_delete_chrome.py` (closed;
  reuse `classify_inventory_path` and `classify_inventory_heading`)
- `src/billy_mcp/ui_writes/products_archive_list.py` (closed
  products-list dump; copy bind-rule shape, do not grow this file)
- `tests/live/test_ui_products_list.py` and
  `tests/live/test_ui_inventory_open.py` (shell contrast)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_products_writes.md`
- `wiki/ui_inventory_open_shell.md`

New files:

- `src/billy_mcp/ui_writes/inventory_archive_list.py`
- `tests/unit/test_ui_inventory_archive_list.py`
- `tests/live/test_ui_inventory_archive_list.py`

Owner dump:

`~/.local/share/billy-mcp/inspect-live-inventory-archive-list.json`

Closed dumps the live test must not rewrite:

- `~/.local/share/billy-mcp/inspect-live-products-form-contract.json`
- `~/.local/share/billy-mcp/inspect-live-products-delete-chrome.json`
- `~/.local/share/billy-mcp/inspect-live-products-archive-list.json`

## Design choices

1. **P10 wins.** Do not remake form-contract. Do not remake
   Mere/Slet. Do not remake the products-list archive dump. Do not
   remake invoice helpers.
2. **TDD first.** Honesty-16 stays 16 and red. First product edit
   is a failing unit fixture against the key set above.
3. **Official labels only.** Count exact **Vis arkiverede**,
   **Arkiverede**, **Skjul arkiverede**. No substring. No body
   sweep. Do not click **Opret produkt**.
4. **Read-only.** Open `/:org_slug/inventory` through the same path
   `ui_inventory_open` uses. Classify. Fail closed if the page is
   the products list. Do not click Mere. Do not open create.
5. **Persist stays fail-closed.** A unique filter is a later
   restore path, not permission to persist. `67CBACB6` stays open.
6. **No remap** until live FastMCP CUD plus independent accept
   plus purge of a real create with restored state.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.products.create` still names `ui_products_create_open`.

Add `REQUIRED_INVENTORY_ARCHIVE_LIST_KEYS` and `missing_keys` in
`inventory_archive_list.py` only after a unit fixture fails
because those names do not exist, or add the fixture first against
a local helper stub. Preferred shape:

- Fixture feeds `{}`.
- Helper returns the missing key list from this plan.
- Current empty dump must fail with that exact set.

Also unit-test the bind rule: unique only when one named count is
1 and the other two are 0. Unit-test that
`create_form_open=true` is not a restore path. Do not edit
`generate_coverage_report.py` or remap coverage. Assert
`product_persist_allowed` still ignores this dump.

### 2. Smallest helper, then one live inspect

- Implement the helper in `inventory_archive_list.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  names.
- Reuse `classify_inventory_path` and
  `classify_inventory_heading`. Do not copy products-list path
  classifiers.
- Live test: READY via existing credential refs. No
  `BILLY_API_TOKEN`. Open `/:org_slug/inventory` only. Classify.
  Write the dump outside git.
- Live test verifies the dump keys without repeating a click if
  the dump already exists from this EXECUTE. Prefer one capture.
- If a delivered dump has every required key, do not recapture.
- Snapshot the three closed product dumps and assert they are
  byte-unchanged.
- Do not call `ui_products_create_execute`.
- Create nothing. Fresh-session check: no tagged
  `MCP-TEST-PRODUCT-` name. `create_form_open` must be `false`.

### 3. Stay red

Do not finish. Do not claim complete. If a unique filter exists,
record it and keep persist fail-closed. If none exists, record
`unique_restore_readback=none`. Either way, archive-until-absent
is still an owner question. Do not treat this dump as a finished
product create.

## Success criteria

- Honesty-16 still length 16 and red
- Failing dump-key fixture existed, then passed
- Live dump has every required key
- `create_form_open` is `false`
- Closed product dumps are byte-unchanged
- No **Gem** / **Gem produkt** / **Opret produkt** click
- `product_persist_allowed` still false
- `ui.parity.products.create` still names
  `ui_products_create_open`
- No credentials, tokens, screenshots, frames, HAR, traces, or
  product names in git
- Branch stays red. Do not finish.

## Post-Mortem

Work completed: one-shot Lagermodul list classify through
`/:org_slug/inventory`. Dump delivered outside git with
`inventory` / `lagermodul`, `create_form_open=false`, named
filter counts 0, `unique_restore_readback=none`. Honesty-16 stayed
red. Persist stayed fail-closed. Commit-mode suite 2072 passed /
83 deselected. Lint passed.

Deviations: independent review (`A337A622`) failed the new helper
as a redundant permanent diagnostic. FIX-VERIFY deleted
`inventory_archive_list.py` and its unit/live tests. Wiki keeps
the dump fact. Helper is not committed.

Review findings: dump contract met. Product create still unbound.
`67CBACB6` unanswered. No remake. No remap.

Cleanup: no product created. Closed dumps unchanged. Ephemeral
profile removed. No frames, HAR, or credentials in git.

Next unresolved coverage slice: invoice draft CUD still unbound
(`FD39FFE7`, `113F1E05`). Product create stays `UI_CHANGED` /
fail-closed until the owner answers `67CBACB6`. Do not add another
product list, dialog, or archive probe. Files and ledger wait.
Stay red.

