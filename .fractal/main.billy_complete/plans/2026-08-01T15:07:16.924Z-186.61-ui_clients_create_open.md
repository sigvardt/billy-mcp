# 186.61 UI clients create open form dual-count contacts.create


## Goal

Product research160 ACCEPT:

1. New read-only FastMCP UI tool **`ui_clients_create_open`** that opens the
   Billy **contact/customer create form** as form open only.
2. Dual-count **exactly** `ui.parity.contacts.create` (maps
   `api.contacts.create` / `POST /v2/contacts`) onto that tool with five UI
   flags true + vision.
3. Green discovery row **`ui.discovery.clients_create`** (new
   `UI_DISCOVERY_FAMILIES` entry `clients_create`; do **not** repurpose existing
   `ui.discovery.customers` list shell on `ui_clients_list`).

Target math (from tip `0d6a5e8` / product `cdfe986`): live/vision **154 → 156**
(discovery + parity); implemented/contract **338 → 340**; GEO NA **100**
unchanged; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research160)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. |
| Soft paths | `emails` / nested salesTax* / bank* / postings / files soft-empty body **127** dual (31/37 seeds). |
| Create form dual | List `/:org_slug/clients` + text CTA **Opret kontakt** opens dialog form dual (`research160_clients_create_dual2.json`): `name` visible, `registrationNo`, `street`, **14** visible inputs both sessions; `form_open_ready` true. |
| Soft `/clients/new` | Chrome-only body **200**, **0** form fields dual — **reject** as success path. |
| Tool gap | `ui_clients_list` is list-only (h1 Kunder, create_action_visible, never opens form). Dual-count create onto list is resource-identity theft. |
| Isolation | Create form ≠ special `POST /invoices/:id/emails`. ≠ suppliers create. ≠ contacts.get/update/delete. |
| Deferred | special.invoice_email (client save TimeoutError; no invoice rows); products.create soft /new 0 fields; nested salesTaxRules/payments steal rejected; bulk 92 + annual red. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research160_residual_dual.json`, `tmp/research160_clients_create_dual2.json`.

## Non-goals

- Greening `special.invoice_email`, products.create, contacts.get/update/delete/bulk_*,
  contactPersons.*, salesTaxRules.*, salesTaxPayments.*, postings.*, bankLines.*,
  productPrices.*, annual_reports, bulk 92
- Dual-counting create onto `ui_clients_list`
- Submitting the form (Gem / Opret / Save / Create) or filling disposable data in
  the product runtime path
- Treating soft `/clients/new` as success
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery + dual-count create) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research160)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_clients_create_open` |
| lane | ui |
| kind | form_open_only (read-only observe) |
| shell_kind | `clients_create` |
| success model | `UiClientsCreateOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools (API contacts create already offline-green) |

### Success model fields (non-PII)

Mirror create-open strictness (`extra=forbid`, no org_slug/url/selector secrets):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/clients` (form is a **dialog on list path**, not soft `/clients/new`) |
| `heading` | Literal `Kunder` (list h1 retained under dialog; do not require a separate create h1) |
| `shell_kind` | Literal `clients_create` |
| `create_dialog_open` | bool — create dialog/form surface present after CTA |
| `name_field_visible` | bool — input `name` visible in form scope |
| `registration_no_field_present` | bool — input `registrationNo` present (visible or in form inventory) |
| `address_or_person_fields_present` | bool — at least one of street / person_email / person_firstName / person_lastName present |
| `shell_markers_present` | bool — aggregate true when dialog form signature holds |

Optional: `create_action_cta_visible` before click for isolation docs; product success requires form open, not CTA alone.

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. **Required path** (research160 dual-proved): open
   `https://mit.billy.dk/{slug}/clients`, then observe-only click text CTA
   **Opret kontakt** (prefer exact text match; do not rely on bare nav "Opret"
   alone if ambiguous).
4. **Reject soft** `/{slug}/clients/new` as success (chrome-only dual; 0 fields).
5. Success when:
   - path class remains list `/:org_slug/clients` (dialog overlay) **or**
     generator-consistent scrubbed class that is **not** soft-empty `/new`
   - create dialog open with form signature:
     - `name` field visible
     - `registrationNo` present
     - address or person field class present (street and/or person_*)
   - shell_markers_present true
6. Close with Escape only after classify (no submit).
7. Never click Gem / Opret (submit) / Save / Create / Slet after form open;
   never fill and save disposable clients in product path.

### Reject as success

| Failure | Code class |
| --- | --- |
| Login page | AUTH_REQUIRED |
| CAPTCHA / passkey / push | AUTH_INTERACTION_REQUIRED |
| Still on list shell only (no name field / no create form after CTA) | UI_CHANGED / shell signature miss |
| Soft `/clients/new` chrome-only (0 fields, empty h1) as only surface | UI_CHANGED / not success |
| Egress deny | EGRESS_DENIED |

### CTA hard ban (tests must prove no click after open)

Never interact with after form open: Gem, Opret (submit), Save, Create, Slet,
Bekræft, archive toggles that mutate, country/autocomplete selections that
persist, any save path used in research disposable trail.

Product path is **open and classify only**. Escape cleanup allowed.

## Coverage generator contract

Mirror `apply_ui_invoices_create_open_shell_evidence` /
`apply_ui_bills_create_open_shell_evidence`:

1. **Constants**: tool name, model/unit/live test reference paths, vision record
   class path `tmp/vision-records/ui_clients_create_open.json`.
2. **Discovery seed**: add `"clients_create"` to `UI_DISCOVERY_FAMILIES`; in
   `build_ui_manifest` discovery loop call
   `apply_ui_clients_create_open_shell_evidence(row)` when
   `family == "clients_create"`.
3. **Dual-count** exact API id only:
   - When `api_row["id"] == "api.contacts.create"`, call apply with
     `parity_of_api_create=True` for `ui.parity.contacts.create`.
4. Set five UI flags true; tool_name; parity_status=`form_open_only`; evidence
   cites research160 dual + live test ref + vision record path class;
   method_or_route describes list + **Opret kontakt** dialog open (never soft
   /new alone).
5. **Must not** green: `api.contacts.list` (stays list tool),
   get/update/delete/bulk_*, contactPersons.*, special.invoice_email,
   suppliers create, products.create.
6. Existing `ui.discovery.customers` / `ui.parity.contacts.list` remain on
   `ui_clients_list` unchanged.
7. After regenerate: live/vision **156**; implemented/contract **340**; complete
   **false**; GEO NA **100**; API live_tested all false with
   `out_of_scope_by_user`.
8. Inventory tests: discovery green; create parity green; residual contacts ops
   + special.invoice_email red; live/vision math 154→156; list shell still
   contacts.list only.

## EXECUTE steps (root)

1. **Models** (`src/billy_mcp/models.py` + `tests/test_models.py`):
   `UiClientsCreateOpenInput` (empty forbid) +
   `UiClientsCreateOpenSuccess` fields above; schema forbids secrets.
2. **Browser** (`src/billy_mcp/browser.py`): Protocol +
   `ui_clients_create_open` implementation; signature helpers for create dialog
   form; unit tests in `tests/unit/test_browser.py`; ensure list tool still
   never opens create form as success.
3. **Server** (`src/billy_mcp/server.py` + registry tests): register tool with
   typed I/O; no generic browser exposure.
4. **Live test** `tests/live/test_ui_clients_create_open.py`:
   dual ephemeral profiles; DOM assert success; second-session independent
   read-back; vision record + purge outside repo; skip if credential refs
   missing; never API token; never submit CTAs; Escape close only.
5. **Coverage generator** + inventory tests asserting discovery green, create
   dual-count green, residuals red, live/vision math 154→156.
6. **Regenerate** coverage manifests (`scripts/generate_coverage_report.py` or
   project regen entry).
7. **Wiki** page `ui_clients_create_open_shell` + `_index` link; non-sensitive;
   cite research160 markers class only; isolation note create dialog ≠ list
   shell ≠ special invoice email; soft `/clients/new` not success.
8. **Offline gate**: `scripts/lint.sh`; offline `scripts/test.sh`.
9. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Registered `ui_clients_create_open` only; typed success/error |
| Discovery | `ui.discovery.clients_create` five flags true |
| Parity | `ui.parity.contacts.create` five flags true; tool same |
| Exact id | Only `api.contacts.create` dual-counted (not list/get/update/delete/bulk) |
| Isolation | list stays on `ui_clients_list`; invoice_email + residual contacts ops red |
| CTA | No Gem/Opret-submit/Save after open; soft `/clients/new` not success |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **156**; implemented/contract **340**; GEO NA **100**; complete **false** |
| Docs | Official docs fingerprint unchanged; no bulk greening |

## Residual after this product (stay red)

- `ui.parity.special.invoice_email`
- contacts.get/update/delete (+ bulk)
- products.create / productPrices.*
- nested salesTaxRules / taxRateDeductionComponents / residual salesTaxRulesets ops
- salesTaxPayments / salesTaxAccounts / salesTaxMetaFields (no steal onto greened shells)
- bankLines / postings steal candidates
- bulk 92 external-contract
- `ui.discovery.annual_reports` org_inaccessible

## Risk notes

- Dialog may not use `role=dialog` alone; inventory by named inputs (`name`,
  `registrationNo`, street/person_*) after **Opret kontakt** click, matching
  research160 probe2.
- Bare "Opret" can hit nav chrome; prefer text **Opret kontakt** as research160.
- Product runtime must never submit; research disposable save timed out and is
  out of product scope.

## Post-mortem (fill after FIX-VERIFY)

- Outcome:
- live/vision:
- implemented/contract:
- Deviations:
- Next residual:

## Post-Mortem

- Outcome: **DELIVERED** product `ui_clients_create_open` form_open_only + discovery
  `clients_create` + dual-count exact `api.contacts.create` (research160 / IR ACCEPT).
- live/vision: **154 → 156**
- implemented/contract: **338 → 340**
- GEO NA: 100 unchanged
- complete: **false** (expected)
- Deviations: none material. IR optional N1–N3 deferred (dialog flag from field
  signature; Escape only in live vision helper; API out_of_scope remains
  status-level policy text).
- Review findings: IR **ACCEPT** (`tmp/grok-review.md`); no required product fixes.
- Tests: lint.sh pass; offline test.sh **1592 passed**, 39 deselected; live dual
  `test_ui_clients_create_open` **1 passed**; vision purge_verified true.
- Cleanup: product path never submits; no disposable client/invoice records from
  product; frames outside repo purged after vision.
- Next residual slice: prefer special.invoice_email with disposable client save
  success + draft dual + send dialog dual (still blocked on client save); else
  products.create form dual if CTA stable; nested salesTaxRules isolation; no
  postings/bankLines steal; no bulk greening without official schema; annual stays
  org_inaccessible red.

