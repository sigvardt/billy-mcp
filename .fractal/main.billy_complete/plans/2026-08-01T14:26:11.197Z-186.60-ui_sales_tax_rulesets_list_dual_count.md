# 186.60 ui salesTaxRulesets list dual-count via settings vat shell

## Goal

Dual-count green **`ui.parity.salesTaxRulesets.list`** by mapping the already
qualified read-only **`ui_settings_vat_open`** Indstillinger **Momssatser** panel
shell to **`api.salesTaxRulesets.list`**, using the same empty-input
`shell_open_only` contract already live for `ui.discovery.settings_vat` and
`ui.parity.taxRates.list`.

No new browser workflow. No new API/UI tools. No get/create/update/delete/bulk
greening for salesTaxRulesets. No salesTaxRules.* or taxRateDeductionComponents
dual-count. No other taxRates.* greening. No annual product. No bulk
external-contract greening. No invoice_email product. No contacts/products
create tools this slice.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research159).  
Dual evidence (tmp, non-git): `tmp/research159_residual_dual.json`.  
Discovery + taxRates.list already green on tip: `ui_settings_vat_open`
(research130 / research158 / 186.59); wiki `wiki/ui_settings_vat_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_vat_open.py`,
vision record class `tmp/vision-records/ui_settings_vat_open.json`.  
API offline list already green: `api.salesTaxRulesets.list` →
`api_sales_tax_rulesets_list` (`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: MD5 `8b94b0135c91fd15fe54ea33e088a4be` / ETag status pair
`wcw4x9hqvu3603` (unchanged research103–159).  
Prior dual-count on same shell: `api.taxRates.list` → `ui_settings_vat_open`
(research158 / 186.59; Satser for salg/køb). This slice dual-counts **Regelsæt**.

## Why this slice (research159)

1. Dual headless sessions research159 residual:
   - Typed `ui_settings_vat_open` dual READY + success
     (`shell_kind=settings_vat`, `vat_panel_markers_present=true` both sessions).
   - Tool signature requires **Regelsæt** + **Satser for salg** + **Satser for
     køb** — Regelsæt is the sales-tax-ruleset browse chrome on Momssatser.
   - Soft seed `salesTaxRulesets` dual soft-empty body_len **127** (= nonsense)
     — no dedicated `/:org_slug/salesTaxRulesets` list URL shell.
   - Soft hub `/settings` dual body **626**, h1 Indstillinger; tool click
     required for VAT panel success.
   - Invoice_email still blocked (detail hrefs 0; client save TimeoutError;
     draft stayed on `/invoices/new`); pure NA still rejected.
   - contacts/products create form dual not product-ready (soft `/new`
     chrome-only; focused create dual CTA flaky).
   - annual_reports soft `/annual_reports` dual h1 **Upsedasse!** (stay red).
   - No API token; no write CTA clicks; ephemeral profiles deleted.
2. Same inventory dual-count pattern as taxRates.list on this shell, with
   **parity_status remaining `shell_open_only`** and exact API row isolation
   (Regelsæt ↔ salesTaxRulesets.list; Satser ↔ taxRates.list already green).
3. get/create/update/delete/bulk UI parity for salesTaxRulesets remain red
   (browse panel only; Opret regelsæt banned). Nested salesTaxRules.* and
   taxRateDeductionComponents.* stay red without separate dual isolation.
4. One new green UI row only; no new tool surface.
5. Explicitly deferred/rejected by research159: invoice_email; contacts/products
   create tools; salesTaxRules.* / taxRateDeductionComponents.*; bankLines →
   recon steal; postings → transactions steal; productPrices pure NA; bulk 92;
   annual greening.

## Non-goals

- Inventing `api_settings_*` or a second UI Momssatser/rulesets tool.
- Greening `ui.parity.salesTaxRulesets.get|create|update|delete|bulk_*`.
- Greening any `salesTaxRules.*` or `taxRateDeductionComponents.*` UI parity.
- Greening residual `taxRates.get|create|update|delete|bulk_*` (list already green).
- Greening `ui.parity.special.invoice_email` or inventing send-email UI tools.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting productPrices, files, bankLines, postings, daybookTransactions.
- New create-form tools (`ui_clients_create_open`, products create).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research130 live
  qualification + research159 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_vat_open_shell_evidence` with a **second** flag
  `parity_of_api_sales_tax_rulesets_list: bool = False` (keep existing
  `parity_of_api_list` for taxRates.list only). When rulesets flag True: keep
  `api_row_id` from parity builder (`api.salesTaxRulesets.list`), append
  research159 evidence mapping Regelsæt section + “API list filters/sort/
  pagination UI not producted”, keep `parity_status=shell_open_only` and
  non-claims for other salesTaxRulesets ops / salesTaxRules.* /
  taxRateDeductionComponents.* / residual taxRates ops; discovery path keeps
  both flags False and `api_row_id=None`; taxRates dual-count path unchanged;
  wire `if api_row["id"] == "api.salesTaxRulesets.list":
  apply_...(parity_of_api_sales_tax_rulesets_list=True)` in the parity builder
  loop next to `api.taxRates.list`. Mutually exclusive flags (never both True
  on one call). Update docstring so discovery still claims no salesTax* except
  the two explicit list dual-counts. |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.salesTaxRulesets.list` → `ui_settings_vat_open` in qualified_ids +
  tool_by_id; qualified shell count **53→54**; live/vision total **153→154**
  (`ui_shell_green + geo_na` = 54 + 100); assert evidence mentions
  `api.salesTaxRulesets.list` + research159 + Regelsæt / filters-sort-pagination
  phrase as appropriate; assert residual salesTaxRulesets ops stay red
  `discovery_required`; assert salesTaxRules.* and taxRateDeductionComponents.*
  stay red; assert taxRates.list still green; assert invoice_email still red;
  update hard-coded `53` / `153` / `ui_shell_green = 53` assertions (and any
  residual family lists that treat all salesTaxRulesets as red). |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_vat_open_shell.md` — dual-count note for
  `api.salesTaxRulesets.list` / Regelsæt / shell open only; keep taxRates.list
  Satser note; non-claims get/create/update/delete/bulk and nested
  salesTaxRules / taxRateDeductionComponents; soft salesTaxRulesets URL empty
  observation; API offline non-live; update desc / `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_vat_open`
- models / server registration
- `tests/live/test_ui_settings_vat_open.py`
- unit browser tests for the VAT settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_vat_open` (existing) |
| Open sequence | hub `/:org_slug/settings` then observe-only click **Momssatser** |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_vat` |
| Panel markers | **Regelsæt**; Satser for salg; Satser for køb |
| Input | empty strict model |
| Soft seeds | reject `settings/vat`, `salesTaxRulesets`, `taxRates`, `momssatser`, other non-VAT panels |
| Soft URL evidence | `/salesTaxRulesets` body 127 soft-empty (research159 dual) |
| Parity row | `ui.parity.salesTaxRulesets.list` / `api_row_id=api.salesTaxRulesets.list` |
| parity_status | `shell_open_only` |
| Write CTAs | never click Opret regelsæt / Opret sats / Gem / Tilføj / Upload |
| Sibling dual-count | `ui.parity.taxRates.list` remains green (Satser; research158) |

## Steps

1. Implement generator second dual-count flag + `api.salesTaxRulesets.list`
   wiring on VAT applicator; leave taxRates.list flag/path intact.
2. Update inventory tests: shells 53→54; live/vision 153→154; residual
   rulesets/rules/deduction ops red; taxRates.list still green; invoice_email red.
3. Wiki dual-count note for Regelsæt / salesTaxRulesets.list.
4. Regenerate coverage (`scripts/generate_coverage_report.py` → manifests/status/report).
5. Run lint + offline `test.sh` (unit/contract/inventory). Live VAT dual already
   greened; re-run only if IR requires.
6. Independent Grok review of dual-count isolation; FIX-VERIFY; COMMIT.

## Acceptance criteria

| Check | Expected |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.salesTaxRulesets.list` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_settings_vat_open` |
| api_row_id | `api.salesTaxRulesets.list` |
| parity_status | `shell_open_only` |
| Evidence | research159 + maps api.salesTaxRulesets.list; Regelsæt; filters/sort/pagination not producted; no PII |
| Sibling | `ui.parity.taxRates.list` still green on same tool |
| Residual salesTaxRulesets ops | stay red / discovery_required |
| Nested salesTaxRules.* / taxRateDeductionComponents.* | stay red |
| special.invoice_email | stays red |
| complete | **false** |
| API live_tested | all false + out_of_scope_by_user |
| Math | live/vision **153→154**; implemented/contract **337→338** |
| Deferred | invoice_email, create forms, bulk 92, annual remain incomplete |

## Risks

| Risk | Mitigation |
| --- | --- |
| Colliding flags break taxRates.list dual-count | Separate flags; never both True; inventory asserts both list rows green |
| Accidentally greening all salesTaxRulesets ops or salesTaxRules.* | Explicit per-op id gate `api.salesTaxRulesets.list` only |
| Claiming Regelsæt proves nested rules rows | Evidence limited to list/browse shell_open_only; residual ops red asserts |
| Wiki still claims “does not green salesTax*” | Edit carefully: list dual-counts only for taxRates.list + salesTaxRulesets.list |
| IR rejects multi-section panel multi dual-count | Same bar as accounts/users dual-count; signature requires Regelsæt; soft URL empty |

## IR focus

- Confirm research159 dual proves VAT tool + Regelsæt in signature + soft
  salesTaxRulesets empty + no steal onto salesTaxRules / taxRateDeductionComponents.
- Confirm taxRates.list remains green and independent.
- Confirm invoice_email remains red (not weak-NA greened).
- Confirm no browser/src product changes required.
- Overall completeness remains FAIL (expected).

## Post-mortem

### Completed

- Dual-counted `ui.parity.salesTaxRulesets.list` → `ui_settings_vat_open`
  (`shell_open_only`, Regelsæt) via generator flag
  `parity_of_api_sales_tax_rulesets_list` (mutually exclusive with taxRates
  `parity_of_api_list`).
- Inventory tests: shells 53→54; live/vision 153→154; residual salesTaxRulesets
  ops red; salesTaxRules/taxRateDeductionComponents red; taxRates.list still
  green; invoice_email red.
- Wiki + `_index` dual-count note for salesTaxRulesets.list; coverage regenerated.
- No `src/` browser/model/server changes (reuse existing VAT shell product).

### Deviations

- None material vs plan 186.60 / research159 ACCEPT product.

### Review findings

- IR **ACCEPT** (`.fractal/main.billy_complete/tmp/grok-review.md`).
- Required product fixes: **none**.
- Optional N1–N2 deferred (vision workflow_ref secondary id; residual probe
  separate-page body capture empty while tool dual-ok).

### Test results

- `lint.sh`: pass (wiki + memory clean; ruff/pyright/coverage inventory/policy).
- Offline `test.sh`: **1586 passed**, 38 deselected (EXECUTE + FIX-VERIFY reconfirm).
- Live full UI suite: not required this slice (reuse existing VAT dual/live
  qualification + research159 dual). No live API.
- Coverage: live/vision **154**; implemented/contract **338**; complete **false**.

### Cleanup state

- Research159 disposable client/draft failed cleanly (no residual markers).
- Profiles purged in research. No frames/HAR/secrets in product diff.
- `api_token_used: false`.

### Next unresolved coverage slice

- Prefer special.invoice_email only with disposable client save success + draft
  dual + send dialog dual (no weak NA).
- Else multi-resource: contacts.create form tool dual, nested salesTaxRules
  isolation, residual taxRates write ops.
- Stay red without new evidence: bulk 92 external-contract; annual_reports
  org_inaccessible; bankLines/postings steal; productPrices pure NA.

