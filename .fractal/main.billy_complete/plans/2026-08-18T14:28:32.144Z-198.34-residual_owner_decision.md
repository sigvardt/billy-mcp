# 198.34 residual table and owner decision

## Pivot

`D68E402A` stops classify dumps. Research built the residual
table from existing coverage, inventories, and dumps only.
Actionable now: none. No remaining honesty-16 row has both a
proved normal mutation control and a proved cleanup path that
is not already live-proved.

`FD39FFE7` still makes invoice draft CUD and product create
mandatory and red. Do not freeze them as done.
`113F1E05` and `A337A622` forbid more invoice CTA or product
archive probes. `67CBACB6` is already in the parent inbox and
has no reply.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Goal

Land the residual table as a durable wiki page and send one
consolidated owner decision. No helper. No dump. No live
Billy session. Stay red.

The decision restates four blocked families in one parent-inbox
message (priority 6 or higher):

1. Invoice: create form has no existing-customer bind after
   every closed probe (`proved_bind=none`). Accept `UI_CHANGED`
   for this org, or name one different normal control.
2. Product: `67CBACB6` unanswered. Does archive-until-absent
   count as restored state, or must create stay unbound?
3. Daybook: `unique_persist_token=none`. Accept `UI_CHANGED`,
   or name the persist token. Do not arm `_ledger_write`.
4. Files: Bilag **Slet** 0. Accept fail-closed, or name a
   delete control.
5. Postings / **Godkend** stay prohibited.

Authority: `96908DC6`, `D68E402A`, `FD39FFE7`, `113F1E05`,
`A337A622`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`.

## Non-goals

- New helper, live inspect, or dump
- Remaking any closed invoice, product, daybook, or Bilag dump
- Arming `_ledger_write` or `LEDGER_SUBMIT_BLOCKER`
- Clicking **Godkend**, **Bogfør**, **Upload filer**, or
  **Gem produkt**
- Greening honesty-16 or remapping remaining `*_open` tool
  names
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Live API token calls, docs re-lock, bulk92, residual29
- Spawning children
- Claiming `complete: true` or finishing the node

## Baseline

HEAD `a5dcdca`. Not a backstop. Memory `state.md` is the only
dirty file (HEAD hash fold). `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations already name preview
tools and are live-proved. Invoice CUD stays
`ui_invoices_*_open`. Product create stays
`ui_products_create_open` and fail-closed. Files stay
`ui_uploads_list`. Daybooks stay `ui_daybooks_open` /
`ui_daybooks_delete_open`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

`67CBACB6` thread is the product-archive question only. This
slice adds one new consolidated decision, not a remake of that
thread.

## Delegation

No children. Root owns the table and the send. Unlimited budget
is not a reason to spawn. The seven write-family children stay
merged and parked. 157 retired descendants stay unmerged.

Reuse (do not remake):

- `coverage/ui_workflows_manifest.yaml`
- `scripts/generate_coverage_report.py` honesty-16
- `wiki/ui_ledger_writes.md`
- `wiki/ui_products_writes.md`
- `wiki/ui_files_writes.md`
- `wiki/ui_invoices_writes.md`
- `.fractal/main.billy_complete/tmp/grok-research.md`

New files:

- `wiki/ui_write_residual_table.md`

No new `src/` or `tests/` files.

## Design choices

1. **P10 wins.** `D68E402A` forbids another live probe. If none
   are actionable, send one decision.
2. **Table is the product.** Wiki holds the residual table.
   Memory folds the same facts. Do not invent a new helper to
   "prove" the table.
3. **One send.** One parent-inbox message. Do not split into
   four radios. Do not re-ask `67CBACB6` as a second thread;
   cite it.
4. **Stay red.** Honesty-16 stays 16. Do not finish.
5. **No remap** until a live FastMCP write plus independent
   accept plus purge exists for that row.

## EXECUTE

### 1. Stay-red check

Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Do not add a failing live
fixture. Do not edit `generate_coverage_report.py`.

### 2. Persist the residual table

Write `wiki/ui_write_residual_table.md` from the research table.
Keep only: row id, coverage tool, registered preview/execute,
mutation-control proof, cleanup/read-back proof, `D68E402A`
category. No iteration labels. No credentials. Then
`wiki update --path=wiki` and `wiki lint --path=wiki`.

Fold the same facts into memory `state.md`, `todo.md`, and
`decisions.md`. No iteration numbers.

### 3. Send one owner decision

Send to the parent inbox (`--parent` or `--node=main`),
priority 6 or higher. Subject names the four blocked families.
Body is the residual conclusion plus the five questions in
Goal. Cite `D68E402A` and `67CBACB6`. Re-read the posted
message before relying on it.

Do not open Billy. Do not create records. Do not add source
helpers.

## Success criteria

- Honesty-16 still length 16 and red
- `wiki/ui_write_residual_table.md` exists and matches the
  research categories
- One new parent-inbox decision is posted and readable
- No new helper, dump, or live session
- Closed dumps and honesty mappings are unchanged
- No credentials, tokens, screenshots, frames, HAR, traces, or
  customer data in git
- Branch stays red. Do not finish.

## Post-Mortem

Work completed: honesty-16 stayed length 16 and red. Wiki
`ui_write_residual_table.md` landed. Memory folded the same
facts. Owner decision `D02702C5` is in the parent inbox. No
helper. No dump. No live Billy session.

Deviations: `fractal radio save D02702C5` failed because the
parent inbox is owner-read-only. The send itself landed.

Review findings: IR PASS. Required fixes: none. The bills
vision run id lives in `tmp/vision-records/ui_bills_writes.json`
and is not repeated in `wiki/ui_bills_writes.md`. That is a
pre-existing family-wiki gap, not a table error.

Test results: `lint.sh` passed (ruff, pyright 0/0/0, coverage
inventory 305/345, repository policy). Commit-mode `test.sh`
2063 passed / 82 deselected. Did not run `BILLY_TEST_MODE=full`
or `ui-full` because completion is still false.

Cleanup: no records created. No frames, HAR, or credentials.

Next unresolved coverage slice: wait for the owner answer on
invoice bind, `67CBACB6`, daybook persist, and files delete.
Stay red. Do not finish.

