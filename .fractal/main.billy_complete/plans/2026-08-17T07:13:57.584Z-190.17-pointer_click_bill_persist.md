# 190.17 Pointer-click Gem som kladde then persist POST

## Goal

Replace the `force=True` **Gem som kladde** locator click with a real
pointer click, watch request and response, then run one tagged draft
bill create/update/delete through `create_server` `call_tool`. Prove
POST `/v2/bills`, independent read-back, and third-session bill plus
supplier absence. Coverage stays red.

Last live create dump already has tag `MCP-UI-B-*`, vendor footer bind,
date, amount `1,00`, and **Gem som kladde**. Persist then stayed on
`/:org_slug/bills/new` with `watched=[]`. Contacts persist already
requires `mouse.click` on a visible enabled hit target. Bills still
use `_click_exact_button(..., force=True)`.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`,
`31F6E753`, `F3F318A2`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Accepting stale vision `run_id=3af923d562b546d597e9f156b5c3227f`
- Live API token calls, SPA `POST /v2/contacts` seed, docs re-lock
- Seeding vendor via `ui_clients_create`
- Pre-filling `expenseCategory`, `taxRate`, or `paymentDate` unless a
  proved pointer click still yields no request
- Widening POST egress unless a request watch names a blocked bills path
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `fdfbcb4`. Clean tree. Not a backstop. `complete=false`. Honesty 16
stay red. Bills `tool_name` stay `ui_bills_*_open`. Contacts remap
`93D7A063` stays closed.

Live create dump is a real `MCP-UI-B-*` record. Persist dump is
`BILLY_ERROR` / `watched=[]` on `/bills/new`. Seven leftover
`billy-live-bills-*` profiles exist from that run.

Cited contract (research):

- Official belongs-to is `contact` / `contactId`
- Persist proof is browser POST 2xx `/v2/bills` only
- Egress already allows exact POST `/v2/bills`, PUT prefix `/v2/bills/`,
  DELETE prefix `/v2/bills/`
- Ticket pattern is already `UiWriteProtocol` / `ConfirmationStore`
- Contacts save path is `inspect_save_delivery` then `page.mouse.click`
- Bills save path is `get_by_role(...).click(force=True)`

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds. No extra CUD parity row is greened by open-only evidence.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/bills_form.py`
- `tests/unit/test_ui_bills_writes.py`
- `tests/live/test_ui_bills_writes.py`
- `wiki/ui_bills_writes.md`

Parked write-family children stay parked. `728BD2E4` child split was
for family implementation. Those seven families are already merged. A
pointer click on the existing draft save control is not a new family.

## Design choices

1. **Honesty first (`728BD2E4`).** Confirm the 16-row open-only gate
   still fails those rows closed. Do not edit the gate.
2. **TDD.** First product edit is a failing unit test: a covered or
   disabled **Gem som kladde** must not count as clicked. A
   `force=True` locator click is not persist proof. Only
   `mouse.click` at the visible enabled button center is a pointer
   hit. Unit dumps stay under `tmp_path`.
3. **Contacts pattern, bill selector.** Inspect the exact
   **Gem som kladde** button (role + exact name). Do not reuse
   `button[data-cy='save-button']` unless a live save dump names that
   attribute. No `evaluate`-click. No `force=True` on draft save.
4. **Watch request and response.** Record abort URL/method. Do not
   widen egress unless that dump names a bills path.
5. **`31F6E753` dump before click.** No submit until the create dump
   still has tag, vendor, date, amount, and **Gem som kladde**, and
   the new save dump shows a pointer hit. If disabled or hit is not
   the button, return `UI_CHANGED` and do not click.
6. **Cleanup reverse.** Delete the draft bill, then the supplier
   (including leftover `MCP-UI-B-6DCF4A5C` if still present). Third
   session proves both gone. Purge leftover `billy-live-bills-*`
   profiles. Do not touch the shared chrome profiles.
7. **Vision.** `author=live_test`, `reviewer_verdict=pending_review`
   only. `run_id` is the frame-dir suffix.

## Execute

1. Run honesty 16. It must stay length 16 and red.
2. Add failing unit tests for covered/disabled save and for pointer
   click as the only save path. Run them. Capture the expected failure.
3. Smallest product change in `bills_form.py` so those tests pass.
   Add a non-PII owner-only save dump (`visible`, `disabled`,
   `hit_target`, `pointer`). Watch request and response.
4. One live FastMCP bills CUD. Never click until the create dump and
   save dump are both live. POST create, **Opdater** PUT, tagged
   **Slet**. Independent second-session read-back. Third-session bill
   and supplier absence.
5. If a proved pointer click still yields no request, dump category /
   tax / visible validation and stop with `UI_CHANGED`. Do not invent
   fills in this slice.
6. Offline `test.sh`. Keep coverage red.

## Done when

- Honesty 16 still red, length 16
- Unit tests refuse `force=True` as persist proof and accept only a
  pointer hit on the exact draft save button
- Live FastMCP create/update/delete of one tagged draft bill passes
  with independent read-back, **or** the save dump proves a new
  `UI_CHANGED` (disabled or covered) without another blind click
- Create dump is still a live `MCP-UI-B-*` record
- Persist is POST 2xx `/v2/bills`, or the request watch names a
  blocked path / missing request after a proved pointer
- Third session shows no bill and no leftover tagged supplier
- Vision record is `live_test` / `pending_review` only, new `run_id`
- Leftover `billy-live-bills-*` profiles are gone
- `complete=false`. Bills `tool_name` still `ui_bills_*_open`

## After this slice (not now)

Independent vision accept + purge, then remap only the three bills CUD
rows.

## Post-Mortem

Work completed:

- Honesty 16 still length 16 and red. Bills `tool_name` stay
  `ui_bills_*_open`. No remap. Official docs not re-locked.
- Draft save `force=True` is gone. Pointer inspect plus `mouse.click`
  is the only save path.
- Offline tests no longer write `inspect-live-bills-save.json`.
- Wait now checks every visible `.ds-moved-with-portal` node, then
  Tabs once. Escape is still forbidden.

Deviations:

- Plan asked for a finished FastMCP CUD. Live execute still returns
  `UI_CHANGED` `source=leftover_portal`. Tab does not dismiss the
  overlay. No POST `/v2/bills`.

Review findings (`tmp/grok-review.md`): FAIL stood. Covered save is
real. Current owner save dump after offline tests is not live proof.
Stale vision `3af923d562b546d597e9f156b5c3227f` not accepted.

Tests:

- Offline `BILLY_TEST_MODE=commit` `test.sh`: 1877 passed, 60
  deselected. Lint passed. Coverage still `complete: false`.
- Live CUD fails on leftover portal. No bill POST.

Cleanup:

- Live write profiles purged. Shared chrome profiles untouched.
- Footer create may have left tagged suppliers. Prove absence on the
  next live run.

Next unresolved coverage slice:

- Dismiss leftover `.ds-moved-with-portal` without Escape, then persist
  `POST /v2/bills`. Then update/delete, independent read-back,
  third-session bill and supplier absence. Stay red. No remap.

