# 2.47 Wave-5k freeze authoring after Wave-5j review gate

## Objective

Integrate the offline Wave-5j bank-line fallback review and advance to a
Wave-5k `bankPayments` freeze contract only if its committed authority opens
that gate; otherwise preserve the documented fail-closed blocker.

## Verified inputs

- The committed Codex Power fallback review accepts the merged 18-tool Wave-5j
  product offline but explicitly says it does **not** authorize Wave-5k work.
  It must therefore be treated as a closed gate even though its test evidence
  and scope are acceptable.
- Research47 is the cited Grok authoring package:
  `.fractal/main.billy_complete/tmp/grok-research.md` sections 5--6, with the
  durable companion plan
  `plans/2026-07-30T03-12-00Z-2.47-wave5k_freeze_page_authoring_package_research.md`.
- The accepted Wave-5k research review pages and
  `wiki/offline_write_probe_rules.md` authorize only `bankPayments` create and
  update. POST/PUT return 401 at the authentication gate; singular DELETE
  returns 405 and is excluded. The planned page must preserve this boundary.

## Execution

1. Monitor `main.billy_complete.wave5j_product_codex_fallback_review` for its
   committed review page. Inspect its diff and evidence, confirm its verdict is
   limited to offline Wave-5j bytes, then merge with
   `git merge --no-ff -m "merge main.billy_complete.wave5j_product_codex_fallback_review"`.
   Run `wiki lint --path=wiki`, resolve only real integration defects, update
   `memory/state.md`, and unsave radio message `5DDFE065` after the merge.
2. Do **not** start the initialized `wave5k_freeze_contract` author leaf. Its
   source gate is closed by the committed review, so delete the unstarted leaf
   without retaining a speculative branch. Record the conflict in memory and
   radio rather than relaxing it by inference.
3. A new authority must explicitly open Wave-5k authoring before recreating the
   focused `--agent=codex-power` contract-author leaf. After a valid contract
   commit, review it independently with `grok`; only that committed ACCEPT can
   open a separately scoped Codex Power product leaf for the four tools.

## Guardrails and success criteria

- No generic HTTP/browser tools, webhooks, bulk bodies, singular delete tools,
  or invented mutable bank-payment fields.
- No green coverage flags until the later product leaf has real offline tests;
  live/UI/vision/bulk/completeness remain fail-closed.
- If and only if the authority gate opens, the next product slice remains
  exactly four tools: create and update preview and execute, targeting
  `/bankPayments` with singular `bankPayment` request and required
  `bankPayments` response roots.
- This is intentionally sequential at the review gate; starting author or
  reviewer work earlier would turn an explicit non-acceptance into an inferred
  approval.
