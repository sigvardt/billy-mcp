# 186.68 ui clients delete open

## Goal

Product research167 ACCEPT:

1. New read-only FastMCP UI tool **`ui_clients_delete_open`** that opens an
   existing client (contact) detail surface, opens **Mere**, and returns typed
   non-PII flags proving delete chrome (**Slet kontakt** visible).
2. Observe-only: **never** click Slet confirm / permanent delete / Arkivér on the
   product path. Harness may seed and cleanup disposable marker clients only.
3. Green exact **`ui.parity.contacts.delete`** / `api.contacts.delete` only onto
   `ui_clients_delete_open` (delete-chrome-open / mere-menu parity).
4. Keep greened `ui_clients_list`, `ui_clients_create_open`, `ui_clients_get_open`,
   and `ui_clients_update_open` unchanged.

Target math (from tip `d642d3c` / product 186.67 clients update open):
live/vision **168 → 169** (parity delete only); implemented/contract
**352 → 353**; complete **false**.

Contacts data-plane egress already on tip (GET/POST/DELETE `/v2/contacts` +
countries from 186.65). **No new egress rules** (cleanup may use SPA session
token via test harness only; not product tool surface).

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research167)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. contacts Supports get/list/create/update/**delete**/bulk. |
| Egress | contacts GET/POST/DELETE already path_allow; products plane also open. invoices/bills GET still deferred. |
| Dual seed | Opret kontakt → name marker → Gem → list marker **true dual**. |
| Dual detail | `/:org_slug/contacts/:id/customer` dual. |
| Primary chrome | Opret, Ret, Mere; **no** primary Slet (`slet_visible_body` false dual). |
| Mere → delete chrome | Mere open **true dual**; after Mere body contains **Slet kontakt** dual (`after_mere_has_slet_kontakt`); also Arkivér kontakt / Indbetaling / Udbetaling / Kontoudtog. |
| Confirm dialog | Not stable-automated this research → product stays **observe-only** (open Mere + assert labels; never confirm). |
| Tool gap | update-open is Ret form; get-open is read overview; **no delete-chrome tool**; `ui.parity.contacts.delete` still `discovery_required`. |
| Isolation | Delete-open ≠ update-open ≠ get-open ≠ create ≠ list. Green only **contacts.delete**. Leave products.get / invoices.get / invoice_email / bulk red. |
| Cleanup harness | SPA `X-Access-Token` from page requests + `context.request` GET/DELETE with `organizationId` (not env `BILLY_API_TOKEN`; not page.evaluate under route). |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research167_focus_dual.json`, `tmp/research167_rest_dual.json`,
`tmp/research167_cleanup_report.json` (all_clean true).

## Non-goals

- Ticketed durable UI delete execute / confirm click / Arkivér execute
- Greening products.get / invoices.get / bills.get / special.invoice_email / bulk
- Replacing or dual-counting onto list / create / get / update tools
- invoices/bills path_allow this slice
- New full `browser_action: allow` on API host
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| Models + browser + server register | Root Grok EXECUTE |
| Live dual-session test + vision record | Root Grok EXECUTE |
| Coverage generator (contacts.delete dual-count) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research167)

### A. Browser egress

No change expected. Keep existing contacts + products path_allows.  
Regression unit tests must still deny invoices/bills GET and invoice email POST.

### B. Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_clients_delete_open` |
| lane | ui |
| kind | delete_chrome_open / mere-menu observe (read-only) |
| shell_kind | `clients_delete` |
| success model | `UiClientsDeleteOpenSuccess` (Pydantic, `extra=forbid`) |
| error model | existing `ToolError` (`AUTH_*`, `UI_CHANGED`, egress/auth codes) |
| API invent | none — `api.contacts.delete` already offline implemented |

### C. Success model fields (non-PII)

No org_slug, raw URL, selector secrets, customer PII, or delete confirmation
payloads in success fields:

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/contacts/:id/customer` (or supplier if dual later; freeze customer for this slice as research167) |
| `shell_kind` | Literal `clients_delete` |
| `detail_open` | bool — on contact profile path with marker/detail ready |
| `mere_open` | bool — Mere menu opened |
| `slet_kontakt_visible` | bool — **Slet kontakt** present after Mere |
| `arkiver_kontakt_visible` | bool — Arkivér kontakt present after Mere (optional support flag; not required alone for pass) |
| `primary_slet_absent` | bool — true when primary chrome has no standalone Slet before Mere (research167: true dual) |
| `shell_markers_present` | bool — aggregate: detail path + mere_open + slet_kontakt_visible |

Strict **reject**:

- list shell or `/clients/empty` as delete chrome
- create dialog as delete
- get-open overview without Mere / without Slet kontakt
- update-open Ret form as delete chrome
- any product path that clicks confirm Slet / permanent delete

### D. Navigation (observe-only product path)

1. Headless Playwright via `BrowserRuntime`; existing egress; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open clients list `https://mit.billy.dk/{slug}/clients`, settle.
4. Open an existing non-header client row/detail
   (`…/contacts/{id}/customer` class). Prefer id link; else row click.
5. Click **Mere** (button role; Danish label).
6. Assert body/menu contains **Slet kontakt**.
7. Return success flags. **Do not** click Slet kontakt confirm or any permanent
   delete/archive control on the product path.
8. Errors: AUTH_*, UI_CHANGED if path/chrome missing, egress deny if applicable.

### E. Live dual test

File: `tests/live/test_ui_clients_delete_open.py` (mirror update_open pattern).

- Dual sessions (primary + secondary independent profiles).
- Seed disposable marker client via UI (unique marker name class R18668-…).
- Call tool / browser method; assert success flags dual.
- DOM assertions: path class contacts/:id/customer; Mere; Slet kontakt.
- Independent second-interface read-back (secondary session) of same surface.
- Vision capture → Grok vision review record under
  `tmp/vision-records/ui_clients_delete_open.json` (non-sensitive); purge frames.
- Cleanup: remove marker (SPA session token + context.request DELETE with
  organizationId preferred; or UI flow if stable). Verify remaining_false dual.
- Never require live API token.

### F. Coverage generator

- Dual-count only `api.contacts.delete` / `ui.parity.contacts.delete` onto
  `ui_clients_delete_open` (discovered/implemented/contract/live/vision true for
  that UI parity row; API row stays live_tested false + out_of_scope_by_user).
- Do **not** steal list/create/get/update tools or greened rows.
- Leave products.get/update/delete, invoices/bills residual, invoice_email, bulk
  92, annual red.
- Expected status after regenerate: live/vision **169**; implemented/contract
  **353**; complete **false**.

### G. Unit / inventory tests

- Models: success construct; extra=forbid; shell_kind/path_class literals.
- Browser unit: delete chrome signature helpers; reject list/create/update-as-delete.
- Server registry: tool name registered; no generic browser tool.
- Coverage inventory asserts contacts.delete dual-count tool_name and remaining
  residuals still red.

### H. Wiki

- `wiki/ui_clients_delete_open_shell.md` scrubbed (no PII, no tokens, no iter
  noise beyond durable contract).
- Update wiki `_index` link if mechanical.

## Execute checklist

1. Models `UiClientsDeleteOpenInput` / `UiClientsDeleteOpenSuccess`.
2. `BrowserRuntime.ui_clients_delete_open` (+ helpers for Mere + Slet kontakt).
3. Server register `ui_clients_delete_open`.
4. Unit tests (models, browser, coverage server/registry as needed).
5. Live dual test + vision record path + cleanup.
6. Coverage generator apply function + wire into build.
7. Regenerate coverage; assert math 169/353 complete false.
8. `lint.sh` + offline `test.sh`.
9. Wiki shell page.
10. IR → FIX-VERIFY → COMMIT. Not node finish.

## Risk / fail-closed

- If Mere does not expose Slet kontakt dual in EXECUTE org → leave red; do not
  false-green.
- If SPA renames Slet kontakt → UI_CHANGED; update contract only with dual re-proof.
- Never green contacts.delete via get/update tools.
- Never claim ticketed delete execute complete.

## Post-mortem (EXECUTE)

- Producted research167 / plan 186.68: `ui_clients_delete_open` via Mere → Slet kontakt
  (maps api.contacts.delete observe-only).
- Models + browser + server + unit/live tests + coverage generator dual-count
  contacts.delete only.
- Coverage regenerated: live/vision **169**; implemented/contract **353**;
  complete false; list/create/get/update tools unchanged.
- lint.sh pass. offline test.sh **1620 passed**, 44 deselected.
- Live dual `tests/live/test_ui_clients_delete_open.py` pass; vision record present
  with purge_verified; residual products.get + invoices/bills + bulk/annual red.
- Wiki: `ui_clients_delete_open_shell.md` + index.
- Frames outside worktree on ssd (`/Volumes/ssd_1/Repositories/billy-mcp-vision-tmp`).
- Ready for REVIEW. Not finish.

## Done when

- Plan file present; research167 cited.
- EXECUTE can implement without reopening scope.
- Ready EXECUTE root Grok only.

## Post-Mortem

### Completed
- Producted `ui_clients_delete_open` (Mere → Slet kontakt observe-only).
- Dual-counted only `ui.parity.contacts.delete` / `api.contacts.delete`.
- Live dual + vision record purge_verified; offline 1620 pass; lint pass.
- Coverage: live/vision 169; implemented/contract 353; complete false.

### Deviations
- None material. Frames stored outside worktree on ssd
  (`/Volumes/ssd_1/Repositories/billy-mcp-vision-tmp`) due to full home volume.
- IR optional N1–N3 deferred (primary_slet after-Mere fallback wording; live seed
  reuse; plan/wiki/live ride commit).

### Review
- IR ACCEPT (`tmp/grok-review.md`); no required product fixes.
- FIX-VERIFY: reconfirmed lint + offline test; no code changes.

### Cleanup
- Live harness seed/cleanup on marker clients; vision frames purged.
- Dedicated org not re-scanned in FIX-VERIFY (product path observe-only).

### Next unresolved coverage slice
- invoices/bills path_allow + get-open after dual block proof
- products.get if detail hydrate dual holds
- special.invoice_email after durable client+invoice path
- contacts bulk_* remain red without official bulk schema
- bulk 92 external-contract; annual_reports org_inaccessible
- API live_tested stays false (`out_of_scope_by_user`)
- Not node finish until complete true under scoped policy

