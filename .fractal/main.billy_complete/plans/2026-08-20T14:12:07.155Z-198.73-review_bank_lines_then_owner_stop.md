# 198.73 review bankLines then owner stop

## Pivot

Parent `3DA6FB8E` is binding. HEAD `df2f475` is the 198.72
auto commit, not a `(failed on <step>)` backstop. Product
slice is already on the branch: typed `api_bank_lines_list`
filters plus docs relock. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Frozen
official scratch stays `billy-api-docs-research-iter72.html`
and `billy-api-docs-chunk-iter72.js`. Do not treat tmp as a
new docs capture. Do not re-GET https://www.billy.dk/api/.

This iteration does **not** implement a new API or UI tool.
It independently reviews 198.72, rechecks coverage once, and
stops for owner input on bulk92 plus six
`READONLY_PROPERTY_TABLE` rows. Keep `complete=false`. Do
not finish. No live API. No UI dumps.

## Goal

Independent review of 198.72 on `df2f475`, then one coverage
recheck, then one owner-input outbox. Residual set stays:

| Remaining red | Count | Kind / blocker | Tools |
| --- | ---: | --- | --- |
| API bulk | 92 | `ambiguous_bulk` / `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` | none |
| API readonly-map | 6 | `readonly_field_map_insufficient` / `READONLY_PROPERTY_TABLE` | none |
| API method-closed | 0 | — | — |
| API meta-delete | 0 | — | — |
| UI owner-scope | 6 | `out_of_scope_by_user` (non-blocking) | none |

The six readonly-map ids stay:

- `api.contactBalancePostings.create`
- `api.contactBalancePostings.update`
- `api.postings.create`
- `api.postings.update`
- `api.transactions.create`
- `api.transactions.update`

Generated snapshot stays implemented **546**, contract **551**,
live/vision **339**, docs lock `pi4s9u10j037qn` /
`d805f3d2bb8e339f7635d6834b4011bd`, `complete=false`.

## Why no child

Review plus a stop report is one node's job. File ownership
would collide on `src/billy_mcp/api/bank_reads.py`,
`tests/api/test_bank_reads.py`,
`scripts/generate_coverage_report.py`, and generated
coverage if a child tried to "fix" 198.72 in parallel.
Seven write children stay completed and merged. Retired
descendants stay unmerged. Do not spawn.

## EXECUTE

No TDD product slice. Do not add tools.

1. Diff landed `BankLinesListRequest` and
   `api_bank_lines_list` against the frozen List filters
   table in `tmp/billy-api-docs-chunk-iter72.js` and plan
   `2026-08-20T13:05:55.752Z-198.72-bank_lines_list_filters.md`.
   Shared `BankListRequest` must still reject `accountId`
   and `q`. GET path is `/bankLines` only.
2. Run the coverage recheck (commit-mode, no `ui-full`):
   - `tests/api/test_bank_reads.py`
   - `tests/coverage/test_official_docs_lock.py`
   - `tests/coverage/test_coverage_inventory.py::test_residual_audit_unimplemented_api_set_is_bulk_plus_readonly_map`
   - `tests/coverage/test_coverage_inventory.py::test_residual_clear_honesty_rows_are_toolless_and_qualified`
   - `tests/coverage/test_coverage_inventory.py::test_bulk_rows_remain_ambiguous_and_toolless`
   - `tests/coverage/test_ui_residual_write_owner_scope.py::test_unimplemented_ui_rows_are_exactly_owner_scoped`
3. If a 198.72 contract test fails, smallest fix in
   `bank_reads.py` / its tests / `BANK_LINE_FILTERS` only.
   Recapture the failure first. Do not touch bulk freeze or
   residual honesty ids.
4. If tests pass, do not edit product files. Record residual
   counts and test names in memory for the owner-input
   report. Do not post that report until independent review
   PASSes.
5. Inbox/feed unread owner radio once more. Do not start a
   docs scan.

## Independent review

Reviewer reads this plan, the research brief, plan 198.72,
`df2f475`, and the frozen chunk. PASS only if:

- `accountId` is required on `api_bank_lines_list`
- optional filters match the official table
- other bank list tools stayed on `BankListRequest`
- docs lock is `pi4s9u10j037qn` / `d805f3d2bb8e339f7635d6834b4011bd`
- bulk92 and six readonly-map rows stay toolless
- `complete is False`
- no live API and no new docs GET

FAIL if a documented bankLines filter is missing, if
`BankListRequest` grew those filters, if accounts list
gained `isBankAccount`, or if residual rows were greened.

## After PASS

One outbox report (priority 6): 198.72 reviewed on `df2f475`;
coverage recheck numbers; exact residual tests; owner input
required on bulk92 plus six readonly-map rows; `complete`
stays false; stopped per `3DA6FB8E`. Keep `96908DC6` and
`9B979A05` saved. Keep `3DA6FB8E` saved until that report
lands.

## Out of scope

Bulk tools, readonly-map create/update tools, empty
payloads, `api_accounts_list` `isBankAccount`, UI dumps,
**Godkend**, annual-report tools, live API, OpenAPI
re-probe, another docs scan, `node finish`.

## Stop line

If a writable official field appears on the six
readonly-map rows, or a bulk body schema appears, keep
those rows red. Do not invent tools. Keep `complete=false`.

Keep `96908DC6` and `9B979A05`. Do not finish.

## Post-Mortem

Work completed: independent review of the bankLines
list-filter slice on `df2f475` PASSed. Frozen official
table matches `BankLinesListRequest`. Shared
`BankListRequest` still rejects `accountId` and `q`.
`api_accounts_list` did not gain `isBankAccount`. Coverage
recheck: 46 targeted tests passed. FIX-VERIFY commit-mode
`test.sh` 2442 passed / 83 deselected. Lint passed. Wiki
lint clean on memory and project wiki. No product edit
this slice. Owner-input outbox posted. `3DA6FB8E` unsaved.

Deviations: no new docs GET (parent `3DA6FB8E`). No
`BILLY_TEST_MODE=full` because completion requirements
are not met.

Review findings: PASS. Required fixes none. Non-blocking
note: `entryDatePeriod` stays a string; inventory lists
the official format tokens.

Cleanup: no live UI, no frames, no disposable records.

Next unresolved coverage slice: owner scope on bulk92
(`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`) plus six
`READONLY_PROPERTY_TABLE` rows
(`api.contactBalancePostings.create` / `.update`,
`api.postings.create` / `.update`,
`api.transactions.create` / `.update`). UI remaining is
six owner-scoped rows. `complete` stays false. Do not
finish.

