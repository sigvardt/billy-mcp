# 186.46 ui accounts list dual-count via settings accounting shell

## Goal

Dual-count green **`ui.parity.accounts.list`** by mapping the already qualified
read-only **`ui_settings_accounting_open`** Indstillinger **Regnskab / Kontoplan**
panel shell to **`api.accounts.list`**, using the same empty-input
`shell_open_only` contract already live for `ui.discovery.settings_accounting`.

No new browser workflow. No new API/UI tools. No get/create/update/delete/bulk
greening. No annual product. No bulk external-contract greening. No files /
taxRates / daybooks / productPrices dual-counts this slice.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research145).  
Dual evidence (tmp, non-git): `tmp/research145_residual_parity_dual.json`.  
Discovery product already green: `ui_settings_accounting_open` (research127 /
prior 186.28); wiki `wiki/ui_settings_accounting_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_accounting_open.py`,
vision record class `tmp/vision-records/ui_settings_accounting_open.json`.  
API offline list already green: `api.accounts.list` → `api_accounts_list`
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–145).  
Prior dual-count precedents: invoices/products/contacts/bills/transactions/
salesTaxReturns/users `.list` via `parity_of_api_list=True` in
`scripts/generate_coverage_report.py` (users is the closest settings-panel
template).

## Why this slice (research145)

1. Dual headless sessions research145:
   - Typed `ui_settings_accounting_open` dual READY + success
     (`shell_kind=settings_accounting`, `accounting_panel_markers_present=true`).
   - Deep dual on `/settings/accounting` → bare `/settings`, h1 Indstillinger,
     labels **Regnskab + Køb + Kontoplan** present, body_len ~1088 (not soft-empty).
   - Soft seed `/:org_slug/accounts` dual rewrites to `/settings` with h2
     **Kontoplan** (real shell, not NA).
   - Soft pure chart paths `kontoplan` / `chart-of-accounts` dual soft-empty
     body_len 127 (= nonsense) — no separate dedicated accounts list route.
   - No API token; no write CTA clicks; ephemeral profiles deleted.
2. Same inventory dual-count pattern as `ui.parity.users.list`, with
   **parity_status remaining `shell_open_only`** (settings multi-section panel,
   not a dedicated `/:org_slug/accounts` list shell; tables may be 0).
3. get/create/update/delete/bulk UI parity remain red (no dual-proved detail/write
   shells; bulk external-contract on API lane).
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research145: files↔Bilag weak map; daybooks editor ≠
   list; taxRates Momssatser multi-resource; organizations company form;
   productPrices nested form (body 190 without Pris markers); bankLines pure NA
   rejected (related bank shells greened).

## Non-goals

- Inventing `api_settings_*` or a second UI accounts/Kontoplan tool.
- Greening `ui.parity.accounts.get|create|update|delete|bulk_save|bulk_delete`.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting files, taxRates, daybooks.list, productPrices, organizations,
  bankLines, postings.
- Implementing chart edit/write UI or API list filters/sort/pagination UI.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research127 live
  qualification + research145 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_accounting_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror users dual-count); when True: keep
  `api_row_id` from parity builder (`api.accounts.list`), append research145
  evidence mapping, keep `parity_status=shell_open_only`; discovery path keeps
  default False and `api_row_id=None`; wire
  `if api_row["id"] == "api.accounts.list": apply_...(parity_of_api_list=True)`
  in the parity builder loop (alongside users/salesTaxReturns) |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.accounts.list` → `ui_settings_accounting_open`; qualified shell
  count **40→41**; live/vision total **120→121** (`ui_shell_green + geo_na`);
  assert evidence mentions `api.accounts.list` + research145; assert other
  accounts ops stay red `discovery_required`; hard-code live total
  `41 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` where present |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_accounting_open_shell.md` — dual-count note for
  `api.accounts.list` / shell open only; non-claims get/create/update/delete/bulk;
  SPA `/accounts` rewrite observation; API offline non-live; update desc /
  `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_accounting_open`
- models / server registration
- `tests/live/test_ui_settings_accounting_open.py`
- unit browser tests for the accounting settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_accounting_open` (existing) |
| Open sequence | SPA seed `/:org_slug/settings/accounting` → bare hub with Regnskab panel |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_accounting` |
| Panel markers | Regnskab; Køb; Kontoplan |
| Input | empty strict model |
| Soft seeds | reject non-accounting settings aliases; pure `kontoplan`/`chart-of-accounts` empty |
| SPA rewrite evidence | `/accounts` → `/settings` + Kontoplan (research145 dual) |
| CTA / write | never Gem / Sæt låsedato / Opret* / Tilføj* / Upload |
| Parity row | `ui.parity.accounts.list` / `api_row_id=api.accounts.list` |
| parity_status | **`shell_open_only`** (not `list_shell_open_only`) |
| API invent | never invent `api_settings_*` |
| Distinct shells | company / vat / invoicing / users settings stay separate |
| get/create/update/delete/bulk UI | stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.accounts.list` wiring on
   `apply_ui_settings_accounting_open_shell_evidence`.
2. Update inventory tests counts and assertions (41 qualified shells; 121
   live/vision with geo NA = 41 + 80).
3. Run coverage generator; confirm status live/vision **121**, complete **false**.
4. Wiki dual-count note on settings accounting shell page + index desc if needed.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (no live API; no
   `BILLY_TEST_MODE=ui-full` completeness claim).
6. Memory EXECUTE notes; leave dual JSON under tmp (non-git) for IR.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.accounts.list` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_settings_accounting_open` |
| parity_status | `shell_open_only` |
| api_row_id | `api.accounts.list` |
| Evidence | research145 + maps api.accounts.list; no PII |
| Other accounts ops | stay red / discovery_required |
| Deferred families | files, taxRates, daybooks, productPrices, orgs, bankLines remain incomplete |
| Tools | No new tool registrations |
| API live | All API live_tested remain false with out_of_scope_by_user |
| Complete | `complete: false` |
| Offline tests | Inventory + non-live suite green |

## Independent review focus

- Confirm research145 dual proves accounting tool + Kontoplan deep markers +
  `/accounts` rewrite (not pure soft-empty NA).
- Confirm only `.list` is greened; writes/get stay red.
- Confirm `shell_open_only` honesty (panel multi-section; not inventing a list path).
- Confirm live/vision 121 math: 41 shells + 80 GEO NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects multi-section panel as accounts list | Same bar as users Brugere dual-count; shell_open_only; tables=0 documented |
| Accidentally greening all accounts ops | Explicit per-op id gate `api.accounts.list` only |
| Count drift (40→41 shells) | Update every hard-coded 40 / 120 assertion in inventory tests |
| Wiki still claims “does not green accounts” | Edit dual-count note carefully; keep non-claims for other ops |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **121**; complete **false**.
- Ready independent review of dual-count package 186.46.
- Not node finish.

## Post-Mortem

### Work completed

- Extended `apply_ui_settings_accounting_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror users dual-count).
- Wired `api.accounts.list` → accounting applicator with `parity_of_api_list=True`.
- Inventory tests: qualified shells 40→41; live/vision 120→121; accounts.list
  dual-count asserts; get/create/update/delete/bulk accounts stay red;
  discovery accounting keeps `api_row_id=None`.
- Regenerated coverage: live/vision 121, implemented/contract 305, complete false.
- Wiki dual-count note on `ui_settings_accounting_open_shell` + `_index` desc.
- No new UI tools; no src/ browser or server changes.

### Deviations

- None material vs plan. Optional IR N1/N2 left as-is (tmp dual JSON non-git;
  live suite not re-run this slice).

### Review findings

- IR 186.46: ACCEPT, required fixes none.
- Overall completeness: FAIL (expected: bulk 92, annual_reports red,
  residual UI parity, API live out of scope).

### Test results

- lint.sh: pass (wiki + ruff + pyright + coverage check + repo policy).
- Offline test.sh: 1565 passed, 35 deselected (EXECUTE and FIX-VERIFY).
- Full BILLY_TEST_MODE=ui-full / completeness suite: not run (complete still false).
- wiki lint: wiki + memory clean.

### Cleanup state

- Research dual used ephemeral profiles (purged). No disposable UI writes.
- No frames/HAR in repo. No credentials in product files.

### Next unresolved coverage slice

- Residual UI parity (files weak Bilag map; productPrices nested form;
  bank recon join binding; Momssatser multi-resource; daybooks list≠editor;
  organizations company form).
- ui.discovery.annual_reports org_inaccessible red.
- 92 ambiguous bulk API rows external-contract red (BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS).
