# 142.6 Wave-5r salesTaxReturns freeze independent review

## Decision

Adopt the existing uncommitted Research75 readiness package rather than restart
research. It is a cited Grok brief that rechecks the current official docs and
unauthenticated method gates after the Wave-5r freeze page reached root. Its
only immediate consequence is to make the required independent freeze review
well-scoped; it does not authorise product code or coverage changes.

The next work is one separable, independent review. Delegate it to a childless
Grok leaf. No Codex Power product child may be created in parallel because the
freeze verdict is its explicit authority gate.

## Baseline to preserve before delegation

1. Inspect the existing Research75 diffs and the ignored scratch brief for
   non-sensitive, cited evidence only. Retain the readiness page, its generated
   index entry, product-ready root-presence correction, and current-state
   update; do not copy raw request/response material into tracked files.
2. Verify the fixed facts before committing: freeze content MD5
   `078aca13828b5e0d71b454c1aa2dc00f`; official-doc MD5
   `8b94b0135c91fd15fe54ea33e088a4be`; update-only method gates (POST/DELETE
   405, JSON-object PUT 401, empty PUT 400); and coverage 178 implemented /
   178 contract / 0 live / 0 vision / `complete: false`.
3. Run `wiki update --path=wiki`, `wiki lint --path=wiki`, the node lint script,
   the non-live/non-vision test suite, and dependency audit. Commit the
   readiness package and this plan with `fractal commit` so a child starts from
   the reviewed root baseline.

## Delegation

Create and launch exactly one focused leaf after the baseline commit:

```sh
fractal node init wave5r_sales_tax_returns_freeze_review \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --base=main.billy_complete \
  --agent=grok \
  --title='Wave-5r Sales-Tax-Returns Freeze Independent Review' \
  --max-depth=0 --max-children=0 --max-descendants=0
```

Configure the child before launch with the following exclusive work contract:

- It may add only `wiki/wave_fiver_freeze_independent_review.md` as the root
  deliverable (plus its own `.fractal/main.billy_complete.wave5r_sales_tax_returns_freeze_review/`
  seed artifacts). It must not alter source, tests, coverage, status, manifests,
  or other wiki pages.
- Independently re-fetch and cite `https://www.billy.dk/api/`, use only
  `https://api.billysbilling.com/v2` for unauthenticated method-gate checks,
  and inspect the reviewed root freeze page. It must distinguish current
  observations from cited prior research.
- Verify the page MD5, one inventory id, exactly two future tool names,
  `PUT /salesTaxReturns/:id`, strict outer `{id, salesTaxReturn}` boundary,
  opaque inner map and matching optional inner id, `salesTaxReturns` success
  root, ticket-only/no-retry execution protocol, readonly/opaque property
  limits, closed create/delete, excluded bulk/webhooks, and unmodified red
  coverage.
- Issue an unambiguous **ACCEPT** or **REJECT**. ACCEPT authorises only the
  later offline Codex Power product leaf. It must explicitly exclude coverage
  greening, live, UI, vision, cleanup qualification, bulk, and completeness.
  Any contract drift or unverified claim is REJECT/fail-closed.
- Never use credentials, a visible browser, production or live write data, or
  persist raw frames, HARs, traces, request bodies, or secrets. Finish only
  after committing the cited review page.

## Integration and next gate

While the child is running, manage only that child: inspect its status and
outbox, answer any question through radio, and prevent scope expansion. On
completion, review its three-dot diff and evidence boundary. Merge a valid
verdict as `merge main.billy_complete.wave5r_sales_tax_returns_freeze_review`,
then regenerate and lint the project wiki.

If and only if the independent page accepts the frozen MD5, the next planning
cycle may create a Codex Power leaf for exactly the two update tools described
in `wiki/wave_fiver_sales_tax_returns_product_ready_research.md`. A REJECT,
document drift, failed test, or missing review leaves
`api.salesTaxReturns.update` red and ends this product path.

## Non-goals

- No product source, registration, test, inventory, or coverage changes now.
- No live/API-token/UI/browser/vision qualification.
- No bulk, create, singular delete, webhook, transaction, special-operation, or
  association-delete work.
- No claim that research or freeze authoring alone completes an approval gate.

## Post-Mortem

### Completed

- Preserved the cited research-readiness package on the root baseline and
  launched the required independent Grok freeze review.
- The review independently re-fetched the official documentation, re-probed
  unauthenticated method gates, and accepted the exact frozen
  `api.salesTaxReturns.update` update-only contract. Its durable, non-sensitive
  record is `wiki/wave_fiver_freeze_independent_review.md`.
- No product code, registry entries, tests, inventory evidence, or coverage
  rows were changed. The update row remains red and the coverage status remains
  incomplete.

### Deviation and cleanup

- After producing the cited ACCEPT record, the Grok child could not authenticate
  for a later PREPARE step and therefore could not commit its own branch. It was
  closed to prevent repeated authentication failures. The already-written
  review is retained as root work for normal verification and commit; it is not
  treated as a substitute Codex review or as a coverage qualification.
- No credentials, live writes, browser evidence, screenshots, traces, HARs, or
  sensitive test data were created. The independent-review scratch is ignored
  temporary evidence only.

### Findings and verification

- The reviewer found no blocking freeze-contract discrepancy.
- Its sole non-blocking note is that the red manifest row has generic response
  wording (`changed_records[]`, `meta.deletedRecords`) while the frozen product
  contract requires a `salesTaxReturns` response root. This is not changed now:
  the row is deliberately unimplemented and product work must establish the
  concrete mapper and tests before its inventory evidence is aligned.
- Required lint, tests, generated-coverage validation, and wiki lint are run
  in this verification pass before the integration commit.

### Next unresolved coverage slice

The approved next slice is a separate Codex Power product leaf for exactly
`api_sales_tax_returns_update_preview` and
`api_sales_tax_returns_update_execute`, followed by a separate independent
product review. Live, UI, vision, bulk, create/delete, cleanup qualification,
and overall completeness remain open.
