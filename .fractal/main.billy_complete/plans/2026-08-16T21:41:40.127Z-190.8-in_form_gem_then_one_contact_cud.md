# 190.8 In-form Gem, then one contact CUD

## Goal

Make the customer-page **Gem** commit a rename. Then prove one tagged contact
through FastMCP create, update, and delete. Independent read-back after each
step. A third session proves absence. Do not green.

Authority: `96908DC6`, `A5FBD4DC`, `E1E454F4`, `E613984F`, `C7DBE974`.
Research: `.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
ETag `"121myuqjdm53603"` / 147934 bytes. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- A second tagged create
- BrowserRuntime-only write as qualification proof
- Live API calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`
- Self-approving vision or purging frames from the live test
- Inventing `paymentTermsDays` unless the live form marks it required

## Baseline

HEAD `b59e362`. Exact name match and **Ja, slet** are in. Create through
FastMCP persists. Update opens **Ret**, fills `{tag}-U`, clicks a **Gem**,
and the form stays open. List does not show the rename.

`_click_save` tries **Gem ændringer**, then the last exact **Gem**. The live
dump clicked the first role-button **Gem**. Neither is the form commit.
`button:text-is('Gem')` matched zero nodes. After click, `input[name=name]`
and **Gem** stayed visible on `/:org_slug/contacts/:id/customer`.

`_update_customer` then goes to `/clients` and returns `NOT_FOUND`. That hides
the real failure: the edit form never closed.

This pass listed `/clients/empty`, **Kontakter**, **Ingen kontakter**, no
`MCP-UI-C-*`. Idle leftover profiles were purged. `chrome-profile` stays.

Coverage `complete=false`. Honesty 16 stays red.

## Delegation

No children. The slice is one helper plus one live file pair:

- `src/billy_mcp/ui_writes/contacts.py`
- `tests/unit/test_ui_contact_exact_labels.py`
- `tests/live/test_ui_contacts_writes.py` only for the one-record CUD

Root Grok implements. A child fork would collide on those files.

## Design choices

1. **TDD first.** First product edit is a failing in-form **Gem** unit test.
   Run it. Capture the failure. Then implement.
2. **In-form Gem, not first or last global Gem.** Click the visible **Gem**
   that shares a container with `input[name=name]` (and **Virksomhed** /
   **Privatperson** when present). Never **Gem kommentar**.
3. **Closed form is the save signal.** After click, if `input[name=name]` and
   **Gem** are still visible, return `UI_CHANGED` ("save did not close the
   edit form"). Do not search the list and call that `NOT_FOUND`.
4. **One tag.** `MCP-UI-C-<8hex>`, then `{tag}-U` after update. No second
   create. Do not create until leftovers are still empty.
5. **Live qualification uses `create_server()` `call_tool`.** Vision stays
   `pending_review`. No greening.
6. **Keyring refs on.** `BILLY_ORGANIZATION_ID` unset until READY returns the
   live slug.

## EXECUTE

### 1. Failing test (first edit)

Extend `tests/unit/test_ui_contact_exact_labels.py`. Fake a customer edit page
with **Gem kommentar**, a decoy **Gem** that is last in document order, and a
**Gem** next to `input[name=name]` plus **Virksomhed**.

| Case | Expected today | Expected after fix |
| --- | --- | --- |
| `_click_save` on that page | last/decoy **Gem** | name-field **Gem** |
| Update after fill, form still open | `NOT_FOUND` after list search | `UI_CHANGED` save-did-not-close |
| **Gem kommentar** only | no exact **Gem** click | no exact **Gem** click |

Keep `test_save_clicks_last_exact_gem` only if create dialog save still uses
last **Gem**. If update and create share `_click_save`, split: create may keep
last **Gem**; update must use the name-field container.

Do not green. Do not run live in this first edit.

### 2. Implement in-form save

Files:

- `src/billy_mcp/ui_writes/contacts.py` — `_click_save` / `_update_customer`
- `tests/unit/test_ui_contact_exact_labels.py`

Rules:

- Update clicks the **Gem** that shares a container with `input[name=name]`.
- After that click, wait briefly. If the name field and **Gem** remain,
  return `UI_CHANGED`. Do not `goto` `/clients` first.
- If the form closed, then go to `/clients` and require an exact `{tag}-U`.
- Do not add official-API `country` / `paymentTermsDays` unless the live form
  shows them required and **Gem** stays disabled.
- If the in-form click still leaves the form open on the later live run,
  type the name (`press_sequentially`) instead of `fill` plus synthetic
  `Event('input')`. That is the fallback in the same slice, not a new plan.

Run the new unit tests. Then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline). Do not claim
completeness.

### 3. One live FastMCP CUD

Only after leftovers are still empty.

```
unset BILLY_ORGANIZATION_ID BILLY_API_TOKEN
BILLY_BROWSER_PRIMARY_REFERENCE=billy-ui-primary \
BILLY_BROWSER_SECONDARY_REFERENCE=billy-ui-secondary \
uv run pytest tests/live/test_ui_contacts_writes.py::test_ui_contacts_create_update_delete_via_call_tool -q --tb=short
```

Sequence: create preview+execute, independent exact read-back of `{tag}`,
update preview+execute to `{tag}-U`, independent exact read-back of `{tag}-U`
(and `{tag}` absent), delete preview+execute, third-session absence.

If create or update succeeds and a later step fails, delete leftover tags
through `ui_clients_delete_*`. Delete write, readback, observer, and cleanup
profiles on every exit. Do not touch `chrome-profile`.

Vision: `author=live_test`, `reviewer_verdict=pending_review` only.

### 4. Do not green

Honesty 16 stays red. `tool_name` stays `ui_clients_*_open` until independent
accept plus purge. `coverage/status.json` stays `complete=false`.

## Done when

- Failing in-form **Gem** test existed, then passed.
- `_update_customer` returns `UI_CHANGED` when **Gem** leaves the form open.
- One live FastMCP create, rename, and delete of a single tag, with exact
  independent read-back and third-session absence, or a leftover-empty org
  plus a concrete save-path failure.
- No leftover `MCP-UI-C-*` names. No leftover live/inspect profiles.
- No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm the in-form **Gem** test is real, the live rename
persisted or the failure is `UI_CHANGED` with the form still open, honesty 16
is still red, and no secrets landed.

## Post-Mortem

Completed: failing then passing unit tests for in-form **Gem** vs last decoy,
and for `button[data-cy='save-button']`. `_update_customer` now clicks that
Ember save button, types the name, and uses list exact-name as persist proof.
Offline commit-mode suite ran after the first helper landing (1831 passed).
Live create through FastMCP still submits.

Deviations: an open edit form is not a failed save. Billy keeps
`input[name=name]` and **Gem** visible after click. Treating that as
`UI_CHANGED` hid a possible list proof. The check was removed. Live list
read-back still does not see `{tag}-U`.

Review: IR FAIL. R1 live persist. R2 no CUD qualification. Honesty 16 red.

Live evidence: `.fractal/main.billy_complete/tmp/inspect-live-save-commit.json`.
The commit control is
`<button class="button primary" data-cy="save-button" data-ember-action="...">`.
Xpath/data-cy click and Ember `$().val()` still leave the list without
`{tag}-U`. Cleanup listed no leftover `MCP-UI-C-*` names.

Create dialog save stays last exact **Gem**. Update save prefers
`button[data-cy='save-button']`. Fake-page create tests now click **Gem**
again.

Next slice: make Ember bind the typed name before `data-cy=save-button`, then
one FastMCP CUD. Do not green.

