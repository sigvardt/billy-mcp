# 2.8 Wave 4 read integration and second cohort

## Objective

Complete the cited Wave-4 offline read slice without changing any live, UI,
vision, write, bulk, special-route, or product-completeness claim. Integrate
the three first-cohort deliveries when their committed archives pass review,
then use the freed worker slots for the two remaining file-disjoint read
modules. Root alone will register the five modules and advance only precise
`implemented` / `contract_tested` coverage evidence from 44 to 94 API rows.

The governing contract is the current Grok brief at
`.fractal/main.billy_complete/tmp/grok-research.md` and
`wiki/wave_four_remaining_clear_reads_contract.md`: the official fingerprint is
ETag `hsisik4g9p3603`, MD5 `c2efda0ee4cf9cf200e14910c5fc6996`; 50 clear
get/list operations remain; only cities, states, and zipcodes lists require
non-empty `countryId`.

## Delegation decision

This remains a manager-led, five-way implementation slice. The three active
Codex Power leaves have exclusive files and must finish their own commits:

| Child | Exclusive files | Tool count | Current disposition |
| --- | --- | ---: | --- |
| `wave4_geo_reads` | `src/billy_mcp/api/geo_reads.py`, `tests/api/test_geo_reads.py` | 8 | implementation reported; await committed archive and review |
| `wave4_tax_reads` | `src/billy_mcp/api/tax_reads.py`, `tests/api/test_tax_reads.py` | 16 | active; await delivery |
| `wave4_bank_reads` | `src/billy_mcp/api/bank_reads.py`, `tests/api/test_bank_reads.py` | 10 | independent offline review passed; await committed archive |

The remaining 16 operations split into two independent leaves, rather than
being absorbed by root, because each has real typed-tool and contract-test
depth. Start a leaf only when a first-cohort worker has become terminal and a
worker slot is free. Both must be initialised with `--agent=codex-power`, own
only these files, receive the frozen brief in their `NODE.md`, and be committed
from their own worktree before launch:

| Child | Exclusive files | Tool count |
| --- | --- | ---: |
| `wave4_balance_invoice_ext_reads` | `src/billy_mcp/api/balance_invoice_ext_reads.py`, `tests/api/test_balance_invoice_ext_reads.py` | 10 |
| `wave4_ledger_user_reads` | `src/billy_mcp/api/ledger_user_reads.py`, `tests/api/test_ledger_user_reads.py` | 6 |

Use these scoped initialisations after the parent baseline is committed:

```bash
fractal node init wave4_balance_invoice_ext_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/balance_invoice_ext_reads.py --scope=tests/api/test_balance_invoice_ext_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Balance and Invoice Extension Reads'
fractal node init wave4_ledger_user_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/ledger_user_reads.py --scope=tests/api/test_ledger_user_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Ledger and User Reads'
```

Each leaf must use the existing Wave-3 pattern: flat Pydantic get/list inputs,
opaque allow-extra records, encoded IDs, `extra="forbid"`, documented query
serialization, optional `meta.paging`, typed 401 mapping, direct FastMCP
registration, and focused contract tests for path/query/roots/paging/errors.
The leaves must not touch `server.py`, coverage artifacts, generated reports,
or shared redaction. They must not invent filters; they must leave all live
flags false and `complete` false.

## Root integration sequence

1. Monitor the three active children, react to material reports, and wait for
   their self-contained commits. For each completed child, inspect the
   three-dot diff, run `git diff --check`, and validate its executable files
   from a clean `git archive` before merging with
   `git merge --no-ff -m "merge <child>" <child>`. Do not merge generated
   scratch evidence or an uncommitted delivery.
2. As each first-cohort slot frees, initialise, configure, baseline-commit,
   and start one second-cohort Codex Power leaf. Steer it to preserve the
   contract distinctions: `/users` is not the existing `/user` special;
   reminder download URLs and user contact fields require redaction; no parent
   list filters are permitted.
3. After all five modules are merged, update root-owned `server.py` imports and
   registrations, add a registry assertion for exactly 94 `api_*` plus two
   `coverage_*` tools, and run a FastMCP flat-parameter smoke check for every
   added tool.
4. Update only the 50 cited rows in
   `scripts/generate_coverage_report.py`'s
   `OFFLINE_API_IMPLEMENTATION_EVIDENCE`, with their focused test references;
   regenerate `coverage/status.json` and `coverage/report.md`. Confirm exactly
   94 implemented and contract-tested API rows, zero live/vision greens, and
   `complete: false`.
5. Extend root redaction regressions for bank identifiers, user email/phone,
   and reminder `downloadUrl` query tokens. Preserve successful structured
   tool payloads; redact only diagnostics/logging.
6. Run formatting, Ruff, Pyright, the full offline test suite, coverage
   generation/checks, dependency and credential scans, public-repository
   checks, and the intentionally fail-closed full-mode gate. Request a fresh
   Grok independent review of the merged tree before any wider claim; repair
   every finding and repeat the relevant verification.

## Acceptance boundaries

- All 50 Wave-4 tools are real typed `api_*` reads with focused tests; no stub
  or generic request/browser tool is introduced.
- Geo lists require only non-empty `countryId`; every other new list rejects
  that invented filter and `offset`.
- Coverage remains fail-closed: 94 offline implemented/contract-tested API
  rows, zero live-tested rows, zero UI/vision advancement, `complete: false`.
- No bulk request body, write tool, webhook, live qualification, UI discovery,
  raw browser evidence, credential, or sensitive test data is added.

## Post-Mortem

### Completed

- Merged the first Wave-4 cohort as three focused, typed read modules: geo (8
  tools), tax (16), and bank (10). Their 213 focused contract tests pass.
- Started the two file-disjoint second-cohort Codex Power leaves for the final
  16 frozen operations: balance/invoice extensions (10) and ledger/users (6).
- Preserved the coverage boundary: the root registry and generated evidence
  remain at 44 offline API tools until all 50 Wave-4 modules can be registered
  and evidenced together.

### Review and verification

- Independent Grok review accepted the first-cohort module quality and
  anti-false-green posture, and correctly failed product completeness and root
  Wave-4 wiring. Its durable record is
  `wiki/wave_four_first_cohort_independent_review.md`.
- Reproduced the review's registry result: 46 total tools, comprising 44
  `api_*` and two `coverage_*`; verified 213 focused first-cohort tests pass.
- The review found no required rewrite of geo, tax, or bank modules. The
  reported missing registration/evidence, second cohort, live, UI, bulk, and
  write qualification remain real blockers rather than rejected findings.

### Cleanup and next slice

- Merged deliveries were validated from clean archives and their exact child
  seed directories were pruned; no generated runtime artifacts or sensitive
  evidence were retained in the product tree.
- Finish and independently review the two active leaves, then register all 50
  Wave-4 tools, add exact focused-test evidence, regenerate coverage, and
  re-review the 94-tool offline surface. Live, UI/vision, bulk, and write rows
  remain red pending their required non-production evidence.
