# 186.39 ui geo cities not_applicable dual-session freeze

## Goal

Product research138 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved geo/reference UI
   parity rows (start with **all six `ui.parity.cities.*`**, and include peer
   families with the same dual soft-empty evidence:
   `countries`, `countryGroups`, `states`, `zipcodes`).
2. Updates `scripts/check_coverage.py` so `parity_status=not_applicable` is
   **accepted only with required evidence fields** (replace the blanket reject).
3. Updates inventory tests that currently assert
   `parity_status != "not_applicable"` for every UI row.
4. Adds a scrubbed wiki page for the freeze.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** `ui_cities_*` tools.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research138).  
Dual evidence (tmp, non-git): `tmp/research138_geo_ui_dual.json`,
`tmp/research138_geo_ui_contrast.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged).  
Design: §10.2 / ~L572 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual (`wiki/ui_annual_reports_inaccessible.md`)
where nav exists → NA rejected.

## Why this slice

1. Dual-count list maps are largely exhausted; residual progress is honest
   parity classification for API-only reference data.
2. Dual independent sessions: no geo nav labels/hrefs; candidate paths
   soft-empty (body_len 127, h1=0) **identical to nonsense paths**; known shells
   (invoices/products/transactions) have h1 + larger body.
3. Soft URL landing is not a productable shell — do not invent `ui_cities_*`.
4. `check_coverage` currently always errors on any `not_applicable` row; must
   become evidence-gated before any NA product lands.
5. API cities reads stay offline-green; API live remains `out_of_scope_by_user`;
   bulk stays red external-contract.

## Non-goals

- Shipping any `ui_cities_*` / `ui_countries_*` / geo UI FastMCP tools.
- Greening soft-empty paths as list shells.
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) or residual 405 geo writes.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding NA to `currencies` / `locales` without dual path contrast (defer).
- Dual-count `salesTaxReturns.list` / `files.list` / `users.list` (next slice).
- `complete: true`, headed browser, children (tight root ownership).
- Node finish.

## Ownership

**Root product. No children** (single-file-family ownership on generator +
checker + inventory tests + wiki). Implementation agent for EXECUTE: **codex-power**
per routing policy when spawning; if EXECUTE stays on this root process, apply
the same file edits here.

| Area | Paths |
| --- | --- |
| NA evidence applicator | `scripts/generate_coverage_report.py` — add
  `apply_ui_geo_reference_not_applicable_evidence(row, *, resource: str)` (name
  flexible) for api_parity rows whose `api_row_id` is in the dual-proved set:
  `api.cities.*`, `api.countries.*`, `api.countryGroups.*`, `api.states.*`,
  `api.zipcodes.*` (all operations: get/list/create/update/bulk_save/bulk_delete). |
| Wire in UI parity loop | Same file `build_ui_manifest` — after default red parity seed, call
  applicator when `api_row_id` matches dual-proved geo resources. **Do not**
  apply to currencies/locales this package. |
| Qualification object | Required machine-readable fields on each NA row, e.g.:
  `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`,
  `sessions=dual_independent_ephemeral`,
  `docs_etag`/`docs_md5`,
  `evidence_ref=research138_geo_ui_dual+contrast`,
  `contrast_controls=invoices,products,transactions,nonsense`. |
| Row flags | For each NA row set:
  `discovered=true`, `implemented=true`, `contract_tested=true`,
  `live_tested=true`, `vision_verified=true`,
  `parity_status=not_applicable`, `tool_name=""`,
  `vision_evidence=None` (no retained frames),
  `method_or_route` documents absence class (no equivalent mit.billy.dk workflow;
  soft-empty == nonsense; no nav),
  `cleanup` documents no records / no tool,
  `test_references` include inventory tests (+ optional offline freeze test path),
  `evidence` cites research138 dual+contrast without org names/slugs/PII. |
| check_coverage | `scripts/check_coverage.py` `ui_errors` — replace blanket
  `not_applicable requires verified UI evidence` with validation:
  NA rows must have decision accepted, evidence_code, dual sessions marker,
  five flags true, empty tool_name, vision_evidence is None, evidence string
  non-empty. Non-NA rows must **not** claim NA. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - Remove/replace global `assert all(parity_status != not_applicable)`.
  - Assert cities (+ peer geo) NA rows satisfy flags + qualification.
  - Assert annual still rejects NA and stays red.
  - Assert bulk 92 still red external-contract.
  - Assert no `ui_cities` / geo UI tool names in planned tools / server registry
    if those assertions exist; at minimum no tool_name on NA rows. |
| Wiki | New `wiki/ui_geo_cities_not_applicable.md` (scrubbed): dual nav absence,
  soft-empty vs shell contrast, NA accepted vs annual NA rejected, evidence
  codes, no credentials. Link from `wiki/_index.md` only if generator/wiki lint
  requires (prefer `wiki` CLI write conventions used by prior shells). |
| Regenerated manifests | Run generator → `coverage/ui_workflows_manifest.yaml`,
  `status.json`, `report.md` (api manifest unchanged except no accidental touch).
  Expect: `complete: false`; live/vision **38 + N** where N = greened NA rows
  (30 if all five dual-proved families × 6 ops); bulk 92 still red. |
| Server tools | **No** new FastMCP tools. Confirm `src/billy_mcp/server.py` gains
  no geo UI tools. |

## Scope matrix (product this package)

| Family | API parity ops | Dual evidence | Product |
| --- | --- | --- | --- |
| cities | 6 | yes | **NA green** |
| countries | 6 | yes | **NA green** |
| countryGroups | 6 | yes | **NA green** |
| states | 6 | yes | **NA green** |
| zipcodes | 6 | yes | **NA green** |
| currencies | 6 | nav-only, no path contrast | **defer** |
| locales | 6 | nav-only, no path contrast | **defer** |

Total NA rows this package: **30**.

## Implementation steps (EXECUTE)

1. Add geo NA applicator + constants (resource set, evidence code, docs etag/md5).
2. Wire applicator in UI parity build for the five dual-proved resources only.
3. Replace check_coverage blanket NA reject with evidence-gated accept.
4. Fix inventory tests (global anti-NA assert → selective; add NA assertions;
   keep annual/bulk invariants).
5. Wiki page (scrubbed research138 durable facts).
6. Regenerate coverage artifacts.
7. Offline verify: `bash .fractal/main.billy_complete/scripts/lint.sh` and
   offline `bash .fractal/main.billy_complete/scripts/test.sh` (or
   `BILLY_TEST_MODE=commit`); `uv run python scripts/check_coverage.py`
   **without** `--require-complete`.
8. Confirm: no secrets/PII in wiki/manifests; no geo UI tools; complete false;
   bulk external-contract still present; annual still NA-rejected.
9. No live re-login required for commit if freeze is inventory-only with
   research138 evidence refs; do **not** claim shell live tests for cities.

## Acceptance criteria

- [ ] All 30 dual-proved geo parity rows: `parity_status=not_applicable`, empty
      `tool_name`, five UI flags true, `vision_evidence is None`.
- [ ] Each carries machine-readable `GEO_UI_NO_EQUIVALENT_WORKFLOW` +
      `not_applicable_decision=accepted` + dual-session evidence refs.
- [ ] `check_coverage.py` accepts evidenced NA; still rejects NA without
      evidence fields; still rejects retained frames.
- [ ] Inventory tests pass; global ban on all NA removed or narrowed.
- [ ] `ui.discovery.annual_reports` still red; `not_applicable_decision=rejected`.
- [ ] 92 bulk rows still red external-contract `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`.
- [ ] No `ui_cities_*` (or peer geo UI) tools registered.
- [ ] `coverage/status.json` `complete: false`; live/vision counts rise by 30
      (or documented actual if counting differs); docs etag/md5 unchanged.
- [ ] Wiki page present, scrubbed, wiki lint clean for project wiki.
- [ ] Offline lint + test suite pass; no credentials in diff.

## Risks and guards

| Risk | Guard |
| --- | --- |
| Soft-empty mistaken for shell | Contrast table locked: nonsense path same class as `/cities` |
| NA on annual | Applicator never touches `annual_reports` discovery |
| Completeness false green | Do not change `coverage_is_complete` bulk zero-requirement; bulk still blocks complete |
| PII in evidence strings | No org slug, company name, or person name in wiki/manifest evidence |
| Currencies/locales overclaim | Explicitly out of scope this package |

## Review / commit

- Independent Grok review of product diff vs research138 (ACCEPT/FAIL).
- FIX-VERIFY offline only for this package (no mandatory live UI suite for NA
  freeze; live suite still green for prior shells if run optionally).
- `fractal commit` product + bookkeeping as usual. **Not** `node finish`.

## Post-mortem (fill at FIX-VERIFY / COMMIT)

- What landed:
- Deviations from plan:
- Next residual pointer:

## Post-Mortem

### Work completed

- Producted research138 dual-session geo UI absence into evidence-backed
  `not_applicable` for **30** UI parity rows (cities, countries, countryGroups,
  states, zipcodes × 6 operations).
- Added `apply_ui_geo_reference_not_applicable_evidence` and
  `GEO_UI_NOT_APPLICABLE_*` constants in `scripts/generate_coverage_report.py`.
- Replaced blanket NA reject in `scripts/check_coverage.py` with
  `not_applicable_ui_errors` (decision accepted, evidence_code, dual sessions,
  evidence_ref, five flags true, empty tool_name, no frames).
- Inventory tests + status counts: live/vision **68**; implemented/contract
  **252**; complete still **false**.
- Wiki: `wiki/ui_geo_cities_not_applicable.md` + sorted `_index` link.
- Regenerated `coverage/ui_workflows_manifest.yaml`, `status.json`, `report.md`.
- No geo UI FastMCP tools; no live API traffic; no bulk/annual greening.

### Deviations

- None material. Optional currencies/locales NA deferred as planned (no dual
  path contrast). No live dual-session pytest added (inventory freeze per plan).

### Review findings

- Independent review **ACCEPT** (`tmp/grok-review.md`); required fixes: **none**.
- Optional N1–N3 deferred (absence vision record; live dual pytest;
  currencies/locales expansion).

### Test results (FIX-VERIFY)

- `bash .fractal/main.billy_complete/scripts/lint.sh` — pass
- `bash .fractal/main.billy_complete/scripts/test.sh` — **1565 passed**, 35
  deselected
- `uv run python scripts/check_coverage.py` — pass (not `--require-complete`)
- `wiki lint` project + memory — clean
- `coverage/status.json` `complete: false` (generator-derived; not hand-set)
- BILLY_TEST_MODE=full **not** run (completion requirements not met)

### Cleanup state

- No disposable Billy records created this package (inventory-only).
- No raw frames retained in repo; `vision_evidence` null on NA rows.
- research138 dual/contrast JSON remains owner tmp only (not committed).

### Next unresolved coverage slice

1. Residual UI parity (e.g. dual-count `salesTaxReturns.list` via
   `ui_vat_declarations_list`, or further reference NA for currencies/locales
   after dual contrast).
2. annual_reports unlock (non-Upsedasse non-prod org) — stays red.
3. 92 bulk external-contract red (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`).
4. Residual offline clear writes still blocked (405 / metadata-only).
5. Not node finish until complete=true under scoped policy + ui-full suite.

