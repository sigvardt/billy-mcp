# 198.41 leftover invoice update then reverse cleanup

## Pivot

Owner `59A3A933` / `FD39FFE7` / `1F1B34F8`: six leftover tagged
invoice drafts already persist. Persist is the fresh `/invoices`
list, not a create POST id. Last execute died mid-run after
`product_name` bind landed on `0ded1e7`. Adopt that code. Do not
create a seventh invoice. Do not remake Kunde, picker, archive,
or daybook dumps.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official docs ETag `"121myuqjdm53603"` is unchanged. Do not
re-lock.

Current `_update_draft` / `_delete_draft` `goto`
`/:org/invoices/{invoice_id}/edit`. Live leftover sweep scrapes
`a[href*='/invoices/']`. Owner open is exact customer text,
ancestor `li[role=row]`, normal click.

## Goal

Prove one FastMCP update of leftover `MCP-UI-INV-6CDB396B`, then
delete all 6 drafts, 6 products, and 6 customers.

1. Offline failing fixture: update/delete preview require
   `contact_name`. Today's inputs have only `id`.
2. Offline failing fixture: `_update_draft` / `_delete_draft`
   open `/invoices` and click the exact `li[role=row]`. Today's
   `goto .../{invoice_id}/edit` is the fail.
3. Offline failing fixture: live leftover sweep names the six
   `59A3A933` customer tags and does not use href scrape as the
   only finder.
4. Smallest fix: add `contact_name` to update/delete tickets,
   open by list row, keep `unit_price > 0` and **Gem som kladde**.
5. One live FastMCP update of `MCP-UI-INV-6CDB396B` to
   `MCP-UI-INV-6CDB396B-U`. Fresh-session list proof. Then
   reverse-delete all 6 triples. Third session empty.
6. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session.

## TDD

`728BD2E4` / `59A3A933`: failing fixture first, then the
smallest fix.

1. Add an offline unit test that
   `ui_invoices_update_preview` and
   `ui_invoices_delete_preview` reject a missing or blank
   `contact_name`. Run it. Capture the fail.
2. Add an offline source test that `_update_draft` and
   `_delete_draft` mention `li[role=row]` and do not
   `goto` `/invoices/{invoice_id}/edit` as the first navigation.
   Today's source is the fail.
3. Add an offline source test that the live leftover sweep
   names `MCP-UI-INV-6CDB396B` and the other five tags, and does
   not treat `_invoice_ids` href scrape as the only finder.
4. Smallest preview fix: add `contact_name` to
   `InvoiceUpdatePreviewInput` and `InvoiceDeletePreviewInput`.
   Ticket target is that tag. Keep `id` only if a later row
   click yields a URL id. Do not persist-prove from POST.
5. Smallest open fix: unfiltered `/invoices`, exact visible
   customer text, ancestor `li[role=row]`, normal click.
   Missing row is `UI_CHANGED`. Never **Godkend**, send, or
   email.
6. Smallest live fix: update only `MCP-UI-INV-6CDB396B`
   (`line_description=MCP-UI-INV-6CDB396B-U`, `unit_price=1`).
   Fresh list must still show that customer. Then delete all 6
   drafts via the same row open, **Mere** / **Slet** /
   **Ja, slet**. Then 6 products (`ui_products_delete_*`,
   scoped `table-item`). Then 6 customers (`ui_clients_delete_*`).
   Third session: no `MCP-UI-INV` / `MCP-UI-PRD` tags.
7. Run `tests/unit/test_ui_invoices_writes.py` and the
   invoice-adjacent commit-mode unit set. Then one live
   FastMCP update plus cleanup. If the row is gone, record
   `UI_CHANGED` and stop. Do not create another invoice.
8. Update `wiki/ui_invoices_writes.md` and
   `wiki/ui_write_residual_table.md` in place. Stay red.

## Out of scope

- Coverage remaps or greening
- Official-docs re-lock
- A new invoice create
- Files or daybook writes
- New inspect dumps or archive probes
- New child nodes
- `node finish`
- Send, email, **Godkend**

## Done when

- Offline tests prove required `contact_name` and list-row open
- Live FastMCP updates one leftover draft, then restores an
  empty org, or returns `UI_CHANGED` without creating new
  records
- Honesty-16 stay red
- No headed browser, no live API, no new dumps

## Post-Mortem

Work completed: update/delete tickets require `contact_name`.
List-row open replaced `goto /invoices/{POST_id}/edit`. Live
row click reached leftover
`/invoices/03dvBZm9QHuYt8jGMM8TNw/edit`.

Deviations: execute treated a PUT request with no status as
persist. Parent `DECEA79B` showed **Enhedspris** still 1,00.
Independent review FAIL. FIX-VERIFY inverted `persist_hit` to
2xx only, dropped request-only watch lines, typed **Enhedspris**
as `2,00` with input/change/Tab, preferred **Opdater**, removed
the update POST fallback, and required execute plus live CUD
to prove a fresh **Enhedspris** 2,00 before delete.

Review findings: all independent-review fails confirmed. None
rejected. Extra miss: update used to accept a later POST as
success, which could create another invoice. That path is gone.

Test results: lint.sh passed. Commit-mode test.sh passed
(2092 passed, 82 deselected). Coverage stays red. Live 2,00
persist is still unproved. Did not run ui-full.

Cleanup state: six leftover drafts, products, and customers
remain.

Next unresolved slice: Ember save must leave **Enhedspris**
2,00 on a fresh row-open session, then reverse-delete all six
triples. Stay red.

