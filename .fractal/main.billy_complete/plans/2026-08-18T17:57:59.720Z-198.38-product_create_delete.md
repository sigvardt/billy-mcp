# 198.38 product create and hard-delete

## Pivot

Owner radio `56354201` supersedes archive-only `67CBACB6` and
research176. Product create and hard-delete work in the live UI.
Do not treat product as `interface_control_absent`. Do not wait
on an archive-as-restore answer.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official `product.name` is required. Official `productPrice.unitPrice`
is required. Official `DELETE /v2/products/:id` exists. The UI
lane must not call it. Do not re-lock docs (live ETag
`"121myuqjdm53603"`). No live API.

Current `BrowserProductSubmitter` fills name only, then
`product_persist_allowed()` returns `UI_CHANGED` because the
closed delete-chrome dump has `proved_delete_path=none`. That
gate is now wrong. Live
`tests/live/test_ui_products_writes.py` still skips on stale
`72C01DF9`. `D326FFB3` already released the slot.

Do not remake form-contract, Mere/Slet, archive, picker, or
Kunde dumps (`A337A622`, `D68E402A`, `113F1E05`). Do not click
**Opret ny**. Do not click **Arkiveret**.

## Goal

Land one tagged FastMCP product create and hard-delete:

1. Offline failing fixture: create preview without
   `unitPrice > 0` returns `VALIDATION_ERROR`.
2. Offline failing fixture: execute must not return the
   dump-gated `UI_CHANGED` once a unique delete path is bound.
3. Require `unitPrice` with `gt=0` on create preview. Bind it
   on the ticket. Fill **Enhedspris** (label first, then
   `input[name='unitPrice']`) before **Gem produkt**.
4. Add `ui_products_delete_{preview,execute}`. Preview binds
   organisation, unique tagged name, and optional id. Execute
   opens `/products` first, clicks the row that shows that
   exact tag's `data-cy=delete-icon`, confirms **Ja, slet**.
   If that icon is missing, try `/inventory` once. Both miss
   is `UI_CHANGED`. Never API-delete.
5. One live FastMCP CUD through `create_server.call_tool`.
   Tag `MCP-UI-PRD-` plus 8 hex. Independent read-back after
   create. Delete. Third session proves **Ingen produkter**.
   Live test writes `author=live_test` and
   `reviewer_verdict=pending_review` only (`C7DBE974`).
6. Stay red. Do not remap `ui.parity.products.create`. Do not
   finish.

Invoice draft CUD with this disposable product is the next
family. Do not start it in this slice.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_products_writes.py`
- `tests/live/test_ui_products_writes.py`
- `src/billy_mcp/ui_writes/products.py`
- `src/billy_mcp/ui_writes/products_delete_chrome.py`
- `wiki/ui_products_writes.md`

The seven write children are completed and merged. Spawning
would split a live proof the root must run anyway.

## TDD

`728BD2E4` / `56354201`: failing fixture first, then the
smallest fix.

1. Add an offline test in
   `tests/unit/test_ui_products_writes.py` that
   `ui_products_create_preview` without a positive `unitPrice`
   returns `VALIDATION_ERROR`. Today's
   `UiProductsCreatePreviewInput.unit_price` is optional, so
   the current preview is the first fail. Run that unit test.
   Capture the fail.
2. Add an offline test that execute with a bound actor does
   not return the dump-gated message
   "until a unique UI delete path is proved". A fake actor
   that records the request is enough. Today's
   `BrowserProductSubmitter` is the first fail.
3. Smallest model fix in `products.py`: required `unitPrice`
   with `gt=0` on create preview. Put it on
   `canonical_request`. Update the registered-property set.
4. Smallest submitter fix: drop `product_persist_allowed()` as
   the persist gate. Fill **Enhedspris** from the ticket
   before **Gem produkt**. Keep Lagermodul
   `write_path="inventory"`, pre-click **Opret produkt**,
   click **Gem produkt**. Leave default account and VAT.
5. Add delete preview/execute in the same module, same ticket
   protocol as bills. Canonical request binds `unique_tag`
   and optional `id`. Execute finds the tagged row, clicks
   `data-cy=delete-icon`, then **Ja, slet**. Register both
   tools. Offline ticket tests: preview writes nothing,
   execute consumes once, replay and wrong org fail closed.
6. Replace the live skip with
   `test_ui_products_create_delete_via_call_tool`. Cleanup
   delete even on fail. Never leave an uncleanable product.
7. After a green live CUD: leave `parity_status` open-only.
   Honesty keeps `implemented` / `live_tested` /
   `vision_verified` false. Independent review writes accept,
   then purge, then remap `tool_name`.

Offline first: existing unit tests stay green except the new
failing fixtures until the fix. Commit-mode lint and contract
stay green. Do not run live API tests.

## Files

- `tests/unit/test_ui_products_writes.py` — unpriced reject,
  dump-gate gone, delete ticket contract
- `src/billy_mcp/ui_writes/products.py` — required price,
  Enhedspris fill, delete tools
- `src/billy_mcp/ui_writes/products_delete_chrome.py` — stop
  using `product_persist_allowed` as the live persist gate
- `tests/live/test_ui_products_writes.py` — FastMCP CUD
- `wiki/ui_products_writes.md` — create + delete contract
- `wiki/ui_write_residual_table.md` — already SYNC-updated;
  keep product as `owner_proved_pending_fastmcp` until accept

Do not edit `coverage/ui_workflows_manifest.yaml` or
`scripts/generate_coverage_report.py` honesty maps.

## Constraints

- Grok only. FastMCP `call_tool` is the proof.
- Headless. No desktop window.
- No live API. No credentialed `api_*` calls.
- No send, approve, email, pay, VAT, users, tokens.
- Do not remake dumps. Do not add classify helpers.
- Stay red. Do not finish.

## Verify

1. New unit tests fail, then pass after the fix.
2. Existing product unit tests pass.
3. Commit-mode lint, type, and contract stay green.
4. Live FastMCP create, independent name read-back, delete,
   third-session **Ingen produkter**.
5. Coverage `complete` stays false. Honesty-16 stay red.

## Post-Mortem

Work completed: required `unitPrice > 0` on create preview. Deleted
the dump-gate `product_persist_allowed` check. Added
`ui_products_delete_{preview,execute}`. Shared
`require_matching_org_slug` now runs in product create and delete
before fill or click. After **Gem produkt**, a still-visible create
dialog is `UI_CHANGED` with any visible validation text.

Deviations: live persist was not proved. Independent review FAIL
was confirmed. No coverage remap. No dump remake. No API-delete.

Review findings:
- Confirmed: product actors skipped the live-slug compare. Fixed.
- Confirmed: live read-back after Gem was `NOT_FOUND`. Dialog
  leftover is now fail-closed. Persist itself is still unproved.
- Rejected: none.

Test results: `lint.sh` passed. Commit-mode `test.sh` passed:
2075 passed, 82 deselected. Live FastMCP CUD is still red. Did
not run `BILLY_TEST_MODE=full` because persist, vision, and
coverage completion are still false.

Cleanup state: no proved created product. Residual class stays
`owner_proved_pending_fastmcp`. Honesty-16 stay red.

Next unresolved coverage slice: prove live persist after **Gem
produkt** on an independent session, then delete via
`delete-icon` + **Ja, slet**, then third-session **Ingen
produkter**. Invoice draft CUD with that disposable product
follows. Stay red.

