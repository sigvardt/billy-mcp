# 186.67 ui clients update open

## Goal

Product research166 ACCEPT:

1. New read-only FastMCP UI tool **`ui_clients_update_open`** that opens an
   existing client (contact) detail surface, activates **Ret** (edit), and
   returns a typed non-PII field inventory with **name value present**.
2. Observe-only: **never** click Gem / save / permanent submit on the product
   path. Harness may seed and cleanup disposable marker clients only.
3. Green exact **`ui.parity.contacts.update`** / `api.contacts.update` only onto
   `ui_clients_update_open` (`form_open_only` / edit-open parity).
4. Keep greened `ui_clients_list`, `ui_clients_create_open`, and
   `ui_clients_get_open` unchanged (list / create form / detail get contracts).

Target math (from tip `63357a6` / product 186.66 products egress):
live/vision **167 → 168** (parity update only; no new discovery seed unless
generator requires one — prefer parity-only +1); implemented/contract
**351 → 352**; complete **false**.

Contacts data-plane egress already on tip (GET/POST/DELETE `/v2/contacts` +
countries from 186.65). **No new egress rules** unless EXECUTE network dual
proves a missing path for Ret hydrate (unlikely).

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research166)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. contacts Supports get/list/create/**update**/delete/bulk. |
| Egress | contacts GET/POST/DELETE already path_allow; products plane also open (186.66). invoices/bills GET still blocked dual — **out of this slice**. |
| Dual seed | Opret kontakt → name marker → Gem → list marker **true dual** (`research166_contacts_update_dual.json`). |
| Dual update surface | Detail path `/:org_slug/contacts/:id/customer` dual; Ret → **13** content fields dual; named includes name, country, city, street, zipcode, phone, registrationNo, ean, person_*; **`name` value = marker dual**. |
| update_chrome | true dual; read-view Gem/Ret present; Slet not on primary chrome (delete deferred). |
| Tool gap | get-open is read overview; create-open is new dialog; **no edit-open tool**; `ui.parity.contacts.update` still `discovery_required`. |
| Isolation | Update-open ≠ get-open ≠ create form_open ≠ list. Green only **contacts.update**. Leave delete/bulk/invoice_email/products.get red. |
| Cleanup | UI Slet (Mere/Slet) dual remaining_false; prefer same pattern as live harness (not page.evaluate fetch under route). |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research166_focus_dual.json`, `tmp/research166_contacts_update_dual.json`,
`tmp/research166_cleanup_report.json`.

## Non-goals

- Greening `contacts.delete` / bulk_* / special.invoice_email
- Ticketed durable UI write / Gem execute for contacts update (API writes offline)
- Replacing or dual-counting onto list / create_open / get_open tools
- Submitting Gem after Ret on the product path (observe only)
- invoices/bills path_allow or products.get this slice
- New full `browser_action: allow` on API host
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| Models + browser + server register | Root Grok EXECUTE |
| Live dual-session test + vision record | Root Grok EXECUTE |
| Coverage generator (contacts.update dual-count) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research166)

### A. Browser egress

No change expected. Keep existing contacts + products path_allows.  
Regression unit tests must still deny invoices/bills GET and invoice email POST.

### B. Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_clients_update_open` |
| lane | ui |
| kind | form_open_only / edit-open (read-only observe after Ret) |
| shell_kind | `clients_update` |
| success model | `UiClientsUpdateOpenSuccess` (Pydantic, `extra=forbid`) |
| error model | existing `ToolError` (`AUTH_*`, `UI_CHANGED`, egress/auth codes) |
| API invent | none — `api.contacts.update` already offline implemented |

### C. Success model fields (non-PII)

No org_slug, raw URL, selector secrets, or customer PII strings in success
payload (marker names stay in test-only fixtures, not model success fields):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/contacts/:id/customer` (research166 dual; freeze if SPA drifts) |
| `shell_kind` | Literal `clients_update` |
| `edit_form_open` | bool — Ret activated and edit fields visible |
| `name_field_visible` | bool |
| `name_field_has_value` | bool — name input has non-empty value (not the value text) |
| `address_or_person_fields_present` | bool — ≥1 of city/street/zipcode/phone/person_* visible |
| `country_field_present` | bool |
| `shell_markers_present` | bool — aggregate edit-ready signature (path + name valued + content fields ≥ N; freeze N from dual, research: 13 content after Ret — require **≥3** named content fields as fail-closed floor) |

Strict **reject**:

- list shell or `/clients/empty` as update
- create dialog without prior detail id path
- get-open overview without Ret (name input empty / fields not in edit mode)
- header-row false positives

### D. Navigation (observe-only product path)

1. Headless Playwright via `BrowserRuntime`; existing egress; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open clients list `https://mit.billy.dk/{slug}/clients`, settle.
4. Open an existing non-header client row/detail
   (`…/contacts/{id}/customer` class). Prefer id link; else row click.
5. Click **Ret** (button role; Danish label).
6. Success when edit form ready:
   - path remains detail class
   - `input[name=name]` visible with non-empty value
   - content field inventory meets floor
7. **Never** click Gem / Gem kontakt / Save / Slet on product path.
8. Independent second-session read-back of same edit-open signature (second
   ephemeral profile; may re-open same org’s existing row or re-seed).

### E. Empty-org / disposable live harness (not a product write tool)

Test org may be empty (research166 clients/empty before seed):

1. Live test creates uniquely named disposable client via UI create + Gem once.
2. Run update-open on that client (Ret + field asserts).
3. Cleanup: UI Slet (Mere → Slet or proven flow from research166 cleanup);
   verify marker absent on fresh list read-back dual.
4. Prefer SPA-header DELETE pattern from `test_ui_clients_get_open` if UI Slet
   flaky; do not leave residual markers.
5. Never product a public ticketed update-execute tool this slice.

### F. Coverage generator

- Shell evidence helper analogous to create/get open tools with
  `parity_of_api_update` (or existing form_open dual-count path for update).
- Dual-count **exact** `api.contacts.update` / `ui.parity.contacts.update` only.
- Do **not** dual-count get, create, list, delete, bulk.
- Assert residuals stay red: contacts.delete/bulk; special.invoice_email;
  products.get; invoices.get; bills.get; annual_reports; bulk 92.
- live/vision 167→168; implemented/contract 351→352; complete false;
  API live_tested false + `out_of_scope_by_user`.

Optional discovery seed: only if generator requires a discovery row for
update_open; prefer parity-only greening without new discovery id.

## Implementation checklist (EXECUTE)

1. **Models** — `UiClientsUpdateOpenInput` / `UiClientsUpdateOpenSuccess` in
   `src/billy_mcp/models.py` + `tests/test_models.py`.
2. **Browser** — protocol + `BrowserRuntime.ui_clients_update_open`; strict
   Ret + edit-ready helpers; never Gem; reject get-without-edit and list-only.
3. **Server** — register FastMCP tool; inject service like other clients tools.
4. **Unit tests** — success path; AUTH on login; UI_CHANGED when name missing;
   reject list-only; reject create-dialog-as-update; reject get-overview without
   Ret; server registration empty input + typed output.
5. **Live test** — `tests/live/test_ui_clients_update_open.py`: dual ephemeral
   profiles, DOM asserts, second-session read-back, vision record under node
   tmp, purge frames, no API token; disposable seed+cleanup.
6. **Coverage generator** + inventory asserts for contacts.update green math;
   list/create/get tools unchanged.
7. **Wiki** — `wiki/ui_clients_update_open_shell.md` (scrubbed) + `_index` links
   from clients list/get/create pages.
8. **Regenerate** coverage manifests/status/report; lint + offline test.sh.
9. Leave research dual JSON in owner tmp only (no secrets/PII committed).

## Acceptance

- Tool registered; offline unit + inventory tests pass.
- Live dual test passes when credentials present.
- Vision record non-sensitive + purge verified.
- Coverage: contacts.update parity green; list + create + get tools unchanged;
  contacts.delete residual red; complete=false; bulk 92 red; annual red;
  API live false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Safety rails

| Risk | Mitigation |
| --- | --- |
| Accidental contact mutation | never Gem/Slet on product path; unit assert no save click |
| Get-open false dual-count as update | require Ret + name valued + edit fields; unit reject overview-only |
| Dual-count steal onto get/create/list | generator exact `api.contacts.update` only; inventory asserts tools unchanged |
| Harness create leftover | reverse cleanup + fresh list read-back dual |
| Live API leakage | unset/forbid `BILLY_API_TOKEN`; api_token_used false |
| PII in success model | booleans + path_class only; no contact names in tool success |
| Children / wrong agent | root-only Grok; no spawn |

## Feasibility

- Uncapped budget/time/cost. Root-only. Research dual evidence on disk.
- Pattern proven by 186.65 get-open + 186.61 create-open.
- Largest risk: SPA Ret label/DOM drift or empty org requiring seed — harness
  already proven in research166.
- If edit-open still impossible after dual reconfirm, do **not** false-green;
  document blocker and leave row red.

## Done when

- Plan producted in EXECUTE and IR ACCEPT (or required fixes landed).
- FIX-VERIFY: lint + offline tests green; coverage math 167→168 / 351→352;
  residual reds as above; complete false.
- Ready COMMIT 186.67. **Not** `fractal node finish`.

## Post-Mortem

- **Product shipped:** `ui_clients_update_open` (Ret edit form open only; maps
  `api.contacts.update` / `ui.parity.contacts.update` as `form_open_only`).
- **No egress change** (contacts path_allow already on tip from 186.65).
- **Coverage math:** live/vision **167→168**; implemented/contract **351→352**;
  complete **false**.
- **Isolation:** list / create_open / get_open tools unchanged; contacts.delete
  residual red; only contacts.update dual-counted onto update tool.
- **Offline tests:** lint pass; test.sh **1615 passed** / 43 deselected
  (EXECUTE + FIX-VERIFY reconfirm).
- **Live dual:** `tests/live/test_ui_clients_update_open.py` pass; vision record
  `tmp/vision-records/ui_clients_update_open.json` accept + purge_verified.
- **IR:** ACCEPT (`tmp/grok-review.md`); required fixes **none**; optional N1–N3
  deferred (shell_markers hard-true; live assert wording; untracked plan/wiki/live).
- **complete:** false. bulk 92 + annual + products.get + invoices/bills get +
  contacts.delete + special.invoice_email residual red.
- **Next residual:** invoices/bills GET path_allow expansion (research166 dual
  ERR_BLOCKED_BY_CLIENT); or contacts.delete Mere→Slet observe; products.get
  re-probe after durable seed; no bulk greening without official schema.

