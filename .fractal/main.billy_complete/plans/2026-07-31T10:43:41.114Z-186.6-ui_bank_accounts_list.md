# 186.6 UI bank accounts list shell

## Goal

Ship the fourth **read-only** business UI FastMCP tool for the Billy bank
accounts list shell frozen in research105, with offline contract tests,
dual-session live interface tests (no API token), and a non-sensitive vision
record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research105).  
Discovery: `.fractal/main.billy_complete/tmp/discovery103/summary.json` bank-accounts
seeds + dual read-back; scrub pointer `tmp/discovery105/summary.json`; frames purged.  
Prior product patterns: tip `1f70ccf` (`ui_clients_list`), `8a0cd6f` (`ui_products_list`),
`e39a154` (`ui_invoices_list`).  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` §10.3
(Bank accounts and reconciliation still red until qualified).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be` (unchanged).

## Why this slice

- Auth lane, invoices, products, and clients list shells are producted and
  live-qualified (UI live/vision 6).
- Research103 dual headless sessions both reached bank-accounts shell:
  path class `/:org_slug/bank-accounts`, h1 `Bankkonti`, CTAs
  `Forbind til bank` and `Importer transaktioner` present (presence only);
  A↔B path/h1/title match; no error shell; no plan-gate heuristic; table_count 0.
- Official docs have **0** `bankAccounts` / `/v2/bankAccounts` mentions — this
  is **UI-only**. Do not invent `api_bank_accounts_*`.
- Inventory target still red: `ui.discovery.bank_accounts` only.
- Adjacent offline API (`bankLines` / `bankPayments` / `bankLineMatches`) stays
  offline; do **not** green any `ui.parity.bankLines.*` / payments / matches
  from this shell.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Inventing `api_bank_accounts_*` or any bankAccounts API product.
- Greening `ui.parity.bankLines.*`, `ui.parity.bankPayments.*`,
  `ui.parity.bankLineMatches.*` from this shell.
- `ui.discovery.bank_reconciliation` product or connect-bank / import workflows.
- Clicking `Forbind til bank` or `Importer transaktioner`.
- Daybooks recovery or daybooks tool.
- Residual 29 / bulk 92 API product or live method probes.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding `auth_status` to READY.
- New login-only `ui_*` tools.
- Greening all 339 UI rows or setting `complete: true`.
- Headed browsers, generic browser MCP tools, credential tool inputs.
- Children (file ownership is one tight cluster on root).

## Ownership

**Root Grok only. No children.** File cluster mirrors clients/invoices/products:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_bank_accounts_list.py` (new; pattern from clients) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` if tool-name map required |
| Coverage generator | `scripts/generate_coverage_report.py` — `apply_ui_bank_accounts_list_shell_evidence` for discovery row only |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only `ui.discovery.bank_accounts` |
| Egress | `coverage/browser_egress.yaml` — append live test path to existing
  `mit.billy.dk` / path-scoped host `test_references` only; do not widen hosts |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_bank_accounts_list_shell.md` after product |

Do **not** spawn residual/bulk, reconciliation, or daybooks children this iteration.

## Contract freeze (research105)

### Tool

- **Name:** `ui_bank_accounts_list`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  filter, credential, account number, or IBAN fields.
- **Success:** non-PII only:
  - `path_class`: literal `/:org_slug/bank-accounts`
  - `heading`: literal `Bankkonti`
  - `connect_bank_action_visible`: bool for exact CTA label **`Forbind til bank`**
    (presence only; never click)
  - `shell_markers_present`: bool for ≥1 shared shell marker
    (Overblik / Fakturering / Menu / Bank) without dumping page text
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` (error shell, wrong path, missing h1 /
  required CTA), `EGRESS_DENIED`, `BILLY_ERROR`.
  Messages must not include org slug, org name, email, password, tokens,
  account numbers, IBANs, balances, or customer values.
- **Navigation:**
  1. Open MCP profile under fixed egress (`coverage/browser_egress.yaml`).
  2. If login signature present → `AUTH_REQUIRED` (callers use auth tools first).
  3. Resolve org slug from session URL or outside-git identity file only.
  4. Goto `https://mit.billy.dk/{slug}/bank-accounts` (same wait policy as clients).
  5. Classify path/h1/CTA/shell markers; fail closed on Upsedasse / Log ind igen.
  6. Query params on real URL OK if path class remains `/:org_slug/bank-accounts`.
  7. Do **not** require table rows (discovery table_count was 0).

### Naming map

| Layer | Name |
| --- | --- |
| UI route | `/bank-accounts` |
| UI heading | `Bankkonti` |
| Primary CTA | `Forbind til bank` (do not click) |
| Secondary CTA (observe only; not required success field) | `Importer transaktioner` |
| Discovery inventory id | `ui.discovery.bank_accounts` |
| Parity inventory id | **none** (no bankAccounts API resource) |
| Offline API | **none for this tool** (do not invent) |
| FastMCP tool | `ui_bank_accounts_list` |

### Inventory honesty when greening (only after real tests pass)

For `ui.discovery.bank_accounts` only:

- `discovered` / `implemented` / `contract_tested` / `live_tested` / `vision_verified` true when earned
- `tool_name`: `ui_bank_accounts_list`
- `parity_status`: `list_shell_open_only`
- `request_fields`: `[]`
- `pagination`: `null`
- `api_row_id`: `null`
- `method_or_route`: observed path class `/:org_slug/bank-accounts`
- Evidence: research105 + dual live + vision record path
- Test refs: unit + `tests/live/test_ui_bank_accounts_list.py`
- Do **not** embed bankLines/bankPayments API schemas on this UI row
- Do **not** green `ui.parity.bankLines.*` / payments / matches
- All API rows stay `live_tested: false` with out_of_scope qualification
- `coverage/status.json` `complete` stays **false**; live/vision counts may rise from 6 → 7

## Work steps (EXECUTE order)

1. **Models** — `UiBankAccountsListInput`, `UiBankAccountsListSuccess` in `models.py`
   (mirror clients success shape with `connect_bank_action_visible` instead of create).
2. **Browser** — protocol `UiBankAccountsListService`, constants for path/h1/CTA,
   `_is_bank_accounts_list_url`, `_has_bank_accounts_list_signature`,
   `BrowserRuntime.ui_bank_accounts_list()` parallel to clients.
3. **Server** — register `ui_bank_accounts_list` tool with typed models; injectable service seam.
4. **Unit tests** — success, AUTH_REQUIRED on login, UI_CHANGED wrong path /
   missing h1 / missing CTA / error shell, query-param path tolerance,
   redaction (no org/account leakage in messages).
5. **Coverage server / inventory map** — tool name mapping for discovery row only;
   update `test_coverage_inventory.py` green-tool map if hard-coded.
6. **Coverage generator** — `apply_ui_bank_accounts_list_shell_evidence` for
   `ui.discovery.bank_accounts` only; call from apply loop next to clients.
7. **Live dual-session** — new `tests/live/test_ui_bank_accounts_list.py`:
   - Skip if credential refs missing
   - Ephemeral profiles A and B under owner data root
   - Login via product auth tools only
   - Call `ui_bank_accounts_list` on each; assert path/heading/connect_bank_action_visible
   - Capture frames outside repo; write vision record; purge; `purge_verified`
   - Never set or use `BILLY_API_TOKEN`
8. **Inventory YAML** — update only `ui.discovery.bank_accounts` when tests pass;
   regenerate status/report.
9. **Egress test_references** — add live bank-accounts test path next to clients.
10. **Wiki** — `wiki/ui_bank_accounts_list_shell.md` (non-PII contract).
11. **Offline suite before claim** — format/lint/type/unit/contract/safety as
    existing scripts require.
12. **Live + vision** — run bank-accounts live test; record vision ACCEPT with purge.

## Test plan

| Layer | Command / target | Pass bar |
| --- | --- | --- |
| Unit / models | `pytest tests/test_models.py tests/unit/test_browser.py tests/unit/test_coverage_server.py -q` | new bank-accounts cases green |
| Inventory honesty | coverage inventory tests | only discovery bank_accounts green among new rows; complete false; API live false |
| Live dual | `pytest tests/live/test_ui_bank_accounts_list.py -m live` | A↔B success + vision purge |
| Safety | existing repository policy / credential scan | no secrets/frames in repo |
| Pre-commit suite | `BILLY_TEST_MODE=commit` project scripts before `fractal commit` | green offline gates |

Do **not** run live API tests. Do not require full `BILLY_TEST_MODE=ui-full` for this slice commit, but do not claim node complete.

## Risks and fail-closed rules

| Risk | Response |
| --- | --- |
| CTA missing or renamed | `UI_CHANGED`; do not invent alternate selectors |
| Session lands on login | `AUTH_REQUIRED`; no auto-login inside list tool |
| Error shell (Upsedasse) | `UI_CHANGED` |
| Org slug missing | fail closed via existing resolve helper; no caller slug input |
| Accidental connect/import click | never click; only count/visibility |
| Greening wrong rows | touch only `ui.discovery.bank_accounts` |
| Inferring bankLines parity | forbidden without separate evidence |
| Inventing API tools from UI path | forbidden |
| Research-only greening | forbidden — tests first |

## Success criteria (slice; node still incomplete)

- Tool `ui_bank_accounts_list` registered with Pydantic I/O.
- Offline unit/contract tests pass for the tool.
- Live dual-session test passes with independent second interface session.
- Vision record exists with non-sensitive verdict + `purge_verified`.
- Inventory row `ui.discovery.bank_accounts` green only as list-shell-open.
- No `api_bank_accounts_*`; no bankLines/payments parity greened from this shell.
- Frames purged; repo clean of credentials/screenshots/HAR/traces.
- `coverage/status.json` still `complete: false`.
- Branch commits pushed via normal commit step.

## Review focus (next step)

- Contract matches research105 path/h1/CTA.
- UI-only honesty (no invented API resource).
- No API live claims; no bulk residual greening.
- Dual read-back independence proven.
- Vision not sole pass condition.
- Message redaction.
- Inventory honesty (`list_shell_open_only`, empty request_fields, null pagination).

## Management decision

No child spawn. Root implements and qualifies this file cluster. The list-shell
pattern is proven three times (invoices, products, clients); splitting adds
merge cost without ownership clarity.

## Post-Mortem

### Completed

- Implemented typed `ui_bank_accounts_list` (models, browser workflow, server registration).
- Offline unit/model/registry/inventory tests; commit-mode suite 1338 passed.
- Live dual-session test passed (EXECUTE + FIX-VERIFY reconfirm); vision record accept with purge_verified.
- Inventory greened only `ui.discovery.bank_accounts` as `list_shell_open_only`
  (empty request_fields, null pagination, api_row_id null).
- Wiki contract page `wiki/ui_bank_accounts_list_shell.md`.
- Independent review: product ACCEPT; completeness FAIL (expected).
- FIX-VERIFY: no product code fixes required; lint + offline 1338 + live reconfirm pass.

### Deviations

- None material vs plan. UI-only (no contacts-style API parity row) as designed.

### Review findings

- Blocking product issues: none.
- Non-blocking: node incomplete; bulk 92 and residual UI remain red; API live stays false with out_of_scope_by_user.

### Tests

- `bash scripts/lint.sh`: pass
- `BILLY_TEST_MODE=commit bash scripts/test.sh`: 1338 passed, 6 deselected
- Live: `pytest tests/live/test_ui_bank_accounts_list.py -m live`: 1 passed (reconfirm)
- `coverage/status.json`: complete false; live/vision 7; API live 0

### Cleanup

- No business records created (read-only shell).
- Vision frames outside repo; purge_verified true.
- No credentials or raw frames in repository.

### Next unresolved coverage slice

- Next UI discovery shells after bank accounts (e.g. quotes, financing, daybooks recovery research first).
- Residual clear API + bulk 92 remain offline-only red.
- Do not invent `api_bank_accounts_*`; do not green bankLines parity without separate evidence.
