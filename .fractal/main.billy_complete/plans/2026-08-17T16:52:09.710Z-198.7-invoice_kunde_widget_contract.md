# 198.7 Invoice Kunde widget contract dump

## Goal

Extend the invoice **Kunde** dump until it records the `51E18E60` widget
contract. First a failing unit fixture that the current live opener and
`after_type` payloads miss those keys. Then the smallest helpers that
record them. Then one live read-only recapture. Derive one next action
only from that dump. If the dump names none, a count-only watched
browser GET `/v2/contacts` after type. Stay red.

Do not treat typed-only as a bind. Do not remap invoice CUD rows.

Authority: `51E18E60`, `96908DC6`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Another click-plus-type write with no new dump keys
- Products, ledger, files, org, or bills
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Guessed selectors, generic portal sweeps, force clicks,
  `evaluate` clicks, Escape, extra keys (`Enter`, `Alt+ArrowDown`)
  unless the new dump names Tab/blur
- Copying the bills vendor wrapper onto this field
- Treating a hidden `contactId` as a bind
- Persisting screenshots, frames, HAR, or traces in git
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `68b46cc`. Memory edits from this continue are uncommitted.
Not a backstop. `complete=false`. Honesty 16 stay red. Contacts and
bills CUD rows already name preview tools. Invoice CUD rows still
name `ui_invoices_*_open`.

Live opener dump is `5E1EDFB4` complete (`missing_keys=[]`).
`named_opener=null`. Hit target is `INPUT` name `contact`.
Live `after_type` is `tag_len=19` / `value_len=19` and still
unbound: two hidden decoys, `option_role_count=0`.

Current dump does **not** record:

- `input.autocomplete_token`
- `input.list_present`
- `datalist_count`
- `datalist_option_count`
- `visible_input_count`
- `a11y_snapshot` counts
- `field_shot` flags

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/invoices_kunde.py`
- `src/billy_mcp/ui_writes/invoices_form_observe.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

Parked write-family children stay parked. Unlimited budget is not
a reason to spawn.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is a failing unit
   fixture. The current live opener and `after_type` payloads fail
   `widget_contract_missing_keys`. Run it. Capture the failure. Then
   implement.
2. **Honesty-16 stays red.** Confirm the coverage gate still
   length 16 before any other edit.
3. **Widget keys are flags and counts.** Allowlist
   `autocomplete_token` as `on` / `off` / `name` / `other` / `empty`.
   Never store the raw autocomplete string if it is not in that set.
   Never store tag, raw placeholder, snapshot name text, or pixels.
4. **One dump-named action after recapture.** Order: native
   datalist option, then a dump-named visual trigger, then one
   Tab/blur if the dump names that path, else a count-only GET
   `/v2/contacts` trace. No Enter. No guessed selector.
5. **Read-only live pass.** Open `/:org_slug/invoices/new`. Recapture
   opener at rest, after one click, and after type. Do not save a
   draft. Delete any contact this pass creates.
6. **Do not remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only. Field shot lives under
   `~/.local/share/billy-mcp/` and is purged after review.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_WIDGET_CONTRACT_KEYS` and
`widget_contract_missing_keys` in `invoices_kunde.py` **only after**
a unit fixture fails because those names do not exist, or add the
fixture first against a local helper stub that is not yet imported
from product code. Preferred shape:

- Fixture copies the current live opener `input` plus current
  `after_type` keys.
- Helper returns the missing key list:
  `autocomplete_token`, `list_present`, `datalist_count`,
  `datalist_option_count`, `visible_input_count`, `a11y_snapshot`,
  `field_shot`.
- Current payloads must fail with that exact set.

Do not edit observe/bind product files until that fixture fails.

Keep `test_typed_contact_dump_is_not_a_kunde_bind`,
`after_type_is_existing_customer_observation`, and
`test_bind_does_not_press_extra_keys_on_unbound_contact`.

### 2. Smallest helper, then live recapture

- Implement the helper in `invoices_kunde.py`.
- Extend `_OWNERSHIP_JS` / `observe_kunde_opener` / `observe_kunde`
  with the new flags. Isolate unit dumps so they cannot overwrite
  the owner files.
- `a11y_snapshot` stores counts only: control count, role counts,
  `listbox_present`. No name text.
- `field_shot` stores `present`, `bytes`, and box. Pixels stay
  outside git.
- Live path opens the new-invoice page and recaptures. Type a
  19-char `MCP-UI-INV-` fixture for chrome only if no disposable
  contact remains. Leave without **Gem som kladde**.
- After the new dump lands, take only the one action section 4 of
  the research brief names. If none, count GET `/v2/contacts`
  after type (path count only, no query string) and stop.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the missing-key rule into memory `state.md` / `todo.md`.
  No iteration labels.

### 3. Do not write a draft

This slice is dump plus at most one dump-named observation. Do
not click **Gem som kladde**. Do not persist an invoice. Do not
add PUT egress.

If observer login fails, recapture from the primary FastMCP
session and stop fail-closed.

### 4. Do not green

Honesty 16 stays red. Invoice `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. Do not call
`apply_ui_invoices_cud_preview_tool_names`.

Run honesty + new unit tests, then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline) after
the helper lands. Live recapture is the product proof, not the
offline suite.

## Done when

- Failing widget-contract fixture existed, then the helper lists
  the missing keys on the current live payloads.
- Observe records those keys. Live recapture writes a complete
  widget dump.
- Either one dump-named action ran, or a count-only GET
  `/v2/contacts` trace ran because no action was named.
- Honesty 16 still red. `complete=false`.
- Zero leftover tagged invoices or contacts. Field shots stay
  outside git and are purged after review if any were written.

## Budget

Unlimited. No children. One slice. Do not expand into other CUD
families this iteration.

## Post-Mortem

Work completed: failing widget-contract fixture first, then
`REQUIRED_WIDGET_CONTRACT_KEYS` / `widget_contract_missing_keys`,
then observe extend, then live read-only recapture. Dump named no
action. Count-only GET `/v2/contacts` is 0. Honesty 16 stay red.
No remap.

Deviations: first live dump missed the field because the page had
not settled. Wait-for-networkidle plus a short field wait fixed
that. No child spawn. No Tab. No chevron click.

Review findings: FAIL. Dump slice met `51E18E60`. Invoice CUD
still unbound. Field shot showed a chevron on the `INPUT` itself.
That is not a dump-named trigger. No draft, no persist, no
read-back, no vision accept.

Tests: honesty-16 pass. Widget unit tests pass. Live widget dump
pass. Offline `BILLY_TEST_MODE=commit` `test.sh` 1901 passed, 61
deselected. Did not run `BILLY_TEST_MODE=full` or `ui-full`.
`complete=false`.

Cleanup: field shot `inspect-live-invoices-kunde-field.png`
purged after review. Zero `billy-live-invoices-*` profiles.
Shared chrome profiles untouched. No invoice or contact created
this slice.

Next unresolved coverage slice: stay on the proved `INPUT`. The
dump named no next action and lookup count is 0. Do not invent a
caret click. Stay red until a later observation names a real bind,
then one FastMCP draft CUD with independent read-back.
