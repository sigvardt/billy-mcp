# 2.45 Wave-5j integration and Wave-5k gate

## Objective

Integrate the committed Wave-5j bank-line product only after reviewing its
committed bytes, obtain the required independent Grok product verdict, and
preserve the cited Wave-5k `bankPayments` freeze handoff without prematurely
authoring or greening the next cohort.

## Adopted state and authority

- The root has uncommitted private-state updates from Grok research45. They are
  intentional: the current cited brief is
  `.fractal/main.billy_complete/tmp/grok-research.md` and confirms the official
  documentation fingerprint remains `hsisik4g9p3603` /
  `c2efda0ee4cf9cf200e14910c5fc6996`.
- The active Codex Power recovery leaf reports its Wave-5j implementation
  checks pass, but its only committed delta is its configuration commit. Root
  must not inspect, merge, or test uncommitted child files.
- Wave-5j authority remains the accepted freeze and product-ready review:
  `wiki/wave_fivej_ticketed_writes_contract.md`,
  `wiki/wave_fivej_freeze_independent_review.md`, and
  `wiki/wave_fivej_product_ready_research_independent_review.md`.
- Research45 authorizes a future Wave-5k offline freeze for exactly
  `bankPayments` create and update. Singular delete stays red because the
  cited unauthenticated `DELETE /bankPayments/:id` response is 405
  `METHOD_NOT_ALLOWED`; bulk, UI, live, and vision remain out of scope.

## Delegation decision

Do not create another child while the recovery leaf is still finishing its
owned commit: the independent product review has to inspect the root's merged
bytes, and Wave-5k freeze authoring is explicitly gated on that review's
ACCEPT. Once the merge and root verification complete, initialize a dedicated
`wave5j_product_review` child with `--agent=grok` for the independent review.
If it accepts, initialize a separate `--agent=codex-power` Wave-5k freeze
authoring leaf from the already-cited research; do not overlap either gate.

## Execution plan

1. Commit the adopted research45 memory state before any child integration, so
   the current authoritative handoff is durable without putting scratch or raw
   evidence into git.
2. Wait for a material recovery-child commit. Inspect only its committed
   three-dot diff and archived bytes; require the frozen nine Wave-5j CUD rows,
   exactly eighteen preview/execute tools, registry 238, offline coverage 166,
   and no live/UI/bulk changes. Run the relevant root checks. Merge only a
   clean, in-scope candidate using a no-fast-forward integration commit.
3. After merging, run repository checks, coverage generation/status checks,
   `wiki lint`, and the dedicated bank-line tests. Confirm all nine rows alone
   moved to implemented and contract-tested while `complete` remains false.
4. Configure and start the independent Grok product-review leaf against those
   merged root bytes. Its deliverable is a cited ACCEPT/REJECT review page;
   acceptance is required before any Wave-5k freeze page or product tool.
5. On Grok ACCEPT, delegate the narrow Wave-5k freeze contract to Codex Power:
   POST `/bankPayments` and PUT `/bankPayments/:id`, opaque `bankPayment`
   bodies, required `bankPayments` response root, no delete/bulk/UI/live
   surface. Follow it with another independent Grok freeze review before any
   Wave-5k product work.

## Non-goals and safety boundaries

- Do not merge an uncommitted child worktree, count its local test report as a
  root acceptance, or claim completion.
- Do not register a `bankPayments` delete tool, green coverage from research,
  hard-code undocumented payload enums, alter the locked API base, or use
  credentials/browser evidence.
- Keep all research snapshots, probes, and raw artifacts in owner-only scratch;
  only sanitized durable findings belong in memory or the project wiki.

## Post-Mortem

- Planned: the root will sequence the required committed-byte integration and
  independent review gates, while preserving the already-cited next-cohort
  boundary.
