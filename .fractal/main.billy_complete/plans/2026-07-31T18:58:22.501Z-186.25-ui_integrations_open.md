# 186.25 ui integrations open soft-empty classification

## Goal

Ship the next **read-only integrations soft-empty classification** business UI
FastMCP tool for the Billy integrations route family, dual-confirmed in
research124 as path `/:org_slug/integrations` with **empty h1 / soft-empty
chrome only** (not Fordele, not a partner list, not marketing), with offline
contract tests, dual-session live interface tests (no API token), and a
non-sensitive vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research124).  
Discovery: `.fractal/main.billy_complete/tmp/discovery124/summary.json` +
`summary_dual_integrations.json`; `api_token_used: false`;
`install_connect_clicks_attempted: false`; frames purged; PII scrubbed.  
Prior product tip: addons 186.24 (`cf93096` / `4fa1976`) `ui_addons_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 Add-ons and integrations (separate inventory row from
`ui.discovery.addons`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–124).

**Research124 decision:** there is **no dedicated in-app integrations content
hub**. Canonical observe path is **`/:org_slug/integrations`** with soft-empty
chrome (empty h1, title class `[org_name] - [org_name]`, menu-only buttons).
Dashboard nav **`Udforsk integrationer`** still targets **Fordele**
(`/:org_slug/add-ons`) — already greened by `ui_addons_open`; must **not** be
success for this tool. Marketing link **Se alle vores integrationer** points at
`https://www.billy.dk/apps/` (outside browser egress allowlist) — never navigate
or click. Soft aliases that leave the integrations path class are rejects.
This product is an **honesty / soft-empty classification** shell, not a content
hub open.

## Why this slice

- UI live/vision is 26 (auth + list/import/open shells through addons).
- Research124 dual headless sessions:
  - Path **`/:org_slug/integrations`**, h1 empty, soft_empty dual true.
  - Dual `path_match` / `h1_match` / `title_match` true; soft empty match.
  - `same_shell_addons_vs_integrations: false` (Fordele stays separate).
  - `dedicated_shell_any_seed: false` across full seed set.
  - Query variants on `integrations?*` (13): all soft empty.
  - `error_either=false`, `plan_gate_either=false`, both sessions READY.
  - Se alle href host `www.billy.dk` → `/apps/` (marketing; not navigated).
  - annual_reports remains inaccessible Upsedasse — **do not green**.
  - inventory Lagermodul observed as tip only — **do not green** this product.
- No official Billy API v2 integrations/addons/apps/marketplace/webhook resource
  (docs + `coverage/api_v2_manifest.yaml` scan: 0 hits). Do **not** invent
  `api_integrations_*` / `api_addons_*` tools.
- Inventory target still red: `ui.discovery.integrations`.
- This product greens **soft-empty shell observed** for that discovery row only.
- Hard ban: never click partner/install CTAs; never open `www.billy.dk`.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Treating Fordele (`ui_addons_open` / `/:org_slug/add-ons` / h1 `Fordele`) as
  integrations success, or re-greening `ui.discovery.addons`.
- Inventing a fake partner list, marketplace, or connected-apps hub.
- Navigating or allowing egress to `www.billy.dk` / marketing apps catalog.
- Producting inventory (`Lagermodul`), settings_*, or annual_reports.
- Inventing any `api_integrations_*`, `api_addons_*`, `api_apps_*`, webhook, or
  partner OAuth tools.
- Clicking any partner CTA, Install/Connect/Enable, Se alle vores integrationer,
  access-token creation, or financing apply.
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.
- Soft aliases that leave integrations path as success.

## Ownership

**Root Grok only. No children.** File cluster mirrors addons/saft open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_integrations_open.py` (new; pattern from addons open; soft-empty success; hard-ban CTAs + marketing nav) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.integrations` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_integrations_open_shell_evidence` for **`ui.discovery.integrations`**
  only (`soft_empty_shell_observed`); wire family `integrations` in discovery
  apply path; ensure `build_browser_egress` retains prior lives + appends
  integrations open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  `mit.billy.dk` and path-scoped `api.billysbilling.com` `test_references`;
  **must not drop** prior UI live tests (addons, saft_exports, exports,
  vat_declarations, reports, transactions, daybooks, financing, bank recon,
  receipt, uploads, bank_accounts, …); do **not** add `www.billy.dk` |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_integrations_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, inventory product, settings, or
addons-repair children this iteration.

## Contract freeze (research124 integrations soft-empty)

### Tool

- **Name:** `ui_integrations_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  partner id, credential, local file path, digest, destination, or write/install
  payload.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/integrations` (optional query params allowed if
    path stays integrations; product may always report the canonical class
    without query for dual-session stability, matching prior open shells).
  - `shell_kind`: literal `soft_empty`
  - `dedicated_shell`: `false`
  - `same_shell_as_addons`: `false`
  - `heading`: empty string or omitted/absent equivalent (must **not** be
    `Fordele` or any integrations marketplace title)
  - `shell_markers_present`: bool for soft-empty chrome markers (authenticated
    app chrome without content h1 / without Fordele hub / without partner grid)
    **without** dumping page text, org names, person names, or marketing content.
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - landed on Fordele / `add-ons` path or h1 `Fordele`
  - soft aliases that leave integrations path (`integration` singular without
    trailing family rules if product requires exact `integrations`; nested
    `integrations/*` if product requires exact leaf; `apps`, `marketplace`,
    `settings/*`, Danish slug seeds)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - dedicated content shell unexpectedly appears (non-empty content h1 that
    would change the freeze — fail closed `UI_CHANGED`, do not invent success)
- **Never:** navigate `www.billy.dk`; click Se alle / Install / Connect /
  partner CTAs; treat addons hub as this tool.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Prefer direct `https://mit.billy.dk/{org_slug}/integrations`.
3. **Do not** follow nav label `Udforsk integrationer` for this tool (it opens
   Fordele / add-ons — wrong row).
4. Independent second session (fresh profile or equivalent) re-opens the same
   path and asserts path / soft-empty / not-Fordele match. **No** API read-back.

### Reject as success

| Class | Examples |
| --- | --- |
| Fordele / addons hub | `add-ons`, h1 `Fordele`, nav Udforsk integrationer target |
| Soft path aliases leaving integrations | `integration` (if product requires exact `integrations`), `apps`, `marketplace`, `plugins`, `partners` |
| Nested under integrations | `integrations/list`, `integrations/new`, `integrations/explore`, … |
| Settings nests | `settings/integrations`, `settings/apps`, `settings/connected-apps` |
| Marketing host | any navigation to `www.billy.dk` / `/apps/` |
| Sibling success shells | exports, financing, inventory Lagermodul, dashboard, products, … |

### CTA / host hard ban (tests must prove no click / no marketing nav)

- Se alle vores integrationer  
- Opret adgangsnøgle / Tilføj som betalingsmetode / Aktivér rykkerservice  
- Kom i gang / Find en bogholder / Ansøg om lån / Læs mere  
- Install / Installér / Connect / Forbind / Aktiver as action buttons  
- Any navigation whose host is not on the Billy app allowlist (`mit.billy.dk`)

Observation of marketing teaser strings is allowed only if the session remains
on mit.billy.dk soft-empty or if tests never open Fordele for this tool’s
success path. **Click count and marketing host navigation must stay 0.**

## EXECUTE steps (root)

1. **Models** — `UiIntegrationsOpenInput` (empty, forbid extra),
   `UiIntegrationsOpenSuccess` (`path_class`, `shell_kind=soft_empty`,
   `dedicated_shell=false`, `same_shell_as_addons=false`, heading empty or
   equivalent, `shell_markers_present`).
2. **Browser** — `UiIntegrationsOpenService` protocol +
   `BrowserRuntime.ui_integrations_open` implementing open + soft-empty
   classify; Fordele/`add-ons` → `UI_CHANGED`; soft-alias → `UI_CHANGED`; CTA
   never clicked; marketing host never navigated.
3. **Server** — register `ui_integrations_open` with typed input/output.
4. **Offline tests** — model validation, browser unit (mock page states for
   soft-empty success / Fordele reject / soft alias / login / error), server
   registration, coverage inventory map.
5. **Coverage generator** — `apply_ui_integrations_open_shell_evidence` for
   `ui.discovery.integrations` only; wire family; egress append without drop;
   regenerate manifests/status (**complete remains false**).
6. **Live tests** — dual-session open; DOM assert path + soft empty + not
   Fordele; independent read-back; soft alias / add-ons rejects; CTA +
   marketing host ban; no API token.
7. **Vision** — capture non-sensitive soft-empty chrome frame(s) outside repo;
   Grok vision record; purge frames; store only non-sensitive review record
   with `purge_verified`.
8. **Wiki** — `wiki/ui_integrations_open_shell.md` (contract + non-claims).
9. **Lint/type/offline suite** before REVIEW; live dual + vision before
   completeness claims for this row.

## FIX-VERIFY / COMMIT gates

- Independent Grok review ACCEPT for product slice (expected overall
  completeness FAIL).
- Egress `test_references` must still include all prior live UI tests + new
  `tests/live/test_ui_integrations_open.py`; must **not** add `www.billy.dk`.
- Coverage: live/vision 27 after product; only `ui.discovery.integrations`
  newly greened among discovery reds; addons stays green; inventory/settings/
  annual stay red.
- `fractal commit` product message only after FIX-VERIFY; not `node finish`.

## Risks

| Risk | Mitigation |
| --- | --- |
| Confusing Fordele with integrations | Unit + live reject add-ons path and h1 Fordele; never use Udforsk nav for this tool |
| Greening as content hub | parity_status `soft_empty_shell_observed`; success fields force dedicated_shell false |
| Accidental marketing / partner navigation | Hard ban; egress unchanged; live assert host stay mit.billy.dk |
| Soft alias treated as success | Strict path class `/:org_slug/integrations` |
| Egress drop of prior lives | Explicit retain list in generator tests / IR checklist |
| PII in vision frames | Scrub; purge; store only non-sensitive record |
| Transient login UI_CHANGED | Same reauth pattern as prior live shells |
| Future Billy ships a real integrations hub | Fail closed `UI_CHANGED` if unexpected content h1 appears; re-research |

## Done when (this plan)

- `ui_integrations_open` implemented, offline green, dual live green, vision
  purge_verified.
- `ui.discovery.integrations` row: discovered/implemented/contract/live/vision
  true; tool_name `ui_integrations_open`; parity `soft_empty_shell_observed`.
- No invent API; no greening of sibling red discovery rows; no addons re-write
  as integrations success.
- Branch ready for REVIEW → FIX-VERIFY → COMMIT (not finish).

## Post-mortem

### Completed
- Producted `ui_integrations_open` for `ui.discovery.integrations` only
  (models, browser, server, offline units, live dual, vision, generator, wiki).
- Path `/:org_slug/integrations`, soft_empty classification; Fordele rejected;
  no marketing egress; no partner CTA clicks.
- No invent API; inventory/settings/annual remain red; addons stays greened separately.

### Deviations
- None material vs plan 186.25 / research124.
- Optional IR nits N1/N2 (live soft-alias / network log) left as-is (non-blocking).

### Review
- Independent Grok review: **ACCEPT** product (`tmp/grok-review.md`).
- Required fixes: **none**.
- Overall completeness: **FAIL** (expected; bulk + remaining UI).

### Tests (FIX-VERIFY reconfirm)
- `lint.sh`: pass
- Offline commit suite: **1484 passed**, 25 deselected (not live/vision)
- Live: `tests/live/test_ui_integrations_open.py` **1 passed** dual + vision purge_verified
- Egress: prior lives retained + integrations live ref on mit.billy.dk and path-scoped API host
- Coverage: implemented/contract 211; live/vision 27; complete false; API live 0

### Cleanup
- Live frames purged (`purge_verified: true` on vision record).
- No disposable org records created (read-only soft-empty open).

### Next unresolved coverage slice
- RESEARCH freeze product `ui.discovery.inventory` (Lagermodul seed from research124).
- Then settings_*; `ui.discovery.annual_reports` stays red (inaccessible Upsedasse).
- Residual ambiguous bulk offline API reds; no live API.
