# 142.15 Wave-5s upload review gate and email-delivery readiness

## Goal

Land the cited research84 reconfirm and repair the newly evidenced upload
containment TOCTOU without turning a Codex fallback review into an authoritative
Grok verdict. Keep Wave-5s-C ready for a tightly scoped Codex Power product
leaf, but do not start that leaf until the repaired upload has authoritative
Grok acceptance.

## Authority and current facts

- `tmp/grok-research.md` research84 cites the unchanged official Billy API
  document (MD5 `8b94b0135c91fd15fe54ea33e088a4be`) and reconfirms only two
  offline-productable specials: nested invoice email and `invoiceDeliveries`
  create.
- The root has the merged exact-byte upload repair. Its focused archive test
  passed, and the repair leaf's Codex fallback found no scoped defect. Neither
  fact satisfies the mandatory Grok product review.
- `wiki/wave_fivesc_research84_handoff_reconfirm.md` is research only. It does
  not green coverage, unlock live/UI work, or authorize a residual clear,
  bulk, transaction, or webhook tool.
- There is no token or UI credential. The authoritative product gate remains
  unavailable because Grok authentication failed before review work; the
  policy-required Codex retry is non-authoritative.
- The fallback's local MockTransport reproduction found that the merged
  buffer-digest guard still follows an outside-root symlink substituted after
  identity revalidation when the outside bytes have the ticketed digest. This
  is a §8.4 containment blocker, not an email/delivery contract change.

## Delegation decision

The active `main.billy_complete.wave5sb_upload_product_codex_fallback_review`
leaf owns the failure report and terminal static review. A new focused
`codex-power` containment-repair leaf is required because the source/test
change is separable, security-critical, and cannot conflict with the later
email/delivery module. Wave-5s-C remains unspawned: starting it before the
upload containment repair and authoritative acceptance violates the frozen
sequencing gate. A duplicate Grok launch would repeat the known pre-edit
authentication failure, so the existing fallback record remains supplemental.

## Execute

1. Validate and retain the root research handoff and state update:
   - inspect the current diff and run `git diff --check`;
   - run `wiki update` and `wiki lint` for the project wiki and lint the node
     memory; and
   - run the repository/credential policy checks that the project scripts
     provide. Keep raw research and probe frames in `tmp/` only.
2. Retain the fallback failure report as non-authoritative security evidence;
   finish/close that reviewer after its terminal report. Run the required root
   checks and commit the research84/state/plan baseline before creating a
   repair child, so it forks a clean, reviewable parent.
3. Initialise a focused containment leaf explicitly routed to Codex Power:

   ```bash
   fractal node init wave5sb_upload_containment_fix \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --agent=codex-power \
     --max-depth=0 --max-children=0 --max-descendants=0 \
     --max-iters=8 --step-timeout=30m
   ```

   Its exclusive production ownership is
   `src/billy_mcp/api/file_upload_writes.py`; its exclusive test ownership is
   `tests/api/test_file_upload_writes.py`. It may add only a focused,
   non-sensitive fallback record or plan. It must not touch coverage state,
   generic write protocol, HTTP client, server registration, email/delivery,
   UI, bulk, or residual rows.

   The repair must acquire the path through configured-root-aware no-follow
   descriptor traversal; validate regular-file metadata from that descriptor;
   read and SHA-256-check the transport buffer from the same descriptor; then
   send only that buffer. Add a deterministic identity-to-open same-digest
   outside-root symlink substitution regression that returns `FILE_NOT_ALLOWED`
   with zero HTTP. Preserve changed-byte, ticket, raw-body/header, no-retry,
   containment, and redaction coverage. A plain path `read_bytes()` after an
   identity check is not acceptable.
4. Recheck the product gate after containment repair and its clean-archive
   verification. If a fresh Grok product audit of the repaired root becomes
   available and explicitly accepts the product, commit the root handoff first,
   then initialise the Wave-5s-C product leaf exactly as:

   ```bash
   fractal node init wave5sc_email_delivery_product \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --agent=codex-power \
     --max-depth=0 --max-children=0 --max-descendants=0 \
     --max-iters=8 --step-timeout=30m
   ```

   Configure the child before launch with exclusive ownership of a new
   invoice-email/delivery write module, its focused tests, registration,
   two coverage rows, and only its necessary test evidence. It must not edit
   the upload module, generic `WriteProtocolService`, client base/allowlist,
   invoice-log read module, residual rows, bulk rows, or UI inventory.
5. The conditional Codex product leaf must implement only these typed tool
   pairs with custom `ConfirmationStore` services:
   - `api_invoices_send_email_preview` / `api_invoices_send_email_execute` for
     `POST /invoices/{invoiceId}/emails`, ticket `target=invoiceId`, canonical
     `{email: ...}`, no destination URL;
   - `api_invoice_deliveries_create_preview` /
     `api_invoice_deliveries_create_execute` for `POST /invoiceDeliveries`,
     ticket `organization_id=invoiceDelivery.organizationId`,
     `target=invoiceId`, canonical `{invoiceDelivery: ...}`, no destination
     URL.

   Inputs must be strict and forbid extras. Delivery accepts only `gln` or
   `cvr`, requires an exact 13-digit `receiverGln` only for `gln`, and never
   invents `receiverCvr`. Email permits one contact-person target and optional
   `copyToUserId`, never a multi-recipient or webhook field. Unknown success
   envelopes must fail closed until live evidence exists.
6. Require contract tests for no-preview HTTP, exact paths and JSON envelopes,
   one-shot/expiry/replay/tool-org-target-payload tampering, GLN validation,
   invalid envelopes, high-side-effect redaction of subject/body, and no retry
   after an external-send POST. Keep both specials offline-only: no live,
   vision, UI, residual, bulk, or completeness greening. Verify child bytes
   from a clean `git archive` before the parent merge and run the full
   non-live root suite after integration.

## Gate outcomes

- First finish and merge the containment repair. If Grok remains unavailable,
  commit the research84/state/fallback/repair evidence and retain the product
  gate as blocked. The next iteration can re-evaluate availability; it must
  not infer an acceptance.
- Only after containment repair plus Grok acceptance, launch the explicitly
  routed `codex-power` Wave-5s-C child and monitor it as the sole owner of the
  two-special implementation slice.
- Any authenticated live or UI work remains separately blocked by missing
  dedicated test-organisation credentials and must stay red.
