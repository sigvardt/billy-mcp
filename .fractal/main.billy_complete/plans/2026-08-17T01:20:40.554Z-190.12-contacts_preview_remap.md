# 190.12 Point contacts CUD rows at preview tools

## Goal

Point `ui.parity.contacts.{create,update,delete}` `tool_name` at the
existing preview tools. Coverage stays red.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `93D7A063`, `EE0A0F1B`.
Research: `.fractal/main.billy_complete/tmp/grok-research.md`. Official
docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

`93D7A063` first half is already done. Durable record is
`author=independent_review`, `reviewer_verdict=accept`,
`purge_verified=true`, `run_id=3d5b151dfd5342258f8734373597f8c1`. Frame
folder is gone. Last iteration remapped before that write and had to
revert. This slice remaps only.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Removing contacts from `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or changing
  its length-16 assertion
- Changing contacts `parity_status` off open-only
- Changing discovery or get-open tool names (`ui.discovery.clients_create`,
  `ui.parity.contacts.get`, `ui_clients_*_open` shells)
- Remapping the other 13 honesty families
- Another tagged create, live CUD, or vision rewrite
- Live API token calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `6e69ff8`. Last commit is 190.11 accept+purge with mapping reverted.
Not a backstop. Tree is clean. `complete=false`. Honesty 16 stay red.

Open-shell evidence still assigns the three CUD rows to
`ui_clients_*_open`. Preview/execute twins already exist in
`src/billy_mcp/ui_writes/contacts.py` and reuse `ConfirmationStore`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds. Vision qualify tests already hold.

## Delegation

No children. Root owns this slice. The files sit on the mainline and
would collide if forked:

- `tests/coverage/test_coverage_inventory.py`
- `scripts/generate_coverage_report.py`
- `coverage/ui_workflows_manifest.yaml` (generated)
- `wiki/ui_contacts_writes.md`

`728BD2E4` child split was for write-family implementation. Those seven
families are already merged and parked. A three-row generator override
is not a new family.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is the failing
   inventory `tool_name` lock. Run it. Capture the failure. Then
   implement.
2. **Override after honesty.** `apply_ui_clients_*_open_shell_evidence`
   still writes `*_open`. Honesty must still force implemented/live/vision
   false and keep open-only status. Set preview `tool_name` only after
   `apply_ui_cud_parity_open_only_honesty`.
3. **Keep honesty 16.** Gate `EE0A0F1B` stays. Open-only status plus
   preview tool is still not green.
4. **Do not touch the accept record.** It already qualifies. Mapping
   must not self-approve evidence.

## EXECUTE

### 1. Failing tests (first edit)

Keep `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
as-is. Run it first and confirm it still passes (16 rows red).

Then edit only `tests/coverage/test_coverage_inventory.py`. Run those
assertions. Capture the failure. Do not edit product files yet.

| Case | Location | Expected today | Expected after fix |
| --- | --- | --- | --- |
| `tool_by_id` CUD map | ~1870-1873 | `ui_clients_*_open` | `ui_clients_{create,update,delete}_preview` |
| Explicit create/update/delete asserts | ~2361, 2375, 2383, 2399 | `ui_clients_*_open` | matching `*_preview` |
| Discovery create-open | ~2390 | `ui_clients_create_open` | unchanged |
| Get-open | ~2367 | `ui_clients_get_open` | unchanged |
| Honesty 16 still red | open-only gate | pass | pass |

Command for the expected failure:

`uv run pytest -q tests/coverage/test_coverage_inventory.py::test_ui_parity_and_egress_are_complete_but_visibly_red`

The first failing product assertion is the preview `tool_name` lock.

### 2. Smallest fix

- `scripts/generate_coverage_report.py`: after
  `apply_ui_cud_parity_open_only_honesty(workflows)`, set
  `ui.parity.contacts.create` → `ui_clients_create_preview`,
  `ui.parity.contacts.update` → `ui_clients_update_preview`,
  `ui.parity.contacts.delete` → `ui_clients_delete_preview`.
  Leave `parity_status` open-only. Leave the honesty set at 16.
  Do not change `UI_CLIENTS_*_OPEN_TOOL_NAME`.
- `uv run python scripts/generate_coverage_report.py` so
  `coverage/ui_workflows_manifest.yaml` matches.
- `wiki/ui_contacts_writes.md`: replace the sentence that says
  `93D7A063` keeps the rows on `ui_clients_*_open`. Say the three CUD
  rows now name preview tools and honesty still keeps them red. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.

Run the inventory tests. Then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline). Do not
claim completeness. Do not run another live CUD. Do not rewrite
`tmp/vision-records/ui_contacts_writes.json`.

### 3. Do not green

Honesty 16 stays red. Contacts `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. `vision_verified` on
the three CUD rows stays false until a later slice removes them from
the honesty set.

## Done when

- Failing preview-tool inventory tests existed, then passed.
- Three contacts CUD `tool_name` values are preview. Discovery/get-open
  stay open. Honesty gate still passes. Offline `test.sh` passes.
- Accept record and purge stay as they are. Zero `vision-tmp/run-*`.
  Zero leftover `MCP-UI-C-*`. Zero `billy-live-contacts-*`.
- No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm only the three CUD rows changed tool names,
honesty 16 is still red, the accept record was not rewritten by this
slice, and no secrets landed.

## Post-Mortem

Completed: honesty gate still passed. Inventory `tool_name` lock failed on
`ui_clients_create_open` vs preview, then passed after the generator
override. Contacts CUD rows name preview tools. Discovery and get-open
stay `*_open`. Honesty 16 stay red. `complete=false`. Accept record
untouched.

Deviation: remapping update/delete left `ui_clients_update_open` and
`ui_clients_delete_open` without a coverage row. Added
`RETAINED_OPEN_SHELL_TOOLS` in `scripts/check_coverage.py` plus a lock
test. Create-open stays on `ui.discovery.clients_create`.

Tests: lint.sh passed. Commit-mode `test.sh` 1844 passed, 59 deselected.
No live CUD. No `ui-full`. No docs re-lock.

Cleanup: zero `vision-tmp/run-*`. Vision record still
`independent_review` / `accept` / `purge_verified`.

### FIX-VERIFY

IR PASS (`tmp/grok-review.md`). No required product fix.

Rejected as non-blockers with evidence:

- Stale `test_references` / form-open evidence on the three CUD rows.
  Honest while honesty 16 keeps them red. Do not rewrite until greening.
- Honesty text still says flags stay false until preview+execute exist.
  Tools already exist; flags stay false by set membership, not by tool
  absence.
- `RETAINED_OPEN_SHELL_TOOLS` is a checker exception, not a coverage
  row. Create-open still has `ui.discovery.clients_create`. Update/delete
  open still have live tests.

Did not run `BILLY_TEST_MODE=full` or `ui-full`. Completion requirements
are not all true. Live API tests stay forbidden.

FIX-VERIFY lint.sh passed. Commit-mode `test.sh` 1844 passed, 59
deselected. Honesty 16 still red. `complete=false`.

Next unresolved coverage slice: remaining 13 honesty CUD families need
live FastMCP proof, independent read-back, and their own vision accept
before remap. Bills or products next. Ledger last.

