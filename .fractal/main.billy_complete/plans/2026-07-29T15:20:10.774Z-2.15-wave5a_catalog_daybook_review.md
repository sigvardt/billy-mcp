# 2.15 Wave-5a catalog/daybook independent review and registration gate

## Goal

Integrate no Wave-5a catalog or daybook source until an independent Grok review
accepts each committed, clean-exported candidate against the cited official
contract.  If accepted, prepare the next root-owned registration slice without
claiming live, UI, vision, bulk, or completeness coverage.

## Evidence and constraints

- Contract brief: `.fractal/main.billy_complete/tmp/grok-research.md`, whose
  cited official docs fingerprint is `hsisik4g9p3603` /
  `c2efda0ee4cf9cf200e14910c5fc6996`.
- The active catalog and daybook leaves own only their source/test pairs.  Their
  handoffs report no server or coverage modifications.
- Catalog must expose the exact MCP root `productPrice`, use `/productPrices`,
  and preserve declared `productPrices` responses for product operations.
- Daybooks use the singular `daybook`, `/daybooks`, and no additional response
  roots.  All execute tools accept only a confirmation ticket.
- The 15 CUD inventory rows remain red until all five resource modules are
  registered in one root server process and the exact test evidence is present.

## Plan

1. Wait only for committed, pushed product/test tips from
   `main.billy_complete.wave5_catalog_writes` and
   `main.billy_complete.wave5_daybook_writes`; never inspect or merge their
   uncommitted worktrees.
2. Spawn a zero-depth `--agent=grok` review leaf once both tips exist.  It owns
   `wiki/wave_fivea_catalog_daybook_independent_review.md` and must compare the
   two three-dot diffs against the root baseline, run focused clean-export
   checks, cite the official docs, and issue an explicit ACCEPT/REJECT for each
   candidate.  It must leave coverage, registration, qualification, and source
   code untouched.  If Grok cannot authenticate before it edits, retry the
   whole review with a `--agent=codex-power` fallback and retain the result as
   non-authoritative evidence.
3. In PREPARE, merge only an ACCEPTed candidate's product/test files with a
   labelled no-ff integration, rejecting child seed content and any coverage or
   registration drift.  Preserve red coverage until the combined gate passes.
4. After both candidate modules land, delegate the root registration/coverage
   slice to Codex Power with disjoint ownership of `server.py`, server tests,
   the 15 inventory rows, and coverage assertions.  Its implementation must
   instantiate exactly one confirmation store and write protocol, register all
   30 Wave-5a tools, and attach actual test references without setting
   `live_tested` or `complete`.
5. Run root formatting, lint, Pyright, non-live tests, coverage validation,
   policy checks, credential scan, and a final Grok review of the integrated
   registration before any offline-green inventory claim.

## Success boundary

This iteration's success is a review-ready, committed pair of leaf tips and a
reviewer launch or an evidenced authentication fallback.  It does not include
registration, inventory greening, live calls, UI work, bulk inference, or a
completion claim.

## Post-Mortem

- Completed: authoritative root Grok review accepted catalog tip `ba4101a`
  (product content `03f5720`) and daybook tip `0d4b0e5` for selective
  product/test merging only.
- Deviation: the first Grok child could not authenticate and produced no review
  artifact. The required Codex Power fallback began, then was stopped once the
  authoritative root Grok review became available; it supplies no verdict.
- Finding resolved: the complete child `.fractal/` seed tree is excluded from
  candidate product review and selective integration. Outside that tree, each
  candidate contains exactly its declared source/test pair.
- Verification: the root suite passed 546 tests; the review overlay passed 105
  tests. No write registration or coverage state changed.
- Unresolved slice: selectively merge the four accepted product/test files,
  then perform the root-owned registration and evidence update for the 15
  Wave-5a offline rows, followed by an independent Grok review.
