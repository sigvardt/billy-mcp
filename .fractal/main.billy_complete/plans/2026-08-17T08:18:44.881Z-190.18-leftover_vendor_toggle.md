# 190.18 Close leftover Leverandor list via search toggle

## Goal

After vendor bind, observe the one leftover Leverandør typeahead
list and close only that list through its owning search toggle.
Then prove **Gem som kladde** is unobscured and pointer-click it.
Persist one tagged draft bill through `create_server` `call_tool`.
Coverage stays red.

Last live create dump already has tag `MCP-UI-B-7085C139`, vendor
footer bind, date, amount `1,00`, and **Gem som kladde**. Save hit
is `DIV.ds-moved-with-portal`. Current code waits for every
`.ds-moved-with-portal` and Tabs once, then returns `UI_CHANGED`
`source=leftover_portal`. Parent `7E7148F6` forbids that generic
sweep. Research names the leftover as
`div.ds-dropdown-list.ds-moved-with-portal` owned by the vendor
wrapper `[data-testid=search]` toggle.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `31F6E753`,
`7E7148F6`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Accepting stale vision `run_id=3af923d562b546d597e9f156b5c3227f`
- Live API token calls, SPA `POST /v2/contacts` seed, docs re-lock
- Seeding vendor via `ui_clients_create`
- Closing every `.ds-moved-with-portal`
- Pressing Escape or Tab as the portal close
- Clicking `[data-testid=circleX]` (clears the bound vendor)
- Force-clicking **Gem som kladde**
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `3a3fdc4`. Not a backstop. `complete=false`. Honesty 16 stay
red. Bills `tool_name` stay `ui_bills_*_open`. Contacts remap
`93D7A063` stays closed.

Live create dump is a real `MCP-UI-B-7085C139` record. Save dump is
covered (`hit_target=DIV.ds-moved-with-portal`, `pointer=false`).
No POST `/v2/bills`. Footer create may have left suppliers
`MCP-UI-B-7085C139` and `MCP-UI-B-6DCF4A5C`.

Cited contract (research):

- Official belongs-to is `contact` / `contactId`
- Persist proof is browser POST 2xx `/v2/bills` only
- Leftover is the one visible vendor list
  (`ds-dropdown-list.ds-moved-with-portal`, **Ingen resultater**,
  footer `Opret "{tag}"`)
- Owning close is `[data-testid=search]` on the vendor
  `[data-testid=input-wrapper]`
- If leftover is the **Opret leverandør** modal instead, close is
  that modal **Gem** only
- Ticket pattern is already `UiWriteProtocol` / `ConfirmationStore`

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/bills_form.py`
- `src/billy_mcp/ui_writes/bills_vendor.py`
- `tests/unit/test_ui_bills_writes.py`
- `tests/live/test_ui_bills_writes.py`
- `wiki/ui_bills_writes.md`

Parked write-family children stay parked. `728BD2E4` child split was
for family implementation. Those seven families are already merged.
Closing one leftover list is not a new family.

## Design choices

1. **Honesty first (`728BD2E4`).** Confirm the 16-row open-only gate
   still fails those rows closed. Do not edit the gate.
2. **TDD.** First product edit is a failing unit: leftover inspect
   must name one visible portal (class token `ds-moved-with-portal`,
   `has_opret` / `has_empty`, owning `search` trigger). A
   wait-all-visible plus Tab is not a close. Closing every
   `.ds-moved-with-portal` is not a close. `circleX` is not a close.
   Unit dumps stay under `tmp_path`.
3. **One observed close (`7E7148F6`).** After vendor bind, dump the
   one leftover Leverandør portal. If it is the vendor list, click
   only the wrapper `[data-testid=search]` toggle. If it is the
   **Opret leverandør** modal, click only that modal **Gem**.
   Re-inspect **Gem som kladde**. Pointer-click only when the hit is
   the button. If no exact safe action, return `UI_CHANGED`
   `source=leftover_portal` and create no bill.
4. **`31F6E753` dump before click.** No submit until the create dump
   still has tag, vendor, date, amount, and **Gem som kladde**, and
   the leftover dump plus save dump show an unobscured pointer hit.
5. **Cleanup reverse.** Delete the draft bill, then leftover tagged
   suppliers. Third session proves both gone. Purge leftover
   `billy-live-bills-*` profiles. Do not touch the shared chrome
   profiles.
6. **Vision.** `author=live_test`, `reviewer_verdict=pending_review`
   only. `run_id` is the frame-dir suffix.

## Execute

1. Run honesty 16. It must stay length 16 and red.
2. Add failing unit tests for leftover inspect and for the search
   toggle as the only list close. Run them. Capture the expected
   failure.
3. Smallest product change in `bills_form.py` / `bills_vendor.py` so
   those tests pass. Stop using Tab or wait-all-visible as the close.
   Write a non-PII leftover dump (class token, flags, owning
   control). Re-inspect save after the one click.
4. One live FastMCP bills CUD. Never click save until the create
   dump, leftover dump, and save dump are live. POST create,
   **Opdater** PUT, tagged **Slet**. Independent second-session
   read-back. Third-session bill and supplier absence.
5. If the search toggle does not uncover save, stop with
   `UI_CHANGED` and the leftover dump. Do not invent another close.
6. Offline `test.sh`. Keep coverage red.

## Done when

- Honesty 16 still red, length 16
- Unit tests refuse wait-all, Tab, Escape, `circleX`, and generic
  portal sweep as the close
- Unit tests accept only the owning search toggle (or modal **Gem**)
  as the leftover close
- Live FastMCP create/update/delete of one tagged draft bill passes
  with independent read-back, **or** the leftover dump proves no
  exact safe close without another blind click
- Create dump is still a live `MCP-UI-B-*` record
- Persist is POST 2xx `/v2/bills`, or `UI_CHANGED` with leftover
  dump after the one named close
- Third session shows no bill and no leftover tagged supplier
- Vision record is `live_test` / `pending_review` only, new `run_id`
- Leftover `billy-live-bills-*` profiles are gone
- `complete=false`. Bills `tool_name` still `ui_bills_*_open`

## After this slice (not now)

Independent vision accept + purge, then remap only the three bills
CUD rows.

## Post-Mortem

Work completed:

- Honesty 16 still length 16 and red. Bills `tool_name` stay
  `ui_bills_*_open`. No remap. Official docs not re-locked.
- Leftover inspect names role, heading, and owning control.
- Close is one named action only: search toggle or visible modal
  **Gem**. Tab-as-close and wait-all-as-close are gone.
- IR FAIL on `modal_gem+search_toggle` is fixed: that follow-up is
  removed.

Deviations:

- Plan asked for a finished FastMCP CUD. Live leftover was the
  **Opret leverandør** dialog. **Gem** was clicked. Save hit stayed
  `DIV.ds-moved-with-portal`. A later live run failed vendor bind.
  No POST `/v2/bills`. No independent read-back this slice.

Review findings (`tmp/grok-review.md`): FAIL stood. Required dump
fields and one-close rule are now in code. Persist is still open.

Tests:

- Targeted leftover units plus honesty 16: 54 passed.
- Offline `BILLY_TEST_MODE=commit` `test.sh`: 1880 passed, 60
  deselected. `lint.sh` passed. Coverage still `complete: false`.
- Live CUD not re-run after the dump/close fix. Last live bind
  failed. `31F6E753` still requires a clean bill list first.

Cleanup:

- Live write profiles purged. Shared chrome profiles untouched.
- Footer create may have left tagged suppliers. Prove absence on
  the next live run.

Next unresolved coverage slice:

- Prove the bill list empty in a fresh session. Delete leftover
  `MCP-UI-B-*` suppliers if they remain. Then one live FastMCP
  draft CUD with create, leftover, and save dumps from the same
  run. Persist POST `/v2/bills` or `UI_CHANGED` with a complete
  leftover dump. Stay red. No remap.

