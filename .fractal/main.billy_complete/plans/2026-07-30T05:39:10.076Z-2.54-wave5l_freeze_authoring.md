# 2.54 Wave-5l salesTaxPayments freeze authoring

## Objective

Author the cited, offline-only freeze contract for the two documented
`salesTaxPayments` write operations. Preserve the fail-closed coverage state;
this is a contract artifact, not product implementation.

## Adopted evidence and prerequisites

- Research54 in `.fractal/main.billy_complete/tmp/grok-research.md` opens only
  this freeze-page step after the accepted Wave-5k bank-payment product.
- The official fingerprint remains ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, body 147934 bytes.
- `coverage/api_v2_manifest.yaml` keeps `api.salesTaxPayments.create` and
  `.update` discovered but red; the two bulk rows remain `ambiguous_bulk` with
  empty tool names. There is no singular-delete row.

## Execution

1. Create `wiki/wave_fivel_ticketed_writes_contract.md`, mirroring the
   accepted Wave-5k contract structure and citing the official docs, research54,
   inventory, probe rules, design, and accepted Wave-5k product prerequisite.
2. Freeze exactly four future tool names: create preview/execute and update
   preview/execute. Specify client-relative `POST /salesTaxPayments` and
   `PUT /salesTaxPayments/:id`, the opaque `salesTaxPayment` root, required
   `salesTaxPayments` success root, strict outer shapes, partial-update inner-id
   equality, and the existing shared ticket protocol.
3. Record the property boundary without inventing a wire schema: immutable
   create fields `salesTaxReturn`, `entryDate`, and `account`; readonly
   `amount` and `side`; `isVoided` only as a documented mutable candidate.
   State explicitly that its live semantics and cleanup are unproven.
4. Exclude singular delete (unauthenticated DELETE is documented 405), bulk,
   webhooks, generic controls, product registration, inventory greening, live,
   UI, vision, credentials, and later resources. Retain `complete: false` and
   the post-product target of 246 tools / 170 offline rows only as a future
   conditional.
5. Run `wiki update --path=wiki` and `wiki lint --path=wiki`; compare the page
   against research54 and the manifest before handing it to an independent Grok
   freeze review. Do not implement or test product code unless that review
   accepts the contract.

## Delegation decision

No child is warranted for this iteration: authoring one contract page from a
completed cited brief is a narrow, non-separable task. The independent Grok
freeze review is a separate gate after the page is committed.

## Expected result

One new project-wiki contract page and the already-produced research54 memory
and plan updates, with no `src/`, `tests/`, or coverage-manifest changes.

## Post-Mortem

- Completed: authored `wiki/wave_fivel_ticketed_writes_contract.md` with the
  exact two-row/four-tool surface and all fail-closed exclusions.
- Review: independent Grok freeze review ACCEPT recorded at
  `wiki/wave_fivel_freeze_independent_review.md`; no blocking findings or fixes.
- Verification: project wiki lint, repository lint, and the 1,038-test
  non-live suite passed.
- Cleanup: no product code, coverage-state changes, credentials, live calls,
  browser artifacts, or persistent organisation data were introduced.
- Next unresolved coverage slice: the independently gated
  `sales_tax_payment_writes.py` product and focused tests; live/UI/vision/bulk
  remain red.
