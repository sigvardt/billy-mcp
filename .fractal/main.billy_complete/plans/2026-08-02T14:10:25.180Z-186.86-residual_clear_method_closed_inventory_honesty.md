# 186.86 residual clear method-closed inventory honesty

## Goal

Product research186 as one **root-only inventory honesty freeze** (no FastMCP write tools, **no greening**):

Align the **29 residual clear API write rows** with [[offline_write_probe_rules]] and the research186 unauth matrix so inventory no longer advertises non-existent `*_preview` tool names.

| Subset | Count | Exact treatment |
| --- | --- | --- |
| Unauth **405** method-closed | **25** | `tool_name=""`; `qualification` block `kind=method_closed_offline`, `blocker_code=METHOD_NOT_ALLOWED_UNAUTH`, `tools_allowed=false`, `live_api=out_of_scope_by_user`, `evidence_ref=research186_residual_unauth` (+ research96 fixture parity); keep `implemented=false`, `contract_tested=false`, `live_tested=false`; `source_kind` stays `clear` (do **not** reclassify as ambiguous_bulk); evidence string cites unauth 405 + docs Supports conflict |
| `api.transactions.create` / `.update` | **2** | `tool_name=""`; `qualification` `kind=readonly_field_map_insufficient`, `blocker_code=READONLY_PROPERTY_TABLE`, unauth **401** noted but **no tools**; stay red |
| `api.transactions.delete` / `api.invoiceReminderAssociations.delete` | **2** | `tool_name=""`; `qualification` `kind=meta_delete_unqualified`, `blocker_code=META_DELETE_NOT_CLEANUP_PROOF`, unauth **200** meta-only; stay red |

### Exact 25 method-closed ids (research186)

```
api.accountNatures.create
api.accountNatures.update
api.balanceModifiers.create
api.balanceModifiers.update
api.bankPayments.delete
api.cities.create
api.cities.update
api.contactBalancePostings.create
api.contactBalancePostings.update
api.countryGroups.create
api.countryGroups.update
api.countries.create
api.countries.update
api.currencies.create
api.currencies.update
api.invoiceReminderAssociations.create
api.invoiceReminderAssociations.update
api.locales.create
api.locales.update
api.postings.create
api.postings.update
api.states.create
api.states.update
api.zipcodes.create
api.zipcodes.update
```

### Exact 4 non-405 residual ids

```
api.transactions.create
api.transactions.update
api.transactions.delete
api.invoiceReminderAssociations.delete
```

### Keep unchanged (REJECT if touched toward green)

| Family | Rule |
| --- | --- |
| Bulk API ×92 | Stay `ambiguous_bulk` + `external_contract_blocker` / empty tool_name |
| UI bulk parity ×58 | Stay discovery_required red |
| `ui.discovery.annual_reports` | Stay `ANNUAL_REPORTS_ORG_INACCESSIBLE` red; NA rejected |
| Already-green implemented/contract **470** | No demotion; no false green of residual 29 |
| Greened UI non-bulk (incl. attachments list dual-count + residual attachments NA) | No change |

### Target math (tip `381b33e` / 186.85)

| Metric | Before | After |
| --- | --- | --- |
| live/vision | 286 | **286** |
| implemented/contract | 470 | **470** |
| residual clear with non-empty planned `tool_name` and no tools | 29 | **0** |
| residual clear with empty `tool_name` + qualification | 0 | **29** |
| complete | false | **false** |

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research186)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. Supports still lists create/update (and some deletes) for residual resources. |
| Unauth matrix | `tmp/research186_residual_unauth.json`: **25×405**, **2×401** (transactions C/U), **2×200** meta delete. Matches `live_probe._RESEARCH96_RESIDUAL_OUTCOMES`. |
| Rules | [[offline_write_probe_rules]]: 405 overrides Supports for offline tools; 200 meta delete is not cleanup; 401 alone insufficient without writable field map (transactions table effectively readonly). |
| Inventory lie | Generator `standard_rows` assigns `tool_name=api_{area}_{operation}_preview` for every Supports write, including residual 29 that have **no registered tools** and stay red. Peer bulk honesty uses empty tool_name + qualification. |
| Brief | `.fractal/main.billy_complete/tmp/grok-research.md` (research186). |
| Completeness | This package does **not** move toward `complete: true`. External bulk, annual, and residual blockers remain. |

## Non-goals / safety

- No new FastMCP tools (write or UI).
- No `implemented=true` / `contract_tested=true` on residual 29.
- No ticketed write registration for 405 methods.
- No transactions create/update tools without documented non-readonly fields.
- No delete tools from unauth 200 meta-only.
- No bulk greening; no annual greening; no UI bulk NA.
- No live `BILLY_API_TOKEN`; no credentialed API qualification; no headed browser.
- No children; only `--agent=grok` if any spawn (none planned).
- API `live_tested` remains false with `out_of_scope_by_user` on green API rows; residual stay red with method-closed / map / meta-delete qualification.
- Do not convert residual clear into `ambiguous_bulk` (wrong kind; bulk is schema-unspecified; residual is method/field blocked).

## Ownership

| Area | Owner |
| --- | --- |
| Generator residual honesty post-process + frozensets | Root Grok EXECUTE |
| Inventory unit tests (29 empty tool_name + qualification classes; counts stable) | Root Grok EXECUTE |
| Wiki: residual method-closed honesty page + `_index` | Root Grok EXECUTE |
| Optional: note in `offline_write_probe_rules.md` that inventory now carries qualification | Root Grok EXECUTE |
| Regenerate coverage manifests/status | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |

## EXECUTE steps (ordered)

### 1. Generator — residual honesty helpers

File: `scripts/generate_coverage_report.py`

1. Add constants (exact frozensets) for:
   - `RESIDUAL_METHOD_CLOSED_IDS` (25)
   - `RESIDUAL_READONLY_MAP_IDS` (`api.transactions.create`, `api.transactions.update`)
   - `RESIDUAL_META_DELETE_IDS` (`api.transactions.delete`, `api.invoiceReminderAssociations.delete`)
2. Add qualification builders peer of `bulk_external_contract_qualification()`:
   - `method_closed_offline_qualification()` → kind / blocker_code / evidence_ref / live_api / tools_allowed / docs etag+md5
   - `readonly_field_map_qualification()` 
   - `meta_delete_unqualified_qualification()`
3. After `standard_rows` (or at final API row assembly), apply `apply_residual_clear_honesty(rows)`:
   - For matching ids: force `tool_name=""`, attach `qualification`, append evidence clause (research186 + gate class), keep `red_status` / no offline implementation evidence.
   - Must run **before** any path that would mark implemented from tool registration (residual must not appear in `OFFLINE_API_IMPLEMENTATION_EVIDENCE`).
4. Ensure regenerate path rewrites `coverage/api_v2_manifest.yaml` + status/report with **same** implemented/contract **470** and live/vision **286**.
5. Status blocker text may mention residual method-closed honesty in addition to bulk/annual (optional; must not claim residual green).

### 2. Inventory unit tests

File: `tests/coverage/test_coverage_inventory.py` (and any focused unit tests if generator helpers need pure unit coverage)

Assert:

| Assertion | Value |
| --- | --- |
| Residual 29 ids still present | all 29 `id`s in API ops |
| All 29 `tool_name` | `""` |
| All 29 `implemented` / `contract_tested` / `live_tested` | `false` |
| 25 method-closed | `qualification.kind == method_closed_offline`, `blocker_code == METHOD_NOT_ALLOWED_UNAUTH`, `tools_allowed is False` |
| 2 readonly-map | `qualification.kind == readonly_field_map_insufficient` |
| 2 meta-delete | `qualification.kind == meta_delete_unqualified` |
| Bulk still | 92 `ambiguous_bulk`, empty tools, external_contract_blocker |
| Annual still | red / NA rejected |
| Counts | implemented/contract **470**, live/vision **286**, complete **false** |
| No accidental green | none of residual 29 in green filter |

Peer style: `test_bulk_rows_remain_ambiguous_and_toolless`.

### 3. Wiki

Create `wiki/residual_clear_method_closed_inventory_honesty.md` (or similar name):

- Scope: inventory honesty only; not product ACCEPT; not complete.
- Cite research186 matrix + offline_write_probe_rules.
- List the three residual classes and bans on tools/greening.
- Link bulk external-contract and annual blockers as separate completeness walls.
- Update `wiki/_index.md` link row (mechanical if wiki CLI regenerates; otherwise hand-add).

Optional one-line cross-link from `wiki/offline_write_probe_rules.md` under residual examples.

### 4. Regenerate and offline verify

```bash
# regenerate inventory (project's usual generate entry — use existing script path)
python scripts/generate_coverage_report.py   # or uv run path used by lint/test hooks

# offline suite only (no live API; no ui-full required for honesty freeze)
bash .fractal/main.billy_complete/scripts/lint.sh
# focused inventory tests + offline test.sh default offline mode
```

Confirm:

- `coverage/status.json`: complete false; implemented/contract 470; live/vision 286
- Residual 29 empty tool_name + qualification
- No new `api_*` tools in server registration for residual ops

### 5. Memory + hygiene

- Update `memory/state.md` Current state for EXECUTE outcome (after EXECUTE, not PLAN fiction).
- Keep research scratch under node `tmp/` (unauth JSON, research brief); do not commit secrets.
- No disposable UI records (none created).

## Independent review checklist (post-EXECUTE)

| Check | Pass condition |
| --- | --- |
| Docs fingerprint | Still matches status official_docs |
| No greening | residual 29 all red; counts 470/286 unchanged |
| No tools | server has no residual residual_*_preview tools |
| Qualification | three kinds correct on exact id sets |
| Bulk/annual untouched | still red |
| Bans | no 405 tools, no meta-delete product, no transactions freeze without fields |
| Complete | false; not node finish |

## Explicit DEFER / REJECT

- Offline product of residual 29 tools
- UI bulk NA dual survey (needs credentialed dual; not this plan)
- annual_reports product or NA
- Bulk schema greening
- `node finish` / `complete: true`
- Live API qualification
- Children / non-grok agents

## Feasibility

| Constraint | Assessment |
| --- | --- |
| Budget | no limit — honesty freeze is small (generator + tests + wiki) |
| Credentials | not required |
| File ownership | root-only; clear |
| Risk | low if frozensets exact and implemented flags stay false |
| Completeness impact | **none** toward complete true — intentional |

## Success criteria for this iteration package

1. Plan executed with residual 29 inventory honesty applied and regenerated.
2. Offline lint + inventory/offline tests pass.
3. IR ACCEPT for honesty-only scope (not completeness).
4. Commit via fractal commit; push; **not** node finish.
5. Memory records residual honesty landed; next residual path still bulk/annual/external.

## Post-Mortem

- **Completed:** residual clear method-closed inventory honesty freeze (research186 / 186.86). Generator `apply_residual_clear_honesty` clears false planned tool names on 29 residual clear API write rows and attaches qualifications (25 method_closed_offline / 2 readonly_field_map_insufficient / 2 meta_delete_unqualified). Regenerated `coverage/api_v2_manifest.yaml`. Unit test `test_residual_clear_honesty_rows_are_toolless_and_qualified`. Wiki `residual_clear_method_closed_inventory_honesty.md` + offline_write_probe_rules note + _index.
- **Deviations:** none material. Optional IR N2 status-blocker wording not applied (non-blocking).
- **IR:** ACCEPT package (`tmp/grok-review.md`); required fixes none. Overall completeness FAIL expected.
- **Tests:** lint.sh pass (wiki+memory clean; coverage 305/345; policy pass). Offline test.sh **1667 passed** / 54 deselected (FIX-VERIFY reconfirm). No ui-full (complete false). No live API.
- **Cleanup:** no disposable UI records; no frames; no BILLY_API_TOKEN.
- **Counts:** live/vision **286**; implemented/contract **470**; complete **false**.
- **Next unresolved coverage slice:** bulk 92 external-contract red + UI bulk parity 58 red; annual_reports org_inaccessible; residual clear 29 remain intentionally red (method-closed / field-map / meta-delete). Not node finish.
