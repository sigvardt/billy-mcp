# 186.83 residual soft-empty NA families (research183)

## Goal

Product research183 ACCEPT as one root-only pure NA freeze (no new `ui_*` tool):

Freeze UI parity **`not_applicable`** for **50** exact non-bulk API ops whose
dedicated mit.billy.dk routes are soft-empty dual (body_len **138** nonsense
chrome) and must not dual-count onto greened parent shells.

### Exact freeze set (50)

| Family | Exact API ids | Count |
| --- | --- | --- |
| daybookBalanceAccounts | `api.daybookBalanceAccounts.{get,list,create,update,delete}` | 5 |
| bankLines | `api.bankLines.{get,list,create,update,delete}` | 5 |
| bankPayments | `api.bankPayments.{get,list,create,update,delete}` | 5 |
| bankLineMatches | `api.bankLineMatches.{get,list,create,update,delete}` | 5 |
| bankLineSubjectAssociations | `api.bankLineSubjectAssociations.{get,list,create,update,delete}` | 5 |
| postings | `api.postings.{get,list,create,update}` (no single delete in docs Supports) | 4 |
| salesTaxAccounts | `api.salesTaxAccounts.{get,list,create,update,delete}` | 5 |
| salesTaxRules | `api.salesTaxRules.{get,list,create,update,delete}` | 5 |
| salesTaxMetaFields | `api.salesTaxMetaFields.{get,list,create,update,delete}` | 5 |
| salesTaxPayments | `api.salesTaxPayments.{get,list,create,update}` (no single delete in Supports) | 4 |
| salesTaxReturns residual | `api.salesTaxReturns.{get,update}` only (list stays tool-green) | 2 |
| **Total** | | **50** |

Evidence: research183 dual independent headless sessions; SPA session token only;
daybook seed+cleanup dual; profiles purged; docs MD5/ETag unchanged. Soft
dedicated routes soft-empty dual. Parent contrast shells greened and unchanged.

**Dual-count bans (hard):**

| Greened parent | Do **not** map residual onto it |
| --- | --- |
| `ui_daybooks_*` / `ui_daybook_transactions_create_open` | daybookBalanceAccounts.* |
| `ui_bank_accounts_list` / `ui_bank_reconciliation_open` | bankLines.*, bankPayments.*, bankLineMatches.*, bankLineSubjectAssociations.* |
| `ui_transactions_list` / `ui_transactions_create_open` | postings.* (different API resource; Posteringer is transactions) |
| `ui_settings_vat_open` | salesTaxAccounts.*, salesTaxRules.*, salesTaxMetaFields.* |
| `ui_vat_declarations_list` | salesTaxPayments.*; salesTaxReturns.get/update (list already dual-counted) |

### Explicit DEFER / REJECT (out of this plan)

- attachments ×5 and files ×3 pure NA (Bilag greened — dual-count policy separate)
- annual_reports greening (Upsedasse dual; org_inaccessible red)
- bulk 92 external-contract greening
- any new product chrome / form / list tool
- re-product bank recon / uploads / transactions / daybooks / vat list / settings VAT
- offline clear-API tool gaps (separate lane)
- live `BILLY_API_TOKEN` / headed browser / `node finish`

Target math (tip `3b976fb` / 186.82):

- NA **+50** full-flag rows
- live/vision **228 → 278** (`74 + GEO_UI_NOT_APPLICABLE_ROW_COUNT`)
- implemented/contract **412 → 462**
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **154 → 204**
- complete **false** (bulk 92, annual, attachments/files residual)

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research183)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. Supports for all 11 families recorded in brief §1. |
| Soft routes dual | body_len **138** dual on dedicated soft paths for all freeze families; no real h1; `Opret=1` is global-nav false positive (`exact=False`), not dedicated create chrome. |
| daybookBalanceAccounts | daybook detail open dual; only Tilføj kassekladdelinje + Ingen postering valgt; Saldo/Balance/Konti **false** dual — no balance-account surface. |
| bank* | soft empty dual; bank accounts h1 Bankkonti greened; recon soft `/bank-reconciliation` empty (tool uses harvested `bank_accounts/:id/sync` — leave alone). |
| postings | soft empty dual; transactions h1 Posteringer greened for transactions.* only. |
| sales tax residual | soft empty dual for accounts/rules/meta/payments; settings + Momsangivelser greened; salesTaxReturns detail soft inputs_n **0** dual — no get/update product. |
| Cleanup | daybook DELETE 200 dual; profiles purged; `api_token_used` false dual. |
| Brief | `.fractal/main.billy_complete/tmp/grok-research.md` research183. |
| Dual | `tmp/research183_focus_dual.json`. |
| Probe | `tmp/research183_focus_probe.py` (tmp only; not required in git). |

Peer pattern: research178 accounts CRUD NA + research182 daybookTransactionLines NA
(exact ids, generator frozensets, inventory tests, wiki).

## Non-goals / safety

- No new FastMCP UI tools.
- No live `BILLY_API_TOKEN` / no credentialed API qualification calls.
- No headed browser / no desktop window.
- No bulk greening; no annual greening; no stub/skip as green.
- No dual-count steal onto greened parents listed above.
- No write submit (Gem/Bogfør/Opret/Slet/Match/Afstem).
- Frames not required for pure NA (vision_evidence null peer).
- API `live_tested` remains false with `out_of_scope_by_user`.
- No children; only `--agent=grok` if any spawn (none planned).

## Ownership

| Area | Owner |
| --- | --- |
| Generator NA freeze (exact 50 ids + count 204 + evidence applicator) | Root Grok EXECUTE |
| Inventory unit tests (count, flags, dual-count bans, bulk still red) | Root Grok EXECUTE |
| Wiki NA page(s) | Root Grok EXECUTE |
| No new live product test file | Root (inventory dual evidence only, peer products/accounts NA) |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research183)

### A. Generator (`scripts/generate_coverage_report.py`)

1. Add exact API id frozensets (never whole-resource prefixes that would NA
   greened list rows or bulk_*):

```python
GEO_UI_NOT_APPLICABLE_RESEARCH183_DAYBOOK_BALANCE_ACCOUNT_IDS = frozenset({
    "api.daybookBalanceAccounts.get",
    "api.daybookBalanceAccounts.list",
    "api.daybookBalanceAccounts.create",
    "api.daybookBalanceAccounts.update",
    "api.daybookBalanceAccounts.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_BANK_LINE_IDS = frozenset({
    "api.bankLines.get",
    "api.bankLines.list",
    "api.bankLines.create",
    "api.bankLines.update",
    "api.bankLines.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_BANK_PAYMENT_IDS = frozenset({
    "api.bankPayments.get",
    "api.bankPayments.list",
    "api.bankPayments.create",
    "api.bankPayments.update",
    "api.bankPayments.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_BANK_LINE_MATCH_IDS = frozenset({
    "api.bankLineMatches.get",
    "api.bankLineMatches.list",
    "api.bankLineMatches.create",
    "api.bankLineMatches.update",
    "api.bankLineMatches.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_BANK_LINE_SUBJECT_ASSOC_IDS = frozenset({
    "api.bankLineSubjectAssociations.get",
    "api.bankLineSubjectAssociations.list",
    "api.bankLineSubjectAssociations.create",
    "api.bankLineSubjectAssociations.update",
    "api.bankLineSubjectAssociations.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_POSTING_IDS = frozenset({
    "api.postings.get",
    "api.postings.list",
    "api.postings.create",
    "api.postings.update",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_SALES_TAX_ACCOUNT_IDS = frozenset({
    "api.salesTaxAccounts.get",
    "api.salesTaxAccounts.list",
    "api.salesTaxAccounts.create",
    "api.salesTaxAccounts.update",
    "api.salesTaxAccounts.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_SALES_TAX_RULE_IDS = frozenset({
    "api.salesTaxRules.get",
    "api.salesTaxRules.list",
    "api.salesTaxRules.create",
    "api.salesTaxRules.update",
    "api.salesTaxRules.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_SALES_TAX_META_FIELD_IDS = frozenset({
    "api.salesTaxMetaFields.get",
    "api.salesTaxMetaFields.list",
    "api.salesTaxMetaFields.create",
    "api.salesTaxMetaFields.update",
    "api.salesTaxMetaFields.delete",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_SALES_TAX_PAYMENT_IDS = frozenset({
    "api.salesTaxPayments.get",
    "api.salesTaxPayments.list",
    "api.salesTaxPayments.create",
    "api.salesTaxPayments.update",
})
GEO_UI_NOT_APPLICABLE_RESEARCH183_SALES_TAX_RETURN_IDS = frozenset({
    "api.salesTaxReturns.get",
    "api.salesTaxReturns.update",
})
```

2. Union all 50 ids into the exact-id match path used by prior research178/182
   freezes (include in `GEO_UI_NOT_APPLICABLE_API_PREFIXES` as full id strings
   **or** explicit exact-set membership — match existing style for research182).

3. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **154 → 204** (+50).

4. Extend `apply_ui_geo_reference_not_applicable_evidence` (or peer applicator)
   with research183 branches:
   - `research_id = research183`
   - `evidence_code = GEO_UI_NO_EQUIVALENT_WORKFLOW`
   - `qualification.kind = ui_not_applicable`
   - `qualification.not_applicable_decision = accepted`
   - `qualification.sessions = dual_independent_ephemeral`
   - per-family `evidence_ref` (stable strings), e.g.
     - `research183_daybook_balance_accounts_soft_empty_dual`
     - `research183_bank_lines_soft_empty_dual`
     - `research183_bank_payments_soft_empty_dual`
     - `research183_bank_line_matches_soft_empty_dual`
     - `research183_bank_line_subject_associations_soft_empty_dual`
     - `research183_postings_soft_empty_dual`
     - `research183_sales_tax_accounts_soft_empty_dual`
     - `research183_sales_tax_rules_soft_empty_dual`
     - `research183_sales_tax_meta_fields_soft_empty_dual`
     - `research183_sales_tax_payments_soft_empty_dual`
     - `research183_sales_tax_returns_get_update_soft_empty_dual`
   - contrast_controls listing greened parents + nonsense soft baseline
   - evidence prose: soft-empty dual body_len 138; no dedicated h1/form;
     dual-count ban named; docs etag/md5; no tool_name
   - `docs_etag` / `docs_md5` from research183
   - all five flags true; `vision_evidence` null; `parity_status=not_applicable`

5. **Must not** change applicators for greened parents:
   - bank accounts list, bank reconciliation open
   - transactions list/create
   - daybooks / daybookTransactions create
   - uploads Bilag / special.files_upload
   - vat declarations list / settings VAT
   - salesTaxReturns.list dual-count

6. **Must not** NA any `*.bulk_save` / `*.bulk_delete` or attachments/files/annual.

### B. Flags per NA row

| Field | Value |
| --- | --- |
| `parity_status` | `not_applicable` |
| `tool_name` | empty |
| discovered / implemented / contract_tested / live_tested / vision_verified | all **true** |
| `vision_evidence` | null |
| `qualification.kind` | `ui_not_applicable` |
| `qualification.not_applicable_decision` | `accepted` |
| `qualification.sessions` | `dual_independent_ephemeral` |
| `qualification.evidence_ref` | contains `research183` |
| `evidence_code` | `GEO_UI_NO_EQUIVALENT_WORKFLOW` |

### C. Tests (`tests/coverage/test_coverage_inventory.py`)

1. Assert `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 204`.
2. Assert live/vision formula still `74 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **278**.
3. Assert `status.json` live_tested_rows / vision_verified_rows **278**;
   implemented/contract **462** (or regenerated values consistent with generator).
4. Dedicated block: all 50 research183 ids NA with research183 evidence_ref and
   accepted decision; tool_name empty; vision_evidence null.
5. Dual-count ban assertions:
   - `ui.parity.salesTaxReturns.list` remains list_shell on `ui_vat_declarations_list` (not NA)
   - `ui.parity.transactions.list` / `.create` remain greened tools (not NA)
   - bank discovery / recon discovery remain greened
   - daybookTransactionLines still NA from research182 (unchanged)
6. Bulk bank/postings/salesTax* rows remain discovery_required / external-contract
   red (not NA).
7. attachments/files/annual still not accepted NA.
8. Run generator regenerate + offline inventory tests; no greening of bulk/annual.

### D. Wiki

One or few scrubbed pages under `wiki/` (peer accounts/daybook lines NA style):

1. Prefer one multi-family page
   `wiki/ui_residual_soft_empty_not_applicable_research183.md`
   **or** split by family if wiki style requires shorter pages:
   - daybook balance accounts
   - bank lines/payments/matches/assoc
   - postings
   - sales tax accounts/rules/meta/payments
   - sales tax returns get/update

Each must include: dual soft-empty evidence summary, dual-count bans, GEO code,
docs etag/md5, research183 dual ref, no credentials/secrets, no iteration-only
noise in durable prose beyond research id.

Update `wiki/_index.md` links if the project wiki expects them (follow peer NA
pages).

### E. Coverage regenerate + status

1. `uv run python scripts/generate_coverage_report.py` (or project’s standard
   regenerate path used in EXECUTE).
2. Confirm `coverage/status.json`: complete **false**; blocker still bulk 92 +
   annual + residual attachments/files; live/vision **278**; implemented/contract
   **462**.
3. `coverage/report.md` regenerated; no red on the 50 freeze rows.

### F. Optional harness

No new live UI test file required (peer accounts/products NA packages used
inventory dual only). Do **not** add live product tests for NA rows.

## EXECUTE checklist (order)

1. Implement generator frozensets + count + evidence applicator only.
2. Add/extend inventory unit tests for research183 freeze + dual-count bans.
3. Write wiki NA page(s).
4. Regenerate coverage manifests + status.
5. Run lint/type/offline tests (`scripts/lint.sh` if present; offline pytest).
6. Do **not** run live API tests; no ui-full completeness claim.
7. Self IR then FIX-VERIFY then COMMIT.

## IR acceptance criteria

ACCEPT only if:

- Exactly the 50 ids are NA with research183 dual evidence; no bulk/annual/attachments/files greened.
- Greened parents unchanged (tool names, parity, live tests).
- GEO count 204; live/vision 278; complete false.
- Offline suite + inventory tests pass; no secrets in wiki/repo.
- Dual-count bans explicit in evidence text.

REJECT if any dual-count steal, bulk NA, annual NA, new stub tool, or live API.

## Post-mortem placeholders (fill after EXECUTE)

- What landed:
- Counts after:
- Deviations from plan:
- Follow-ups (attachments/files dual-count policy; annual; bulk 92):

## Budget / spawn

- Cost/time: no limit — still root-only; do not spawn children (ownership is one
  generator module + inventory tests + wiki).
- Management: pure root EXECUTE; no fractal node init.

## Done when

Generator freeze + tests + wiki + regenerated coverage committed on
`main.billy_complete` via `fractal commit`. Complete remains false. Not
`node finish`.

## Post-Mortem

### Work completed
- Pure NA freeze research183: **50** exact non-bulk UI parity rows
  (`daybookBalanceAccounts`, `bankLines`/`bankPayments`/`bankLineMatches`/
  `bankLineSubjectAssociations`, `postings`, `salesTaxAccounts`/`Rules`/
  `MetaFields`/`Payments`, `salesTaxReturns.get|update`).
- Generator: `GEO_UI_NOT_APPLICABLE_RESEARCH183_IDS` + family map + count
  **154→204**; evidence applicator with dual-count bans.
- Inventory tests: dedicated research183 freeze + updated related-shell and
  salesTaxReturns residual assertions.
- Wiki: `wiki/ui_residual_soft_empty_not_applicable_research183.md` + index.
- Coverage regenerated: live/vision **278**, implemented/contract **462**,
  GEO NA **204**, complete **false**.

### Deviations
- None material. IR optional N1 (probe Upsedasse soft-empty labeling) deferred;
  product correctly keeps annual NA rejected.

### Review findings
- Package **ACCEPT** (`tmp/grok-review.md`). Required fixes: none.
- Completeness **FAIL** expected (bulk 92, annual, attachments/files).

### Test results
- `lint.sh`: pass (format, ruff, pyright, coverage inventory, repo policy).
- `test.sh` offline: **1664 passed** / 54 deselected.
- `check_coverage.py`: pass (305 API / 345 UI).
- No ui-full / BILLY_TEST_MODE=full (completion requirements not met).

### Cleanup state
- Research dual daybook seed deleted dual; profiles purged; no API token used.
- No live product writes; no frames/HAR in repo.

### Next unresolved coverage slice
- attachments ×5 + files ×3 dual-count policy vs Bilag (`ui_uploads_list`)
- annual_reports org inaccessible red
- bulk 92 external-contract freeze (docs-only)
- offline clear-API tool gaps (separate lane)
