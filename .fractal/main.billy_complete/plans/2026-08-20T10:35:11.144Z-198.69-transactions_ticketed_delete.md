# 198.69 transactions ticketed delete

## Pivot

`7B947636` is binding under `9B979A05`. HEAD `da833d3` is a clean
association-delete close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-7b947636.html`,
`billy-api-docs-chunk-research-7b947636.js`,
`research-7b947636-transactions-section.html`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the association-delete lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2transactions`.
Supports: `get by id, list, create, update, bulk save, delete,
bulk delete`. Singular delete **is listed**. Property table is
exactly ten rows, all immutable or readonly. No delete-body
table.

Intro `#deleting-a-record`: `DELETE /v2/invoices/:id`. The request
should not have a body. DELETE is idempotent; a missing id still
returns `200 OK`. That narrative explains the historical unauth
meta-only 200. It is **not** cleanup proof and must not keep this
row red (`7B947636`, wiki rule 3).

Unauth missing-id DELETE 200 must not override Supports delete.
The all-readonly property table blocks create/update only
(`A485F530`). Residual honesty that left this row toolless is
superseded for singular delete only. Bulk stays
`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not invent bulk tools.
Do not add `api_transactions_create_*` or
`api_transactions_update_*`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_transactions_delete_*` tools. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.transactions.delete`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.transactions.delete` | `api_transactions_delete_preview` / `_execute` | `DELETE /transactions/:id` | non-empty `id` only; no JSON body |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 4 method-closed + 2 readonly
map = 6. Meta-delete set becomes empty. Bulk 92 stay red.
Implemented 546, contract 551 (HEAD 545 / 550). Live/vision stays
339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds DELETE with `json_body=None`,
canonical request `{id}`, and an encoded path. Sibling products
are `src/billy_mcp/api/bank_payment_writes.py`
(`api_bank_payments_delete_*`) and
`src/billy_mcp/api/invoice_reminder_association_writes.py`
(`api_invoice_reminder_associations_delete_*` on `da833d3`). New
delete-only module. Do not extend `ledger_user_reads.py` or
`ui_writes/ledger.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_transaction_delete.py` with a local
   `make_server` / `call_tool` that registers only
   `register_transaction_write_tools`:
   - Isolated register count is 2. Preview fields:
     `api_transactions_delete_preview` `{id}`,
     `api_transactions_delete_execute` `{confirmation_ticket}`.
     `id` and `confirmation_ticket` have `minLength` 1.
     `additionalProperties` is false. No `input` wrapper. No
     nested `transaction` on delete preview. No create/update
     tools on this server.
   - Outer extras and empty `id` fail `ValidationError` on
     `TransactionDeletePreviewInput`. Execute extras fail on
     `WriteExecuteInput`. Preview still makes no HTTP.
   - FastMCP `call_tool` with empty `id`, extra keys, or a nested
     `transaction` payload on delete preview raises FastMCP
     validation. No HTTP.
   - Preview binds canonical request `{"id": "txn-1"}` and
     expected effect `{action: delete, resource: transaction,
     id: txn-1}`. Preview makes no HTTP.
2. New `tests/api/test_transaction_delete_tickets.py`:
   - Execute DELETEs
     `https://api.billysbilling.com/v2/transactions/<urlencoded id>`
     with `request.content == b""`. Encoded id `txn /?` becomes
     `/v2/transactions/txn%20%2F%3F`.
   - Success fixture
     `{"meta": {"deletedRecords": {"transactions": ["txn /?"]}}}`
     maps `changed_records={}` and
     `deleted_records={"transactions": ["txn /?"]}`. Do not expose
     a top-level `transaction` write success field. Missing plural
     root is allowed on DELETE.
   - Tamper, replay, expiry, missing auth (`token=""`), and failed
     write not retried. Preview and missing-auth execute make no
     network.
   - Wrong executor without adding transactions create: the ticket
     harness also registers `register_bank_payment_write_tools` on
     the same `WriteProtocolService`. A transactions ticket on
     `api_bank_payments_delete_execute` (and the reverse) returns
     `CONFIRMATION_MISMATCH` and makes no HTTP.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(meta_delete) == 0`, residual `== 6`. Assert
   `api.transactions.delete` is **absent** from
   `RESIDUAL_META_DELETE_IDS`. Keep the 4 method-closed ids and the
   2 readonly-map ids. Bump every hardcoded
   `implemented_rows == 545` / `contract_tested_rows == 550` in
   that file to **546 / 551**. `complete` stays false. Live/vision
   counts stay 339.
4. Map the id in `OPERATION_TEST_REFERENCES` after
   `api.transactions.list`. On HEAD `tool_name` is empty and the
   row is meta-delete unqualified; the new mapping should fail
   until honesty lets the preview name through.
5. Add
   `test_transactions_delete_is_ticketed_offline_and_not_live_tested`
   next to
   `test_invoice_reminder_associations_delete_is_ticketed_offline_and_not_live_tested`.
   Assert `tool_name == api_transactions_delete_preview`,
   `method_or_route == DELETE /v2/transactions/:id`,
   `request_fields == ["id"]`, `implemented` and `contract_tested`
   true, `live_tested` false,
   `qualification.live_api == out_of_scope_by_user`,
   `qualification.kind != meta_delete_unqualified`.
6. `tests/unit/test_coverage_server.py`: add
   `TRANSACTION_DELETE_API_TOOL_NAMES` with the two delete tools
   (same shape as `BANK_PAYMENT_DELETE_API_TOOL_NAMES`). Bump
   `len(api_tool_names) == 315` to **317**. Include the frozenset
   in the `tool_names` union. `tests/coverage/test_api_live_qualification_semantics.py`:
   bump `len(implemented) == 206` to **207**.

Keep each new test file under 250 lines.

Run:

```text
uv run pytest tests/api/test_transaction_delete.py tests/api/test_transaction_delete_tickets.py tests/coverage/test_coverage_inventory.py tests/unit/test_coverage_server.py tests/coverage/test_api_live_qualification_semantics.py -q --tb=line
```

Capture the first red.

## Implement

New `src/billy_mcp/api/transaction_writes.py` (delete only, well
under 250 lines):

- `TransactionDeletePreviewInput`: `id: str = Field(min_length=1)`,
  `extra=forbid`, frozen. No nested payload.
- Preview: `api_transactions_delete_preview(id)`.
- Execute: `api_transactions_delete_execute(confirmation_ticket)`.
- Spec: `WriteMethod.DELETE`, `payload=None`,
  `collection_path="/transactions"`, `singular_root="transaction"`,
  `plural_root="transactions"`, `organization_id=None`, expected
  effect `{action: delete, resource: transaction, id}`.
- Register both tools from `src/billy_mcp/server.py` next to
  `register_invoice_reminder_association_write_tools`. Reuse
  `WriteProtocolService`. Do not add a second protocol.

`scripts/generate_coverage_report.py`:

- Remove `api.transactions.delete` from `RESIDUAL_META_DELETE_IDS`
  so the set is empty.
- Add `OPERATION_TEST_REFERENCES` for the delete id, pointing at
  the new delete test files plus `SERVER_REGISTRY_TEST_REFERENCE`.
- Do not touch bulk helpers, the 4 method-closed ids, or the
  readonly map (`api.transactions.create` / `.update`).
- Keep `RESOURCE_SUPPORTS` `("transactions", True, True, True)`.
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

`live_probe.py` may still list this id as unauth 200
`METADATA_ONLY_UNQUALIFIED`. Leave that historical matrix. It is
not the contract.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  residual honesty remaining is 6. This row is a ticketed offline
  tool (`api_transactions_delete_preview`). Meta-delete remaining
  is none. Not completeness.
- `wiki/offline_write_probe_rules.md`: confirm `transactions`
  singular delete follows `bankPayments.delete` /
  `invoiceReminderAssociations.delete`. Historical unauth 200 is
  not the contract. Update the confirmed-examples row that still
  says transactions DELETE 200 plus "do not freeze from Supports
  alone" so delete is the ticketed exception and create/update
  stay readonly-map.

Keep research181 UI NA pages as history. Do not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.transactions.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- Do not add create/update tools (`A485F530`).
- Do not start `contactBalancePostings` / `postings` writes.
- Do not accept a JSON delete body, `{}`, or a nested
  `transaction` on delete preview.
- Do not reuse `ui_transactions_create_preview` /
  `ui_transactions_create_execute` as API delete proof.
- `complete` stays false.
- Saved `9B979A05` still owns the remaining 6 residual rows and
  bulk92. Saved `96908DC6` still owns overall finish. Keep
  `7B947636` saved until COMMIT, then unsave and start
  `A485F530`.

## Independent review

After EXECUTE, review must re-parse `#v2transactions` on the
locked HTML/chunk and confirm:

1. Supports still lists singular delete (and create, update, bulk
   save, bulk delete).
2. Property table is still ten readonly/immutable fields. Delete
   has no body table.
3. Tools exist as preview plus execute for **delete only**. No
   create/update API tools. No bulk tools. No UI delete tools.
4. Inventory `tool_name` for delete is
   `api_transactions_delete_preview`.
5. Execute path is `DELETE /transactions/:id` with encoded id and
   empty body.
6. Preview input is `{id}` only. Empty id and extras fail.
7. `live_tested` is false.
8. Remaining residual honesty is 6 toolless rows. Meta-delete
   count is 0.
9. No 200-meta-only green. No bulk tools. No
   `api.transactions.create` / `.update` work in this slice.

## Post-Mortem

Work completed: ticketed offline `api.transactions.delete` in
`src/billy_mcp/api/transaction_writes.py`. Preview takes a
non-empty `id` with `extra=forbid`. Execute takes
`confirmation_ticket` only and sends `DELETE /transactions/:id`
with an encoded id and no JSON body. Empty id, extras, and a
nested `transaction` payload fail at the FastMCP boundary.
Coverage regenerated: implemented 546, contract 551, live/vision
339, complete false. Residual honesty remaining 6 (4
method-closed + 2 readonly-map). Meta-delete remaining is none.

Deviations: none from official Supports delete or the intro
no-body rule. Registry count asserts bumped (`api` tools 317,
implemented API rows 207). Wrong-executor tests use bank-payment
delete on the same harness instead of inventing transactions
create. Delete contract tests live in
`tests/api/test_transaction_delete.py` and
`tests/api/test_transaction_delete_tickets.py` so each test file
stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
`test.sh` `2429 passed, 83 deselected`. No live UI records.
Next unresolved slice after COMMIT: saved `A485F530`
research-only the six readonly-map create/update rows. Residual
honesty remaining 6. Bulk 92 stay red. `complete` stays false.

