# 186.59 ui taxRates list dual-count via settings vat shell

## Goal

Dual-count green **`ui.parity.taxRates.list`** by mapping the already qualified
read-only **`ui_settings_vat_open`** Indstillinger **Momssatser** panel shell to
**`api.taxRates.list`**, using the same empty-input `shell_open_only` contract
already live for `ui.discovery.settings_vat`.

No new browser workflow. No new API/UI tools. No get/create/update/delete/bulk
greening for taxRates. No salesTax* / taxRateDeductionComponents dual-count.
No annual product. No bulk external-contract greening. No invoice_email product.
No productPrices / contacts.create / products.create this slice.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research158).  
Dual evidence (tmp, non-git): `tmp/research158_residual_dual.json`.  
Discovery product already green: `ui_settings_vat_open` (research130); wiki
`wiki/ui_settings_vat_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_vat_open.py`,
vision record class `tmp/vision-records/ui_settings_vat_open.json`.  
API offline list already green: `api.taxRates.list` → `api_tax_rates_list`
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: MD5 `8b94b0135c91fd15fe54ea33e088a4be` / ETag status pair
`wcw4x9hqvu3603` (unchanged research103–158).  
Prior dual-count precedents: `api.accounts.list` → `ui_settings_accounting_open`
(research145 / 186.46); `api.users.list` → `ui_settings_users_open` (research141).

## Why this slice (research158)

1. Dual headless sessions research158:
   - Typed `ui_settings_vat_open` dual READY + success
     (`shell_kind=settings_vat`, `vat_panel_markers_present=true` both sessions).
   - Soft seeds `taxRates` / `tax-rates` / `momssatser` / `settings/vat` /
     `settings/tax` dual soft-empty body_len **127** (= nonsense) — no separate
     dedicated taxRates list URL shell.
   - Soft hub `/settings` dual body **626**, h1 Indstillinger, **Momssatser** in
     side nav; default panel is not VAT success without tool click.
   - Invoice_email still blocked (detail hrefs 0; client create failed; draft
     stayed on `/invoices/new`); pure NA still rejected (Levering + create-send
     chrome exist).
   - No API token; no write CTA clicks; ephemeral profiles deleted.
2. Same inventory dual-count pattern as `ui.parity.accounts.list`, with
   **parity_status remaining `shell_open_only`** (settings multi-section panel;
   markers Regelsæt + Satser for salg + Satser for køb; not a dedicated
   `/:org_slug/taxRates` list shell).
3. get/create/update/delete/bulk UI parity remain red (no dual-proved detail/write
   shells; bulk external-contract on API lane).
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research158: invoice_email dual/NA; products/contacts
   create; productPrices steal; salesTaxRulesets / taxRateDeductionComponents;
   organizations.get / users.get; bankLines → recon steal; postings →
   transactions steal.

## Non-goals

- Inventing `api_settings_*` or a second UI Momssatser/taxRates tool.
- Greening `ui.parity.taxRates.get|create|update|delete|bulk_save|bulk_delete`.
- Greening any `salesTax*` or `taxRateDeductionComponents` UI parity rows.
- Greening `ui.parity.special.invoice_email` or inventing send-email UI tools.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting productPrices, files, bankLines, postings, daybookTransactions.
- Implementing tax rate write UI or API list filters/sort/pagination UI.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research130 live
  qualification + research158 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_vat_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror accounting dual-count); when True:
  keep `api_row_id` from parity builder (`api.taxRates.list`), append research158
  evidence mapping + “API list filters/sort/pagination UI not producted”, keep
  `parity_status=shell_open_only` and non-claims for other taxRates ops /
  salesTax* families; discovery path keeps default False and `api_row_id=None`;
  wire `if api_row["id"] == "api.taxRates.list": apply_...(parity_of_api_list=True)`
  in the parity builder loop (alongside accounts/users/daybooks) |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.taxRates.list` → `ui_settings_vat_open` in qualified_ids +
  tool_by_id; qualified shell count **52→53**; live/vision total **152→153**
  (`ui_shell_green + geo_na` = 53 + 100); assert evidence mentions
  `api.taxRates.list` + research158 + filters/sort/pagination phrase; assert
  other taxRates ops stay red `discovery_required`; update hard-coded
  `52` / `152` / `ui_shell_green = 52` assertions |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_vat_open_shell.md` — dual-count note for
  `api.taxRates.list` / shell open only; non-claims get/create/update/delete/bulk
  and other tax* / salesTax* families; soft taxRates URL empty observation;
  API offline non-live; update desc / `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_vat_open`
- models / server registration
- `tests/live/test_ui_settings_vat_open.py`
- unit browser tests for the VAT settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_vat_open` (existing) |
| Open sequence | hub `/:org_slug/settings` then observe-only click **Momssatser** |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_vat` |
| Panel markers | Regelsæt; Satser for salg; Satser for køb |
| Input | empty strict model |
| Soft seeds | reject `settings/vat`, `taxRates`, `momssatser`, other non-VAT panels |
| Soft URL evidence | `/taxRates` etc. body 127 soft-empty (research158 dual) |
| CTA / write | never Opret / Opret regelsæt / Opret sats / Gem* / Tilføj* |
| Parity row | `ui.parity.taxRates.list` / `api_row_id=api.taxRates.list` |
| parity_status | **`shell_open_only`** (not `list_shell_open_only`) |
| API invent | never invent `api_settings_*` |
| Distinct shells | company / accounting / invoicing / users / user settings stay separate |
| get/create/update/delete/bulk UI | stay red |
| Adjacent tax families | salesTaxRulesets / taxRateDeductionComponents / salesTax* stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.taxRates.list` wiring on
   `apply_ui_settings_vat_open_shell_evidence` (mirror
   `apply_ui_settings_accounting_open_shell_evidence` structure).
2. Update inventory tests counts and assertions (53 qualified shells; 153
   live/vision with geo NA = 53 + 100).
3. Run coverage generator; confirm status live/vision **153**, implemented/
   contract **337**, complete **false**.
4. Wiki dual-count note on settings VAT shell page + index desc if needed.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (no live API; no
   `BILLY_TEST_MODE=ui-full` completeness claim).
6. Memory EXECUTE notes; leave dual JSON under tmp (non-git) for IR.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.taxRates.list` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_settings_vat_open` |
| parity_status | `shell_open_only` |
| api_row_id | `api.taxRates.list` |
| Evidence | research158 + maps api.taxRates.list; filters/sort/pagination not producted; no PII |
| Other taxRates ops | stay red / discovery_required |
| Adjacent tax families | salesTax* / taxRateDeductionComponents remain incomplete |
| Deferred | invoice_email, productPrices, create forms, bulk 92, annual remain incomplete |
| Tools | No new tool registrations |
| API live | All API live_tested remain false with out_of_scope_by_user |
| Complete | `complete: false` |
| Offline tests | Inventory + non-live suite green |

## Independent review focus

- Confirm research158 dual proves VAT tool + Momssatser markers + soft taxRates
  URLs empty (not pure soft-empty NA of the UI capability).
- Confirm only `.list` is greened; writes/get and other tax* stay red.
- Confirm `shell_open_only` honesty (panel multi-section; not inventing a list path).
- Confirm live/vision 153 math: 53 shells + 100 GEO NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.
- Confirm invoice_email remains red (not weak-NA greened).

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects multi-section panel as taxRates list | Same bar as accounts Regnskab / users Brugere dual-count; shell_open_only; markers include Satser for salg/køb |
| Accidentally greening all taxRates ops or salesTax* | Explicit per-op id gate `api.taxRates.list` only |
| Count drift (52→53 shells) | Update every hard-coded 52 / 152 assertion in inventory tests |
| Wiki still claims “does not green taxRates” | Edit dual-count note carefully; keep non-claims for other ops and families |
| Confusing salesTaxReturns list (already green on vat_declarations) | Different tool/surface; taxRates is Momssatser settings panel |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **153**; complete **false**.
- Ready independent review of dual-count package 186.59.
- Not node finish.

## Post-Mortem

### Work completed

- Generator: `apply_ui_settings_vat_open_shell_evidence(parity_of_api_list=)` +
  wire `api.taxRates.list` only (mirror accounts.list dual-count).
- Inventory tests: shells 52→53; live/vision 152→153; residual taxRates ops red;
  geo freeze test updated so only list is live among taxRates.*.
- Coverage regenerated: live/vision **153**; implemented/contract **337**;
  complete **false**.
- Wiki: `ui_settings_vat_open_shell` dual-count note + `_index` desc.
- No new UI/API tools; no `src/` browser changes.
- Offline: lint pass; `test.sh` **1586 passed**, 38 deselected (EXECUTE +
  FIX-VERIFY reconfirm).
- IR: **ACCEPT** product (`tmp/grok-review.md`); no required fixes; optional
  N1–N2 deferred (vision workflow_ref; research dual auxiliary page-body).

### Outcomes vs plan

- Matched plan 186.59 / research158 slice. No deviations.
- complete remains false (expected). No ui-full / node finish.

### Follow-ups

- Residual: special.invoice_email (needs disposable client + draft dual); other
  taxRates ops; salesTax* / taxRateDeductionComponents dual packages;
  productPrices; create-form tools; bulk 92 external-contract; annual_reports
  org_inaccessible.
- complete remains false; do not node finish until Completion Requirements met.
