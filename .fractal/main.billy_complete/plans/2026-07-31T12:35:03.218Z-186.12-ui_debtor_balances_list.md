# 186.12 UI debtor balances list shell

## Goal

Ship the next **read-only list-shell-open** business UI FastMCP tool for Billy
debtor balances (receivables / Tilgodehavender), frozen in research111, with
offline contract tests, dual-session live interface tests (no API token), and a
non-sensitive vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research111).  
Discovery: `.fractal/main.billy_complete/tmp/discovery111/summary.json` dual
session seed `debtorbalance`; frames purged; summary PII scrubbed;
`api_token_used: false`; `writes_attempted: false`.  
Prior product patterns: tip `713f2e8` (`ui_bills_list`), invoices list shell.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` §10.3
(Debtor and creditor balances stay red until qualified).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–111).

## Why this slice

- Auth lane plus list/import shells through bills are producted (UI live/vision 13).
- Research111 dual headless sessions both reached the debtor balances shell:
  path class `/:org_slug/debtorbalance`, h1 `Tilgodehavender`, CTA
  `Opret faktura` present (presence only); A↔B path/h1/title match; no error
  shell; no plan-gate heuristic.
- Official docs have **no** `/v2/debtorbalance` resource. Related offline API
  families (`contactBalancePayments`, `contactBalancePostings`,
  `balanceModifiers`) stay offline-contract only; do **not** invent
  `api_debtor_balances_*`.
- Inventory target still red: `ui.discovery.debtor_balances`.
- This product greens **shell open only** for that discovery row (bank_accounts
  pattern: discovery-only, no forced false API-parity list row). Do not green
  contactBalance* / balanceModifiers parity or creditor discovery.
- Alias seeds (`debtor-balances`, `debtor_balances`, `tilgodehavender`,
  `receivables`, …) resolve to non-list shells — product navigates only
  `/debtorbalance`.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Inventing `api_debtor_balances_*` or greening bulk residual API rows.
- Invoice create from the CTA (never click `Opret faktura`).
- Greening `ui.discovery.creditor_balances` (dual-matched for **next** slice).
- Greening `ui.parity.contactBalancePayments.*`,
  `ui.parity.contactBalancePostings.*`, `ui.parity.balanceModifiers.*`.
- Uploads, receipt_inbox, daybooks, settings_*, bank_reconciliation.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; new login-only `ui_*` tools.
- Setting `complete: true` or greening all 339 UI rows.
- Headed browsers, generic browser MCP tools, credential tool inputs.
- Children (file ownership is one tight cluster on root).

## Ownership

**Root Grok only. No children.** File cluster mirrors bills/invoices list:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_debtor_balances_list.py` (new; pattern from bills/invoices) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for discovery.debtor_balances |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_debtor_balances_list_shell_evidence` for **`ui.discovery.debtor_balances`**
  only (`list_shell_open_only`) |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — append live test path to existing
  `mit.billy.dk` `test_references` only; do not widen hosts |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_debtor_balances_list_shell.md` after product |

Do **not** spawn residual/bulk, creditor, uploads, or payment children this
iteration.

## Contract freeze (research111)

### Tool

- **Name:** `ui_debtor_balances_list`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  filter, credential, contact, or amount fields.
- **Success:** non-PII only:
  - `path_class`: literal `/:org_slug/debtorbalance`
  - `heading`: literal `Tilgodehavender`
  - `create_action_visible`: bool for exact control label **`Opret faktura`**
    (presence only; never click)
  - `shell_markers_present`: bool for ≥1 shared shell marker
    (Overblik / Fakturering / Fakturaer / Udgifter / Menu / Bank) without
    dumping page text
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` (error shell, wrong path, missing h1 /
  required control), `EGRESS_DENIED`, `BILLY_ERROR`.
  Messages must not include org slug, org name, email, password, tokens,
  contact names, invoice numbers, or balances.
- **Navigation:**
  1. Open MCP profile under fixed egress (`coverage/browser_egress.yaml`).
  2. If login signature present → `AUTH_REQUIRED` (callers use auth tools first).
  3. Resolve org slug from session URL or outside-git identity file only.
  4. Goto `https://mit.billy.dk/{slug}/debtorbalance` (same wait policy as
     bills/invoices list shells).
  5. Classify with path regex `^/[^/]+/debtorbalance$` (query params allowed on
     real URL; path class remains `/:org_slug/debtorbalance`).
  6. Signature: h1 text `Tilgodehavender` and visible `Opret faktura`.
  7. Reject alias paths (`debtor-balances`, `debtor_balances`, `debtorbalances`,
     `tilgodehavender`, `receivables`, `accounts-receivable`) as `UI_CHANGED`
     if somehow navigated for this list tool.
- **Session:** no API token; no screenshot persistence in production tool path;
  live test captures frames only outside git for vision then purges.

### Inventory rows

Primary only: `ui.discovery.debtor_balances`  
No shell-open API parity row (no official `/v2/debtorbalance`; bank_accounts
pattern).

| Flag | Target after product |
| --- | --- |
| discovered | true |
| implemented | true |
| contract_tested | true |
| live_tested | true |
| vision_verified | true |
| tool_name | `ui_debtor_balances_list` |
| method_or_route | `mit.billy.dk /:org_slug/debtorbalance` (list shell open only) |
| parity_status | `list_shell_open_only` |
| response_fields | path_class, heading, create_action_visible, shell_markers_present |
| cleanup | not_applicable; read-only observation creates no records |
| side_effects | none |
| evidence | research111 dual-session + product; no invent debtor API; no create click |

Do not set `vision_evidence` to embed frame bytes. Durable vision record lives
under `tmp/vision-records/ui_debtor_balances_list.json` (gitignored/tmp) with
`purge_verified: true`.

Do **not** green `ui.discovery.creditor_balances` or any
`ui.parity.contactBalance*` / `balanceModifiers*` rows.

## Work sequence (EXECUTE)

1. **Models** — `UiDebtorBalancesListInput`, `UiDebtorBalancesListSuccess` in
   `models.py` (mirror `UiBillsList*` / invoices).
2. **Browser** — path helper, signature helper, `UI_CHANGED` factory,
   `UiDebtorBalancesListService` protocol, `BrowserRuntime.ui_debtor_balances_list`,
   no create / menu click.
3. **Server** — register `ui_debtor_balances_list` with typed empty input
   binding like other UI tools.
4. **Offline tests** — model contract (no extra fields; success literals);
   browser unit tests for path accept/reject (including alias reject),
   AUTH_REQUIRED, UI_CHANGED, success mapping with fakes; server registration
   name and call path.
5. **Coverage** — extend generator apply helper + inventory gate map for
   discovery.debtor_balances only; regenerate report; assert UI live/vision
   13→14; API live stays 0; `complete` stays false.
6. **Egress** — append `tests/live/test_ui_debtor_balances_list.py` to
   mit.billy.dk test_references only.
7. **Live test** — dual ephemeral profiles; login READY; dual
   `ui_debtor_balances_list`; assert path/heading/create flags; capture frames
   outside repo; write vision record; purge frames; mark purge_verified; never
   set BILLY_API_TOKEN.
8. **Wiki** — `wiki/ui_debtor_balances_list_shell.md` (contract + non-claims).
9. **Lint/type/unit/contract/safety** before claiming FIX-VERIFY ready;
   live+vision before completeness of this row.

## Independent review criteria (REVIEW)

ACCEPT product only if all hold:

- Typed tool exists; no generic browser/HTTP exposure.
- Empty input; success fields match freeze; no PII in models.
- Unit/contract coverage for path signature and error codes (including alias
  reject for hyphen/underscore variants).
- Live dual session without API token; independent second profile read-back.
- Vision record non-sensitive with purge_verified; no frames in repo.
- Only `ui.discovery.debtor_balances` greened for this shell; no invent debtor
  API; no greening of creditor, contactBalance*, or uploads discovery.
- `coverage/status.json` complete remains false; API live_tested remain false.
- No credentials, tokens, org slugs, contact names, or amounts in committed
  files.

## Feasibility

- Budget: no limit (uncapped run). Slice is one list shell matching bills.
- Blockers: none known; credentials via keyring outside git; discovery111 READY.
- Children: none (clear single-owner file cluster).
- Risks:
  - CTA `Opret faktura` is shared with invoices list — boolean presence only;
    never navigate create or click.
  - Empty receivables list has no HTML `table` — do not require table for
    shell pass (research111: table_count 0).
  - Multi-h1 possible (bills lesson) — use first matching h1 text if multiple.
  - English locale would change heading; treat as UI_CHANGED until re-frozen
    (current test org Danish).

## Done when (this iteration product only)

- `ui_debtor_balances_list` implemented and registered.
- Offline suite green for new unit/contract paths.
- Live dual + vision purge_verified for this row.
- `ui.discovery.debtor_balances` all five flags true; status live/vision
  13→14; complete false.
- Wiki shell page present.
- Ready for COMMIT after FIX-VERIFY / REVIEW ACCEPT.

## Post-Mortem

### Work completed

- Research111 dual headless freeze for debtor balances list shell; plan 186.12.
- Implemented `ui_debtor_balances_list` (models, browser, server, offline + live
  tests, coverage generator evidence for `ui.discovery.debtor_balances` only,
  egress refs, wiki shell page).
- IR ACCEPT product; completeness FAIL as expected.
- FIX-VERIFY: no IR-required product fixes; lint + offline + live reconfirm clean.

### Deviations

- Root-only execution (no children), matching plan ownership.
- Discovery-only green (no forced `ui.parity.contactBalance*` list row), matching
  bank_accounts pattern and research111 non-claims.

### Review findings

- Product ACCEPT; no required product blockers.
- Optional non-blocking notes only (shared first-h1 helper later; EN locale would
  need re-freeze).

### Test results

- `bash .fractal/main.billy_complete/scripts/lint.sh`: pass.
- `bash .fractal/main.billy_complete/scripts/test.sh` (offline): **1384 passed**,
  12 deselected.
- Live reconfirm: `tests/live/test_ui_debtor_balances_list.py` **1 passed**.
- Vision `purge_verified: true`.
- Did not run `BILLY_TEST_MODE=full` / `ui-full` completeness gate: node not complete.

### Cleanup state

- No business records created (read-only shell open).
- Ephemeral live profiles removed by test teardown.
- Frames purged; durable vision record non-sensitive only.

### Next unresolved coverage slice

- `ui.discovery.creditor_balances` → product `ui_creditor_balances_list`
  (`/:org_slug/creditorbalance`, h1 `Skyldige udgifter`, CTA `Opret køb` observe
  only) — dual-matched in research111, separate slice.
- Then uploads / remaining discovery families; residual bulk API offline reds.
