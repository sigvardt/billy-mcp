# 198.57 accountNatures ticketed create and update

## Pivot

`9B979A05` is binding. HEAD `5f28557` is a clean 198.56 docs-lock
close, not a `(failed on <step>)` backstop. Uncommitted files are
memory-only (`state.md`, `decisions.md`) from this iteration's
SYNC. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). `#v2accountnatures` Supports:
`get by id, list, create, update, bulk save, bulk delete`.
Property table: `reportType` enum, `name` string,
`normalBalance` enum. None readonly. None required. No singular
delete. Intro create envelope is a singular JSON root (product
example `POST /v2/products` with `"product": { ... }`).

Unauth 405 must not override that table. Wave-5b freeze that left
`accountNatures` out on 405 is superseded.

Do not spawn. Do not remake UI dumps. Do not invent
`api_account_natures_delete_*`. Do not implement geo, reminder
associations, `bankPayments.delete`, transactions, or bulk.
Do not finish. No live API.

## Goal

Land the smallest proved cohort: ticketed offline
`api.accountNatures.create` and `api.accountNatures.update`.

| Inventory id | Preview / execute | Path | Request root |
| --- | --- | --- | --- |
| `api.accountNatures.create` | `api_account_natures_create_preview` / `_execute` | `POST /accountNatures` | `accountNature` |
| `api.accountNatures.update` | `api_account_natures_update_preview` / `_execute` | `PUT /accountNatures/:id` | `accountNature` |

Response map stays `changed_records[]` plus `meta.deletedRecords`.
`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 23 method-closed + 2 readonly
map + 2 meta delete = 27. Bulk 92 stay red.

## Why no child

Work shares `src/billy_mcp/api/account_writes.py`,
`src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. Extend `tests/api/test_account_writes.py`:
   - Register count becomes 16 (was 12). New preview fields:
     `api_account_natures_create_preview` `{accountNature}`,
     `api_account_natures_update_preview` `{id, accountNature}`,
     both executes `{confirmation_ticket}`.
   - Outer extras and empty `id` fail `ValidationError`.
   - Preview create sends no HTTP. Execute POSTs
     `https://api.billysbilling.com/v2/accountNatures` with
     exactly `{"accountNature": <payload>}`.
   - Preview update sends no HTTP. Execute PUTs
     `/v2/accountNatures/<id>` with
     `{"accountNature": <payload>}`.
   - Tamper, replay, expiry, and wrong execute-tool bind fail
     closed.
   - Payload may include `reportType`, `name`, `normalBalance`.
     Do not invent enum members. Extra inner keys may pass
     through as opaque JSON like account groups (`customField`
     pattern). Do not add a second write protocol.
2. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 23`, residual `== 27`. The two
   natures write ids are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. After generator regenerate,
   `implemented_rows == 525` and `contract_tested_rows == 530`
   (HEAD is 523 / 528; both flags move with the two tools).
   `complete` stays false. Live/vision counts stay 339.
3. Map `api.accountNatures.create` / `.update` in
   `OPERATION_TEST_REFERENCES` to
   `tests/api/test_account_writes.py` plus the server registry
   test. On HEAD the inventory still has empty `tool_name`;
   the new mapping test should fail until honesty lets the
   preview names through.

Run:
`uv run pytest tests/api/test_account_writes.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/account_writes.py`:

- Add `AccountNatureCreatePreviewInput` (`accountNature`) and
  `AccountNatureUpdatePreviewInput` (`id`, `accountNature`),
  extra=forbid.
- Register four tools using `_account_specification` with
  `collection_path="/accountNatures"`,
  `singular_root="accountNature"`,
  `plural_root="accountNatures"`,
  `resource="accountNature"`.
- No delete tools. Supports omits singular delete.

`src/billy_mcp/server.py`: already calls
`register_account_write_tools`. Do not add a second register.

`scripts/generate_coverage_report.py`:

- Remove `api.accountNatures.create` and
  `api.accountNatures.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for those two ids to
  `tests/api/test_account_writes.py` plus
  `SERVER_REGISTRY_TEST_REFERENCE`.
- Do not touch bulk helpers, the other 23 method-closed ids,
  readonly map, or meta delete.
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 23. Natures create/update are
  ticketed offline tools. Not completeness.
- `wiki/offline_write_probe_rules.md`: rule 3 no longer
  silently overrides Supports when the official property table
  has a writable field map. Unauth 405 is not the contract.
  Live API still out of scope.

Keep `wiki/wave_fiveb_ticketed_writes_contract.md` as history.
Do not rewrite it as if natures were in Wave-5b.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.accountNatures.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- UI `accountNatures` parity stays `not_applicable`
  (research142).
- Inner enums stay opaque JSON. Do not invent values.
- `complete` stays false.

## Independent review

After EXECUTE, review must re-parse `#v2accountnatures` on the
locked HTML and confirm:

1. Supports still lists create and update, not singular delete.
2. Tools exist only for those two ops.
3. Inventory `tool_name` is the preview name.
4. `live_tested` is false.
5. Remaining residual honesty is 27 toolless rows.
6. No 405-based green. No invented enums. No bulk tools.

## Post-Mortem

Work completed: ticketed offline `api.accountNatures.create` and
`api.accountNatures.update` in `src/billy_mcp/api/account_nature_writes.py`.
Radio `EAB2F91B` replaced the first opaque `dict[str, JsonValue]`
payload with nested `AccountNaturePayload` (`reportType`, `name`,
`normalBalance`, extra=forbid). FastMCP preview parameters publish
that schema. Nested extras fail at the tool boundary. Empty payload
is allowed. No singular delete. Bulk stay red.

Deviations: implementation lives in its own module instead of
`account_writes.py` (250 LOC ceiling). Ticket tests live in
`tests/api/test_account_nature_write_tickets.py`. Plan TDD that
inner extras may pass through like account groups is superseded by
`EAB2F91B`.

Review findings: independent review PASS on cohort shape. Required
fixes none. Confirmed defect from `EAB2F91B`: opaque payload.
Fixed. Rejected as a fail: leftover 405 evidence strings on the
remaining 23 method-closed rows. Follow-up only.

Test results: focused natures write tests 17 passed. Independent
typed-payload review PASS with no confirmed defects. `lint.sh`
passed. Default `test.sh`: 2152 passed, 83 deselected. Wiki lint
clean on project wiki and memory. `BILLY_TEST_MODE=full` was not
run because completion requirements are still false.

Cleanup state: no live records. No frames. No credentials.

Next unresolved coverage slice: remaining method-closed rows with
official writable field maps (geo, country groups, reminder
associations, `bankPayments.delete`). Residual honesty 27. Bulk 92.
`complete` stays false. Do not finish.

