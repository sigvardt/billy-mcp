# 2.41 Wave-5j Bank-Line Freeze Contract

## Decision

The current official-doc research is fresh and independently accepted only as
a freeze-authoring handoff. The next bounded deliverable is therefore the
Wave-5j offline contract page for nine clear singular bank-line CUD operations.
Product implementation, coverage greens, and further qualification remain
blocked on a separate independent Grok review of that page.

This concern has a clean boundary and is delegated to one Codex Power leaf:
`wave5j_freeze_contract`. It owns only the new Wave-5j contract page and
ordinary wiki-index regeneration. No parallel implementation child is valid
because the product gate is not yet open.

## Frozen inputs

- Cited research: `.fractal/main.billy_complete/tmp/grok-research.md`
  (research41; ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934-byte official-doc body).
- Authoring gate: `wiki/wave_fivej_freeze_ready_research_independent_review.md`
  (ACCEPT, handoff only).
- Style and protocol pattern: `wiki/wave_fivei_ticketed_writes_contract.md`.
- Machine inventory: `coverage/api_v2_manifest.yaml` rows for
  `bankLineMatches`, `bankLines`, and `bankLineSubjectAssociations`.

## Leaf deliverable

1. Draft `wiki/wave_fivej_ticketed_writes_contract.md` with all nine inventory
   rows and eighteen preview/execute tool names; retain every row red.
2. Freeze the exact locked routes, singular roots, request and response-root
   handling, field ambiguities, ticket protocol, probe evidence, exclusions,
   and later test obligations stated by research41.
3. Preserve explicit exclusions: all bulk rows, `bankPayments`,
   `balanceModifiers`, files specials, live/UI/vision, webhooks, and generic
   controls. Do not create implementation modules, tests, or coverage changes.
4. Run `wiki update` and `wiki lint` for the shared wiki. The child may commit
   only its leaf page plus mechanically generated index work.

## Gate after merge

Request an independent Grok freeze review of the exact committed contract.
Only an ACCEPT opens a later, separate research refresh and Codex Power product
implementation leaf. A REJECT is repaired before that gate is retried.
