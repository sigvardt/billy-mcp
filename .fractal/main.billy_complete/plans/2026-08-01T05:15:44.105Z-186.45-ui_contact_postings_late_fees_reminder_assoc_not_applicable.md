# 186.45 ui contactBalancePostings invoiceLateFees invoiceReminderAssociations not_applicable freeze

## Goal

Product research144 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved join/meta UI
   parity rows for three families only (**19 rows total**):
   - **contactBalancePostings** — 6 ops (get, list, create, update, bulk_save,
     bulk_delete; no singular delete in inventory)
   - **invoiceReminderAssociations** — 7 ops (get, list, create, update,
     delete, bulk_save, bulk_delete)
   - **invoiceLateFees** — 6 ops (get, list, create, update, bulk_save,
     bulk_delete; no singular delete in inventory)
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so these three `api.<resource>.`
   prefixes are included.
3. Updates inventory tests for NA count 61→**80** and live/vision baseline
   101→**120** (`40` shell/parity dual-counts + `80` NA).
4. Adds a scrubbed wiki page for the freeze; optional one-line cross-link from
   prior geo / accountGroups NA wiki pages.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   these families.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research144).  
Dual evidence (tmp, non-git): `tmp/research144_residual_parity_dual.json`.  
Shell markers (tmp): `tmp/research144_shell_markers.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–143).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–40 geo/currencies; 186.43 accountNatures+balanceModifiers;
186.44 accountGroups (`wiki/ui_account_groups_not_applicable.md`).

## Why this slice

1. research143 greened accountGroups only; deferred productPrices and multi-surface
   families. research144 dual-probed residual join/meta soft seeds and isolated
   three families with soft-empty == nonsense **and** no dual-proved dedicated
   or list-shell label workflow (Rykker/Morarente/Gebyr/Tilknytning absent on
   greened invoices/balances/settings shells).
2. Dual independent READY sessions: soft seeds for the three families body_len
   **127**, empty h1, identical to nonsense `zz-research144-no-such-route`;
   contrast tools dual-ok (accounting, vat, company, daybooks, bank, recon,
   products, transactions, uploads, vat_declarations).
3. Soft URL landing is not a productable shell — do not invent UI tools.
4. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class).
5. Explicit rejections stay red: productPrices (`/products/new` non-empty),
   bankLineMatches/bankLineSubjectAssociations (recon shell),
   daybookBalanceAccounts (daybooks editor), salesTax*/taxRateDeduction
   (Momssatser), nested lines/files/postings/org/bank/accounts/taxRates.

## Non-goals

- Shipping any new FastMCP tools for these families
  (`ui_contact_balance_postings_*`, `ui_invoice_late_fees_*`,
  `ui_invoice_reminder_associations_*`).
- Greening soft-empty paths as list shells.
- **productPrices** NA (research144 reconfirm: `/products/new` dual body 190).
- **bankLineMatches** / **bankLineSubjectAssociations** NA (recon greened).
- **daybookBalanceAccounts** NA (daybooks editor greened).
- **salesTax*** / **taxRateDeductionComponents** NA (Momssatser greened).
- Dual-counting accounts/taxRates/daybooks/organizations/bankPayments/bankLines.
- Nested invoiceLines/billLines/contactPersons dual-count or NA as pure absence.
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Closing offline API write gaps for contactBalancePostings / associations
  (API lane separate; live_tested stays false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with
  `api.contactBalancePostings.`, `api.invoiceReminderAssociations.`,
  `api.invoiceLateFees.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 80` (prior 61 + **19** ops). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH144_RESOURCES = frozenset({
  "contactBalancePostings", "invoiceReminderAssociations", "invoiceLateFees"})`
  (mirror research143 set). Prefer **same evidence code**
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research144 families: evidence_ref
  `research144_contact_invoice_join_dual`; nonsense path
  `zz-research144-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_contact_invoice_join`; contrast controls include
  greened dual-ok tools from research144. Keep research138–143 branches intact. |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefixes alone should wire all
  **19** parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count 80; prefixes include the three families.
  - research144 evidence_ref branch assertions (mirror research143).
  - live/vision `40 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **120** when
    ROW_COUNT=80.
  - annual still NA-rejected red; bulk 92 red; no new tools for these families;
    productPrices/bankLine*/salesTax* parity still red (not greened). |
| Wiki | New
  `wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`
  (scrubbed) + sorted `_index` link. Optional one-line cross-link from
  accountGroups NA page. |
| Coverage regenerate | Run generator → refresh
  `coverage/ui_workflows_manifest.yaml`, `coverage/status.json`,
  `coverage/report.md`. |

## Product decision matrix

| Family | API parity ops | Dual evidence | Product |
| --- | ---: | --- | --- |
| contactBalancePostings | 6 | soft-empty dual = nonsense; no list-shell posting labels | **NA package** |
| invoiceReminderAssociations | 7 | soft-empty dual = nonsense; no Rykker/association labels | **NA package** |
| invoiceLateFees | 6 | soft-empty dual = nonsense; no Morarente/Gebyr labels | **NA package** |
| productPrices | 7 | soft empty + `/products/new` non-empty | **stay red** |
| bankLineMatches / bankLineSubjectAssociations | 7+7 | soft empty + recon greened | **stay red** |
| daybookBalanceAccounts | 7 | soft empty + daybooks greened | **stay red** |
| salesTax* / taxRateDeduction* | many | soft empty + Momssatser greened | **stay red** |
| annual_reports discovery | 1 | Upsedasse org_inaccessible | **stay red** |
| bulk API | 92 | external contract blocker | **stay red** |

## Execution steps (EXECUTE)

1. Extend `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with the three prefixes;
   `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 80`; add
   `GEO_UI_NOT_APPLICABLE_RESEARCH144_RESOURCES`.
2. Extend `apply_ui_geo_reference_not_applicable_evidence` with research144
   branch (evidence_ref, nonsense path, dual flag, family labels, contrast
   controls). Preserve prior research138–143 branches.
3. Update `tests/coverage/test_coverage_inventory.py` for count 80, research144
   evidence assertions, live/vision 120 baseline, red-stay assertions for
   productPrices and related-shell rejections.
4. Write wiki page + `_index` link (scrubbed; no credentials/slugs/raw frames).
5. Regenerate coverage manifests/status/report via generator entrypoint used
   by prior NA packages.
6. Run offline checks: lint + non-live `test.sh` (or targeted inventory +
   coverage server tests if suite length matches prior NA iterations).
7. Confirm `coverage/status.json`: live_tested_rows/vision_verified_rows **120**;
   complete **false**; no greening of excluded families.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row count | Exactly **19** new NA UI rows for the three prefixes; total GEO NA **80**. |
| Flags | Each NA row: discovered/implemented/contract_tested/live_tested/vision_verified all true; parity_status not_applicable. |
| Evidence | evidence_code `GEO_UI_NO_EQUIVALENT_WORKFLOW`; research144 evidence_ref; no PII. |
| Exclusions red | productPrices, bankLineMatches, bankLineSubjectAssociations, daybookBalanceAccounts, salesTaxRules, annual_reports still incomplete. |
| Tools | No new tool registrations for these families. |
| API live | All API live_tested remain false with out_of_scope_by_user. |
| Complete | `complete: false`. |
| Offline tests | Inventory + non-live suite green (same bar as 186.44). |

## Independent review focus

- Confirm research144 dual soft-empty == nonsense for all three families.
- Confirm related-shell rejections are not greened as NA.
- Confirm count math 61+19=80 and live/vision 40+80=120.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.

## Risks

| Risk | Mitigation |
| --- | --- |
| Nested invoice-detail late-fee/rykker UI exists but unopened | Research144 shell markers false on list/settings; same bar as balanceModifiers. IR may flag optional follow-up; do not block package if dual soft-empty holds. |
| ROW_COUNT mismatch if inventory op sets differ | Assert exact prefixes + len(na_rows)==80 in tests. |
| Over-greening bank/salesTax soft seeds | Prefix list is explicit three families only. |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage committed path ready for IR.
- live/vision **120**; GEO NA **80**; complete **false**.
- Ready independent review of NA package 186.45.
- Not node finish.


## Post-Mortem

### Work completed

- Extended GEO_UI_NOT_APPLICABLE_API_PREFIXES with api.contactBalancePostings.,
  api.invoiceLateFees., api.invoiceReminderAssociations.
- Set GEO_UI_NOT_APPLICABLE_ROW_COUNT = 80 and RESEARCH144_RESOURCES frozenset.
- Added research144 branch in apply_ui_geo_reference_not_applicable_evidence.
- Regenerated coverage: live/vision 120, implemented/contract 304, complete false.
- Wiki ui_contact_postings_late_fees_reminder_assoc_not_applicable.md plus _index link.
- Inventory tests: NA count 80, package 19 rows, exclusion red asserts.

### Deviations

- None material vs plan. Optional IR N1/N2 left as-is (no product change).

### Review findings

- IR 186.45: ACCEPT, required fixes none.
- Overall completeness: FAIL (expected: bulk 92, annual_reports red,
  residual UI parity, API live out of scope).

### Test results

- lint.sh: pass (wiki + ruff + pyright + coverage check + repo policy).
- Offline test.sh: 1565 passed, 35 deselected (EXECUTE and FIX-VERIFY).
- Full BILLY_TEST_MODE=ui-full / completeness suite: not run (complete still false).

### Cleanup state

- Research dual used ephemeral profiles (purged). No disposable UI writes.
- No frames/HAR in repo. No credentials in product files.

### Next unresolved coverage slice

- Residual UI parity (productPrices nested dual-count; bank recon join binding;
  Momssatser multi-resource; accounts Kontoplan; organizations company settings).
- ui.discovery.annual_reports org_inaccessible red.
- 92 ambiguous bulk API rows external-contract red (BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS).
- Offline API write gaps where still red on clear ops (outside this UI NA slice).
