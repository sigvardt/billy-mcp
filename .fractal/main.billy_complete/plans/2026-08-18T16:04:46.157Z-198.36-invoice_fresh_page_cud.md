# 198.36 invoice fresh invoices/new then draft CUD

## Pivot

Owner radio `9310BC17` supersedes `802D71CF` and the claim that
invoice Kunde is `interface_control_absent`. A customer created
through the normal UI appears in Kunde on a **new**
`/invoices/new` page after the chevron opens. The prior
`UI_CHANGED` is a stale invoice page opened before the customer
existed.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official `POST /v2/invoices` still requires immutable `contact`.
`state` defaults to `"draft"`. `lines` required on create. Do not
re-lock docs (live ETag `"121myuqjdm53603"`). No live API.

Do not remake picker, CTA, Ember, fiber, listener, or archive
dumps (`113F1E05`, `D68E402A`, `A337A622`). Do not remove bind
helpers. Do not ask Joakim to inspect routine UI.

## Goal

Prove invoice draft create, update, and delete through FastMCP
after this exact order:

1. Create one uniquely tagged disposable customer with
   `ui_clients_create_preview` / `ui_clients_create_execute`.
2. Confirm that name on an independent clients list
   (`EC676F84`, already in the live test).
3. Only then open a **new page** or hard-navigate
   `/:org_slug/invoices/new`. Do not bind on a page that sat on
   that route before the customer existed.
4. Open the existing chevron helper, pick the exact visible tag.
   Never **Opret ny**. Never type-only as the bind.
5. Draft CUD through
   `ui_invoices_{create,update,delete}_{preview,execute}`.
   CTA **Gem som kladde** / **Slet**. Never **Godkend**, Send, or
   email.
6. Independent read-back after each invoice step.
7. Delete invoice first, customer second. Prove both absences on
   a third session.

Stay red. Honesty-16 stays false until independent accept and
purge. Do not finish the node.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `wiki/ui_invoices_writes.md`

The seven write children are completed and merged. Spawning would
split a live proof the root must run anyway.

## TDD

`728BD2E4`: failing fixture first, then the smallest fix.

1. Add an offline sibling in
   `tests/unit/test_ui_invoices_writes.py` that reads
   `_create_draft` and `bind_kunde` source. Require:
   - after customer confirm, a new page or `location` assign to
     `/invoices/new` before `bind_kunde`
   - `bind_kunde` calls `chevron_offset_from_box` (or the existing
     chevron click helper) before `_type_kunde`
   - `_click_scoped_option` still wins over **Opret ny**
   Current `_create_draft` only `page.goto` on the same page and
   `bind_kunde` clicks the textbox then types. That is the first
   fail. Run the unit test. Capture the fail.
2. Smallest fix in `invoices_form.py`: after the write session
   is ready, open a new Playwright page and `goto`
   `/:org_slug/invoices/new`. Do not reuse a page that already
   loaded that route. Do not `evaluate` script.
3. Smallest fix in `bind_kunde`: click the existing chevron
   offset on the proved field, wait for the exact tag, click it.
   Type only if the option is still missing after the chevron
   click. Keep every bind helper. Never **Opret ny**.
4. Live
   `test_ui_invoices_create_update_delete_via_call_tool` must
   still confirm the customer before invoice preview. After that
   confirm it must not keep a preloaded invoice page. Run the
   live test. Cleanup invoice then customer even on fail.
5. If the option is still missing after a new `/invoices/new`
   plus chevron, record the exact `UI_CHANGED` and stop. Do not
   add a dump helper. Do not ask the owner to inspect.
6. After a green live CUD: leave `parity_status` open-only.
   Honesty keeps `implemented` / `live_tested` /
   `vision_verified` false. Independent review writes accept,
   then purge, then remap `tool_name` to preview tools.

Offline first: existing unit tests stay green except the new
failing fixture until the fix. Commit-mode lint and contract stay
green. Do not run live API tests.

## Files

- `tests/unit/test_ui_invoices_writes.py` — failing fixture for
  preloaded page plus chevron bind
- `src/billy_mcp/ui_writes/invoices_form.py` — new page or hard-nav
  before bind
- `src/billy_mcp/ui_writes/invoices_form_bind.py` — chevron then
  exact-tag pick
- `tests/live/test_ui_invoices_writes.py` — only if the live
  sequence still opens a stale page
- `wiki/ui_invoices_writes.md` — stale-page recovery, no inspect
  ask
- Memory `state.md` / `todo.md` — present tense

Do not edit `generate_coverage_report.py` honesty IDs. Do not
re-lock official docs. Do not green coverage. Do not add a
diagnostic module.

## Out of scope

- Product persist (`67CBACB6` / `A337A622`)
- Daybook persist token and `_ledger_write`
- Files upload / Bilag delete
- Postings / **Godkend**
- A second copy of `D02702C5`
- New children
- Live API
- Owner inspect of routine UI

## Feasibility

No time or cost cap. One live FastMCP slice plus two small
helpers. Independent review is a later step. This iteration can
land the failing fixture, the remount, the chevron pick, and a
real CUD attempt. If live bind still fails after a new
`/invoices/new` plus chevron, the deliverable is the exact
`UI_CHANGED`, not another dump.

## Post-Mortem

Completed:

- Failing fixtures for new-page create and chevron-before-type.
- `submit_draft_invoice` closes the bootstrap page and opens a
  new Playwright page before create bind.
- `bind_kunde` clicks `[data-cy='dropdown-icon']` (mouse center)
  in the contact `.pickerfield`. `chevron_offset_from_box` stays
  as fallback. Exact visible tag pick. Never **Opret ny**.
- Live FastMCP: customer confirm, then bind
  `scoped:existing_option`. Browser GET `/v2/invoices` includes
  `contactId`. **Gem som kladde** `POST /v2/invoices` **422**,
  `grossAmount=0`. No invoice id. Cleanup deleted the customer.
- Honesty-16 and invoice `tool_name` stay on `*_open`.

Deviations:

- First live click on the textbox chevron offset timed out:
  `dropdown-icon` intercepts pointer events. Clicked that control
  instead. Not a new dump helper.
- Speculative quantity/unitPrice fills did not change the 422
  and were reverted.

Review: FAIL. Bind proved. Persist 422. Stay red. Required
fixes applied: residual intro matches the 422 row; exact-tag
pick is unique-only. Persist remains the next coverage slice.

Tests: lint passed. Commit-mode `test.sh` 2067 passed after
FIX-VERIFY. Live CUD still failed at persist 422. No `ui-full`.
No live API token. `complete` stays false.

Cleanup: live `finally` deleted the tagged customer. No invoice
id. Stay red.

