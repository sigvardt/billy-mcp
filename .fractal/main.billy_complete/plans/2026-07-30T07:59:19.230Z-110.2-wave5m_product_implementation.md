# 110.2 Wave-5m product implementation

## Objective

Implement the cited, freeze-approved offline `contactBalancePayments` write
slice without widening it: create and update only, represented by four
ticketed FastMCP tools. Preserve all live, UI, vision, bulk, delete, and
completeness rows as red.

## Authoritative input

- Research58 in `.fractal/main.billy_complete/tmp/grok-research.md` rechecks
  the official Billy API documentation at ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes. Its unauthenticated probes
  show `POST` and `PUT` return 401 while singular `DELETE` returns 405.
- `wiki/wave_fivem_ticketed_writes_contract.md` and
  `wiki/wave_fivem_freeze_independent_review.md` authorise exactly the two
  inventory operations below. The bank-payment sample is not request-schema
  evidence for this resource.

| Inventory row | Preview tool | Execute tool | Client-relative write | Required roots |
| --- | --- | --- | --- | --- |
| `api.contactBalancePayments.create` | `api_contact_balance_payments_create_preview` | `api_contact_balance_payments_create_execute` | `POST /contactBalancePayments` | `contactBalancePayment` / `contactBalancePayments` |
| `api.contactBalancePayments.update` | `api_contact_balance_payments_update_preview` | `api_contact_balance_payments_update_execute` | `PUT /contactBalancePayments/:id` | `contactBalancePayment` / `contactBalancePayments` |

## Delegation decision

This is a separable implementation-and-contract-test slice with no source-file
overlap with the active fallback freeze reviewer, so it belongs to a focused
Codex Power child. The already-complete research is cited and committed before
launch; no additional current-document research is needed for the frozen
surface.

1. Checkpoint the research handoff, current state, and this plan on root.
2. Configure and launch `wave5m_contact_balance_payment_product` with
   `--agent=codex-power --model=gpt-5.6-terra`, no descendants, and a budget
   sized for implementation, verification, memory fold, and a clean finish.
   It exclusively owns:
   - `src/billy_mcp/api/contact_balance_payment_writes.py`
   - `tests/api/test_contact_balance_payment_writes.py`
   - narrowly necessary registration in `src/billy_mcp/server.py`
   - the two `contactBalancePayments` create/update rows in
     `coverage/api_v2_manifest.yaml`, the generated coverage artefacts, and
     tests that require their precise counts
3. The child mirrors the existing sales-tax-payment pattern: strict outer
   Pydantic inputs, opaque inner JSON map, update body-ID equality, the shared
   `WriteProtocolService`, exact executor binding, one write/no retry, and
   required-plural-root-only mapping. Its real tests must prove schema shape,
   mutation-free preview, URL encoding, request envelope, replay/tamper/expiry
   failures, typed client errors, and absence of delete or bulk tools.
4. The child may set only create/update `implemented` and `contract_tested`
   after its tests pass; change the create cleanup text to state that singular
   delete is unsupported. It must regenerate coverage while retaining live and
   vision at zero and `complete: false`.
5. Leave the active fallback freeze reviewer restricted to its existing
   non-authoritative review scope. Do not merge or let it overwrite the
   accepted freeze record. After the product child delivers and root integrates,
   launch an independent Grok product review; use Codex Power only if Grok
   fails before it edits a review record.

## Hard boundaries

- No singular delete, bulk save/delete, PATCH, webhook, generic HTTP/browser
  controls, UI work, browser use, live tests, credentials, or test organisation
  mutations.
- Do not infer a typed contact-balance-payment payload, association wire shape,
  extra success roots, a cleanup strategy, or bank-payment field names.
- Expected offline target after genuine tests: 250 `api_*` tools and 172 API
  rows implemented plus contract-tested. This is not a completeness claim.

## Root follow-up

Review the child from a clean archive and a three-dot diff, merge only its
owned product and generated coverage bytes, run the root non-live suite, then
open the independent product-review gate. Keep research and freeze evidence
separate from product acceptance.

## Post-Mortem

- Completed: checkpointed the cited implementation handoff, launched the
  focused Codex Power product child, and merged the supplementary fallback
  freeze record without changing source, coverage, or freeze authority.
- Deviation: root does not implement the product directly; the separable
  source/test/coverage slice remains exclusively child-owned while its review
  record stays independent.
- Review: review58 accepts the research and existing authoritative freeze gate
  only. It correctly rejects product acceptance while the root registry lacks
  the four write tools.
- Verification: direct registry inspection reports 246 `api_*` tools and no
  Wave-5m write twins. The two rows remain false for implementation and
  contract testing; root lint and 1056 non-live tests pass.
- Cleanup: no API credentials, browser activity, live organisation changes,
  or raw evidence were introduced on root.
- Next unresolved coverage slice: merge the child only after clean-archive and
  three-dot review, then obtain a dedicated independent product verdict; live,
  UI/vision, bulk, and completeness remain red.
