# 2.16 Wave-5a root registration research

## Objective

Re-verify official Billy API docs and freeze the exact contract for the next
Codex Power slice: register all 30 Wave-5a ticketed write tools on the root
FastMCP shell and offline-green only the 15 inventory rows.

## Findings (summary)

- Official docs fingerprint unchanged: ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes.
- Catalog and daybook modules are merged on root; create_server still exposes
  only 94 read api_* tools.
- AST already sees 124 api_* tool name strings (execute-twin gate allows them);
  greening still requires OFFLINE_API_IMPLEMENTATION_EVIDENCE + regenerate.
- Unauth probes reconfirm 401 on POST/PUT and empty 200 DELETE on all five
  Wave-5a collections.
- Full cited brief: `.fractal/main.billy_complete/tmp/grok-research.md`.

## Recommended slice

1. Wire ConfirmationStore + WriteProtocolService + four register_*_write_tools.
2. Evidence map for 15 inventory ids; regenerate; keep live_tested false.
3. Update server registry test to 124 api_* tools.
4. Offline suite only; independent Grok review after.

## Non-goals

- No inventory greening from research alone, no live work, no bulk, no UI.
