# 198.43 exact Enhedspris locator then leftover update

## Pivot

Owner `F1A2EFC2` / `DECEA79B` / `B9C4FA7C` / `59A3A933`: leftover
`MCP-UI-INV-6CDB396B` still shows **Enhedspris** 1,00. Owner filled
the exact INPUT `name=unitPrice` `placeholder=Enhedspris` with
`2,00` and the value became `2,00`. Live FastMCP returned
`UI_CHANGED` (`shown_len=3`) before any line PUT. That is a
wrong locator, not Billy rejecting `2,00`. Official
https://www.billy.dk/api/ (2026-08-19, ETag `"121myuqjdm53603"`):
line price is `PUT /v2/invoiceLines/:id` `unitPrice`. Invoice
`lines` must not be set on an existing invoice. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Do not
re-lock docs (`728BD2E4`). Do not create a seventh invoice. Do
not remake dumps.

HEAD `53a4433` already treats only 2xx `PUT /v2/invoiceLines/` as
update persist. Keep that. The fail is `unit_price_field` walking
label text and `following::input[1]`, then `commit_unit_price`
re-resolving a different node after fill.

## Goal

Prove one FastMCP update of leftover `MCP-UI-INV-6CDB396B` to
**Enhedspris** `2,00` on a fresh row-open session, then delete all
6 drafts, 6 products, and 6 customers.

1. Offline failing fixture: `unit_price_field` must not walk
   `get_by_label` / `get_by_text` / `following::input`. Today's
   helper still does.
2. Offline failing fixture: after `fill('2,00')` the same
   locator's `input_value()` must be exactly `2,00`.
   `shown_len=3` must fail.
3. Offline: keep
   `input[name='unitPrice'][placeholder='Enhedspris']`. A bare
   `input[name='unitPrice']` stays absent (`21645EFB`).
4. Smallest fix: bind only that selector. Fill `2,00` once on
   that locator. Assert `2,00` before **Opdater**. Keep list-row
   open, line PUT persist, and `prove_fresh_unit_price`.
5. One live FastMCP update of `MCP-UI-INV-6CDB396B` to
   `unit_price=2`. Fresh-session **Enhedspris** `2,00`. Then
   reverse-delete all 6 triples. Third session empty.
6. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `src/billy_mcp/ui_writes/invoices_form_price.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child and forbids `ui_invoice_lines_*` tools.

## TDD

`728BD2E4` / `F1A2EFC2`: failing fixture first, then the smallest
fix.

1. Change `test_price_is_two_accepts_danish_decimals` (or add a
   sibling) so `unit_price_field` source has
   `input[name='unitPrice'][placeholder='Enhedspris']` and does
   **not** contain `following::input`, `get_by_label`, or a bare
   `input[name='unitPrice']`. Run it. Capture the fail.
2. Add an offline source test that `commit_unit_price` fills
   `danish_price` (`2,00`) on the bound locator and reads that
   same locator. Today's re-resolve plus integer `"2"` /
   `press_sequentially` is the fail if still present after the
   locator lock.
3. Smallest locator fix: `unit_price_field` returns only the
   evidenced selector when it is visible. Missing or not visible
   is `UI_CHANGED`.
4. Smallest fill fix: `fill('2,00')` once. Tab only after
   `input_value() == '2,00'`. Do not dispatch extra `change`.
5. Live FastMCP `call_tool` update of leftover
   `MCP-UI-INV-6CDB396B`. Assert `2,00` before save. Require
   `PUT /v2/invoiceLines/:id` 2xx. Fresh session must show
   `2,00` on the same input. Then reverse-delete 6 drafts, 6
   products, 6 customers. Third session: no `MCP-UI-INV` /
   `MCP-UI-PRD` tags.
6. Vision record: `author=live_test`,
   `reviewer_verdict=pending_review`. Do not self-accept. Do not
   purge in the live test. Honesty-16 stay red.

## Out of slice

New invoice create, files, daybook, **Godkend**, emails, bulk,
docs re-lock, dump remakes, child spawn, `ui_invoice_lines_*`
tools, live API, persist-watch rewrite.

## Success

One FastMCP update that a fresh interface session reads as
**Enhedspris** `2,00`, then an empty org. Coverage stays red
until independent accept and purge.

## Post-Mortem

Work completed: offline bind of
`input[name='unitPrice'][placeholder='Enhedspris']`. Fill `2,00`
once on that locator. Persist watch remains 2xx
`PUT /v2/invoiceLines/:id`. `reveal_line_editor` and frame search
were added then removed after IR FAIL (`3D5A9B9C`). Missing-input
details now return sanitized path, heading, exact-input count,
product-text count, and input count.

Deviations: live FastMCP update did not fill `2,00`. Headless
leftover edit heading is `Rediger fakturakladde` with
`exact_count=0` after a full-render wait. Owner `3CB4D807` sees
the exact input on the same URL.

Review: IR FAIL. Guessed line click and frame walk removed.

Tests: lint passed. Commit-mode `test.sh` 2095 passed, 82
deselected. Live invoice CUD failed `UI_CHANGED` with heading
`Rediger fakturakladde`, `exact_count=0`, `input_count=8`. No
seventh invoice. Six leftover triples remain.

Cleanup: not run. Stay red.

Next slice: wait or recapture the headless heading and URL until
the exact visible main-frame input is present, then fill `2,00`,
line PUT, fresh `2,00`, reverse-delete six triples. No guessed
clicks.

