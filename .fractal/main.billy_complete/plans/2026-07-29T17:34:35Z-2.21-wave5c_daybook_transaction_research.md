# Plan 2.21 — Wave-5c daybook transaction research

## Goal

Refresh the official-contract research brief for the next offline ticketed-write
slice (`daybookTransactions` + `daybookTransactionLines`) after Wave-5b repair
ACCEPT.

## Done this research step

- Re-fetched https://www.billy.dk/api/ → ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (unchanged).
- Unauth probes for Wave-5c collections and adjacent blockers (no secrets, no
  persistent data) → `tmp/write-probes-research21.json`.
- Cited brief replaced at `tmp/grok-research.md`.
- Memory state updated. No coverage greening. No product code changes.

## Next (Plan / Implement; not this step)

1. Promote brief to `wiki/wave_fivec_ticketed_writes_contract.md`.
2. Implement 12 tools (6 inventory rows) with mandatory `execute_tool_name`.
3. Offline tests + registry 154 `api_*` + coverage 124 offline; live stays 0.
4. Independent product review before Wave-5d.

## Boundaries

- Research only. No green coverage. No headed browser. No live token.
