# 2.55 Wave-5l Sales-Tax-Payment Product

## Objective

Deliver the already research-authorised, offline-only Wave-5l API slice:
exactly the create and update preview/execute pairs for singular
`salesTaxPayments`. The frozen source of truth is
`wiki/wave_fivel_ticketed_writes_contract.md`, accepted by
`wiki/wave_fivel_freeze_independent_review.md`; the current cited Grok
product-ready brief is `.fractal/main.billy_complete/tmp/grok-research.md`.

## Gate and scope

- Official docs remain ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`; unauthenticated POST and PUT are 401,
  while singular DELETE is an explicit 405 refusal.
- Authorise only these tools:
  `api_sales_tax_payments_create_preview`,
  `api_sales_tax_payments_create_execute`,
  `api_sales_tax_payments_update_preview`, and
  `api_sales_tax_payments_update_execute`.
- The inner `salesTaxPayment` object is opaque; outer inputs are strict. A
  supplied inner `id` must equal the update route id.
- Keep the shared `WriteProtocolService` / confirmation store, exact executor
  binding, one-request execution, and client-relative `/salesTaxPayments`
  paths. Required response root is only `salesTaxPayments`.
- Do not add delete or bulk tools, UI/browser work, live qualification,
  webhooks, a second confirmation store, or a completeness claim. Do not edit
  `sales_tax_writes.py`.

## Delegation decision

This is a cohesive but substantive implementation-and-test slice with a clean
file boundary, so delegate it to one child rather than edit concurrently on
root. The child is a `codex-power` leaf because it owns repository-local
implementation and verification; current official research is already cited.
There is no parallel review child yet because the independent review must
inspect the merged, committed root product.

Before launch, checkpoint the adopted research handoff, current state, and
this plan on root (after restoring the runtime-only `codex-power.err` change),
then run the following configuration flow from the root worktree:

```text
fractal node init wave5l_sales_tax_payment_product \
  --path=/Volumes/ssd_1/Repositories/billy-mcp \
  --base=main.billy_complete \
  --agent=codex-power \
  --max-cost=320 --max-iter-cost=80 --max-iters=10 \
  --step-timeout=30m --max-depth=0 --max-children=0 --max-descendants=0
```

Configure the resulting `NODE.md` before launch with the frozen contract and
the following exclusive product-file ownership:

- `src/billy_mcp/api/sales_tax_payment_writes.py` (new)
- `tests/api/test_sales_tax_payment_writes.py` (new)
- `src/billy_mcp/server.py`
- `scripts/generate_coverage_report.py`
- `tests/unit/test_coverage_server.py`
- generated `coverage/api_v2_manifest.yaml`, `coverage/status.json`, and
  `coverage/report.md`

The child may maintain only its own `.fractal/main.billy_complete.wave5l_sales_tax_payment_product/`
seed for lifecycle state. It must not modify the root wiki, other product
modules, the contract page, or any other coverage row. Root will exclude child
seed artifacts from the product merge.

## Child deliverable and verification

1. Mirror the bank-payment implementation and contract suite with the
   sales-tax-payment names, roots, paths, and exact four-tool registration.
2. Cover flat schemas, strict outer inputs, route/body-id equality, opaque map
   preservation, mutation-free preview, canonical ticket binding, URL-encoded
   update path, required-root validation, typed HTTP errors, no-token no-I/O,
   ticket tampering/wrong executor/replay/expiry, and no retry after a failed
   write.
3. Update generated coverage only after real tests pass: create and update
   become implemented and contract-tested, live remains false; registry becomes
   246 `api_*` tools and offline coverage becomes 170 rows, with
   `complete: false`.
4. Run formatting, Ruff, Pyright, coverage/policy checks, the focused suite,
   and `BILLY_TEST_MODE=commit` non-live tests. It must not claim a full-mode
   pass while red coverage remains. Commit its product bytes and report the tip
   and exact checks through its outbox.

## Root integration and follow-up

1. Monitor the child through radio and node status; if it stops without the
   deliverable, inspect the committed diff and either continue it or explicitly
   absorb the task.
2. Inspect the committed three-dot product diff and a clean archive, reject any
   child-seed leakage or out-of-scope changes, then merge the accepted child
   with `git merge --no-ff -m "merge main.billy_complete.wave5l_sales_tax_payment_product"`.
3. Run root formatting, lint/type/policy checks, focused tests, generated
   coverage validation, and the non-live suite. Reconcile the wiki index only
   if the merge changes shared wiki content.
4. Only after the root merge, request a fresh Grok independent product review.
   A product ACCEPT may record offline implementation only; live, UI, vision,
   bulk, singular delete, and completeness remain fail-closed.

## Post-Mortem

- Completed: checkpointed the cited research, configured and launched one bounded
  Codex Power implementation leaf, and kept root free of unreviewed product
  changes.
- Verification: root baseline non-live tests passed (1,038), as did Ruff,
  Pyright, coverage validation, and repository policy before launch.
- Review: Grok ACCEPTed the research package and plan only; it confirmed no
  root product module exists yet and found no research-plan defect to fix.
- Cleanup: no credentials, live records, browser evidence, or generated raw
  artifacts were created on root; the transient runtime error log is excluded
  from the next commit.
- Next unresolved slice: inspect the child’s committed product diff, integrate
  only its allowed product bytes, then obtain a separate Grok product review;
  delete, bulk, live, UI, vision, and completeness remain red.
