# 186.77 ui organizations get update dual-count create NA

## Goal

Product research177 ACCEPT:

1. Dual-count exact UI parity rows onto existing tool **`ui_settings_company_open`**:
   - `api.organizations.get` → `ui.parity.organizations.get`  
     `parity_status=detail_open_only` (company form identity freeze: Navn/CVR/Adresse family)
   - `api.organizations.update` → `ui.parity.organizations.update`  
     `parity_status=form_open_only` (same panel + **Gem ændringer** observe-only)
2. Freeze UI parity **`not_applicable`** for exact:
   - `api.organizations.create` → `ui.parity.organizations.create`  
     (no create-virksomhed CTA dual; research177)
3. Keep greened `ui.discovery.settings_company` and `ui.parity.organizations.list`
   (`shell_open_only`) unchanged. Do **not** dual-count-steal list onto get/update.
4. **No** new FastMCP tool (peer daybooks list+create on one tool). **No** bulk_* greening.
5. Observe-only product path: **never** click Gem ændringer / Tilføj ejer / upload / Opret*.

Target math (tip `cf87db9` / product 186.76):
live/vision **178 → 181**; implemented/contract **362 → 365**;
`GEO_UI_NOT_APPLICABLE_ROW_COUNT` **110 → 111**; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research177)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. organizations Supports get/list/create/update/bulk save/bulk delete (no singular delete). |
| Company panel dual | path `/:org_slug/settings` dual; labels Navn/CVR/Adresse/Land/Telefon/Fax/E-mail dual; inputs_n 12 dual; names `name`, `registrationNo`, `street`, `city`, `countryId`, `phone`, `email`, … dual |
| Update chrome | **Gem ændringer = 1 dual**; never clicked |
| Create CTAs | Opret/Ny/Tilføj virksomhed **0 dual** → NA create |
| List already green | `ui_settings_company_open` maps organizations.list only (research146) |
| Peer pattern | daybooks.list (`shell_open_only`) + daybooks.create (`form_open_only`) on same tool (research156/157) |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research177_focus_dual.json`.  
Probe: `tmp/research177_focus_probe.py` (tmp only; not required in git).

## Non-goals

- New `ui_organizations_*` tools
- Clicking Gem ændringer or mutating org settings
- organizations.bulk_save / bulk_delete greening
- accounts get/create/update/delete NA (research177 ready follow-on 186.78+)
- special.invoice_email NA (ready follow-on)
- daybooks residual; users residual; invoiceLines dual-count steal
- annual_reports; bulk 92 external-contract
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Egress changes (not required for settings shell)
- Changing greened list shell_open_only semantics

## Ownership

| Area | Owner |
| --- | --- |
| Generator dual-count get/update + create NA | Root Grok EXECUTE |
| Inventory unit tests (count 111; get/update green; create NA; list unchanged; bulk red) | Root Grok EXECUTE |
| Live test strengthen (optional DOM form/Gem asserts; never click Gem) | Root Grok EXECUTE |
| Wiki company shell dual-count + create NA note | Root Grok EXECUTE |
| Vision: reuse existing company vision record path if still valid; else dual reconfirm + purge | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research177)

### A. Generator dual-count (`scripts/generate_coverage_report.py`)

1. Extend `apply_ui_settings_company_open_shell_evidence` with mutually exclusive flags
   (peer daybooks applicator):

```python
def apply_ui_settings_company_open_shell_evidence(
    row: dict[str, Any],
    *,
    parity_of_api_list: bool = False,
    parity_of_api_get: bool = False,
    parity_of_api_update: bool = False,
) -> None:
```

   - Raise if more than one of list/get/update is true.
   - **list** (existing): `parity_status=shell_open_only`; evidence research146; api_row_id list.
   - **get** (new): `parity_status=detail_open_only`; method_or_route notes company identity form
     open only; evidence research177 dual form fields (Navn/CVR/Adresse…); api_row_id get;
     maps api.organizations.get; never Gem.
   - **update** (new): `parity_status=form_open_only`; method_or_route notes Gem ændringer
     form open only; evidence research177; api_row_id update; sensitivity medium (form write surface
     observe-only, peer daybooks.create); never Gem.
   - Discovery row (no flags): unchanged shell_open_only; api_row_id None.

2. Wire mapping in UI parity generation:

```python
if api_row["id"] == "api.organizations.list":
    apply_...(row, parity_of_api_list=True)
elif api_row["id"] == "api.organizations.get":
    apply_...(row, parity_of_api_get=True)
elif api_row["id"] == "api.organizations.update":
    apply_...(row, parity_of_api_update=True)
```

3. Update applicator docstrings/non-claims: remove “does not green get/update”; keep
   “does not green create/bulk” until create NA applied separately.

### B. Generator NA create

1. Exact id set (not `api.organizations.` prefix — would NA list/get/update/bulk):

```python
GEO_UI_NOT_APPLICABLE_RESEARCH177_ORG_CREATE_IDS: frozenset[str] = frozenset(
    {"api.organizations.create"}
)
```

2. Include exact string in `GEO_UI_NOT_APPLICABLE_API_PREFIXES` (same technique as
   research176 product ids / research150 specials).
3. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **110 → 111**.
4. Extend `apply_ui_geo_reference_not_applicable_evidence`:
   - `is_research177 = api_row_id in GEO_UI_NOT_APPLICABLE_RESEARCH177_ORG_CREATE_IDS`
   - research_id `research177`; evidence_ref `research177_organizations_create_dual`
   - prose: company panel dual present; create CTA 0 dual; list/get/update dual-count separate
   - contrast: settings company tool exists for list/get/update; create still absent

### C. Flags

**Get/update dual-count rows**

| Field | get | update |
| --- | --- | --- |
| tool_name | `ui_settings_company_open` | same |
| parity_status | `detail_open_only` | `form_open_only` |
| api_row_id | `api.organizations.get` | `api.organizations.update` |
| discovered…vision_verified | all true | all true |
| vision_evidence | null (reuse company shell vision; purge verified) | null |
| cleanup | not_applicable; read-only | same |
| Never | click Gem ændringer | same |

**Create NA row**

| Field | Value |
| --- | --- |
| parity_status | `not_applicable` |
| tool_name | empty |
| flags | all five true |
| vision_evidence | null |
| qualification.kind | `ui_not_applicable` |
| not_applicable_decision | `accepted` |
| sessions | `dual_independent_ephemeral` |
| evidence_ref | contains `research177` |
| evidence_code | `GEO_UI_NO_EQUIVALENT_WORKFLOW` |

### D. Tests (`tests/coverage/test_coverage_inventory.py`)

1. `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 111`.
2. Qualified set includes:
   - `ui.parity.organizations.list` → tool company, `shell_open_only`, research146
   - `ui.parity.organizations.get` → tool company, `detail_open_only`, research177, api get
   - `ui.parity.organizations.update` → tool company, `form_open_only`, research177, api update
3. Create is **NA** (not discovery_required): in geo NA rows; evidence_code GEO; research177 branch.
4. Remove create from “still red discovery_required” residual list; keep bulk_save/bulk_delete red.
5. Tool map: get/update rows map to `ui_settings_company_open`.
6. Update any aggregate live/vision math asserts that hardcode 178/362 if present.

### E. Live test (`tests/live/test_ui_settings_company_open.py`)

Optional strengthen (recommended; still observe-only):

1. After dual tool success, assert page has company identity freeze dual:
   - label or name presence for Navn/`name` and CVR/`registrationNo` (or existing panel markers if already strong enough).
   - button **Gem ændringer** count ≥ 1 dual.
2. **Never** click Gem ændringer / Tilføj ejer / upload.
3. Keep dual independent ephemeral profiles; no `BILLY_API_TOKEN`.
4. Vision: reuse `tmp/vision-records/ui_settings_company_open.json` if purge_verified still true;
   else capture frames outside repo, write non-sensitive record, purge, mark verified.

No model change required if success already freezes company_panel_markers_present + path/heading/shell_kind.
Only add model fields if live asserts need typed product fields (prefer no model churn).

### F. Wiki

1. Update `wiki/ui_settings_company_open_shell.md`:
   - Dual-count get (research177 detail_open_only) + update (form_open_only)
   - Non-claims: create is NA (research177); bulk stay red; never Gem
2. Optional short `wiki/ui_organizations_create_not_applicable.md` (peer products NA wiki)  
   or fold create NA into company shell page Non-claims — prefer dedicated NA page if peer packages do.

### G. Regenerate coverage

```bash
unset VIRTUAL_ENV
uv run python scripts/generate_coverage_report.py
# or project’s existing regenerate entry used by prior EXECUTE
```

Confirm `coverage/status.json`: implemented/contract **365**; live/vision **181**; complete false;
blocker still mentions bulk 92 + annual + residual UI.

## Implementation order (EXECUTE)

1. Generator dual-count flags + mapping + create NA set + count 111 + NA evidence branch.
2. Inventory tests updates.
3. Live test optional DOM strengthen.
4. Wiki update.
5. Regenerate coverage manifests/status/report.
6. `bash .fractal/main.billy_complete/scripts/lint.sh`
7. Offline `BILLY_TEST_MODE=commit bash .fractal/main.billy_complete/scripts/test.sh` (or project commit mode).
8. Live company dual test if credentials present (required for live_tested claim on new rows — dual-count reuses live tool path; existing live test must still pass; strengthen if asserts added).
9. Stop for IR / FIX-VERIFY / COMMIT. Not finish.

## Risks and guards

| Risk | Guard |
| --- | --- |
| Prefix NA steals list/get/update/bulk | Exact id `api.organizations.create` only |
| Dual-count steal list onto get | Separate flags; list stays shell_open_only; get detail_open_only |
| Clicking Gem mutates production org | Observe-only; never click write CTAs in product/tests |
| False-green create via company form | Create is NA; form is get/update of **active** org only |
| accounts/invoice_email scope creep | Explicit non-goals; follow-on slices |
| Disk ENOSPC on root | TMPDIR under worktree on ssd_1; no root browser profiles |

## Done when

- Generator greens organizations.get + update on `ui_settings_company_open` with correct parity_status.
- organizations.create is NA with research177 evidence; count 111.
- organizations.list discovery + list parity unchanged shell_open_only.
- bulk org rows still red external-contract.
- Inventory + offline tests pass; live company dual passes; lint/types clean.
- coverage status live/vision 181, contract 365, complete false.
- Plan post-mortem filled at FIX-VERIFY. Not node finish.

## Post-mortem

_(fill at FIX-VERIFY)_

## Post-Mortem

### Completed
- Dual-count `ui.parity.organizations.get` (`detail_open_only`) and
  `ui.parity.organizations.update` (`form_open_only`) onto existing
  `ui_settings_company_open` (research177 company form + Gem ændringer dual).
- NA freeze exact `api.organizations.create` / `ui.parity.organizations.create`
  (create CTAs 0 dual; GEO count 110→111).
- Generator mutual-exclusion flags; inventory asserts; wiki company shell +
  create NA page; coverage regenerated to implemented/contract **365**, live/vision **181**.
- Offline `BILLY_TEST_MODE=commit`: **1642 passed** / 50 deselected.
- Live company dual: **1 passed** (EXECUTE); FIX-VERIFY reconfirm after N1 Gem assert.
- IR: product **ACCEPT**; overall completeness **FAIL** expected.

### Deviations
- No new FastMCP tool (plan dual-count path).
- Optional IR N1 applied in FIX-VERIFY (live Gem ændringer observe-only assert).
- Optional IR N2 (evidence prose noise) deferred as cosmetic.

### Review findings
- Required fixes: none.
- Optional N1: applied. N2: deferred.

### Tests
- lint.sh: pass (wiki index updated at EXECUTE).
- Offline commit-mode: 1642 passed.
- Live: `tests/live/test_ui_settings_company_open.py` pass.
- No ui-full / no live API.

### Cleanup
- Research177 profiles purged dual; live ephemeral profiles purged; vision
  record purge_verified true. Root disk still low (~256 MiB); TMPDIR on ssd_1
  worktree; no saxo /tmp touch.

### Next unresolved coverage slice
- Prefer **accounts.get+create+update+delete NA** (+4) from research177 dual absence.
- Then **special.invoice_email NA** (+1).
- DEFER daybooks residual, users residual, invoiceLines embedded dual-count.
- bulk 92 external-contract + annual_reports org_inaccessible remain blockers.
- complete remains false. Not node finish.
