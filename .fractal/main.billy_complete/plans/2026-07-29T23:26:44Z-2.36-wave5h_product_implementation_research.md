# Research36 — Wave-5h product implementation handoff

## Verdict
Official docs fingerprint unchanged (`hsisik4g9p3603` / `c2efda0ee4cf9cf200e14910c5fc6996` / 147934). Wave-5h freeze and product-ready research remain ACCEPT. Next implementation slice is offline attachment JSON CUD product (6 tools → registry 208 / coverage 151). No coverage greened.

## Brief
`.fractal/main.billy_complete/tmp/grok-research.md`

## Unauth probes (no secrets)
- POST/PUT `/attachments` → 401
- DELETE `/attachments/missing` → 200 (not cleanup)
- POST/PUT salesTaxAccounts + salesTaxMetaFields → 401; DELETE missing → 200
- POST `/files` valid JSON → 401; invalid JSON → 400 INVALID_JSON
- accountNatures create / postings delete → 405 (stay blocked)

## Codex Power slice
Implement `src/billy_mcp/api/attachment_writes.py` under `wiki/wave_fiveh_ticketed_writes_contract.md`: opaque attachment root, bodyless delete, no files multi-root, shared ConfirmationStore, real offline suites only.

## Out of scope
Bulk, binary files special, live/UI/vision, Wave-5i product before freeze.

## Execution plan

### Decision and split

The cited Grok brief has refreshed the official contract without source drift,
and the accepted Wave-5h freeze plus product-ready review open the offline
implementation gate. The attachment JSON CUD slice is an independent API area
with a new module, focused contract suite, registration, and coverage evidence;
it warrants one focused Codex Power leaf rather than an inline change. The
already-completed supplemental static review is retained as non-authoritative
evidence only, so no duplicate review child will be started before product
exists.

### Leaf delivery: `wave5h_attachment_product`

Create a Codex Power leaf after the current planning evidence is committed:

```text
fractal node init wave5h_attachment_product \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --base=main.billy_complete --agent=codex-power \
  --title='Wave-5h Attachment Product' \
  --max-iters=8 --max-depth=0 --max-children=0 --max-descendants=0 \
  --step-timeout=20m
```

The child exclusively owns these product files (and may add the named new test
file):

- `src/billy_mcp/api/attachment_writes.py`
- `src/billy_mcp/server.py`
- `tests/api/test_attachment_writes.py`
- `tests/api/test_attachment_cross_executor.py`
- `tests/unit/test_coverage_server.py`
- `scripts/generate_coverage_report.py`
- generated coverage artifacts and directly affected coverage tests

It must implement only the six frozen tools:
`api_attachments_{create,update,delete}_{preview,execute}`. Preview inputs are
strict outer Pydantic models with an opaque `attachment` object; update checks a
provided inner `id` against the route id; delete is bodyless. All tools share
the root `WriteProtocolService`, bind each exact executor name, map only the
`attachments` root, and preserve optional delete metadata. The child must not
implement attachment bulk, JSON `files` create, binary upload, UI/auth/live
flows, or modify the common write protocol without escalating a demonstrated
defect.

Its product tests must prove flat FastMCP schemas, no preview HTTP mutation,
exact POST/PUT/DELETE paths and bodies, response-root boundaries, validation,
auth/not-found mapping, ticket tampering/expiry/replay/binding, and a
cross-resource executor-name mismatch before consumption or HTTP. It may flip
only the three attachment CUD rows after those real tests and root registry
coverage evidence pass; target totals are 208 `api_*` tools and 151
implemented/contract-tested API rows, with live/UI/vision/bulk remaining red.

### Root follow-through

1. Configure and start the leaf with the accepted Wave-5h contract and this
   cited research brief as its immutable scope.
2. Monitor it through radio; merge only a committed, clean product branch and
   independently re-run the non-live verification plus coverage honesty checks.
3. Request a new Grok product review after merge. Its scope is the root diff,
   official docs freshness, registry/coverage arithmetic, and fail-closed
   exclusions; it must not treat the existing static fallback review as the
   authoritative product gate.
4. Keep all live, interface, vision, binary-file, bulk, and Wave-5i work out
   of this plan until their distinct evidence gates are met.

## Post-Mortem

- Completed: the current official-doc handoff was independently Grok-accepted
  as research only, the root baseline recorded the evidence, and the focused
  Codex Power attachment-product leaf was configured and launched from that
  baseline.
- Review result: there was no root product diff to accept. The independent
  review reconfirmed the three attachment CUD rows remain red, the registry is
  still 202 tools, and `complete` remains false. Its sole required future work
  is real product implementation followed by a separate product review.
- Verification: both knowledge stores lint with no issues; formatting, Ruff,
  Pyright, coverage and repository-policy checks pass; the non-live suite is
  892 passing tests. No full-completeness mode was run because the maintained
  live, UI, vision, bulk, and special-route rows are intentionally red.
- Cleanup: research and review used no credentials, browser session, frames,
  HAR files, traces, or persistent Billy records. No coverage row was changed
  by research or review.
- Next unresolved coverage slice: the active child must ship the six singular
  attachment preview/execute tools, real offline suites, registry 208, and
  coverage 151. Root must then merge only a clean result and obtain a distinct
  Grok product review; bulk and binary `/files` remain excluded.
