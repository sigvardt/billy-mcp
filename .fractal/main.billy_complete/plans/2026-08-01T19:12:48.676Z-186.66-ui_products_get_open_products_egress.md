# 186.66 ui products get open + products data-plane egress

## Goal

Product research165 ACCEPT:

1. **Scoped browser egress expansion** on `api.billysbilling.com` `path_allow`
   so the Billy SPA can complete products UI data-plane XHR needed for list
   rows, detail open, and empty-org disposable seed/cleanup (today blocked dual:
   `GET /v2/products` → `net::ERR_BLOCKED_BY_CLIENT`; contacts plane already open
   from 186.65).
2. New read-only FastMCP UI tool **`ui_products_get_open`** that opens a
   **product detail** surface as observe-only get/open (never Gem/Slet submit).
3. Green exact **`ui.parity.products.get`** / `api.products.get` only onto
   `ui_products_get_open` (`detail_open_only` / get-open parity).
4. Keep greened `ui_products_list`, `ui_products_create_open`,
   `ui_products_import`, and productPrices dual-NA unchanged.

Target math (from tip `73c8ebc` / product 186.65 clients get open):
live/vision **167 → 168** (parity get only; no new discovery seed unless
generator requires one — prefer parity-only +1); implemented/contract
**351 → 352**; complete **false**.

If EXECUTE proves durable create is required for an empty org after GET allow
(research165: product list 0 rows dual under blocked GET; contacts empty dual),
include test-only disposable create/cleanup inside the live test (not a
ticketed UI write tool). Add `GET /v2/accounts` (and optional
`GET /v2/salesTaxRulesets`) path_allow only if seed form dual proves they are
required. Do **not** green products.create as durable write.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research165)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. products Supports get by id / list / create / update / delete / bulk. |
| Contacts plane | 186.65 greened contacts.get; `GET /v2/contacts` **200** dual. Org contacts **empty** (`/:org_slug/clients/empty` dual). |
| Products residual | `GET /v2/products` dual **ERR_BLOCKED_BY_CLIENT**; products list 0 rows dual; products_get detail_ready **false** dual. |
| Sibling residual | invoices/bills GET + summary blocked dual (header-only grids); invoice_email n/a until invoices plane. DEFER. |
| Tool gap | `ui_products_list` list chrome only; `ui_products_create_open` form open only (inventory Opret produkt); no get/detail tool; `ui.parity.products.get` still `discovery_required`. |
| Isolation | Get-open ≠ list shell ≠ create form_open ≠ import shell. Green only **products.get**. Leave update/delete/bulk/invoice_email red. |
| Deferred | special.invoice_email; invoices.get; bills.get; contacts.update/delete; products.update/delete; salesTaxRules NA steal; bulk 92; annual_reports Upsedasse. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research165_focus_dual.json` (+ probe `research165_focus_probe.py`).

## Non-goals

- Full `browser_action: allow` on `api.billysbilling.com` (keep path_allow)
- Greening invoices/bills/emails/attachments/taxRates egress this slice
  (only products data plane + accounts GET if seed requires; optional
  salesTaxRulesets GET if seed form cannot complete without it)
- Greening `products.update` / `delete` / bulk_* / special.invoice_email
- Ticketed durable UI write tool for products create (API writes stay offline)
- Replacing or dual-counting onto `ui_products_list` /
  `ui_products_create_open` / `ui_products_import`
- Submitting Gem/Slet/Rediger save on detail after open (product path)
- Treating `…/empty` list empty-state as detail (research165 classifier FP note)
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| Egress YAML + allow-matrix unit/coverage tests | Root Grok EXECUTE |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record | Root Grok EXECUTE |
| Coverage generator (products.get dual-count) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research165)

### A. Browser egress (`coverage/browser_egress.yaml`)

Host `api.billysbilling.com` remains `browser_action: path_allow`.

**Keep** existing contacts + countries + auth/bootstrap rules from 186.65.

**Add** (minimum set; adjust only with EXECUTE network dual proof):

| match | methods | path | purpose |
| --- | --- | --- | --- |
| prefix | GET | `/v2/products` | list + get-by-id SPA reads |
| prefix | POST | `/v2/products` | test-harness disposable create only if org empty |
| prefix | DELETE | `/v2/products` | live cleanup of disposable seed |
| prefix | GET | `/v2/accounts` | product form account options for seed (blocked dual today) |
| prefix | GET | `/v2/salesTaxRulesets` | optional; only if seed form dual cannot complete without it |

**Keep denied** (explicit regression):

- `POST /v2/invoices/`…`/emails` (invoice_email later)
- `GET /v2/invoices`, `GET /v2/invoices/summary`
- `GET /v2/bills`, `GET /v2/bills/summary`
- `GET /v2/attachments` (+ counts) unless EXECUTE proves product detail cannot
  open without it (prefer omit)
- `api.billy.dk` deny
- default_action deny

Update host `evidence` / `condition` / `test_references` to cite research165 +
new live test. Offline coverage inventory assertions that enumerate path_allows
must expect the new products rules and still assert invoices/emails paths remain
absent.

### B. Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_products_get_open` |
| lane | ui |
| kind | detail_open_only (read-only observe) |
| shell_kind | `products_get` |
| success model | `UiProductsGetOpenSuccess` (Pydantic, `extra=forbid`) |
| error model | existing `ToolError` (`AUTH_*`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — `api.products.get` already offline implemented |

### C. Success model fields (non-PII)

Mirror `UiClientsGetOpenSuccess` style (no org_slug, raw URL, selector secrets,
customer PII):

| Field | Contract |
| --- | --- |
| `path_class` | Freeze exact Literal from EXECUTE dual after open (candidates: `/:org_slug/products/:id`, inventory-rooted product detail, or SPA-specific product profile path — **observe, do not invent**). Reject `…/empty` and pure list paths. |
| `shell_kind` | Literal `products_get` |
| `detail_open` | bool — product detail surface open |
| `product_name_visible` | bool — product name chrome/field visible on detail |
| `edit_action_visible` | bool — Ret/Rediger (or equivalent) chrome if present; may be false if detail is pure observe |
| `detail_markers_present` | bool — aggregate: strict detail_ready (path off list **and** name/content signature; **not** header-only row/dialog; **not** create form open alone) |
| `shell_markers_present` | bool — same aggregate used by existing open tools |

Strict **reject**:

- Header-row false positives (`Navn / Pris / …` alone)
- Catalog list chrome without detail path change
- Inventory create form (`products_create` / Opret produkt form) as get success
- Soft `/products/new` chrome-only body (research163)

### D. Navigation (observe-only product path)

1. Headless Playwright via `BrowserRuntime`; egress from updated
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Prefer open `https://mit.billy.dk/{slug}/products`, settle until list XHR can
   complete under new egress. If catalog has no openable rows after allow but
   inventory holds products, EXECUTE may freeze inventory list path as alternate
   entry (document in model/path_class).
4. Open first **non-header** data row or id link → detail.
   - Prefer anchor `…/products/{id}` (or dual-proven product id path).
   - Else row click excluding header rows (research164/165 classifier rules).
5. Success when strict detail_ready:
   - path_changed to product detail (not list, not `/empty`, not create form)
   - product name visible and/or content signature dual-proven
   - never treat table header dialog as success
6. Escape only after classify; **never** click Gem / Slet / submit / permanent edit.
7. Independent second-session read-back of same detail signature.

### E. Empty-org live test harness (not a product write tool)

Research165 shows product list empty under blocked GET; after GET allow org may
still be empty.

If after GET allow the list has zero openable rows dual:

1. Live test may open inventory Opret produkt form (reuse create-open selectors),
   fill disposable marker name + minimal account/ruleset/unitPrice as dual
   proves required, click Gem **once**, wait for list marker.
2. Then run get-open on that product row.
3. Cleanup: delete disposable via UI if DELETE allowed; verify marker absent on
   fresh list read-back (second session preferred).
4. Never leave residual markers; never product a public ticketed create-execute
   tool in this slice.
5. If POST allow is unnecessary (rows already exist), omit POST/DELETE path
   rules to keep egress minimal (EXECUTE may tighten after dual).

### F. Coverage generator

- Apply shell evidence helper analogous to clients get / products create-open.
- Dual-count **exact** `api.products.get` / `ui.parity.products.get` only.
- Do **not** dual-count list, create, update, delete, bulk, productPrices, import.
- Assert residuals stay red: products.update/delete/bulk; special.invoice_email;
  invoices.get; bills.get; contacts.update/delete; annual_reports; bulk 92
  external-contract.
- Assert greened tools unchanged: `ui_products_list`, `ui_products_create_open`,
  `ui_products_import`, productPrices NA, `ui_clients_get_open`.
- live/vision 167→168; implemented/contract 351→352; complete false;
  API live_tested false + `out_of_scope_by_user`.

## Implementation checklist (EXECUTE)

1. **Egress** — edit `coverage/browser_egress.yaml` products (+ accounts /
   optional rulesets) path_allows; unit tests for allow/deny matrix; coverage
   inventory egress assertions (still deny invoices/emails samples).
2. **Models** — `UiProductsGetOpenInput` / `UiProductsGetOpenSuccess` in
   `src/billy_mcp/models.py` + `tests/test_models.py`; freeze path_class Literal
   from post-egress dual observation.
3. **Browser** — protocol + `BrowserRuntime.ui_products_get_open`; strict
   detail open helpers; never submit; reject empty-state and create-form.
4. **Server** — register FastMCP tool with typed return.
5. **Unit tests** — success path; reject header-only; reject list-without-detail;
   reject create-form-as-get; egress still denies invoices/emails samples;
   contacts rules still present.
6. **Live test** — `tests/live/test_ui_products_get_open.py`: dual ephemeral
   profiles, DOM assertions, second-session read-back, vision record under
   node tmp, purge frames, no API token; optional disposable seed+cleanup.
7. **Coverage generator** + inventory tests for products.get green math;
   residual products.update/delete stay red.
8. **Wiki** — `wiki/ui_products_get_open_shell.md` (scrubbed) + index links from
   products list / create pages; note egress dependency.
9. **Regenerate** coverage manifests/status/report; lint + offline test.sh.
10. Leave research dual JSON in owner tmp only (not commit secrets/PII).

## Acceptance

- Egress allows products GET (and only the extra methods EXECUTE proves needed).
- Tool registered; offline unit + inventory tests pass.
- Live dual test passes when credentials present.
- Vision record non-sensitive + purge verified.
- Coverage: products.get parity green; list + create-open + import tools
  unchanged; residual reds as above; complete=false; bulk 92 red; annual red;
  API live false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Safety rails

| Risk | Mitigation |
| --- | --- |
| Over-broad API browser allow | path_allow only; unit deny invoices/bills/emails samples; keep contacts rules |
| Header-row / empty-state false green | strict detail_ready; unit test header + `/empty` + create-form rejection |
| Accidental product mutation | never Gem/Slet in product path; live asserts no unintended marker unless harness owned |
| Harness create leftover | reverse cleanup + fresh list read-back dual |
| Dual-count steal onto list/create | generator exact `api.products.get` only; inventory asserts list/create/import tools unchanged |
| Live API leakage | unset/forbid `BILLY_API_TOKEN`; api_token_used false |
| Children / wrong agent | root-only Grok; no spawn |

## Feasibility

- Uncapped budget/time/cost. Root-only. Research dual evidence already on disk.
- Pattern proven by 186.65 contacts get + path_allow.
- Largest risk: SPA product detail DOM/path differs after egress unlock, or seed
  form needs extra GET allows (accounts/rulesets) — freeze model literals in
  EXECUTE from fresh dual, not from pre-egress false opens.
- If get-open still impossible after minimal egress (platform blocker), do **not**
  false-green; fall back to egress-only product with complete=false and document
  blocker (prefer still shipping get-open).

## Done when

- Plan producted in EXECUTE and IR ACCEPT (or required fixes landed).
- FIX-VERIFY: lint + offline tests green; coverage math 167→168 / 351→352;
  residual reds as above; complete false.
- Ready COMMIT 186.66. **Not** `fractal node finish`.

## Post-Mortem

- **Product shipped:** products UI data-plane **egress only** (research165 plan
  fallback). No `ui_products_get_open` tool (detail surface not dual-openable).
- **Egress added:** GET/POST/DELETE `/v2/products`, GET `/v2/accounts`, GET
  `/v2/salesTaxRulesets` on `api.billysbilling.com` path_allow (contacts retained).
- **Dual evidence:** GET products 200; catalog seed POST 200 with Opret produkter
  + Gem produkt; list shows disposable marker; residual cleaned via SPA
  `x-access-token` DELETE (list empty after). Inventory seed 422 without
  inventoryAccountId.
- **Why get tool not greened:** table-item / product-name click stays on list;
  `/products/:id` and `/edit` never hydrate form (nav chrome only). Greening
  list-row as products.get would be false parity. products.get stays red.
- **live/vision:** 167 (unchanged). **implemented/contract:** 351 (unchanged).
- **Offline tests:** lint pass; test.sh **1610 passed** / 42 deselected
  (EXECUTE + FIX-VERIFY reconfirm).
- **IR:** ACCEPT (`tmp/grok-review.md`); required fixes **none**; optional N1–N3
  deferred (evidence wording list/seed vs detail; inventory DELETE assert;
  DELETE path breadth).
- **complete:** false. bulk 92 + annual + products.get residual red.
- **Next:** re-probe product detail after any SPA change, or alternate get surface
  with dual proof; invoices.get after invoices path_allow; no bulk greening.
