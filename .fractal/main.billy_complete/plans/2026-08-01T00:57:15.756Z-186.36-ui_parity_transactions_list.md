# 186.36 ui parity transactions list dual-count shell open

## Goal

Dual-count green **`ui.parity.transactions.list`** by mapping the already
qualified read-only **`ui_transactions_list`** Posteringer shell to
**`api.transactions.list`**, using the same empty-input list-shell-open-only
contract already live for `ui.discovery.transactions`.

No new browser workflow. No new API write tools. No annual product. No residual
405 ticketed writes.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research135).  
Discovery product tip (already green): `ui_transactions_list` (research118 /
prior 186.19).  
Live evidence already on tip: `tests/live/test_ui_transactions_list.py`, vision
record class `tmp/vision-records/ui_transactions_list.json`.  
API offline list already green: `api.transactions.list` → `api_transactions_list`
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–135).  
Prior dual-count precedents: `ui.parity.invoices.list`, `ui.parity.products.list`,
`ui.parity.contacts.list`, `ui.parity.bills.list` via
`parity_of_api_list=True` in `scripts/generate_coverage_report.py`.

## Why this slice (research135 decision tree)

1. **`ui.discovery.annual_reports` stays red.** Dual re-observe Upsedasse! + CVR
   companies URL hint; not plan gate; not success shell; no invent
   `api_annual_*` / `ui_annual_*` product.
2. **Research primary pivot was residual offline clear API writes (29).** After
   plan-time check against `wiki/offline_write_probe_rules.md` and
   `src/billy_mcp/live_probe.py` Research96 fixture:
   - Nearly all residual candidates are
     `PERMANENT_METHOD_NOT_ALLOWED` (405) or
     `METADATA_ONLY_UNQUALIFIED` / readonly-risk.
   - Fixture semantics: `registers_tool: false`,
     `changes_coverage_state: false` — **do not ship ticketed tools or green
     coverage from residuals this iteration.**
3. **Research secondary path is productable now:** inventory dual-count for a
   greened discovery list shell that has a real API list row.
   - `ui.discovery.transactions` already green with `ui_transactions_list`.
   - `ui.parity.transactions.list` still all-false / empty tool_name.
   - Same dual-count pattern as invoices/products/contacts/bills.
4. **Not daybooks.list dual-count.** Discovery daybooks tool is
   `ui_daybooks_open` editor shell (`/:org_slug/daybooks/new`), not a daybooks
   **list** shell — wrong shape for `api.daybooks.list` parity.
5. **UI live/vision stays 37→38** after successful dual-count (one new parity
   row; discovery already counted).

## Non-goals

- Greening `ui.discovery.annual_reports` or inventing annual API/UI tools.
- Shipping ticketed tools for residual 405 / metadata-only candidates
  (accountNatures, geo reference writes, postings, bankPayments.delete,
  invoiceReminderAssociations, transactions CUD, etc.).
- Greening any `api.transactions.create|update|delete` or bulk rows.
- Greening `ui.parity.daybooks.*` or other parity ops beyond **list**.
- Implementing API list filters/sort/pagination UI (shell-open only, empty tool
  input — same as invoices R1).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; generic browser MCP tools.
- Setting `complete: true`.
- Children (tight root file ownership).
- Bulk API body contracts (92 remain red).

## Ownership

**Root Grok only. No children.**

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_transactions_list_shell_evidence` with `parity_of_api_list: bool = False`;
  wire `api.transactions.list` in parity loop; discovery path keeps
  `parity_of_api_list=False` (or default) |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — map
  `ui.parity.transactions.list` → `ui_transactions_list`; assert dual green
  like bills/invoices parity |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only; no hand edit of
  green flags) |
| Optional wiki | short `wiki/ui_transactions_list_shell.md` note that parity.list
  dual-counts the same shell (if page exists, append; else skip unless required
  by index) |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_transactions_list`
- `src/billy_mcp/models.py` success models
- `src/billy_mcp/server.py` tool registration
- `tests/live/test_ui_transactions_list.py` (already dual-session)
- Offline unit/browser tests already covering the tool

If inventory tests demand a re-run of live proof, re-run the existing live test
only — **do not** rewrite the workflow.

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_transactions_list` (existing) |
| UI path class | `/:org_slug/transactions` (optional period query allowed) |
| Success h1 | `Posteringer` |
| Input | empty strict model (no filters) |
| Nested create shell | `/:org_slug/transactions/:segment` **reject** |
| CTA | `Ny postering` observe-only; never click create/save |
| Parity row | `ui.parity.transactions.list` / `api_row_id=api.transactions.list` |
| parity_status | `list_shell_open_only` (align with invoices/bills when parity=True) |
| API invent | **never** invent transaction write tools from this product |
| annual / daybooks | **not** greened |

## EXECUTE steps

1. **Read tip product** briefly: existing apply function + invoices
   `parity_of_api_list=True` branch as template.
2. **Patch generator**
   - Signature:
     `apply_ui_transactions_list_shell_evidence(row, *, parity_of_api_list: bool = False)`.
   - When `parity_of_api_list`:
     - append evidence: maps `api.transactions.list` to UI list-shell open only;
     - set `parity_status` to `list_shell_open_only` (match invoices);
     - leave `api_row_id` as set by parity row builder (do not force `None`);
     - discovery path must still set `api_row_id=None` or leave discovery default.
   - Discovery evidence must stop claiming “does not green … ui.parity.transactions.*”
     **when** parity is intended; keep “does not green daybooks / CUD / bulk”.
   - In parity loop after bills/contacts:
     `if api_row["id"] == "api.transactions.list": apply_...(row, parity_of_api_list=True)`.
   - Discovery family `transactions` call remains default / False.
3. **Patch inventory tests** so qualified rows include
   `ui.parity.transactions.list` with tool `ui_transactions_list` and evidence
   mentioning `api.transactions.list`.
4. **Regenerate** coverage via the project’s existing generator entrypoint
   (same as prior products; typically
   `uv run python scripts/generate_coverage_report.py` or the path used in
   prior commits — match tip convention).
5. **Offline verify** before any completeness claim:
   - ruff/format/pyright as pre-commit suite requires;
   - `tests/coverage/test_coverage_inventory.py` + related unit tests;
   - do **not** run live API tests;
   - live interface suite only if inventory/live coupling requires (prefer
     offline-only if dual-count is inventory-only).
6. **Safety checks:** no credentials, no frames in repo, no annual green, no
   residual 405 tools registered.
7. **Memory + optional outbox** after product lands.

## REVIEW criteria

**ACCEPT only if all hold:**

- `ui.parity.transactions.list`: discovered/implemented/contract/live/vision all
  true; tool_name `ui_transactions_list`; evidence maps `api.transactions.list`.
- `ui.discovery.transactions` remains green (not regressed).
- `ui.discovery.annual_reports` still red.
- No residual 405 tools added; no bulk green.
- API `live_tested` still false with out_of_scope_by_user.
- Offline inventory/unit suite green for touched paths.
- `coverage/status.json` complete still false; live/vision **38** expected
  (37 + 1 parity row).

**REJECT if:** greening annual; inventing transaction writes; greening full API
filter/pagination UI; registering residual ticketed tools; inventing
`api_annual_*`.

## COMMIT

- Message style: `main.billy_complete: iteration 186.36 (ui parity transactions list dual-count shell open)`.
- Use `fractal commit` (pushes). Bookkeeping commit if tip requires
  (memory/plans) per prior pattern.
- Pre-commit: formatting, lint, type, unit, contract, safety as node rules.

## Feasibility

- Budget: no limit — slice is inventory dual-count, small.
- Blockers: none for this path; annual remains deferred red.
- Children: none.
- Residual offline writes deferred until policy allows product tools (docs change
  or authenticated non-prod method proof) — **out of this plan’s EXECUTE**.

## Exit

Ready EXECUTE. Not node finish.

## Post-Mortem

### Completed

- Dual-counted `ui.parity.transactions.list` onto existing `ui_transactions_list`
  via `apply_ui_transactions_list_shell_evidence(parity_of_api_list=True)`.
- Regenerated UI coverage manifests; status live/vision **38**.
- Inventory tests updated (qualified 38).
- Wiki `ui_transactions_list_shell` + `_index` dual-count note; wiki lint clean.
- Optional IR O1: vision-record workflow_ref lists discovery + parity row ids
  (tmp only; not in git).

### Deviations

- Research135 primary residual offline API writes (29) **not** producted: all
  blocked by `live_probe` (`registers_tool: false` / 405 / metadata-only).
  Plan correctly pivoted to inventory dual-count secondary path.
- No new browser tool or live re-run; reused research118 dual-session + vision
  evidence (same shell contract as invoices/bills dual-count).

### Review

- IR 186.36 **ACCEPT** (`tmp/grok-review.md`). No required fixes.
- O1 hygiene applied (vision workflow_refs).

### Tests / cleanup

- `lint.sh`: pass (after wiki index desc sync).
- `test.sh` (commit / offline): **1561 passed**, 35 deselected live/vision.
- No full/ui-full run: `coverage/status.json` `complete: false` (bulk + UI
  incomplete remain).
- No live API; no disposable records; annual still red; frames not recreated.

### Next unresolved coverage slice

1. `ui.discovery.annual_reports` still Upsedasse-inaccessible (research135).
2. Residual 29 non-bulk writes offline-blocked (no ticketed tools until method
   proof).
3. 92 ambiguous bulk API rows still red.
4. Remaining UI parity rows largely red after list dual-counts (only 5 parity
   list greens: invoices, products, contacts, bills, transactions).

Not node finish.
