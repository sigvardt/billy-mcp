# 186.63 ui productPrices not_applicable freeze


## Goal

Product research162 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved UI parity rows for
   **productPrices only** (**7 rows total**):
   - get, list, create, update, delete, bulk_save, bulk_delete
   - (matches inventory shape for full CRUD + bulk; singular delete present)
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so `api.productPrices.` is included.
3. Updates inventory tests for NA count 100→**107** and live/vision baseline
   157→**164** (`57` shell/parity dual-counts + `107` NA).
4. Adds a scrubbed wiki page for the freeze; one-line cross-link from peer NA
   wiki pages and/or products list shell wiki.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   this family.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research162).  
Dual evidence (tmp, non-git): `tmp/research162_soft_longwait_dual.json`,
`tmp/research162_focus_dual.json`, `tmp/research162_detail_dual.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–161).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–51 geo/currencies/account*/contact*/invoiceReminders/
specials delivery+logs; 186.52–62 dual-count/shell products (products.list
shell stays greened; productPrices was deferred until research162 soft long-wait).

## Why this slice

1. research161 greened suppliers create discovery only; deferred productPrices
   pure NA (prior short soft runs / products/new non-empty concern).
2. research162 soft **long-wait** dual: `/productPrices` and `/product-prices`
   body_len **127**, empty h1, dual-agree **identical** to nonsense
   `zz-r162-none` body **127** (after waiting out “Henter”).
3. Contrast: dual `ui_products_list` READY as **products/Produkter** shell
   (`/:org_slug/products`); `ui_products_import` greened. That shell is the
   **products** resource, not productPrices CRUD. Soft productPrices is not a
   price-management workflow.
4. Soft URL landing is not a productable prices list/create shell — do not invent
   UI tools. API product create may embed `prices[]` on **products** (docs) —
   that is products.create embed, not a `/productPrices` UI.
5. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class as
   research148–150 packages).
6. Explicit rejections/deferrals stay red this slice:
   - special.invoice_email (client save not durable; virtual invoice list)
   - products.create form open (no CTA; `/products/new` chrome-only body 190)
   - products.get / invoices.get / contacts.get detail (header-drawer false
     positive; table tbody td_count 0)
   - quotes/recurring create (CTA without form fields)
   - postings / bankLines / invoiceLines / salesTaxRules pure NA (steal risk
     onto greened bank/transactions/invoices/settings_vat shells)
   - bulk 92 external-contract
   - annual_reports Upsedasse dual (org inaccessible; NA rejected)

## Non-goals

- Shipping any new FastMCP tools (`ui_product_prices_*`).
- Greening soft-empty paths as list shells.
- Dual-counting productPrices onto `ui_products_list` / `ui_products_import`
  (different resource; products shell stays products.list only).
- Greening special.invoice_email, products.create, detail gets this slice.
- Pure NA for postings/bankLines/invoiceLines/salesTaxRules this slice.
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Changing offline API productPrices tools (API lane separate; live_tested
  stays false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.productPrices.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 107` (prior 100 + **7** ops). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH162_RESOURCES = frozenset({"productPrices"})`
  (mirror research148–149 sets). Prefer **same evidence code**
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. Update header
  comment that research162 adds productPrices (7 ops). |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research162 family **before** older catch-alls: evidence_ref
  `research162_product_prices_dual`; nonsense path
  `zz-r162-none` (or `zz-research162-no-such-route` if consistent with
  research id — match dual JSON seed name `zz-r162-none` in evidence text);
  dual flag e.g. `dual_agree_soft_empty_productPrices`; contrast controls
  include `products` (`ui_products_list`), `products_import`, and peer shells
  (invoices/clients/suppliers/uploads). Keep research138–150 branches intact. |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefix alone should wire all
  **7** parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count uses `GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **107**.
  - research162 evidence_ref branch assertions (mirror research148).
  - live/vision `57 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **164** when
    ROW_COUNT=107.
  - implemented/contract = offline_evidence + 57 + geo_na → **348**
    (prior 341 = offline + 57 + 100; +7 NA).
  - Assert seven `api.productPrices.*` UI parity rows are NA green.
  - annual still NA-rejected red; bulk 92 red; no new tools for this family;
    special.invoice_email / products.create residual red; products.list shell
    stays greened with tool `ui_products_list` (not emptied by NA).
  - Keep research147–150 asserts intact. |
| Wiki | New `wiki/ui_product_prices_not_applicable.md` (scrubbed) + sorted
  `_index` link. Cross-link from `wiki/ui_products_list_shell.md` and/or
  peer NA page e.g. `wiki/ui_specials_invoice_delivery_logs_not_applicable.md`
  or contactPersons NA (same soft-empty class). |
| Coverage regenerate | Run generator → refresh `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md`. implemented/contract rise with
  UI NA greening. complete=false; API live_tested false. |

## EXECUTE steps (ordered)

1. Patch `scripts/generate_coverage_report.py` constants + research162 branch in
   applicator (prefix, ROW_COUNT 107, RESEARCH162 set, evidence_ref, contrast).
2. Patch `tests/coverage/test_coverage_inventory.py` for 107 / 164 / 348 /
   seven-row productPrices asserts + research162 evidence_ref; keep older
   package tests; keep `ui_shell_green = 57`.
3. Add wiki freeze page + `_index` + peer cross-link(s).
4. Regenerate coverage artifacts via project generator/check scripts as used in
   186.49–51 (do not hand-edit YAML status).
5. Run lint + offline `test.sh` (no live API; live UI suite not required for this
   inventory-only freeze before IR, matching NA package pattern).
6. Leave residual dual JSON in owner tmp only (not commit).

## Acceptance

- Seven `ui.parity.productPrices.*` rows: discovered/implemented/contract_tested/
  live_tested/vision_verified all true; `parity_status=not_applicable`; empty
  tool_name; evidence_code GEO_UI_NO_EQUIVALENT_WORKFLOW; evidence_ref contains
  research162; not_applicable_decision accepted.
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 107`; live/vision status **164**.
- implemented/contract status **348**.
- No new ui_* tools; no productPrices dual-count onto products list/import.
- special.invoice_email / products.create residual red; products.list shell
  remains greened.
- complete=false; bulk 92 red; annual red; API live_tested false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Risks

| Risk | Mitigation |
| --- | --- |
| Dual-count onto products list by mistake | Assert empty tool_name + not_applicable; products.list tool stays `ui_products_list` |
| Greening products.create with productPrices | Explicit non-goal; assert products.create still discovery_required red |
| Soft-empty alone without long-wait | research162 long-wait dual proves body 127 after Henter settles |
| Nested price editor appears later | Document reopen trigger; NA only for dedicated productPrices UI absence |
| ROW_COUNT drift | Single constant drives tests + status formula |

## Post-mortem

- **Outcome:** Producted research162 productPrices GEO NA freeze (7 ops).
  IR **ACCEPT** with no required fixes. Offline suite and lint green.
- **Actual live/vision:** 157 → **164** (57 shells + 107 NA).
- **Actual NA count:** 100 → **107**.
- **Actual implemented/contract:** 341 → **348**.
- **complete:** still **false** (expected).
- **Tests:** lint.sh pass; test.sh offline **1598 passed**, 40 deselected.
- **Deviations:** none vs plan. Optional IR N1–N3 deferred (zz-r162-none naming;
  untracked plan/wiki ride commit; API productPrices.bulk_* still offline-red).
- **Cleanup:** no disposable Billy records (observe-only research). Profiles
  purged during research. No credentials/frames in repo.
- **Next unresolved coverage slice:** residual UI parity — prefer
  special.invoice_email with durable disposable client save + draft dual; else
  products.create form dual if CTA stable; nested salesTaxRules isolation; no
  postings/bankLines steal; bulk 92 external-contract and annual_reports
  org_inaccessible remain red.
- **Not done:** node finish; complete true; live API; invent UI tools.

