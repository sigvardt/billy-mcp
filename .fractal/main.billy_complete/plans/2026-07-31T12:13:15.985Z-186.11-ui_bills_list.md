# 186.11 UI bills list shell

## Goal

Ship the next **read-only list-shell-open** business UI FastMCP tool for Billy
purchases (bills), frozen in research110, with offline contract tests, dual-
session live interface tests (no API token), and a non-sensitive vision record
that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research110).  
Discovery: `.fractal/main.billy_complete/tmp/discovery110/summary.json` dual
session seed `bills`; frames purged; summary PII scrubbed;
`api_token_used: false`; `writes_attempted: false`.  
Prior product patterns: tip `7429657` (`ui_suppliers_list`), invoices/products
list shells (API-backed list + discovery dual green).  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` §10.3
(Purchases and suppliers stay red until qualified; suppliers greened).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–110).

## Why this slice

- Auth lane plus list/import shells through suppliers are producted (UI
  live/vision 11).
- Research110 dual headless sessions both reached the bills list shell:
  path class `/:org_slug/bills`, h1 `Køb`, CTA `Opret køb` present (presence
  only); visible `/bills/new` href; A↔B path/h1/title/CTA match; no error
  shell; no plan-gate heuristic.
- Official docs expose **`/v2/bills`** (and `billLines`); **no** `/v2/purchases`.
  Offline `api.bills.*` / `api.billLines.*` clear CRUD already contract-green;
  bulk still red; live false / out of scope. Do **not** invent `api_purchases_*`.
- Inventory target still red: `ui.discovery.purchases` (design route family name)
  and full `ui.parity.bills.*` / `billLines.*`.
- This product greens **shell open only** for discovery purchases + optional
  shell-open `ui.parity.bills.list` (invoices/products pattern). Do not green
  create/update/delete/bulk parity or billLines.
- Aliases `purchases`, `purchase`, `bill`, `regninger`, `kob` are **not** real
  list shells (empty h1) — product navigates only `/bills`.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Inventing `api_purchases_*` or greening bulk `api.bills.*` / `billLines.*`.
- Bill create/edit/approve/void/pay UI (never click `Opret køb`,
  `Håndter alle kladder`, or any `bills/new` control).
- API filter/sort/pagination UI product.
- Greening creditor/debtor balances, uploads, daybooks, settings_*, or other
  discovery rows observed in research110.
- Re-greening suppliers/invoices beyond incidental registration lists.
- Residual bulk 92 / residual clear API product or live method probes.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding `auth_status` beyond existing auth tools.
- New login-only `ui_*` tools.
- Greening all 339 UI rows or setting `complete: true`.
- Headed browsers, generic browser MCP tools, credential tool inputs.
- Children (file ownership is one tight cluster on root).

## Ownership

**Root Grok only. No children.** File cluster mirrors invoices/suppliers list:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_bills_list.py` (new; pattern from `test_ui_invoices_list.py` / suppliers) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for discovery purchases (+ parity.bills.list if greened) |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_bills_list_shell_evidence` for **`ui.discovery.purchases`** and
  **`ui.parity.bills.list`** shell-open-only (`parity_of_api_list` flag like invoices) |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for those rows |
| Egress | `coverage/browser_egress.yaml` — append live test path to existing
  `mit.billy.dk` `test_references` only; do not widen hosts |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_bills_list_shell.md` after product |

Do **not** spawn residual/bulk, balances, uploads, or bill create children
this iteration.

## Contract freeze (research110)

### Tool

- **Name:** `ui_bills_list`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  filter, credential, or bill fields.
- **Success:** non-PII only:
  - `path_class`: literal `/:org_slug/bills`
  - `heading`: literal `Køb`
  - `create_action_visible`: bool for exact control label **`Opret køb`**
    (presence only; never click)
  - `shell_markers_present`: bool for ≥1 shared shell marker
    (Overblik / Fakturering / Udgifter / Menu / Køb / Leverandører) without
    dumping page text
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` (error shell, wrong path, missing h1 /
  required control), `EGRESS_DENIED`, `BILLY_ERROR`.
  Messages must not include org slug, org name, email, password, tokens, bill
  numbers, voucher numbers, or supplier names.
- **Navigation:**
  1. Open MCP profile under fixed egress (`coverage/browser_egress.yaml`).
  2. If login signature present → `AUTH_REQUIRED` (callers use auth tools first).
  3. Resolve org slug from session URL or outside-git identity file only.
  4. Goto `https://mit.billy.dk/{slug}/bills` (same wait policy as invoices /
     suppliers list shells).
  5. Classify with path regex `^/[^/]+/bills$` (query params allowed on real
     URL; path class remains `/:org_slug/bills`).
  6. Signature: h1 text `Køb` and visible `Opret køb`.
  7. Reject alias paths (`purchases`, `purchase`, `bill`, `regninger`, `kob`,
     `bills/new`) as `UI_CHANGED` if somehow navigated for the list tool.
- **Session:** no API token; no screenshot persistence in production tool path;
  live test captures frames only outside git for vision then purges.

### Inventory rows

Primary: `ui.discovery.purchases`  
Also (invoices dual-row pattern): `ui.parity.bills.list` shell-open-only —
rewrite request/filter/pagination to empty tool contract; map `api.bills.list`
to UI list-shell open only; do **not** embed full bills list filter schema.

| Flag | Target after product |
| --- | --- |
| discovered | true |
| implemented | true |
| contract_tested | true |
| live_tested | true |
| vision_verified | true |
| tool_name | `ui_bills_list` |
| method_or_route | `mit.billy.dk /:org_slug/bills` (list shell open only) |
| parity_status | `list_shell_open_only` |
| response_fields | path_class, heading, create_action_visible, shell_markers_present |
| cleanup | not_applicable; read-only observation creates no records |
| side_effects | none |
| evidence | research110 dual-session + product; no invent purchases API; no create click |

Do not set `vision_evidence` to embed frame bytes. Durable vision record lives
under `tmp/vision-records/ui_bills_list.json` (gitignored/tmp) with
`purge_verified: true`.

Do **not** green `ui.parity.bills.{get,create,update,delete,bulk_*}` or any
`ui.parity.billLines.*`.

## Work sequence (EXECUTE)

1. **Models** — `UiBillsListInput`, `UiBillsListSuccess` in `models.py`
   (mirror `UiInvoicesList*` / suppliers).
2. **Browser** — path helper, signature helper, `UI_CHANGED` factory,
   `UiBillsListService` protocol, `BrowserRuntime.ui_bills_list`,
   no create / bulk-draft / menu click.
3. **Server** — register `ui_bills_list` with typed empty input binding like
   other UI tools.
4. **Offline tests** — model contract (no extra fields; success literals);
   browser unit tests for path accept/reject (including purchases alias),
   AUTH_REQUIRED, UI_CHANGED, success mapping with fakes; server registration
   name and call path.
5. **Coverage** — extend generator apply helper + inventory gate map for
   discovery.purchases and parity.bills.list; regenerate report; assert UI
   live/vision 11→13 (if dual row) or 11→12 (discovery-only — prefer dual);
   API live stays 0; `complete` stays false.
6. **Egress** — append `tests/live/test_ui_bills_list.py` to mit.billy.dk
   test_references only.
7. **Live test** — dual ephemeral profiles; login READY; dual `ui_bills_list`;
   assert path/heading/create flags; capture frames outside repo; write vision
   record; purge frames; mark purge_verified; never set BILLY_API_TOKEN.
8. **Wiki** — `wiki/ui_bills_list_shell.md` (contract + non-claims).
9. **Lint/type/unit/contract/safety** before claiming FIX-VERIFY ready;
   live+vision before completeness of this row.

## Independent review criteria (REVIEW)

ACCEPT product only if all hold:

- Typed tool exists; no generic browser/HTTP exposure.
- Empty input; success fields match freeze; no PII in models.
- Unit/contract coverage for path signature and error codes (including alias
  reject for `/purchases`).
- Live dual session without API token; independent second profile read-back.
- Vision record non-sensitive with purge_verified; no frames in repo.
- Only `ui.discovery.purchases` and shell-open `ui.parity.bills.list` greened
  for this shell; no invent purchases API; no greening of bill create/bulk,
  balances, or uploads discovery.
- `coverage/status.json` complete remains false; API live_tested remain false.
- No credentials, tokens, org slugs, bill numbers, or supplier names in
  committed files.

## Feasibility

- Budget: no limit (uncapped run). Slice is one list shell matching invoices.
- Blockers: none known; credentials via keyring outside git; discovery110 READY.
- Children: none (clear single-owner file cluster).
- Risks:
  - Discovery row id is `purchases` while path is `bills` — tool name and
    path must stay bills-aligned; inventory map documents the rename.
  - `Opret køb` may appear as link text rather than `button` — use text
    locator presence like other CTAs (research110 confirmed visible).
  - Empty list has no HTML table — do not require table for shell pass.
  - Secondary control `Håndter alle kladder` must never be treated as create CTA.

## Done when (this iteration product only)

- `ui_bills_list` implemented and registered.
- Offline suite green for new unit/contract paths.
- Live dual + vision purge_verified for this row.
- `ui.discovery.purchases` (+ shell-open `ui.parity.bills.list`) all five flags
  true; status live/vision increased; complete false.
- Wiki shell page present.
- Ready for COMMIT after FIX-VERIFY / REVIEW ACCEPT.

## Post-Mortem

### Work completed

- Research110 dual headless freeze for purchases→bills list shell; plan 186.11.
- Implemented `ui_bills_list` (models, browser, server, offline + live tests,
  coverage generator evidence for `ui.discovery.purchases` and shell-open
  `ui.parity.bills.list`, egress refs, wiki shell page).
- Live multi-h1 signature fix: use first h1 + first CTA (bills shell has 2 h1s).
- IR ACCEPT product; completeness FAIL as expected.
- FIX-VERIFY: no IR-required product fixes; lint + offline + live reconfirm clean.

### Deviations

- Root-only execution (no children), matching plan ownership.
- Multi-h1 first-match not in original research freeze prose; required for live
  pass and documented in IR as evidence-backed.
- IR auto-accept vision record from live harness is supporting only; DOM dual
  session remains primary (same pattern as prior shells).

### Review findings

- Product ACCEPT; no required product blockers.
- Optional non-blocking notes only (shared first-h1 helper later; per-row API
  live_qualification machine field already policy-level in status blocker).

### Test results

- `bash .fractal/main.billy_complete/scripts/lint.sh`: ruff format/check, pyright,
  coverage check, repository policy — pass.
- `bash .fractal/main.billy_complete/scripts/test.sh` (offline): **1376 passed**,
  11 deselected.
- Live reconfirm: `tests/live/test_ui_bills_list.py` **1 passed**.
- Vision `purge_verified: true`.
- Did not run `BILLY_TEST_MODE=full` / `ui-full` completeness gate: node not complete.

### Cleanup state

- No disposable Billy records created (read-only shell).
- Frames purged; no screenshots/HAR in repo product paths.
- Complete remains false.

### Next unresolved coverage slice

- Debtor/creditor balances discovery shells, then uploads; residual bulk offline
  API reds (92). API live remains out of scope by user.
