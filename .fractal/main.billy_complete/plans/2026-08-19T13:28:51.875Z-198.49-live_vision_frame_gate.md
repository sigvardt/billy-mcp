# 198.49 Live-test gate for invoice pre-submit screenshot

## Pivot

Owner `48ABEEB7` and design §15.11 / §19.15: production must
not persist screenshots. HEAD `bbbb1c8` already rejects caller
paths through `allowed_vision_frame_dir`. That is only the path
half. `_create_draft` still writes `02_before_submit.png` when
`BILLY_VISION_FRAME_DIR` is an existing
`~/.local/share/billy-mcp/vision-tmp/run-*` directory.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn a child. Do not remake
dumps. Do not remake leftover reverse-clean. Do not remake the
five-frame live CUD. Do not green.

Last commit `bbbb1c8` captured the filled form before **Gem som
kladde**. No `(failed on <step>)` backstop. Memory dirty only.

Owner `330C5913`: org independently empty after run
`60b6d620772644f3bca9609c8ae53846`. Keep `pending_review`.
Independent five-frame review is the following slice, after
this gate lands.

## Goal

Failing fixture, then gate the pre-submit screenshot to
approved live-test mode plus the owner-only `run-*` directory.
Stay red.

1. Offline failing fixture: an allowed `vision-tmp/run-*`
   dest plus `BILLY_TEST_MODE=commit` (or unset) and a
   `/unit/` `PYTEST_CURRENT_TEST` must not return a write
   path.
2. Smallest fix: `live_allowed_vision_frame_dir` wraps the
   existing path check. Return the dest only when the path
   is allowed **and** either `BILLY_TEST_MODE` is `ui-full`
   or `full`, or `PYTEST_CURRENT_TEST` contains `/live/`.
3. `_create_draft` uses that helper. Never
   `Path(os.environ["BILLY_VISION_FRAME_DIR"])`.
4. No live rerun. No coverage remap. Honesty-16 stay red.

## Why no child

Four files the root already owns:

- `src/billy_mcp/vision_evidence.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `tests/unit/test_vision_evidence.py`
- `tests/unit/test_ui_invoices_writes.py`

Offline safety only. Research forbids a new child.
`D68E402A` forbids a new inspect helper. The seven write
children stay completed and merged.

## TDD

`48ABEEB7` / design §15.11 / `728BD2E4`: failing fixture
first, then the smallest fix.

1. Add an offline fixture that:
   - Builds an allowed dest with `owner_only_frame_dir(base=...)`.
   - Asserts `allowed_vision_frame_dir` still accepts that dest.
   - Asserts the live helper returns `None` when
     `BILLY_TEST_MODE` is unset or `commit` and
     `PYTEST_CURRENT_TEST` is a `/unit/` path or absent.
   - Asserts the live helper returns the dest when
     `BILLY_TEST_MODE` is `ui-full` or `full`, or when
     `PYTEST_CURRENT_TEST` contains `/live/`.
   - Source-locks `_create_draft` to call the live helper,
     not bare `allowed_vision_frame_dir`, and never
     `Path(os.environ[...])`.
   Run it. Capture the fail.
2. Smallest fix:
   - Add `live_allowed_vision_frame_dir(raw, *, base=None)`
     in `vision_evidence.py`.
   - Keep `allowed_vision_frame_dir` as the path check.
   - Reuse the bills dump pattern: `/live/` in
     `PYTEST_CURRENT_TEST` is the bounded internal hook.
   - Approved live-test modes are `ui-full` and `full` from
     node `scripts/test.sh`. `commit` must not screenshot.
   - `_create_draft` calls the live helper.
3. Offline unit/contract/safety on the touched files.
   Do not run live UI. Do not set `BILLY_API_TOKEN`.
   Do not rewrite accept records. Do not purge frames.

## Out of scope

- Independent Grok vision of run `60b6d620`.
- Remap of `ui.parity.invoices.*` or honesty-16.
- Product, bills, files, daybook, or leftover probes.
- Official-docs re-lock.

## Done when

- The new fixture fails on HEAD, then passes after the helper.
- Production create does not screenshot from an allowed
  `run-*` env outside live-test mode.
- Live `tests/live/test_ui_invoices_writes.py` still qualifies
  through `/live/` in `PYTEST_CURRENT_TEST`.
- Coverage `complete` stays false. Stay red. Not finish.

## Post-Mortem

Work completed: `live_allowed_vision_frame_dir` plus path
allowlist. First form used OR (`ui-full`/`full` or
`PYTEST_CURRENT_TEST` `/live/`). Owner `02CB47D8` corrected
that to AND. Failing fixture reproduced the OR hole
(`/live/` + `BILLY_TEST_MODE=commit` still returned a dest).
Helper now requires both. Live invoice CUD sets
`BILLY_TEST_MODE=ui-full`. No live CUD rerun.

Deviations: independent review first said PASS on the OR
form. Binding `02CB47D8` withdrew that. Five-frame accept
and purge stayed out of this slice.

Review: IR PASS on the path-plus-OR package. Binding
correction is the required fix. Stay red.

Tests: conjunctive unit fixtures pass. Commit-mode
`scripts/test.sh` 2109 passed, 83 deselected. Lint passed.

Cleanup: no new live records. Org remains independently
empty after run `60b6d620`. Frames stay pending_review.

Next unresolved: independent accept and purge of
`60b6d620772644f3bca9609c8ae53846`. Honesty-16 stay red.

