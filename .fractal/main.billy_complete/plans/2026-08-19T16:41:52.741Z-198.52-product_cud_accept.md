# 198.52 Independent product CUD four-frame accept

## Pivot

Owner `A41C242F` / `FD39FFE7` / design §10.4 / §14.3: independent
Grok vision of the four product CUD frames, then purge, then
preview-tool remap. HEAD `cd31744` already captured frames and
the **Enhedspris** equality gate. The pending record is
`tmp/vision-records/ui_products_writes.json`
(`run_id=29c1b1de26a14976aaa1b98bfe9de46e`, `author=live_test`,
`pending_review`). Frames still exist outside git.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn a child. Do not remake
dumps (`A337A622`). Do not remake leftover reverse-clean. Do not
remake the four-frame live CUD. Do not green honesty-16.

Last commit `cd31744` is a clean 198.51 close, not a
`(failed on <step>)` backstop. Working tree clean.

Grok already read the four frames. Visible values:

- `01_before.png`: **Ingen produkter**. CTA **Opret produkter**
- `02_before_submit.png`: **Opret produkt**. Name
  `MCP-UI-PRD-F3DFD66D`. **Priser** `1,00` **DKK**.
  `1110 - Salg`. **Normalt salg af varer**. **Gem produkt**
- `03_after_create.png`: `1 produkt`. One row
  `MCP-UI-PRD-F3DFD66D` / **Salg** / `1,00 DKK`
- `04_after_delete.png`: **Ingen produkter** again

Accept it. Independent second-session presence and third-session
absence stay the DOM/read-back proof (`A41C242F`). Vision is
never the sole pass. Do not remake the live write for
`94A4C844`; frame `02` already shows `1,00`.

## Goal

Independent accept and purge of run `29c1b1de`, then point
`ui.parity.products.create` at `ui_products_create_preview`.
Honesty-16 stay red.

1. Offline fixture: pending `author=live_test` record does not
   qualify. Four PNGs exist. `qualifies_for_coverage_vision` is
   false. (`tests/coverage/test_ui_products_writes_vision.py`
   already has this branch.)
2. Write `grok-review.md` naming the four frames. Then
   `write_vision_record(..., author="independent_review",
   reviewer_verdict="accept")`, `purge_frame_dir`,
   `mark_purge_verified`.
3. Remap `ui.parity.products.create` to
   `ui_products_create_preview`. Leave `parity_status`
   `form_open_only`. Honesty still forces implemented / live /
   vision false. `ui.discovery.products_create` stays
   `ui_products_create_open`. research176 get/update/delete
   stay toolless `not_applicable`.
4. No live rerun. No honesty-16 membership change.

## Why no child

Six files the root already owns, plus the vision record and
wiki. Independent vision must be this Grok
(`wiki/review_provenance_rules.md`). Research forbids a new
child. `D68E402A` forbids a new inspect helper. The seven write
children stay completed and merged.

## TDD

`A41C242F` / `C7DBE974` / design §14.3 / `728BD2E4`: lock the
pending record first, then accept and purge, then remap.

1. Run `tests/coverage/test_ui_products_writes_vision.py` on
   HEAD. It must pass while pending: `author=live_test`,
   `pending_review`, four PNG names present,
   `qualifies_for_coverage_vision` false.
2. Independent review:
   - Write `.fractal/main.billy_complete/tmp/grok-review.md`
     with the four-frame visible values. Verdict accept.
   - Rewrite the vision record with
     `write_vision_record(..., author="independent_review",
     reviewer_verdict="accept")`, same `run_id` and
     `workflow_ref`, same assertion refs plus the grok-review
     path. `purge_verified=False`.
   - `purge_frame_dir` of that `run-*` directory.
   - `mark_purge_verified`.
   - The coverage fixture then takes the accept branch: author,
     accept, `purge_verified`, frame dir gone,
     `qualifies_for_coverage_vision` true.
   - Live tests still must not contain `reviewer_verdict="accept"`
     or `purge_frame_dir(` (`C7DBE974`).
3. Remap:
   - Change `tests/coverage/test_coverage_inventory.py` so
     `ui.parity.products.create` expects
     `ui_products_create_preview` (parity lookup around line
     1134 and the expected-tool map around 1866). Keep
     `ui.discovery.products_create` on
     `ui_products_create_open`. Keep `live_tested is False` and
     `parity_status == form_open_only`.
   - Run that test on HEAD. Capture the fail (HEAD still names
     `ui_products_create_open`).
   - Add `UI_PRODUCTS_CREATE_PREVIEW_TOOL_NAMES` and
     `apply_ui_products_create_preview_tool_name` next to the
     contacts/bills/orgs/invoices maps. Call it after honesty
     in `generate_ui_inventory`.
4. Wiki + memory: residual table moves product create to
   `already_live_proved_honesty_red`. `ui_products_writes.md`
   records accept + purge. Stay red. Not finish.
5. Offline unit/contract/coverage on the touched files.
   Do not run live UI. Do not set `BILLY_API_TOKEN`.
   Do not remake frames. Do not green honesty-16.

## Out of scope

- Live product CUD rerun.
- Product update tools.
- Files, daybook, leftover probes, archive dumps.
- Official-docs re-lock.
- `complete: true` or `fractal node finish`.

## Done when

- Vision record is `author=independent_review`,
  `reviewer_verdict=accept`, `purge_verified=true`,
  `run_id=29c1b1de26a14976aaa1b98bfe9de46e`.
- Frame directory is gone. No PNG / HAR / trace in the repo.
- `ui.parity.products.create` `tool_name` is
  `ui_products_create_preview`.
- Honesty-16 rows stay `implemented=false`, `live_tested=false`,
  `vision_verified=false`. `complete` stays false.
- Offline coverage fixtures pass. Stay red. Not finish.

## Post-Mortem

Work completed: independent accept and purge of product run
`29c1b1de26a14976aaa1b98bfe9de46e`. Coverage names
`ui_products_create_preview`. Honesty-16 stay red. IR FAIL:
`C7DBE974` `_WRITE_LIVE_TESTS` omitted the product CUD live
test. FIX-VERIFY added `tests/live/test_ui_products_writes.py`
to that tuple. No live remake. Accept record kept.

Deviations: first commit-mode run failed on
`test_products_create_honesty_row_still_names_open_shell`. That
assertion was remapped to preview and stays red.

Review: IR FAIL on the provenance lock. Required fix landed.

Tests: provenance lock passes. lint passed. commit-mode
2114 passed, 83 deselected. Stay red. Not finish.

Cleanup: tagged product already deleted. Frame dir gone.
`purge_verified=true`. Org independently empty after
`A41C242F`.

Next unresolved: files and daybook stay fail-closed /
prohibited. Honesty-16 stay red. Do not finish.

