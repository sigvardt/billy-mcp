# 198.66 zipcodes ticketed create and update

## Pivot

`8FE83270` is binding under `9B979A05`. HEAD `850c80a` is a clean
198.65 states close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-8fe83270.html`,
`billy-api-docs-chunk-research-8fe83270.js`,
`research-8fe83270-zipcodes-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the states lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2zipcodes`.
Supports: `get by id, list, create, update, bulk save, bulk
delete`. No singular delete. Property table is exactly six
rows with empty notes: `zipcode` string, `city` belongs-to,
`state` belongs-to, `country` belongs-to, `latitude` float,
`longitude` float.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.zipcodes.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_zipcodes_*` or bulk tools. Do not fold this into geo reads.
Do not start `balanceModifiers` (`7453B98F`). Do not finish.
No live API.

## Goal

Land one proved cohort: ticketed offline `api.zipcodes.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.zipcodes.create` | `api_zipcodes_create_preview` / `_execute` | `POST /zipcodes` | nested optional `{zipcode, city, state, country, latitude, longitude}` |
| `api.zipcodes.update` | `api_zipcodes_update_preview` / `_execute` | `PUT /zipcodes/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 6 method-closed + 2 readonly
map + 2 meta-delete = 10. Bulk 92 stay red. Implemented 542,
contract 547 (HEAD 540 / 545). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload with belongs-to strings is
`src/billy_mcp/api/state_writes.py`. Float fields copy
`src/billy_mcp/api/currency_writes.py` (`exchangeRate`).

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_zipcode_writes.py`, copied from states
   for the typed optional nested payload, with number-field
   asserts copied from currencies:
   - Register count is 4. Preview fields:
     `api_zipcodes_create_preview` `{zipcode}`,
     `api_zipcodes_update_preview` `{id, zipcode}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{zipcode, city, state, country, latitude, longitude}`.
     JSON schema types: `zipcode`, `city`, `state`, `country` are
     string; `latitude` and `longitude` are number.
     `additionalProperties` is false on outer and nested schemas.
     No `input` wrapper. Nested `required` is empty or absent.
   - Outer extras, empty update `id`, `zipcodeId`, `cityId`,
     `stateId`, `countryId`, a JSON array for `zipcode`/`city`,
     nested objects for belongs-to fields, string/object/array
     `latitude` or `longitude`, and other inner extras fail
     `ValidationError` / FastMCP `extra_forbidden`. Execute extras
     fail. Preview still makes no HTTP.
   - Empty nested object `{"zipcode": {}}` is valid. Partial
     payloads such as `{"zipcode": "2100"}` or
     `{"latitude": 55.68, "longitude": 12.57}` are valid. Do not
     require any nested field. Do not put route `id` inside
     `ZipcodePayload`.
   - Preview create sends no HTTP. Canonical request is
     `{"zipcode": { ...set keys only... }}`. Expected effect
     `{action: create, resource: zipcode}`. Inner key `zipcode`
     is the postal-code string, not the record id.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_zipcode_write_tickets.py`, copied from
   state ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/zipcodes` with the
     exact nested JSON. Execute PUTs
     `/v2/zipcodes/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/zipcodes/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `zipcodes` plus optional `meta.deletedRecords`. Do not
     expose a top-level `zipcode` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 6`, residual `== 10`. Assert
   `api.zipcodes.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk zipcodes rows
   unspecified. Bump every hardcoded `implemented_rows == 540` /
   `contract_tested_rows == 545` in that file to **542 / 547**.
   `complete` stays false. Live/vision counts stay 339.
4. Map both ids in `OPERATION_TEST_REFERENCES` next to existing
   `api.zipcodes.get` / `.list`. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_zipcodes_writes_are_ticketed_offline_and_not_live_tested`
   next to the states inventory tests.
6. `tests/unit/test_coverage_server.py`: add
   `ZIPCODE_WRITE_API_TOOL_NAMES` (four tools). Bump
   `len(api_tool_names) == 305` to **309**.
   `tests/coverage/test_api_live_qualification_semantics.py`:
   bump `len(implemented) == 201` to **203**.

Run:
`uv run pytest tests/api/test_zipcode_writes.py tests/api/test_zipcode_write_tickets.py tests/coverage/test_coverage_inventory.py tests/unit/test_coverage_server.py tests/coverage/test_api_live_qualification_semantics.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/zipcode_writes.py` (new), copy
`state_writes.py` and borrow float typing from
`currency_writes.py`:

- `ZipcodePayload`: `zipcode: str | None = None`,
  `city: str | None = None`, `state: str | None = None`,
  `country: str | None = None`, `latitude: float | None = None`,
  `longitude: float | None = None`,
  `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{zipcode}`.
- Update preview input: `{id min_length=1, zipcode}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/zipcodes`. Singular root `zipcode`.
  Plural root `zipcodes`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_zipcode_write_tools` next to
`register_state_write_tools`. Do not change geo reads.

`scripts/generate_coverage_report.py`:

- Remove `api.zipcodes.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids after
  `api.zipcodes.list`.
- Do not touch bulk helpers, the other 6 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` zipcodes create/update true,
  delete false (`("zipcodes", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 6. These two rows are ticketed
  offline tools. Residual honesty remaining 10. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional string `zipcode`, belongs-to
  `city`/`state`/`country`, and float `latitude`/`longitude` is
  enough to ship the ticketed POST/PUT. Reject `zipcodeId`,
  `cityId`, `stateId`, and `countryId`. Unauth 405 is not the
  contract. Empty payload is allowed. Do not copy list filter
  `countryId` into the write payload as required. Inner `zipcode`
  is the postal-code field, not the route id.

Keep `wiki/ui_geo_cities_not_applicable.md` as history. Do
not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.zipcodes.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.zipcodes.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research138).
- Do not accept `zipcodeId`, `cityId`, `stateId`, `countryId`,
  or a JSON object/array for string/belongs-to fields, or a
  string/object/array for `latitude`/`longitude`.
- Do not require any nested field.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.
- Saved `7453B98F` waits until this commit. Saved `9B979A05`
  still owns the remaining 10 residual rows and bulk92. Saved
  `96908DC6` still owns overall finish.

## Independent review

After EXECUTE, review must re-parse `#v2zipcodes` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still six optional fields: `zipcode`
   string, `city` belongs-to, `state` belongs-to, `country`
   belongs-to, `latitude` float, `longitude` float.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /zipcodes` and `PUT /zipcodes/:id`.
6. Nested payload rejects `zipcodeId`, `cityId`, `stateId`,
   `countryId`, arrays, nested objects, and extra keys. Empty
   nested object is valid. JSON schema type `string` holds for
   `zipcode`/`city`/`state`/`country`. JSON schema type `number`
   holds for `latitude`/`longitude`.
7. `live_tested` is false.
8. Remaining residual honesty is 10 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.
   No `balanceModifiers` work in this slice.

## Post-Mortem

Work completed: ticketed offline `api.zipcodes.create` and
`.update` in `src/billy_mcp/api/zipcode_writes.py`. Preview
tools take a nested payload of optional string `zipcode`,
belongs-to `city`/`state`/`country`, and float `latitude`/
`longitude` with `extra=forbid`. `zipcodeId`, `cityId`,
`stateId`, `countryId`, arrays, nested objects, and extra keys
fail at the FastMCP boundary. Empty nested object is valid.
Numeric JSON strings for `latitude`/`longitude` coerce the
same way `exchangeRate` integers do. Execute binds
`POST /zipcodes` and `PUT /zipcodes/:id` through the shared
write protocol. Coverage regenerated: implemented 542,
contract 547, live/vision 339, complete false. Residual honesty
remaining 10.

Deviations: none from the official field table. Registry count
asserts also bumped (`api` tools 309, implemented API rows 203).
Ticket and execute contract tests live in
`tests/api/test_zipcode_write_tickets.py` so each test file
stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
suite `2373 passed, 83 deselected`. Cleanup: no live UI records.
Next unresolved slice after COMMIT: saved `7453B98F` ticketed
`balanceModifiers` create and update. Residual honesty remaining
10. Bulk 92 stay red. `complete` stays false.

