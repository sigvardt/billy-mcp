# 198.24 Organizations update accept and purge

## Goal

Radio `23709235` is delivered on HEAD `892e1a3`: empty phone is a
clear, live FastMCP tagged set plus empty restore passed, four-state
frames exist outside git. This slice does **not** remake that write.

Independent Grok review of those frames, bind the vision `run_id` to
the frame folder, purge, then remap only
`ui.parity.organizations.update` to `ui_organizations_update_preview`.
Honesty 16 stay red. Do not finish.

Authority: `96908DC6`, `23709235`, `AD8966F2`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Another live phone write or dump remake
- Greening any of the 16 CUD honesty rows
- Remapping before accept and purge
- Remapping `ui.discovery.settings_company`,
  `ui.parity.organizations.list`, or
  `ui.parity.organizations.get`
- Inventing `ui_organizations_create_*`, `ui_annual_*`, or
  `api_settings_*`
- Calling `api.billysbilling.com` or using a live API token
- Remaking invoice inspects or the product delete-chrome dump
- Opening a browser
- Persisting company name, CVR, address, email, phone values,
  slugs, URLs, tokens, or screenshots
- Ledger, files, invoices, products, or a new contacts/bills CUD
- Docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `892e1a3`. Clean. Not a backstop. `complete=false`. Honesty
16 stay red. Contacts and bills CUD rows already name preview
tools. Invoice CUD stays `ui_invoices_*_open` and frozen.
Products create stays `ui_products_create_open` and frozen.

Pending record (gitignored):
`tmp/vision-records/ui_organizations_writes.json`
`author=live_test`, `pending_review`,
`run_id=affdb4f98390481880e4bcd554ec4fd9`.

Frames (owner-only):
`~/.local/share/billy-mcp/vision-tmp/run-0937a009bf7e496ca2ce15a8af313868/`
`01_initial`, `02_before_submit`, `03_after_set`, `04_restored`.
Folder mtime matches the record timestamp
`2026-08-18T06:03:03Z`. `01` and `04` are byte-identical empty
Telefon. `02` and `03` show a tagged `+4500` plus 8 hex.
**Gem ændringer** is off-viewport. Vision is never the sole pass.

Live test did not pass `run_id=frame_dir.name`. Bills does.
Independent accept must rewrite `run_id` to
`0937a009bf7e496ca2ce15a8af313868` and keep the old id in
`assertion_refs`.

Stale
`.fractal/main.billy_complete/tmp/live-organizations-writes-blocker.txt`
says `NOT_FOUND` from an earlier fail. Ignore it as current
status. Delete only as hygiene.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16). Do not remake it.

`tests/coverage/test_coverage_inventory.py` still expects
`ui.parity.organizations.update` → `ui_settings_company_open`.
That is the failing remap fixture.

`apply_ui_settings_company_open_shell_evidence(...,
parity_of_api_update=True)` still writes the open tool name.
Contacts and bills remap after honesty via
`apply_ui_*_cud_preview_tool_names`. Copy that pattern.

## Delegation

No children. Root owns this slice. The seven write-family
children stay merged and parked. Unlimited budget is not a
reason to spawn.

Reuse (do not remake):

- `tmp/vision-records/ui_contacts_writes.json` accept shape
- `tmp/vision-records/ui_bills_writes.json` accept shape
- `src/billy_mcp/vision_evidence.py`
  (`write_vision_record`, `purge_frame_dir`,
  `mark_purge_verified`, `qualifies_for_coverage_vision`)
- `scripts/generate_coverage_report.py`
  `apply_ui_contacts_cud_preview_tool_names` /
  `apply_ui_bills_cud_preview_tool_names`
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_organizations_writes.md`
- `wiki/review_provenance_rules.md`

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing fixture: inventory
   `ui.parity.organizations.update` still names
   `ui_settings_company_open`. Run it. Capture the expected
   failure. Do not remap until accept and purge land.
2. **Independent review before remap (`C7DBE974`, `23709235`).**
   This step writes `author=independent_review`. The live test
   must not write `accept`.
3. **Bind `run_id` to the existing folder hex.** Do not remake
   frames to fix the id.
4. **Remap only the update CUD row.** Discovery, list, and get
   stay on `ui_settings_company_open`. That open tool stays
   registered.
5. **Honesty stays red.** After remap, `parity_status` stays
   `form_open_only`. `implemented`, `live_tested`, and
   `vision_verified` stay false on the honesty overlay.
6. **If reject:** stay red. Do not remap. Do not remake frames
   unless the reject names a missing required state these four
   files do not contain.
7. **No browser.** No live API. No new tagged phone.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add a focused fixture that currently fails, next to the
existing inventory map in
`tests/coverage/test_coverage_inventory.py` (or a sibling):

- `ui.parity.organizations.update` `tool_name` is
  `ui_organizations_update_preview`
- `ui.discovery.settings_company`,
  `ui.parity.organizations.list`, and
  `ui.parity.organizations.get` still name
  `ui_settings_company_open`
- Honesty-16 still length 16 and still not green

Also add a vision fixture that the pending record does not
`qualifies_for_coverage_vision` until accept+purge. Do not
have the test write `accept`.

Run the remap fixture. Capture the expected failure. Do not
edit `generate_coverage_report.py` yet.

### 2. Independent review

Open the four existing frames. Compare them to
`tests/live/test_ui_organizations_writes.py` and the pending
record. Write
`.fractal/main.billy_complete/tmp/grok-review.md` with the
verdict and the non-sensitive checklist from research section
3d. Do not quote company values.

Accept only if all of these hold:

- Four files exist outside the repo
- Timestamp join to the pending record
- `01` empty, `02` tagged focused, `03` tagged unfocused,
  `04` empty and byte-identical to `01`
- Live test used FastMCP preview+execute, exact
  `input[name=phone]` read-back, and a third-session empty
  restore
- No forbidden panel was the edit target

Vision cannot prove the save click. That is already accepted
as not a sole-pass gap. Do not recapture full-page frames.

### 3. Accept write and purge (only if step 2 accepts)

Rewrite `tmp/vision-records/ui_organizations_writes.json`:

- `author=independent_review`
- `reviewer_verdict=accept`
- `run_id=0937a009bf7e496ca2ce15a8af313868`
- `purge_verified=false` first
- assertion refs: live test path, `input[name=phone]`, old
  live `run_id` `affdb4f98390481880e4bcd554ec4fd9`,
  `tmp/grok-review.md`
- `second_interface_ref` stays
  `fresh-session-input-name-phone`

Then `purge_frame_dir` that folder. Confirm it is gone.
Then `mark_purge_verified`. Confirm
`qualifies_for_coverage_vision` is true.

Do not commit frames. Do not commit the vision JSON if
gitignored.

### 4. Remap (only after step 3)

In `scripts/generate_coverage_report.py`:

```
UI_ORGANIZATIONS_UPDATE_PREVIEW_TOOL_NAMES = {
    "ui.parity.organizations.update": "ui_organizations_update_preview",
}
```

Add `apply_ui_organizations_update_preview_tool_name` next to
the contacts and bills helpers. Call it after honesty and the
bills remap. Leave `parity_status=form_open_only`. Do not
flip implemented / live / vision.

Update `tests/coverage/test_coverage_inventory.py` so the
update CUD row expects `ui_organizations_update_preview`.
Keep list/get/discovery on the open tool.

Regenerate coverage with the existing generator path the
suite already uses. Honesty-16 must still fail-closed red.

### 5. Wiki and memory

Update `wiki/ui_organizations_writes.md`: accept recorded,
frames gone, tool remap named, honesty still red. No phone
values. No iteration labels.

Update memory `state.md` and `todo.md` the same way. Next
open work after this slice is not another org-phone dump.

### 6. Verify

- Honesty-16 still length 16 and red
- Remap fixture now passes
- Vision qualify helper is true on the rewritten record
- Frame folder is gone
- No secrets, frames, or company values in the diff
- Do not run a live Billy write
- Do not run live API tests

## REVIEW / FIX-VERIFY

Independent review of this slice is the accept write itself.
FIX-VERIFY only if remap or purge is incomplete. Do not
remap on a reject. Stay red either way.

## COMMIT

Commit the remap, tests, and wiki. Stay red. Do not finish.
Invoice and products stay `UI_CHANGED`. Ledger last.

## Post-Mortem

Completed: independent accept of the empty-phone four-state run
(`run_id=0937a009bf7e496ca2ce15a8af313868`), purge verified,
`ui.parity.organizations.update` remapped to
`ui_organizations_update_preview`. Honesty 16 stay red.
`complete=false`. Offline `test.sh` passed 1998 tests after the
egress PUT rule was restored in `build_browser_egress`.

Deviations: no children. Visual accept happened in EXECUTE; the
iteration IR re-checked the durable record after purge. Generator
briefly dropped the organization PUT allow; FIX-VERIFY keeps that
allow in the source map.

Review: IR PASS. No blocking fix.

Residuals applied: live pending write now binds
`run_id=frame_dir.name` and will not overwrite an accepted
record. Memory no longer treats the product delete-chrome close
as a finished product (`FD39FFE7`).

Residual rejected: honesty `test_references` still name the
company-open live test. Contacts and bills remapped CUD rows
keep the same overlay pattern. Changing it here would be
inventory-prose churn, not a green-cell bug.

Tests: lint plus offline `test.sh` after residuals, 1999 passed.
No live rewrite. No `BILLY_TEST_MODE=full` or `ui-full`. Cleanup:
phone restored empty; frames gone; no new records.

Next unresolved slice: `FD39FFE7` invoice draft CUD, then
product create, new evidence-backed path, before files or ledger.

