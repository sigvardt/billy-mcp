# 186.35 ui settings subscription open empty Abonnement panel with dual live and vision

## Goal

Ship the next **read-only subscription settings open-shell** FastMCP tool for
Billy's Indstillinger **Abonnement** empty panel, dual-confirmed in research134
as final path `/:org_slug/settings` opened via hub open then observe-only
side-nav click **`Abonnement`**, with h1 **`Indstillinger`** and **empty h2
set** (no panel markers), form/input counts 0, distinct from company / Regnskab
/ Faktura / Profil / Momssatser / Brugere / Adgangsnøgler / Betas panels, with
offline contract tests, dual-session live interface tests (no API token), and a
non-sensitive vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research134).  
Discovery: `.fractal/main.billy_complete/tmp/discovery134/summary.json` +
`summary_dual_settings_subscription.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; dual click path/h1/empty-h2 match for
Abonnement; longer SPA settle still empty; frames purged; PII scrubbed.  
Prior product tip: settings beta 186.34 (`dfb5f9b` / `d19af4f`)
`ui_settings_beta_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–134).

**Research134 decision:** product **must** open via authenticated hub
`/:org_slug/settings` then exact-text / role click of side label **`Abonnement`**
(observe-only; never write/billing CTAs). Success requires **empty h2 set**
and **must reject** company, accounting, invoicing, user/Profil, vat/Momssatser,
users/Brugere, access_token/Adgangsnøgler, and beta (Betas + Tidlig adgang)
panels. Soft seed `settings/subscription` dual-opens the same empty panel on
hub path — optional dual observation only; **not** the product contract
(prefer hub+click for same pattern as beta/access_token/users/vat/user).
Other subscription-ish seeds (`settings/abonnement`, `settings/billing`,
`settings/plan(s)`, bare `subscription` / `abonnement` / `billing`) soft-empty
or wrong leaf — **reject** as sole success. No official settings or
subscription / plan CRUD API — do **not** invent `api_settings_*`,
`api_subscription_*`, or `api_plan_*`. This product is **empty-panel open-shell
classification only** (integrations soft-empty precedent). Global chrome tokens
`Fakturering` / `Pro` must **not** alone classify success. If Billy later fills
panel content, expect `UI_CHANGED` and re-freeze.

## Why this slice

- UI live/vision is 36 (through settings beta).
- Research134 dual headless sessions:
  - Click **Abonnement** from hub → path `/:org_slug/settings`, h1
    `Indstillinger`, dual empty h2 `[]`, form_count 0, input_count 0,
    iframe_count 0, write CTAs none; empty_panel_chrome dual true.
  - Longer SPA settle still empty (not load race).
  - Company → Abonnement re-click dual clears company h2s to empty panel.
  - Soft seed `settings/subscription` dual empty panel on hub path — note only;
    product still uses hub+click.
  - Soft seeds billing/plan/abonnement soft-empty or `/:id` — **reject**.
  - Hub default company panel — **reject**.
  - Control clicks same sessions: Virksomhed → company true; Betas → beta true;
    Abonnement → empty panel true (company/beta false).
  - Must **not** use loose body/side-nav chrome labels (Abonnement in rail only
    on other panels) as subscription success without empty-h2 signature after
    Abonnement open path.
  - annual_reports Upsedasse stays red.
- Official docs: no `/v2/settings`, no subscription settings CRUD. No invent API.
- Target still red: `ui.discovery.settings_subscription` only for this product.
- Hard ban: never click Opgrader* / Skift abonnement / Skift plan / Betal / Køb
  / Opret* / Gem* / Tilføj* / Slet / Upload / Invitér* / Overdrag* /
  Opret adgangsnøgle / Opret betalingsmetode / Annuller / Opsig.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening annual_reports or re-greening other settings_* from this panel open.
- Producting settings write / ticketed plan change / payment method workflows.
- Inventing any `api_settings_*`, `api_subscription_*`, `api_plan_*`, or webhook
  tools.
- Treating bare hub open (company default) or soft-empty billing seeds as
  success.
- Treating filled future subscription content as this empty-panel contract
  (that would be `UI_CHANGED` + re-freeze).
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows / parity bulk.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings beta open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_subscription_open.py` (new; pattern from beta open; hub+click Abonnement; hard-ban billing/write CTAs; reject company default + accounting + invoicing + user + vat + users + access_token + beta + soft empty aliases) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_subscription` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_subscription_open_shell_evidence` for
  **`ui.discovery.settings_subscription`** only; wire family
  `settings_subscription` in discovery apply path; ensure
  `build_browser_egress` retains prior lives (including settings company +
  accounting + invoicing + user + vat + users + access_token + beta) + appends
  subscription open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings beta, access_token,
  users, vat, user, invoicing, accounting, company, inventory, integrations,
  addons, saft_exports, exports, vat_declarations, reports, transactions,
  daybooks, financing, bank recon, receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_subscription_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, or children
this iteration.

## Contract freeze (research134 Abonnement empty panel open shell)

### Tool

- **Name:** `ui_settings_subscription_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, write payload, or secret
  token fields.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf; SPA does **not** keep soft-empty billing paths).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_subscription`.
  - `empty_panel`: bool true when h2 set is empty and no other settings panel
    signature matches.
  - `subscription_panel_markers_present` / empty-panel markers: true only when
    empty h2 after Abonnement open path (research134 dual). Optional fields
    may mirror integrations soft-empty style (`dedicated_shell=false`) if that
    fits model family — keep non-PII and stable.
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** classify as success.
  - Must be **distinct from accounting**: accounting panel trio
    (`Regnskab` + `Køb` + `Kontoplan`) must **not** classify as success.
  - Must be **distinct from invoicing**: invoicing panel
    (`Faktura` + `Produkter` + (`Betalingsmetoder` or `Standard fakturalogo`))
    must **not** classify as success.
  - Must be **distinct from user/Profil**: user panel
    (`Profil` + `Billede` + `Sprog og tema` + `Skift adgangskode`) must
    **not** classify as success.
  - Must be **distinct from vat/Momssatser**: vat panel triad
    (`Regelsæt` + `Satser for salg` + `Satser for køb`) must **not** classify
    as success.
  - Must be **distinct from users/Brugere**: users panel pair
    (`Brugere` + `Revisorer og bogholdere`) must **not** classify as success.
  - Must be **distinct from access_token/Adgangsnøgler**: access-token panel
    (h2 includes `Adgangsnøgler`) must **not** classify as success.
  - Must be **distinct from beta/Betas**: beta panel pair
    (`Betas` + `Tidlig adgang`) must **not** classify as success.
  - Prefer form_count 0 and input_count 0 (research134 dual) as supporting
    asserts in tests; treat non-zero forms after Abonnement as `UI_CHANGED`
    risk if write surfaces appear.
  - Optional observe-only: rail label Abonnement visible; **no page dump**, no
    person names, no emails, no org secrets, no plan prices.
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path or wrong panel
    (`settings/abonnement`, `settings/billing`, `settings/plan`, …)
  - hub open but **wrong panel** (company default; Regnskab; Faktura; Profil;
    Momssatser; Brugere; Adgangsnøgler; Betas)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
  - **filled** subscription panel h2 content that is not the empty-panel freeze
    (re-freeze required; do not invent fields)
- **Never:** click Opgrader* / Skift abonnement / Skift plan / Betal / Køb /
  Opret* / Gem* / Tilføj* / Upload / Slet / Invitér* / Overdrag* /
  Opret adgangsnøgle / Opret betalingsmetode / Annuller / Opsig; invent
  `api_settings_*` or `api_subscription_*` tools; treat company, accounting,
  invoicing, user, vat, users, access_token, or beta open as this tool's
  success; use loose body/side-nav chrome labels (Abonnement in rail only)
  without empty-h2 signature after open path; log or return secrets.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Open hub `https://mit.billy.dk/{org_slug}/settings` (company default is
   expected intermediate; **not** success).
3. Observe-only click exact side label **`Abonnement`** (prefer role
   link/button, fall back to exact text). Never click write/billing CTAs.
4. Do **not** accept soft seed `settings/billing` / `settings/plan` /
   `settings/abonnement` alone as success. Soft seed `settings/subscription`
   may open empty panel dual (research134) but product path remains hub+click.
   Do **not** accept bare hub company default.
5. Independent second session (fresh profile) re-opens hub + clicks Abonnement
   and asserts path / h1 Indstillinger / empty h2 / not other panels.
   **No** API read-back. **No** `BILLY_API_TOKEN`.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers / Ejere |
| Accounting panel | seed `settings/accounting` / Regnskab+Køb+Kontoplan |
| Invoicing panel | seed `settings/invoicing` / Faktura+Produkter+… |
| User/Profil panel | hub+Profil / Billede+Sprog og tema+Skift adgangskode |
| VAT/Momssatser panel | hub+Momssatser / Regelsæt+Satser for salg+Satser for køb |
| Users/Brugere panel | hub+Brugere / Brugere+Revisorer og bogholdere |
| Access-token panel | hub+Adgangsnøgler / h2 Adgangsnøgler |
| Beta panel | hub+Betas / Betas+Tidlig adgang |
| Soft empty paths | `settings/abonnement`, `settings/billing`, `settings/plan(s)`, bare `subscription` / `billing` |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, other settings_* tool success |

### CTA hard ban (tests must prove no click)

- Opgrader / Skift abonnement / Skift plan / Betal / Køb / Annuller / Opsig  
- Opret* / Tilføj* / Gem ændringer / Gem / Save  
- Slet / Delete / Upload / file input activate  
- Invitér* / Overdrag* / Opret adgangsnøgle / Opret betalingsmetode  

Observation of CTA **text** is allowed if present. **Click count for write
CTAs must stay 0.** The only allowed navigation click is the side label
**`Abonnement`** (and only as open path).

### Sensitivity

- Scrub opaque long tokens, emails, person names, plan prices from
  vision/logs/body asserts.
- Empty panel content is valid open-shell success (research134 test org state).
- Global chrome `Pro` / `Fakturering` are **not** panel success markers alone.

## EXECUTE steps (root)

1. **Models:** add `UiSettingsSubscriptionOpenSuccess` (and empty input) with
   `shell_kind="settings_subscription"`, path/heading, empty-panel markers;
   reject wrong shell kinds in model tests.
2. **Browser:** implement `ui_settings_subscription_open` like beta open:
   hub open → click Abonnement → assert empty-panel signature; reject other
   settings panels and soft-empty seeds; return typed success or `UI_CHANGED`
   / auth errors.
3. **Server:** register tool `ui_settings_subscription_open` with injectable
   service seam (mirror beta).
4. **Unit tests:** browser fakes for success empty panel, company default
   reject, beta reject, soft seed reject, write-CTA not clicked; model tests;
   coverage server tool list includes new name.
5. **Coverage generator:** apply evidence only to
   `ui.discovery.settings_subscription`; map tool name; append egress live path
   without dropping prior settings lives (esp. beta + access_token + users +
   vat + user + company …).
6. **Live test:** dual ephemeral profiles, login READY, open A + independent B
   read-back, DOM asserts, vision frames outside repo, purge_verified record
   under `.fractal/main.billy_complete/tmp/vision-records/`.
7. **Wiki:** `wiki/ui_settings_subscription_open_shell.md` (shell contract;
   empty panel; never invent API).
8. **Offline suite:** run non-live tests; regenerate coverage; confirm
   live/vision 37 for this row only; `complete: false`; annual still red.
9. **No children.** Root-only.

## Acceptance criteria

- [ ] Tool registered; typed success/error; empty input forbid extras.
- [ ] Offline unit + model + coverage inventory tests pass.
- [ ] Live dual session pass; second session independent; no API token.
- [ ] Vision record non-sensitive; frames purged; `purge_verified: true`.
- [ ] Coverage row `ui.discovery.settings_subscription` discovered +
  implemented + contract_tested + live_tested + vision_verified true.
- [ ] No other discovery/parity rows greened; annual still red.
- [ ] Egress retains all prior settings lives + subscription live path.
- [ ] `complete: false` remains; no invent API tools.
- [ ] No credentials/frames/HAR/traces in repo.

## Risks

| Risk | Mitigation |
| --- | --- |
| Empty-panel false positive on SPA failure | Dual sessions + longer settle freeze; company/beta contrast same run; form/input 0 |
| Soft seed ambiguity | Product prefers hub+click only; reject soft-empty leaves |
| Future filled panel | `UI_CHANGED`; re-research; do not invent fields now |
| Egress drop of prior lives | Generator append-only; unit assert beta/access_token/… refs remain |
| Click write/billing CTA | Hard ban list + unit/live asserts zero write clicks |
| Accidental annual green | Generator scoped to subscription row only |

## Post-mortem

_(fill after EXECUTE / FIX-VERIFY)_

## Post-Mortem

### Completed
- Producted `ui_settings_subscription_open` for `ui.discovery.settings_subscription`
  (empty Abonnement panel via hub + observe-only click Abonnement).
- Models, browser signature, server registration, unit/model/server tests, live
  dual-session + vision record, coverage generator/egress, wiki shell page.
- Offline commit suite: **1561 passed** (35 live/vision deselected).
- Live reconfirm with optional N1 empty-h2 vision assert: **passed**;
  vision `purge_verified: true`.
- IR 186.35: **ACCEPT** (no required fixes).

### Deviations
- Research rec shell_kind label `settings_subscription_empty_panel` shipped as
  `settings_subscription` with `empty_panel: true` (plan contract).
- Optional IR N1 applied in FIX-VERIFY (empty h2 count in live vision helper).
- Added `LoginControl.nth` protocol method for empty-h2 iteration (pyright).

### Review findings
- Product ACCEPT. Overall completeness still FAIL (expected).

### Cleanup
- No disposable business records created (read-only open).
- Raw frames purged after vision; no media artifacts in repo.

### Next unresolved coverage slice
- `ui.discovery.annual_reports` stays red (Upsedasse inaccessible freeze).
- Residual 92 ambiguous bulk API contracts offline-only reds.
- UI API-parity bulk still largely red.
- Do **not** node finish; `complete: false`.

