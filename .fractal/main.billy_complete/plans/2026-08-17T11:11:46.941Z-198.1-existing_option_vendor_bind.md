# 198.1 Bind existing vendor option, not create footer

## Goal

After typing Leverandør, bind the exact existing supplier option.
Do not click `Opret "{tag}"` when that option is visible.
Selection completion is the leftover close. Pointer-click
**Gem som kladde** only when `elementFromPoint` is the button.
Persist one tagged draft bill through `create_server` `call_tool`.
Coverage stays red.

A no-click live snapshot after selecting a leftover `MCP-UI-B-*`
supplier shows no vendor list, no create dialog, no search toggle,
`circleX` only, and save hit = the button. The remaining
`.ds-moved-with-portal` is `DropzoneFullScreenWrapper`
`role=presentation`. That is not a leftover close.

Authority: `96908DC6`, `91A2C363`, `728BD2E4`, `E004E7D5`,
`31F6E753`, `7E7148F6`, `A6FB8FC2`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Accepting stale vision `run_id=3af923d562b546d597e9f156b5c3227f`
- Live API token calls, SPA `POST /v2/contacts` seed, docs re-lock
- Seeding vendor via `ui_clients_create`
- Clicking create footer when an exact option exists
- Clicking search toggle or a second modal **Gem** after bind
- Clicking `[data-testid=circleX]`
- Closing `DropzoneFullScreenWrapper`
- Pressing Escape or Tab as a close
- Force-clicking **Gem som kladde**
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `d0b347b`. Not a backstop. `complete=false`. Honesty 16 stay
red. Bills `tool_name` stay `ui_bills_*_open`. Contacts remap
`93D7A063` stays closed.

Bill list is `/:org_slug/bills/empty`. Leftover tagged suppliers
remain from earlier footer creates. Current bind still prefers
`pick_portal_create_index` and returns `scoped:portal_footer`.
Live test asserts that footer bind. That is the product hole.

Cited contract (research):

- Official belongs-to is `contact` / `contactId`
- Persist proof is browser POST 2xx `/v2/bills` only
- Existing-option bind leaves save unobscured
- Dropzone wrapper is not leftover
- Close after that bind is none
- Footer create is last resort only when no exact option exists

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/bills_form.py`
- `src/billy_mcp/ui_writes/bills_vendor.py`
- `tests/unit/test_ui_bills_writes.py`
- `tests/live/test_ui_bills_writes.py`
- `wiki/ui_bills_writes.md`

Parked write-family children stay parked. Closing one bind order
is not a new family.

## Design choices

1. **Honesty first (`728BD2E4`).** Confirm the 16-row open-only gate
   still fails those rows closed. Do not edit the gate.
2. **TDD.** First product edit is a failing unit: when a portal
   snapshot has an exact existing option (`has_tag` and not only a
   create footer), bind index is that option. Create-footer index is
   not chosen. After that bind, leftover kind is none. Dropzone
   class token / `role=presentation` is not leftover and does not
   cover save. Search toggle and modal **Gem** are not closes.
   Unit dumps stay under `tmp_path`.
3. **Selection is the close (`91A2C363`).** Click the exact option
   text in the one short visible vendor list. Do not click
   `Opret "{tag}"` when that option exists. After bind, dump leftover.
   If kind is none and save hit is the button, pointer-click
   **Gem som kladde**. If a unique tag has no existing option,
   footer plus one modal **Gem** is bind, not leftover close. Do
   not add a second close (`A6FB8FC2`). If save stays covered,
   return `UI_CHANGED` `source=leftover_portal`.
4. **Live CUD uses a leftover supplier.** Find one leftover
   `MCP-UI-B-*` supplier first. Use that name as `unique_tag` so
   the observed existing-option path runs. Prove the bill list
   empty before create (`31F6E753`). Dump tag, vendor, date,
   amount, **Gem som kladde**, and `vendor_bind=scoped:existing_option`
   before the save click. Never book, approve, pay, or email.
5. **Cleanup reverse.** Delete the draft bill, then leftover tagged
   suppliers. Third session proves both gone. Purge leftover
   `billy-live-bills-*` and `billy-research-leftover-*` profiles.
6. **Vision.** `author=live_test`, `reviewer_verdict=pending_review`
   only. `run_id` is the frame-dir suffix.

## Execute

1. Run honesty 16. It must stay length 16 and red.
2. Add failing units for existing-option bind before footer, for
   leftover kind none after that bind, and for dropzone-not-cover.
   Run them. Capture the expected failure.
3. Smallest product change in `bills_vendor.py` / `bills_form.py`
   so those tests pass. `_bind_from_scoped_lists` picks the exact
   option first. `leftover_portal_covers_save` ignores
   `DropzoneFullScreenWrapper`. Live test accepts
   `vendor_bind=scoped:existing_option` when a leftover supplier
   is reused.
4. One live FastMCP bills CUD. No save click until the create dump
   and leftover dump show an unobscured pointer hit. POST create,
   **Opdater** PUT, tagged **Slet**. Independent second-session
   read-back. Third-session bill and supplier absence.
5. If existing-option bind still leaves save covered, stop with
   `UI_CHANGED` and the leftover dump. Do not invent another close.
6. Offline `test.sh`. Keep coverage red.

## Done when

- Honesty 16 still red, length 16
- Unit tests pick exact existing option before create footer
- Unit tests treat dropzone as not leftover and not a save cover
- Unit tests still refuse search toggle, modal **Gem**, wait-all,
  Tab, Escape, `circleX`, and generic portal sweep as the close
  after an existing-option bind
- Live FastMCP create/update/delete of one tagged draft bill
  passes with independent read-back, **or** the leftover dump
  proves no exact safe close without another blind click
- Create dump is a live `MCP-UI-B-*` record
- Persist is POST 2xx `/v2/bills`, or `UI_CHANGED` with leftover
  dump after existing-option bind
- Third session shows no bill and no leftover tagged supplier
- Vision record is `live_test` / `pending_review` only, new `run_id`
- Leftover research and live-bills profiles are gone
- `complete=false`. Bills `tool_name` still `ui_bills_*_open`

## After this slice (not now)

Remap only the three bills CUD rows after this accepted vision
record. Do not green the other 10 honesty families.

## Post-Mortem

Work completed:

- Honesty 16 still length 16 and red. Bills `tool_name` stay
  `ui_bills_*_open`. No remap. Official docs not re-locked.
- Existing-option bind wins over create footer. Dropzone wrapper
  is not leftover. Close after that bind is none.
- Live FastMCP draft-bill create, update, and delete passed.
  `vendor_bind=scoped:existing_option`. Independent list read-back
  and third-session absence passed.
- IR PASS. Vision `run_id=9920dd9476474b41aef70e6d66d24638` is
  `independent_review` / `accept` / `purge_verified`. Frame folder
  is gone.
- Create persist dump is now a separate file so update PUT cannot
  overwrite the create POST.

Deviations:

- Live CUD reused leftover supplier `MCP-UI-B-3BDC6A0A` as the tag
  instead of minting a new vendor. Update changed description only.
- Unique-tag create-footer path was not re-run live.

Review findings (`tmp/grok-review.md`): PASS this slice. Stay red.
Required accept, purge, and create persist snapshot. Done.

Tests:

- Live bills CUD: passed (180.94s).
- Offline `BILLY_TEST_MODE=commit` `test.sh`: 1883 passed, 60
  deselected. Coverage still `complete: false`.

Cleanup:

- Bill and leftover tagged suppliers deleted. Fresh list empty.
- Vision frames purged. Shared chrome profiles untouched.

Next unresolved coverage slice:

- Remap only `ui.parity.bills.create|update|delete` to
  `ui_bills_*_preview`. Keep honesty 16 red. Then the remaining 10
  CUD families.

