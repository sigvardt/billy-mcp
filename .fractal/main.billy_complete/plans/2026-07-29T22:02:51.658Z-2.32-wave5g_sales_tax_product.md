# 2.32 Wave-5g sales-tax write product

## Decision

The fresh cited Grok brief at `.fractal/main.billy_complete/tmp/grok-research.md`
reconfirms an unchanged official-documents fingerprint and the already-accepted
Wave-5g contract. The next work is a bounded, independent implementation
slice, so delegate it to one Codex Power leaf rather than re-researching or
adding an unauthorised parallel domain.

No additional Grok research child is needed before implementation: the brief,
the frozen contract `wiki/wave_fiveg_ticketed_writes_contract.md`, and its
independent freeze acceptance are the required cited evidence. A separate Grok
product review remains mandatory only after the implementation is merged.

## Child implementation leaf

Create `main.billy_complete.wave5g_sales_tax_product` with
`--agent=codex-power`, childless limits, and a generous uncapped run because
the node must leave a clean commit rather than stop at a partial module. Its
`NODE.md` will make the following file ownership explicit:

```sh
fractal node init wave5g_sales_tax_product \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --agent=codex-power --title='Wave-5g Sales-Tax Product' \
  --scope=src/billy_mcp,tests/api,tests/unit,scripts,coverage \
  --inherit=scripts,skills,config --max-iters=4 \
  --max-depth=0 --max-children=0 --max-descendants=0 --step-timeout=30m
```

No dollar or elapsed-time cap is assigned because the node and its parent have
an explicit uncapped budget; the four-iteration limit leaves room for
implementation, verification, correction, and a deliberate clean close. Before
starting it, commit the authored child seed with `fractal commit --init` from
the child worktree.

- `src/billy_mcp/api/sales_tax_writes.py`
- `tests/api/test_sales_tax_writes.py`
- `tests/api/test_sales_tax_cross_executor.py`
- the Wave-5g import/registration lines in `src/billy_mcp/server.py`
- the Wave-5g registry constants/assertions in `tests/unit/test_coverage_server.py`
- the six-generator-evidence entries in `scripts/generate_coverage_report.py`
- generated `coverage/api_v2_manifest.yaml`, `coverage/status.json`, and
  `coverage/report.md`, regenerated by the repository generator only

The leaf must implement exactly these six inventory rows as twelve ticketed
tools, without duplicate coverage rows:

| Operation | Preview / execute pair | Client request |
| --- | --- | --- |
| `salesTaxRulesets.create` | `api_sales_tax_rulesets_create_*` | `POST /salesTaxRulesets`, root `salesTaxRuleset` |
| `salesTaxRulesets.update` | `api_sales_tax_rulesets_update_*` | `PUT /salesTaxRulesets/:id`, root `salesTaxRuleset` |
| `salesTaxRulesets.delete` | `api_sales_tax_rulesets_delete_*` | `DELETE /salesTaxRulesets/:id`, no body |
| `salesTaxRules.create` | `api_sales_tax_rules_create_*` | `POST /salesTaxRules`, root `salesTaxRule` |
| `salesTaxRules.update` | `api_sales_tax_rules_update_*` | `PUT /salesTaxRules/:id`, root `salesTaxRule` |
| `salesTaxRules.delete` | `api_sales_tax_rules_delete_*` | `DELETE /salesTaxRules/:id`, no body |

Implementation invariants: clone the established `tax_writes.py` pattern;
ruleset specs use optional `additional_plural_roots=("salesTaxRules",)` and
rule specs use none; outer inputs forbid extras while inner objects stay
opaque; every executor passes a literal server-owned tool name to the one root
`WriteProtocolService`; previews make no HTTP requests; and writes have no
retry. The leaf must register through the root's shared confirmation store,
not create one locally.

The focused tests must cover flat FastMCP schemas, precise request
method/path/body, escaped IDs, partial-update ID equality, bodyless deletes,
typed auth/not-found errors, ticket tamper/expiry/replay and binding failures,
same- and cross-resource executor mismatches before HTTP, optional parent
child-root mapping, and no fabricated parent root for rule results. It must
then regenerate coverage and prove the exact offline-only state: 202 `api_*`
tools, 148 implemented and contract-tested rows, zero live/vision rows, all
bulk/UI/special rows unchanged, and `complete: false`.

The leaf must run focused pytest, generator/checker validation, Ruff and
Pyright, plus the repository non-live safety suite before committing. It must
not perform live API calls, browser work, create data, write a bulk tool, or
claim completeness.

## Root integration and review

When the leaf commits, inspect its three-dot diff and a clean `git archive`,
merge it with `--no-ff`, regenerate wiki indexes if needed, and run the full
non-live suite from root. Then launch a new `--agent=grok` child for a
read-only independent product audit of the merged root commit. That reviewer
must recheck the current official documentation fingerprint and issue a
durable wiki verdict; a Codex fallback cannot replace the Grok gate.

Only an accepted post-merge Grok product review may promote this offline slice
as implemented/contract-tested. It still cannot promote live, UI, vision,
bulk, special-route, or overall-completeness status.

## Sequencing

1. Preserve the just-produced research handoff and create/configure the Codex
   Power leaf from the committed Wave-5g freeze baseline.
2. Monitor the leaf, merge only its committed product diff, and check that the
   generated coverage delta is limited to the six named CUD rows.
3. Run root verification and obtain the independent Grok product audit.
4. Record the audit result and keep all unqualified rows fail-closed.

## Post-Mortem

- Completed: froze the child contract, launched the Codex Power leaf with the
  exact Wave-5g product ownership, and recorded Grok acceptance of the
  research handoff.
- Deviation: no root product implementation was attempted because the leaf
  exclusively owns the product files; research acceptance is deliberately not
  product-code acceptance.
- Review: the product-ready handoff had no required fixes. A separate product
  review remains mandatory after merge.
- Verification and cleanup: root non-live lint and test suites passed (850
  tests); the leaf owns new offline contract coverage. No live, UI, vision, or
  ambiguous-bulk evidence was claimed, and no credentials or raw browser
  evidence were created.
- Next unresolved coverage slice: complete, merge, and independently review
  the sales-tax rulesets/rules twelve-tool cohort; live/UI/vision/bulk/
  completeness rows remain red.
