# 186.75 ui invoices delete open

## Goal

Product research175 ACCEPT:

1. New read-only FastMCP UI tool **`ui_invoices_delete_open`** that opens an
   existing draft invoice **edit** surface, opens **Mere**, and returns typed
   non-PII flags proving delete menu chrome (**Slet** text visible; primary
   Slet **button** absent).
2. Observe-only: **never** click Slet / Slet faktura / confirm / second Slet /
   Gem / Gem som kladde / Godkend og send / Send on the product path. Harness
   may seed and cleanup disposable marker invoices only via SPA token.
3. Green exact **`ui.parity.invoices.delete`** / map `api.invoices.delete` onto
   `ui_invoices_delete_open` (`delete_chrome_open_only` / Mere-menu text parity).
4. Keep greened `ui_invoices_list`, `ui_invoices_create_open`,
   `ui_invoices_get_open`, and `ui_invoices_update_open` unchanged. Do **not**
   dual-count delete onto get/update tools. Same path class as get/update;
   **distinct assertion family** (Mere→Slet text vs form/detail freeze).

Target math (tip `2712ce7` / product 186.74 invoices update open):
live/vision **174 → 175**; implemented/contract **358 → 359**; complete
**false**.

**No** browser egress change this slice (POST/DELETE invoices + contacts +
products + accounts + salesTaxRulesets already committed for SPA seed/cleanup).

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research175)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. invoices Supports get/list/create/update/bulk save/**delete**/bulk delete. DELETE `/v2/invoices/:id`. |
| SPA seed dual | contact + product + draft invoice POST 200 dual under **committed** egress. |
| Edit path dual | soft **`/:org_slug/invoices/:id/edit`** path dual. |
| Primary Slet button | **0 dual** (not bills-style primary Slet). |
| Mere → delete chrome | Mere open **true dual**; get_by_text exact **Slet** =1 dual; **Duplikér** =1 dual; role=menuitem/menu **0 dual**; role=button Slet after Mere **0 dual**; Annuller **0** (never clicked Slet). |
| Body after Mere | “Duplikér Slet” dual. `delete_menu_ready` **true dual**. |
| Pattern peer | `ui_clients_delete_open` (Mere → menu text; never confirm). **Not** `ui_bills_delete_open` (primary Slet + confirm Annuller). |
| Residual rejects | products.get soft detail marker false / inputs 0 dual; list_click false green. invoice_email soft paths inputs 0. bulk/annual stay red. |
| Isolation | delete_chrome ≠ update form_open ≠ get detail_open ≠ list/create. Green only **invoices.delete**. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research175_focus_dual.json`.  
Probe: `tmp/research175_focus_probe.py`.

## Non-goals

- Any change to `coverage/browser_egress.yaml` this slice
- Greening invoices.update again (already green via `ui_invoices_update_open`)
- Greening products.get*, special.invoice_email, invoices bulk_*
- bulk_* greening; annual_reports greening
- Ticketed durable UI write for invoice delete (no real DELETE from UI tool path)
- Clicking Mere→Slet to confirm / permanent delete
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Dual-counting delete onto list/create/get/update tools
- Greening on body substring "slet" alone without Mere open + exact Slet text

## Ownership

| Area | Owner |
| --- | --- |
| Models + `BrowserRuntime.ui_invoices_delete_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count invoices.delete only | Root Grok EXECUTE |
| Unit tests (models + browser injectable) | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research175)

### A. Browser egress

**No change.** Reuse committed invoices/contacts/products/accounts/salesTaxRulesets
path_allows for harness seed/cleanup only. Product tool is pure UI navigation.

### B. Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_invoices_delete_open` |
| lane | ui |
| kind | delete_chrome_open_only / mere-menu observe (read-only) |
| shell_kind | `invoices_delete` |
| path_class | `/:org_slug/invoices/:id/edit` |
| success model | `UiInvoicesDeleteOpenSuccess` (Pydantic, `extra=forbid`) |
| error model | existing `ToolError` (`AUTH_*`, `UI_CHANGED`, egress/auth codes) |
| API invent | none — `api.invoices.delete` already offline implemented |

### C. Success model fields (non-PII)

No org_slug, raw URL, selector secrets, customer PII, invoice numbers, or delete
confirmation payloads in success fields:

| Field | Contract |
| --- | --- |
| `edit_open` | bool — on invoice edit path class |
| `mere_open` | bool — Mere opened |
| `slet_text_visible` | bool — exact text **Slet** present after Mere (not primary button) |
| `dupliker_visible` | bool — **Duplikér** present after Mere (support flag; dual true research175) |
| `primary_slet_absent` | bool — primary Slet **button** count 0 before/at open (true dual) |
| `shell_markers_present` | bool — nav/shell chrome present |

Strict **reject**:

- list shell or `/invoices/new` as delete chrome
- get/update success without Mere open + Slet text
- primary Slet button path (bills pattern) without Mere menu
- any product path that clicks Slet / confirm / permanent delete

### D. Navigation (observe-only product path)

1. Headless Playwright via `BrowserRuntime`; existing egress; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open invoices list `https://mit.billy.dk/{slug}/invoices`, settle.
4. Open first non-header draft invoice edit (`…/invoices/{id}/edit`). Prefer
   soft edit after seed; else list row → edit. Soft `/invoices/{id}` may redirect
   to edit (research174/175 dual).
5. Pre-Mere: count role=button Slet must be **0**; Mere present.
6. Click **Mere** once; settle; assert exact text Slet visible (and Duplikér if
   available). Do **not** click Slet / Duplikér / Udskriv / Gem / Godkend / Send.
7. Return typed success flags. Escape/dismiss Mere optional after flags (must not
   submit delete).
8. Soft `/invoices/new` is not success. Soft path without Mere→Slet is not success.

### E. Coverage generator dual-count

Map **only**:

- `ui.parity.invoices.delete` → `ui_invoices_delete_open`
- discovery/parity green when implemented + contract_tested + live_tested + vision
- API row `api.invoices.delete`: keep implemented/contract; `live_tested` false +
  `out_of_scope_by_user` (UI live does **not** green API live)

Do **not** dual-count onto list/create/get/update invoice tools.

### F. Live dual test

`tests/live/test_ui_invoices_delete_open.py`:

- Dual ephemeral profiles; second session independent read-back
- Seed disposable contact+product+draft invoice via SPA token capture (not env
  `BILLY_API_TOKEN`)
- Call product tool; assert success flags dual
- Never product-path delete confirm
- Cleanup reverse-order SPA DELETE; dual list/marker clean
- Vision record non-sensitive + purge_verified; frames outside repo
- Skip if browser credential refs missing

### G. Unit / offline

- Model construction + `extra=forbid`
- Browser unit with injectable page seam for Mere + Slet text
- Server registry lists `ui_invoices_delete_open`
- Coverage inventory asserts tool_name on parity row when green path wired
- Offline `BILLY_TEST_MODE=commit` / lint.sh before commit

### H. Wiki

Shell page for the tool (non-sensitive contract summary). No credentials, no
frames, no raw dual JSON in wiki.

## Files (expected touch set)

| Path | Change |
| --- | --- |
| `src/billy_mcp/models.py` | `UiInvoicesDeleteOpenSuccess` |
| `src/billy_mcp/browser.py` | `ui_invoices_delete_open` + helpers |
| `src/billy_mcp/server.py` | register tool |
| `scripts/generate_coverage_report.py` | dual-count invoices.delete |
| `tests/test_models.py` | model unit |
| `tests/unit/test_browser.py` (or peer) | injectable unit |
| `tests/unit/test_coverage_server.py` | registry/coverage |
| `tests/live/test_ui_invoices_delete_open.py` | dual live |
| `tmp/vision-records/ui_invoices_delete_open.json` | non-sensitive |
| `wiki/…` shell for tool | short page |
| `coverage/*` via generator only | status 359/175 |

## Acceptance criteria (EXECUTE + tests)

1. Tool registered; typed success/error; never clicks Slet confirm  
2. Offline commit suite pass (unit+contract)  
3. Live dual pass when credentials present  
4. Vision record written; frames purged  
5. Generator → invoices.delete green; list/create/get/update unchanged; live/vision
   175; contract 359; complete false  
6. `check_coverage` consistent without require-complete  
7. No credentials/tokens/screenshots in repo diff  

## IR acceptance criteria

- ACCEPT if: Mere→Slet text dual; primary_slet_absent; never-submit enforced;
  dual-count only invoices.delete; get/list/create/update tools unchanged;
  assertion split vs update/get holds; no egress churn; cleanup dual proven;
  vision accept+purge.
- REJECT if: dual-counts wrong rows; clicks Slet confirm; greened products/
  invoice_email; uses BILLY_API_TOKEN; collapses into update/get without Mere;
  incomplete cleanup.

## Out of slice residual (leave red)

- products.get/update/delete  
- special.invoice_email  
- bulk 92 external-contract; annual_reports org_inaccessible  
- API live_tested stays false (`out_of_scope_by_user`)  
- many discovery_required UI parity rows  

## Feasibility

- Research dual evidence present and cleanup proven.  
- Pattern already shipped (`ui_clients_delete_open`).  
- No egress expansion risk.  
- Root-only; budget unlimited; no children needed.  
- Complete remains false after slice (expected).

## Ready for EXECUTE

Root Grok implements plan 186.75 from research175. Not finish.

## Post-Mortem

- Outcome: DELIVERED `ui_invoices_delete_open` (delete_chrome_open_only maps
  `api.invoices.delete` / `ui.parity.invoices.delete`). IR ACCEPT; no required
  fixes (optional N1 assert message wording applied in FIX-VERIFY; N2 capture
  Mere chrome deferred).
- live/vision: 174 → 175
- implemented/contract: 358 → 359
- complete: false (bulk 92 external-contract; annual_reports org_inaccessible;
  residual UI parity open: products.get/update/delete, special.invoice_email,
  many discovery_required rows)
- Tests: lint.sh pass; offline BILLY_TEST_MODE=commit **1642 passed** / 50
  deselected; live dual `tests/live/test_ui_invoices_delete_open.py` pass
  (EXECUTE + IR evidence); vision record purge_verified true.
- Cleanup: SPA seed contact+product+invoice deleted reverse-order; dual list
  read-back clean; profiles ephemeral; frames purged outside repo.
- Deviations: none material. No egress path_allow expansion. Clients-delete
  Mere-text pattern (not bills primary-Slet confirm).
- Next unresolved coverage slice: prefer products.get only if soft detail
  dual-stable; special.invoice_email still no durable form; no bulk greening;
  no annual green without access. Not node finish.
