# 186.84 attachments list dual-count + files create dual-count + files list/get NA (research184)
## Goal

Product research184 as one root-only inventory freeze (no new `ui_*` tool):

1. **Dual-count** onto existing `ui_uploads_list`:
   - `ui.parity.attachments.list` ← `api.attachments.list`
   - `ui.parity.files.create` ← `api.files.create` (upload-surface open only; peer of `special.files_upload`)
2. **NA** with `GEO_UI_NO_EQUIVALENT_WORKFLOW`:
   - `ui.parity.files.list` ← `api.files.list`
   - `ui.parity.files.get` ← `api.files.get`

Exact ids only. No other attachments/files ops. No bulk. No annual.

### Exact freeze set (4)

| Decision | Exact API id | UI parity id | Mapping |
| --- | --- | --- | --- |
| dual-count list | `api.attachments.list` | `ui.parity.attachments.list` | `tool_name=ui_uploads_list`, `parity_status=list_shell_open_only` |
| dual-count create open | `api.files.create` | `ui.parity.files.create` | same tool; upload-surface open only (never set file input / never submit); `parity_status` create_chrome_open_only **or** list_shell_open_only matching apply helper peer `special.files_upload` |
| NA | `api.files.list` | `ui.parity.files.list` | `parity_status=not_applicable`, empty tool_name |
| NA | `api.files.get` | `ui.parity.files.get` | same NA pattern |

Evidence: research184 dual independent headless sessions; SPA session only;
no disposable writes; profiles purged; docs MD5/ETag unchanged. Bilag reload
hits `/v2/attachments` dual (class count 14), **not** `/v2/files` (0). Soft
`/attachments|/files|/filer` soft-empty dual (body_len 138).

**Hard dual-count / NA bans:**

| Do | Do not |
| --- | --- |
| Map attachments.**list** to Bilag list shell | Map attachments.get/create/update/delete to Bilag |
| Map files.**create** to Bilag upload surface (open only) | Map files.list/get to Bilag (SPA is attachments inventory) |
| NA files.list/get with soft-empty + files-hit absence | Pure-NA **all** attachments.* while Bilag is attachment inventory |
| Keep `special.files_upload` dual-count | Steal receipt_inbox / invent new tools |
| Keep bulk + annual red | Green bulk 92 or annual_reports |

### Explicit DEFER / REJECT (out of this plan)

- `attachments.get|create|update|delete` (no detail/Slet/join-form dual)
- attachments bulk ×2, files bulk ×2, all other bulk 92
- annual_reports greening
- new product tool / form submit / file pick
- re-product `ui_uploads_list` DOM contract beyond inventory flags
- offline clear-API tool gaps (29 non-bulk)
- live `BILLY_API_TOKEN` / headed browser / `node finish`

Target math (tip `3794a8c` / 186.83):

- dual-count greens **+2** (attachments.list, files.create)
- NA full-flag **+2** (files.list, files.get)
- live/vision **278 → 282**
- implemented/contract **462 → 466**
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **204 → 206**
- complete **false** (attachments residual get/create/update/delete, annual, bulk 92, offline clear not-impl)

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research184)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. `/v2/attachments` full CRUD+bulk; `/v2/files` get/list/create+bulk (no single update/delete). Intro: upload files via **POST /v2/files** can auto-create attachments. |
| Bilag identity | Greened `/:org_slug/uploads` h1 **Bilag** dual; SPA resource hits **attachments** dual, **files** absent dual. |
| Soft routes | `/attachments`, `/files`, `/filer` soft-empty dual; not the Bilag shell. |
| List chrome | `ui_uploads_list` ok dual; file_input_present + upload CTA dual; **Slet=0** dual; no tbody rows for get/detail dual. |
| Upload surface | Already greened as `ui.parity.special.files_upload` → `ui_uploads_list` (research155). `files.create` is the same binary POST surface open-only dual-count. |
| files.list/get NA | Soft empty dual + Bilag is not files list (hits=0) → `GEO_UI_NO_EQUIVALENT_WORKFLOW` without dual-count steal. |
| Cleanup | No seeds; profiles purged dual; `api_token_used` false dual. |
| Brief | `.fractal/main.billy_complete/tmp/grok-research.md` research184. |
| Dual | `tmp/research184_focus_dual.json`. |
| Probe | `tmp/research184_focus_probe.py` (tmp only). |

Peer pattern: daybooks list/create dual-count flags on one apply helper; special.files_upload dual-count on `apply_ui_uploads_list_shell_evidence`; research183 exact-id NA frozensets + inventory tests + wiki.

## Non-goals / safety

- No new FastMCP UI tools; no model/server/browser product changes required unless inventory tests force a comment-only wiki update.
- Never set `input[type=file]`; never submit upload/delete/save.
- No live `BILLY_API_TOKEN` / no credentialed API qualification calls.
- No headed browser / no desktop window.
- No bulk greening; no annual greening; no stub/skip as green.
- No dual-count of attachments.get/create/update/delete or files.list/get onto Bilag.
- No dual-count steal onto `ui_receipt_inbox_list`.
- Frames not required for dual-count/NA inventory flags (existing live vision on uploads list already qualifies the shared tool).
- API `live_tested` remains false with `out_of_scope_by_user`.
- No children; only `--agent=grok` if any spawn (none planned).

## Ownership

| Area | Owner |
| --- | --- |
| Generator dual-count flags on uploads apply + NA freeze (files list/get) | Root Grok EXECUTE |
| Inventory unit tests (exact 4 outcomes, bans, count 206, residual red) | Root Grok EXECUTE |
| Wiki: update `ui_uploads_list_shell.md` mapping table + optional research184 NA note page | Root Grok EXECUTE |
| Regenerate coverage manifests/status | Root Grok EXECUTE |
| No new live product test file | Root (reuse `tests/live/test_ui_uploads_list.py`) |
| Independent review | Root Grok (self-IR) |

## EXECUTE steps (ordered)

### 1. Generator — dual-count extend `apply_ui_uploads_list_shell_evidence`

File: `scripts/generate_coverage_report.py`

1. Add kwargs (mutually exclusive per call with each other and with discovery-only):
   - `parity_of_api_attachments_list: bool = False`
   - `parity_of_api_files_create: bool = False`
   - Keep `parity_of_special_files_upload: bool = False`
2. Raise `ValueError` if more than one parity flag is true (peer daybooks exclusivity).
3. When any parity flag true, leave `api_row_id` bound to the workflow row’s API id (caller already sets / apply must not null it). Discovery-only path still sets `api_row_id=None`.
4. Append research184 evidence strings:
   - attachments.list: Bilag SPA hits `/v2/attachments` dual; soft `/attachments` empty dual; list_shell_open_only; never greener than list shell.
   - files.create: maps `api.files.create` to Bilag upload-surface open only (peer special.files_upload); never set file input / never submit.
5. Update docstring: **does** dual-count attachments.list + files.create under research184; still does **not** green attachments get/create/update/delete or files.list/get.

Wire in UI parity builder (near special.files_upload):

```python
if api_row["id"] == "api.attachments.list":
    apply_ui_uploads_list_shell_evidence(row, parity_of_api_attachments_list=True)
if api_row["id"] == "api.files.create":
    apply_ui_uploads_list_shell_evidence(row, parity_of_api_files_create=True)
# existing
if api_row["id"] == "api.special.files_upload":
    apply_ui_uploads_list_shell_evidence(row, parity_of_special_files_upload=True)
```

Optional: for files.create set `parity_status="create_chrome_open_only"` if that stays consistent with inventory tests; otherwise keep `list_shell_open_only` like special.files_upload. Prefer **create_chrome_open_only** for files.create if tests allow.

### 2. Generator — NA freeze files.list + files.get

1. Add `GEO_UI_NOT_APPLICABLE_RESEARCH184_IDS = frozenset({
     "api.files.list",
     "api.files.get",
   })`
2. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **204 → 206** (comment: + research184 files list/get).
3. Include research184 ids in `geo_ui_not_applicable_api_row` (or dedicated branch in applicator).
4. Evidence applicator fields (peer research183):
   - `parity_status=not_applicable`
   - `qualification.not_applicable_decision=accepted`
   - `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`
   - `evidence_ref` containing `research184` (e.g. `research184_files_list_get_soft_empty_dual`)
   - `method_or_route` mentions research184 soft-empty + Bilag is attachments inventory (files SPA hits 0)
   - all five coverage flags true; `tool_name` empty/null; `vision_evidence` null
5. Family map optional (files list/get only).

### 3. Inventory tests — replace “attachments/files stay red” assumptions

File: `tests/coverage/test_coverage_inventory.py` (and any related shell tests)

1. **Remove/replace** assertions that force **all** `api.attachments.*` and `api.files.*` non-NA and non-live (lines ~867–889 research183 residual block and research183 test bans on `ui.parity.attachments.list`).
2. Add `test_research184_attachments_list_files_create_dualcount_and_files_list_get_na`:
   - `ui.parity.attachments.list`: tool `ui_uploads_list`, live/vision true, `list_shell_open_only`, api_row_id `api.attachments.list`, evidence/method mentions research184 or Bilag attachments hits.
   - `ui.parity.files.create`: tool `ui_uploads_list`, live/vision true, open-only status, api_row_id `api.files.create`.
   - `ui.parity.files.list` + `ui.parity.files.get`: NA accepted, research184 evidence_ref, five flags true, tool empty.
   - `ui.parity.attachments.get|create|update|delete`: still **not** live_tested / not NA accepted (stay discovery_required red).
   - bulk attachments/files still red.
   - `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 206` and frozenset size 2 for research184 ids.
   - dual-count exclusivity: receipt_inbox not mapped to files/attachments; special.files_upload still green on same tool.
3. Update any residual-open counters / related-shell assertions that expected attachments.list red.
4. Keep research183 tests intact for the 50 NA set.

### 4. Wiki (durable, scrubbed)

1. Update `wiki/ui_uploads_list_shell.md`:
   - Inventory mapping table: add attachments.list + files.create dual-count (research184).
   - Revise “Explicit non-claims” so it no longer says no greening of all files*/attachments*; narrow to residual get/create/update/delete for attachments and files.list/get NA.
   - Sources: cite official docs + research184 dual path class (no secrets, no org PII).
2. Optional short page `wiki/ui_files_list_get_not_applicable_research184.md` (peer residual NA pages) — only if index/link hygiene wants a dedicated NA record; otherwise fold into uploads shell page.
3. Refresh wiki `_index.md` descriptions if the page title/desc change (fractal commit may regenerate link rows; still fix dangling desc if needed).

### 5. Regenerate coverage

```bash
uv run python scripts/generate_coverage_report.py   # or project’s established regenerate entry
# confirm:
# - coverage/status.json live_tested_rows ~282, implemented/contract ~466, complete false
# - blocker still mentions bulk/annual/attachments residual (not falsely complete)
```

Do **not** set complete true. Do **not** green bulk/annual/attachments residual ops.

### 6. Offline verify (before IR)

```bash
bash .fractal/main.billy_complete/scripts/lint.sh
BILLY_TEST_MODE=commit bash .fractal/main.billy_complete/scripts/test.sh
# expect offline suite pass (prior ~1664 + any new inventory asserts)
uv run python scripts/check_coverage.py   # complete false OK
```

No `ui-full` / no new live UI run required if flags-only dual-count/NA peer pattern holds and live uploads test already exists.

### 7. IR → FIX-VERIFY → COMMIT

- IR against research184 brief + this plan + regenerated manifests.
- Required fixes only; optional notes for later attachments detail product.
- COMMIT product message naming dual-count + NA package; push via `fractal commit`.
- Not node finish.

## Feasibility

| Constraint | Fit |
| --- | --- |
| Budget | No limit; small inventory slice |
| Children | None — single ownership of generator/tests/wiki |
| Live UI creds | Not required for EXECUTE if reusing greened live tool |
| Blockers unchanged after | bulk 92, annual, attachments get/create/update/delete |
| Risk | Tests still hard-assert “all files/attachments red” — must update those first or suite fails |
| Risk | apply helper nulls api_row_id when no special flag — extend carefully |
| Risk | over-greening attachments.create from upload CTA — **banned** explicitly |

## Success criteria (this iteration product)

- [x] Exact 4 rows producted as specified; no extras greened
- [x] `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 206`
- [x] Inventory tests pass including residual red for attachments get/create/update/delete
- [x] Offline lint + pytest pass
- [x] Wiki mapping updated without secrets
- [x] status.json complete false; bulk/annual not greened
- [x] Ready IR/COMMIT; not finish

## Post-mortem (FIX-VERIFY 2026-08-02)

- **What landed:** Root-only inventory package (no new tool). Dual-count
  `ui.parity.attachments.list` + `ui.parity.files.create` onto existing
  `ui_uploads_list`. NA `ui.parity.files.list` + `ui.parity.files.get` with
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` (research184 dual soft-empty + Bilag SPA hits
  attachments not files). Generator dual-count mutual exclusivity; GEO count 206.
  Wiki: `ui_uploads_list_shell.md` + `ui_files_list_get_not_applicable_research184.md`.
- **Delta counts actual:** live/vision **278 → 282**; implemented/contract
  **462 → 466**; GEO NA **204 → 206**; complete **false** (unchanged).
- **IR verdict:** package **ACCEPT**; overall completeness **FAIL** expected;
  required fixes **none**. Optional N1: dual JSON prefer_slice label clarified
  to `dual_count_attachments_list_and_files_create_on_ui_uploads_list` (tmp only).
- **Tests:** lint.sh pass; offline test.sh **1665 passed** / 54 deselected;
  wiki lint clean (wiki + memory). No `ui-full` (not complete).
- **Cleanup:** research dual profiles already purged; no disposable seeds;
  no secrets in tracked tree.
- **Deviations:** none vs plan product scope. Research auto-verdict string was
  slightly misnamed; product matched plan § exact freeze set.
- **Deferred residual (next slice candidates):**
  1. attachments get/create/update/delete UI parity (detail/Slet/join form) or
     evidenced NA when dual proves no chrome
  2. annual_reports non-Upsedasse access / productable shell
  3. bulk 92 external-contract (docs/assets only; no live API)
  4. offline API clear not-impl residual (separate lane)
- **Not node finish.**

