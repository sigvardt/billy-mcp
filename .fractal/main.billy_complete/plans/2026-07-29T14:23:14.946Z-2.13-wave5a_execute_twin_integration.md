# 2.13 Wave 5a execute-twin coverage gate and root review

## Objective

Unblock the first three Wave-5a candidate deliveries without corrupting the
one-row-per-official-operation coverage inventory.  The root must recognise an
`api_*_execute` tool as the required non-inventory confirmation companion of a
documented `api_*_preview` tool, while still rejecting every unpaired domain
tool.  This change does not make any inventory row green and does not alter
`coverage/status.json` or generated coverage artifacts.

The cited source of the write contract is
`.fractal/main.billy_complete/tmp/grok-research.md`, refreshed against
https://www.billy.dk/api/ with ETag `hsisik4g9p3603` and MD5
`c2efda0ee4cf9cf200e14910c5fc6996`.  The operator directive in radio message
`09974D34` is the integration authority: failed leaf Grok-review attempts after
edits are not grounds to discard committed candidate bytes; authenticated Grok
review happens from root before any merge.

## Planning decision

Three child leaves already occupy the available worker slots alongside root.
Their code is separable, but the coverage gate is expressly root-owned and
touches shared checker/test files, so it will be implemented in root during
EXECUTE rather than spawning a competing leaf.  No extra node is created until
the existing leaves are closed.  Once a slot is free, root will create the
following childless Grok reviewer with a shared-wiki-only delivery:

```bash
fractal node init wave5a_root_candidate_review \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --base=main.billy_complete --agent=grok --scope=wiki \
  --max-depth=0 --max-children=0 --max-descendants=0
```

Its `NODE.md` must name the exact candidate branch tips, require review of each
three-dot diff and frozen official contract, and prohibit product edits,
coverage changes, or completeness claims.  Its shared-wiki finding is the
pre-merge decision record; required findings are fixed by Codex Power and then
re-reviewed.

## Execute sequence

1. Preserve the existing root-memory update and replace its now-stale
   seed-only-child statement with the actual candidate state only after the
   candidate tips and review results are verified.  It remains a fail-closed
   record: live, UI, vision, bulk, special, and `complete` state remain red.
2. In `scripts/check_coverage.py`, derive allowed execute companions solely
   from inventory `api_*_preview` names.  During the registered-tool check,
   accept only the exact `*_execute` counterpart as a non-inventory companion;
   all unrelated `api_*`/`ui_*` registrations must continue to fail.  Do not
   add execute rows, aliases, or qualification evidence to the manifest.
3. Add focused regressions in `tests/coverage/test_coverage_inventory.py`:
   a real inventory-backed `api_contacts_create_execute` registration is
   accepted; an unmatched execute-like registration is rejected; the existing
   uninventoried-tool and files-upload checks remain intact.  Run the focused
   coverage tests and `scripts/check_coverage.py --reject-false-completeness`.
4. Inspect the exact candidate diffs from
   `wave5_protocol_response_mapping`, `wave5_contact_writes`, and
   `wave5_contact_person_writes`.  Validate each owned product/test pair from
   a clean `git archive`; verify typed preview/execute shape, no write retry,
   no inventory/status mutation, and all claimed focused tests.  The current
   candidate commits are backstop commits after local work, not review approval.
5. After the three leaves are intentionally closed and a slot is free, run the
   root Grok candidate review described above.  Do not merge on the leaf's
   inaccessible-review failure; merge only after a cited root review accepts
   its actual branch tips or all required findings are closed.
6. Merge accepted candidates one at a time with labelled `--no-ff` merge
   commits: mapper first, then contacts and contact persons.  Re-run formatter,
   Ruff, Pyright, the non-live suite, coverage checker, and policy scan on the
   merged root.  Announce only material integrations.
7. With protocol mapping on root, launch the catalog and daybook Codex Power
   leaves from the merged baseline; catalog declares both `products` and
   `productPrices` roots for embedded prices.  Registration, inventory-evidence
   updates, and the remaining twelve operations stay a later explicit slice.

## Acceptance criteria for this slice

- A registered `api_*_execute` tool is allowed only when the exact
  `api_*_preview` inventory tool exists; no duplicate inventory row is created.
- The checker still rejects arbitrary/unpaired API or UI registrations, bulk,
  webhook, and files-create inventions.
- The current three candidate modules preserve their frozen 9-operation/18-tool
  subset of the 15-operation/30-tool design, but no API row has `implemented`
  or `contract_tested` advanced until root server registration and its own
  evidence are delivered.
- The independent root Grok review is recorded before candidate merges; live
  qualification and all UI/vision work remain red.

## Post-Mortem

- Completed the root-only coverage-checker preparation: an `api_*_execute`
  registration is accepted only as the exact non-inventory companion of a
  documented `api_*_preview` tool. The regression accepts
  `api_contacts_create_execute` and rejects an unpaired execute-like name;
  neither the manifest nor coverage status was changed.
- The independent root Grok review accepted the checker preparation and the
  protocol candidate's declared multi-root response mapping, while correctly
  rejecting the original contact nested-input shape and contact-person update
  schema. Both candidate branches now contain the requested source repairs,
  but the repaired tips still require a fresh independent review before any
  root integration.
- Verification passed: `scripts/lint.sh`, `scripts/test.sh` (**511 passed**),
  focused checker tests (**12 passed**), and the false-completeness checker.
  The previous clean-archive candidate checks were 26 mapper, 18 contact, and
  11 contact-person tests; the Grok overlay evidence recorded 58 focused tests.
- No live, browser, UI, vision, bulk, authentication, inventory-green, or
  completeness evidence was produced. `BILLY_TEST_MODE=full` was intentionally
  not run because required live/UI gates visibly remain red. No raw browser
  evidence was created; temporary candidate archives remain in ignored scratch.
- Next unresolved coverage slice: commit the root checker preparation, obtain
  independent approval of the repaired tips, selectively integrate product/test
  files only, then continue with catalog/daybook writes and root registration.
