# 198.29 Invoice create-form customer control inspect

## Pivot

P10 radio `113F1E05` supersedes the Tilgodehavender CTA dump in the
seeded plan name. Do not execute or commit another route-entry
diagnostic. Official invoice-list **Opret faktura** already proved
those entry points land on the same empty create form.

`FD39FFE7` still binds. Stay red. Do not finish.

## Goal

Inspect the **already loaded** invoice create form customer control
and derive one exact normal interaction that should expose or select
an existing customer. Use scoped DOM/AX plus page-local frontend
source or bundle/component evidence. No live API calls. No new
**Opret faktura** entry dump.

TDD first. Add a focused failing fixture for the derived interaction
before the smallest fix. If that interaction exists, prove invoice
draft create/update/delete through FastMCP with fresh-session
read-back and reverse cleanup. If no materially different normal
interaction can be derived, record `UI_CHANGED` and move to the
mandatory product-create slice. Do not accumulate more diagnostic
helpers.

Do not type blindly. Do not remake closed picker dumps. Do not
click **Godkend**, **Send**, or email.

Authority: `113F1E05`, `96908DC6`, `FD39FFE7`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Closed route entries:
`880dce8` list-CTA, `C0721A14` customer-detail. Closed form
probes: `2498696` scoped dump, `3b025f4` page-wide draft-save,
`e49c8f3` **Vælg kunde** rest, structure compare, `28C8FBC8`,
`31B0C7A6`, fiber, `E87B6AEF`, `07600147`, `452E0773`,
`51E18E60`, `A3AB03C3`, `9F777B8F`, `8EFD0EAD`, `4A5CD1E7`,
`F12B607E`. Do not remake them.

Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Executing or committing a Tilgodehavender / quotes / Duplikér /
  receivables **Opret faktura** landing dump
- Any other alternate-entry CTA enumeration
- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview until CUD is live-proved
  and independent review accepts
- Live API token calls, guessed `/v2/` fetches, docs re-lock
- Remaking closed picker, Ember, fiber, listener, descendant,
  list-CTA, or customer-detail dumps
- Page-wide observers, portal sweeps, force or `evaluate` clicks
- Persisting source text, locators, URLs, slugs, names, or screenshots
- Claiming `complete: true` or stopping the root
- Spawning children

## Baseline

HEAD `880dce8`. Not a backstop. `complete=false`. Honesty 16 stay
red. Invoice CUD rows still name `ui_invoices_*_open`. List CTA
dump is closed: same empty form, `proved_bind=none`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice.

Reuse existing create-form helpers and closed dumps as read-only
evidence. New work is one focused fixture plus the smallest local
inspect/fix, not a new route helper.

## Design choices

1. **P10 wins.** Do not open `ui_debtor_balances_list` for a CTA
   dump. Do not click a new **Opret faktura**.
2. **TDD first.** Honesty-16 stays 16 and red. First product edit
   is a failing fixture that names the derived interaction keys
   or names `unique_normal_action=false` with
   `next_slice=product_create`.
3. **Local evidence only.** Scoped DOM/AX on the already-known
   contact `INPUT` / `.pickerfield`. Page-local frontend source or
   already-loaded bundle/component tokens. No live API. No CDP
   remake of Ember/fiber/listener dumps.
4. **One interaction or none.** A proved interaction must be a
   unique unused normal action that is not a closed click/type/
   Gem/route. Else `UI_CHANGED` and product create is next.
5. **CUD only if that interaction binds.** FastMCP draft
   create/update/delete, fresh-session read-back, reverse cleanup.
   Stay red until independent accept + purge.
6. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.

## EXECUTE

### 1. Failing tests first

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
must pass (length 16, red).

Add a focused failing fixture for the create-form customer-control
inspect. Empty evidence must fail `missing_keys` or fail the
derived-interaction gate. Do not add a debtor-CTA helper.

### 2. Local inspect, then one interaction or stop

On the loaded `/invoices/new` form only:

- Read scoped DOM/AX of the known customer control.
- Read page-local frontend source or already-loaded component
  tokens. Persist allowlisted name tokens and booleans only.
- Derive at most one unused normal interaction.
- If derived, implement the smallest fixture-backed path and try
  that one interaction through FastMCP.
- If not derived, write `UI_CHANGED`, `unique_normal_action=false`,
  `next_slice=product_create`. Do not add more inspect helpers.

Never persist source, URLs, query values, or customer text.

### 3. CUD only if bind proved

If and only if the derived interaction binds an existing customer:

1. One disposable invoice draft CUD through FastMCP.
2. Independent fresh-session read-back after create, update, delete.
3. Reverse cleanup. Never **Godkend**, **Send**, email.

Otherwise skip CUD and start product-create research/fixture work
in the same EXECUTE only if time remains after the inspect is
recorded. Product create still must not remake delete-chrome.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus honesty-16.
No live API. `complete=false`.

## Done when

- Honesty-16 still length 16 and red
- No Tilgodehavender or other alternate-entry CTA dump landed
- Either one derived interaction was fixture-backed and CUD ran,
  or `UI_CHANGED` is recorded and product create is the next slice
- No remake of closed dumps
- Root still running
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice. P10 is the contract.
Stay red.

## Post-Mortem

Completed: `113F1E05` honored. No Tilgodehavender or other entry
CTA dump. Closed-probe restatement helper plus product persist
fail-closed. Invented `arrow_open` unique-action path removed after
IR FAIL. Helper now ignores fabricated tokens. Wiki states this is
not a new scoped DOM/AX or page-local source inspect.

Deviations: P10 asked for a local inspect of the loaded create form.
That inspect was not run. FIX-VERIFY chose IR option 4b: do not
claim a new inspect, do not remake closed dumps.

Review: FAIL on inspect method. Coverage still red. Product persist
gate accepted. No remap.

Tests: honesty-16 passed. Create-form and product persist unit
tests passed after the invented-token fix. Commit-mode `test.sh`
and lint run in this step.

Cleanup: no tagged customer, no invoice, no product created. Owner
dump restates closed flags only.

Next unresolved coverage slice: invoice draft CUD still unbound
under `FD39FFE7`. Product create persist stays fail-closed until a
unique UI delete path exists. Do not remake closed dumps. Stay red.
