# 186.44 ui accountGroups not_applicable freeze

## Goal

Product research143 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved reference UI
   parity rows for **`accountGroups` only** (all seven ops: get, list, create,
   update, delete, bulk_save, bulk_delete) — **7 rows total**.
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so `api.accountGroups.` is included.
3. Updates inventory tests for NA count 54→**61** and live/vision baseline
   94→**101** (`40` shell/parity dual-counts + `61` NA).
4. Adds a scrubbed wiki page for the freeze; optional one-line cross-link from
   geo / accountNatures NA wiki pages.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** `ui_account_groups_*`
   tools.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research143).  
Dual evidence (tmp, non-git): `tmp/research143_residual_parity_dual.json`.  
Product form contrast (tmp): `tmp/research143_product_form_observe.json`
(supports **rejecting** productPrices NA this slice).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–142).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39 geo NA; 186.40 currencies/locales NA; 186.43
accountNatures+balanceModifiers NA
(`wiki/ui_account_natures_balance_modifiers_not_applicable.md`).

## Why this slice

1. research142 deferred productPrices/organizations/bank* soft-seed NA;
   research143 ran a dedicated dual freeze and isolated **accountGroups** as
   the only residual family with soft-empty == nonsense **and** no alternate
   UI workflow (Kontoplan is accounts multi-section, not accountGroups CRUD).
2. Dual independent READY sessions: soft seeds `accountGroups` /
   `account-groups` body_len **127**, empty h1, identical to nonsense
   `zz-research143-no-such-route`; contrast tools dual-ok
   (`ui_settings_accounting_open` multi-h2 including Kontoplan,
   `ui_settings_vat_open`, `ui_settings_company_open`, `ui_daybooks_open`,
   bank + products shells).
3. Soft URL landing is not a productable shell — do not invent UI tools.
4. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class).
5. API accountGroups clear ops stay offline-green where already producted;
   bulk API stay red; API live remains `out_of_scope_by_user`.

## Non-goals

- Shipping any `ui_account_groups_*` FastMCP tools.
- Greening soft-empty paths as list shells.
- **productPrices** NA (research143: `/products/new` non-soft dual surface;
  reject NA).
- **organizations** NA or dual-count (settings company surface exists).
- **bankPayments** / **bankLines** NA or dual-count (Bankkonti/Afstemning
  shells exist; resource binding unproven).
- Dual-counting `accounts.list` (Kontoplan multi-section — deferred).
- Dual-counting `taxRates.list` (Momssatser multi-resource — deferred).
- Dual-counting `daybooks.list` (editor `daybooks/new` — deferred).
- NA for files/postings (uploads/transactions alternate workflows).
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.accountGroups.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 61` (prior 54 + **7** accountGroups ops
  including singular delete). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH143_RESOURCES = frozenset({"accountGroups"})`
  (mirror research142 set). Prefer **same evidence code**
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research143 families: evidence_ref
  `research143_account_groups_dual`; nonsense path
  `zz-research143-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_accountGroups`; contrast controls include
  settings_accounting / settings_vat / settings_company / daybooks_new /
  bank-accounts / products (research143 dual-ok). Keep research138/139/142
  branches intact. |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefix alone should wire all
  **7** accountGroups parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count 61; prefixes include accountGroups.
  - research143 evidence_ref branch assertions (mirror research142).
  - live/vision `40 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **101** when
    ROW_COUNT=61.
  - annual still NA-rejected red; bulk 92 red; no accountGroups UI tools;
    productPrices/organizations/bank* parity still red (not greened). |
| Wiki | New `wiki/ui_account_groups_not_applicable.md` (scrubbed) + sorted
  `_index` link. Optional one-line cross-link from geo / accountNatures NA
  pages. |
| Coverage regenerate | Run generator → refresh
  `coverage/ui_workflows_manifest.yaml`, `coverage/status.json`,
  `coverage/report.md`. |

## Product decision matrix

| Family | API parity ops | Dual evidence | Product |
| --- | ---: | --- | --- |
| **accountGroups** | **7** | soft-empty == nonsense (research143) | **not_applicable** |
| (prior) accountNatures/balanceModifiers | 12 | research142 | keep NA |
| (prior) geo + currencies/locales | 42 | research138–139 | keep NA |
| productPrices | 7 | soft empty + products form surface | **defer / reject NA** |
| organizations | 6 | soft empty + settings company | **defer** |
| bankPayments / bankLines | 14 | soft empty + bank shells | **defer** |
| accounts.list | 1 | multi-section Kontoplan | **defer** |
| taxRates.list | 1 | Momssatser multi-resource | **defer** |
| daybooks.list | 1 | editor not list | **defer** |
| annual_reports | discovery | nav exists, Upsedasse | stay red |

## Work sequence (EXECUTE)

1. Extend `GEO_UI_NOT_APPLICABLE_API_PREFIXES` + `ROW_COUNT=61` + research143
   resource set `{accountGroups}`.
2. Refresh applicator evidence strings / evidence_ref for research143 family;
   keep research138/139/142 paths.
3. Confirm `build_ui_manifest` applies to all 7 accountGroups UI parity rows.
4. Update inventory tests (counts, prefixes, evidence_ref branches, live/vision
   101).
5. Write wiki page + `_index` link.
6. Regenerate coverage manifests/status/report.
7. Run offline coverage/unit tests + lint (no live API; no full live UI suite
   required unless IR demands — NA has no new live tool).
8. Do **not** green coverage outside the 7 rows; complete false.

## Acceptance checks

- [x] 7 UI parity rows for accountGroups: `parity_status=not_applicable`, five
      flags true, empty tool_name.
- [x] `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 61`; live/vision status **101**.
- [x] implemented/contract status **285** (278+7) if inventory counts all NA
      rows (verify after regenerate).
- [x] evidence cites research143 dual soft-empty == nonsense; no credentials/slugs.
- [x] productPrices/organizations/bankPayments/bankLines **not** greened.
- [x] annual_reports still red NA-rejected; bulk 92 still red.
- [x] No new ui_* tools for accountGroups; no API live_tested flip.
- [x] Offline inventory tests pass; complete false.
- [x] Wiki page present; generator regenerated manifests (COMMIT later).

## Review focus (IR)

- Confirm design §10.2 NA accepted only for true absence (accountGroups), not
  soft-empty abuse of real shells (Kontoplan/Momssatser/Bankkonti/products).
- Confirm productPrices explicitly **not** greened despite soft seeds.
- Confirm annual contrast still rejected.
- Confirm live/vision math: 40 dual-count/shell + 61 NA = 101.
- Confirm no invent `ui_account_groups_*` and no API live green.

## Post-mortem

- **Result:** Producted research143 NA freeze for `accountGroups` (7 UI parity
  rows). GEO NA count 54→61; live/vision 94→101; implemented/contract 285.
  IR **ACCEPT** (no required fixes). complete remains false.
- **Deviations:** None vs plan. Optional IR N1/N2 (evidence prose hygiene /
  dual JSON judgment listing productPrices) left as no-op.
- **Review findings:** Product ACCEPT; overall completeness FAIL (bulk 92,
  annual_reports, residual UI parity, API live out of scope).
- **Tests:** lint.sh pass; offline test.sh 1565 passed / 35 deselected
  (EXECUTE + FIX-VERIFY reconfirm). No live API; no full ui-full suite
  required for this NA package.
- **Cleanup:** No disposable UI records; dual research profiles deleted
  during research; no frames; egress unchanged.
- **Next unresolved coverage slice:** Residual dual-count/NA candidates
  deferred by research143 (productPrices nested form; organizations via
  company settings; bankPayments/bankLines binding; accounts Kontoplan
  multi-section; taxRates Momssatser multi-resource; daybooks.list editor).
  annual_reports stays red; bulk 92 external-contract stays red.
