# 198.15 Invoice Kunde descendant map

## Goal

Map visible descendants inside the proved `pickerfield` (`E87B6AEF`).
The wrapper-center click is closed (`07600147`:
`changed_node_count=0`). Do not repeat it.

TDD first. Read-only walk of visible nodes inside that wrapper only.
For each descendant record tag category, role, class-token
categories, `data-*` names, `{dx,dy,w,h}` from the wrapper,
`pointer-events`, and registered listener types. Omit text and
every attribute value. Classify the already-loaded wrapper click
handler as `input_ignored`, `suffix_accepted`, `toggle_accepted`,
or `none`. Commit that enum plus a unique-target contract. Never
commit source, URLs, ids, names, tokens, source maps, or
screenshots.

Click once only if exactly one unused normal descendant is proved
and that target is not the contact `INPUT`, not wrapper-center, and
not the `9F777B8F` overlay geometry. Then create one disposable
tagged customer through FastMCP, confirm it fresh, attach scoped
mutation capture, click that descendant, and select an exact match
only if visible evidence proves it. Else record `UI_CHANGED`.
Delete any created customer. Prove absence. Stay red.

Do not remap invoice CUD rows. Do not do official-doc work.

Authority: `E87B6AEF`, `96908DC6`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Closed: `51E18E60`, `A3AB03C3`,
`9F777B8F`, `8EFD0EAD`, `4A5CD1E7`, `F12B607E`, `452E0773`,
`07600147`. Research: `.fractal/main.billy_complete/tmp/grok-research.md`
section 5. Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934
bytes. Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Repeating overlay, chevron, widget, picker-inventory,
  typed-only, event-causality, `F12B607E` route, `452E0773`
  control, or `07600147` wrapper-center click
- Treating `action_option_role_count==0` as a closed picker
- Typing, Tab, Enter, Escape, ArrowDown, blur, or another
  `change` dispatch
- Force click, `evaluate` click, generic portal sweep
- Fetching a script URL or issuing a `/v2/` URL
- Storing raw names, raw text, source, URLs, slugs, or
  screenshots in git
- Products, ledger, files, org, or bills
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Copying the bills vendor wrapper onto this field
- Persisting screenshots, frames, HAR, or traces in git
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `1936b7c`. Memory edits from this continue are uncommitted.
Not a backstop. `complete=false`. Honesty 16 stay red. Contacts
and bills CUD rows already name preview tools. Invoice CUD rows
still name `ui_invoices_*_open`.

`07600147` live dump
(`~/.local/share/billy-mcp/inspect-live-invoices-kunde-post-click.json`):

- `baseline_input_tag=INPUT`
- wrapper categories include `pickerfield`
- `baseline_hidden_subtree_count=2`
- `click_target=pickerfield`
- `click_count=1`
- `changed_node_count=0`
- `changed_nodes=[]`
- `exact_match_target=false`
- no descendant map
- no `wrapper_handler_guard`

Owner-only field dump (cite only, do not recapture): input center
is `INPUT` name `contact`. Right-edge hit is a nameless `DIV`
40x40 at `dx=242`, `pointer-events=auto`,
`hit_shares_smallest_wrapper=true`. `input_child_count=0`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. Existing files:

- `src/billy_mcp/ui_writes/invoices_kunde_post_click.py`
- `src/billy_mcp/ui_writes/invoices_kunde_control.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `src/billy_mcp/ui_writes/invoices.py`
- `src/billy_mcp/ui_writes/contacts.py`
- `tests/unit/test_ui_invoices_kunde_post_click.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

New files (do not reuse control / post-click / route / event / trace
key sets):

- `src/billy_mcp/ui_writes/invoices_kunde_descendants.py`
- `tests/unit/test_ui_invoices_kunde_descendants.py`

Parked write-family children stay parked. Unlimited budget is not
a reason to spawn. `728BD2E4` child-split applies after this field
is bound, not to a read-only map.

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing unit fixture. Current
   post-click dump flags (`click_target=pickerfield`,
   `changed_node_count=0`, `baseline_hidden_subtree_count=2`, no
   descendant keys) fail `kunde_descendant_missing_keys`. Run it.
   Capture the failure. Then implement. Do not reuse
   `REQUIRED_KUNDE_POST_CLICK_KEYS` as this fixture.
2. **Honesty-16 stays red.** Do not remap invoice tools.
3. **Read-only first.** The live map does not need a tagged
   customer. Open `/:org_slug/invoices/new`. Walk visible
   descendants inside the proved `pickerfield` only. Cap 16.
   Exclude the wrapper itself.
4. **Handler classify only.** Use CDP on the already-parsed
   `legacy-core` `scriptId`. Persist `wrapper_handler_guard`
   only. Never persist source, URLs, or tokens. Never fetch a
   script URL.
5. **Unique unused target.** `unique_target` is true only when
   exactly one visible interactive descendant is not the contact
   `INPUT` and not the `9F777B8F` overlay geometry. Category is
   `input` / `suffix` / `toggle` / `none`.
6. **Click gate.** Click that descendant once only after
   `unique_target=true`. Then create one `MCP-UI-INV-` + 8 hex
   customer through FastMCP, confirm it fresh, attach scoped
   observer, click, and select only if visible evidence proves
   an exact match. Else `UI_CHANGED` and no click.
7. **Cleanup.** If a customer was created, delete it. Prove
   absence in a fresh session. Purge raw handler text under
   `~/.local/share/billy-mcp/`. Commit allowlisted summary only.
8. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
9. **No remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.
10. **Live test short-circuit.** After the dump is delivered,
    later `ui-full` must verify the file and must not repeat a
    closed click.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_KUNDE_DESCENDANT_KEYS` and
`kunde_descendant_missing_keys` in
`invoices_kunde_descendants.py` **only after** a unit fixture
fails because those names do not exist, or add the fixture first
against a local helper stub. Preferred shape:

- Fixture copies the current live post-click dump flags only
  (`click_target=pickerfield`, `changed_node_count=0`,
  `baseline_hidden_subtree_count=2`; no urls, no tag text).
- Helper returns the missing key list from research section 5:
  `wrapper_tag`, `visible_descendant_count`, `descendants`,
  `unique_target`, `unique_target_category`,
  `wrapper_handler_guard`, `kunde_descendant_missing_keys`.
- Current post-click dump must fail with that exact set.

Do not edit bind/observe product files until that fixture fails.

Keep `REQUIRED_KUNDE_POST_CLICK_KEYS` and the closed
`07600147` / `452E0773` / `F12B607E` / `4A5CD1E7` /
`8EFD0EAD` fixtures.

### 2. Smallest helper, then live capture

- Implement the helper in `invoices_kunde_descendants.py`.
- Sanitizers: tag category, role, class-token categories,
  `data-*` names, `{dx,dy,w,h}` from wrapper, `pointer-events`,
  listener types. Never persist text or attribute values.
- `unique_target_from_descendants(payload)` is true only when
  exactly one unused interactive descendant remains.
- `wrapper_handler_guard_from_metadata` returns only the
  allowlisted enum.
- Isolate unit dumps so they cannot overwrite owner files.
- Live path through `create_server` `call_tool`:
  1. Open `/:org_slug/invoices/new`. Wait until
     `input[name=contact]` and the `pickerfield` wrapper are
     present.
  2. Read-only descendant map. No click.
  3. Classify the already-loaded wrapper click handler. Detach.
  4. Write allowlisted keys to
     `~/.local/share/billy-mcp/inspect-live-invoices-kunde-descendants.json`.
  5. If `unique_target` is true and the target is unused, one
     focused fixture, one FastMCP tagged customer, fresh confirm,
     scoped observer, one descendant click. Continue the draft
     CUD only if save is already safely proved. Else
     `UI_CHANGED`.
  6. If a customer was created, delete it. Prove absence in a
     fresh session.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the descendant gate into memory `state.md` / `todo.md`.
  No iteration labels.

### 3. Do not write a draft unless save is already proved

Default stop is the allowlisted descendant map. Do not click
**Gem som kladde** unless this same live run also proves line,
date, and the draft-save control before submit.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus the honesty-16
gate. Do not run live API tests. Do not claim `complete: true`.

## Done when

- Honesty-16 still length 16 and red
- Failing descendant fixture existed, then the helper lists
  missing keys on the current post-click dump
- Live run wrote the allowlisted descendant map and
  `wrapper_handler_guard` without repeating a closed click
- Either one unused unique descendant plus a focused failing
  fixture and one click, or `UI_CHANGED`
- Any created customer deleted. Fresh session proves absence
- Raw handler text purged. No credentials, names, source, or
  frames in git
- Invoice CUD rows still name `ui_invoices_*_open`
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice fits. No spawn.
Research section 5 is the contract. Stay red.

## Post-Mortem

Completed: TDD fixture, descendant helper, inspect, live FastMCP login,
second-session `/:org_slug/invoices/new`, read-only map inside the
proved `pickerfield`. Offline suite 1941 passed, 69 deselected. Live
descendant test passed. Honesty 16 still red. Invoice rows still name
`ui_invoices_*_open`.

Deviations: no unique unused target and no click. Overlay `DIV` at
`dx=242` is the closed `9F777B8F` geometry. `wrapper_handler_guard=none`.
`UI_CHANGED`. No customer created.

Review: FAIL. Slice dump contract met. Invoice CUD still unbound.
Required: commit untracked helpers, no remap, no overlay or wrapper
click, stay red.

Cleanup: no tagged customer. Fresh leftover list empty. Dump has no
URL, tag, source, or token. No frames.

Next unresolved slice: a different read-only capture after this dump
is committed. Stay red.

