# 186.70 ui invoices get open

## Goal

Product research169 ACCEPT:

1. **Scoped browser egress expansion** on `api.billysbilling.com` `path_allow`:
   keep GET `/v2/invoices` (186.69); **add** prefix POST and DELETE
   `/v2/invoices` so live harness can SPA-seed a disposable draft invoice and
   delete it without `BILLY_API_TOKEN`.
2. New read-only FastMCP UI tool **`ui_invoices_get_open`** that opens an
   **invoice detail/edit observe surface** (`/:org_slug/invoices/:id/edit`
   class) without Gem / Send / Slet / submit.
3. Green exact **`ui.parity.invoices.get`** / map `api.invoices.get` onto
   `ui_invoices_get_open` (`detail_open_only` / get-open parity).
4. Keep greened `ui_invoices_list` and `ui_invoices_create_open` unchanged
   (list-shell and form_open_only). Do **not** dual-count get onto those tools.

Target math (tip `89d7411` / product 186.69):
live/vision **169 → 170**; implemented/contract **353 → 354**; complete
**false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research169)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. invoices Support get/list/create/update/bulk. |
| GET plane | Committed 186.69 GET invoices/bills reconfirmed dual ok; billing integrations still blocked. |
| SPA seed dual | contact + product (no forced bad accountId) + invoice with **line productId** POST 200 dual under TEMP POST/DELETE invoices. Without productId → 422 `invoiceLine.productId` required. |
| Detail dual | After seed, list marker dual; text click → **`/:org_slug/invoices/:id/edit`**, name_n≥3, marker in body, **detail_ready true dual**. |
| products residual | list marker dual; soft URL detail **false dual** — REJECT greening products.get this slice. |
| bills residual | billLine `unitPrice` UNKNOWN_ATTRIBUTE dual — DEFER bills.get. |
| Isolation | Get-open ≠ list shell ≠ create form_open ≠ special.invoice_email. Green only **invoices.get**. |
| Deferred | products.get/update/delete; bills.get/update/delete; invoices.update/delete; invoice_email; bulk 92; annual_reports. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research169_focus_dual.json`, `tmp/research169_seed_retry_dual.json`.  
Temp egress reference: `tmp/research169_egress_temp.yaml` (do not commit TEMP bills lines).

## Non-goals

- Full `browser_action: allow` on `api.billysbilling.com`
- POST/DELETE `/v2/bills` or invoiceLines path_allow unless EXECUTE live network
  proves empty detail cannot settle without them (prefer **minimal** invoices
  POST+DELETE only; nested lines on create already worked via POST body)
- POST `…/emails` / invoice_email path_allow
- Greening `invoices.update` / `delete` / bulk_* / special.invoice_email
- Greening products.get* or bills.*
- Ticketed durable UI write tool for invoice create (API writes stay offline;
  live seed is harness-only SPA token)
- Submitting Gem / Send faktura / Slet / Godkend after open
- Dual-counting get onto `ui_invoices_list` / `ui_invoices_create_open`
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| `coverage/browser_egress.yaml` POST+DELETE invoices + evidence | Root Grok EXECUTE |
| Unit allow-matrix (`tests/unit/test_browser.py`) | Root Grok EXECUTE |
| Models + `BrowserRuntime.ui_invoices_get_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count invoices.get | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research169)

### A. Browser egress (`coverage/browser_egress.yaml`)

Host `api.billysbilling.com` remains `browser_action: path_allow` and
`api_client_action: exclusive_allow`.

**Keep** existing auth/bootstrap + contacts GET/POST/DELETE + countries GET +
products GET/POST/DELETE + accounts GET + salesTaxRulesets GET + invoices GET +
bills GET.

**Add** (minimum dual-proven seed/cleanup set):

| match | methods | path | purpose |
| --- | --- | --- | --- |
| prefix | POST | `/v2/invoices` | disposable draft seed (SPA token; nested lines in body) |
| prefix | DELETE | `/v2/invoices` | reverse-order cleanup after live test |

**Do not add this slice** (unless EXECUTE proves hard block):

- POST/DELETE `/v2/bills`, `/v2/invoiceLines`, `/v2/billLines`
- any `…/emails` path
- full host allow

Update host `evidence` / `purpose` / `test_references` to cite research169 +
`tests/live/test_ui_invoices_get_open.py`. Deny regressions stay: unlisted
billing integrations, api.billy.dk, default deny.

### B. Tool: `ui_invoices_get_open`

| Field | Contract |
| --- | --- |
| Name | `ui_invoices_get_open` |
| Input | empty / org-from-session only (mirror clients get: no caller id required if first openable draft is used; optional future id freeze only if dual needs it — prefer open first non-header list row / marker path like research169 text click) |
| Success model | `UiInvoicesGetOpenSuccess` with booleans only (no raw ids/PII): e.g. `detail_open`, `path_class` pattern, `edit_chrome_present`, `entry_date_control_present`, `line_chrome_present` / similar non-sensitive flags; `shell_kind=invoices_get` |
| Path class | `/:org_slug/invoices/:id/edit` (or equivalent detail that is not `/new` and not bare list) |
| Errors | typed `ToolError` for auth/egress/UI_CHANGED; never generic browser control |
| Forbidden | Gem, Send, Slet, Godkend, print/download submit, email send |

Mirror structure of `ui_clients_get_open` (models → browser runtime → server
registry → unit tests with injectable page/context).

### C. Live dual qualification

File: `tests/live/test_ui_invoices_get_open.py`

1. Two ephemeral profiles; credential refs; **unset BILLY_API_TOKEN**.
2. Session A: login → SPA seed contact + product + invoice (marker
   `R18670-TMP-INVOICE-GET-DO-NOT-USE` class description / product name);
   assert list shows draft; call `ui_invoices_get_open`; DOM assert edit path +
   form chrome; capture non-sensitive vision frames outside repo.
3. Session B: independent login; open same surface (tool or list→detail);
   read-back dual flags; no API token read-back.
4. Cleanup: DELETE invoice → product → contact (SPA token); fresh UI list
   verify markers absent dual.
5. Vision record: `tmp/vision-records/ui_invoices_get_open.json` (verdict,
   assertion refs, purge verified); purge raw frames.

### D. Coverage generator

- Add constants for tool name + live test reference (mirror clients get).
- `apply_ui_invoices_get_open_shell_evidence(..., parity_of_api_get=True)` for
  exact `api.invoices.get` → `ui.parity.invoices.get`.
- `parity_status=detail_open_only`; implemented/contract/live/vision true for
  that row only.
- Leave red: invoices.update/delete/bulk_*, bills.get*, products.get*,
  special.invoice_email, annual_reports, bulk 92.
- Inventory tests: assert invoices.get green; sibling residuals still
  discovery_required; no false green of create/list tools as get.

### E. Wiki

Short page `wiki/ui_invoices_get_open_shell.md` (+ index): detail get-open only;
seed/cleanup via SPA; not email; not bills/products get.

## EXECUTE checklist

1. Egress YAML + unit matrix + inventory egress assertions.
2. Models (`UiInvoicesGetOpenSuccess` / input) + browser method + helpers for
   invoice detail signature (edit URL + control counts).
3. Server register `ui_invoices_get_open`.
4. Unit browser/server tests (mock page; no live network).
5. Coverage generator dual-count + `tests/coverage/test_coverage_inventory.py`.
6. Live dual test + vision record writer.
7. Wiki shell.
8. `bash scripts/lint.sh` and offline `bash scripts/test.sh` (and live get test
   under ui credentials when running live mode).
9. Regenerate coverage (`generate_coverage_report` / project convention).
10. Confirm math 170/354; complete false; API live_tested false.

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Empty org has no invoice without seed | Live test owns SPA seed; tool itself remains observe-open of existing row |
| SPA seed needs extra GET paths | Already have products/contacts GET/POST/DELETE; do not widen without dual proof |
| Tool opens create form (`/new`) by mistake | Strict path class reject `/new`; require edit/detail signature |
| Accidental email/send | Never click Send; no emails path_allow |
| False green products/bills get | Inventory asserts remain discovery_required |
| Cleanup leak | Reverse DELETE + dual UI list verify; marker naming unique to 186.70 |

## Done when

- Plan executed; IR ACCEPT or required fixes applied.
- Offline suite green; live dual get-open + vision record pass.
- `coverage/status.json`: live/vision **170**, implemented/contract **354**,
  complete **false**, invoices.get parity green, residuals red as listed.
- Committed + pushed via `fractal commit`. Not `node finish`.

## Post-mortem

_(fill after FIX-VERIFY)_

## Post-mortem

### Completed

- Shipped `ui_invoices_get_open` (detail_open_only) dual-counting
  `ui.parity.invoices.get` / `api.invoices.get`.
- Browser egress: exact POST `/v2/invoices` + prefix DELETE `/v2/invoices`
  (emails and bills writes remain denied); GET invoices unchanged from 186.69.
- Live dual test with SPA disposable seed (contact+product+invoice, no
  `BILLY_API_TOKEN`), independent profile B, vision record with purge verified.
- Generator, unit, inventory, models, wiki shell, server registration.

### Deviations

- List h1 multi-match: product uses `.first` on invoices list signature (same
  class as bills create).
- POST is **exact** (not prefix) so `POST …/emails` stays denied.
- Optional IR N1–N3 left as-is (evidence wording, org id hardcode, commit
  packaging).

### Review

- IR product **ACCEPT** (`tmp/grok-review.md`); no required fixes.
- Completeness **FAIL** (expected).

### Tests

- `lint.sh`: pass.
- Offline `test.sh`: **1624 passed**, 45 deselected.
- Live: `tests/live/test_ui_invoices_get_open.py` pass (EXECUTE + residual
  reconfirm via vision record).
- Coverage: live/vision **170**; implemented/contract **354**; complete
  **false**.
- Wiki lint: wiki + memory clean.
- Did **not** run `BILLY_TEST_MODE=full` / ui-full — completeness not met.

### Cleanup

- Live harness deletes invoice→product→contact; markers absent on list
  read-back; frames purged (`purge_verified: true`).

### Next unresolved slice (not greened)

- products.get detail_ready still false class.
- bills.get (billLine seed shape; unitPrice unknown).
- invoices.update/delete; special.invoice_email.
- bulk 92 external-contract; annual_reports org_inaccessible.
