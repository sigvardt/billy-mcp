# 186.42 ui users list dual-count via settings Brugere shell

## Goal

Dual-count green **`ui.parity.users.list`** by mapping the already qualified
read-only **`ui_settings_users_open`** Indstillinger **Brugere** panel shell to
**`api.users.list`**, using the same empty-input shell-open-only contract already
live for `ui.discovery.settings_users`.

No new browser workflow. No new API/UI tools. No get/update/bulk greening.
No annual product. No bulk external-contract greening. No taxRates/files/accounts
dual-counts this slice.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research141).  
Dual evidence (tmp, non-git): `tmp/research141_residual_parity_dual.json`.  
Discovery product already green: `ui_settings_users_open` (research131 /
prior 186.32); wiki `wiki/ui_settings_users_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_users_open.py`,
vision record class `tmp/vision-records/ui_settings_users_open.json`.  
API offline list already green: `api.users.list` → `api_users_list`
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–141).  
Prior dual-count precedents: invoices/products/contacts/bills/transactions/
salesTaxReturns `.list` via `parity_of_api_list=True` in
`scripts/generate_coverage_report.py`.

## Why this slice (research141)

1. Dual headless sessions research141:
   - Typed `ui_settings_users_open` dual READY + success
     (`shell_kind=settings_users`, `users_panel_markers_present=true`).
   - Soft seed `/:org_slug/users` dual soft-empty body_len 127 (= nonsense);
     soft `settings/users` dual soft-empty (product stays hub+click Brugere).
   - No API token; no write CTA clicks; ephemeral profiles deleted.
2. Same inventory dual-count pattern as `ui.parity.salesTaxReturns.list`, but
   **parity_status remains `shell_open_only`** (settings panel, not a dedicated
   list shell).
3. get/update/bulk UI parity remain red (no dual-proved detail/write shells;
   bulk external-contract on API lane). Docs: users Supports has **no create**.
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research141: taxRates multi-resource Momssatser panel;
   files↔Bilag weak map; accounts/Kontoplan multi-section; NA package for
   accountNatures/balanceModifiers.

## Non-goals

- Inventing `api_settings_*` or a second UI users tool.
- Greening `ui.parity.users.get|update|bulk_save|bulk_delete`.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting taxRates, files, accounts, productPrices, organizations.
- Implementing user invite/update UI or API list filters/sort/pagination UI.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research131 live
  qualification + research141 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_users_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror VAT dual-count); when True: keep
  `api_row_id` from parity builder (`api.users.list`), append research141
  evidence mapping, keep `parity_status=shell_open_only`; discovery path keeps
  default False and `api_row_id=None`; wire
  `if api_row["id"] == "api.users.list": apply_...(parity_of_api_list=True)` in
  parity builder |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.users.list` → `ui_settings_users_open`; qualified shell count
  **39→40**; live/vision total **81→82** (`ui_shell_green + geo_na`); assert
  evidence mentions `api.users.list` + research141; assert get/update/bulk users
  parity stay red `discovery_required` |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_users_open_shell.md` — dual-count note for
  `api.users.list` / list open only; non-claims get/update/bulk; clarify offline
  API tools stay non-live; update desc / `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_users_open`
- models / server registration
- `tests/live/test_ui_settings_users_open.py`
- unit browser tests for the users settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_users_open` (existing) |
| Open sequence | hub `/:org_slug/settings` then observe-only click **Brugere** |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_users` |
| Panel markers | Brugere; Revisorer og bogholdere |
| Input | empty strict model |
| Soft seeds | reject (`settings/users`, `/users`, etc.) |
| CTA / write | never Invitér / Overdrag / Gem / Opret |
| Parity row | `ui.parity.users.list` / `api_row_id=api.users.list` |
| parity_status | **`shell_open_only`** (not `list_shell_open_only`) |
| API invent | never invent `api_settings_*` |
| Distinct shells | `ui_settings_user_open` (Profil) stays separate |
| get/update/bulk UI | stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.users.list` wiring.
2. Update inventory tests counts and assertions (40 qualified shells; 82
   live/vision with geo NA).
3. Run coverage generator; confirm status live/vision 82, complete false.
4. Wiki dual-count note on settings users shell page + index desc if needed.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (default commit mode).
6. Stop for independent review of the dual-count package only.

## Verification gates

- Offline tests pass; no live API.
- `ui.parity.users.list` green with tool `ui_settings_users_open`,
  `parity_status=shell_open_only`, evidence cites research141 + `api.users.list`.
- Other `ui.parity.users.*` remain red.
- Discovery `ui.discovery.settings_users` still green with `api_row_id=null`.
- Bulk 92 external-contract red; annual red; complete false.
- No secrets, frames, HAR, or credentials in the product diff.

## Risks

| Risk | Mitigation |
| --- | --- |
| Reviewer rejects settings panel as list parity | Document dual `/users` soft-empty + Brugere panel as the only dual-proved users surface; parity_status is shell_open_only not list_shell |
| Evidence string regresses discovery non-claims | Keep discovery path evidence without dual-count suffix; only parity flag adds research141 mapping |
| Count constants drift | Single `ui_shell_green = 40` and geo_na unchanged at 42 |

## Independent review focus

- One dual-count row integrity only.
- No false-green get/update/bulk or API live_tested.
- complete remains false.

## Post-mortem (fill after COMMIT)

### What shipped

- (pending)

### Independent review

- (pending)

### Tests / cleanup

- (pending)

### Next unresolved coverage slice

1. Residual UI parity (taxRates isolation, accounts/Kontoplan, files, further
   dual-counts / NA freezes).
2. `ui.discovery.annual_reports` stays red (`ANNUAL_REPORTS_ORG_INACCESSIBLE`).
3. 92 ambiguous bulk API rows stay external-contract red.
4. Residual non-bulk UI writes without dual-proved shells stay red.
5. API `live_tested` remains false with `out_of_scope_by_user`.

## Post-Mortem (fill after COMMIT)

### What shipped

- Dual-count greened `ui.parity.users.list` onto existing
  `ui_settings_users_open` (research141; `parity_of_api_list` on settings users
  applicator; `parity_status=shell_open_only`).
- Inventory live/vision **81→82**; implemented/contract **265→266**.
- Wiki dual-count note on `wiki/ui_settings_users_open_shell.md` + `_index.md`.
- No new tools, no `src/` changes, no API live greening.
- get/update/bulk users UI parity stay red.

### Independent review

- Independent review: **ACCEPT** (`tmp/grok-review.md`). Required fixes: none.
- Optional N1–N2 deferred (tmp dual JSON not committed; discovery evidence
  wording).
- Overall completeness: **FAIL** (expected).

### Tests / cleanup

- lint.sh: pass (wiki lint clean project + memory).
- test.sh offline: **1565 passed**, 35 deselected (EXECUTE + FIX-VERIFY).
- check_coverage pass; complete false; bulk 92 external-contract; annual red.
- No disposable Billy records; no new vision frames; no secret material in diff.
- Did not run `BILLY_TEST_MODE=full` / ui-full: completeness requirements not met.

### Next unresolved coverage slice

1. Residual UI parity dual-counts / dual-proved NA or shells (beyond users.list).
2. `ui.discovery.annual_reports` stays red (`ANNUAL_REPORTS_ORG_INACCESSIBLE`).
3. 92 ambiguous bulk API rows stay external-contract red
   (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`).
4. Residual non-bulk UI writes without dual-proved shells stay red.
5. API `live_tested` remains false with `out_of_scope_by_user`.
