# 190.5 Accept clients empty path and live contacts CUD

## Goal

Unblock live contacts create/update/delete. The dedicated org now opens
customers on `/:org_slug/clients/empty` with heading Kontakter. List and
create-open still require bare `/clients` and heading Kunder, so they return
`UI_CHANGED` after a READY login. Accept both path classes and both headings,
reuse that chrome in the contact write actor, then qualify contacts CUD
through `create_server()` `call_tool`. Green only those three rows after
proof.

Authority: `96908DC6`, `E1E454F4`, `E613984F`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
fingerprint is unchanged. Do not re-lock it.

## Non-goals

- Greening the other 13 CUD honesty rows
- Setting `BILLY_ORGANIZATION_ID` from memory or the identity file
- Live API calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children (shared `models.py`, `browser.py`, `contacts.py`, honesty)
- Claiming `complete: true`
- New generic browser or HTTP tools
- Self-approving vision or purging frames from the live test

## Baseline

HEAD `ac4269c`. DualSessionLogin READY returns the live URL slug. Ticket
execute compares that slug. Live write tests cannot write `accept` or purge.

Live inspect (no submit): login signature still matches; READY has a slug;
same-server `ui_clients_list` and `ui_clients_create_open` are `UI_CHANGED`;
same write runtime then sees path `/:org_slug/clients/empty`, heading
Kontakter, CTA Opret kontakt, and a create dialog with `name`,
`registrationNo`, `street`, `person_firstName`, and Gem.

`UiClientsListSuccess` and `UiClientsCreateOpenSuccess` hard-code
`path_class="/:org_slug/clients"` and `heading="Kunder"`.
`_CLIENTS_LIST_PATH` is `^/[^/]+/clients$`. Get/update/delete open also
require that list URL and heading.

Coverage `complete=false`. Sixteen CUD rows stay red until live CUD proof.

`tmp/grok-research.md` is this run's brief.

## Delegation

No children. A child fork would fight over `models.py`, `browser.py`,
`ui_writes/contacts.py`, and the honesty set. Root Grok implements.

## Design choices

1. **TDD first.** First product edit is a failing unit test. Run it. Capture
   the failure. Then implement.
2. **Accept both chrome shapes.** Bare `/:org_slug/clients` plus Kunder stays
   valid. `/:org_slug/clients/empty` plus Kontakter is also valid. Soft
   `/clients/new` still fails. Report the observed path_class and heading.
3. **Quotes empty is the pattern.** Widen `_CLIENTS_LIST_PATH` the same way
   quotes accept `/empty`.
4. **Reuse create-open chrome in the write actor.** `_create_customer` must
   wait for the research160 form signature after `_click_clients_create_cta`,
   not a six-second role-button loop.
5. **Live test uses `create_server()`.** Prefix the keyring refs. Separate
   profiles for write, observer, and cleanup. `BILLY_ORGANIZATION_ID` stays
   unset. Qualify login and CUD through `call_tool`. Vision stays
   `pending_review`.
6. **Green only after live pass plus independent accept.** Honesty set stays
   16 until the live test passes and an independent review accepts. Then lift
   only the three contacts ids.

## EXECUTE

### 1. Failing tests (first edit)

Extend `tests/unit/test_browser.py` and `tests/test_models.py`.

| Case | Expected today | Expected after fix |
| --- | --- | --- |
| Fake `/org/clients/empty`, h1 Kontakter, CTA Opret kontakt, `ui_clients_list` | `UI_CHANGED` | success, `path_class="/:org_slug/clients/empty"`, `heading="Kontakter"` |
| Fake `/org/clients`, h1 Kunder, same CTA | success | still success, heading Kunder |
| Fake `/org/clients/empty` plus create dialog fields, `ui_clients_create_open` | `UI_CHANGED` | success, name field visible |
| Fake `/org/clients/new` | `UI_CHANGED` | still `UI_CHANGED` |
| `UiClientsListSuccess(heading="Kontakter", path_class="/:org_slug/clients/empty")` | validation error | constructs |

Do not green. Do not run live in this first edit.

### 2. Implement chrome + write actor

Files:

- `src/billy_mcp/models.py` — `UiClientsListSuccess` and
  `UiClientsCreateOpenSuccess` accept both path classes and both headings
- `src/billy_mcp/browser.py` — `_CLIENTS_LIST_PATH` allows `/empty`; heading
  helper accepts Kontakter or Kunder; get/update/delete open use the same
  list URL helper
- `src/billy_mcp/ui_writes/contacts.py` — create uses
  `_click_clients_create_cta` plus form-signature wait; goto `/clients` is
  fine if the empty redirect is accepted
- `tests/test_models.py` — dump and schema for both headings
- `tests/unit/test_coverage_server.py` — fake wait payload if it asserts
  exact Kunder-only dumps
- `wiki/ui_clients_list_shell.md`, `wiki/ui_clients_create_open_shell.md`,
  `wiki/ui_contacts_writes.md` — empty path and Kontakter are valid

Do not edit `generate_coverage_report.py` or `coverage/status.json` yet.

### 3. Live contacts test

Keep `tests/live/test_ui_contacts_writes.py` on `create_server()`:

1. Temp `BILLY_BROWSER_PROFILE`. Keyring refs set. Org id unset.
2. `auth_login_start` / `auth_login_wait` through `call_tool`.
3. Stop if READY has no slug, slugs differ, or
   `AUTH_INTERACTION_REQUIRED`.
4. Unique tag `MCP-UI-C-<8hex>`. Preview + execute create, update, delete.
5. Independent execute-path read-back must see the name after create and
   the new name after update.
6. After delete, start a third fresh profile, log it in, prove the name is
   gone.
7. Four-state frames in owner-only temp storage. Pending vision record only.
8. Cleanup leftover tags through `ui_clients_delete_*` if a later step
   fails. Delete write, readback, observer, and cleanup profiles on every
   exit, including login `UI_CHANGED`.

If Gem is disabled without a live-required field, stop and record the
blocker. Do not invent official-API `country` / `paymentTermsDays` unless
the form shows them.

### 4. Green only the three contacts rows

Only after the live test passes and an independent review writes accept
plus purge:

- Remove `ui.parity.contacts.create|update|delete` from
  `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` (length 13).
- Point those three `tool_name` values at `ui_clients_*_preview`.
- Keep the other 13 red.

Do not green from a pending vision record.

### 5. Verify

Run the new unit tests, then the commit-mode suite and lint. Run the live
contacts test with the keyring refs. Do not run live API. Do not run
`ui-full` as a completeness claim.

## Done when

- `/clients/empty` plus heading Kontakter is a clients list, not `UI_CHANGED`
- Bare `/clients` plus Kunder still passes
- Live contacts CUD pass through `create_server()` `call_tool`
- Third-session cleanup is proved and leftover tags are gone
- Only the three contacts CUD rows are green, if and only if that proof
  and an independent accept landed
- No live API, no docs re-lock, no `complete: true`

## Post-Mortem

### Completed

- `/clients/empty` and heading Kontakter accepted on list and create-open.
- Offline suite 1818 passed. Lint passed.
- Live create through FastMCP submits; second session sees the new name.
- Update no longer clicks Gem kommentar via substring `text=Gem`.

### Deviations

- Live CUD did not finish. Update rename is not on the list after save.
- Live Ret form showed Opret, Ret, Gem kommentar and no `input[name=name]`.
- A later live login returned UI_CHANGED. No more login retries this step.

### Review

IR FAIL stands. R1 confirmed. R2 unproved. Honesty 16 still red. No greening.

### Tests

Commit-mode suite: 1818 passed. Live contacts: create pass, update fail.

### Cleanup

Named live-contact and inspect profiles deleted. `chrome-profile` not touched.

### Next coverage slice

Open the real contact edit form (not comment-save), prove rename on a second
session, then delete and third-session cleanup. Do not green until that proof
and an independent accept.

