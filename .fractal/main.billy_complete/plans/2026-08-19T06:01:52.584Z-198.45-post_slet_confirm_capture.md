# 198.45 post-Slet confirm capture

## Pivot

Owner `4EAEAFFD`: after commit `9a7f4a1`, do not wait for
**Ja, slet**. One read-only FastMCP path clicks exact **Mere**
then exact **Slet** once, then captures the scoped dialog or
overlay, roles, text, active element, sanitized URL and heading,
visible button and link labels, geometry and z-index, and
whether navigation or DELETE occurred. Derive the confirm
control from that capture. Failing fixture first. Direct
browser proves **Slet** is a SPAN inside `A.link` with
`data-ember-action` under `LI`. That does not prove the confirm
label. Never force or evaluate click, sweep portals, guess
labels, or delete outside FastMCP.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"121myuqjdm53603"`): `DELETE /v2/invoices/:id` has no body and
is idempotent. Official docs do not name a UI confirm label.
Do not re-lock docs (`728BD2E4`). Do not create a seventh
invoice. Do not remake dumps. Do not spawn a child.

HEAD `9a7f4a1` already proves leftover
`MCP-UI-INV-6CDB396B` **Enhedspris** `2,00` in a second
session. Unique **Mere** and exact **Slet** match `70DB45E6`.
Delete then waits for hardcoded `CONFIRM_DELETE = "Ja, slet"`
and records no DELETE. Egress already allows DELETE prefix
`/v2/invoices`.

## Goal

Capture the exact post-Slet state on leftover
`MCP-UI-INV-6CDB396B` through FastMCP. If the dump names one
unique confirm label, click that label, require DELETE 2xx, then
reverse-clean the six leftover triples. Stay red.

1. Offline failing fixture: `delete_draft_invoice` must not wait
   for hardcoded **Ja, slet** before a post-Slet capture exists.
   Today's `_click_confirm` is the fail.
2. After unique **Mere** and one exact **Slet** click, record
   the scoped dump keys from `4EAEAFFD`. No confirm click.
3. Live FastMCP read-only on leftover `MCP-UI-INV-6CDB396B`.
4. If the dump names one unique confirm, smallest execute fix,
   DELETE 2xx, fresh-session absence, then reverse-clean.
5. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `src/billy_mcp/ui_writes/invoices_form_delete.py`
- `src/billy_mcp/ui_writes/invoices_form_page.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child. `D68E402A` forbids a permanent diagnostic helper
unless the failing fixture cannot observe the post-Slet subtree
without it. Prefer a function on the delete module plus an
owner-only tmp dump, not a new inspect tool.

## TDD

`728BD2E4` / `4EAEAFFD`: failing fixture first, then the
smallest fix.

1. In `tests/unit/test_ui_invoices_writes.py`, replace
   `test_invoice_delete_waits_for_confirm` so the delete module
   must capture post-Slet state and must not call
   `_click_confirm` / wait for `CONFIRM_DELETE` before that
   capture exists. Run it. Capture the fail.
2. Smallest capture: after `_click_unique_mere` and one exact
   **Slet** click, record allowlisted keys only:
   `heading_token`, sanitized `path_class`, `active_tag`,
   `active_role`, visible button labels, visible link labels,
   dialog or overlay count, role tokens, `hit_tag` (`a` or
   `span`), `delete_seen`, `navigated`. Never store customer
   text, ids, raw URLs, or frames. Prefer the unique visible
   **Slet** `A.link` that owns the SPAN when that node is
   unique. Otherwise click the unique visible exact text
   already proved by `70DB45E6` and record `hit_tag`.
3. Live FastMCP read-only on leftover `MCP-UI-INV-6CDB396B`.
   Open the list row, unique **Mere**, one **Slet**, dump,
   stop. Do not click confirm. Do not delete. Assert the dump
   keys. Write the dump outside git under owner-only tmp.
4. If the dump names one unique confirm label, add a focused
   failing fixture for that exact click, then the smallest
   execute fix. Require `DELETE /v2/invoices/:id` 2xx and a
   fresh session with no `MCP-UI-INV-6CDB396B` row. Then
   reverse-delete the other five drafts, six products, and six
   customers. Third session: no `MCP-UI-INV` / `MCP-UI-PRD`
   tags.
5. If the dump shows no overlay, no DELETE, and no navigation,
   record `UI_CHANGED` with those counts. Do not guess a
   second click. Do not portal-sweep. Do not copy bills
   `force=True`.
6. Vision record: `author=live_test`,
   `reviewer_verdict=pending_review`. Do not self-accept. Do
   not purge in the live test. Honesty-16 stay red.

## Out of slice

New invoice create, files, daybook, **Godkend**, emails, bulk,
docs re-lock, dump remakes, child spawn, `ui_invoice_lines_*`
tools, live API, invoiceLines PUT, viewport changes, guessed
**Ja, slet**, bills `force=True`, a seventh leftover.

## Success

A post-Slet dump that names the normal confirm, or a documented
`UI_CHANGED` with those exact counts. Then, only if named, one
FastMCP delete with DELETE 2xx and an empty leftover set.
Coverage stays red until independent accept and purge.

## Post-Mortem

Work completed: failing fixture first. Delete no longer waits for
**Ja, slet**. Unique **Mere**, then the `A` ancestor of unique
**Slet**. Owner-only dump plus `E8EA9823` `candidates` rows
(role, labels, geometry, z-index, active_contained). Live
FastMCP leftover recapture on `MCP-UI-INV-6CDB396B`:
`hit_tag=a`, `candidates=[]`, dialog/alertdialog/aria-modal
counts 0, `delete_seen=false`, `navigated=false`. Execute
returns `UI_CHANGED`. Offline lint passed. Honesty-16 stay red.

Deviations: first live dump lacked overlay candidates. Split
capture into `invoices_form_delete_dump.py`. Recapture then
satisfied the empty-candidate contract. No confirm click. No
seventh invoice. No child.

Review: IR FAIL on missing candidates. FIX-VERIFY added the
candidate list. Live empty list after counted locators matches
`E8EA9823`. Delete persist and leftover cleanup still fail.

Tests: unit invoice writes passed. Live leftover CUD failed on
unnamed confirm after a complete dump. Commit-mode `test.sh`
2099 passed, 82 deselected.

Cleanup: six leftover triples remain. Next slice: a named
confirm from new evidence, or keep `UI_CHANGED` without a
guessed second click. Stay red. Do not finish.

