# 186.81 ui daybook transactions create open + residual NA package

## Goal

Product research181 ACCEPT as one root-only package (full slice, not A-only):

### A. Tool — `ui_daybook_transactions_create_open`

1. New typed FastMCP tool `ui_daybook_transactions_create_open`
   (`create_chrome_open_only`).
2. Maps **only** `api.daybookTransactions.create` →
   `ui.parity.daybookTransactions.create`.
3. Path class **`/:org_slug/daybooks/:id`** (research181 dual SPA-seeded
   daybook detail / editor).
4. Observe-only open daybook id surface; assert create chrome markers dual:
   **`Tilføj kassekladdelinje`** and **`Ingen postering valgt`**
   (and/or Posteringer / Kassekladde context markers).
5. Never click Tilføj to submit lines, never Bogfør, never confirm Slet,
   never permanent delete, never Gem line post.
6. Keep greened `ui_daybooks_open`, `ui_daybooks_get_open`,
   `ui_daybooks_delete_open` unchanged. Do **not** dual-count get/delete
   tools onto create. Do **not** map daybookTransactionLines.create onto
   this tool (lines stay residual unless a later NA package).

Peer pattern: shell/chrome open-only tools (e.g. daybooks get/delete open,
settings shells) — text/DOM presence without write submit.

### B. NA freeze — daybookTransactions dedicated non-create (4)

| UI row | API op |
| --- | --- |
| `ui.parity.daybookTransactions.get` | `api.daybookTransactions.get` |
| `ui.parity.daybookTransactions.list` | `api.daybookTransactions.list` |
| `ui.parity.daybookTransactions.update` | `api.daybookTransactions.update` |
| `ui.parity.daybookTransactions.delete` | `api.daybookTransactions.delete` |

Evidence: research181 dual — dedicated routes soft-empty body_len 138;
seeded dtx id routes not detail forms (`get_ready` false); embedded_only true
on daybook detail. Create is product (A), not NA. Do **not** map delete to
daybook Mere→Slet (already greened as daybooks.delete). Do **not** NA bulk_*.

### C. NA freeze — taxRates non-list (4)

| UI row | API op |
| --- | --- |
| `ui.parity.taxRates.get` | `api.taxRates.get` |
| `ui.parity.taxRates.create` | `api.taxRates.create` |
| `ui.parity.taxRates.update` | `api.taxRates.update` |
| `ui.parity.taxRates.delete` | `api.taxRates.delete` |

Evidence: research181 dual — settings Momssatser list panel ready; Opret/Ret/
Gem/Slet chrome 0; soft `/tax-rates/:id` etc inputs 0. Keep
`ui_settings_vat_open` for **list only**. Do **not** NA bulk_*.

### D. NA freeze — salesTaxRulesets non-list (4)

| UI row | API op |
| --- | --- |
| `ui.parity.salesTaxRulesets.get` | `api.salesTaxRulesets.get` |
| `ui.parity.salesTaxRulesets.create` | `api.salesTaxRulesets.create` |
| `ui.parity.salesTaxRulesets.update` | `api.salesTaxRulesets.update` |
| `ui.parity.salesTaxRulesets.delete` | `api.salesTaxRulesets.delete` |

Evidence: research181 dual — Regelsæt text on VAT settings shell; dedicated
soft routes body_len 138; no get/create/update/delete chrome. list stays on
`ui_settings_vat_open`. Do **not** NA bulk_*.

### E. NA freeze — taxRateDeductionComponents non-bulk (5)

| UI row | API op |
| --- | --- |
| `ui.parity.taxRateDeductionComponents.get` | `api.taxRateDeductionComponents.get` |
| `ui.parity.taxRateDeductionComponents.list` | `api.taxRateDeductionComponents.list` |
| `ui.parity.taxRateDeductionComponents.create` | `api.taxRateDeductionComponents.create` |
| `ui.parity.taxRateDeductionComponents.update` | `api.taxRateDeductionComponents.update` |
| `ui.parity.taxRateDeductionComponents.delete` | `api.taxRateDeductionComponents.delete` |

Evidence: research181 dual — no nav/dedicated path; soft-empty class; no form
chrome. Do **not** NA bulk_*.

### F. NA freeze — transactions get/update/delete (3)

| UI row | API op |
| --- | --- |
| `ui.parity.transactions.get` | `api.transactions.get` |
| `ui.parity.transactions.update` | `api.transactions.update` |
| `ui.parity.transactions.delete` | `api.transactions.delete` |

Evidence: research181 dual — list h1 Posteringer ready; id/detail routes lack
Ret/Gem/Slet form dual (`get_ready` false). Keep `ui_transactions_list` for
list. Do **not** product `transactions.create` in this plan (Ny postering
optional add-on deferred). Do **not** NA bulk_*.

### Explicit DEFER / REJECT (out of this plan)

- `ui_transactions_create_open` (Ny postering) — research181 product-ready
  add-on; next slice
- `daybookTransactionLines.*` non-bulk — residual / later NA or product
- `salesTaxReturns.get/update` — research181 DEFER (primary 502; not dual)
- bulk 92 external-contract (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`)
- annual_reports org_inaccessible
- offline 29 clear API gaps (separate offline lane)
- re-product daybooks get/delete or taxRates.list / transactions.list shells

Target math (tip after 186.80 / 385/201/129):

- Product +1 + NA 4+4+5+3+4 = **21** rows greened under scoped policy
- live/vision **201 → 222**
- implemented/contract **385 → 406**
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **129 → 149** (+20 NA; product is not GEO NA)
- complete **false**

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research181)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. daybookTransactions Supports create; taxRates/salesTaxRulesets/taxRateDeductionComponents/transactions Supports as tabled in brief. |
| SPA daybook seed dual | POST/DELETE `/v2/daybooks` 200 dual; optional draft daybookTransaction POST/DELETE 200 dual under session token only. |
| Create chrome | `Tilføj kassekladdelinje` + `Ingen postering valgt` true dual on `/:org_slug/daybooks/:id`. |
| Dedicated dtx surfaces | soft-empty 138 dual; get_ready false dual → NA g/l/u/d. |
| taxRates / rulesets / deduction | list panel / soft-empty / no write chrome dual → NA packages. |
| transactions g/u/d | list green; detail chrome absent dual → NA; create deferred. |
| salesTaxReturns | DEFER not dual (502 primary). |
| Cleanup / token | cleanup dual; api_token_used false; profiles purged. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`  
Dual: `.fractal/main.billy_complete/tmp/research181_focus_dual.json`

## File ownership (root EXECUTE)

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` — input/success for `ui_daybook_transactions_create_open` |
| Browser | `src/billy_mcp/browser.py` — protocol + impl (daybook id open; assert Tilføj kassekladdelinje + empty postering markers; never submit) |
| Server | `src/billy_mcp/server.py` — register tool |
| Generator | `scripts/generate_coverage_report.py` — daybookTransactions.create tool map; GEO NA set +20 ids; live test refs |
| Egress | `coverage/browser_egress.yaml` — allowlist live test on daybooks paths if required (reuse daybooks allow if already present) |
| Unit tests | `tests/unit/test_browser.py`, `tests/test_models.py`, coverage unit tests as peers |
| Live dual | `tests/live/test_ui_daybook_transactions_create_open.py` — dual seed daybook, open create chrome, independent second session, cleanup, vision record path |
| Vision | non-sensitive record only; raw frames outside repo; purge after review |

Do **not** invent generic browser tools. Do **not** use `BILLY_API_TOKEN`.  
SPA seed/cleanup only under existing daybooks egress.

## EXECUTE steps (ordered)

1. Add models + browser method + server registration for
   `ui_daybook_transactions_create_open`.
2. Wire coverage generator: tool map for create; GEO NA evidence strings for
   the 20 NA ops (research181 citation); regenerate manifests/status.
3. Unit/contract tests for models + browser fake page markers + coverage row
   flags.
4. Live dual test: headless primary+secondary; seed disposable daybook (and
   cleanup); assert chrome; second-session read-back; vision review record;
   purge frames; assert no API token.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` before claiming EXECUTE
   done. Live test in EXECUTE or FIX-VERIFY as peer packages did.
6. Confirm counts: live/vision 222; implemented/contract 406; GEO NA 149;
   complete false; bulk still red; API live false.

## Safety / qualification gates

- Headless only; Billy host allowlist; API base lock.
- Observe-only create chrome (no line submit / no Bogfør / no Slet confirm).
- Independent second-interface read-back (not API).
- Vision mandatory but not sole pass.
- Cleanup verified; no credentials/screenshots/HAR in repo.
- No bulk greening; no API `live_tested` true; no node finish.

## Done when

- Tool registered and dual live test passes with vision record + purge.
- Exactly the NA rows listed B–F greened as `not_applicable` with evidence;
  create row tool-green; bulk untouched; deferred items still red/deferred.
- Offline suite green; coverage math matches target; complete false.

## Post-mortem (fill after FIX-VERIFY)

- Actual counts:
- Deviations from plan:
- Follow-ups:

## Post-Mortem

### Work completed

- Producted `ui_daybook_transactions_create_open` (`create_chrome_open_only`) mapping
  only `api.daybookTransactions.create` on `/:org_slug/daybooks/:id` with
  Tilføj kassekladdelinje + Ingen postering valgt observe-only.
- GEO NA freeze for 20 ops: daybookTransactions g/l/u/d (4), taxRates g/c/u/d (4),
  salesTaxRulesets g/c/u/d (4), taxRateDeductionComponents×5, transactions g/u/d (3).
- Live dual + vision accept + purge verified.
- FIX-VERIFY N1: live test path added to generated browser egress test_references.

### Deviations

- Did not product `ui_transactions_create_open` (Ny postering dual-ready) this
  slice — deferred by plan.
- Did not NA/product daybookTransactionLines — deferred.
- salesTaxReturns get/update still deferred (research181 non-dual).

### Review findings

- IR **ACCEPT** package; overall completeness **FAIL** (expected).
- Required fixes: none.
- Optional N1 egress live-ref: **applied** in FIX-VERIFY.

### Test results

- lint.sh: pass
- offline test.sh: 1658 passed / 53 deselected (EXECUTE); re-run in FIX-VERIFY
- live: `tests/live/test_ui_daybook_transactions_create_open.py` pass
- coverage: live/vision 222; implemented/contract 406; GEO NA 149; complete false
- No ui-full (not complete)

### Cleanup state

- Disposable daybooks deleted dual in live harness; vision frames purged
  (purge_verified true); no API token used.

### Next unresolved coverage slice

- Prefer: `ui_transactions_create_open` (Ny postering) and/or
  daybookTransactionLines residual; bulk 92 external-contract; annual_reports
  org_inaccessible; offline API 29 discovered-only; salesTaxReturns get/update
  re-probe. complete remains false.
