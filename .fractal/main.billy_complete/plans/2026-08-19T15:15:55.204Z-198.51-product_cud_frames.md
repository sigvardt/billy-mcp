# 198.51 Product CUD four-frame capture

## Pivot

Owner `FD39FFE7` / `56354201` / design §10.4 / §14.3: product
create/delete is live-proved through FastMCP, but the pending
vision record `b1597ed1c6d0426cad3b4d9b4b45d125` has an empty
frame dir. Independent Grok cannot accept an empty folder.
HEAD `4470c8e` already accepted invoice CUD. This slice captures
four write frames on the existing product CUD and leaves
`pending_review`. Do not accept. Do not purge. Do not remap.
Do not green.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn a child. Do not remake
dumps (`A337A622`). Do not remake leftover reverse-clean. Do
not accept leftover `217dcc7d`. Do not accept empty `b1597ed1`.

Last commit `4470c8e` is a clean 198.50 close, not a
`(failed on <step>)` backstop. Working tree clean.

## Goal

Wire four owner-only frames onto the existing FastMCP product
create/delete, then run that live test once. Honesty-16 stay red.

1. Offline fail-first: live product test still uses
   `write_vision_record` and never sets `BILLY_VISION_FRAME_DIR`.
   `products_submit` never writes `02_before_submit.png`.
2. Capture:
   - `01_before.png` empty `/products` (**Ingen produkter**)
   - `02_before_submit.png` filled create dialog before **Gem produkt**
   - `03_after_create.png` tagged row on unfiltered `/products`
   - `04_after_delete.png` restored empty list
3. Live helper is `write_live_pending_unless_accepted`. Live tests
   still must not write `accept` or purge (`C7DBE974`).
4. No honesty-16 membership change. No preview-tool remap.

## Why no child

Root already owns `src/billy_mcp/ui_writes/products*.py` and
`tests/live/test_ui_products_writes.py` after
`ui_products_writes` merged. Independent vision is later, on
this Grok (`wiki/review_provenance_rules.md`). Research forbids
a new child. `D68E402A` forbids a new inspect helper. The seven
write children stay completed and merged.

## TDD

`48ABEEB7` / `02CB47D8` / `C7DBE974` / design §10.4 / `728BD2E4`:
lock the missing capture first, then implement, then live-run.

1. Add `tests/coverage/test_ui_products_writes_vision.py`
   modeled on `test_ui_invoices_writes_vision.py`:
   - Load `tmp/vision-records/ui_products_writes.json`.
   - If `author=live_test`: assert `pending_review`,
     `purge_verified=false`, `qualifies_for_coverage_vision` is
     false, and `~/.local/share/billy-mcp/vision-tmp/run-<run_id>`
     exists with the four PNG names.
   - Run it on HEAD. It must fail: dir `b1597ed1...` is empty.
2. Add source locks in `tests/unit/test_ui_products_writes.py`:
   - Live test sets `BILLY_TEST_MODE=ui-full` and
     `BILLY_VISION_FRAME_DIR` before create execute.
   - Observer captures `01_before.png` before create,
     `03_after_create.png` after FastMCP create, and
     `04_after_delete.png` after third-session absence.
   - Assert `02_before_submit.png` is a file after
     `ui_products_create_execute`.
   - Call `write_live_pending_unless_accepted`. Do not call
     `write_vision_record`. Do not write `accept` or
     `purge_frame_dir(`.
   - In `products_submit.py` `_fill_and_save`: fill name and
     **Enhedspris**, then `live_allowed_vision_frame_dir`, then
     `02_before_submit.png`, then **Gem produkt**.
   - Run on HEAD. Capture the fail.
3. Implement the smallest capture:
   - `_fill_and_save` after `_fill_price` and before `_GEM`:
     if `live_allowed_vision_frame_dir` returns a dest, prove
     `input[name=name]` equals the tagged name, screenshot
     `02_before_submit.png`, fail `UI_CHANGED` if the file is
     missing or empty. Production without the conjunction
     writes nothing.
   - Live test: `monkeypatch.setenv` for mode and frame dir.
     Add `_capture` like invoices (products list, no search).
     Keep leftover sweep and disposable `MCP-UI-PRD-` tag.
     Keep DELETE 2xx watch and third-session **Ingen produkter**.
4. Wiki + memory: residual table stays
   `owner_proved_pending_fastmcp`. `ui_products_writes.md`
   records that frames are now captured and still pending.
   Stay red. Not finish.
5. Offline unit/contract/coverage on the touched files.
   Then run only
   `tests/live/test_ui_products_writes.py::test_ui_products_create_delete_via_call_tool`
   under `BILLY_TEST_MODE=ui-full`. Do not set `BILLY_API_TOKEN`.
   Do not remake dumps. Do not green honesty-16.

After the live run the pending `run_id` will be the new `run-*`
directory, not `b1597ed1`. Update the coverage fixture to use
`record.run_id` (not a frozen empty id). Four PNGs must exist
for that run. `qualifies_for_coverage_vision` stays false.

## Out of scope

- Independent accept or purge of any product run.
- Coverage remap to `ui_products_create_preview`.
- Product update tools.
- Files, daybook, leftover probes, archive dumps.
- Official-docs re-lock.
- `complete: true` or `fractal node finish`.

## Done when

- Live product CUD writes four owner-only PNGs and a pending
  `author=live_test` record through
  `write_live_pending_unless_accepted`.
- `02_before_submit.png` is gated by
  `live_allowed_vision_frame_dir`. Production writes no frame.
- Offline source locks and the pending-vision fixture pass.
- Live FastMCP create/delete still proves unfiltered presence
  then DELETE 2xx then third-session absence.
- Honesty-16 rows stay `implemented=false`, `live_tested=false`,
  `vision_verified=false`. `complete` stays false.
- Stay red. Not finish. Independent accept is the next slice.

## Post-Mortem

Work completed: four owner-only product frames on
`run_id=29c1b1de26a14976aaa1b98bfe9de46e` through FastMCP
create/delete. `write_live_pending_unless_accepted`. Capture is
gated by `live_allowed_vision_frame_dir`. IR FAIL: capture proved
name only. FIX-VERIFY passed expected price into
`capture_filled_create_form` and requires normalized Enhedspris
equality before screenshot. No live remake. Stay red.

Deviations: empty `b1597ed1` was not accepted. New run id after
live capture. Owner `94A4C844` added a price read-back gate
after EXECUTE.

Review: IR FAIL on missing unit-price DOM proof. Required fix
landed offline. Owner `A41C242F` independently empty invoices,
products, and contacts. Frames still pending.

Tests: live product CUD passed. commit-mode suite passed after
capture. Price fixtures pass. Stay red. Not finish.

Cleanup: tagged product deleted. Org independently empty.
Frame dir kept for later accept.

Next unresolved: independent accept and purge of
`29c1b1de26a14976aaa1b98bfe9de46e`, then remap
`ui.parity.products.create` to `ui_products_create_preview`.
Honesty-16 stay red.

