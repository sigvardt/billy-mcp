# 198.64 locales ticketed create and update

## Pivot

`05F3200D` is binding under `9B979A05`. HEAD `c412588` is a clean
198.63 currencies close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are none.
Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-05f3200d.html`,
`billy-api-docs-chunk-research-05f3200d.js`,
`research-05f3200d-locales-section.html`,
`research-05f3200d-locales-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the currencies lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2locales`.
Supports: `get by id, list, create, update, bulk save, bulk
delete`. No singular delete. Property table is exactly two rows
with empty notes: `name` string, `icon` string.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.locales.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_locales_*` or bulk tools. Do not fold this into
`reference_reads.py`. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.locales.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.locales.create` | `api_locales_create_preview` / `_execute` | `POST /locales` | nested optional `{name, icon}` |
| `api.locales.update` | `api_locales_update_preview` / `_execute` | `PUT /locales/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 10 method-closed + 2 readonly
map + 2 meta-delete = 14. Bulk 92 stay red. Implemented 538,
contract 543 (HEAD 536 / 541). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload copy shape is
`src/billy_mcp/api/currency_writes.py`. Country groups already
ship optional string `icon`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_locale_writes.py`, copied from currencies
   for the typed optional nested payload:
   - Register count is 4. Preview fields:
     `api_locales_create_preview` `{locale}`,
     `api_locales_update_preview` `{id, locale}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{name, icon}`. JSON schema types: both are string.
     `additionalProperties` is false on outer and nested schemas.
     No `input` wrapper. Nested `required` is empty or absent.
   - Outer extras, empty update `id`, `localeId`, a JSON array
     for `name` or `icon`, nested objects, and other inner extras
     fail `ValidationError` / FastMCP `extra_forbidden`.
     Execute extras fail. Preview still makes no HTTP.
   - Empty nested object `{"locale": {}}` is valid. Partial
     payloads such as `{"name": "X"}` or `{"icon": "da"}` are
     valid. Do not require `name` or `icon`.
   - Preview create sends no HTTP. Canonical request is
     `{"locale": { ...set keys only... }}`. Expected effect
     `{action: create, resource: locale}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_locale_write_tickets.py`, copied from
   currency ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/locales` with the
     exact nested JSON. Execute PUTs
     `/v2/locales/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/locales/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `locales` plus optional `meta.deletedRecords`. Do not
     expose a top-level `locale` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 10`, residual `== 14`. Assert
   `api.locales.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk locales rows
   unspecified. Bump every hardcoded `implemented_rows == 536` /
   `contract_tested_rows == 541` in that file to **538 / 543**
   (seven pairs). `complete` stays false. Live/vision counts stay
   339.
4. Map both ids in `OPERATION_TEST_REFERENCES` to
   `tests/api/test_locale_writes.py`,
   `tests/api/test_locale_write_tickets.py`, plus
   `SERVER_REGISTRY_TEST_REFERENCE`, next to the existing
   `api.locales.get` / `.list` rows. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_locales_writes_are_ticketed_offline_and_not_live_tested`
   next to the currencies inventory tests.

Run:
`uv run pytest tests/api/test_locale_writes.py tests/api/test_locale_write_tickets.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/locale_writes.py` (new), copy
`currency_writes.py`:

- `LocalePayload`: `name: str | None = None`,
  `icon: str | None = None`, `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{locale}`.
- Update preview input: `{id min_length=1, locale}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/locales`. Singular root `locale`.
  Plural root `locales`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_locale_write_tools` next to
`register_currency_write_tools`. Do not change reference
reads.

`scripts/generate_coverage_report.py`:

- Remove `api.locales.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids.
- Do not touch bulk helpers, the other 10 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` locales create/update true,
  delete false (`("locales", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 10. These two rows are ticketed
  offline tools. Residual honesty remaining 14. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional string `name` and `icon` is
  enough to ship the ticketed POST/PUT. Reject `localeId`. Unauth
  405 is not the contract. Empty payload is allowed.

Keep `wiki/ui_currencies_locales_not_applicable.md` as history. Do
not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.locales.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.locales.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research139).
- Do not accept `localeId` or a JSON object/array for `name`
  or `icon`.
- Do not require any nested field.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.
- Saved `9B979A05` still owns the remaining 14 residual rows
  and bulk92. Saved `96908DC6` still owns overall finish.

## Independent review

After EXECUTE, review must re-parse `#v2locales` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still two optional fields: `name` string,
   `icon` string.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /locales` and `PUT /locales/:id`.
6. Nested payload rejects `localeId`, arrays, nested objects, and
   extra keys. Empty nested object is valid. JSON schema type
   `string` holds for both fields.
7. `live_tested` is false.
8. Remaining residual honesty is 14 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.

## Post-Mortem

Work completed: ticketed offline `api.locales.create` and
`.update` in `src/billy_mcp/api/locale_writes.py`. Preview
tools take a nested payload of optional string `name` and
`icon` with `extra=forbid`. `localeId`, arrays, nested
objects, and extra keys fail at the FastMCP boundary. Empty
nested object is valid. Execute binds `POST /locales` and
`PUT /locales/:id` through the shared write protocol.
Coverage regenerated: implemented 538, contract 543, live/vision
339, complete false. Residual honesty remaining 14.

Deviations: none from the official field table. Registry count
asserts also bumped (`api` tools 301, implemented API rows 199).
Ticket and execute contract tests live in
`tests/api/test_locale_write_tickets.py` so each test file
stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
suite `2314 passed, 83 deselected`. Cleanup: no live UI records.
Next unresolved slice after COMMIT: saved `AB6ABE84` ticketed
`states` create and update. Residual honesty remaining 14.
Bulk 92 stay red. `complete` stays false.

