# 142.8 Wave-5r Product Integration and Independent Review

## Decision

Treat Research77 as the current cited contract brief: its official-document
body is byte-identical to Research76 and it explicitly preserves the gate
order. The completed Wave-5r salesTaxReturns leaf is the only ready product
integration. Product review must be sequential, because an independent Grok
review needs the exact merged root commit. No Wave-5s implementation starts
until that review gives an explicit offline-only verdict.

## Existing evidence to preserve

1. Review the uncommitted Research77 wiki page, offline probe-rules update,
   root index, memory state, and plan. Confirm they are cited, non-sensitive,
   research-only, and do not green any row.
2. Refresh and lint the project wiki; run the required local quality suite
   before committing this research baseline. The resulting commit gives all
   later review work a stable, auditable starting point.
3. Keep Research77's boundaries intact: `invoiceLogs` is only the recommended
   next offline special; files upload, invoice email, invoice delivery,
   transactions CUD, 405 false friends, bulk, live, and UI remain unstarted.

## Product verification and integration

1. Inspect the three-dot diff for
   `main.billy_complete.wave5r_sales_tax_returns_product` at `cf33343` against
   the accepted Wave-5r freeze and product-implementation research. Verify it
   changes exactly the update pair, registration, contract tests, and honest
   generator evidence.
2. Verify executable artifacts from a clean archive of the child branch, then
   rerun the targeted formatting, lint, type, API contract, registry, coverage
   integrity, and non-live/non-vision checks on the root integration candidate.
   Check that preview is network-free; execute performs one `PUT
   /salesTaxReturns/:id`; the `salesTaxReturns` response root is enforced; and
   tickets cover tamper, replay, expiry, and sensitive-value redaction.
3. Merge with `git merge --no-ff -m
   "merge main.billy_complete.wave5r_sales_tax_returns_product"` only if every
   verification passes. Strip the exact child node seed directory from the
   pending merge before committing, so the parent receives the deliverables
   rather than the child's runtime seed. Regenerate coverage and assert the
   root evidence is precisely 179 implemented/contract-tested, 0 live/vision,
   264 API tools, and `complete: false`.
4. Update current-state memory to say the product is merged but not yet
   independently accepted. Refresh/lint the wiki and commit the labelled merge
   integration after the full required non-live checks pass. The saved child
   handoff is then complete and can be unsaved.

## Independent review child (sequential)

After the root integration commit, create a childless Grok review leaf with
the explicit route required by policy:

```sh
fractal node init wave5r_sales_tax_returns_product_review \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --base=main.billy_complete \
  --agent=grok \
  --title='Wave-5r salesTaxReturns Product Independent Review' \
  --max-depth=0 --max-children=0 --max-descendants=0
```

Its exclusive deliverable is
`wiki/wave_fiver_product_independent_review.md`. It must independently inspect
the merged root tip and current official documentation; verify the exact two
tool names, strict wire/root/ticket behavior, coverage/registry arithmetic,
and fresh test evidence; and issue **ACCEPT** or **REJECT** for the offline
product only. Its review must explicitly exclude live, UI, vision, bulk,
salesTaxReturns create/delete, cleanup qualification, and overall completeness.
It must not modify product source or coverage status except to report a
defect. Commit and finish only that review record. If its Grok run fails before
editing because of authentication, quota, or rate limit, retry the full review
step with the permitted fallback route and record that the fallback is not a
Grok acceptance.

## Deferred work

Wave-5s starts only after the product review ACCEPT. Its first candidate is the
read-only `api_invoice_logs_list` special from Research77; no implementation or
coverage change for it belongs in this iteration.

## Post-Mortem

### Completed

- Committed the cited Research77 baseline, clean-archive verified the completed
  product leaf, and merged the seven product artifacts at `5ad69a6` without
  importing the child seed.
- Root evidence is 179 implemented and contract-tested API rows, 264 API tools,
  zero live/vision rows, and `complete: false`.
- The authoritative Grok product review accepted the offline-only update pair
  in `wiki/wave_fiver_product_independent_review.md`. A duplicate review leaf
  had no edits and was intentionally closed to avoid a competing verdict.

### Verification and cleanup

- Clean-archive formatting, Ruff, Pyright, 19 focused tests, and coverage
  integrity passed before integration. Root lint, type, policy, coverage
  integrity, and 1,163 non-live/non-vision tests pass after review.
- No live API calls, UI/browser actions, screenshots, traces, or sensitive
  material were created. The product review explicitly keeps create/delete,
  bulk, live, UI, vision, cleanup, and completeness fail-closed.

### Next unresolved coverage slice

- Wave-5s-A is now eligible to implement only `api_invoice_logs_list` under
  the existing cited Research77 brief.
