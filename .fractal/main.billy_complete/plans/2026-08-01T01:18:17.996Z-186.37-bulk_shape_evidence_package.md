# 186.37 bulk offline shape evidence package (no green)

## Goal

Lock research136 offline bulk **shape evidence** into durable wiki + inventory
hints + fail-closed fixtures/tests **without** greening any of the 92
`ambiguous_bulk` rows, without registering bulk FastMCP tools, and without
live credentialed API traffic.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research136).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged).  
Probe artifacts: `tmp/write-probes-research136.json`,
`tmp/write-probes-research136-body.json`.  
Prior harness: `src/billy_mcp/live_probe.py` research95/96 bulk-delete forms;
`scripts/generate_coverage_report.py` `bulk_rows()`.

## Why this slice (not bulk tools, not UI geo)

1. Official docs only list Supports “bulk save/delete”; no `/bulk` path, no
   body/response examples (research136 §1–2).
2. Unauth probes freeze method-level shapes only:
   - **bulk_save:** `PUT /v2/{plural}/bulk`; non-object root → **400**
     `INVALID_REQUEST_BODY` (“object as root”); any JSON object root → **401**
     `AUTHENTICATION_REQUIRED` (field-level bulk schema post-auth, unproven).
   - **bulk_delete:** `DELETE /v2/{plural}?ids[]=…`; empty ids → **400**
     `INVALID_DELETE_ID_ARRAY` with form
     ``DELETE /{resource}?ids[]=123&ids[]=456``; synthetic non-empty ids →
     unauth **200** meta-only (**not** effect proof).
3. User scope forbids live API qualification; full response/partial-failure
   contract cannot be completed this iteration. Shipping tools would false-green.
4. Dual-count list maps exhausted; residual 29 blocked; annual red. Evidence
   package is the only honest bulk progress under offline policy.
5. Secondary geo `not_applicable` deferred (needs live dual UI + vision; separate
   iteration).

## Non-goals

- Greening any `api.*.bulk_save` / `bulk_delete` (`implemented`,
  `contract_tested`, or tool_name non-empty).
- Registering `api_*_bulk_*` preview/execute tools on the FastMCP server.
- Live `BILLY_API_TOKEN` probes or authenticated bulk mutation.
- Greening residual 29 writes, bulk UI parity, annual_reports.
- Inventing plural-key body schema (`{"contacts":[…]}`) as proven contract —
  only object-root + method/path classes are offline-proven.
- Treating unauth 200 meta-only bulk-delete as cleanup or success contract.
- `complete: true`, live/vision count changes, headed browser, children.

## Ownership

**Root Grok only. No children.**

| Area | Paths |
| --- | --- |
| Fail-closed bulk_save fixtures | `src/billy_mcp/live_probe.py` — add research136
  bulk_save body matrix (object-root required; array/null/string/no-body →
  INVALID_REQUEST_BODY; object root → AUTHENTICATION_REQUIRED at unauth gate);
  keep bulk_delete research95/96; ensure candidate routes stay
  `PUT /{collection}/bulk` and `DELETE /{collection}` + `ids[]` only |
| Unit tests | `tests/unit/test_live_probe.py` — assert research136 bulk_save matrix
  semantics; fail-closed (no network open flags); bulk still 92 candidates;
  no tool registration helpers |
| Coverage generator | `scripts/generate_coverage_report.py` `bulk_rows()` — enrich
  **hints only**:
  - bulk_save `method_or_route`: `PUT /v2/{resource}/bulk` (shape freeze;
    Supports still ambiguous for full contract)
  - bulk_delete `method_or_route`: `DELETE /v2/{resource}?ids[]=` form class
  - `request_fields`: shape hints e.g. bulk_save `["json_object_root"]`;
    bulk_delete `["ids[]"]` (query) — **not** full field schemas
  - `errors`: include `INVALID_REQUEST_BODY` / `INVALID_DELETE_ID_ARRAY` as
    appropriate alongside auth errors
  - `evidence`: cite research136 + docs etag/md5 + offline unauth only
  - **Keep** `source_kind`/`contract_status` = `ambiguous_bulk`,
    `tool_name` = `""`, `implemented`/`contract_tested`/`live_tested` = false |
| Inventory tests | `tests/coverage/test_coverage_inventory.py` — extend
  `test_bulk_rows_remain_ambiguous_and_toolless` (or sibling) to assert shape
  hints present **and** still ambiguous/toolless; reject accidental green bulk |
| Regenerated manifests | run generator → `coverage/api_v2_manifest.yaml`,
  `status.json`, `report.md` (complete must stay false; live/vision 38) |
| Wiki | `wiki/offline_write_probe_rules.md` — research136 bulk_save object-root
  table + reaffirm bulk remains empty-tool red;
  `wiki/wave5u_method_probe_contract.md` — note research136 bulk_save body
  matrix still BLOCK BEFORE NETWORK for real method; update `updated` |
| Wiki index | only if new page; prefer extend existing pages (no new page
  unless lint requires) |

## Implementation steps (EXECUTE)

1. Implement `research136_bulk_save_body_matrix()` (name flexible) in
   `live_probe.py` with immutable assessments mirroring research95 style:
   - non-object bodies → validation error classification
   - object root bodies → unauth auth-required classification
   - `registers_tool: false`, `opens_network: false` / BLOCK BEFORE NETWORK
   - no empty-array no-op claim
2. Unit tests covering matrix invariants + existing bulk_delete fixtures still
   pass.
3. Update `bulk_rows()` hint fields; regenerate coverage manifests.
4. Inventory tests for 92 still ambiguous + new hint assertions.
5. Wiki promotion (scrub iteration labels from durable prose; cite research136
   path as parent scratch, durable rules on wiki).
6. Run offline suite subset: unit live_probe + coverage inventory + ruff/pyright
   as needed for touched files. No live UI suite required for this slice.
7. Confirm `coverage/status.json`: `complete: false`, bulk count 92,
   implemented/contract 222 (or unchanged count), live/vision 38.

## Acceptance criteria

- [x] No bulk FastMCP tools in server registry.
- [x] All 92 bulk rows: `source_kind=ambiguous_bulk`, empty `tool_name`,
      `implemented=false`, `contract_tested=false`, `live_tested=false`.
- [x] Bulk rows carry offline shape hints (`PUT …/bulk`, `DELETE …?ids[]=`,
      object-root / `ids[]` request hints, relevant error codes).
- [x] `live_probe` encodes research136 bulk_save fail-closed matrix with unit
      tests.
- [x] Wiki offline rules + wave5u contract updated; wiki lint clean.
- [x] Offline unit + coverage tests pass; no secrets in tracked files.
- [x] `complete: false`; no annual/residual greening.

## Review focus

- No false-green bulk (checker must still reject implemented bulk).
- Do not claim plural-key body schema proven.
- Unauth 200 meta-only bulk-delete remains non-evidence.
- Docs fingerprint still locked to known etag/md5.

## Risks

| Risk | Mitigation |
| --- | --- |
| Enriching request_fields looks like full contract | Document as shape hints only; keep contract_status ambiguous_bulk |
| Tests hit live network | Fixtures pure offline; no httpx to Billy in unit tests |
| Generator regen churn | Only bulk_rows fields change; re-run full generator once |

## Post-mortem

(filled after EXECUTE / REVIEW)

## Ready for EXECUTE

Root-only bulk shape evidence package per research136 primary recommendation.

## Post-Mortem

### Completed
- research136 bulk shape evidence package: fail-closed
  `research136_bulk_save_body_matrix` in `src/billy_mcp/live_probe.py` with unit
  tests; `bulk_rows()` inventory shape hints under required
  `AMBIGUOUS Supports:` prefix (`PUT …/bulk` object-root, `DELETE …?ids[]=`);
  wiki promotion (`offline_write_probe_rules`, `wave5u_method_probe_contract`);
  regenerated coverage manifests.
- No bulk FastMCP tools; all 92 bulk rows remain `ambiguous_bulk`, empty
  `tool_name`, `implemented`/`contract_tested`/`live_tested` false.
- Independent review **ACCEPT** (`tmp/grok-review.md`); no required fixes.
  Optional N1 (`json_object_root` label) deferred as non-blocking.

### Deviations
- None material. `method_or_route` kept `AMBIGUOUS Supports:` prefix so
  `check_coverage.py` accepts shape text (required after first generator draft
  that omitted the prefix).

### Review findings
- Product ACCEPT; completeness FAIL expected (bulk 92, residual 29, UI parity,
  annual_reports, API live out_of_scope_by_user).

### Verification
- `bash .fractal/main.billy_complete/scripts/lint.sh` pass.
- `bash .fractal/main.billy_complete/scripts/test.sh` pass: **1563 passed**, 35
  deselected (~6m).
- `scripts/check_coverage.py` pass; `complete: false`; live/vision 38.
- Wiki lint: clean.
- No `BILLY_TEST_MODE=full` / ui-full (completeness not claimed).
- No live API token; no headed browser; no frame evidence this slice.

### Cleanup
- No disposable Billy records created.
- Probe artifacts remain under node `tmp/` (non-sensitive offline probe JSON).

### Next unresolved coverage slice
1. Residual 29 clear offline writes stay blocked (405/metadata) — do not ship
   ticketed tools without method proof.
2. Bulk 92 remain red until full body/response contract exists under offline
   policy (shape freeze only).
3. UI parity `not_applicable` / deeper workflows for no-UI API families (e.g.
   geo cities/countries) — secondary research path.
4. `ui.discovery.annual_reports` stays red (Upsedasse) until org/product unlock.
5. Dual-count list maps exhausted under wiki freezes.
