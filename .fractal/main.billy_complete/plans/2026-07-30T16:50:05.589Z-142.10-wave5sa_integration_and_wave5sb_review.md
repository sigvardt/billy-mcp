# 142.10 Wave-5s-A integration and Wave-5s-B research review

## Objective

Integrate the committed, list-only `api_invoice_logs_list` product after a
clean-tree verification, preserve the new cited files-upload research handoff,
and commission independent Grok reviews for both gates.  This remains an
offline-only cycle: live, UI, vision, bulk, singular invoice-log reads, JSON
file creation, and file uploads to Billy are not claimed.

## Current evidence and gates

- `main.billy_complete.wave5sa_invoice_logs_product` completed at `851c42a`.
  Its product diff adds one typed GET `/invoiceLogs` tool, coverage evidence,
  registry wiring, and focused contract tests.  The prior Grok research page
  and its independent research review freeze the list-only query and response
  contract.
- The root worktree contains the Grok research79 files-upload handoff.  It
  cites the current official documentation and unauthenticated method matrix,
  but is not itself a product or a coverage green-light.
- The files-upload handoff has three malformed trailing `)` lines (wiki, plan,
  and private state); repair those editorial artifacts before committing the
  research package.
- Product acceptance for Wave-5s-A must be a distinct Grok review after the
  merge.  A separate Grok review of research79 is required before assigning
  the next Codex Power implementation leaf.

## Decomposition decision

Two independent, current-document review tasks can run concurrently only after
the root has committed research79 and merged the Wave-5s-A product.  Spawn two
Grok leaves then, each owning one new review page and its own `.fractal` seed:

1. `wave5sa_invoice_logs_product_review`: review the merged tool, tests,
   inventory status, and current official documentation; write only
   `wiki/wave_fivesa_invoice_logs_product_independent_review.md`.
2. `wave5sb_files_upload_research_review`: independently recheck research79
   against current official documentation and the no-generic-upload boundary;
   write only `wiki/wave_fivesb_files_upload_research_independent_review.md`.

Neither leaf may edit `_index.md`; the parent will regenerate it when their
review pages merge.  Do not spawn the later Codex Power upload product until
both review verdicts have been read and accepted.

## Execute sequence

1. Repair the three trailing editorial artifacts, update/lint the wiki, and
   run the required non-live formatting, lint, type, unit/contract, coverage,
   safety, dependency, and credential checks.  Commit the files-upload
   research package and this plan before creating new children.
2. Review `851c42a` with a three-dot code diff and a clean `git archive`
   checkout.  In that checkout run the focused invoice-log, registry, coverage
   generator/checker, and non-live/non-vision test suite; confirm the locked
   API base and that only the documented four query values are emitted.
3. If and only if those checks pass, merge with
   `git merge --no-ff -m "merge main.billy_complete.wave5sa_invoice_logs_product"`
   and rerun the root verification set.  Preserve `complete: false`, live 0,
   vision 0, and red bulk/UI rows.
4. Initialise, configure, commit, and start both Grok review leaves with
   `--agent=grok`, no implementation ownership, no screenshots or live data,
   and cited verdict pages.  Their completion requirements must be satisfiable
   from their own committed review page; no parent sign-off gate is imposed.
5. Report the integration and reviewer gates over radio.  If either reviewer
   rejects, keep the affected coverage red and route a bounded correction to a
   Codex Power leaf before requesting a fresh Grok review.

## Verification targets

- The research79 wiki page, private state, and plan have no stray trailing
  text; `wiki update` and `wiki lint` are clean.
- Clean-archive and root checks pass for `api_invoice_logs_list`; the server
  has 265 API tools and coverage reports 180 implemented/contract-tested rows,
  zero live/vision rows, and `complete: false`.
- The merge is labelled, source changes remain limited to the approved
  invoice-log product, and no credentials, browser artefacts, or live data are
  introduced.
- Both Grok leaves are running on the integrated root baseline with disjoint
  review-page ownership.  The Wave-5s-B implementation stays unstarted until
  both review gates are independently accepted.

## Post-Mortem

Completed: the Wave-5s-A product commit `851c42a` was independently
clean-archive checked, then integrated with labelled merge `5b8719b`. Root
verification passed with 1180 non-live tests, 265 registered API tools, 180
implemented/contract-tested API rows, zero live/vision rows, and
`complete: false`.

Deviation: both intended Grok review leaves failed at authentication before
their first review action. Their claimed uncommitted research-review draft was
discarded because it had no valid Grok provenance and exceeded its ownership
scope. The required retry path uses two isolated Codex Power fallback review
leaves; their conclusions remain limited and do not satisfy the mandatory Grok
audit.

Cleanup: no UI/live/browser actions, credentials, coverage greening, or upload
product tools were introduced. The next unresolved coverage slice is review
integration followed by the ticketed files-upload product, while live/UI/vision,
bulk, and the Grok-auth blocker remain red.
