# 198.55 API live_tested semantics

## Pivot

`7C9348E1` is binding. HEAD `b049384` is a clean 198.54 close,
not a `(failed on <step>)` backstop. Memory edits from SYNC are
in-progress, not product work.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19 GET): ETag
`W/"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`,
147924 bytes. Page chunk
`api-docs-2c3fd2ccd1de2fed.js` MD5 `7902955e4d1a56ecea2d85551f17b49c`.
Inventory lock stays `wcw4x9hqvu3603` /
`8b94b0135c91fd15fe54ea33e088a4be`. Do not re-lock.

Exact docs delta: two intro sentences, `GET /v2/user/organizations`
became `GET /v2/organizations` (minus 10 bytes). Supports lines,
bulk92 wording, resource TOC, and OpenAPI 404s are unchanged.
Do not treat this as a bulk unlock. Do not change
`api.special.user_organizations` or `api_user_list_organizations`.

Do not spawn. Do not remake UI dumps. Do not arm `_ledger_write`.
Do not finish. No live API.

## Goal

Match NODE.md completion requirement 2, which overrides design
§12.2 live-API green:

- Every API row keeps `live_tested=false`.
- Every API row carries machine-readable
  `qualification.live_api=out_of_scope_by_user`.
- API completeness is `discovered`, `implemented`,
  `contract_tested`. Not `live_tested`.
- Do not set `qualification.kind=out_of_scope_by_user` on an API
  operation. That kind remains owner-skip for UI rows only
  (annual reports, residual-five writes).
- Do not green `live_tested`.
- `complete` stays false on bulk92.

## Why no child

Generator, checker, coverage loader, and inventory tests share
`scripts/generate_coverage_report.py`. A child would fork that
file. Seven write children stay completed and merged. 62 leftover
descendants stay retired.

## TDD

Add `tests/coverage/test_api_live_qualification_semantics.py`.
On HEAD it must fail. Assert:

1. All 305 API rows have `live_tested is False`.
2. All 305 have a dict `qualification` with
   `live_api == "out_of_scope_by_user"`.
3. No API row has `qualification.kind == "out_of_scope_by_user"`.
4. Implemented rows (184) use
   `kind == "live_api_deferred"` and
   `scope_code == "LIVE_API_OWNER_SKIP"`. Existing tools stay.
   `tools_allowed` is not forced false.
5. Residual 29 and bulk 92 keep their current kinds and
   `tools_allowed is False`.
6. `coverage_is_complete` is false when an otherwise-green API
   row has `live_tested=True`.
7. `coverage_is_complete` is true for a toy API+UI pair when API
   `live_tested` is false, `live_api` is set, the three offline
   flags are true, there is no `ambiguous_bulk`, and UI is fully
   green including vision.
8. `require_complete_errors` does not demand API
   `live_tested=true`. It still fails on missing `live_api`, on
   API `live_tested=true`, and on remaining `ambiguous_bulk`.

Run that file first. Capture the failure. Then implement.

Also rewrite the existing toys in
`test_status_completeness_is_derived_from_row_evidence` and
`test_require_complete_checks_each_row_state_and_bulk_resolution`
so they no longer pass API `live_tested=True` as the green path.

## Implement

`scripts/generate_coverage_report.py`:

- `API_QUALIFICATION_FIELDS = ("discovered", "implemented",
  "contract_tested")`.
- `api_row_is_qualified(row)`: owner-kind skip stays UI-only
  via `is_owner_out_of_scope`. For API rows require the three
  offline flags, `live_tested is False`, and
  `qualification.live_api == "out_of_scope_by_user"`.
- `coverage_is_complete` uses that helper for API rows.
- `live_api_deferred_qualification()` returns
  `kind=live_api_deferred`, `scope_code=LIVE_API_OWNER_SKIP`,
  `live_api=out_of_scope_by_user`. Do not copy
  `kind=out_of_scope_by_user`.
- `apply_api_live_api_deferred(operations)` after
  `apply_residual_clear_honesty` in `build_api_manifest`.
  Attach only when `qualification` is missing. Do not overwrite
  bulk or residual 29.
- Keep `DOCS_ETAG` / `DOCS_MD5` on the lock. Optional
  non-authoritative `docs_etag_observed=tmhc6wpdc835zt` on the
  new helper only. Do not rewrite 305 evidence strings.

`scripts/check_coverage.py` `require_complete_errors`: drop the
API `live_tested=true` demand. Add failures for API
`live_tested=true` and missing `live_api`.

`src/billy_mcp/coverage.py` `_is_green`: API lane is
discovered + implemented + contract_tested, and `live_tested`
must be false. UI lane unchanged.

Do not edit `coverage/status.json` by hand. Run
`python scripts/generate_coverage_report.py` so yaml/json/report
regenerate. `complete` must stay false. Blocker may stay the
bulk freeze. `live_tested_rows` stays 339 (UI only).

## Verify

```
uv run pytest tests/coverage/test_api_live_qualification_semantics.py \
  tests/coverage/test_coverage_inventory.py -q
uv run ruff check scripts/generate_coverage_report.py \
  scripts/check_coverage.py src/billy_mcp/coverage.py \
  tests/coverage/test_api_live_qualification_semantics.py
uv run pyright scripts/generate_coverage_report.py \
  scripts/check_coverage.py src/billy_mcp/coverage.py \
  tests/coverage/test_api_live_qualification_semantics.py
```

Do not run `ui-full`. Do not run live API tests.

## Out of slice

- Re-locking official docs
- `api.special.user_organizations` path rewrite
- Bulk tools or residual 29 tools
- Live UI / live API
- `fractal node finish`

Independent review after EXECUTE. Then COMMIT.

## Post-Mortem

Landed. Independent review PASS. No required fixes.

Work completed:

- Failing `tests/coverage/test_api_live_qualification_semantics.py`
  first, then generator/checker/loader.
- `apply_api_live_api_deferred` after residual honesty. 184
  implemented rows `kind=live_api_deferred`. All 305 have
  `live_api=out_of_scope_by_user`. `live_tested` stays false.
- `coverage_is_complete` / `require_complete_errors` no longer
  demand API `live_tested=true`. They reject API
  `live_tested=true` and missing `live_api`.
- Regenerated `coverage/api_v2_manifest.yaml`. `status.json`
  `complete` stays false. Docs lock unchanged.

Deviations: `api.files.create` empty `tool_name` kept as the
raw-binary alias. Test allows that one empty tool.

Review findings: none required. Residual only:
`_is_green` does not read `live_api`; completeness still does.
Rejected as a COMMIT blocker.

Tests: lint.sh pass. `BILLY_TEST_MODE=commit` test.sh 2128
passed, 83 deselected. `ui-full` / `full` not run because
completion requirements are not met.

Cleanup: no Billy records, no frames.

Next slice: `89DEED22` official lock reconcile, then 121
offline blockers. Do not finish.

