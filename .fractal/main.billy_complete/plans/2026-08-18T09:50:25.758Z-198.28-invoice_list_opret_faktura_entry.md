# 198.28 Invoice list Opret faktura entry

## Goal

Record whether one official invoice-index **Opret faktura** click
lands with a prebound tagged customer or an already-open existing
option. Do not `goto /invoices/new`.

`FD39FFE7` is binding. Invoice draft CUD stays mandatory and red.
The `2498696` scoped dump is delivered and closed:
`unique_action=none`, `proved_bind=none`, seven non-interactive
pickerfield rows. `8EBC19D5` is applied. Do not remake that dump.
Do not remake the page-wide draft-save dump. Do not remake the
picker set.

Official guides (cited in `tmp/grok-research.md`) start from the
invoice **index** named **Opret faktura** button. Current
`_create_draft` skips that and goes to `/invoices/new`. This
slice records the Ember list-to-create landing only.

TDD first. Through the real FastMCP boundary, create one tagged
customer, open the invoice list, click exact **Opret faktura**
once if unique, then persist **only** the research section 5 keys:

- `list_path_class` (`invoices` / `invoices_empty` / `other`)
- `exact_list_opret_faktura_count` (int)
- `list_opret_faktura_role` (`button` / `link` / `none`)
- `clicked_list_opret_faktura` (bool)
- `destination_path_class` (`invoices_new` /
  `invoices_new_with_query` / `other` / `none`)
- `destination_query_token_class` (`contact` / `none` / `other`)
- `destination_heading_token` (`opret_faktura` / `other` / `none`)
- `contact_input_present` (bool)
- `contact_input_value_len` (int)
- `contact_input_matches_tag` (bool)
- `option_role_count` (int)
- `aria_expanded_token` (`true` / `false` / `missing`)
- `unique_action` (`existing_option` / `none`)
- `proved_bind` (`list_cta_existing_option` /
  `list_cta_prebound` / `none`)
- `missing_keys`

`proved_bind` is `list_cta_prebound` only when the contact field
already matches the tagged customer. `proved_bind` is
`list_cta_existing_option` only when `unique_action=existing_option`
after that click and no type. Otherwise `UI_CHANGED`.

Do not type into `input[name=contact]`. Do not click the
`pickerfield`. Do not click **Gem som kladde**. Do not click
**Opret ny**. Do not click **Godkend**.

Authority: `96908DC6`, `FD39FFE7`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Closed: `2498696` scoped dump,
`3b025f4` page-wide draft-save dump, `e49c8f3` **Vælg kunde**
rest dump, `C0721A14`, structure compare, `28C8FBC8`,
`31B0C7A6`, fiber, `E87B6AEF`, `07600147`, `452E0773`,
`51E18E60`, `A3AB03C3`, `9F777B8F`, `8EFD0EAD`, `4A5CD1E7`,
`F12B607E`, `D326FFB3`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview until CUD is live-proved
  and independent review accepts
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status` in the dump-only half
- Writing `author=independent_review` / `accept` / `purge_verified`
- Remaking the scoped dump or overwriting
  `inspect-live-invoices-draft-save-scoped.json`
- Remaking the page-wide draft-save dump, **Vælg kunde** rest
  dump, or closed picker, Ember, fiber, listener, descendant,
  post-click, wrapper-center, overlay, typed, route, structure,
  or customer-detail **Opret faktura** dumps
- Treating Fakturering **Kunder** as a new path (same
  `/:org_slug/clients` shell)
- Page-wide counts of **Opret ny**
- Generic portal sweeps
- Typing into `input[name=contact]`
- Clicking the `pickerfield` wrapper or the overlay at `dx=242`
- Clicking **Gem som kladde**, **Opret ny**, **Godkend**,
  **Godkend og send**, **Send**, or **Duplikér**
- Copying the bills Leverandør wrapper onto this field
- Guessed URLs, guessed selectors, force clicks, `evaluate`
  clicks, blind keys
- Calling CDP, fetching a script URL, or issuing a `/v2/` URL
- Persisting source, locators, URLs, slugs, names, or screenshots
- Product create, ledger, files, org, quotes convert, Duplikér
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `2498696`. Memory edits from SYNC are uncommitted. Not a
backstop. `complete=false`. Honesty 16 stay red. Contacts, bills,
and organizations CUD rows name preview tools. Invoice CUD rows
still name `ui_invoices_*_open`. Offline invoice preview/execute
helpers exist. `_create_draft` still `goto /invoices/new` then
`bind_kunde`. That bind is unbound.

Scoped dump is delivered and closed. Official index CTA landing
has not been recorded.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. `728BD2E4` child-split applies
after invoice CUD is bound, not to this inspect. Unlimited budget
is not a reason to spawn. Parked write-family children stay parked.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/invoices.py` (ticket pair; no remap yet)
- `src/billy_mcp/browser.py` `ui_invoices_list`
- `src/billy_mcp/ui_writes/page_flow.py`
- `src/billy_mcp/ui_writes/invoices_customer_detail_prebind.py`
  (role/count and path-class helpers only; do not remake that dump)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_invoices_list_shell.md`

New files:

- `src/billy_mcp/ui_writes/invoices_list_opret_faktura.py`
- `tests/unit/test_ui_invoices_list_opret_faktura.py`
- `tests/live/test_ui_invoices_list_opret_faktura.py`

Do not append this inspect to
`tests/live/test_ui_invoices_writes.py`,
`tests/live/test_ui_invoices_draft_save_scoped.py`, or
`tests/live/test_ui_invoices_list.py`.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-invoices-list-opret-faktura.json`

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing unit fixture. An empty
   dump must fail `missing_keys` with the exact research section 5
   set. Run it. Capture the failure. Then implement the helper.
2. **Honesty-16 stays red.** Do not remap invoice tools in the
   dump-only half.
3. **New dump file.** Do not write to the scoped or page-wide
   draft-save dump files.
4. **Tagged customer first.** Create `MCP-UI-INV-` + 8 hex through
   FastMCP. Confirm the name in a fresh session. Then open the
   invoice list through `ui_invoices_list`.
5. **One official list CTA.** Count exact **Opret faktura** on
   that list only (button or link). Click only when the count is
   1. Do not count sidebar **Opret ny**. Do not `goto /invoices/new`.
6. **Landing only.** After the click, record path class, query
   token class, heading token, contact field length, tag match,
   `option_role_count`, and `aria_expanded_token`. No type. No
   picker click. No **Gem som kladde**.
7. **Bind rules.** `list_cta_prebound` requires
   `contact_input_matches_tag=true`. `list_cta_existing_option`
   requires `unique_action=existing_option` with no type.
   Else `UI_CHANGED`.
8. **Query values stay out of git.** Persist token class only.
9. **Invoice must not persist.** If a draft appears, delete it
   first, then the customer. Fresh-session absence required.
10. **CUD is a second half of the same EXECUTE.** Only if
    `proved_bind` is `list_cta_prebound` or
    `list_cta_existing_option`. Smallest route from that landing.
    Do not fall back to typed `bind_kunde`.
11. **Default stop is `UI_CHANGED`.** If the landing is the same
    empty closed form, do not remake picker dumps. Report the
    owner question from research section 5. Do not freeze invoice
    CUD as done.
12. **Vision.** Live test may write `author=live_test` and
    `reviewer_verdict=pending_review` only.
13. **No remap** until independent accept + purge of a real CUD.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_LIST_OPRET_FAKTURA_KEYS` and `missing_keys` in
`invoices_list_opret_faktura.py` only after a unit fixture fails
because those names do not exist, or add the fixture first
against a local helper stub. Preferred shape:

- Fixture feeds `{}` or a flags-only stub.
- Helper returns the missing key list from research section 5.
- Current empty dump must fail with that exact set.
- `unique_action=none` must not become a proved bind.
- `destination_query_token_class=contact` alone is not a bind.

Do not edit `invoices_form.py` or remap coverage until that
fixture fails and, later, bind is proved.

Keep the closed scoped / picker / Ember / fiber / listener /
descendant / draft-save-validation / customer-detail fixtures.

### 2. Smallest helper, then one live list-CTA inspect

- Implement the helper in `invoices_list_opret_faktura.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs,
  query values, or customer text.
- Derive `unique_action` and `proved_bind` from the Goal rules.
  If bind fails, set `code=UI_CHANGED`.
- Isolate unit dumps so they cannot overwrite owner files.
- Live path through `create_server` `call_tool`:
  1. Login. Create tagged customer through FastMCP. Confirm in a
     fresh session.
  2. Open `ui_invoices_list`. Set `list_path_class`.
  3. Count exact **Opret faktura** button/link. Set role and count.
  4. Click only when count is 1. Set `clicked_list_opret_faktura`.
  5. Record destination path class, query token class, heading.
  6. Record contact field presence, value length, tag match,
     `option_role_count`, and `aria_expanded_token`.
  7. Set `unique_action` and `proved_bind`.
  8. Do not type. Do not click pickerfield. Do not **Gem som
     kladde**. Do not **Godkend**.
  9. Write allowlisted keys to
     `~/.local/share/billy-mcp/inspect-live-invoices-list-opret-faktura.json`.
  10. Delete the tagged customer through FastMCP and prove
      absence. If a draft invoice appeared, delete it first.
- After the dump is delivered, later `ui-full` verifies the file
  and returns unless CUD half is in the same run.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the gate into memory `state.md` / `todo.md`. No iteration
  labels.

### 3. List-CTA bind CUD only if proved

If `proved_bind` is `list_cta_prebound` or
`list_cta_existing_option`:

1. Add a failing product fixture that the current `_create_draft`
   path does not satisfy (it still `goto /invoices/new`).
2. Implement the smallest route from the proved landing.
3. One disposable invoice draft CUD through FastMCP. Independent
   fresh-session read-back after create, update, and delete.
   Reverse cleanup: invoice then customer.
4. Do not remap `tool_name` until independent review accepts and
   purge is verified. Stay red in this run even if CUD persist
   works.
5. Never **Godkend**, **Send**, email, book, or pay.

If the list CTA does not bind, skip this half.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus the honesty-16
gate. Do not run live API tests. Do not claim `complete: true`.

## Done when

- Honesty-16 still length 16 and red
- Failing list-CTA fixture existed, then the helper lists
  `missing_keys` on an empty dump
- Live dump written through FastMCP list-open plus one official
  **Opret faktura** click
- Keys match research section 5. No URL, source, query value, or
  customer text in the dump or git
- Either `proved_bind` is `list_cta_prebound` or
  `list_cta_existing_option` and the CUD half ran, or
  `UI_CHANGED`, no leftover customer or invoice, list-CTA
  evidence recorded, invoice CUD still red and not frozen as done
- No remake of the scoped dump, the page-wide draft-save dump,
  or the closed picker set
- Invoice CUD rows still name `ui_invoices_*_open` unless the
  CUD half completed and independent review already accepted
  (not expected in this pass)
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice fits. No spawn.
Research section 5 is the contract. Stay red.

## Post-Mortem

Completed: TDD empty-dump fixture, list-CTA helper, live FastMCP
inspect. Official empty-list **Opret faktura** button clicked
once. Destination `invoices_new` with no query. Contact empty.
`option_role_count=0`. `proved_bind=none`. `UI_CHANGED`. Honesty
16 still red. Invoice rows still name `ui_invoices_*_open`.
`complete=false`. Tagged customer deleted. Absence proved.

Deviations: no invoice CUD half. Bind unbound. `ui_invoices_list`
returned `UI_CHANGED` on `/invoices/empty`. Live test stayed on
that path class and did not rewrite the list tool.

Review: FAIL on bind, not on method. Dump keys met. No remap. No
picker remake. Owner question posted. Do not freeze as done.

Tests: honesty-16 2 passed. List-CTA unit tests 8 passed. Live
narrow dump delivered. Commit-mode `test.sh` 2037 passed, 80
deselected. Lint and type checks run in FIX-VERIFY.

Cleanup: tagged customer deleted through FastMCP. Fresh-session
absence proved. No leftover invoice. Dump has no URL, name, or
ticket.

Next unresolved coverage slice: invoice draft CUD still red
under `FD39FFE7`. Official unused list entry is now closed.
Owner must name a different bind or keep the row red. Product
create waits. Do not remake this dump or the closed picker set.

