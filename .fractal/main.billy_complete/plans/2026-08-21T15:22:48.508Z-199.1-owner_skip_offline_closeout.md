# 199.1 owner skip offline closeout

## Pivot

Parent `21A3D94F` is binding. HEAD `72aebd2` is the docs-only bulk
skip, not a `(failed on <step>)` backstop. Uncommitted files are
node memory only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Live official
page GET 2026-08-21 15:18:21Z: ETag `"usuwcuphx03ct7"`, MD5
`e1f8e5d5081645709ba736265ab9a098`, 156776 bytes. Frozen lock
stays `pi4s9u10j037qn` / `d805f3d2bb8e339f7635d6834b4011bd`. Do
not relock. Do not re-GET https://www.billy.dk/api/. Do not GET
`/v2/*`.

This iteration encodes owner skips in generator, checker, tests,
wiki, and regenerated coverage. No new FastMCP tools. No bulk
payloads. No live UI. No live API. Fractal owns offline
qualification only. Do not claim fresh production MCP
qualification. Do not run `BILLY_TEST_MODE=ui-full`.

## Goal

`coverage/status.json` `complete: true` under owner scope, with
the residual API set still visible, toolless, and not green.

| Row set | Count | After this slice |
| --- | ---: | --- |
| API bulk mentions | 92 | `source_kind=ambiguous_bulk`, `kind=out_of_scope_by_user`, `scope_code=BULK_CONTRACT_UNDOCUMENTED_OWNER_SKIP`, `supersedes_blocker_code=BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` |
| API readonly-map create/update | 6 | `source_kind=clear`, `kind=out_of_scope_by_user`, `scope_code=READONLY_PROPERTY_TABLE_OWNER_SKIP`, `supersedes_blocker_code=READONLY_PROPERTY_TABLE` |
| UI owner-scope | 6 | unchanged (`FE6FA4B1` / `DC3B8E96`) |

Six readonly-map ids stay:

- `api.contactBalancePostings.create`
- `api.contactBalancePostings.update`
- `api.postings.create`
- `api.postings.update`
- `api.transactions.create`
- `api.transactions.update`

Keep `tool_name=""`. Keep `implemented`, `contract_tested`, and
`live_tested` false. Keep `tools_allowed=false` and
`live_api=out_of_scope_by_user`. Do not invent
`{posting|transaction: {organization}}`. Do not treat research136
`PUT /bulk` or `ids[]` as a contract.

Generated snapshot after regenerate:

- `complete: true`
- implemented **546**, contract **551**, live/vision **339**
- API total still **305** (92 bulk still counted in
  `api_ambiguous_bulk`)
- applicable API **207** (`305-92-6`)
- blocker `No manifest qualification blockers remain.`
- docs lock unchanged

`--require-complete` must pass after the same owner-skip rule
is applied to API bulk, API readonly-map, and existing UI owner
skips.

## Why no child

`21A3D94F`: do not spawn unless genuinely necessary. Generator,
checker, inventory tests, and regenerated yaml/json all share
`scripts/generate_coverage_report.py`. A child would fork that
file. Seven write children stay completed and merged. Retired
descendants stay unmerged.

## TDD

Failing gate first. Do not regenerate coverage until the new
tests exist and fail on current `72aebd2` artifacts.

1. Rewrite generated-status tests that pin `complete is False`
   and `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` as the current
   blocker so they expect owner-skip + `complete is True`.
2. Rewrite `test_bulk_rows_remain_ambiguous_and_toolless` to
   keep 92 toolless `ambiguous_bulk` rows, but require
   `qualification.kind=out_of_scope_by_user` and
   `scope_code=BULK_CONTRACT_UNDOCUMENTED_OWNER_SKIP`.
3. Rewrite residual honesty tests so the six rows stay
   toolless with `kind=out_of_scope_by_user` and
   `scope_code=READONLY_PROPERTY_TABLE_OWNER_SKIP`. Census of
   unimplemented API ids stays bulk92 plus those six.
4. Fix `require_complete_errors` toy coverage: an
   `ambiguous_bulk` row that is owner-scoped must not fail the
   gate. A non-scoped `ambiguous_bulk` row still fails.
5. Run those tests. They must fail before generator edits.

## EXECUTE

No tools. No payload models. No docs relock.

1. Replace `bulk_external_contract_qualification()` use on bulk
   rows with owner-skip qualification. Keep `source_kind` and
   `contract_status` `ambiguous_bulk`. Keep empty `tool_name`
   and empty `response_fields`. Keep historical research136/137
   evidence as history. Cite `radio:21A3D94F`.
2. After `apply_residual_clear_honesty`, overlay owner-skip on
   `RESIDUAL_READONLY_MAP_IDS` the same way. Do not move those
   ids to `source_kind=ambiguous_bulk`.
3. `is_owner_out_of_scope` stays `qualification.kind ==
   out_of_scope_by_user`.
4. `coverage_is_complete`: ignore owner-scoped API rows in the
   `ambiguous_bulk` wall and in `api_row_is_qualified`.
5. `qualification_blocker`: if only owner-scoped residuals
   remain, return `No manifest qualification blockers remain.`
6. `require_complete_errors` in `scripts/check_coverage.py`:
   skip owner-scoped rows for the ambiguous-bulk gate, the
   incomplete-API gate, and the incomplete-UI gate. Keep the
   every-API-row `live_tested is False` and
   `live_api=out_of_scope_by_user` checks, including skipped
   rows.
7. Add generated `source_counts` (or qualification counts)
   `api_owner_skipped_bulk=92`,
   `api_owner_skipped_readonly_map=6`. Do not drop rows from
   `api_total`.
8. `uv run python scripts/generate_coverage_report.py`. Never
   hand-edit `coverage/status.json`.
9. Wiki: keep `wiki/undocumented_bulk_owner_skip.md`; add a
   readonly-map owner-skip page; update
   `wiki/residual_clear_method_closed_inventory_honesty.md`
   completeness walls so they no longer call bulk92 a
   completion blocker. `wiki update` then `wiki lint`.
10. Offline verify:
    - `BILLY_TEST_MODE=commit bash .fractal/main.billy_complete/scripts/test.sh`
    - `uv run python scripts/check_coverage.py --require-complete`
    - `uv run python scripts/check_repository_policy.py --release`
    If commit-mode is too wide for the step, at least the
    coverage tests listed in TDD plus lint/type via
    `.fractal/main.billy_complete/scripts/lint.sh`.
11. Do not finish in EXECUTE. Independent review is the next
    step. Do not unsave `21A3D94F` until finish.

## Files

- `scripts/generate_coverage_report.py`
- `scripts/check_coverage.py`
- generated `coverage/status.json`, `coverage/report.md`,
  `coverage/api_v2_manifest.yaml` (generator only)
- `tests/coverage/test_coverage_inventory.py`
- `tests/coverage/test_ui_residual_write_owner_scope.py`
- `tests/coverage/test_api_live_qualification_semantics.py`
- `tests/coverage/test_official_docs_lock.py`
- `tests/coverage/test_ui_cud_proved_honesty_promote.py`
- `tests/unit/test_coverage_server.py`
- `wiki/undocumented_bulk_owner_skip.md`
- `wiki/residual_clear_method_closed_inventory_honesty.md`
- new wiki page for `READONLY_PROPERTY_TABLE_OWNER_SKIP`

Do not edit `NODE.md`. Do not touch `src/billy_mcp/api/*`
write modules. Do not touch UI write actors.

## Stop rules

- Fail closed if a test wants a bulk or readonly-map tool name.
- Fail closed if `live_tested` becomes true on any API row.
- Fail closed if docs ETag/MD5 change.
- Fail closed if `ui-full` or a headed browser starts.
- Fail closed if `complete: true` appears without the 92+6 rows
  still present and toolless.

## Post-Mortem

Work completed:

- Encoded 92 bulk mentions as `out_of_scope_by_user` /
  `BULK_CONTRACT_UNDOCUMENTED_OWNER_SKIP`.
- Encoded six readonly-map create/update rows as
  `READONLY_PROPERTY_TABLE_OWNER_SKIP`.
- Completeness and `--require-complete` exclude owner-scoped API
  and UI rows. Regenerated coverage: `complete: true`, implemented
  546, contract 551, live/vision 339. Docs lock unchanged.
- Independent review PASS. Required fixes none.
- Wiki: `undocumented_bulk_owner_skip`,
  `readonly_property_table_owner_skip`, residual honesty walls,
  and `offline_write_probe_rules` bulk decision updated.

Deviations:

- Did not relock official docs to live ETag `"usuwcuphx03ct7"`.
  Residual tables still match the skip (`88C4B0A9`, IR).
- Did not run `BILLY_TEST_MODE=ui-full` or `full`. Owner
  `21A3D94F`: Fractal owns offline qualification. Fresh production
  MCP qualification is owner-session after merge.
- Bare `BILLY_TEST_MODE=commit` `test.sh` is interrupted inside
  `tests/unit/test_browser.py` (asyncio KeyboardInterrupt, 0
  failures while progressing). Verified instead with
  `pytest -m "not live and not vision" --ignore=tests/unit/test_browser.py`.

Review findings: PASS, required fixes none. Rejected as required
fixes: docs fingerprint drift, NODE.md `ui-full` gate,
`test_browser` hang. Evidence in
`.fractal/main.billy_complete/tmp/grok-review.md`.

Test results:

- `scripts/lint.sh` pass
- `uv run python scripts/check_coverage.py --require-complete` pass
- `uv run python scripts/check_repository_policy.py --release` pass
- coverage tests 122 passed
- 2159 passed, 83 deselected, ignoring `test_browser.py`

Cleanup: no live records. No frames. No credentials in the diff.

Next: COMMIT this encode. Then `fractal node finish` under
`21A3D94F` offline closeout. Do not claim fresh production MCP
qualification. Do not unsave `21A3D94F` until finish. Owner
session after merge runs live interface qualification.
`ui-full` is not a Fractal closeout gate for this resume.
