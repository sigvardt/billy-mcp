# 198.61 countryGroups ticketed create and update

## Pivot

`AF8E5A1F` is binding under `9B979A05`. HEAD `9a3a677` is a clean
198.60 cities close, not a `(failed on <step>)` backstop. Product
tree matches origin. Uncommitted files are node memory only
(`state.md`, `todo.md`, `decisions.md`). Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-af8e5a1f.html`,
`billy-api-docs-chunk-research-af8e5a1f.js`,
`research-af8e5a1f-countrygroups-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the cities lock (MD5 `7902955e4d1a56ecea2d85551f17b49c`).
Anchor `#v2countrygroups`. Supports: `get by id, list, create,
update, bulk save, bulk delete`. No singular delete. Property
table is exactly three rows with empty notes: `name` string,
`icon` string, `memberCountryIds` string.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.countryGroups.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_country_groups_*` or bulk tools. Do not fold this into
`geo_reads.py`. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.countryGroups.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.countryGroups.create` | `api_country_groups_create_preview` / `_execute` | `POST /countryGroups` | nested optional `{name, icon, memberCountryIds}` |
| `api.countryGroups.update` | `api_country_groups_update_preview` / `_execute` | `PUT /countryGroups/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 16 method-closed + 2 readonly
map + 2 meta-delete = 20. Bulk 92 stay red. Implemented 532,
contract 537 (HEAD 530 / 535). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload copy shape is
`src/billy_mcp/api/city_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_country_group_writes.py`, copied from
   cities for the typed optional nested payload:
   - Register count is 4. Preview fields:
     `api_country_groups_create_preview` `{countryGroup}`,
     `api_country_groups_update_preview` `{id, countryGroup}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{name, icon, memberCountryIds}`.
     `additionalProperties` is false on outer and nested schemas.
     No `input` wrapper. Nested `required` is empty or absent.
   - Outer extras, empty update `id`, `memberCountries`, a JSON
     array for `memberCountryIds`, nested objects, and other inner
     extras fail `ValidationError` / FastMCP `extra_forbidden`.
     Execute extras fail. Preview still makes no HTTP.
   - Empty nested object `{"countryGroup": {}}` is valid. Partial
     payloads such as `{"name": "X"}` are valid. Do not require
     `memberCountryIds`. Official type is string, not array.
   - Preview create sends no HTTP. Canonical request is
     `{"countryGroup": { ...set keys only... }}`. Expected effect
     `{action: create, resource: countryGroup}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_country_group_write_tickets.py`, copied
   from cities ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/countryGroups` with the
     exact nested JSON. Execute PUTs
     `/v2/countryGroups/<urlencoded id>`. Encoded id `group /?`
     becomes `/v2/countryGroups/group%20%2F%3F`.
   - Success fixture maps only declared changed root
     `countryGroups` plus optional `meta.deletedRecords`. Do not
     expose a top-level `countryGroup` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 16`, residual `== 20`. Assert
   `api.countryGroups.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk countryGroups rows
   unspecified. Bump every hardcoded `implemented_rows == 530` /
   `contract_tested_rows == 535` in that file to **532 / 537**
   (seven pairs). `complete` stays false. Live/vision counts stay
   339.
4. Map both ids in `OPERATION_TEST_REFERENCES` to
   `tests/api/test_country_group_writes.py`,
   `tests/api/test_country_group_write_tickets.py`, plus
   `SERVER_REGISTRY_TEST_REFERENCE`. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_country_groups_writes_are_ticketed_offline_and_not_live_tested`
   next to the cities inventory tests.

Run:
`uv run pytest tests/api/test_country_group_writes.py tests/api/test_country_group_write_tickets.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/country_group_writes.py` (new), copy
`city_writes.py`:

- `CountryGroupPayload`: `name: str | None = None`,
  `icon: str | None = None`,
  `memberCountryIds: str | None = None`, `extra=forbid`, frozen.
- Create preview input: `{countryGroup}`.
- Update preview input: `{id min_length=1, countryGroup}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/countryGroups`. Singular root `countryGroup`.
  Plural root `countryGroups`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_country_group_write_tools` next to
`register_city_write_tools`. Do not change geo reads.

`scripts/generate_coverage_report.py`:

- Remove `api.countryGroups.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids.
- Do not touch bulk helpers, the other 16 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` countryGroups create/update true,
  delete false (`("countryGroups", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 16. These two rows are ticketed
  offline tools. Residual honesty remaining 20. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional `name`, `icon`, and
  `memberCountryIds` is enough to ship the ticketed POST/PUT.
  Official type of `memberCountryIds` is string, not array.
  Unauth 405 is not the contract. Empty payload is allowed.

Keep `wiki/ui_geo_cities_not_applicable.md` as history. Do not
remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.countryGroups.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.countryGroups.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research138).
- Do not accept `memberCountries` or a JSON array for
  `memberCountryIds`.
- Do not require any nested field.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.

## Independent review

After EXECUTE, review must re-parse `#v2countrygroups` on the
locked HTML and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still three optional string fields: `name`,
   `icon`, `memberCountryIds`.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /countryGroups` and
   `PUT /countryGroups/:id`.
6. Nested payload rejects `memberCountries`, arrays, nested
   objects, and extra keys. Empty nested object is valid.
7. `live_tested` is false.
8. Remaining residual honesty is 20 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.

## Post-Mortem

Work completed: ticketed offline `api.countryGroups.create` and
`.update` in `src/billy_mcp/api/country_group_writes.py`. Preview
tools take a nested payload of optional string `name`, `icon`,
and `memberCountryIds` with `extra=forbid`. Official type of
`memberCountryIds` is string. Arrays, `memberCountries`, nested
objects, and extra keys fail at the FastMCP boundary. Empty
nested object is valid. Execute binds `POST /countryGroups` and
`PUT /countryGroups/:id` through the shared write protocol.
Coverage regenerated: implemented 532, contract 537, live/vision
339, complete false. Residual honesty remaining 20.

Deviations: none from the official field table. Ticket and
execute contract tests live in
`tests/api/test_country_group_write_tickets.py` so each test file
stays under the 250-line ceiling. Evidence map names both test
files plus the server registry test.

Review findings: independent review PASS. Required fixes none.
Nits rejected: execute descriptions saying "ticket" is not a
contract field; `live_probe.py` may still list these ids in the
historical unauth 405 matrix, and that is not the contract.
Inventory qualification is `live_api_deferred`. Do not start
`4DE5EE18` countries create/update in this step.

Test results: focused country-group write, inventory, lock, and
registry tests passed. Default `test.sh` (commit mode, not live,
not vision) after the two count-assert bumps: 2233 passed, 83
deselected. `lint.sh` passed. Wiki lint clean on project wiki and
memory. `BILLY_TEST_MODE=full` was not run because completion
requirements are still false.

Cleanup state: no live records. No frames. No credentials.

Next unresolved coverage slice: ticketed `countries` create and
update (`4DE5EE18`), then remaining geo writes, readonly
transaction writes, and meta deletes. Residual honesty 20.
Bulk 92. `complete` stays false. Do not finish.

