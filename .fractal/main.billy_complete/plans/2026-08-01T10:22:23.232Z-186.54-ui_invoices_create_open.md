# 186.54 UI invoices create open shell + dual-count create


## Goal

Product research153 ACCEPT:

1. New read-only FastMCP UI tool **`ui_invoices_create_open`** that opens the
   Billy invoice **create form** (`Opret faktura` / `/:org_slug/invoices/new`)
   as form/shell open only.
2. Dual-count **exactly** `ui.parity.invoices.create` (maps
   `api.invoices.create` / `POST /v2/invoices`) onto that tool with five UI
   flags true + vision.
3. Green discovery row **`ui.discovery.invoices_create`** (new
   `UI_DISCOVERY_FAMILIES` entry; do **not** repurpose existing
   `ui.discovery.invoices` list shell).

Target math (from tip `4929360`): live/vision **145 → 147** (discovery +
parity); GEO NA **100** unchanged; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research153)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. |
| Soft paths | `emails` / `invoiceEmails` / nonsense soft-empty body **127** dual. |
| Create form dual | Soft `/invoices/new` body **580**, h1 **Opret faktura**, soft_empty **false** dual. List CTA **Opret faktura** opens same form (primary create trail). |
| Form chrome class | Godkend og send, Vis preview, **Gem som kladde**, Mere, Evt. besked til kunde, Fakturanr., Dato, Betalingsfrist, Produkt/Beskrivelse/Antal/Enhedspris, Tilføj linje, Vedhæft PDF. |
| Tool gap | `ui_invoices_list` is list-only (h1 Fakturaer, CTA visible, never entered). Dual-count create onto list is resource-identity theft. |
| Isolation | Create form ≠ special `POST /invoices/:id/emails` (no Emne/contactPerson/copyToUser dual). ≠ Levering settings (settings/invoicing body **1494**). |
| Deferred | special.invoice_email (empty list + failed disposable draft); files_upload pure NA rejected; postings steal rejected; get/update/delete/bulk stay red. |

Brief: `tmp/grok-research.md`. Dual: `tmp/research153_residual_dual.json`.

## Non-goals

- Greening `special.invoice_email`, `special.files_upload`, `postings.*`,
  productPrices, tax*, bank*, invoices.get/update/delete/bulk_*
- Dual-counting create onto `ui_invoices_list`
- Clicking Godkend / Godkend og send / Send / Gem som kladde / Vis preview
  submit / Tilføj linje / Vedhæft / Slet / Mere actions that mutate
- Saving or sending any invoice; creating disposable records in product path
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery + dual-count create) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research153)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_invoices_create_open` |
| lane | ui |
| kind | form_open_only / shell_open_only (read-only observe) |
| shell_kind | `invoices_create` (or generator-consistent string) |
| success model | `UiInvoicesCreateOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools (API create already offline-green) |

### Success model fields (non-PII)

Mirror list-shell strictness (`extra=forbid`, no org_slug/url/selector secrets):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/invoices/new` (scrub SPA slug; accept class equivalent if Billy redirects under invoices/*new* — fail closed if list path only) |
| `heading` | Literal `Opret faktura` |
| `shell_kind` | Literal `invoices_create` |
| `draft_save_chrome_visible` | bool — **Gem som kladde** present (must not click) |
| `line_chrome_visible` | bool — Produkt/Beskrivelse **or** Tilføj linje class present |
| `shell_markers_present` | bool — aggregate signature true when form is the create shell |

Optional extra marker bool only if tests need it (e.g. `approve_and_send_chrome_visible` for isolation docs); do not require special-email fields.

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Preferred path: open `https://mit.billy.dk/{slug}/invoices/new` directly
   (research153 soft route dual-proved real form).
4. Fallback: open `/{slug}/invoices` then observe-only click **Opret faktura**
   (exact CTA dual-proved; never click if already on create form).
5. Success when:
   - path class is create form (`/:org_slug/invoices/new` or scrubbed SPA
     equivalent under invoices create — **not** list `/:org_slug/invoices`
     alone)
   - h1 **Opret faktura**
   - form markers dual-stable class: **Gem som kladde** + line chrome
     (Produkt/Beskrivelse or Tilføj linje)
6. Never click Godkend / Godkend og send / Send / Gem som kladde / Vis preview /
   Tilføj linje / Vedhæft / Mere menus / Slet / contact pickers / product
   pickers.

### Reject as success

| Failure | Code class |
| --- | --- |
| Login page | AUTH_REQUIRED |
| CAPTCHA / passkey / push | AUTH_INTERACTION_REQUIRED |
| Still on list shell only (h1 Fakturaer, no create form) | UI_CHANGED / shell signature miss |
| Egress deny | EGRESS_DENIED |
| Soft empty SPA chrome (body ≤132, no h1) | not a success path |

### CTA hard ban (tests must prove no click)

Never interact with: Godkend, Godkend og send, Send, Send e-mail, Gem, Gem som
kladde, Vis preview, Tilføj linje, Vedhæft, Slet, Mere action items that submit,
customer/product autocomplete selection, bulk actions.

Product path is **open and classify only**.

## Coverage generator contract

Mirror `apply_ui_invoices_list_shell_evidence` +
`apply_ui_settings_user_organizations_open_shell_evidence` patterns:

1. **Constants**: tool name, model/unit/live test reference paths, vision record
   class path `tmp/vision-records/ui_invoices_create_open.json`.
2. **Discovery seed**: add `"invoices_create"` to `UI_DISCOVERY_FAMILIES`; in
   `build_ui_manifest` discovery loop call
   `apply_ui_invoices_create_open_shell_evidence(row)` when
   `family == "invoices_create"`.
3. **Dual-count** exact API id only:
   - When `api_row["id"] == "api.invoices.create"`, call apply with
     `parity_of_api_create=True` (or equivalent) for
     `ui.parity.invoices.create`.
4. Set five UI flags true; tool_name; parity_status=`form_open_only` or
   `shell_open_only` (pick one and use consistently; prefer
   `form_open_only` if inventory tests allow free string, else
   `shell_open_only`); evidence cites research153 dual + live test ref +
   vision record path class.
5. **Must not** green: `api.invoices.list` (stays list tool),
   get/update/delete/bulk_*, special.invoice_email, files_upload, Levering
   settings rows.
6. Existing `ui.discovery.invoices` / `ui.parity.invoices.list` remain on
   `ui_invoices_list` unchanged.
7. After regenerate: live/vision **147**; implemented/contract **+2** (or
   generator actual); complete **false**; GEO NA **100**; API live_tested all
   false with `out_of_scope_by_user`.
8. Inventory tests: discovery green; create parity green; residual invoice ops
   + special.invoice_email red; live/vision math 145→147.

## EXECUTE steps (root)

1. **Models** (`src/billy_mcp/models.py` + `tests/test_models.py`):
   `UiInvoicesCreateOpenInput` (empty forbid) +
   `UiInvoicesCreateOpenSuccess` fields above; schema forbids secrets.
2. **Browser** (`src/billy_mcp/browser.py`): Protocol +
   `ui_invoices_create_open` implementation; signature helpers for create form;
   unit tests in `tests/unit/test_browser.py` (mock page where pattern exists);
   ensure list tool still never navigates into create as success.
3. **Server** (`src/billy_mcp/server.py` + registry tests): register tool with
   typed I/O; no generic browser exposure.
4. **Live test** `tests/live/test_ui_invoices_create_open.py`:
   dual ephemeral profiles; DOM assert success; second-session independent
   read-back; vision record + purge outside repo; skip if credential refs
   missing; never API token; never submit CTAs.
5. **Coverage generator** + inventory tests asserting discovery green, create
   dual-count green, residuals red, live/vision math 145→147.
6. **Regenerate** coverage manifests (`scripts/generate_coverage_report.py` or
   project regen entry).
7. **Wiki** page `ui_invoices_create_open_shell` + `_index` link; non-sensitive;
   cite research153 markers class only; isolation note create form ≠ special
   invoice email.
8. **Offline gate**: `scripts/lint.sh`; offline `scripts/test.sh` (no live
   required for COMMIT path if live gated by credentials — follow prior
   invoices list / settings shell commits).
9. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Registered `ui_invoices_create_open` only; typed success/error |
| Discovery | `ui.discovery.invoices_create` five flags true |
| Parity | `ui.parity.invoices.create` five flags true; tool same |
| Exact id | Only `api.invoices.create` dual-counted (not list/get/update/delete/bulk) |
| Isolation | list stays on `ui_invoices_list`; invoice_email/files_upload/postings stay red |
| CTA | No Godkend/Send/Gem/Tilføj/Vedhæft/Slet in product path |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **147**; GEO NA **100**; complete **false** |
| Offline | lint + offline test suite green |
| Secrets | No tokens/PII in repo/wiki/coverage |

## Independent review focus

- Resource identity: create form ≠ list shell ≠ special POST emails ≠ Levering settings.
- Exact create id wiring only (no bulk_*, no list steal).
- Soft empty alone is not the green path; greened tool + dual reconfirm is.
- No accidental greening of invoice_email via “Godkend og send” or “Evt. besked til kunde”.
- live/vision math: discovery +1 and parity +1 → 147; complete false.
- Never submit draft or send in product or tests.

## Expected coverage math

| Metric | Before (tip 4929360) | After product |
| --- | --- | --- |
| live/vision | 145 | **147** |
| NA rows | 100 | 100 unchanged |
| discovery families | 35 | **36** (`invoices_create`) |
| implemented/contract | 329 | ~331 |
| complete | false | false |

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| SPA path not literally `/invoices/new` | Prefer direct navigation dual-proved; scrub path class; fail closed if only list path |
| Accidental Gem som kladde click via role match | Explicit ban list; open-only — no click on save chrome |
| “Godkend og send” misread as email special | IR + inventory assert special.invoice_email stays red |
| Phone/org name in evidence | Scrub PII; store class markers only in wiki/coverage |
| Live credentials missing in CI | Live test skip pattern matching sibling invoices list tests; still ship offline contract + inventory |
| Generator over-green other invoices.* | Exact `api.invoices.create` match only |

## Done when (this product only)

- Tool + models + server + unit/model tests landed
- Live dual-session test present and green when credentials configured
- Coverage regenerated: discovery + invoices.create green; residuals red; 147 live/vision; complete false
- Wiki + inventory tests green; lint + offline test.sh green
- IR ACCEPT (or required fixes applied)
- Ready COMMIT via fractal commit — not node finish

## Ready for EXECUTE

Root-only product from research153. No spawn. Not finish.

## Post-Mortem

### Completed

- Root EXECUTE producted `ui_invoices_create_open` (models, browser, server,
  unit, live dual, coverage generator discovery `invoices_create` + exact dual-count
  `api.invoices.create`, wiki shell page + `_index`).
- IR **ACCEPT** (`tmp/grok-review.md`): no required fixes; optional N1–N2 deferred.
- FIX-VERIFY: lint.sh pass; wiki lint memory+project clean; offline test.sh
  **1578 passed** / 37 deselected; coverage live/vision **147**, implemented/contract
  **331**, complete **false**; create parity + discovery green; residual invoice
  get/update/delete/bulk + special.invoice_email stay red.
- Vision record purge_verified true (node tmp, outside repo frames).

### Deferred

- Optional N1: list→Opret faktura fallback navigation (direct `/invoices/new` only).
- Optional N2: re-measure draft/line bools after signature (hardcoded True after pass).
- special.invoice_email (empty list + failed draft earlier; Levering blocks weak NA).
- special.files_upload dual-count policy; postings nested; multi-resource tax*/bank*/productPrices.
- invoices.get/update/delete/bulk_* UI.
- Bulk 92 external-contract; annual_reports org_inaccessible.
- API live remains out_of_scope_by_user.

### Notes

- Prefer residual dual-count/NA next: special.invoice_email (need disposable draft +
  send dialog dual or stronger NA package); files_upload dual-count policy;
  nested postings. Complete false; do not node finish.

### Test results

| Gate | Result |
| --- | --- |
| lint.sh | pass |
| wiki lint (memory + project) | clean after memory PREPARE bullet rejoin |
| offline test.sh | 1578 passed, 37 deselected |
| live create open | 1 passed (EXECUTE) |
| coverage | live/vision 147; contract/impl 331; complete false |

### Cleanup

- No disposable invoice left (product open-only; research153 create attempt failed).
- Live profiles ephemeral; vision frames purged (purge_verified true).

