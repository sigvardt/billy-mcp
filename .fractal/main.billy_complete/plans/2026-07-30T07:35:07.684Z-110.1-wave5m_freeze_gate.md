# 110.1 Wave-5m freeze integration and gate

## Objective

Integrate the completed Wave-5m `contactBalancePayments` freeze contract,
validate it as a documentation-only, fail-closed change, and obtain an
independent freeze verdict before opening the cited four-tool implementation
slice. Preserve the separate Wave-5l product fallback review without treating
it as the still-required Grok gate.

## Contract and current state

- Grok research57 is the controlling cited product handoff in
  `.fractal/main.billy_complete/tmp/grok-research.md`: official documentation
  ETag `hsisik4g9p3603`, MD5 `c2efda0ee4cf9cf200e14910c5fc6996`, body length
  147934. It authorises the Wave-5m implementation only after a freeze ACCEPT.
- The completed contract child is
  `main.billy_complete.wave5m_contact_balance_payment_freeze_contract` at
  `7f702c0`; its authored file is
  `wiki/wave_fivem_ticketed_writes_contract.md`. Its scope is singular
  `contactBalancePayments` create and update only.
- The frozen future surface is exactly four tools: create preview/execute and
  update preview/execute; client-relative POST/PUT `/contactBalancePayments`,
  singular root `contactBalancePayment`, required response root
  `contactBalancePayments`. Singular DELETE is 405 and bulk remains empty-tool
  red.
- Root has uncommitted research57 state and probe-rule refreshes that correctly
  record Wave-5l as 246 API tools / 170 offline implemented and contract-tested
  rows. Retain them. Restore only the generated root `codex-power.err` removal.
- The re-routed Wave-5l Codex fallback review is active. It may add only its
  fallback review record; even a PASS does not close the required Grok review.

## Delegation decision

The completed contract, its independent review, and the future implementation
have disjoint ownership. A manager integration plus a clean-context freeze
review is warranted now; the product leaf is deliberately deferred until the
review result is explicit.

1. Integrate the completed Codex Power contract child on root after verifying
   its committed bytes.
2. Spawn a Grok freeze-review leaf after that root checkpoint. This is the
   required independent review and owns only its review record.
3. If Grok fails before task edits due to authentication, quota, or rate limit,
   create a separate Codex Power fallback reviewer for the full review with
   `--model=gpt-5.6-terra`; its record must say it is fallback evidence and must
   not claim the mandatory final Grok audit is satisfied.
4. Do not spawn the Wave-5m product leaf unless the reviewer records an explicit
   ACCEPT under the contract. A rejection or an unavailable reviewer leaves all
   Wave-5m implementation rows red.

## Execute

1. Validate `7f702c0` from a clean archive and a three-dot diff. Accept only
   `wiki/wave_fivem_ticketed_writes_contract.md` and its generated root-index
   row as deliverable bytes. Reject all child `.fractal/` seed, plan, memory,
   runtime-log, skill, and script artifacts from the root merge.
2. Restore the generated root error-log deletion, retain the research57 state
   and `offline_write_probe_rules.md` changes, then merge the contract child
   with `--no-ff --no-commit`. Remove its complete child seed from the merge
   index and working tree before committing the labelled integration. Run
   `wiki update` and `wiki lint` after resolving the index, then execute the
   coverage generator/checker, focused contract/coverage tests, formatter,
   Ruff, Pyright, policy scan, and `BILLY_TEST_MODE=commit` suite. Do not alter
   coverage green state during this documentation-only integration.
3. Configure and launch the independent review leaf from the committed root
   checkpoint:

   ```bash
   fractal node init wave5m_freeze_review_grok \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --base=main.billy_complete --agent=grok \
     --max-cost=320 --max-iter-cost=80 --max-iters=10 --step-timeout=30m \
     --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Its only project deliverable is
   `wiki/wave_fivem_freeze_independent_review.md` plus its index row. It must
   cite the official documentation and research57, inspect the root contract,
   run the non-live checks, give explicit ACCEPT/FAIL, and preserve red
   live/UI/vision/bulk/delete/completeness status. It may not edit source,
   tests, coverage state, or the contract page.
4. If the Grok launch fails before review edits for one of the routing-policy
   reasons, configure a `wave5m_freeze_review_codex_fallback` leaf with the
   same byte scope and an explicit `--agent=codex-power --model=gpt-5.6-terra`.
   Its sole record is `wiki/wave_fivem_freeze_codex_fallback_review.md`; it must
   preserve the outstanding Grok requirement and report the verified failure
   cause through radio.
5. Only after an ACCEPT, create the future product leaf with this exclusive
   ownership: `src/billy_mcp/api/contact_balance_payment_writes.py`,
   `tests/api/test_contact_balance_payment_writes.py`, the narrowly necessary
   `server.py` registration, generator-driven coverage artifacts, and the two
   inventory-row cleanup notes. It uses `--agent=codex-power
   --model=gpt-5.6-terra` and research57 as the cited contract. Required
   offline outcome is exactly four tools, 250 API tools, and 172 implemented +
   contract-tested API rows; live and vision remain zero and `complete` remains
   false.

## Fail-closed boundaries

- Never implement singular delete, bulk, webhook, UI, browser, live-test, or
  invoice-late-fee/reminder work in this slice.
- Never copy the official bankPayment sample fields into an opaque
  `contactBalancePayment` map, promise an association wire shape, or infer
  update semantics beyond the frozen contract.
- A 401 unauthenticated probe opens an offline contract only; it does not prove
  a valid payload, related response roots, or cleanup. The 405 DELETE result
  forbids a delete tool and means live cleanup remains unqualified.
- No review, freeze, or research record may make a completeness claim. The
  broad live/UI/vision/bulk requirements stay red until independently tested.

## Post-Mortem

- Completed: merged the reviewed Wave-5m offline contract and retained its
  exact four-tool, two-row boundary without changing product source or coverage
  qualifications.
- Deviation: the assigned Grok review child could not authenticate before
  editing. The routing-policy Codex fallback remains separate and explicitly
  cannot close the Grok requirement. An independent Grok review record supplied
  through the required review handoff accepted the freeze; its durable,
  non-sensitive record is `wiki/wave_fivem_freeze_independent_review.md`.
- Review: accepted the cited contract MD5
  `b42947fb2c3fbfce6f22fbc4f595c63b`; confirmed POST/PUT unauthenticated 401,
  singular DELETE 405, no delete/bulk/webhook expansion, and product source
  absence. The only finding is the stale create cleanup text, intentionally
  deferred until the product leaf has real create tests and can replace it with
  an executable cleanup path.
- Verification: local lint, strict type checks, coverage/policy checks, and
  the commit suite pass (1056 tests). Both wiki stores were regenerated and
  linted clean; the memory index reports its existing empty-content note only.
- Cleanup: no browser evidence, credentials, HAR, trace, or test data was
  created. `coverage/status.json` remains generator-owned and false.
- Next unresolved coverage slice: implement and contract-test the four
  accepted `contactBalancePayments` ticketed tools in a dedicated product leaf;
  keep live, UI/vision, delete, and bulk rows red.
