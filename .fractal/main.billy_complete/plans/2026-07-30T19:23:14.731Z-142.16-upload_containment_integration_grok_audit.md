# 142.16 Upload containment integration and Grok product audit

## Goal

Integrate the completed Wave-5s-B descriptor-containment repair, preserve the
research85 email/delivery reconfirm as research-only, and start the mandatory
independent Grok product audit. The audit outcome, not the Codex fallback,
decides whether Wave-5s-C can start.

## Adopted state and boundaries

- `wave5sb_upload_containment_fix` completed at `b621355` after a clean-archive
  focused suite. Its production changes are limited to
  `src/billy_mcp/api/file_upload_writes.py` and
  `tests/api/test_file_upload_writes.py`.
- The repair replaces the unsafe post-identity pathname read with configured
  root descriptor traversal using no-follow opens, then reads, hashes, and
  uploads one descriptor buffer. Its terminal and intermediate same-digest
  outside-root symlink tests require `FILE_NOT_ALLOWED` and zero HTTP; the
  changed-bytes race still requires `FILE_CHANGED` and zero HTTP.
- The already-written `research85` page is a cited Grok research brief only.
  It reconfirms the frozen Wave-5s-C email/delivery wire and keeps all residual
  clear rows, bulk, UI, live, vision, and completeness red.
- Do not start Wave-5s-C or alter its two inventory rows unless the repaired
  upload product receives a real Grok ACCEPT. A Codex fallback after a pre-edit
  Grok authentication failure is useful but non-authoritative.

## Delegation decision

The implementation child is complete and has disjoint ownership, so this node
must merge and verify it. A fresh, clean-baseline independent review is a
separable security-critical task and must be owned by a `grok` leaf. No second
implementation child is spawned: the only candidate, Wave-5s-C email/delivery,
is hard-gated on that review.

## Execute

1. Verify the completed child from committed bytes before integration:
   - inspect `git diff main.billy_complete...main.billy_complete.wave5sb_upload_containment_fix`;
   - extract `b621355` into a temporary directory and run its focused upload
     suite from those archived bytes; and
   - retain the current `coverage/status.json` fail-closed state (the repair
     changes containment correctness, not coverage qualification).
2. Merge the child with the required labelled no-fast-forward merge. The
   repository's generated wiki-index merge driver failed locally; use the
   temporary `merge.wiki.driver=true` override to retain the parent index, then
   regenerate it with `wiki update --path=<project wiki>` and validate with
   `wiki lint --path=<project wiki>`. Do not hand-edit generated index rows.
3. Update the shared record and private state to say containment is merged and
   that the only remaining Wave-5s-B product gate is the Grok audit. Preserve
   research85's explicit non-claims and add no coverage greening. Run the root
   focused upload suite, non-live root test suite, formatting, Ruff, Pyright,
   coverage consistency, repository-policy, and credential-safety checks.
4. Commit the integrated repair, research85 handoff, index, and state using
   `fractal commit` only after those checks pass, so the reviewer forks a clean
   root baseline.
5. Initialise and configure the independent-review leaf after the root commit:

   ```bash
   fractal node init wave5sb_upload_product_grok_repair \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --agent=grok \
     --max-depth=0 --max-children=0 --max-descendants=0 \
     --max-iters=8 --step-timeout=30m
   ```

   Give it exclusive ownership of a new non-sensitive wiki review record and
   its node files. It must independently re-check the cited official-doc
   fingerprint, the repaired descriptor acquisition path, terminal and
   intermediate symlink races, changed-byte behaviour, ticket/replay/no-retry
   boundaries, root API lock, redaction, clean-archive evidence, and research85
   non-claims. Its verdict must be explicit: ACCEPT, FAIL, or BLOCKED; only an
   evidence-backed Grok ACCEPT opens Wave-5s-C.
6. If Grok fails before edits because of authentication, quota, or rate limit,
   rerun the full static-review step with a `codex-power` leaf as the routing
   policy requires. Record that fallback as non-authoritative and retain the
   Grok product gate; never auto-switch after Grok has edited review material.

## Verification and handoff

- Expected root repair checks: the child’s focused suite, root non-live suite,
  `uv run ruff format --check .`, `uv run ruff check .`, `uv run pyright`,
  `uv run python scripts/check_coverage.py --reject-false-completeness`, and
  `uv run python scripts/check_repository_policy.py`.
- Keep all live, browser, UI, bulk, and production-data tests out of this
  iteration. No token, UI credential, raw browser evidence, or disposable Billy
  record is required for the containment repair or its independent static audit.
- On a Grok ACCEPT, merge the review record in the next PREPARE step and then
  initialise the already-frozen, explicitly `codex-power` Wave-5s-C product
  leaf. On FAIL/BLOCKED, preserve the evidence, keep the two specials red, and
  do not infer an acceptance.

## Post-Mortem

- Completed: integrated the descriptor-safe upload containment repair as
  `b54f8f9`, then verified the root non-live suite and safety/tooling checks.
- Deviation: the first dedicated Grok child could not authenticate before edits;
  the required routing fallback was started but made non-authoritative. A
  subsequent explicitly Grok-routed root independent review supplied the
  required ACCEPT directly, so no child-review merge was needed.
- Review findings: no confirmed defect remained. The review accepted the
  repaired upload offline only and retained `live_tested: false`; it also
  accepted research85 only as a Wave-5s-C handoff.
- Cleanup: no browser, credentials, live records, or coverage greening. The
  fallback child was directed to stop because the authoritative review made its
  duplicate review unnecessary.
- Next unresolved slice: start the frozen Wave-5s-C ticketed email/delivery
  product leaf; live/UI/bulk/residual rows remain red.
