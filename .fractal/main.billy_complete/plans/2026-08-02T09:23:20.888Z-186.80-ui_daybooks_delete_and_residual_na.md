# 186.80 ui daybooks delete open + residual NA package

## Goal

Product research180 ACCEPT as one root-only package (full slice, not A-only):

### A. Tool — `ui_daybooks_delete_open`

1. New typed FastMCP tool `ui_daybooks_delete_open` (`delete_chrome_open_only`).
2. Maps **only** `api.daybooks.delete` → `ui.parity.daybooks.delete`.
3. Path class **`/:org_slug/daybooks/:id`** (research180 dual SPA-seeded daybook).
4. Observe-only open **Mere** on id path; assert menu body contains **Slet**
   (menu text dual: `Eksportér som .CSV` / `.XLS` / `Importér` / `Slet`).
5. Never confirm Slet, never permanent delete, never Gem/Bogfør/line post.
6. Keep greened `ui_daybooks_open` (list+create) and `ui_daybooks_get_open`
   unchanged. Do **not** dual-count those tools onto delete.

Peer chrome: `ui_invoices_delete_open` (mere_slet text) preferred over
`ui_bills_delete_open` (confirm/Annuller), because dual shows menu text Slet
with Slet role=button count 0 after Mere.

### B. NA freeze — daybooks.update

| UI row | API op |
| --- | --- |
| `ui.parity.daybooks.update` | `api.daybooks.update` |

Evidence: research180 dual — id path Mere-only; no Gem/Opdater/Ret/name form;
soft `/edit` and `/update` empty (inputs 0). Do not dual-count get tool onto update.

### C. NA freeze — billLines non-bulk (5)

| UI row | API op |
| --- | --- |
| `ui.parity.billLines.get` | `api.billLines.get` |
| `ui.parity.billLines.list` | `api.billLines.list` |
| `ui.parity.billLines.create` | `api.billLines.create` |
| `ui.parity.billLines.update` | `api.billLines.update` |
| `ui.parity.billLines.delete` | `api.billLines.delete` |

Evidence: research180 dual — bill seed 200 with
`accountId`+`taxRateId`+`description`+`amount`; edit path lineish dual
(inputs_n 11); dedicated `/billLines` soft empty (inputs 0); embedded_only true.
Do **not** dual-count onto `ui_bills_*`. Do **not** NA bulk_*.

### D. NA freeze — users.get + users.update (2)

| UI row | API op |
| --- | --- |
| `ui.parity.users.get` | `api.users.get` |
| `ui.parity.users.update` | `api.users.update` |

Evidence: research180 dual — Brugere + Revisorer og bogholdere panel markers;
no Ret/detail form dual. Keep `ui_settings_users_open` for list only.
Do **not** NA bulk_*. SPA `GET /v2/users` 403 under session token is not live
API qualification and does not block UI NA.

### Explicit DEFER / REJECT (out of this plan)

- bulk 92 external-contract (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`)
- annual_reports org_inaccessible
- offline 29 clear API gaps (separate offline lane)
- re-product daybooks.get / invoiceLines NA (already 186.79)

Target math (tip after 186.79 / 376/192):

- live/vision **192 → 201** (+1 delete tool + 8 NA)
- implemented/contract **376 → 385**
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **121 → 129** (+8)
- complete **false**

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research180)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. daybooks Supports update+delete; billLines full CRUD+bulk; users get/list/update+bulk. |
| SPA daybook seed dual | POST/DELETE `/v2/daybooks` 200 dual under committed egress. |
| Delete chrome | Mere visible dual; menu body contains Slet dual (`menu_body_has_slet` true dual). Elevates research179 DEFER. |
| Update form | `update_ready` false dual; name_inputs 0; soft edit/update empty → NA. |
| Bill seed fixed | POST `/v2/bills` 200 dual with expense account + purchase taxRate + description + amount (not `text`). |
| billLines dedicated | false dual; embedded on bill edit true dual → NA package. |
| users panel | list markers dual; get/update false dual → NA package. |
| Cleanup / token | cleanup dual; api_token_used false; profiles purged. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`  
Dual: `.fractal/main.billy_complete/tmp/research180_focus_dual.json`

## File ownership (root EXECUTE)

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` — input/success for `ui_daybooks_delete_open` |
| Browser | `src/billy_mcp/browser.py` — protocol + impl (mere open, Slet menu text assert, never confirm) |
| Server | `src/billy_mcp/server.py` — register tool |
| Generator | `scripts/generate_coverage_report.py` — daybooks.delete tool map; GEO NA set +8 ids; live test refs |
| Egress | `coverage/browser_egress.yaml` — allowlist `tests/live/test_ui_daybooks_delete_open.py` on daybooks paths if required |
| Unit tests | `tests/unit/test_browser.py`, `tests/test_models.py`, coverage unit tests as peers |
| Live dual | `tests/live/test_ui_daybooks_delete_open.py` — dual seed daybook, open delete chrome, independent second session, cleanup, vision record path |
| Vision | `tmp/vision-records/ui_daybooks_delete_open.json` (non-sensitive; purge raw frames) |
| Wiki | short durable note on daybooks delete chrome + NA freezes (no iteration labels) |

NA freezes: same generator pattern as invoiceLines NA (186.79) /
accounts NA (186.78) — exact row ids only; evidence strings cite research180.

## Acceptance criteria

1. Offline suite green (`BILLY_TEST_MODE=commit` / default offline path in `scripts/test.sh` + lint/type as pre-commit peers).
2. Live dual `test_ui_daybooks_delete_open` pass with DOM + second-session read-back; vision record accept; raw frames purged.
3. Coverage: `ui.parity.daybooks.delete` green on `ui_daybooks_delete_open`; `daybooks.update` + billLines five + users two NA green; bulk not NA; API `live_tested` remains false with `out_of_scope_by_user`.
4. Counts: live/vision **201**; implemented/contract **385**; GEO NA **129**; complete **false**.
5. Independent review ACCEPT on product package; completeness FAIL expected.
6. No credentials, screenshots, HAR, traces in repo.

## Execution order

1. Models + browser delete chrome (mere_slet) + server registration.
2. Generator NA sets + daybooks.delete mapping + egress live test path.
3. Unit tests then offline full suite.
4. Live dual test + vision record (credentials via keyring opaque refs only).
5. Wiki note; regenerate coverage; self-check counts.
6. Stop for REVIEW (not finish).

## Agent / spawn

- Root-only Grok. Do **not** spawn children for this slice (file ownership is shared browser/generator).
- Node seed requires `--agent=grok` for any child; routing policy codex-power is overridden by seed for this node.
- No `codex-power` / `claude` / bare `codex` fallbacks.

## Non-goals

- Node finish / complete true
- Live API tests or `BILLY_API_TOKEN` qualification
- Bulk greening; annual greening
- Headed browser; persistent test data
- Dual-count parent bills/daybooks/users list tools onto residual ops

## Post-mortem (fill after EXECUTE / IR)

- Status:
- Shipped:
- Counts:
- Deviations:

## Post-Mortem

### Status
Package ready for COMMIT. Completeness still false (bulk 92, annual_reports, residual UI).

### Completed
- Product: `ui_daybooks_delete_open` (Mere→Slet delete_chrome_open_only → api.daybooks.delete).
- NA: daybooks.update; billLines get/list/create/update/delete (5); users.get + users.update (2).
- Live dual + vision accept + purge verified.
- FIX-VERIFY: IR required fixes none; applied optional N1 (egress mit.billy.dk live test ref for delete).

### Deviations
- Product iterates daybook ids (prefer later) because system journals may omit Slet; live harness always seeds disposable daybook.
- N2 SPA toast noted in IR; no code change.

### Review
- IR ACCEPT package; completeness FAIL expected (`tmp/grok-review.md`).

### Tests
- lint.sh: pass
- offline test.sh: **1653 passed** / 52 deselected
- live: `tests/live/test_ui_daybooks_delete_open.py` pass (EXECUTE)
- No ui-full (not complete)

### Coverage
- live/vision **201**; implemented/contract **385**; GEO NA **129**; complete **false**
- API live_tested remains false (`out_of_scope_by_user`)

### Cleanup
- Disposable daybooks deleted in live harness; vision frames purged; research profiles purged.

### Next unresolved slice
- Residual UI discovery_required / plan-gated (annual_reports, nested daybook*, bulk UI, etc.)
- Clarify 92 bulk external-contract from docs only (no live API)
- Offline 29 clear API gaps remain a separate offline lane

