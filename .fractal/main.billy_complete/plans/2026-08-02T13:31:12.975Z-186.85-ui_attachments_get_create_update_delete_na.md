# 186.85 ui attachments get create update delete not applicable
## Goal

Product research185 as one root-only inventory freeze (no new `ui_*` tool):

**NA** with `GEO_UI_NO_EQUIVALENT_WORKFLOW` for exact residual attachments ops:

| Decision | Exact API id | UI parity id | Mapping |
| --- | --- | --- | --- |
| NA | `api.attachments.get` | `ui.parity.attachments.get` | `parity_status=not_applicable`, empty `tool_name` |
| NA | `api.attachments.create` | `ui.parity.attachments.create` | same |
| NA | `api.attachments.update` | `ui.parity.attachments.update` | same |
| NA | `api.attachments.delete` | `ui.parity.attachments.delete` | same |

Exact ids only. Keep greened:

- `ui.parity.attachments.list` → `ui_uploads_list` (research184 dual-count)
- `ui.parity.files.create` → `ui_uploads_list` (research184 dual-count)
- `ui.parity.files.list` / `files.get` → NA research184
- `ui.parity.special.files_upload` → `ui_uploads_list`

No bulk. No annual. No offline clear×29 product.

### Hard dual-count / NA bans

| Do | Do not |
| --- | --- |
| NA exact four residual attachments ops | Dual-count Bilag upload as `attachments.create` (conflicts research184 `files.create`) |
| Keep `attachments.list` dual-count on Bilag | Invent `ui_attachments_get/create/update/delete` tools |
| Keep soft `/attachments` empty dual as absence evidence | Infer delete green without dual Slet chrome |
| Keep bulk + annual red | Green bulk 92 or annual_reports |
| Peer products/accounts residual NA while list stays green | Pure-NA `attachments.list` |

### Explicit DEFER / REJECT (out of this plan)

- attachments bulk ×2, files bulk ×2, all other bulk 92
- annual_reports greening (Upsedasse dual reconfirmed research185)
- seed non-empty Bilag + re-probe row chrome
- offline clear-API tool gaps (29 non-bulk)
- live `BILLY_API_TOKEN` / headed browser / `node finish`
- re-product `ui_uploads_list` DOM beyond inventory flags

### Target math (tip `df93af7` / 186.84)

| Metric | Before | After |
| --- | --- | --- |
| live/vision | 282 | **286** (+4 NA full flags) |
| implemented/contract | 466 | **470** |
| `GEO_UI_NOT_APPLICABLE_ROW_COUNT` | 206 | **210** (+4) |
| complete | false | **false** (annual + bulk 92 + offline clear×29 + UI bulk parity) |

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research185)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. `/v2/attachments` full CRUD+bulk; owner/file immutable; upload auto-create via **POST /v2/files**. |
| Bilag identity | SPA reload hits `/v2/attachments` **14 dual**, **files 0 dual** (research184+185). |
| Soft `/attachments` | soft-empty dual (body_len ~77–138). |
| List chrome residual | **Slet=0, Gem=0, tbody=0, click candidates=0 dual** — cannot product get/update/delete open chrome from empty list. |
| Create join form | Vedhæft=0 dual; soft empty; upload surface already greened as **files.create** + **special.files_upload**. |
| Adjacent shells | bills/invoices/daybookTransactions: no attachment href / Vedhæft dual on list. |
| Annual | Upsedasse dual — keep red. |
| Peer | research176 products.get/update/delete NA while list/create stay tool-green; research178 accounts get/create/update/delete NA while list green. |
| Brief | `.fractal/main.billy_complete/tmp/grok-research.md` (research185). |
| Dual | `tmp/research185_focus_dual.json`. |
| Probe | `tmp/research185_focus_probe.py` (tmp only; observe-only). |
| Cleanup | no seeds; profiles purged dual; `api_token_used` false dual. |

## Non-goals / safety

- No new FastMCP UI tools; no model/server/browser product changes.
- Never set `input[type=file]`; never submit upload/delete/save.
- No live API token / no credentialed API qualification calls.
- No headed browser / no desktop window.
- No bulk greening; no annual greening; no stub/skip as green.
- No dual-count of residual attachments ops onto Bilag upload or receipt inbox.
- Frames not required for NA inventory flags (`vision_evidence` null peer NA rows).
- API `live_tested` remains false with `out_of_scope_by_user`.
- No children; only `--agent=grok` if any spawn (none planned).

## Ownership

| Area | Owner |
| --- | --- |
| Generator research185 NA frozenset + count 210 + evidence branch | Root Grok EXECUTE |
| Inventory unit tests (exact 4 NA, bans, residual red bulk/annual) | Root Grok EXECUTE |
| Wiki: `ui_attachments_get_create_update_delete_not_applicable.md` + `_index` | Root Grok EXECUTE |
| Optional comment on `ui_uploads_list_shell.md` residual closed | Root Grok EXECUTE |
| Regenerate coverage manifests/status | Root Grok EXECUTE |
| No new live product test file | Root (inventory-only freeze) |
| Independent review | Root Grok (self-IR) |

## EXECUTE steps (ordered)

### 1. Generator — research185 exact-id NA freeze

File: `scripts/generate_coverage_report.py`

1. Add after research184 set:

```python
# research185: attachments get/create/update/delete only
# (list dual-counted on Bilag research184; upload create is files.create dual-count;
#  bulk external-contract red; soft /attachments empty dual; Bilag no Slet/Gem/tbody dual).
GEO_UI_NOT_APPLICABLE_RESEARCH185_IDS: frozenset[str] = frozenset(
    {
        "api.attachments.get",
        "api.attachments.create",
        "api.attachments.update",
        "api.attachments.delete",
    }
)
```

2. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **206 → 210** (comment: + research185 attachments residual 4).

3. Update comments that say “attachments get/create/update/delete stay red” (research184 block + uploads apply docstring) to “closed by research185 NA” / “list stays dual-count; residual NA research185”.

4. `geo_ui_not_applicable_api_row`: return True if id in `GEO_UI_NOT_APPLICABLE_RESEARCH185_IDS` (peer 183/184 exact sets). Prefer exact frozenset over bloating `GEO_UI_NOT_APPLICABLE_API_PREFIXES` (those prefixes use startswith; full ids for products work there too, but exact set matches 183/184 style for multi-op resource residuals).

5. In `apply_ui_geo_reference_not_applicable_evidence`:
   - `is_research185 = api_row_id in GEO_UI_NOT_APPLICABLE_RESEARCH185_IDS`
   - Docstring paragraph for research185 (soft empty + Bilag list no get/update/delete chrome + ban dual-count create onto upload).
   - Evidence branch peer research176/184:
     - `research_id = "research185"`
     - `evidence_ref = "research185_attachments_get_create_update_delete_dual"`
     - method_or_route / evidence text cites dual: soft `/attachments` empty; Bilag SPA attachments hits; Slet/Gem/tbody/click candidates 0; files hits 0; upload already greened as files.create/special.files_upload; never invent tools.
   - Flags: discovered/implemented/contract_tested/live_tested/vision_verified all true; `parity_status=not_applicable`; `tool_name` empty; `vision_evidence` null; `qualification.not_applicable_decision=accepted`; `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`.

6. Status blocker string: still mentions bulk 92 + annual; may drop “attachments residual” wording if present after regen (keep annual + bulk + residual offline if status template mentions UI residual generically).

### 2. Inventory tests

File: `tests/coverage/test_coverage_inventory.py` (and any related coverage tests)

1. Replace assertions that force `ui.parity.attachments.get|create|update|delete` stay red / discovery_required / not NA (research184 residual block ~1112+ and any residual lists ~1775).

2. Add `test_research185_attachments_get_create_update_delete_na`:
   - For each of the four UI rows: `parity_status=not_applicable`, five flags true, empty tool, research185 in evidence/method/evidence_ref, `not_applicable_decision=accepted`, `GEO_UI_NO_EQUIVALENT_WORKFLOW`.
   - `ui.parity.attachments.list` still tool `ui_uploads_list`, live true, list_shell_open_only (unchanged research184).
   - `ui.parity.files.create` still dual-count on `ui_uploads_list`.
   - `ui.parity.files.list` / `get` still research184 NA.
   - attachments/files bulk still red (not live, not NA accepted).
   - `ui.discovery.annual_reports` still incomplete / discovery_required.
   - `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 210` and `len(RESEARCH185_IDS) == 4`.

3. Update any global greened-row math that assumed GEO NA **206** and residual attachments red (e.g. counts around 74 + GEO_NA + dual-counts). After research185: GEO NA **210**; dual-count greens for attachments.list + files.create remain (+2 from research184 baseline).

4. Keep bans: no pure-NA of attachments.list; no greening of bulk/annual.

### 3. Wiki

1. Create `wiki/ui_attachments_get_create_update_delete_not_applicable.md` peer of `ui_products_get_update_delete_not_applicable.md`:
   - Decision NA accepted for exact four API ids
   - Dual evidence table (non-sensitive)
   - Contrast: list greened Bilag; create upload greened as files.*; annual not NA
   - API lane note: offline attachment tools unchanged; live_tested false out_of_scope_by_user
2. Link from `wiki/_index.md` and optionally one line on `wiki/ui_uploads_list_shell.md` residual closed by research185.

### 4. Regenerate coverage

```bash
uv run python scripts/generate_coverage_report.py
# or project's documented regen path if different
```

Confirm `coverage/status.json`:

- complete **false**
- live_tested_rows **286**, vision_verified_rows **286**
- implemented_rows **470**, contract_tested_rows **470** (if status uses those counters for UI+API together as today)
- official_docs md5/etag unchanged
- blocker still bulk + annual (and offline/API as applicable)

### 5. FIX-VERIFY (this iteration after EXECUTE)

```bash
uv run ruff check scripts/generate_coverage_report.py tests/coverage
uv run pytest tests/coverage/test_coverage_inventory.py -q
# commit-mode suite as required before commit
```

No live UI suite required for inventory-only NA freeze (peer research176/184). Do not run live API tests.

### 6. Independent review (root self-IR)

Accept only if:

- Exact four ids NA; list still dual-count green
- No dual-count of create onto Bilag upload
- Bulk/annual still red; complete false
- Counts 210 / 286 / 470 as planned
- Wiki present; no secrets/frames in repo

## Children

None. Single-file ownership on generator + inventory tests + wiki is clear; spawning would only add merge noise.

## Feasibility

- Budget: no limit; slice is inventory-only (~small).
- Blockers: none for this package; annual/bulk remain global complete=false blockers after product.
- Interrupted work: none (tip clean product-wise; only memory dirty from SYNC/RESEARCH).

## Success criteria (package)

- [ ] Four residual attachments UI parity rows NA accepted with research185 evidence
- [ ] attachments.list + files.* research184 outcomes unchanged
- [ ] GEO NA count 210; live/vision 286; implemented/contract 470
- [ ] bulk 92 + annual still red; complete false
- [ ] Inventory tests pass; coverage regenerated
- [ ] Wiki page + index
- [ ] Not node finish

## Post-Mortem

**Closed:** 2026-08-02T13:58:53Z (iter85 FIX-VERIFY)

### Work completed

- Producted research185 inventory freeze: exact NA for `api.attachments.get`,
  `create`, `update`, `delete` → `ui.parity.attachments.*` with
  `GEO_UI_NO_EQUIVALENT_WORKFLOW`.
- Generator: `GEO_UI_NOT_APPLICABLE_RESEARCH185_IDS`, ROW_COUNT 206→210, prefixes,
  `geo_ui_not_applicable_api_row`, evidence/method/levering branches; uploads
  docstring notes residual closed by research185.
- Inventory tests updated + `test_research185_attachments_get_create_update_delete_na`.
- Regenerated `coverage/status.json` + UI manifest; counts live/vision **286**,
  implemented/contract **470**, GEO NA **210**, `complete: false`.
- Wiki: `ui_attachments_get_create_update_delete_not_applicable.md` + `_index`,
  cross-links on uploads shell and files research184 page.

### Deviations

- None material vs plan. Dual probe had non-blocking `BrowserRuntime.stop`
  AttributeError after observations (profiles still purged dual).
- Root-only; no children.

### Independent review

- `tmp/grok-review.md`: **PASS (package) / ACCEPT**.
- Required fixes: **none**.
- Node complete still FAIL (expected): bulk 92 + annual_reports + offline clear
  residual.

### Test results (FIX-VERIFY)

- `bash .fractal/main.billy_complete/scripts/lint.sh` — pass (wiki/memory lint,
  ruff format/check, pyright, coverage inventory check, repo policy).
- `bash .fractal/main.billy_complete/scripts/test.sh` (commit mode) — **1666
  passed**, 54 deselected.
- Did **not** run `BILLY_TEST_MODE=full` / `ui-full`: completion requirements not
  all true (`complete: false`).

### Cleanup state

- Research dual profiles purged dual; no disposable org records created.
- No raw frames/HAR/traces committed.
- API `live_tested` remains false with out_of_scope_by_user.

### Next unresolved coverage slice

1. **External-contract bulk 92** — stay red until official bulk body/response
   schema exists (research137); no bulk tools.
2. **`ui.discovery.annual_reports`** — Upsedasse / org inaccessible; need
   non-Upsedasse shell on dedicated non-prod org.
3. **Offline API clear not-impl (~29)** — separate offline write product lane
   (e.g. geo create/update, transactions write, postings write) where Supports
   allows defensible contract freezes without live API.
4. Optional: disposable Bilag seed + dual row chrome re-probe only if product
   needs get/delete open tools later (not required while NA stands).

**Not node finish.** Ready COMMIT 186.85.
