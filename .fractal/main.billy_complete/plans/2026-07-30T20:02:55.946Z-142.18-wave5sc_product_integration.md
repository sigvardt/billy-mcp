# 142.18 Wave-5s-C product integration and independent review

## Objective

Preserve the cited research87 contract reconfirmation, then integrate only the
committed Wave-5s-C email/delivery product leaf and obtain a fresh, independent
Grok product review. This is an offline-only slice: research alone does not
green coverage, and live, UI, vision, residual, bulk, and completeness work
remain red.

## Adopted evidence and constraints

- `wiki/wave_fivesc_research87_product_in_flight_reconfirm.md` is the current
  cited Grok brief. The official documentation body remains byte-identical at
  MD5 `8b94b0135c91fd15fe54ea33e088a4be`.
- The active Codex Power leaf exclusively owns the four email/delivery tools,
  its tests, registration, and the two special manifest rows. Root must not
  duplicate or edit that work before merge.
- The only contract paths are `POST /v2/invoices/{invoiceId}/emails` with an
  `email` body and `POST /v2/invoiceDeliveries` with an `invoiceDelivery` body.
  The product must keep ticket-only execution, GLN validation, no automatic
  send retry, no raw destination URL, and fail-closed response handling.
- The leaf may set only `implemented` and `contract_tested` on the two special
  rows. `live_tested`, UI, vision, residual clear 29, bulk 92, and
  `complete` stay false.

## Management decision

No new child is spawned now. The bounded Codex Power product leaf is active
and owns the only implementation scope; an independent Grok review must fork
after its committed product bytes are merged to root. Starting that review now
would make it inspect the wrong baseline.

## Execution plan

1. Preserve and validate the existing research87 plan/wiki/memory additions:
   cited source chain, explicit non-claims, no credentials or raw evidence,
   and no coverage greening.
2. Monitor `main.billy_complete.wave5sc_email_delivery_product`; do not merge
   WIP. Require its full verification and a committed completion report.
3. Verify the committed child bytes from a clean `git archive`: inspect the
   three-dot diff for owned files only, run its focused tests and mandated root
   format/lint/type/coverage/policy/credential checks, and confirm coverage
   reports exactly 184 implemented + contract-tested rows with live/UI/vision
   still zero and `complete: false`.
4. Merge a passing child using a labelled `--no-ff` merge. Regenerate and lint
   the project wiki; update present-tense memory only with confirmed facts; and
   announce the material integration.
5. Spawn a fresh Grok leaf from the merged root for an independent product
   review. Its file ownership is limited to its review plan/wiki/memory record;
   it must inspect committed bytes, re-run representative checks, verify the
   cited contract, and distinguish offline product acceptance from all still
   red qualification lanes.
6. If the review identifies a defect, route the bounded fix to Codex Power and
   repeat archive verification plus fresh Grok review. If it accepts, record
   offline product acceptance only; do not advance into residual, bulk, live,
   UI, vision, or completeness without their separate evidence gates.

## Post-Mortem

- Preserved the cited research87 records without treating them as product
  acceptance. The finished Codex Power leaf was archive-verified, then merged
  with the labelled no-fast-forward merge `0efceae`.
- Archive verification passed the 32 focused email/delivery tests and coverage
  checker. The child additionally reported 1,255 passing non-live tests plus
  format, lint, Pyright, policy, audit, and wiki checks; root lint and
  commit-mode tests passed.
- Fresh independent Grok Review87 accepted the four-tool product offline only.
  Coverage is 184 implemented/contract-tested rows with live/vision zero and
  `complete: false`; no review finding required a code fix.
- The next unresolved coverage slices remain residual clear 29, ambiguous bulk
  92, UI/auth/vision, and dedicated non-production live qualification.
