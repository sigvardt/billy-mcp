# 198.39 product unfiltered products read-back

## Pivot

Owner `C6DA7FC8`: `B777AA82` `NOT_FOUND` is our session/search
miss, not Billy persist. Official docs ETag `"121myuqjdm53603"`
is unchanged. Do not re-lock. Do not requalify API contracts.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.

HEAD `3101495` already has slug compare, required `unitPrice`,
**Enhedspris** fill, still-open dialog `UI_CHANGED`, and
delete preview/execute. After **Gem produkt** it then:

1. Proves persist on `path="inventory"` first
   (`src/billy_mcp/ui_writes/products_submit.py`).
2. Falls back to `path="products"` only after `NOT_FOUND`.
3. `prove_text_on_fresh_page` fills
   `input[type='search'], input[placeholder*='øg' i]` when
   exact text is missing.
4. Live `_open_named_list` and
   `products_delete._delete_tagged_row` also fill search
   first.

Owner proof is an unfiltered `/:org/products` list after the
modal closes. Archive dump already has
`search_control_visible=false`. A hidden search fill can empty
the list.

Do not remake form-contract, Mere/Slet, archive, picker, or
Kunde dumps (`A337A622`, `D68E402A`, `113F1E05`). Do not
click **Opret ny**. Do not click **Arkiveret**. Do not change
the Lagermodul create form path this slice.

## Goal

Prove one tagged FastMCP product persist on an unfiltered
`/products` page, then hard-delete it:

1. Offline failing fixture: create read-back must
   hard-navigate `/:org/products` first. Current source starts
   on `inventory`.
2. Offline failing fixture: product persist and delete must
   count the exact tag on the unfiltered list before any
   search fill.
3. Smallest fix: after dialog close, independent session
   `goto /:org/products`, wait for load, exact-text count
   with no search. Inventory only after that miss. Same
   unfiltered-first rule for delete-icon find and the live
   list helper. Do not change shared
   `prove_text_on_fresh_page` search-first behaviour for
   contacts or bills unless a new opt-in flag keeps them
   unchanged.
4. One live FastMCP create through `create_server.call_tool`.
   Tag `MCP-UI-PRD-` plus 8 hex. Fresh-session unfiltered
   `/products` read-back. Delete. Third session proves
   **Ingen produkter**. Live test writes `author=live_test`
   and `reviewer_verdict=pending_review` only (`C7DBE974`).
5. Stay red. Do not remap `ui.parity.products.create`. Do not
   finish.

Invoice draft CUD is the next family after this CUD is
live-proved. Do not start it in this slice.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_products_writes.py`
- `tests/live/test_ui_products_writes.py`
- `src/billy_mcp/ui_writes/products_submit.py`
- `src/billy_mcp/ui_writes/products_delete.py`
- `src/billy_mcp/ui_writes/page_flow.py` (opt-in only)
- `wiki/ui_products_writes.md`

The seven write children are completed and merged. A child
would split a live proof the root must run anyway.

## TDD

`728BD2E4` / `C6DA7FC8`: failing fixture first, then the
smallest fix.

1. Add an offline source test in
   `tests/unit/test_ui_products_writes.py` that
   `products_submit.py` calls persist read-back with
   `path="products"` before any `path="inventory"`. Today's
   first `prove_text_on_fresh_page` path is `inventory`, so
   this is the first fail. Run that unit test. Capture the
   fail.
2. Add an offline source test that product persist read-back
   and `_delete_tagged_row` do not fill a search locator
   before the unfiltered exact-text count. Today's
   `search.first.fill` in `products_delete.py` and the live
   `_open_named_list` helper is the fail. Shared
   `page_flow.prove_text_on_fresh_page` may keep search-first
   for other families if product create stops using that
   fallback as its first proof.
3. Smallest create fix: after **Gem produkt** and dialog
   close, call an independent-session proof of the exact name
   on `/:org/products` with no search. Inventory is only the
   second attempt, also unfiltered. Hidden Ember dialog nodes
   stay not-failure.
4. Smallest delete/live fix: `_delete_tagged_row` and
   `_open_named_list` read the unfiltered list first. Fill
   search only after an unfiltered miss, or not at all if the
   exact tag is already visible.
5. Run `tests/unit/test_ui_products_writes.py` and the
   product-adjacent commit-mode unit set. Then one live
   FastMCP CUD. If unfiltered `/products` still misses after
   this fix, record `UI_CHANGED` and stop. Do not invent a
   new create CTA.
6. Update `wiki/ui_products_writes.md` and
   `wiki/ui_write_residual_table.md` in place. Stay red.

## Out of scope

- Coverage remaps or greening
- Official-docs re-lock
- Invoice draft CUD
- Files or daybook writes
- New inspect dumps or archive probes
- New child nodes
- `node finish`

## Done when

- Offline tests prove `/products` first and no search before
  the unfiltered count
- Live FastMCP create either proves the tagged name on a
  fresh unfiltered `/products` page and then deletes it, or
  returns `UI_CHANGED` without leftover records
- Honesty-16 stay red
- No headed browser, no live API, no new dumps

## Post-Mortem

Completed: create persist on unfiltered visible `/products`.
Delete now scopes to tagged `data-cy=table-item`, reveals
row actions, clicks that row's `delete-icon`, confirms
**Ja, slet**. `force=True` removed. Live
`test_ui_products_create_delete_via_call_tool` passed
(175s). Owner leftover `MCP-UI-PRD-*` tags were deleted.
Fresh list proved **Ingen produkter**.

Deviations: first live persist used hidden Ember
`get_by_text` (false pass). Review required `visible_body`.
Delete first targeted page-wide `delete-icon` under a
`section-header`. Owner `BF91E28F` named `table-item`.

Review: FAIL on delete and leftovers. Confirmed and fixed.

Tests: live CUD 1 passed (175s). Lint 0 errors after
`LoginControl.hover`. Commit-mode `2079 passed, 82
deselected`. Related unit 308 passed.

Cleanup: organisation empty of these tags after the live
run. Honesty stays red. No vision accept.

Next slice: invoice draft CUD with a disposable customer
and this product path. Stay red. Do not finish.

