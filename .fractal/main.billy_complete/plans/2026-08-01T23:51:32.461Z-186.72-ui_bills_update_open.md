# 186.72 ui bills update open

## Goal

Product research172 ACCEPT:

1. New read-only FastMCP UI tool **`ui_bills_update_open`** that opens a **bill
   edit form observe surface** frozen as `/:org_slug/bills/:id/edit` (Ret
   regning form chrome dual). Never Opdater / Godkend / Slet / Træk / Upload /
   Registrer betaling / submit. Observe-only form open for API parity.
2. Green exact **`ui.parity.bills.update`** / map `api.bills.update` onto
   `ui_bills_update_open` (`form_open_only`).
3. Keep greened `ui_bills_list`, `ui_bills_create_open`, and `ui_bills_get_open`
   unchanged. Do **not** dual-count update onto those tools. Path class is
   **distinct** from get (`/:org_slug/bills/:id` detail_open_only).
4. **No** browser egress change this slice (POST/DELETE bills + taxRates GET
   already committed in 186.71 for SPA seed/cleanup).

Target math (tip `5fb1846` / product 186.71):
live/vision **171 → 172**; implemented/contract **355 → 356**; complete
**false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research172)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. bills Supports get/list/create/**update**/bulk save/**delete**/bulk delete. PUT `/v2/bills/:id` partial update convention. |
| SPA seed dual | contact + draft bill nested lines (accountId, taxRateId, description, amount; no paymentDate) POST 200 dual under **committed** egress. product+invoice also seeded dual for residual probes only. |
| Edit form dual | soft **`/:org_slug/bills/:id/edit`** path dual; chrome dual: Ret regning, Godkend, Opdater, Slet, Leverandør, Bilagsdato, Forfaldsdato; buttons Slet=1 Godkend=1 Opdater=1; inputs ≥2; update_form_ready **true dual**. |
| Get contrast | soft `/:org_slug/bills/:id` detail dual (marker; Ret køb; **no** Slet/Godkend/Opdater on read detail). Update freezes **edit** path only. |
| Text-click list | lands edit dual (same chrome family). |
| Residual rejects | products.get detail_ready **false dual**; invoices.delete Slet **false dual** on draft; invoice_email soft paths no send_form dual; bulk/annual stay red. |
| Isolation | form_open_only ≠ list shell ≠ create form ≠ get detail_open. Green only **bills.update**. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research172_focus_dual.json`.  
Probe: `tmp/research172_focus_probe.py`.

## Non-goals

- Any change to `coverage/browser_egress.yaml` this slice
- Greening `bills.delete` (Slet chrome dual ready for **later** separate tool
  `ui_bills_delete_open`; do not dual-count delete with this tool)
- Greening invoices.update/delete, products.get*, special.invoice_email
- bulk_* greening; annual_reports greening
- Ticketed durable UI write for bill update (no real PUT; no approval ticket
  path for UI form open)
- Submitting Opdater / Godkend / Slet / Træk / Upload / Registrer betaling
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Dual-counting update onto list/create/get tools

## Ownership

| Area | Owner |
| --- | --- |
| Models + `BrowserRuntime.ui_bills_update_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count bills.update only | Root Grok EXECUTE |
| Unit tests (models + browser injectable) | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research172)

### A. Browser egress

**No change.** Reuse committed:

- exact POST `/v2/bills`
- prefix DELETE `/v2/bills`
- prefix GET `/v2/taxRates`
- prefix GET `/v2/bills`
- contacts POST/DELETE (seed contact if needed)
- accounts GET (expense account pick)

Harness SPA token only. Never `BILLY_API_TOKEN`.

### B. Tool: `ui_bills_update_open`

| Field | Contract |
| --- | --- |
| Name | `ui_bills_update_open` |
| Input | empty / org-from-session only (mirror get-open: open first openable draft / marker path; no caller-provided generic browser control; optional internal seed when list empty) |
| Success model | `UiBillsUpdateOpenSuccess` with booleans / path_class only (no raw ids/PII): e.g. `form_open`, `path_class`, `ret_regning_chrome`, `opdater_present`, `leverandor_or_dates_chrome`, `inputs_present`; `shell_kind=bills_update` |
| Path class | **`/:org_slug/bills/:id/edit` only**. Not bare list, not `/new`, not read-only `/:org_slug/bills/:id` (that is get). |
| Errors | typed `ToolError` for auth/egress/UI_CHANGED; never generic browser control |
| Forbidden | Opdater, Godkend, Slet, Træk, Upload fil, Registrer betaling, any submit |

Mirror structure of `ui_bills_get_open` / `ui_clients_update_open` (models →
browser runtime → server registry → unit tests with injectable page/context).

### C. Live dual qualification

File: `tests/live/test_ui_bills_update_open.py`

| Rule | Requirement |
| --- | --- |
| Dual sessions | two independent ephemeral profiles; second is independent read-back |
| Auth | browser credential references only; no API token env |
| Seed | if no draft bill: SPA POST contact (supplier) + bill with nested line (account+taxRate+description+amount; omit paymentDate); unique R18672 markers |
| Open path | navigate to `/:org_slug/bills/:id/edit` (or list text-click → edit) |
| DOM assert | path_class edit; Ret regning (or equivalent) + Opdater present + Leverandør/Bilagsdato family; inputs ≥2 |
| Never | click Opdater/Godkend/Slet/Træk/Upload/Registrer betaling |
| Read-back | second session opens same path class / form chrome independently |
| Cleanup | DELETE bill → contact (and any seed deps) reverse order; fresh list read-back clean dual |
| Vision | non-sensitive record under `tmp/vision-records/ui_bills_update_open.json`; frames outside repo; purge verified |
| api_token_used | false |

### D. Coverage generator

In `scripts/generate_coverage_report.py`:

- Constants: tool name, live test ref, vision record path, research172 cite
- `apply_ui_bills_update_open_shell_evidence(row, parity_of_api_update=True)`
  sets discovered/implemented/contract_tested/live_tested/vision_verified,
  parity_status=`form_open_only`, tool_name, evidence, test_references
- Apply **only** when row maps `api.bills.update` / `ui.parity.bills.update`
- Do **not** apply to list/create/get/delete/bulk bills rows
- Status math: live/vision 172; implemented/contract 356; complete false
- Blocker text may still mention residual UI + bulk + annual

### E. Wiki

Short shell page `wiki/ui_bills_update_open_shell.md` (pattern from
`ui_bills_get_open_shell.md`): path freeze, never-submit, dual-count only
bills.update, research172 pointer scrubbed of iteration noise where durable.

## File touch list (EXECUTE)

| Path | Action |
| --- | --- |
| `src/billy_mcp/models.py` | add `UiBillsUpdateOpenSuccess` (+ input if needed) |
| `src/billy_mcp/browser.py` | `ui_bills_update_open` workflow |
| `src/billy_mcp/server.py` | register tool |
| `scripts/generate_coverage_report.py` | dual-count apply bills.update |
| `tests/unit/test_browser.py` / models unit | injectable open + flags |
| `tests/live/test_ui_bills_update_open.py` | dual live |
| `tests/…` inventory if asserts fixed tool set | extend |
| `wiki/ui_bills_update_open_shell.md` | shell note |
| `coverage/*` | regenerate via generator (not hand-edit green) |
| `coverage/browser_egress.yaml` | **no change** |

## Verification (EXECUTE → FIX-VERIFY)

1. `bash .fractal/main.billy_complete/scripts/lint.sh` pass  
2. Offline: `BILLY_TEST_MODE=commit` / default offline test.sh (unit+contract) pass  
3. Live dual: `tests/live/test_ui_bills_update_open.py` pass (when credentials present)  
4. Vision record written; frames purged  
5. `uv run python scripts/generate_coverage_report.py` → bills.update green;
   list/create/get unchanged; live/vision 172; contract 356; complete false  
6. `scripts/check_coverage.py` (without require-complete) consistent  
7. No credentials/tokens in repo diff  

## IR acceptance criteria

- ACCEPT if: form_open_only on edit path dual; never-submit enforced; dual-count
  only bills.update; get/list/create tools unchanged; no egress churn; no bulk
  greening; cleanup dual proven; vision record non-sensitive.
- REJECT if: dual-counts wrong rows; submits writes; greened delete/products/
  invoice_email; uses BILLY_API_TOKEN; same path freeze as get without distinct
  edit assertions; incomplete cleanup.

## Out of slice residual (leave red)

- ui.parity.bills.delete (later `ui_bills_delete_open`)
- invoices.update/delete; products.get/update/delete
- special.invoice_email
- bulk 92 external-contract; annual_reports org_inaccessible
- API live_tested stays false (`out_of_scope_by_user`)

## Ready for EXECUTE

Root Grok implements plan 186.72 from research172. Not finish.

## Post-Mortem

- Outcome: DELIVERED ui_bills_update_open (form_open_only maps api.bills.update /
  ui.parity.bills.update). IR ACCEPT; no required fixes (optional N1 docstring
  wording fixed in FIX-VERIFY; N2 egress prose left as-is).
- live/vision: 171 → 172
- implemented/contract: 355 → 356
- complete: false (bulk 92 external-contract; annual_reports org_inaccessible;
  residual UI parity open)
- Tests: lint.sh pass; offline BILLY_TEST_MODE=commit 1631 passed / 47 deselected;
  live dual tests/live/test_ui_bills_update_open.py pass (EXECUTE + IR evidence);
  vision record purge_verified true.
- Cleanup: SPA seed contact+bill deleted reverse-order; dual list read-back clean;
  profiles ephemeral; frames purged outside repo.
- Deviations: none material. No egress path_allow expansion (reused 186.71).
- Next unresolved coverage slice: prefer ui_bills_delete_open (Slet chrome dual
  research172) or invoices form residual; products.get still dual-false; no bulk
  greening; no annual green without access. Not node finish.

