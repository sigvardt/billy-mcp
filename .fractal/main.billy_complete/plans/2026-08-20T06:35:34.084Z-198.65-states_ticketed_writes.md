# 198.65 states ticketed create and update

## Pivot

`AB6ABE84` is binding under `9B979A05`. HEAD `113810e` is a clean
198.64 locales close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-ab6abe84.html`,
`billy-api-docs-chunk-research-ab6abe84.js`,
`research-ab6abe84-states-section.html`,
`research-ab6abe84-states-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the locales lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2states`.
Supports: `get by id, list, create, update, bulk save, bulk
delete`. No singular delete. Property table is exactly three
rows with empty notes: `stateCode` string, `name` string,
`country` belongs-to.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.states.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_states_*` or bulk tools. Do not fold this into geo reads.
Do not start zipcodes (`8FE83270`). Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.states.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.states.create` | `api_states_create_preview` / `_execute` | `POST /states` | nested optional `{stateCode, name, country}` |
| `api.states.update` | `api_states_update_preview` / `_execute` | `PUT /states/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 8 method-closed + 2 readonly
map + 2 meta-delete = 12. Bulk 92 stay red. Implemented 540,
contract 545 (HEAD 538 / 543). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload with belongs-to strings is
`src/billy_mcp/api/city_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_state_writes.py`, copied from cities
   for the typed optional nested payload:
   - Register count is 4. Preview fields:
     `api_states_create_preview` `{state}`,
     `api_states_update_preview` `{id, state}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{stateCode, name, country}`. JSON schema types: all
     three are string. `additionalProperties` is false on outer
     and nested schemas. No `input` wrapper. Nested `required` is
     empty or absent.
   - Outer extras, empty update `id`, `stateId`, `countryId`, a
     JSON array for `name` or `stateCode`, nested objects for
     `country`, and other inner extras fail `ValidationError` /
     FastMCP `extra_forbidden`. Execute extras fail. Preview
     still makes no HTTP.
   - Empty nested object `{"state": {}}` is valid. Partial
     payloads such as `{"name": "X"}` or `{"country": "US"}` are
     valid. Do not require `stateCode`, `name`, or `country`.
   - Preview create sends no HTTP. Canonical request is
     `{"state": { ...set keys only... }}`. Expected effect
     `{action: create, resource: state}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_state_write_tickets.py`, copied from
   city ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/states` with the
     exact nested JSON. Execute PUTs
     `/v2/states/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/states/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `states` plus optional `meta.deletedRecords`. Do not
     expose a top-level `state` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 8`, residual `== 12`. Assert
   `api.states.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk states rows
   unspecified. Bump every hardcoded `implemented_rows == 538` /
   `contract_tested_rows == 543` in that file to **540 / 545**.
   `complete` stays false. Live/vision counts stay 339.
4. Map both ids in `OPERATION_TEST_REFERENCES` next to existing
   `api.states.get` / `.list`. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_states_writes_are_ticketed_offline_and_not_live_tested`
   next to the locales inventory tests.
6. `tests/unit/test_coverage_server.py`: add
   `STATE_WRITE_API_TOOL_NAMES` (four tools). Bump
   `len(api_tool_names) == 301` to **305**.
   `tests/coverage/test_api_live_qualification_semantics.py`:
   bump `len(implemented) == 199` to **201**.

Run:
`uv run pytest tests/api/test_state_writes.py tests/api/test_state_write_tickets.py tests/coverage/test_coverage_inventory.py tests/unit/test_coverage_server.py tests/coverage/test_api_live_qualification_semantics.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/state_writes.py` (new), copy
`city_writes.py`:

- `StatePayload`: `stateCode: str | None = None`,
  `name: str | None = None`, `country: str | None = None`,
  `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{state}`.
- Update preview input: `{id min_length=1, state}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/states`. Singular root `state`.
  Plural root `states`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_state_write_tools` next to
`register_city_write_tools`. Do not change geo reads.

`scripts/generate_coverage_report.py`:

- Remove `api.states.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids after
  `api.states.list`.
- Do not touch bulk helpers, the other 8 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` states create/update true,
  delete false (`("states", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 8. These two rows are ticketed
  offline tools. Residual honesty remaining 12. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional string `stateCode`, `name`,
  and belongs-to `country` is enough to ship the ticketed POST/PUT.
  Reject `stateId` and `countryId`. Unauth 405 is not the
  contract. Empty payload is allowed. Do not copy list filter
  `countryId` into the write payload as required.

Keep `wiki/ui_geo_cities_not_applicable.md` as history. Do
not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.states.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.states.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research138).
- Do not accept `stateId`, `countryId`, or a JSON object/array
  for `stateCode`, `name`, or `country`.
- Do not require any nested field.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.
- Saved `8FE83270` waits until this commit. Saved `9B979A05`
  still owns the remaining 12 residual rows and bulk92. Saved
  `96908DC6` still owns overall finish.

## Independent review

After EXECUTE, review must re-parse `#v2states` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still three optional fields: `stateCode`
   string, `name` string, `country` belongs-to.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /states` and `PUT /states/:id`.
6. Nested payload rejects `stateId`, `countryId`, arrays, nested
   objects, and extra keys. Empty nested object is valid. JSON
   schema type `string` holds for all three fields.
7. `live_tested` is false.
8. Remaining residual honesty is 12 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.
   No zipcodes work in this slice.

## Post-Mortem

Work completed: ticketed offline `api.states.create` and
`.update` in `src/billy_mcp/api/state_writes.py`. Preview
tools take a nested payload of optional string `stateCode`,
`name`, and belongs-to `country` with `extra=forbid`.
`stateId`, `countryId`, arrays, nested objects, and extra keys
fail at the FastMCP boundary. Empty nested object is valid.
Execute binds `POST /states` and `PUT /states/:id` through the
shared write protocol. Coverage regenerated: implemented 540,
contract 545, live/vision 339, complete false. Residual honesty
remaining 12.

Deviations: none from the official field table. Registry count
asserts also bumped (`api` tools 305, implemented API rows 201).
Ticket and execute contract tests live in
`tests/api/test_state_write_tickets.py` so each test file
stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
suite `2341 passed, 83 deselected`. Cleanup: no live UI records.
Next unresolved slice after COMMIT: saved `8FE83270` ticketed
`zipcodes` create and update. Then saved `7453B98F` ticketed
`balanceModifiers` create and update. Residual honesty remaining
12. Bulk 92 stay red. `complete` stays false.

