# 198.44 fresh page and invoice PUT prefix

## Pivot

Owner `32662804`: leftover
`/invoices/03dvBZm9QHuYt8jGMM8TNw/edit` at 1280x720 has heading
`Rediger fakturakladde`, exact_count=1, input_count=11. Headless
`exact_count=0` / `input_count=8` is a stale or incomplete
headless session, not viewport drift. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
https://www.billy.dk/api/ (2026-08-19, ETag `"121myuqjdm53603"`):
`PUT /v2/invoices/:id` updates only set properties. Line price
persist stays `PUT /v2/invoiceLines/:id`. Do not re-lock docs
(`728BD2E4`). Do not create a seventh invoice. Do not remake
dumps. Do not spawn a child.

HEAD `e40820f` already waits for
`input[name='unitPrice'][placeholder='Enhedspris']` and fills
`2,00` on that locator. Create already closes the bootstrap page
and `new_page()`. Update reuses the `/` page. Egress allows
GET/POST/DELETE for `/v2/invoices` and no PUT.

## Goal

Unblock leftover FastMCP update of `MCP-UI-INV-6CDB396B` to
**Enhedspris** `2,00` on a fresh row-open session, then delete
all 6 drafts, 6 products, and 6 customers.

1. Offline failing fixture: real `BrowserEgressPolicy` must allow
   `PUT https://api.billysbilling.com/v2/invoices/abc` and deny
   collection `PUT /v2/invoices` and `PATCH`. Today's manifest
   fails the allow.
2. Offline failing fixture: coverage inventory must require PUT
   prefix `/v2/invoices/` and forbid collection PUT and PATCH,
   matching bills.
3. Offline failing fixture: `submit_draft_invoice` must
   `new_page()` after the org slug check for update and delete,
   not only create. Today's `if action == "create"` is the fail.
4. Smallest fix: add PUT prefix `/v2/invoices/` (trailing slash,
   never collection, never PATCH, never emails). Close the
   bootstrap page for update and delete, same as create. Keep the
   exact-input wait and `2,00` fill.
5. One live FastMCP update of `MCP-UI-INV-6CDB396B` to
   `unit_price=2`. Fresh-session **Enhedspris** `2,00`. Then
   reverse-delete all 6 triples. Third session empty.
6. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `coverage/browser_egress.yaml`
- `tests/unit/test_browser.py`
- `tests/coverage/test_coverage_inventory.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child and forbids `ui_invoice_lines_*` tools.

## TDD

`728BD2E4` / `32662804`: failing fixture first, then the smallest
fix.

1. In `tests/unit/test_browser.py`
   `test_browser_policy_real_manifest_allows_contacts_data_plane`,
   assert `policy.allows(..., "PUT")` for
   `/v2/invoices/abc`, and deny collection PUT and PATCH. Run it.
   Capture the fail.
2. In `tests/coverage/test_coverage_inventory.py`, assert PUT
   prefix `/v2/invoices/` and forbid collection PUT and PATCH,
   matching the bills block. Run it. Capture the fail.
3. Add an offline source test that `submit_draft_invoice` closes
   the bootstrap page for `action == "update"` and
   `action == "delete"`, not only `"create"`. Today's
   `if action == "create"` is the fail.
4. Smallest egress fix: PUT prefix `/v2/invoices/` in
   `coverage/browser_egress.yaml`. Cite `32662804` next to the
   bills PUT evidence. Never collection PUT. Never PATCH. Never
   emails.
5. Smallest page fix: after the live org slug matches the ticket,
   close the bootstrap page and `new_page()` for every draft
   action, then `open_invoice_row` / create. Keep
   `unit_price_field` wait and `commit_unit_price` `2,00`.
6. Live FastMCP `call_tool` update of leftover
   `MCP-UI-INV-6CDB396B`. Assert `2,00` before save. Require
   `PUT /v2/invoiceLines/:id` 2xx. Fresh session must show
   `2,00` on the same input. Then reverse-delete 6 drafts, 6
   products, 6 customers. Third session: no `MCP-UI-INV` /
   `MCP-UI-PRD` tags.
7. Vision record: `author=live_test`,
   `reviewer_verdict=pending_review`. Do not self-accept. Do not
   purge in the live test. Honesty-16 stay red.

If live save fires only `PUT /v2/invoices/:id` and no
`invoiceLines` PUT, record redacted method/path/status and keep
update red. Do not treat header PUT 2xx as Enhedspris persist
(`B9C4FA7C`). Do not add `/v2/invoiceLines` egress unless a live
blocked XHR names that path.

## Out of slice

New invoice create, files, daybook, **Godkend**, emails, bulk,
docs re-lock, dump remakes, child spawn, `ui_invoice_lines_*`
tools, live API, persist-watch rewrite, invoiceLines egress,
viewport changes, guessed clicks.

## Success

One FastMCP update that a fresh interface session reads as
**Enhedspris** `2,00`, then an empty org. Coverage stays red
until independent accept and purge.

## Post-Mortem

Work completed: failing PUT-prefix tests, then PUT prefix
`/v2/invoices/` (never collection, never PATCH, never emails).
Update `new_page()` after org slug, same as create. GET prefix
`/v2/invoiceLines` for line-editor render. Live FastMCP leftover
update of `MCP-UI-INV-6CDB396B` filled `2,00` and a second session
read that price. SPA save wait is 2xx PUT `/v2/invoices/:id`.
`persist_hit` still rejects header PUT as line persist.

Deviations: GET `/v2/invoiceLines` was added after live
`exact_count=0` / `product_text_count=0`. PUT `/v2/invoiceLines/`
was added from `watched=[]` then removed (`D859572B`). Delete was
not in the original fail-first list; unique Mere and exact Slet
now match `70DB45E6`, then **Ja, slet** is not visible and DELETE
is not recorded.

Review: IR FAIL. Update 2,00 is not finished CUD. No vision
accept. No purge. Honesty-16 stay red.

Tests: lint passed. Commit-mode `test.sh` 2097 passed, 82
deselected. Live leftover CUD still fails on delete confirm.

Cleanup: six leftover triples remain. Next slice: prove
**Ja, slet** plus DELETE 2xx and reverse-clean, without adding
invoiceLines PUT.

