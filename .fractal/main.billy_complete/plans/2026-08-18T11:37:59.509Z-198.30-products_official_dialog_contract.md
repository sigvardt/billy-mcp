# 198.30 Product official dialog contract inspect

## Pivot

`113F1E05` closed further invoice entry-CTA dumps.
`unique_normal_action=false` on the invoice create form.
`next_slice=product_create`. `FD39FFE7` still binds both families.
Stay red. Do not finish.

The Mere/Slet delete-chrome dump is closed (`D326FFB3` /
`63b2528`). `proved_delete_path=none`. Do not remake it. Official
product setup (5 Feb 2026) names **Gem produkt** and **Arkiveret
(skjul fra lister)**, not **Slet produkt**. Official API field is
`isArchived`. Current execute clicks exact **Gem** and fail-closes
on that dump.

## Goal

Inspect the already-loaded Lagermodul **Opret produkt** dialog and
classify the official save and archive controls. Read-only. TDD
first. Do not persist a product.

Persist **only**:

- `inventory_path_class` (`inventory` / `other`)
- `inventory_heading_token` (`lagermodul` / `other` / `none`)
- `create_form_open` (bool)
- `dialog_heading_token` (`opret_produkt` / `ret_produkt` /
  `other` / `none`)
- `name_field_visible` (bool)
- `gem_produkt_count` (int)
- `gem_count` (int)
- `save_cta_token` (`gem_produkt` / `gem` / `other` / `none`)
- `arkiveret_count` (int)
- `cleanup_token` (`archive_checkbox` / `none`)
- `unique_cleanup_path` (`archive_checkbox` / `none`)
- `proved_submit` (`gem_produkt` / `none`)
- `missing_keys`

`proved_submit` is `gem_produkt` only when `gem_produkt_count=1`.
`unique_cleanup_path` is `archive_checkbox` only when
`arkiveret_count=1`. A count of 0 or greater than 1 is `none`.
Do not click **Gem**. Do not click **Gem produkt**. Do not tick
**Arkiveret**. Do not open products **Mere**.

If the official controls are present, persist still stays
fail-closed. Ask the owner whether archive-until-absent from the
product list counts as restored state. Do not freeze the row as
done. Do not invent `ui_products_delete_*`. Do not SPA-DELETE
`/v2/products`.

Authority: `96908DC6`, `FD39FFE7`, `113F1E05`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Closed: Mere/Slet
delete-chrome, invoice picker, Ember, fiber, listener, structure,
list CTA, Tilgodehavender CTA. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.products.create` to
  `ui_products_create_preview`
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or product
  `parity_status`
- Calling `ui_products_create_execute` or clicking any save
- Overwriting
  `~/.local/share/billy-mcp/inspect-live-products-delete-chrome.json`
- Inventing `ui_products_delete_*` or `api_inventory_*`
- Using `DELETE /v2/products` as cleanup
- Remaking invoice dumps or altering invoice coverage
- Recapturing catalog `/products` create CTA (research163)
- Picture upload, force clicks, `evaluate` clicks, guessed
  selectors
- Persisting source, locators, URLs, slugs, names, or screenshots
- Ledger, files, org, or a new contacts/bills CUD
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `5e634d2`. Not a backstop. `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations CUD rows already name
preview tools. Invoice CUD stays `ui_invoices_*_open` and unbound.
Product create preview/execute already registered. Manifest still
points the honesty row at `ui_products_create_open`.
`BrowserProductSubmitter` fail-closes when
`product_persist_allowed` is false.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. `728BD2E4` child-split already
landed the seven write-family modules. Those children stay merged
and parked. Unlimited budget is not a reason to spawn.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/products.py` (ticket pair; no remap;
  persist stays fail-closed)
- `src/billy_mcp/ui_writes/products_delete_chrome.py` (closed dump;
  do not extend its key set)
- `src/billy_mcp/browser.py` research163 form flags
  (`_products_create_form_field_flags`, **Opret produkt** CTA)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `tests/live/test_ui_products_create_open.py` (form-open contrast)
- `wiki/ui_products_writes.md`
- `wiki/ui_products_create_open_shell.md`

New files:

- `src/billy_mcp/ui_writes/products_form_contract.py`
- `tests/unit/test_ui_products_form_contract.py`
- `tests/live/test_ui_products_form_contract.py`

Do not grow `tests/live/test_ui_products_writes.py` or the
delete-chrome files this pass.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-products-form-contract.json`

## Design choices

1. **P10 wins.** Do not remake Mere/Slet. Do not remake invoice
   helpers.
2. **TDD first.** Honesty-16 stays 16 and red. First product edit
   is a failing unit fixture against the key set above.
3. **Official labels only.** Count **Gem produkt**, **Gem**,
   **Arkiveret (skjul fra lister)**, and dialog heading tokens via
   exact role or exact text. No substring. No body sweep.
4. **Read-only.** Open inventory **Opret produkt**. Classify.
   Escape. No submit.
5. **Persist stays fail-closed.** Archive is hide-from-lists, not
   delete. Owner must decide before any create.
6. **No remap** until live FastMCP CUD plus independent accept
   plus purge of a real create with restored state.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.products.create` still names `ui_products_create_open`.

Add `REQUIRED_PRODUCTS_FORM_CONTRACT_KEYS` and `missing_keys` in
`products_form_contract.py` only after a unit fixture fails
because those names do not exist, or add the fixture first against
a local helper stub. Preferred shape:

- Fixture feeds `{}`.
- Helper returns the missing key list from this plan.
- Current empty dump must fail with that exact set.

Do not edit `generate_coverage_report.py` or remap coverage.

### 2. Smallest helper, then one live inspect

- Implement the helper in `products_form_contract.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  names.
- Live test: READY via existing credential refs. Open
  `/:org_slug/inventory`, click **Opret produkt**, classify,
  Escape. Write the dump outside git.
- Live test verifies the dump keys without repeating a click if
  the dump already exists from this EXECUTE. Prefer one capture.
- Do not call `ui_products_create_execute`.

### 3. Stay red

Do not finish. Do not claim complete. If official controls are
present, record them and keep persist fail-closed. Post the owner
question from research section 5. Do not treat archive as a
finished cleanup path.

## Success criteria

- Honesty-16 still length 16 and red
- Failing dump-key fixture existed, then passed
- Live dump has every required key
- No **Gem** / **Gem produkt** click
- No tagged product created
- Invoice coverage untouched
- Delete-chrome dump untouched
- No children spawned
- Node stays red

## Post-Mortem

Completed: TDD empty-dump fixture, form-contract helper, live
read-only Lagermodul inspect. Live dialog:
`proved_submit=gem_produkt`, `unique_cleanup_path=archive_checkbox`,
`dialog_heading_token=opret_produkt`. No **Gem** / **Gem produkt**
click. No product created. Honesty 16 still red.
`ui.parity.products.create` still names `ui_products_create_open`.
`complete=false`. Owner question `67CBACB6` is open.

Independent review: FAIL. Dump contract met. Product CUD still
unbound. Persist stays fail-closed on delete-chrome
`proved_delete_path=none`.

FIX-VERIFY: confirmed IR. No remap. No persist. Bound the still
fail-closed execute click to **Gem produkt** so a later persist
cannot click the missing **Gem** label. Offline fake-page test
updated. Persist gate still reads the delete-chrome dump.

Tests: honesty-16 2 passed. Form-contract unit tests plus persist
lock. Live form-contract passed. Offline `test.sh` run in this
step. Lint run in this step.

Cleanup: no tagged product created. Delete-chrome dump untouched.

Next unresolved slice: wait for the owner on archive-as-cleanup.
Do not remake this dump. Do not remake Mere/Slet. Invoice CUD
stays unbound under `113F1E05`. Files and ledger still wait.

