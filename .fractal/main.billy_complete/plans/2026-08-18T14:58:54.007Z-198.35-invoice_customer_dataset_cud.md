# 198.35 invoice customer dataset then draft CUD

## Pivot

Owner radio `EC676F84` (P10) supersedes the research wait and the
invoice `interface_control_absent_UI_CHANGED` claim.

Research (`.fractal/main.billy_complete/tmp/grok-research.md`) still
holds for product, daybook, files, and postings. It is wrong on
invoice: the owner-visible session shows the Kunde control works.
The live DOM is textbox **Vælg kunde** plus **Ingen kontakter
fundet** and **Opret ny**. The blocker is an empty customer
dataset, not broken typeahead.

Do not patch or probe the dropdown. Do not remake picker, CTA,
Ember, fiber, listener, or validation dumps (`113F1E05`,
`D68E402A`).

Official docs still require invoice `contact` (immutable, required)
on `POST /v2/invoices`. No live API.

## Goal

Prove invoice draft create, update, and delete through FastMCP
against a customer that already exists in the dedicated
non-production organisation.

1. Create one uniquely tagged disposable customer with
   `ui_clients_create_preview` / `ui_clients_create_execute`.
2. Confirm that name in a fresh interface session (clients list or
   customer detail). This is the missing step in the current live
   invoice CUD test.
3. Use that exact name as `contact_name` for
   `ui_invoices_{create,update,delete}_{preview,execute}`.
   Draft only. CTA **Gem som kladde** / **Slet**. Never send,
   approve, email, or **Godkend**.
4. Independent read-back after each invoice step.
5. Delete the invoice first, then the customer. Prove both absences
   on a third fresh session.

Stay red. Honesty-16 stays false until independent accept and
purge. Do not finish the node.

## Why no child

This is one family on files the root already owns:
`tests/live/test_ui_invoices_writes.py`,
`src/billy_mcp/ui_writes/invoices_form_bind.py`,
`src/billy_mcp/ui_writes/invoices.py`,
`wiki/ui_invoices_writes.md`. The seven write children are
completed and merged. Spawning would split ownership of a live
proof the root must run anyway.

## TDD

`728BD2E4`: start with a failing test, then the smallest fix.

1. Add a focused assertion in
   `tests/live/test_ui_invoices_writes.py::test_ui_invoices_create_update_delete_via_call_tool`
   (or a thin sibling in the same file): after contact execute,
   a second authenticated session must see the tagged customer
   before any invoice preview. Current code only `asyncio.sleep(2)`
   then invoice create. That is the expected first fail.
2. Run that live test. Capture the fail. Do not add a dump helper.
3. Smallest fix: call the existing clients list / exact-name
   read-back already used by contacts CUD. Only if bind then
   fails after that proved customer, change `bind_kunde` to click
   the **Vælg kunde** textbox and pick the exact existing option.
   Do not click **Opret ny**. Do not add a new diagnostic module.
4. If a verified customer still does not appear in Kunde, stop
   and ask the orchestrator to inspect (`EC676F84`). Do not
   accumulate dumps.
5. After a green live CUD: leave `parity_status` open-only so
   honesty keeps `implemented` / `live_tested` / `vision_verified`
   false. Independent review writes accept, then purge. Remap
   invoice CUD `tool_name` to preview tools only after that, same
   as contacts and bills.

Offline first: unit tests in `tests/unit/test_ui_invoices_writes.py`
must still pass. Commit-mode lint and contract stay green.

## Files

- `tests/live/test_ui_invoices_writes.py` — independent contact
  confirm, then existing CUD, invoice-then-customer cleanup
- `src/billy_mcp/ui_writes/invoices_form_bind.py` — only if bind
  fails after a proved customer
- `wiki/ui_invoices_writes.md` — owner correction, no dropdown probe
- `wiki/ui_write_residual_table.md` — invoice rows become
  actionable / live-proved-honesty-red after proof, not
  `interface_control_absent_UI_CHANGED`
- Memory `state.md` / `todo.md` — present tense

Do not edit `generate_coverage_report.py` honesty IDs. Do not
re-lock official docs. Do not green coverage in this slice.

## Out of scope

- Product persist (`67CBACB6` / `A337A622`)
- Daybook persist token and `_ledger_write`
- Files upload / Bilag delete
- Postings / **Godkend**
- A second copy of `D02702C5`
- New children
- Live API

## Feasibility

No time or cost cap. One live FastMCP slice plus optional bind
tweak. Independent review is a later step. This iteration can
land the failing fixture, the contact confirm, and a real CUD
attempt. If live bind still fails after a proved customer, the
deliverable is the orchestrator ask, not another dump.

## Post-Mortem

Completed:

- Failing unit fixture: live invoice CUD must confirm the tagged
  customer on an independent clients list before invoice preview.
- Live helper `_open_named_list` now waits for networkidle and
  search settle, matching contact execute read-back.
- Live CUD test asserts `submitted=true` then `_list_has_name`
  on `clients`. Cleanup deletes invoice then customer.
- `bind_kunde` clicks **Vælg kunde**, picks an existing option,
  and no longer clicks **Opret ny**.
- Independent clients confirm passed. Invoice create still
  `UI_CHANGED` (`Billy Kunde existing option is not visible`).
- Inspect ask `802D71CF` sent. Independent review FAIL.
- Residual table invoice rows rewritten to
  `kunde_option_missing_after_proved_customer`.
- Leftover `billy-live-invoices-*` profiles purged.
  `chrome-profile` kept.

Deviations:

- Research brief said wait. Owner `EC676F84` unblocked the
  customer-dataset path mid-iteration.
- Bind change did not produce a visible existing option.

Review: FAIL. No greening. Honesty-16 and invoice `tool_name`
stay on `*_open`.

Tests: lint.sh passed after removing unused `_click_named_opener`
and `_click_scoped_footer`. Commit-mode `test.sh` 2065 passed, 82
deselected. Live invoice CUD fails at create execute as expected
until inspect. No `ui-full` run.

Cleanup: no leftover tagged invoice id. Contact delete ran in
finally. Owner-only invoice profiles gone.

Next unresolved slice: owner inspect `802D71CF`. Do not remake
picker dumps. Product, daybook, and files still wait.
`complete` stays false.

