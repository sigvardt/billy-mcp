# 186.76 ui products get update delete not applicable

## Goal

Product research176 ACCEPT:

1. Freeze UI parity **`not_applicable`** for exact API ops:
   - `api.products.get` → `ui.parity.products.get`
   - `api.products.update` → `ui.parity.products.update`
   - `api.products.delete` → `ui.parity.products.delete`
2. Evidence: dual independent headless sessions prove **no equivalent** get-detail,
   update-form, or delete-chrome UI workflow on the dedicated non-production org
   (research176). Soft detail/edit chrome-only; list click is false-green only;
   list Mere is Export/Import only (Slet 0 dual). Inventory create form is
   **create only** (already greened) and must not dual-count as update.
3. Keep greened `ui_products_list` and `ui_products_create_open` unchanged.
   Do **not** dual-count list/create onto get/update/delete.
4. Do **not** green `products.bulk_save` / `products.bulk_delete` (external-contract
   bulk freeze). Do **not** green `special.invoice_email`. Do **not** add new
   `ui_products_get_open` / `update_open` / `delete_open` tools.
5. Peer pattern: research162 productPrices NA and research150 exact special ids
   (operation-scoped, not whole-resource prefix that would steal list/create).

Target math (tip `fdb3824` / product 186.75):
live/vision **175 → 178**; implemented/contract **359 → 362**;
`GEO_UI_NOT_APPLICABLE_ROW_COUNT` **107 → 110**; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research176)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. products Supports get/list/create/update/bulk save/delete/bulk delete. |
| SPA seed dual | product POST 200 dual under committed egress; cleanup DELETE 200; markers gone dual. |
| Soft detail/edit/overview | nav chrome only; marker false; inputs_n 0 dual. |
| List shell | marker true dual; click stays `/:org_slug/products`; weak list-only false-green true dual. |
| List Mere | Eksportér/Importér produkter dual; Slet/Slet produkt/Duplikér/Arkivér **0 dual**. |
| Ret/href/row | no Ret; no product-id links; no detail open dual. |
| Inventory create | dual form fields — create only (already greened). |
| Strong get/update/delete ready | **false dual**. |
| Design | UI parity NA only when no equivalent UI workflow (not annual-style inaccessible nav). |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research176_focus_dual.json`.  
Probe: `tmp/research176_focus_probe.py` (tmp only; not required in git).

## Non-goals

- New FastMCP UI tools for products get/update/delete
- Greening products.get via list row alone
- Greening update via create form dual-count
- Greening delete via export/import Mere
- products.bulk_* greening; productPrices re-open; special.invoice_email
- annual_reports greening; bulk 92 external-contract
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Changing greened list/create tools or egress

## Ownership

| Area | Owner |
| --- | --- |
| Generator NA freeze (exact api.products.get/update/delete) | Root Grok EXECUTE |
| Inventory unit tests (count 110; three rows NA; list/create still non-NA) | Root Grok EXECUTE |
| Wiki NA page | Root Grok EXECUTE |
| Optional dual live NA harness only if peer packages require it | Root Grok EXECUTE (productPrices used inventory dual only — no live test file) |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research176)

### A. Generator (`scripts/generate_coverage_report.py`)

1. Add exact API row ids (not full `api.products.` prefix — that would NA list/create/bulk):

```python
GEO_UI_NOT_APPLICABLE_RESEARCH176_PRODUCT_IDS: frozenset[str] = frozenset(
    {
        "api.products.get",
        "api.products.update",
        "api.products.delete",
    }
)
```

2. Include those ids in `GEO_UI_NOT_APPLICABLE_API_PREFIXES` as exact strings
   (same technique as `api.special.invoice_delivery` / `api.special.invoice_logs`).
3. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **107 → 110**.
4. Extend `apply_ui_geo_reference_not_applicable_evidence`:
   - `is_research176 = api_row_id in GEO_UI_NOT_APPLICABLE_RESEARCH176_PRODUCT_IDS`
   - Branch metadata:
     - research_id = `research176`
     - evidence_ref = `research176_products_get_update_delete_dual`
     - dual_agree_flag / family_label describing products get/update/delete absence
     - contrast shells: products list + products create + invoices/clients (show real shells exist; get/update/delete still absent)
     - evidence prose: soft detail chrome-only; list click false-green; Mere Export/Import only; inventory create is create-only
5. Ensure apply path runs for UI parity rows whose `api_row_id` matches (same hook as other geo NA).
6. **Must not** change list/create dual-count applicators for `ui_products_list` /
   `ui_products_create_open`.

### B. Flags per NA row

| Field | Value |
| --- | --- |
| `parity_status` | `not_applicable` |
| `tool_name` | empty |
| discovered / implemented / contract_tested / live_tested / vision_verified | all **true** |
| `vision_evidence` | null |
| `qualification.kind` | `ui_not_applicable` |
| `qualification.not_applicable_decision` | `accepted` |
| `qualification.sessions` | `dual_independent_ephemeral` |
| `qualification.evidence_ref` | contains `research176` |
| `evidence_code` | `GEO_UI_NO_EQUIVALENT_WORKFLOW` |

### C. Tests (`tests/coverage/test_coverage_inventory.py`)

- Update `GEO_UI_NOT_APPLICABLE_ROW_COUNT` assertion **110**.
- Assert exactly three rows for get/update/delete are NA with research176 evidence.
- Assert `ui.parity.products.list` and `.create` remain **not** `not_applicable`
  (tool-backed green).
- Assert `api.products.bulk_save` / `bulk_delete` UI parity remain **not** NA
  (bulk external-contract / discovery path unchanged — do not steal bulk into this freeze).
- Keep productPrices research162 assertions intact.
- Adjust any aggregate counts that hardcode `107` or `68 + GEO_UI_NOT_APPLICABLE_ROW_COUNT`.

### D. Wiki

- Add `wiki/ui_products_get_update_delete_not_applicable.md` (peer
  `ui_product_prices_not_applicable.md` structure).
- Link contrast: list shell, create open shell, productPrices NA.
- Explicit non-claims: bulk_*, invoice_email, annual, live API.

### E. Live harness

- **No new live tool test** required if peer productPrices path holds (inventory dual
  freeze only). Research dual JSON stays in tmp/owner storage; not committed.
- If generator/tests require a durable non-sensitive dual summary file under
  `tests/` or `coverage/`, add only redacted flags (no tokens, no org PII).

### F. Egress

- **No** `browser_egress.yaml` change.

### G. API lane

- Offline `api.products.get/update/delete` tools unchanged.
- API `live_tested` remains false with `out_of_scope_by_user`.

## Implementation steps (EXECUTE)

1. Patch generator constants + `apply_ui_geo_reference_not_applicable_evidence` research176 branch.
2. Update `tests/coverage/test_coverage_inventory.py` counts and research176 assertions.
3. Write wiki page.
4. Run `python scripts/generate_coverage_report.py` (or project equivalent) to refresh
   `coverage/ui_workflows_manifest.yaml`, `coverage/status.json`, `coverage/report.md`.
5. Run lint + offline test suite (`BILLY_TEST_MODE=commit` or project default offline).
6. Verify math: live/vision 178; implemented/contract 362; complete false; list/create green tools unchanged.
7. Stop for IR (no coverage greening of bulk/invoice_email/annual).

## Verification

| Check | Expect |
| --- | --- |
| docs MD5/ETag | unchanged |
| three products get/update/delete UI rows | NA accepted dual evidence research176 |
| products.list / create | still tool-backed green, not NA |
| products bulk UI rows | not greened by this freeze |
| GEO_UI_NOT_APPLICABLE_ROW_COUNT | 110 |
| complete | false |
| env BILLY_API_TOKEN in product path | unused |
| headed browser | none |

## Risks

| Risk | Mitigation |
| --- | --- |
| Prefix `api.products.` NA steals list/create | Use **exact** ids only |
| Weak list-only get green | Explicit reject; evidence documents false-green risk |
| IR challenges NA because list shows product rows | Design: list is list op; get-by-id has no dual detail workflow (contrast clients get drawer) |
| Bulk accidentally NA | Assert bulk rows not in RESEARCH176 set |

## Post-Mortem

- **Outcome:** DELIVERED. NA freeze exact `api.products.get` + `update` + `delete`
  (research176 dual absence). IR **ACCEPT**. No required fixes. Optional N1
  method_or_route research176 wording applied in FIX-VERIFY; N2 no-op (tmp dual).
- **Coverage delta:** live/vision 175→**178**; implemented/contract 359→**362**;
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT` 107→**110**; complete **false**.
- **Artifacts:** generator research176 branch + exact ids; inventory tests;
  wiki `ui_products_get_update_delete_not_applicable.md`; coverage regenerated.
- **Tests:** lint.sh pass; offline BILLY_TEST_MODE=commit **1642 passed** /
  50 deselected. No ui-full (not complete).
- **Cleanup:** research dual cleanup_all_clean dual true; no production frames.
- **Deviations:** none material. Shared method_or_route template still used for
  other NA families; research176 override only.
- **Next unresolved slice:** special.invoice_email (durable form dual or
  evidence-backed NA); residual discovery_required parity; bulk 92
  external-contract; annual_reports org_inaccessible. No bulk greening without
  official schema. API live_tested remains false (`out_of_scope_by_user`).

## Ready EXECUTE

Root-only Grok. Plan is complete for EXECUTE of NA freeze products.get+update+delete.
