# 198.46 Bind invoice confirm Ja, slet faktura

## Pivot

Owner `D2FD1919`: leftover edit Mere then Slet shows heading
**Bekræft**, text **Vil du slette denne kladdefaktura?**,
button **Annuller**, exact active button **Ja, slet faktura**.
Generic **Ja, slet** is the miss. Bind that exact button after
the `E8EA9823` capture. Confirm only through FastMCP. Require
DELETE 2xx, fresh-session absence, reverse cleanup.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"121myuqjdm53603"`): `DELETE /v2/invoices/:id` has no body and
is idempotent 200 with `meta.deletedRecords.invoices[]`. Official
docs do not name a UI confirm label. Do not re-lock (`728BD2E4`).
Do not remake the post-Slet dump. Do not spawn a child. Do not
create a seventh invoice.

HEAD `c4ae6ab` already has unique **Mere**, `A` **Slet**, and
the overlay candidate dump. Live recapture: `candidates=[]`,
`delete_seen=false`. Empty candidates after those locators is
valid. The dump only lists role=dialog / alertdialog /
aria-modal. Owner still saw **Ja, slet faktura**. Do not wait
for `role=dialog`.

Current fail: `delete_draft_invoice` returns `UI_CHANGED`
"Billy invoice delete confirm is not named." after capture.
`CONFIRM_DELETE` is still generic **Ja, slet** and unused.

## Goal

Delete leftover `MCP-UI-INV-6CDB396B` through FastMCP by
clicking exact **Ja, slet faktura** after capture. Then delete
the other five drafts, six products, and six customers. Prove
empty lists in a fresh session. Stay red.

1. Offline failing fixture: after `capture_post_slet`, delete
   must click exact **Ja, slet faktura**. It must not click
   generic **Ja, slet**. Capture still runs first (`4EAEAFFD`).
2. Smallest fix: `CONFIRM_DELETE = "Ja, slet faktura"`. After
   capture, if `delete_seen` wait persist; else click that
   exact visible button, then `wait_persist(..., "DELETE")`.
   Add the exact token to `_LABEL_ALLOW`.
3. Live FastMCP leftover delete + reverse cleanup.
4. Stay red. Do not remap `ui.parity.invoices.*`. Do not finish.

## Why no child

One family on files the root already owns:

- `src/billy_mcp/ui_writes/invoices_form_page.py`
- `src/billy_mcp/ui_writes/invoices_form_delete.py`
- `src/billy_mcp/ui_writes/invoices_form_delete_dump.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Leftover
cleanup must run on the root live session. Research forbids a
new child. `D68E402A` forbids a new helper.

## TDD

`728BD2E4` / `D2FD1919`: failing fixture first, then the
smallest fix.

1. In `tests/unit/test_ui_invoices_writes.py`, add a fixture that
   `delete_draft_invoice` after `capture_post_slet` references
   `CONFIRM_DELETE` / **Ja, slet faktura** and does not click
   generic **Ja, slet**. Keep the existing capture-first
   assertions. Run it. Capture the fail.
2. Smallest fix:
   - `CONFIRM_DELETE = "Ja, slet faktura"`
   - After capture: if `dump["delete_seen"]` then
     `wait_persist(seen, method="DELETE")`
   - Else `click_exact(page, CONFIRM_DELETE)` then wait persist
   - If the exact button is missing, `UI_CHANGED` with
     sanitized URL, heading, exact-button count, plus dump keys
   - Add `"ja, slet faktura"` to `_LABEL_ALLOW`
   - Update `allowlisted_label` unit assert
   - No `force`. No `evaluate`. No portal sweep.
3. Offline unit/contract/safety for the touched files. Then live
   `tests/live/test_ui_invoices_writes.py` through FastMCP
   `ui_invoices_delete_{preview,execute}` on the six leftover
   contacts, products, then customers. Require DELETE 2xx and
   third-session absence. Do not create a seventh invoice.
4. Vision: `author=live_test`,
   `reviewer_verdict=pending_review`. Do not self-accept. Do
   not purge in the live test.

## Out of slice

Files, daybook, **Godkend**, emails, bulk, docs re-lock, dump
remakes, child spawn, `ui_invoice_lines_*`, live API,
invoiceLines PUT, bills `force=True`, generic **Ja, slet**,
a seventh leftover.

## Success

One FastMCP leftover delete with exact **Ja, slet faktura**,
DELETE 2xx, and empty leftover lists after reverse cleanup.
Honesty-16 stay red. Do not finish.

## Budget

No cost or time cap. This is one TDD slice plus one live
leftover cleanup. Do not expand.

## Post-Mortem

Completed:

- `CONFIRM_DELETE = "Ja, slet faktura"` after the post-Slet dump.
- Offline suite 2101 passed. Lint passed.
- Live FastMCP leftover invoice drafts deleted (`DELETE 200`).
- Product absence is unfiltered `/products` table-item, not body
  text. Write-page waits for the row to leave after **Ja, slet**.
- Live leftover product and customer cleanup passed.

Deviations:

- Independent review failed first on leftover product
  `CONFLICT`. Body-text read-back was a false leftover. After
  the row-scope fix, `MCP-UI-PRD-86AB7365` still needed a
  write-page wait. Then contact `NOT_FOUND` meant already gone.

Review: `tmp/grok-review.md` FAIL on leftover products. That
product leftover is now gone. No invoice vision record. Honesty-16
stay red.

Tests: lint.sh pass. `BILLY_TEST_MODE=commit` test.sh 2101
passed. Live leftover cleanup 1 passed. Did not run `ui-full`.

Cleanup: earlier leftover read-back was unsettled. Owner
`01FCBA13` still has seven leftover triples. Do not treat the
org as empty.

Next slice: product `DELETE /v2/products/:id` 2xx watch
(`A0633A17`), then reverse-clean the seven settled triples,
then independent vision. Stay red. Do not finish.
