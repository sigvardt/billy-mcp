# 198.59 invoiceReminderAssociations ticketed create and update

## Pivot

`2D09964C` is binding under `9B979A05`. HEAD `78a4f2e` is a clean
198.58 bank-payments-delete close, not a `(failed on <step>)`
backstop. Uncommitted files are memory-only from SYNC. Research
brief: `.fractal/main.billy_complete/tmp/grok-research.md`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). `#v2invoicereminderassociations` Supports:
`get by id, list, create, update, bulk save, delete, bulk delete`.
Property table is exactly three rows: `reminder` belongs-to
required, `invoice` belongs-to required, `lateFee` belongs-to
readonly.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Delete stays `meta_delete_unqualified`. Bulk stays
`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`.

Do not spawn. Do not remake UI dumps. Do not invent
`api_invoice_reminder_associations_delete_*` or bulk tools. Do not
fold this into `invoice_reminder_writes.py`. Do not finish. No live
API.

## Goal

Land one proved cohort: ticketed offline
`api.invoiceReminderAssociations.create` and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.invoiceReminderAssociations.create` | `api_invoice_reminder_associations_create_preview` / `_execute` | `POST /invoiceReminderAssociations` | nested `{reminder, invoice}` |
| `api.invoiceReminderAssociations.update` | `api_invoice_reminder_associations_update_preview` / `_execute` | `PUT /invoiceReminderAssociations/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 20 method-closed + 2 readonly
map + 2 meta-delete = 24. Bulk 92 stay red. Implemented 528,
contract 533 (HEAD 526 / 531). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed nested payload copy shape is
`src/billy_mcp/api/account_nature_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_invoice_reminder_association_writes.py`,
   copied from natures for the typed nested payload and from bank
   payments for ticket protocol:
   - Register count is 4. Preview fields:
     `api_invoice_reminder_associations_create_preview`
     `{invoiceReminderAssociation}`,
     `api_invoice_reminder_associations_update_preview`
     `{id, invoiceReminderAssociation}`, both executes
     `{confirmation_ticket}`. Nested properties are exactly
     `{reminder, invoice}`. `additionalProperties` is false on
     outer and nested schemas. No `input` wrapper.
   - Outer extras, empty update `id`, empty `reminder`, empty
     `invoice`, missing either required key, `lateFee`,
     `reminderId`, `invoiceId`, and other inner extras fail
     `ValidationError` / FastMCP `extra_forbidden`. Execute extras
     fail. Preview still makes no HTTP.
   - Preview create sends no HTTP. Canonical request is
     `{"invoiceReminderAssociation": {"reminder": <id>, "invoice": <id>}}`.
     Expected effect `{action: create, resource: invoiceReminderAssociation}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`. Empty nested
     object is invalid because both fields are required (unlike
     natures).
   - Execute POSTs
     `https://api.billysbilling.com/v2/invoiceReminderAssociations`
     with exactly that JSON. Execute PUTs
     `/v2/invoiceReminderAssociations/<urlencoded id>`. Encoded id
     `assoc /?` becomes
     `/v2/invoiceReminderAssociations/assoc%20%2F%3F`.
   - Success fixture maps only declared changed roots plus optional
     `meta.deletedRecords`. Do not expose a top-level
     `invoiceReminderAssociation` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 20`, residual `== 24`. Assert
   `api.invoiceReminderAssociations.create` and `.update` are
   **absent** from `RESIDUAL_METHOD_CLOSED_IDS`. Keep
   `.delete` in `RESIDUAL_META_DELETE_IDS`. Bump every hardcoded
   `implemented_rows == 526` / `contract_tested_rows == 531` in
   that file to **528 / 533** (seven pairs). `complete` stays
   false. Live/vision counts stay 339.
3. Map both ids in `OPERATION_TEST_REFERENCES` to
   `tests/api/test_invoice_reminder_association_writes.py` plus
   `SERVER_REGISTRY_TEST_REFERENCE`. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.

Run:
`uv run pytest tests/api/test_invoice_reminder_association_writes.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/invoice_reminder_association_writes.py` (new):

- `InvoiceReminderAssociationPayload`: `reminder: str`,
  `invoice: str`, both `min_length=1`, `extra=forbid`, frozen.
- Create preview input: `{invoiceReminderAssociation}`.
- Update preview input: `{id min_length=1, invoiceReminderAssociation}`.
- Dump only set documented fields. Both are required, so the
  dumped object always has both keys.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/invoiceReminderAssociations`. Singular root
  `invoiceReminderAssociation`. Plural root
  `invoiceReminderAssociations`.

`src/billy_mcp/server.py`: import and call
`register_invoice_reminder_association_write_tools` next to
`register_invoice_reminder_write_tools`. Do not change reminder
create.

`scripts/generate_coverage_report.py`:

- Remove `api.invoiceReminderAssociations.create` and `.update`
  from `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids.
- Do not touch bulk helpers, the other 20 method-closed ids,
  readonly map, or meta delete.
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 20. These two rows are ticketed
  offline tools. Residual honesty remaining 24. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of `reminder` and `invoice` is enough to
  ship the ticketed POST/PUT. Unauth 405 is not the contract.
  `lateFee` stays readonly and is rejected at the FastMCP
  boundary.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.invoiceReminderAssociations.delete` stays
  `meta_delete_unqualified`.
- `api.invoiceReminderAssociations.bulk_save` / `.bulk_delete`
  stay `ambiguous_bulk`.
- UI parity stays `not_applicable` (research144).
- Do not accept `reminderId` / `invoiceId` aliases.
- Do not allow omitted required keys on update even though the
  intro describes partial updates.
- `complete` stays false.

## Independent review

After EXECUTE, review must re-parse
`#v2invoicereminderassociations` on the locked HTML and confirm:

1. Supports still lists create and update.
2. Property table is still `reminder` required, `invoice`
   required, `lateFee` readonly.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /invoiceReminderAssociations` and
   `PUT /invoiceReminderAssociations/:id`.
6. Nested payload rejects `lateFee` and extra keys.
7. `live_tested` is false.
8. Remaining residual honesty is 24 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.

## Post-Mortem

Work completed: ticketed offline `api.invoiceReminderAssociations.create`
and `.update` in
`src/billy_mcp/api/invoice_reminder_association_writes.py`. Preview
tools take a nested payload of required `reminder` and `invoice`
strings with `extra=forbid`. `lateFee` and GET-style `Id` aliases
fail at the FastMCP boundary. Execute binds
`POST /invoiceReminderAssociations` and
`PUT /invoiceReminderAssociations/:id` through the shared write
protocol. Coverage regenerated: implemented 528, contract 533,
live/vision 339, complete false. Residual honesty remaining 24.

Deviations: ticket and execute contract tests live in
`tests/api/test_invoice_reminder_association_write_tickets.py`
so each test file stays under the 250-line ceiling. Evidence map
names both test files plus the server registry test. No product
deviation from the official field table.

Review findings: independent review PASS. Required fixes none.
`live_probe.py` may still list these ids in the historical unauth
405 matrix. That is not the contract. Inventory qualification is
`live_api_deferred`. Rejected as a fail.

Test results: focused association write, inventory, and registry
tests passed. Default `test.sh` (commit mode, not live, not vision):
2185 passed, 83 deselected. `lint.sh` passed. Wiki lint clean on
project wiki. `BILLY_TEST_MODE=full` was not run because completion
requirements are still false.

Cleanup state: no live records. No frames. No credentials.

Next unresolved coverage slice: ticketed `cities` create/update
(`8C5F08A8`), then remaining geo/country-group writes, readonly
transaction writes, and meta deletes. Residual honesty 24. Bulk 92.
`complete` stays false. Do not finish.

