# 2.5 Wave 2 root integration and bill query correction

## Objective

Expose the three verified Wave-2 filtered read areas through the real stdio
server and record only their real offline implementation/contract evidence.
The completed daybook module is already merged; bill and invoice product modules
must be reviewed from clean archives before integration. This slice must retain
the fail-closed live and interface qualification gate.

## Contract and evidence boundary

- `.fractal/main.billy_complete/tmp/grok-research.md` is the current cited Grok
  brief. Its 10:43Z official-document fingerprint is unchanged:
  `hsisik4g9p3603` / `c2efda0ee4cf9cf200e14910c5fc6996`.
- The frozen Wave-2 wiki contract and the brief define only `GET`/list tools
  for invoices, bills, and daybook transactions. They explicitly exclude
  writes, bulk, specials, uploads, UI workflows, and live claims.
- Bills `q` is a non-empty free-text search term. It searches documented fields
  but is not an enum of those field names; the completed bill branch currently
  violates this and must be corrected before its tools are exposed.
- `BILLY_API_TOKEN` and dedicated authenticated UI credentials remain absent.
  Every `live_tested` and every UI/vision field remains false, and full mode
  must continue to reject incomplete coverage.

## Root execution plan

1. Review the bill and invoice child three-dot diffs and run each focused suite,
   Ruff, formatting, and Pyright from a clean `git archive`. Integrate ready
   branches with labelled no-fast-forward merges. Daybook is already merged.
2. Correct the bill product module and focused test so `q` is `str` with
   `min_length=1`, accepts representative free text, and still rejects unknown
   filters. Do not alter the documented bill sort or state enum allowlists.
3. In `src/billy_mcp/server.py`, import and invoke the bill, invoice, and
   daybook registration functions after the four Wave-1 registrations. Extend
   `tests/unit/test_coverage_server.py` to assert exactly 22 real `api_*`
   tools plus the two `coverage_*` tools, with no stub or generic transport
   capability.
4. Add exactly these six entries to
   `OFFLINE_API_IMPLEMENTATION_EVIDENCE`, each citing its focused API module
   test and the server registry test:
   `api.invoices.get/list`, `api.bills.get/list`, and
   `api.daybookTransactions.get/list`. Regenerate all coverage artifacts using
   the generator; never hand-edit generated manifests or status.
5. Assert the generated phase and report accurately describe 22 offline
   implementation/contract rows but `complete: false`, all live rows false,
   every UI row red, and every ambiguous bulk row red/tool-less.
6. Run the non-live/non-vision suite, full formatter/lint/Pyright/safety
   checks, coverage false-completeness check, repository policy scan, and the
   expected failing full qualification gate. Record the exact remaining block
   rather than treating the expected full-mode non-zero result as success.

## Delegation decision

No new child is spawned for this slice. The invoice leaf has completed its
isolated product files and awaits clean-archive review. The bill correction is
a sub-iteration repair in a completed leaf; root absorbs it because the same
integration must atomically change server registration, root registry tests,
the generator evidence map, and generated coverage. Splitting that coupled
boundary would create merge conflicts and leave a known-invalid tool exposed.

## Acceptance criteria

- The server exposes 22 real typed read-only `api_*` tools plus the two
  coverage tools, including exactly six Wave-2 tools and no writes or generic
  controls.
- Each Wave-2 list forwards only the official filter allowlist, including free
  text `q` for bills; it rejects unsupported fields locally and preserves
  documented singular/list roots and optional paging.
- Exactly 22 API rows have offline implementation and contract-test evidence;
  only the six Wave-2 rows change in this slice. No live, UI, vision, special,
  write, or bulk qualification changes.
- Generated coverage is deterministic and still `complete: false`; the full
  qualification command fails only because the remaining rows lack required
  qualification evidence.

## Post-Mortem

Completed: merged and clean-archive verified the bill and invoice deliveries;
daybook was already merged. Root now registers the six Wave-2 get/list tools,
records exactly their offline implementation and contract-test evidence, and
generates 22 such API rows while preserving `complete: false` and all live/UI
status as false.

Deviation: the completed bill delivery incorrectly modelled `q` as an enum of
the searched field names. Root corrected it to a non-empty free-text search
string and added a representative query test. Independent Grok review then
identified the matching empty-string allowance on invoice `q`; it was reproduced
and corrected with a focused rejection test. Invoice date/time filters remain
opaque documented strings because no live non-production observation defines a
canonical stricter wire format.

Review: Grok accepted the official inventory arithmetic, Wave-2 filter
allowlists, real-tool/real-test evidence, base/egress locks, and fail-closed
coverage behavior. It rejected product completeness: 283 API rows remain
without offline implementation, all live/UI evidence remains absent, and 92 bulk
rows remain officially ambiguous. Those states stay red.

Verification: clean archive checks passed for bill (22 tests) and invoice (14
tests), along with Ruff, formatting, and Pyright. The integrated focused suite
passed 63 tests. Final root lint, type, coverage, and repository-policy checks
passed; the non-live suite passed 128 tests. Full mode continues to fail only at
the required completeness gate (`complete=false`, 92 ambiguous bulk rows, and
incomplete API/UI evidence).

Cleanup: no credential, live organisation data, browser frame, HAR, trace, or
screenshot was created. The next unresolved slice is research-backed Wave-3
unfiltered clear reads, followed by ticketed writes and live/UI qualification
when dedicated credentials are available.
