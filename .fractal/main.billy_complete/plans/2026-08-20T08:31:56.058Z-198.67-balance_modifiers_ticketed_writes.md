# 198.67 balanceModifiers ticketed create and update

## Pivot

`7453B98F` is binding under `9B979A05`. HEAD `b95f96f` is a clean
198.66 zipcodes close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-7453b98f.html`,
`billy-api-docs-chunk-research-7453b98f.js`,
`research-7453b98f-balanceModifiers-section.html`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the zipcodes lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor
`#v2balancemodifiers`. Supports: `get by id, list, create,
update, bulk save, bulk delete`. No singular delete. Property
table is exactly six rows: `modifier` belongs-to-reference
immutable required, `subject` belongs-to-reference immutable
required, `amount` float readonly, `entryDate` date readonly,
`realizedCurrencyDifference` float readonly, `isVoided` boolean
readonly.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.balanceModifiers.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_balance_modifiers_*` or bulk tools. Do not fold this into bank
reads. Do not start `invoiceReminderAssociations.delete`
(`4DBD7C3F`). Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.balanceModifiers.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.balanceModifiers.create` | `api_balance_modifiers_create_preview` / `_execute` | `POST /balanceModifiers` | nested required `{modifier, subject}` |
| `api.balanceModifiers.update` | `api_balance_modifiers_update_preview` / `_execute` | `PUT /balanceModifiers/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 4 method-closed + 2 readonly
map + 2 meta-delete = 8. Bulk 92 stay red. Implemented 544,
contract 549 (HEAD 542 / 547). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed required nested belongs-to strings are
`src/billy_mcp/api/invoice_reminder_association_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_balance_modifier_writes.py`, copied from
   invoice reminder association writes. The source file is 258
   lines; trim so the copy stays under 250:
   - Register count is 4. Preview fields:
     `api_balance_modifiers_create_preview` `{balanceModifier}`,
     `api_balance_modifiers_update_preview` `{id, balanceModifier}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{modifier, subject}`. JSON schema types are string.
     Nested `required` is `{modifier, subject}`.
     `additionalProperties` is false on outer and nested schemas.
     No `input` wrapper.
   - Outer extras, empty update `id`, missing `modifier` or
     `subject`, empty strings, `modifierId`, `subjectId`,
     readonly `amount` / `entryDate` /
     `realizedCurrencyDifference` / `isVoided`, arrays, nested
     objects, and other inner extras fail `ValidationError` /
     FastMCP `extra_forbidden`. Execute extras fail. Preview still
     makes no HTTP.
   - Empty nested object `{"balanceModifier": {}}` is invalid.
     Partial `{"modifier": "modifier-1"}` is invalid. Do not put
     route `id` inside `BalanceModifierPayload`.
   - Preview create sends no HTTP. Canonical request is
     `{"balanceModifier": {"modifier": "...", "subject": "..."}}`.
     Expected effect `{action: create, resource: balanceModifier}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_balance_modifier_write_tickets.py`, copied
   from association ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/balanceModifiers` with the
     exact nested JSON. Execute PUTs
     `/v2/balanceModifiers/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/balanceModifiers/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `balanceModifiers` plus optional `meta.deletedRecords`. Do not
     expose a top-level `balanceModifier` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 4`, residual `== 8`. Assert
   `api.balanceModifiers.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk balanceModifiers rows
   unspecified. Bump every hardcoded `implemented_rows == 542` /
   `contract_tested_rows == 547` in that file to **544 / 549**.
   `complete` stays false. Live/vision counts stay 339.
4. Map both ids in `OPERATION_TEST_REFERENCES` next to existing
   `api.balanceModifiers.get` / `.list`. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_balance_modifiers_writes_are_ticketed_offline_and_not_live_tested`
   next to the zipcodes inventory tests.
6. `tests/unit/test_coverage_server.py`: add
   `BALANCE_MODIFIER_WRITE_API_TOOL_NAMES` (four tools). Bump
   `len(api_tool_names) == 309` to **313**.
   `tests/coverage/test_api_live_qualification_semantics.py`:
   bump `len(implemented) == 203` to **205**.

Run:
`uv run pytest tests/api/test_balance_modifier_writes.py tests/api/test_balance_modifier_write_tickets.py tests/coverage/test_coverage_inventory.py tests/unit/test_coverage_server.py tests/coverage/test_api_live_qualification_semantics.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/balance_modifier_writes.py` (new), copy
`invoice_reminder_association_writes.py`:

- `BalanceModifierPayload`: `modifier: str = Field(min_length=1)`,
  `subject: str = Field(min_length=1)`,
  `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{balanceModifier}`.
- Update preview input: `{id min_length=1, balanceModifier}`.
- Dump with `model_dump()`. Both keys always present.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/balanceModifiers`. Singular root
  `balanceModifier`. Plural root `balanceModifiers`.
  `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_balance_modifier_write_tools` next to
`register_invoice_reminder_association_write_tools`. Do not change
bank reads.

`scripts/generate_coverage_report.py`:

- Remove `api.balanceModifiers.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids after
  `api.balanceModifiers.list`.
- Do not touch bulk helpers, the other 4 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` balanceModifiers create/update true,
  delete false (`("balanceModifiers", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 4. These two rows are ticketed
  offline tools. Residual honesty remaining 8. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of required belongs-to-reference strings
  `modifier` and `subject` is enough to ship the ticketed POST/PUT.
  Reject `modifierId`, `subjectId`, `amount`, `entryDate`,
  `realizedCurrencyDifference`, and `isVoided`. Unauth 405 is not
  the contract. Empty payload is not allowed. Do not invent a
  subject-prefix enum.

Keep `wiki/ui_account_natures_balance_modifiers_not_applicable.md`
as history. Do not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.balanceModifiers.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.balanceModifiers.delete`. Supports does not list
  singular delete.
- UI parity stays `not_applicable` (research142).
- Do not accept `modifierId`, `subjectId`, readonly fields, or a
  JSON object/array for the belongs-to-reference strings.
- Do not make `modifier` or `subject` optional.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.
- Saved `4DBD7C3F` waits until this commit. Saved `9B979A05`
  still owns the remaining 8 residual rows and bulk92. Saved
  `96908DC6` still owns overall finish.

## Independent review

After EXECUTE, review must re-parse `#v2balancemodifiers` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still six fields: required immutable
   belongs-to-reference `modifier` and `subject`; readonly
   `amount`, `entryDate`, `realizedCurrencyDifference`, and
   `isVoided`.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /balanceModifiers` and
   `PUT /balanceModifiers/:id`.
6. Nested payload requires non-empty `modifier` and `subject`.
   It rejects `modifierId`, `subjectId`, readonly fields, arrays,
   nested objects, extra keys, and an empty nested object. JSON
   schema type `string` holds for both fields. Nested `required`
   is `{modifier, subject}`.
7. `live_tested` is false.
8. Remaining residual honesty is 8 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.
   No `invoiceReminderAssociations.delete` work in this slice.

## Post-Mortem

Work completed: ticketed offline `api.balanceModifiers.create` and
`.update` in `src/billy_mcp/api/balance_modifier_writes.py`. Preview
tools take a nested payload of required non-empty belongs-to-reference
strings `modifier` and `subject` with `extra=forbid`. `modifierId`,
`subjectId`, `amount`, `entryDate`, `realizedCurrencyDifference`,
`isVoided`, arrays, nested objects, extra keys, and an empty nested
object fail at the FastMCP boundary. Execute binds
`POST /balanceModifiers` and `PUT /balanceModifiers/:id` through the
shared write protocol. Coverage regenerated: implemented 544,
contract 549, live/vision 339, complete false. Residual honesty
remaining 8.

Deviations: none from the official field table. Registry count
asserts also bumped (`api` tools 313, implemented API rows 205).
Ticket and execute contract tests live in
`tests/api/test_balance_modifier_write_tickets.py` so each test file
stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
`test.sh` `2401 passed, 83 deselected`. No live UI records.
Next unresolved slice after COMMIT: saved `4DBD7C3F` ticketed
singular `invoiceReminderAssociations` delete. Residual honesty
remaining 8. Bulk 92 stay red. `complete` stays false.
