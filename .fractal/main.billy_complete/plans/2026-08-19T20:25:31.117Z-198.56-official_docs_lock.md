# 198.56 official docs lock reconcile

## Pivot

`89DEED22` is binding. HEAD `3aa56ec` is a clean 198.55 close, not
a `(failed on <step>)` backstop. Worktree is clean.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19 GET, two matching
fetches this iteration): HTTP 200, **147924** bytes, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`,
SHA-256 `2004e749155d8b995456364ca821df6af2f42da4740f151730af46e7230ea69b`.
Page chunk `api-docs-2c3fd2ccd1de2fed.js`: **208452** bytes, ETag
`"7902955e4d1a56ecea2d85551f17b49c"`,
`last-modified: 2026-08-19T08:16:31Z`. OpenAPI/Swagger probes 404.

The live page is stable and fully captured (HTML SSR plus page
chunk). `89DEED22` forbids leaving `coverage/status.json` on the
obsolete lock merely because bulk Supports text is unchanged.

Exact docs delta versus lock `wcw4x9hqvu3603` /
`8b94b0135c91fd15fe54ea33e088a4be`: minus 10 bytes. Two intro
copies of `GET /v2/user/organizations` became
`GET /v2/organizations`. That is **prose** for the existing
`api.organizations.list` row. It is not a new inventory row and
does not retarget `api_organizations_list`. Supports lines (46,
four families), bulk92 wording, organizations resource table, and
`https://api.billysbilling.com/v2/user` stay unchanged.

Do not drop `api.special.user_organizations` or
`api_user_list_organizations`. Do not spawn. Do not remake UI
dumps. Do not arm `_ledger_write`. Do not invent bulk or residual
tools. Do not finish. No live API.

## Goal

Promote the official source lock to the captured 2026-08-19 page
without greening coverage:

- Generator `DOCS_ETAG` / `DOCS_MD5` become `tmhc6wpdc835zt` /
  `053f755f52e3926b028e29325e3670d4`.
- `coverage/status.json` `official_docs` matches after regenerate.
- `api.organizations.list` stays `GET /v2/organizations` /
  `api_organizations_list`.
- `api.special.user_organizations` stays
  `GET /v2/user/organizations` / `api_user_list_organizations`.
- Special-row evidence records that live intro prose now cites
  `GET /v2/organizations` and that `/user/organizations` is absent
  from the current official page.
- 92 bulk and 29 residual rows stay unimplemented, empty
  `tool_name`.
- `complete` stays false. API `live_tested` stays false.

## Why no child

Generator constants, special-row evidence, regenerated yaml/json,
wiki Lock paragraph, and inventory tests share
`scripts/generate_coverage_report.py`. A child would fork that
file. Seven write children stay completed and merged. Retired
leftover descendants stay unmerged.

## TDD

Add `tests/coverage/test_official_docs_lock.py`. On HEAD it must
fail. Assert:

1. `generator.DOCS_ETAG == "tmhc6wpdc835zt"`.
2. `generator.DOCS_MD5 == "053f755f52e3926b028e29325e3670d4"`.
3. `generator.DOCS_ETAG_OBSERVED == generator.DOCS_ETAG` and
   `generator.DOCS_MD5_OBSERVED == generator.DOCS_MD5` (lock
   promoted; no remaining drift pair).
4. `coverage/status.json` `official_docs.etag` / `.md5` match those
   values.
5. Manifest `official_docs` matches.
6. `api.organizations.list` `method_or_route` is
   `GET /v2/organizations` and `tool_name` is
   `api_organizations_list`.
7. `api.special.user_organizations` `method_or_route` is
   `GET /v2/user/organizations` and `tool_name` is
   `api_user_list_organizations`.
8. That special row's `evidence` contains both
   `GET /v2/organizations` and `/user/organizations` (intro now
   names the list row; special path kept though absent from the
   current page).
9. Exactly 92 `source_kind=ambiguous_bulk` rows, all with empty
   `tool_name` and `implemented is False`.
10. Every id in `RESIDUAL_CLEAR_HONESTY_IDS` (29) has empty
    `tool_name` and `implemented is False`.
11. `status.complete is False`.
12. All 305 API rows still have `live_tested is False` and
    `qualification.live_api == "out_of_scope_by_user"`.

Run that file first. Capture the expected failure. Then implement.

## Implement

`scripts/generate_coverage_report.py`:

- Set `DOCS_ETAG = "tmhc6wpdc835zt"` and
  `DOCS_MD5 = "053f755f52e3926b028e29325e3670d4"`.
- Set `DOCS_ETAG_OBSERVED` / `DOCS_MD5_OBSERVED` to the same
  values. Update the comment: lock promoted to the captured
  2026-08-19 page.
- After `base_api_row` for `api.special.user_organizations`,
  append one evidence sentence: live intro lists user companies
  via `GET /v2/organizations` (`api.organizations.list`);
  `GET /v2/user/organizations` is absent from the current official
  page; tool path kept. Do not change `method_or_route` or
  `tool_name`.
- Do not rewrite `api_organizations_list` paging or path.
- Do not touch bulk or residual honesty helpers.

Run `python scripts/generate_coverage_report.py` so yaml/json/report
regenerate. Do not edit `coverage/status.json` by hand.

`wiki/api_live_qualification_semantics.md` Lock paragraph: the
inventory lock is now ETag `tmhc6wpdc835zt` / MD5
`053f755f52e3926b028e29325e3670d4`. Intro prose cites
`GET /v2/organizations` for the existing list row. Special
`/user/organizations` is kept. Do not invent bulk tools. No
iteration labels.

## Verify

```
uv run pytest tests/coverage/test_official_docs_lock.py \
  tests/coverage/test_api_live_qualification_semantics.py \
  tests/coverage/test_coverage_inventory.py \
  tests/api/test_bootstrap_reads.py -q
uv run ruff check scripts/generate_coverage_report.py \
  tests/coverage/test_official_docs_lock.py
uv run pyright scripts/generate_coverage_report.py \
  tests/coverage/test_official_docs_lock.py
```

Do not run `ui-full`. Do not run live API tests.

## Out of slice

- Dropping or retargeting `api_user_list_organizations`
- Changing `api_organizations_list` path or paging
- Bulk tools or residual 29 tools
- Live UI / live API
- `fractal node finish`

Independent review after EXECUTE. Then COMMIT.

After the slice: `complete` still false. Blocker text can stay the
bulk freeze. The 121 offline blockers remain red.

## Post-Mortem

Landed. Independent review PASS. No required fixes.

Work completed:

- Failing `tests/coverage/test_official_docs_lock.py` first, then
  generator lock promote and special-row evidence.
- `DOCS_ETAG` / `DOCS_MD5` are `tmhc6wpdc835zt` /
  `053f755f52e3926b028e29325e3670d4`. Observed pair collapsed.
- `api.organizations.list` unchanged. `api.special.user_organizations`
  kept on `GET /v2/user/organizations` with evidence that live intro
  cites the list row.
- Regenerated yaml/json. `complete` stays false.
- Wiki Lock paragraph updated.
- FIX-VERIFY: special evidence attaches by row id, not `rows[-1]`.

Deviations: none on the lock. Observed-constant equality is
tautological by design after collapse; status/manifest still assert
the live literals.

Review findings: none required. Residual id-lookup applied.

Tests: lint.sh pass. `BILLY_TEST_MODE=commit` test.sh 2134 passed,
83 deselected on EXECUTE and again on FIX-VERIFY. `ui-full` /
`full` not run because completion requirements are not met.

Cleanup: no Billy records, no frames.

Next slice (`9B979A05`): split the 121 by evidence, not one
permanent-red bucket. 25 method-closed vs current Supports, then
readonly/meta-delete from official tables, then bulk92 from
official assets plus static first-party clients. Smallest proved
cohort. Unresolved stay red. No live API. Do not finish.

