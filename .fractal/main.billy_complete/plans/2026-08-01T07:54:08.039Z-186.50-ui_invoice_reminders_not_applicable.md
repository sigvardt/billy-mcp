# 186.50 ui invoiceReminders not_applicable freeze


## Goal

Product research149 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved UI parity rows for
   **invoiceReminders only** (**5 rows total**):
   - get, list, create, bulk_save, bulk_delete
   - (matches official docs Supports line; **no** singular update/delete)
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so `api.invoiceReminders.` is included.
3. Updates inventory tests for NA count 93→**98** and live/vision baseline
   135→**140** (`42` shell/parity dual-counts + `98` NA).
4. Adds a scrubbed wiki page for the freeze; one-line cross-link from peer NA
   wiki pages (contactPersons / research144 join NA including
   invoiceReminderAssociations / geo).
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   this family.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research149).  
Dual evidence (tmp, non-git): `tmp/research149_residual_dual.json`,
`tmp/research149_shell_markers_dual.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–148).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–40 geo/currencies; 186.43–44 account natures/groups;
186.45 contactBalancePostings + invoiceLateFees + invoiceReminderAssociations;
186.48 contactBalancePayments; 186.49 contactPersons; 186.46–47 dual-count
accounts.list / organizations.list (shells stay at 42).

## Why this slice

1. research148 greened contactPersons NA only; deferred invoiceReminders as
   secondary NA-ready with the same soft-empty class.
2. research149 dual-probed residual soft seeds after 186.49: soft
   `/invoiceReminders` (and kebab/DA `rykkere` / `reminders`) body_len **127**,
   empty h1, dual-agree identical to nonsense `zz-research149-no-such-route`.
3. Contrast: dual `ui_invoices_list` READY as **Fakturaer** shell
   (`/:org_slug/invoices`); shell-marker dual on `/invoices` shows
   **Rykker / Rykkere / Morarente markers dual false** (faktura/kunde true).
   That shell is not an invoiceReminders API workflow.
4. Associations already greened NA (research144 / 186.45) — this package is the
   parent resource only.
5. Soft URL landing is not a productable rykker list/create shell — do not invent
   UI tools.
6. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class as
   research144–148 join/meta packages).
7. Explicit rejections/deferrals stay red this slice:
   - specials delivery/logs/user_orgs (settings Levering ambiguity)
   - attachments pure NA (Bilag greened, bilag/upload markers dual true)
   - taxRateDeductionComponents / taxRates (Momssatser multi-resource)
   - productPrices (`/products/new` non-empty dual)
   - bankLine*/bankPayments (bank shells greened)
   - daybook* (daybooks editor greened; list dual-count weak)
   - postings (no steal from transactions.list)
   - files (Bilag weak map)

## Non-goals

- Shipping any new FastMCP tools (`ui_invoice_reminders_*`).
- Greening soft-empty paths as list shells.
- Dual-counting invoiceReminders onto `ui_invoices_list` (different resource;
   rykker markers dual false → NA, not dual-count).
- Greening specials / attachments / taxRate* this slice.
- Dual-counting daybooks.list / taxRates / files / bankLines / postings /
  productPrices this slice.
- Re-touching invoiceReminderAssociations (already NA green).
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Changing offline API invoiceReminders tools (API lane separate; live_tested
  stays false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.invoiceReminders.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 98` (prior 93 + **5** ops). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH149_RESOURCES = frozenset({"invoiceReminders"})`
  (mirror research148 set). Prefer **same evidence code**
  `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. Update header
  comment that research149 adds invoiceReminders (5 ops). |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research149 family: evidence_ref
  `research149_invoice_reminders_dual`; nonsense path
  `zz-research149-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_invoiceReminders`; contrast controls include
  `ui_invoices_list` (Fakturaer), and other greened dual-ok tools from
  research149 (clients, uploads, settings). Keep research138–148 branches
  intact. Order branch checks so research149 is evaluated before older
  catch-alls (same pattern as 148→147). |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefix alone should wire all
  **5** parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count uses `GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **98**.
  - research149 evidence_ref branch assertions (mirror research148).
  - live/vision `42 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **140** when
    ROW_COUNT=98.
  - Assert five `api.invoiceReminders.*` UI parity rows are NA green.
  - Keep research148 contactPersons asserts green.
  - Keep invoiceReminderAssociations already NA green (research144).
  - annual still NA-rejected red; bulk 92 red; no new tools for this family;
    attachments/specials/productPrices/bankLine*/bankPayments stay red as
    intended this slice.
  - Replace prior "invoiceReminders stays red" assertion from 186.49 with
    research149 green package assert. |
| Wiki | New `wiki/ui_invoice_reminders_not_applicable.md` (scrubbed dual
  evidence; no credentials/slugs/frames). Update `wiki/_index.md`. One-line
  peer cross-link from
  `wiki/ui_contact_persons_not_applicable.md` and/or
  `wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`. |
| Coverage regenerate | Run generator → refresh `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md`. implemented/contract may move
  with UI NA greening (historical pattern: 319→~324). complete=false; API
  live_tested false. |

## EXECUTE steps (ordered)

1. Patch `scripts/generate_coverage_report.py` constants + research149 branch in
   applicator (prefix, ROW_COUNT 98, RESEARCH149 set, evidence_ref, contrast).
2. Patch `tests/coverage/test_coverage_inventory.py` for 98 / 140 / five-row
   invoiceReminders asserts + research149 evidence_ref; flip prior red assert;
   keep older package tests.
3. Add wiki freeze page + `_index` + peer cross-link(s).
4. Regenerate coverage artifacts via project generator/check scripts as used in
   186.49 (do not hand-edit YAML status).
5. Run lint + offline `test.sh` (no live API; live UI suite not required for this
   inventory-only freeze before IR, matching 186.48–186.49 pattern).
6. Leave residual dual JSON in owner tmp only (not commit).

## Acceptance

- Five `ui.parity.invoiceReminders.*` rows: discovered/implemented/contract_tested/
  live_tested/vision_verified all true; `parity_status=not_applicable`; empty
  tool_name; evidence_code GEO_UI_NO_EQUIVALENT_WORKFLOW; evidence_ref contains
  research149; not_applicable_decision accepted.
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 98`; live/vision status 140.
- No new ui_* tools; no invoiceReminders dual-count onto invoices.
- attachments / specials / bank* / productPrices / daybooks / taxRates residual
  red as intended.
- invoiceReminderAssociations remains NA green (unchanged).
- complete=false; bulk 92 red; annual red; API live_tested false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Risks

| Risk | Mitigation |
| --- | --- |
| Dual-count onto invoices by mistake | Assert empty tool_name + not_applicable; invoices shell stays invoices.list only |
| Greening specials with invoiceReminders | Explicit non-goal; assert special reds remain |
| Confusion with invoiceReminderAssociations | Separate resource prefix; associations already green; package is parent only |
| ROW_COUNT drift | Single constant drives tests + status formula |
| Shell soft-empty alone without marker isolation | research149 shell-markers dual already proves rykker markers false on /invoices |
| invoices shell body_len dual variance (298 vs 280) | Markers dual-agree on rykker false; do not require body_len equality for isolation claim |

## Post-mortem

- **Outcome:** Producted research149 invoiceReminders GEO NA freeze (5 ops).
  IR **ACCEPT** with no required fixes. Offline suite and lint green.
- **Actual live/vision:** 135 → **140** (42 shells + 98 NA).
- **Actual NA count:** 93 → **98**.
- **Actual implemented/contract:** 319 → **324**.
- **complete:** still **false** (expected).
- **Tests:** lint.sh pass; test.sh offline **1565 passed**, 35 deselected.
- **Deviations:** none vs plan. Optional IR N1–N3 deferred (probe shell_paths
  TargetClosedError; invoices body_len dual variance; specials Levering defer).
- **Cleanup:** no disposable Billy records (observe-only research). Profiles
  purged during research. No credentials/frames in repo.
- **Next unresolved coverage slice:** residual UI parity — specials isolation
  after Levering, taxRateDeduction/taxRates multi-resource, files/Bilag map,
  bank recon match isolation, postings no-steal, productPrices nested, daybooks
  dual-count weak map; bulk 92 external-contract and annual_reports
  org_inaccessible remain red.
- **Not done:** node finish; complete true; live API; invent UI tools.
