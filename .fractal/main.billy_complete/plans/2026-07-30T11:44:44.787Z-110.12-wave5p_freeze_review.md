# 110.12 Wave-5p organizations freeze independent review

## Decision

The organizations create/update freeze is on root but is not independently
accepted. Its cited research68 package is current and establishes that the
product surface must remain blocked until an independent Grok review reaches an
explicit verdict. This review is independent of the Wave-5o product review and
has disjoint wiki ownership, so both reviewers will run in parallel.

## Frozen review baseline and evidence

- Root baseline: `885269f4ecbf40cf2fd78f24035174b5c1fc15c3`.
- Freeze page: `wiki/wave_fivep_ticketed_writes_contract.md`, content MD5
  `2742eda7bafecd619aba5fa8ad0694c5`.
- Current cited package: `.fractal/main.billy_complete/tmp/grok-research.md`
  (research68); official body ETag `wcw4x9hqvu3603`, 147934 bytes, MD5
  `8b94b0135c91fd15fe54ea33e088a4be`.
- Locked-base probes use JSON `{}` with no credentials: organizations POST and
  PUT return 401 `AUTHENTICATION_REQUIRED`; singular DELETE returns 405
  `METHOD_NOT_ALLOWED`.
- Exact future surface: organization create/update preview and execute twins
  only. Bulk rows stay empty-tool red; no organization-delete, webhook, live,
  UI, vision, or completeness claim is permitted.

## Child contract

In EXECUTE, configure and launch a leaf with the explicit Grok route:

```text
fractal node init wave5p_freeze_review --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete --agent=grok --max-cost=320 --max-iter-cost=80 --max-iters=8 --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
```

The child owns only `wiki/wave_fivep_freeze_independent_review.md`, its
generated project-wiki index row, and its own seed. It must independently
verify the freeze against the cited official documentation and method gates;
the strict opaque outer inputs; the four exact future tool names and paths;
the required `organizations` root; ticket expiry/single-use/exact bindings;
the DELETE-405 cleanup restriction; high-sensitivity card-data redaction; and
the exclusion of bulk, generic transport, webhooks, live, UI, vision, and
completeness claims. It may report `ACCEPT` only if the contract is faithful and
the root remains fail-closed; otherwise record actionable defects. It must not
edit product code, tests, coverage, status, source contracts, or the Wave-5o
review. If Grok fails before edits solely from authentication, quota, or rate
limiting, retry the entire review with `--agent=codex-power`; never switch after
edits.

## Parent follow-through

After completion, inspect and merge the review record only, regenerate/lint
the wiki, and verify the verdict against the fixed root baseline. Only an
authoritative `ACCEPT` on root authorizes a later Codex Power leaf to implement
the four organizations create/update tools from research68. A research result,
freeze page, child completion, or non-authoritative review does not authorize
that implementation.
