# 186.41 ui salesTaxReturns.list dual-count via ui_vat_declarations_list

## Goal

Dual-count green **`ui.parity.salesTaxReturns.list`** by mapping the already
qualified read-only **`ui_vat_declarations_list`** Momsangivelser shell to
**`api.salesTaxReturns.list`**, using the same empty-input list-shell-open-only
contract already live for `ui.discovery.vat_declarations`.

No new browser workflow. No new API/UI tools. No get/update/bulk greening.
No annual product. No bulk external-contract greening.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research140).  
Dual evidence (tmp, non-git): `tmp/research140_sales_tax_returns_parity_dual.json`.  
Discovery product already green: `ui_vat_declarations_list` (research120 /
prior 186.21); wiki `wiki/ui_vat_declarations_list_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_vat_declarations_list.py`,
vision record class `tmp/vision-records/ui_vat_declarations_list.json`.  
API offline list already green: `api.salesTaxReturns.list` →
`api_sales_tax_returns_list` (`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–140).  
Prior dual-count precedents: invoices/products/contacts/bills/transactions
`.list` via `parity_of_api_list=True` in `scripts/generate_coverage_report.py`.

## Why this slice (research140)

1. research139 deferred dual-count residual; dual session research140 closed it:
   - Typed tool dual READY + success (heading Momsangivelser, period chrome).
   - Direct `/:org_slug/vat-declarations` dual h1 match; soft aliases dual
     soft-empty body_len 127; nested detail seeds not list shell.
   - No write markers; no API token; no writes.
2. Same inventory dual-count pattern as `ui.parity.transactions.list`.
3. get/update/bulk UI parity remain red (no dual-proved shells; bulk
   external-contract on API lane).
4. One new green UI row only; no new tool surface.

## Non-goals

- Inventing `api_vat_*` or second UI list tool for salesTaxReturns.
- Greening `ui.parity.salesTaxReturns.get|update|bulk_save|bulk_delete`.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Implementing API list filters/sort/pagination UI (shell-open only).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running live UI suite unless IR or inventory demands it (reuse research120
  live qualification + research140 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_vat_declarations_list_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror transactions); wire
  `api.salesTaxReturns.list` in parity builder with
  `parity_of_api_list=True`; discovery path keeps default False |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.salesTaxReturns.list` → `ui_vat_declarations_list`; qualified
  count 38→39 where that constant is shell-only; live/vision total
  **80→81**; assert evidence mentions `api.salesTaxReturns.list` and
  filters/sort/pagination not producted; assert get/update/bulk salesTaxReturns
  parity stay red |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_vat_declarations_list_shell.md` — dual-count note for
  `api.salesTaxReturns.list` / list only; non-claims get/update/bulk; remove
  blanket “does not re-scope salesTaxReturns” wording that blocks dual-count |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_vat_declarations_list`
- models / server registration
- `tests/live/test_ui_vat_declarations_list.py`
- unit browser tests for the VAT shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_vat_declarations_list` (existing) |
| UI path class | `/:org_slug/vat-declarations` (optional query allowed) |
| Success h1 | `Momsangivelser` |
| Input | empty strict model (no filters) |
| Soft aliases | reject (vat, moms, sales-tax-returns, underscore forms, etc.) |
| Nested detail/write seeds | not list success |
| CTA / write | observe-only; never declare/submit/export/download |
| Parity row | `ui.parity.salesTaxReturns.list` / `api_row_id=api.salesTaxReturns.list` |
| parity_status | `list_shell_open_only` |
| API invent | never invent `api_vat_*` |
| Distinct shells | `ui_settings_vat_open` (Momssatser) stays separate |
| get/update/bulk UI | stay red |

## EXECUTE steps

1. **Read tip product:** `apply_ui_transactions_list_shell_evidence(..., parity_of_api_list=...)`
   and `apply_ui_vat_declarations_list_shell_evidence` as templates.
2. **Patch generator**
   - Signature:
     `apply_ui_vat_declarations_list_shell_evidence(row, *, parity_of_api_list: bool = False)`.
   - When `parity_of_api_list` is false (discovery): keep `api_row_id=None`;
     evidence: research120 + product; soft aliases; empty list valid; does not
     green annual/exports/settings; does not green get/update/bulk salesTaxReturns
     parity (list dual-count is separate call path).
   - When true: do not null `api_row_id`; append
     `; maps api.salesTaxReturns.list to UI list-shell open only` and
     filters/sort/pagination UI not producted clause (match invoices/transactions).
   - Parity loop: after transactions dual-count branch, add
     `if api_row["id"] == "api.salesTaxReturns.list": apply_...(row, parity_of_api_list=True)`.
3. **Patch inventory tests**
   - Add id/tool mapping for `ui.parity.salesTaxReturns.list`.
   - Bump shell-qualified count if hardcoded 38→39; overall live/vision
     80→81 if asserted.
   - Assert dual-count evidence strings.
   - Assert remaining `ui.parity.salesTaxReturns.*` (except list) stay
     discovery_required / red.
4. **Wiki** update `ui_vat_declarations_list_shell.md` dual-count + non-claims;
   `_index` only if description must change.
5. **Regenerate coverage** via project’s usual generator entry
   (`uv run python scripts/generate_coverage_report.py` or existing make/script).
6. **Verify offline:** lint.sh + offline test.sh (expect ~1565+ pass class);
   `check_coverage` if separate; complete remains false.
7. **Do not** hand-edit green flags in YAML.

## REVIEW / COMMIT gates

- IR: dual-count integrity (one row; tool reuse; get/update/bulk red; bulk 92
  red; annual red; complete false).
- FIX-VERIFY offline pass; then COMMIT + push. Not node finish.

## Expected coverage after product

| Metric | Before | After (expected) |
| --- | ---: | ---: |
| UI live/vision | 80 | **81** |
| Shell tools greened | 38 | 38 (no new tool) |
| Dual-count parity lists | 5 | **6** (+salesTaxReturns.list) |
| NA reference rows | 42 | 42 |
| complete | false | false |
| bulk external-contract | 92 | 92 |
| annual discovery | red | red |

## Risks

| Risk | Mitigation |
| --- | --- |
| Evidence text still says “does not re-scope salesTaxReturns” and fails IR | Rewrite discovery vs parity evidence carefully |
| Hardcoded qualified count 38/80 drift | Grep inventory tests for 38/80 and update only intentional counters |
| Accidental greening of get/update | Explicit red assertions on non-list salesTaxReturns parity |
| Complete true false positive | Leave bulk/annual blockers; do not touch completeness policy |

## Post-mortem

### Completed

- Dual-count greened `ui.parity.salesTaxReturns.list` onto existing
  `ui_vat_declarations_list` via `parity_of_api_list` on
  `apply_ui_vat_declarations_list_shell_evidence` (research140).
- Wired `api.salesTaxReturns.list` in UI parity builder.
- Inventory tests: qualified shells 38→39; live/vision 80→81; get/update/bulk
  salesTaxReturns parity stay red with assertions.
- Wiki `ui_vat_declarations_list_shell.md` + `_index` dual-count note.
- Regenerated coverage: implemented/contract **265**; live/vision **81**;
  complete **false**.
- No new FastMCP tools; no live API; no browser/server product changes.

### Deviations

- None material vs plan 186.41.
- Optional IR N1 (live re-run of VAT list test) deferred — inventory dual-count
  reuses research120 live + research140 dual proof (same as transactions dual-count).
- Optional IR N2 (test comment wording) deferred non-blocking.

### Review

- Independent review: **ACCEPT** (`tmp/grok-review.md`). Required fixes: none.
- Overall completeness: **FAIL** (expected).

### Tests / cleanup

- lint.sh: pass (wiki lint clean project + memory).
- test.sh offline: **1565 passed**, 35 deselected (EXECUTE + FIX-VERIFY).
- No disposable Billy records; no new vision frames; no secret material in diff.
- Did not run `BILLY_TEST_MODE=full` / ui-full: completeness requirements not met.

### Next unresolved coverage slice

1. Residual UI parity dual-counts / dual-proved NA or shells (beyond
   salesTaxReturns.list).
2. `ui.discovery.annual_reports` stays red (`ANNUAL_REPORTS_ORG_INACCESSIBLE`).
3. 92 ambiguous bulk API rows stay external-contract red
   (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`).
4. Residual non-bulk UI writes without dual-proved shells stay red.
5. API `live_tested` remains false with `out_of_scope_by_user`.


## Done when

- Generator dual-counts `ui.parity.salesTaxReturns.list`.
- Inventory tests green offline; live/vision 81.
- Wiki records dual-count + non-claims.
- complete false; no new tools; no live API.
- IR ACCEPT (or required fixes applied).
- Committed and pushed. Not finish.
