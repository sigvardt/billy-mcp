# 186.89 UI product-plane bulk chrome dual NA soft tool


## Goal

Product research189 as one **root-only** inventory package: promote the **soft tool dual-absent bulk-chrome** subset of remaining product-plane UI bulk parity honesty rows to evidenced UI `not_applicable`.

| Subset | Count | Treatment |
| --- | ---: | --- |
| Soft tool dual-absent (real greened tool panels) | **9 resources / 18 UI rows** | `parity_status=not_applicable`; empty `tool_name`; all five UI flags **true** (NA bookkeeping, geo/research188 style); qualification `ui_not_applicable` + `UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL` |
| Empty-shell dual-absent (contacts/invoices/bills + lines) | **5 / 10** | **Hold** research187 honesty (`discovery_required` + `UI_BULK_CHROME_DUAL_REQUIRED`); optional later empty-list package |
| Research188 strong dual-NA already producted | **15 / 30** | Unchanged; keep `UI_BULK_CHROME_ABSENT_DUAL` |
| API bulk | **92** | Unchanged `external_contract_blocker` |
| Residual clear | **29** | Unchanged honesty red |
| annual_reports | **1** | Unchanged org_inaccessible |

No FastMCP bulk tools. No live API. No headed browser. Not node finish. `complete` stays **false**.

### Exact 9 resources (research189 §4.1 / recommended_soft_na_resources)

```
salesTaxAccounts
salesTaxMetaFields
salesTaxPayments
salesTaxReturns
salesTaxRules
salesTaxRulesets
taxRateDeductionComponents
taxRates
users
```

### Exact 18 row ids

For each resource `R` above:

```
ui.parity.<R>.bulk_save
ui.parity.<R>.bulk_delete
```

Linked API rows (stay red, empty tool):

```
api.<R>.bulk_save
api.<R>.bulk_delete
```

### Panel binding (research189 dual)

| Panel / greened tool | Path class after re-open | Resources |
| --- | --- | --- |
| `ui_settings_vat_open` → re-open hub `/settings` + Momssatser | `/:org_slug/settings` | taxRates, taxRateDeductionComponents, salesTaxRules, salesTaxRulesets, salesTaxAccounts, salesTaxMetaFields, salesTaxPayments |
| `ui_settings_users_open` → re-open hub + Brugere | `/:org_slug/settings` | users |
| `ui_vat_declarations_list` → re-open `/vat-declarations` | `/:org_slug/vat-declarations` | salesTaxReturns |

Notes for evidence text:

- Soft SPA seeds (`/settings/vat`, `/settings/users`, `/vat-returns`) remain soft-empty == nonsense (research188); tool open is required and dual-proved.
- VAT panel may show preference toggles (checkbox_visible 2, rowish 0, tables 0) — **not** row multi-select bulk chrome (same class as settings_accounting in research188).
- Product tools close their Playwright page in `finally`; research189 re-opened panels for bulk metrics after dual tool success.

### Explicitly excluded from this package (REJECT if NA'd)

| Resource | Why held |
| --- | --- |
| contacts, invoices, invoiceLines, bills, billLines | Dual still on `/…/empty` list shells only; greened create tools are open-only (no durable disposable populate this pass). Use optional later package `UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST_SHELL` only with IR accept. |

## Why this slice (research189)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged; bulk Supports-only 46+46 |
| Dual sessions | Independent ephemeral READY/READY; org non-prod; `api_token_used=false`; no writes/submits; profiles purged |
| Survey | `tmp/research189_bulk_followon_dual.json` — soft_tool `bulk_chrome_absent_dual_real_tool_panel` **9**; empty_shell `bulk_chrome_absent_dual_empty_list_shell` **5**; present **0**; disagree **0** |
| Design §10.2 | UI bulk NA only with dual evidence no multi-select / bulk-action chrome on real panels — satisfied for soft subset |
| STEER `1E6AF3C9` | Dual-session probes required before inventory-only honesty; soft 18 now dual-proved via tools |
| Baseline | After 186.88: live/vision **316**, implemented/contract **500**, UI bulk honesty remaining **28** |

## Non-goals (REJECT triggers)

- Setting any of the 18 to product tools or non-empty `tool_name`
- NA of the held 10 empty-shell rows under strong / tool-panel evidence codes
- Greening API bulk 92, residual clear 29, or annual_reports
- Claiming `complete:true` or running ui-full as finish
- Live API traffic; headed browser; disposable records left behind
- Demoting already-green non-bulk UI rows or research188 strong 30
- Shipping any `ui_*_bulk_*` / `api_*_bulk_*` tools
- Spawning children (single-generator + tests + wiki ownership)

## Target coverage math (this package alone)

| Metric | Before | After |
| --- | ---: | ---: |
| live/vision | 316 | **334** (+18) |
| implemented/contract | 500 | **518** (+18) |
| UI product-plane bulk honesty remaining | 28 | **10** (empty-shell only) |
| complete | false | **false** |

Completeness walls still open after land: API bulk 92 + residual clear 29 + empty-shell UI bulk 10 + annual_reports 1.

## Files to touch (EXECUTE)

| Path | Change |
| --- | --- |
| `scripts/generate_coverage_report.py` | Add frozensets (9 resources / 18 UI ids); shell/panel map; `ui_bulk_chrome_absent_dual_tool_panel_qualification()`; `apply_ui_product_plane_bulk_chrome_dual_na_soft_tool(workflows)` **after** honesty apply **and after** `apply_ui_product_plane_bulk_chrome_dual_na_strong`; update `qualification_blocker` text to remaining UI bulk ×10 (empty-shell only) + annual; keep API bulk / residual text |
| regenerate | Run coverage generator so manifests + `coverage/status.json` + `coverage/report.md` match |
| `tests/coverage/test_coverage_inventory.py` | New `test_ui_product_plane_bulk_chrome_dual_na_soft_tool_rows`; update strong/honesty tests so remaining honesty after strong+soft = **10** empty-shell ids only; assert soft 18 NA; strong 30 still NA; API bulk 92 still blocked; counts **334/518** complete false |
| `wiki/ui_product_plane_bulk_chrome_dual_na_soft_tool.md` | Durable dual-NA page (peer of strong dual-NA + honesty); not product ACCEPT |
| `wiki/ui_product_plane_bulk_chrome_dual_na_strong.md` | Note soft 18 lifted by research189; empty 10 still held |
| `wiki/ui_product_plane_bulk_parity_inventory_honesty.md` | Note 18 more rows lifted; 10 remain discovery_required |
| `wiki/offline_write_probe_rules.md` | One-line cross-link if needed |
| `wiki/_index.md` | Link + desc (or fractal commit regenerate) |
| `.fractal/.../memory/state.md` | EXECUTE/IR notes |

Do **not** invent product `src/` tools for bulk. Do **not** green coverage outside the 18 UI bulk rows.

## Implementation steps (EXECUTE order)

1. **Constants** next to research188 strong helpers:
   - `UI_BULK_CHROME_DUAL_NA_SOFT_TOOL_RESOURCES: frozenset[str]` (exact 9 camelCase names)
   - `UI_BULK_CHROME_DUAL_NA_SOFT_TOOL_IDS` derived as `{f"ui.parity.{r}.bulk_save", f"ui.parity.{r}.bulk_delete"}` — assert len == 18
   - `UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL_EVIDENCE_CODE = "UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL"`
   - `UI_BULK_CHROME_DUAL_NA_SOFT_TOOL_PANELS: dict[str, dict[str, str]]` mapping resource → `{shell_id, tool_ref, path_class}` from research189
2. **Invariants on constants:**
   - soft resources ∩ strong resources == empty
   - soft ids ⊂ `UI_PRODUCT_PLANE_BULK_HONESTY_IDS`
   - soft resources ∩ empty-shell held set == empty
   - len(soft resources) == 9; len(soft ids) == 18
3. **Qualification builder** (mirror research188 / geo NA fields):
   - `kind: ui_not_applicable`
   - `evidence_code: UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL`
   - `not_applicable_decision: accepted`
   - `sessions: dual_independent_ephemeral`
   - `evidence_ref: research189_bulk_followon_dual`
   - `docs_md5` / `docs_etag`: locked fingerprint (`8b94b0135c91fd15fe54ea33e088a4be` / `wcw4x9hqvu3603`)
   - `linked_api_row_id: api.<R>.bulk_*`
   - `tools_allowed: false`
   - `empty_list_shell: false`
   - `tool_panel: true`
   - panel/tool_ref from map above
4. **`apply_ui_product_plane_bulk_chrome_dual_na_soft_tool(workflows)`**:
   - For each row id in soft frozenset:
     - force `parity_status=not_applicable`
     - set five UI flags true; `tool_name=""`
     - set qualification + method_or_route + evidence citing research189 dual tool success + re-open bulk-chrome absence (no multi-select bulk chrome; preference toggles non-bulk; no writes)
     - `vision_verified` true with `vision_evidence` null (DOM dual only; research took no frames)
   - Do **not** touch empty-shell honesty rows or research188 strong rows beyond applying after them
5. **Wire** in `build_ui_manifest` **after** `apply_ui_product_plane_bulk_chrome_dual_na_strong` so soft NA wins over discovery_required for the 18 without disturbing the strong 30.
6. **Blocker string** refresh: replace remaining UI bulk `×28 discovery_required (empty-shell 10 + soft VAT/users 18)` with `×10 discovery_required (empty-shell 10) UI_BULK_CHROME_DUAL_REQUIRED`; keep API bulk / residual / annual text.
7. **Regenerate** coverage artifacts via existing generator entrypoint used by tests/package (same as 186.88).
8. **Tests** (offline inventory only for this package gate):
   - Soft package: exact 18 NA + evidence_code + evidence_ref + empty tool + flags true + linked API still red/blocked
   - Remaining honesty after strong∪soft: exactly the 10 empty-shell bulk ids still `discovery_required` / `UI_BULK_CHROME_DUAL_REQUIRED`
   - Strong package still 30 NA
   - Status: implemented/contract **518**, live/vision **334**, complete **false**
   - No live ui-full required for package land
9. **Wiki**: new soft-tool dual NA page + honesty/strong cross-links; no product ACCEPT claim.
10. **IR / COMMIT**: independent review against research189 JSON + docs fingerprint; commit-mode lint + offline tests; not node finish.

## Verification checklist (FIX-VERIFY / IR)

| Check | Pass criteria |
| --- | --- |
| Docs fingerprint | MD5/ETag still locked match |
| Soft 18 | NA + empty tool + five flags true + `UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL` + `research189_bulk_followon_dual` |
| Empty 10 | still discovery_required honesty |
| Strong 30 | still NA with research188 evidence |
| API bulk 92 | still external_contract_blocker; no tools |
| Counts | 518 / 334 / complete false |
| Secrets | no credentials, frames, HAR, tokens in repo |
| Tools | no new bulk FastMCP tools |

## Out of scope follow-ons (next plans, not this EXECUTE)

1. Optional `ui_product_plane_bulk_chrome_dual_na_empty_list` for contacts/invoices/bills(+lines) with empty-list evidence code + IR gate.
2. Residual clear 29 and API bulk 92 (docs/policy walls).
3. annual_reports org unlock.

## Children

None. Root-only generator/tests/wiki ownership; same as research188 package.

## Done when

- Soft 18 rows producted NA with dual tool-panel evidence
- Tests green offline (commit-mode)
- Wiki durable record present
- Status 518/334 complete false
- Empty 10 + API bulk + residual + annual still honest red
- Ready FIX-VERIFY / IR / COMMIT 186.89; **not** node finish

## Sources

1. `.fractal/main.billy_complete/tmp/grok-research.md` (research189)
2. `.fractal/main.billy_complete/tmp/research189_bulk_followon_dual.json`
3. Plan precedent: `plans/*-186.88-ui_product_plane_bulk_chrome_dual_na_strong.md`
4. `scripts/generate_coverage_report.py` strong dual-NA apply
5. `wiki/ui_product_plane_bulk_chrome_dual_na_strong.md`
6. https://www.billy.dk/api/ (docs lock)
7. Design §10.2 UI parity NA rule

## Post-Mortem

### Work completed

- Producted research189 soft tool dual-absent bulk chrome NA package for exact **9 resources / 18 UI bulk rows** (`UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL`).
- Generator: `UI_BULK_CHROME_DUAL_NA_SOFT_TOOL_*` constants, panel map, qualification builder, `apply_ui_product_plane_bulk_chrome_dual_na_soft_tool` wired after honesty + strong dual-NA.
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` raised 240 → **258** (+18).
- Coverage regenerated: implemented/contract **518**, live/vision **334**, complete **false**; blocker UI bulk remaining **×10 empty-shell**.
- Inventory tests updated + new `test_ui_product_plane_bulk_chrome_dual_na_soft_tool_rows`.
- Wiki: `ui_product_plane_bulk_chrome_dual_na_soft_tool.md` + honesty/strong/index/offline cross-links.

### Deviations

- None material vs plan 186.89. Empty-shell package intentionally not landed (held for later IR/optional package).
- No disposable create/populate for empty lists (greened create tools remain open-only).

### Review findings

- Independent review **PASS (package)**; **node complete FAIL** expected.
- Report: `.fractal/main.billy_complete/tmp/grok-review.md`
- Required fixes: **none**.

### Test results (FIX-VERIFY)

- `bash scripts/lint.sh` — pass
- `BILLY_TEST_MODE=commit bash scripts/test.sh` — **1670** passed, 54 deselected
- Did **not** run `ui-full` / `full` (complete remains false; live UI suite not required for inventory package land)
- Wiki lint: project wiki + memory — no issues

### Cleanup state

- Research189 dual profiles purged; no disposable org records created; no frames/HAR/secrets in package.

### Next unresolved coverage slice

1. UI product-plane bulk empty-shell honesty **×10** (contacts/invoices/bills + lines) — optional empty-list dual-NA package or disposable populate product
2. API bulk **×92** external-contract wall
3. Residual clear honesty **×29**
4. annual_reports org_inaccessible **×1**

Node finish blocked until those walls close under scoped qualification policy.
