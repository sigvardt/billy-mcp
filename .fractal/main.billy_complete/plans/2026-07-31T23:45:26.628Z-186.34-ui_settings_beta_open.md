# 186.34 ui settings beta open Betas panel with dual live and vision

## Goal

Ship the next **read-only betas settings open-shell** FastMCP tool for Billy's
Indstillinger **Betas / Tidlig adgang** panel, dual-confirmed in research133 as
final path `/:org_slug/settings` opened via hub open then observe-only side-nav
click **`Betas`**, with h1 **`Indstillinger`** and required content h2 pair
**`Betas`** + **`Tidlig adgang`**, distinct from company / Regnskab / Faktura /
Profil / Momssatser / Brugere / Adgangsnøgler panels and from Abonnement empty
chrome, with offline contract tests, dual-session live interface tests (no API
token), and a non-sensitive vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research133).  
Discovery: `.fractal/main.billy_complete/tmp/discovery133/summary.json` +
`summary_dual_settings_click.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; dual click path/h1/h2 match for Betas;
frames purged; PII scrubbed in discovery summaries.  
Prior product tip: settings access token 186.33 (`79e81ae`)
`ui_settings_access_token_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–133).

**Research133 decision:** product **must** open via authenticated hub
`/:org_slug/settings` then exact-text / role click of side label **`Betas`**
(observe-only; never write CTAs). Success requires **strict h2 pair `Betas` +
`Tidlig adgang`** and **must reject** company, accounting, invoicing,
user/Profil, vat/Momssatser, users/Brugere, access_token/Adgangsnøgler, and
empty subscription chrome. Soft seed `settings/betas` dual-opens beta and
rewrites to hub path — optional dual observation only; **not** the product
contract (prefer hub+click for same pattern as access_token/users/vat/user).
Other beta-ish seeds (`settings/beta`, `settings/labs`,
`settings/experimental`, `settings/tidlig-adgang`, `settings/early-access`)
soft-empty or wrong — **reject** as sole success. No official settings or beta /
early-access CRUD API — do **not** invent `api_settings_*`, `api_beta_*`, or
`api_feature_flag_*`. This product is **open-shell classification only**.
Historical beta copy may match bare `Opret` substring — **observe-only**, never
click. Empty active-beta list + “Ingen aktive betaer er tilgængelige nu.” is a
valid open shell in the test org.

## Why this slice

- UI live/vision is 35 (through settings access token).
- Research133 dual headless sessions:
  - Click **Betas** from hub → path `/:org_slug/settings`, h1 `Indstillinger`,
    dual path/h1/h2 match true.
  - Panel h2 dual-matched pair: `Betas`, `Tidlig adgang`.
  - Body chrome (scrubbed): side rail + Betas / Betatester prose + Tidlig adgang
    + “Ingen aktive betaer…” + historical beta blurbs; form_count 0;
    input_count 8 (chrome; not write forms).
  - Soft seed `settings/betas` dual beta_panel_success true (path rewrites to
    hub) — note only; product still uses hub+click.
  - Soft seeds `settings/beta`, labs, experimental, tidlig-adgang, early-access
    soft-empty — **reject**.
  - Hub default company panel — **reject**.
  - Control clicks same sessions: Adgangsnøgler → access_token true (beta
    false); Brugere → users true; Virksomhed → company true; Abonnement →
    empty h2 weak chrome (stays red).
  - Must **not** use loose body/side-nav “Betas” rail labels as success on other
    panels (prior false-positive lesson).
  - subscription empty chrome and annual_reports Upsedasse stay red.
- Official docs: beta / betas / tidlig / early access / feature flag **absent**.
  No invent API tools.
- Target still red: `ui.discovery.settings_beta` only for this product.
- Hard ban: never click Opret* / Gem* / Tilføj* / Slet / Upload / Opgrader* /
  Invitér* / Overdrag* / Opret adgangsnøgle.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening other `settings_*` rows (subscription) or re-greening
  company/accounting/invoicing/user/vat/users/access_token from this panel open.
- Producting settings write / ticketed enable-or-toggle beta workflows.
- Inventing any `api_settings_*`, `api_beta_*`, `api_feature_flag_*`, or webhook
  tools.
- Treating bare hub open (company default) or soft-empty beta seeds as success.
- Producting annual_reports or Abonnement empty chrome this slice.
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings access token open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_beta_open.py` (new; pattern from access_token open; hub+click Betas; hard-ban Opret*; reject company default + accounting + invoicing + user + vat + users + access_token + soft empty aliases) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_beta` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_beta_open_shell_evidence` for
  **`ui.discovery.settings_beta`** only; wire family `settings_beta` in
  discovery apply path; ensure `build_browser_egress` retains prior lives
  (including settings company + accounting + invoicing + user + vat + users +
  access_token) + appends beta open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings access_token, users,
  vat, user, invoicing, accounting, company, inventory, integrations, addons,
  saft_exports, exports, vat_declarations, reports, transactions, daybooks,
  financing, bank recon, receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_beta_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, subscription
product, or children this iteration.

## Contract freeze (research133 beta / Betas open shell)

### Tool

- **Name:** `ui_settings_beta_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, write payload, or secret
  token fields.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf; SPA does **not** keep `/settings/beta` soft-empty path).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_beta`.
  - `beta_panel_markers_present`: bool true when required markers are
    visible — require h2 set includes **both** **`Betas`** and
    **`Tidlig adgang`** (strict pair; research133 dual).
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** both classify as
    the active panel signature.
  - Must be **distinct from accounting**: accounting panel trio
    (`Regnskab` + `Køb` + `Kontoplan`) must **not** classify as success.
  - Must be **distinct from invoicing**: invoicing panel
    (`Faktura` + `Produkter` + (`Betalingsmetoder` or `Standard fakturalogo`))
    must **not** classify as success.
  - Must be **distinct from user/Profil**: user panel
    (`Profil` + `Billede` + `Sprog og tema` + `Skift adgangskode`) must
    **not** classify as success.
  - Must be **distinct from vat/Momssatser**: vat panel triad
    (`Regelsæt` + `Satser for salg` + `Satser for køb`) must **not** classify
    as success.
  - Must be **distinct from users/Brugere**: users panel pair
    (`Brugere` + `Revisorer og bogholdere`) must **not** classify as success.
  - Must be **distinct from access_token/Adgangsnøgler**: access-token panel
    (h2 includes `Adgangsnøgler` without beta pair) must **not** classify as
    success.
  - Must be **distinct from subscription empty chrome**: empty h2 / no panel
    markers after Abonnement click must **not** classify as success.
  - Optional observe-only: empty active-beta copy present (“Ingen aktive
    betaer…”) is fine; **no page dump**, no person names, no emails, no org
    secrets.
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path or wrong panel
    (`settings/beta` soft-empty, `settings/labs`, `settings/experimental`, …)
  - hub open but **wrong panel** (company default; Regnskab; Faktura; Profil;
    Momssatser; Brugere; Adgangsnøgler; empty Abonnement chrome)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
- **Never:** click Opret* / Gem* / Tilføj* / Upload / Slet / Opgrader* /
  Invitér* / Overdrag* / Opret adgangsnøgle; invent `api_settings_*` or
  `api_beta_*` tools; treat company, accounting, invoicing, user, vat, users,
  or access_token open as this tool's success; use loose body/side-nav chrome
  labels (Betas in rail only) as beta success without h2 pair; log or return
  secrets.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Open hub `https://mit.billy.dk/{org_slug}/settings` (company default is
   expected intermediate; **not** success).
3. Observe-only click exact side label **`Betas`** (prefer role link/button,
   fall back to exact text). Never click write CTAs.
4. Do **not** accept soft seed `settings/beta` (labs/experimental/…) alone as
   success. Soft seed `settings/betas` may open beta dual (research133) but
   product path remains hub+click. Do **not** accept bare hub company default.
5. Independent second session (fresh profile) re-opens hub + clicks Betas and
   asserts path / h1 Indstillinger / h2 Betas + Tidlig adgang.
   **No** API read-back. **No** `BILLY_API_TOKEN`.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers / Ejere |
| Accounting panel | seed `settings/accounting` / Regnskab+Køb+Kontoplan |
| Invoicing panel | seed `settings/invoicing` / Faktura+Produkter+… |
| User/Profil panel | hub+Profil / Billede+Sprog og tema+Skift adgangskode |
| VAT/Momssatser panel | hub+Momssatser / Regelsæt+Satser for salg+Satser for køb |
| Users/Brugere panel | hub+Brugere / Brugere+Revisorer og bogholdere |
| Access-token panel | hub+Adgangsnøgler / h2 Adgangsnøgler |
| Soft empty paths | `settings/beta`, `settings/labs`, `settings/experimental`, `settings/tidlig-adgang`, `settings/early-access` |
| Other hub panels | Abonnement empty chrome |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, other settings_* tool success |

### CTA hard ban (tests must prove no click)

- Opret* / Tilføj* / Gem ændringer / Gem / Save  
- Slet / Delete / Upload / file input activate  
- Opgrader / Invitér* / Overdrag* / Opret adgangsnøgle  

Observation of CTA **text** is allowed (bare `Opret` may appear in historical
beta copy). **Click count for write CTAs must stay 0.** The only allowed
navigation click is the side label **`Betas`** (and only as open path).

### Sensitivity

- Scrub opaque long tokens, emails, person names from vision/logs/body asserts.
- Empty active beta list is valid open-shell success (research133 test org state).

## EXECUTE steps (root)

1. **Models** — `UiSettingsBetaOpenInput` (empty, forbid extra),
   `UiSettingsBetaOpenSuccess` (`path_class`, `heading=Indstillinger`,
   `shell_kind=settings_beta`, `beta_panel_markers_present`).
2. **Browser** — protocol + `BrowserRuntime.ui_settings_beta_open`:
   hub open → click Betas → strict signature (h2 pair Betas + Tidlig adgang;
   reject other panel signatures); `UI_CHANGED` on miss; never write CTA click.
   Mirror access_token open structure; reuse settings hub URL helpers.
3. **Server** — register tool with empty input schema; inject service seam.
4. **Offline tests** — models round-trip; browser unit success + AUTH_REQUIRED +
   UI_CHANGED for company/users/access_token/wrong panel/error shell; server
   registration + call.
5. **Coverage** — inventory map `ui.discovery.settings_beta` →
   `ui_settings_beta_open`; generator evidence apply for that row only;
   regenerate manifests/status/report (**complete stays false**).
6. **Egress** — append `tests/live/test_ui_settings_beta_open.py` to existing
   host refs without dropping prior live tests (especially access_token +
   users + vat + user + company + accounting + invoicing).
7. **Live dual + vision** — dual sessions open beta; DOM assert path/h1/h2 pair;
   independent second session read-back; vision accept + purge_verified under
   owner tmp (not git); frames purged after review.
8. **Wiki** — `wiki/ui_settings_beta_open_shell.md` contract summary.
9. **Offline suite** — non-live pytest green before REVIEW; live beta test
   green when credentials available.

## Verification gates

| Gate | Pass criteria |
| --- | --- |
| Offline unit/models/server | new beta open tests pass |
| Non-live suite | full offline suite still green |
| Live dual | both sessions success; path/h1/h2 pair match; no API token |
| Independent read-back | second session only (no API) |
| Vision | durable record + purge_verified; no frames in repo |
| Coverage row | only `ui.discovery.settings_beta` greened for live/vision |
| Egress integrity | prior settings lives still referenced |
| Safety | no credentials/tokens/frames/HAR in git; no invent API tools |
| Completeness | `complete: false` still (subscription + annual + bulk + parity reds) |

## Risks

| Risk | Mitigation |
| --- | --- |
| False positive from side-nav “Betas” chrome on other panels | Require h2 pair Betas + Tidlig adgang; reject access_token/users/company signatures |
| Soft seed `settings/betas` vs click-nav | Document dual seed; product uses hub+click only |
| Bare “Opret” CTA false alarm | Observe text allowed; assert zero write clicks |
| Egress drop of access_token live | Explicit retention list in generator/tests |
| Greening subscription/annual | Explicit non-goals + inventory asserts |

## Out of scope after this product

- `ui.discovery.settings_subscription` (empty chrome dual; stays red)
- `ui.discovery.annual_reports` (inaccessible freeze; stays red)
- UI parity bulk rows still red
- 92 ambiguous bulk API reds
- Node finish / complete true

## Post-Mortem

- **What landed:** `ui_settings_beta_open` product (models, browser hub+click Betas,
  server registration, offline unit/model/server tests, live dual + vision,
  coverage generator evidence for `ui.discovery.settings_beta` only, egress
  append without drop, wiki shell contract).
- **Deviations:** Soft seed `settings/betas` dual-opens beta (research133) but
  product uses hub+click only (same pattern as access_token/users). No invent
  API tools.
- **Review findings:** IR ACCEPT; required fixes none; optional N1 applied
  (live vision body also asserts `Tidlig adgang`).
- **Test results:** lint pass; offline `scripts/test.sh` 1553+ passed (pre-N1);
  live dual pass; vision purge_verified. FIX-VERIFY reconfirm after N1.
- **Cleanup:** no disposable records; frames purged outside repo.
- **Next unresolved coverage slice:** `ui.discovery.settings_subscription`
  (empty chrome dual stays red) and `ui.discovery.annual_reports` (inaccessible
  Upsedasse freeze stays red); residual bulk API 92 reds; UI parity bulk red.
  Not node finish; `complete: false`.

