# 198.37 invoice priced line then draft CUD

## Pivot

Owner radio `1F1B34F8` supersedes treating persist `POST /v2/invoices`
**422** as a Billy UI blocker. Kunde bind on a fresh `/invoices/new`
plus `[data-cy='dropdown-icon']` stays proved (`9310BC17`,
`vendor_bind=scoped:existing_option`). The 422 body carried
`grossAmount=0`. That is invalid zero-value fixture data after the
bind.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official `invoiceLine.unitPrice` is required. `quantity` defaults to
1. `description` is optional. `product` is required in the official
schema. Support starts the line at **Vælg produkt**, then check
**Antal** and **Enhedspris**. Persist in that article is **Godkend**;
qualification uses **Gem som kladde** only. Do not re-lock docs
(live ETag `"121myuqjdm53603"`). No live API.

198.36 already remounted the create page and bound Kunde. Its
post-mortem: guessed `quantity` / `unitPrice` name fills did not
change the 422 and were reverted. This slice must not repeat those
guessed selectors. Use owner-visible labels first.

Do not remake picker, CTA, Ember, fiber, listener, archive, or
Kunde dumps (`113F1E05`, `D68E402A`, `A337A622`). Do not click
**Opret ny** on **Vælg produkt**. Do not persist a product
(`67CBACB6`).

## Goal

Land a priced draft invoice line through FastMCP after the proved
Kunde bind:

1. Offline failing fixture: draft create must reject an unpriced
   line (`unit_price` missing or `<= 0`).
2. Add a required positive `unit_price` to create (and update)
   preview. Bind it on the ticket. Keep tagged
   `line_description` for read-back.
3. After the proved chevron Kunde bind: keep **Evt. beskrivelse**;
   confirm **Antal** is 1 or fill 1; fill **Enhedspris** with a
   reversible positive price using the owner label (not a guessed
   `name=`). Bind **Vælg produkt** only if an existing option is
   already visible. Never **Opret ny**.
4. Live FastMCP draft CUD. Independent read-back after each step.
   Delete invoice first, customer second. Prove both absences on a
   third session.
5. Never **Godkend**, Send, or email.

Stay red. Honesty-16 stays false until independent accept and
purge. Do not finish the node.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `wiki/ui_invoices_writes.md`

The seven write children are completed and merged. Spawning would
split a live proof the root must run anyway.

## TDD

`728BD2E4` / `1F1B34F8`: failing fixture first, then the smallest
fix.

1. Add an offline test in
   `tests/unit/test_ui_invoices_writes.py` that
   `ui_invoices_create_preview` without a positive `unit_price`
   returns `VALIDATION_ERROR` (missing or `<= 0`). Current
   `InvoiceCreatePreviewInput` has no price field, so today's
   `_create_args()` preview is the first fail. Run that unit test.
   Capture the fail.
2. Smallest model fix in `invoices.py`: required `unit_price` with
   `gt=0` on create and update preview. Put it on
   `_create_request` / `_update_request` so the ticket binds the
   exact price. Update the registered-property set. Keep
   `line_description`. Refuse **Godkend** / send / email as now.
3. Smallest form fix in `invoices_form.py` /
   `invoices_form_bind.py`: after proved Kunde bind, fill
   **Enhedspris** from the ticket price using the owner label
   (`get_by_label` / exact **Enhedspris**). Confirm **Antal** is 1
   or fill 1 the same way. Keep `fill_line` for the tagged
   description. Do not restore the reverted guessed
   `invoiceLines.0.unitPrice` click unless a scoped live inspect
   on the already-open form names that `name` after a label fill
   fails (`1F1B34F8` inspect, `D68E402A` no new helper module).
4. **Vælg produkt**: if a unique existing option is already
   visible after the price fill, pick it. If the picker is closed
   and persist still 422s on missing `productId`, one
   dropdown-icon click on that line control (same proved pattern
   as Kunde, not a new dump) then pick one existing option. Never
   **Opret ny**. If no existing product exists, record
   `67CBACB6` overlap, delete any customer, stop. Do not create a
   product.
5. Live
   `test_ui_invoices_create_update_delete_via_call_tool` must
   send the positive `unit_price`. Keep customer-first plus fresh
   `/invoices/new`. Cleanup invoice then customer even on fail.
6. After a green live CUD: leave `parity_status` open-only.
   Honesty keeps `implemented` / `live_tested` /
   `vision_verified` false. Independent review writes accept,
   then purge, then remap `tool_name` to preview tools.

Offline first: existing unit tests stay green except the new
failing fixture until the fix. Commit-mode lint and contract stay
green. Do not run live API tests.

## Files

- `tests/unit/test_ui_invoices_writes.py` — unpriced-line reject
- `src/billy_mcp/ui_writes/invoices.py` — `unit_price` on preview
  and ticket
- `src/billy_mcp/ui_writes/invoices_form.py` — pass price into fill
- `src/billy_mcp/ui_writes/invoices_form_bind.py` — label fill for
  **Enhedspris** / **Antal**; existing-option product only
- `tests/live/test_ui_invoices_writes.py` — pass positive price
- `wiki/ui_invoices_writes.md` and
  `wiki/ui_write_residual_table.md` — priced-line fact, stay red
- Memory `state.md` / `todo.md` — present tense

Do not edit `generate_coverage_report.py` honesty IDs. Do not
re-lock official docs. Do not green coverage. Do not add a
diagnostic module.

## Out of scope

- Product persist (`67CBACB6` / `A337A622`)
- Daybook persist token and `_ledger_write`
- Files upload / Bilag delete
- Postings / **Godkend**
- A second copy of `D02702C5`
- New children
- Live API
- Owner inspect of routine UI
- Repeating 198.36 guessed `name=` quantity / unitPrice fills
  without a new named locator

## Feasibility

No time or cost cap. One live FastMCP slice plus the preview
price field. Independent review is a later step. This iteration
can land the failing fixture, the ticketed price, the label fill,
and a real CUD attempt. If persist still 422s after a positive
**Enhedspris** and no existing product is visible, the
deliverable is that exact `UI_CHANGED` / `67CBACB6` overlap, not
another dump.

## Post-Mortem

Completed:

- Failing fixture: create preview rejects missing / `0` / `-1`
  `unit_price`. FastMCP preview without the field is rejected.
- `InvoiceCreatePreviewInput` and `InvoiceUpdatePreviewInput`
  require `unit_price > 0`. The ticket binds that price.
- `fill_priced_line` fills **Evt. beskrivelse**, **Antal**, and
  **Enhedspris**. Live GET then included `grossAmount=1`.
- `_bind_existing_product` opens **Vælg produkt** and refuses
  **Opret ny**. Live: no existing option. `UI_CHANGED`.
- Residual create/update/delete rows now share
  `owner_decision_67CBACB6`. Honesty-16 and `tool_name` stay on
  `*_open`.
- Parent radio `6997BB50` states the overlap.

Deviations:

- First live persist was still `POST 422` after `grossAmount=1`.
  That is missing `product`, not a zero price. Second live run
  stopped before POST when the product option was absent.
- Did not remap coverage. Did not click **Opret ny**. Did not
  persist a product.

Review: FAIL. Priced-line contract accepted. Invoice CUD not
done. Required narrative fix applied. Stay red.

Tests: lint.sh passed after typing `_request_unit_price`.
Commit-mode `test.sh` 2069 passed. No `ui-full`. No live API
token. `complete` stays false.

Cleanup: live `finally` deleted the tagged customer. No invoice
id. Stay red.

Next slice: existing product option, or an owner answer to
`67CBACB6`. Then FastMCP draft CUD, independent read-back,
accept, purge, remap.

