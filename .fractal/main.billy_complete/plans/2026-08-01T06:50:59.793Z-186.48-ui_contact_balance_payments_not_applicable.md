# 186.48 ui contactBalancePayments not_applicable freeze

## Goal

Product research147 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved UI parity rows for
   **contactBalancePayments only** (**6 rows total**):
   - get, list, create, update, bulk_save, bulk_delete
   - (no singular delete in inventory; matches API Supports / inventory shape)
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so `api.contactBalancePayments.` is
   included.
3. Updates inventory tests for NA count 80→**86** and live/vision baseline
   122→**128** (`42` shell/parity dual-counts + `86` NA).
4. Adds a scrubbed wiki page for the freeze; one-line cross-link from the
   research144 join/meta NA wiki page.
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   this family.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research147).  
Dual evidence (tmp, non-git): `tmp/research147_soft_routes_dual.json`,
`tmp/research147_residual_parity_dual.json`.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–146).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–40 geo/currencies; 186.43–44 account natures/groups;
186.45 contactBalancePostings + invoiceLateFees + invoiceReminderAssociations
(`wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`);
186.46–47 dual-count accounts.list / organizations.list (shells stay at 42).

## Why this slice

1. research146 greened organizations.list dual-count only; deferred
   contactBalancePayments NA (secondary) and multi-surface dual-counts.
2. research147 dual-probed residual soft seeds after 186.47: soft
   `/contactBalancePayments` body_len **127**, empty h1, dual-agree identical to
   nonsense `zz-research147-no-such-route`.
3. Contrast: dual `ui_debtor_balances_list` / `ui_creditor_balances_list` READY
   as **balance** shells (h1 Tilgodehavender / Skyldige udgifter); soft
   debtorbalance/creditorbalance routes dual-non-empty with **Betaling marker
   false**. Those shells are not a contactBalancePayments workflow.
4. Soft URL landing is not a productable payment shell — do not invent UI tools.
5. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class as
   research144 join/meta package, which already NA'd contactBalance**Postings**).
6. Explicit rejections stay red: productPrices (`/products/new` non-empty dual),
   bankLineMatches/bankPayments (bank shells greened), daybook* (daybooks
   editor greened), salesTax*/taxRates (Momssatser greened), postings (no steal
   from transactions.list), files/attachments (Bilag weak map), daybooks.list
   dual-count (editor-not-list weak map).

## Non-goals

- Shipping any new FastMCP tools (`ui_contact_balance_payments_*`).
- Greening soft-empty paths as list shells.
- Dual-counting daybooks.list onto `ui_daybooks_open` (editor ≠ list).
- Dual-counting taxRates/files/bankLines/postings/productPrices this slice.
- **bankPayments** pure NA (bank accounts/recon greened).
- **productPrices** NA (research147 reconfirm: `/products/new` dual body 190).
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Changing offline API contactBalancePayments tools (API lane separate;
  live_tested stays false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with
  `api.contactBalancePayments.`; set
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 86` (prior 80 + **6** ops). Add
  `GEO_UI_NOT_APPLICABLE_RESEARCH147_RESOURCES = frozenset({
  "contactBalancePayments"})` (mirror research144 set). Prefer **same evidence
  code** `GEO_UI_NO_EQUIVALENT_WORKFLOW` for minimal checker impact. |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research147 family: evidence_ref
  `research147_contact_balance_payments_dual`; nonsense path
  `zz-research147-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_contactBalancePayments`; contrast controls include
  debtor_balances, creditor_balances, and other greened dual-ok tools from
  research147. Keep research138–144 branches intact. Order branch checks so
  research147 is evaluated before older catch-alls (same pattern as 144→143). |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; prefix alone should wire all
  **6** parity rows — verify in `build_ui_manifest`. |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count uses `GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **86**.
  - research147 evidence_ref branch assertions (mirror research144).
  - live/vision `42 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **128** when
    ROW_COUNT=86.
  - Assert six `api.contactBalancePayments.*` UI parity rows are NA green.
  - annual still NA-rejected red; bulk 92 red; no new tools for this family;
    productPrices/bankLine*/bankPayments/salesTax*/postings/daybooks parity
    still red where intended (not greened by this package). |
| Wiki | New
  `wiki/ui_contact_balance_payments_not_applicable.md`
  (scrubbed) + sorted `_index` link. Cross-link from
  `wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`
  (peer join/meta NA; contactBalance**Postings** ≠ Payments). |
| Coverage regenerate | Run generator → refresh `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md`. implemented/contract may move
  with UI NA greening (expect UI green +6; API totals unchanged). |

## Execute steps (root)

1. Extend generator prefixes, ROW_COUNT 86, RESEARCH147 resources, research147
   evidence branch in `apply_ui_geo_reference_not_applicable_evidence`.
2. Update inventory tests (research147 branch + CBP six-row asserts; live/vision
   math via constant).
3. Add wiki page + `_index` desc; optional peer cross-link.
4. Regenerate coverage inventory.
5. Run lint + offline test suite (no live API; no ui-full required this slice).
6. Confirm status: live/vision **128**; GEO NA **86**; shells **42**;
   complete **false**.

## Inventory targets (post-product)

| Metric | Before | After |
| --- | ---: | ---: |
| UI live_tested / vision_verified | 122 | **128** |
| GEO / join NA rows | 80 | **86** |
| Qualified UI shells (dual-count + discovery) | 42 | **42** (unchanged) |
| `ui.parity.contactBalancePayments.*` green | 0 | **6** (all NA) |
| API live_tested | 0 (out_of_scope_by_user) | unchanged |
| complete | false | **false** |
| Bulk external-contract red | 92 | 92 |
| annual_reports | red org_inaccessible | red |

## Row contract (each of 6)

| Field | Value |
| --- | --- |
| id | `ui.parity.contactBalancePayments.<op>` |
| api_row_id | `api.contactBalancePayments.<op>` |
| tool_name | empty |
| parity_status | `not_applicable` |
| discovered/implemented/contract_tested/live_tested/vision_verified | true |
| vision_evidence | null |
| qualification.evidence_code | `GEO_UI_NO_EQUIVALENT_WORKFLOW` |
| qualification.not_applicable_decision | `accepted` |
| Evidence | research147 dual soft-empty + balance-shell contrast; no Betaling dual marker |
| API lane | clear ops offline green where already producted; bulk red external-contract; live_tested false |

## Independent review focus

- Confirm research147 dual proves soft CBP empty == nonsense and balance shells
  dual-ok without Betaling resource markers.
- Confirm only contactBalancePayments six ops greened; bankPayments and
  contactBalancePostings (already NA) not confused.
- Confirm live/vision 128 math: 42 shells + 86 NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.
- Confirm complete false; bulk 92 and annual still red.

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects NA because balance create CTA exists | Explicit non-claim: Opret/Bogfør on balance shells is balance workflow, not contactBalancePayments; Betaling dual false; soft CBP empty |
| Confusing Postings vs Payments | Separate wiki; evidence_ref research147; inventory asserts only Payments prefix |
| Accidentally greening bankPayments | Prefix only `api.contactBalancePayments.`; bankPayments stays red |
| Count drift (80→86, 122→128) | Drive tests off `GEO_UI_NOT_APPLICABLE_ROW_COUNT` and `42 + ROW_COUNT` |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **128**; complete **false**.
- Ready independent review of NA package 186.48.
- Not node finish.

## Post-Mortem

### Work completed

- Extended `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with `api.contactBalancePayments.`.
- Set `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 86` (prior 80 + 6).
- Added `GEO_UI_NOT_APPLICABLE_RESEARCH147_RESOURCES` and research147 branch in
  `apply_ui_geo_reference_not_applicable_evidence` (evidence_ref
  `research147_contact_balance_payments_dual`).
- Inventory tests: research147 evidence_ref branch; six-row CBP package asserts;
  live/vision 122→128; related families (bankPayments, daybooks, postings,
  taxRates, files, …) stay red.
- Wiki: `ui_contact_balance_payments_not_applicable.md` + `_index` + peer
  cross-link from research144 join package.
- Regenerated coverage: live/vision 128; NA 86; implemented/contract 312;
  complete false.
- No new UI tools; no src/ browser or server product changes.

### Deviations

- None material vs plan. Optional IR N1/N2 no-op (tmp dual JSON non-git;
  residual probe route-phase noise deferred; soft-routes dual is authority).

### Review findings

- IR 186.48: ACCEPT, required fixes none.
- Overall completeness: FAIL (expected: bulk 92, annual_reports red,
  residual UI parity, API live out of scope).

### Test results

- lint.sh: pass (wiki + ruff + pyright + coverage check + repo policy).
- offline test.sh: 1565 passed, 35 deselected (EXECUTE and FIX-VERIFY).
- Full BILLY_TEST_MODE=ui-full / completeness suite: not run (complete still false).
- wiki lint: wiki + memory clean.

### Cleanup state

- Research dual used ephemeral profiles (purged). No disposable UI writes.
- No frames/HAR in repo. No credentials in product files.

### Next unresolved coverage slice

- Residual UI parity (taxRates Momssatser multi-resource; files weak Bilag map;
  bank recon match isolation; postings no-steal; productPrices nested;
  daybooks.list editor-not-list weak map). Bulk 92 external-contract and
  annual_reports org_inaccessible remain red.

