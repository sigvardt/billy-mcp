# 198.6 Invoice existing Kunde type

## Goal

Type one FastMCP-created tagged customer into the proved invoice
**Kunde** `INPUT`. First a failing unit fixture that the current live
`after_type` dump (`tag_len=15`, hidden decoys) is a dummy type, not
an existing-customer observation. Then the smallest helper that
refuses that dummy. Then one live pass that creates `MCP-UI-INV-`
+ 8 hex through `ui_clients_*` and types that exact name into
`input[name=contact]`. Bind only if a visible existing option is
clicked. Stay red.

Do not treat typed-only as a bind. Do not remap invoice CUD rows.

Authority: `5E1EDFB4`, `96908DC6`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Products, ledger, files, org, or bills
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Guessed selectors, generic portal sweeps, force clicks,
  `evaluate` clicks, Escape, extra keys (`Enter`, `Alt+ArrowDown`)
- Copying the bills vendor wrapper onto this field
- Treating a hidden `contactId` as a bind
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `86be5da`. Clean tree. Not a backstop. `complete=false`.
Honesty 16 stay red. Contacts and bills CUD rows already name
preview tools. Invoice CUD rows still name `ui_invoices_*_open`.

Complete `5E1EDFB4` opener dump
(`inspect-live-invoices-kunde-opener.json`):

- `missing_keys=[]`
- `named_opener=null`
- Hit target is `INPUT` name `contact` (`ember-text-field`)
- No `FORM`, no label/for, no aria expander
- After click and after type: two hidden decoys, `option_role_count=0`

Current phase dump `after_type` has `tag_len=15`. That is the 198.5
dummy. Live CUD tags are `MCP-UI-INV-` + 8 hex (`tag_len=19`).
`test_typed_contact_dump_is_not_a_kunde_bind` already refuses bind
on a 19-char unbound dump. It does not yet refuse the 15-char dummy
as an existing-customer observation.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

198.5 post-mortem: live pytest failed at observer login before
create, so no tagged customer was created first.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/invoices_kunde.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `src/billy_mcp/ui_writes/invoices_form_observe.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

Parked write-family children stay parked. Unlimited budget is not
a reason to spawn.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is a failing unit
   fixture. The current live `after_type` (`tag_len=15`, hidden
   decoys, `existing_index=null`) is not an existing-customer
   observation. Run it. Capture the failure. Then implement.
2. **Honesty-16 stays red.** Confirm the coverage gate still
   length 16 before any other edit.
3. **Dummy vs existing.** A helper must return false for the 15-char
   dummy dump and true only when `value_len == tag_len ==
   len(unique_tag)` and `unique_tag` matches `MCP-UI-INV-[0-9A-F]{8}`.
   That helper is not a bind. Bind still requires
   `kunde_phase_is_bound` (visible option or create footer).
4. **One dump-named action.** Click and type on the proved
   `input[name=contact]`. `_click_field_once` already does this.
   Do not add named-opener guesses.
5. **Existing customer first.** Live create uses `ui_clients_*`
   then types that exact name. Independent second session for
   invoice read-back. Third session after delete. Delete the
   contact last.
6. **Do not remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add a unit fixture that the current live `after_type` payload
(`tag_len=15`, two hidden decoys, `existing_index=null`) is not an
existing-customer observation. A helper such as
`after_type_is_existing_customer_observation` must return false for
that dump and for any tag that is not `MCP-UI-INV-` + 8 hex. Do not
edit product files yet. Capture the failure.

Keep `test_typed_contact_dump_is_not_a_kunde_bind` and
`test_bind_does_not_press_extra_keys_on_unbound_contact`.

### 2. Smallest helper, then live type

- Implement the helper in `invoices_kunde.py`. Never store the tag
  in dumps. Isolate unit dumps so they cannot overwrite the owner
  files.
- Live path already creates the contact then types. Keep that
  order. Type only into the proved `INPUT`. Recapture opener and
  phase dumps after the existing-customer type.
- If a visible existing option appears, click it
  (`scoped:existing_option`). Create footer last.
- If no visible option: `UI_CHANGED`. Do not add extra keys. Do
  not treat a hidden `contactId` as a bind. Keep the new dump.
- Refresh `wiki/ui_invoices_writes.md`: dummy 15-char type is not
  existing-customer proof. Then `wiki update --path=wiki` and
  `wiki lint --path=wiki`.
- Fold the dummy-vs-existing rule into memory `state.md` /
  `todo.md`. No iteration labels.

### 3. Live CUD only if a visible option is clicked

If and only if `vendor_bind=scoped:existing_option` after the
existing-customer type:

1. Draft save **Gem som kladde**. Persist is browser POST 2xx
   `/v2/invoices`.
2. Independent session read-back of the line. Update. Delete.
3. Contact delete. Third-session absence.
4. Zero leftover `MCP-UI-INV-*` and `billy-live-invoices-*`
   profiles.

If observer login fails again, recapture from the primary FastMCP
session only after the contact exists, then stop fail-closed. Do
not add PUT egress unless a live update is PUT and a failing test
exists first.

### 4. Do not green

Honesty 16 stays red. Invoice `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. Do not call
`apply_ui_invoices_cud_preview_tool_names`.

Run honesty + new unit tests, then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline) after
the helper lands. Live existing-customer type (and CUD only if
clicked) is the product proof, not the offline suite.

## Done when

- Failing dummy-type fixture existed, then the helper refuses
  `tag_len=15` as an existing-customer observation.
- One live pass created a tagged customer and typed that exact
  name into the proved `INPUT`.
- Either a visible option was clicked and one FastMCP draft CUD
  has independent read-back and cleanup, or the run stopped
  fail-closed with `UI_CHANGED` and the new dump still has no
  visible option.
- Honesty 16 still red. `complete=false`.
- Zero leftover tagged invoices or contacts. Frames stay outside
  git if any live write ran.

## Budget

Unlimited. No children. One slice. Do not expand into other CUD
families this iteration.

## Post-Mortem

Work completed: failing dummy-type fixture first, then
`after_type_is_existing_customer_observation`. Live FastMCP created
a tagged customer and typed that 19-char name into the proved
`INPUT` (`value_len=19`). No visible option. Execute `UI_CHANGED`.
Honesty 16 stay red. No remap.

Deviations: the helper was briefly used as a bind gate and removed
after the fake-page execute test failed on fixture tag
`MCP-UI-INV-ACT`. Observation stays a classifier, not a bind.
No child spawn.

Review findings: FAIL. Existing-customer type is not a bind. No
draft, no persist, no read-back, no vision.

Tests: honesty-16 pass. Dummy-type unit test pass. Extra keys still
absent. Offline `BILLY_TEST_MODE=commit` `test.sh` 1898 passed.
Did not run `BILLY_TEST_MODE=full` or `ui-full`. `complete=false`.

Cleanup: leftover scan `remain_count 0`. Zero
`billy-live-invoices-*` profiles. Shared chrome profiles untouched.
No invoice created. No vision frames to purge.

Next unresolved coverage slice: stay on the proved `INPUT`.
Recapture whether a hidden `contactId` appears and whether browser
GET `/v2/contacts` fires after the existing-customer type. Do not
invent selectors or extra keys. Stay red until a visible option,
then one FastMCP draft CUD with independent read-back.

