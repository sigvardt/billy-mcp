# 190.10 Allow contacts PUT then one CUD

## Goal

Let the already-proved save click reach Billy. Official update is
`PUT /v2/contacts/:id`. Browser egress today allows GET/POST/DELETE only,
so the SPA request is `abort("blockedbyclient")` and the isolate dump
shows `interface_status=null`. Add PUT only on `/v2/contacts` (the
`:id` update route). Do not add PATCH unless the live interface proves
it. Record blocked requests when there is no response, then run one
FastMCP create-update-delete. Do not green.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `A5FBD4DC`, `E613984F`,
`E1E454F4`, `C7DBE974`, `F3F318A2`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Changing the name input or using evaluate-click
- Greening any of the 16 CUD honesty rows
- Pointing `tool_name` at `ui_clients_*_preview`
- A second tagged create
- BrowserRuntime-only write as qualification proof
- Live API token calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- PATCH on contacts unless the live interface request method is PATCH
- PUT/PATCH for invoices, bills, products, organizations, or daybooks
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`
- Self-approving vision or purging frames from the live test

## Baseline

HEAD `2126d76` plus uncommitted memory notes. Last commit is 190.9 pointer
proof, not a backstop. Create through FastMCP persists (POST allowed).
Pointer click on `button[data-cy='save-button']` is proved. Dump:
`.fractal/main.billy_complete/tmp/inspect-live-persist-isolate.json`.
Zero `billy-live-contacts-*`. `chrome-profile` stays. Coverage
`complete=false`. Honesty 16 stays red. Gate
`tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py` already
holds.

`test_browser_policy_real_manifest_allows_contacts_data_plane` asserts
GET/POST/DELETE on `/v2/contacts` and does not yet require PUT. PATCH
must stay denied until the live interface proves it.

## Delegation

No children. Root owns this slice. The files sit on the mainline and
would collide if forked:

- `coverage/browser_egress.yaml`
- `src/billy_mcp/ui_writes/contacts_persist.py`
- `tests/unit/test_browser.py`
- `tests/unit/test_ui_contact_exact_labels.py`
- `tests/coverage/test_coverage_inventory.py`
- `tests/live/test_ui_contacts_writes.py`
- `wiki/ui_contacts_writes.md`

Parked UI-write children stay parked. Do not continue or reset them.

## Design choices

1. **TDD first.** First product edit is failing tests. Run them. Capture
   the failure. Then implement.
2. **PUT only, contacts only (`F3F318A2`).** Official inventory is
   `PUT /v2/contacts/:id`. Do not add PATCH unless the live save request
   method is PATCH. Do not widen other resources.
3. **Watch request and requestfailed.** An aborted route has no
   `response`. The dump must show method, path_class, and failure class
   instead of all-null traffic.
4. **Keep the proved click.** `button[data-cy='save-button']` pointer
   click only. Do not refill the name.
5. **Reuse the ticket pair.** `ui_clients_update_preview` /
   `ui_clients_update_execute`. No new tools.
6. **One tag.** `MCP-UI-C-<8hex>`, then `{tag}-U`.
7. **Live qualification uses `create_server()` `call_tool`.** Vision
   stays `pending_review`. No greening.
8. **Keyring refs on.** `BILLY_ORGANIZATION_ID` unset until READY
   returns the live slug.

## EXECUTE

### 1. Failing tests (first edit)

Keep `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
as-is. It is the durable honesty gate from `EE0A0F1B`. Run it first and
confirm it still passes (16 rows red).

Then add failing tests. Run them. Capture the failure. Do not edit
product files yet.

| Case | File | Expected today | Expected after fix |
| --- | --- | --- | --- |
| PUT `/v2/contacts/<id>` | `tests/unit/test_browser.py` | `allows` is false | `allows` is true |
| PATCH `/v2/contacts/<id>` | same | `allows` is false | `allows` stays false unless live proves PATCH |
| Manifest lists PUT on `/v2/contacts` | `tests/coverage/test_coverage_inventory.py` | assertion fails | assertion holds. PATCH remains absent |
| No `response`, request aborted | `tests/unit/test_ui_contact_exact_labels.py` | `interface_status` null, no failure class | `UI_CHANGED` details include method, path_class, and `blockedbyclient` or equivalent failure class |

GET/POST/DELETE on `/v2/contacts` stay allowed. Invoice email POST stays
denied.

### 2. Smallest fix

- `coverage/browser_egress.yaml`: add prefix `/v2/contacts` method
  `PUT` on `api.billysbilling.com`. Leave PATCH and other resources
  unchanged.
- `src/billy_mcp/ui_writes/contacts_persist.py`: watch `request` and
  `requestfailed` as well as `response`. Record method, path_class, and
  failure class. No token. No raw body.
- Do not change the pointer click. Do not change `_fill_name`.
- `wiki/ui_contacts_writes.md`: one sentence that update persist needs
  PUT `/v2/contacts/:id` on the browser allowlist. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.

Run the new unit tests. Then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline). Do not
claim completeness.

### 3. One live FastMCP CUD

Only after leftovers are still empty.

```
unset BILLY_ORGANIZATION_ID BILLY_API_TOKEN
BILLY_BROWSER_PRIMARY_REFERENCE=billy-ui-primary \
BILLY_BROWSER_SECONDARY_REFERENCE=billy-ui-secondary \
uv run pytest tests/live/test_ui_contacts_writes.py::test_ui_contacts_create_update_delete_via_call_tool -q --tb=short
```

Sequence: create preview+execute, independent exact `{tag}`, update to
`{tag}-U`, independent exact `{tag}-U` or `UI_CHANGED` with method and
failure class, delete, third-session absence.

If create or update succeeds and a later step fails, delete leftover
tags through `ui_clients_delete_*`. Delete write, readback, observer,
and cleanup profiles on every exit. Do not touch `chrome-profile`.

Vision: `author=live_test`, `reviewer_verdict=pending_review` only.

Write the live dump to
`.fractal/main.billy_complete/tmp/inspect-live-persist-isolate.json`.
No credentials. No raw frames in git.

### 4. Do not green

Honesty 16 stays red. `tool_name` stays `ui_clients_*_open` until
independent accept plus purge. `coverage/status.json` stays
`complete=false`.

## Done when

- Failing PUT and requestfailed tests existed, then passed. PATCH stays
  denied unless the live save used PATCH.
- Offline `test.sh` passes. Honesty gate still passes.
- One live FastMCP create, update, and delete of a single tag, with
  exact independent read-back and third-session absence, or a
  leftover-empty org plus a concrete `UI_CHANGED` dump that names the
  blocked method.
- No leftover `MCP-UI-C-*` names. No leftover live/inspect profiles.
- No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm the allowlist change is contacts-only, the
live rename persisted or failed closed with method and failure class,
honesty 16 is still red, and no secrets landed.

## Post-Mortem

Completed: PUT `/v2/contacts/` (id only) on browser egress. PATCH and
collection PUT stay denied. Persist watches request/requestfailed and
waits for a PUT response. One live FastMCP CUD passed. Offline
`test.sh` 1839. Honesty 16 red. `complete=false`. Vision
`pending_review`. Success execute now returns redacted PUT method,
status, path class, and name class.

Deviations: first live attempts raced the PUT response; wait was added.
Same-session IR refused to write vision accept or purge.

Review: FAIL, do not green. Product slice sound. Completeness and
vision-accept not met.

Tests: lint clean. Targeted persist/PUT tests 22 passed. Live CUD 1
passed. `BILLY_TEST_MODE=full` not run: completion requirements are
not all true.

Cleanup: successful run started and ended on empty Kontakter. Zero
`billy-live-contacts-*` profiles. Frames remain in owner-only
`vision-tmp/run-3d5b151dfd5342258f8734373597f8c1` until a later
independent Grok accept and purge.

Next slice: independent Grok vision accept and purge for that run
only. Then point the three contacts parity rows at preview tools.
Do not green until that. Other honesty-16 families still red.

