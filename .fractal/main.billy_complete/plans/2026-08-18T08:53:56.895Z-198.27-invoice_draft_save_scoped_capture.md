# 198.27 Invoice draft-save scoped capture

## Goal

Record whether one exact **Gem som kladde** click on empty
`/:org_slug/invoices/new` produces a **contact-owned** list, and
derive at most one normal action from that scoped evidence.

`2683CE6B` is binding. Stop page-wide one-off dumps. The
`3b025f4` draft-save dump is delivered and closed:
`gem_clicked=true`, `invoice_persisted=false`,
`validation_message_present=false`, `ingen_kontakter_count=0`,
`opret_ny_count=1`, `option_role_count=0`, `proved_bind=none`.
Independent review failed that dump: page-wide **Opret ny** is
the sidebar decoy on official shot 1. Do not remake it.

Official shot 2 (cited in `tmp/grok-research.md`) puts a second
**Opret ny** in the list footer under **Vælg kunde**, with
tooltip **Dette felt skal udfyldes.** and empty copy **Ingen
kontakter fundet.** This slice must prove ownership, not count
the whole page.

TDD first. Through the real FastMCP boundary, create one tagged
customer, open create, attach a read-only observer, click
**Gem som kladde** once, then persist **only** the research
section 5 keys:

- `gem_clicked` (bool)
- `invoice_persisted` (bool)
- `contact_input_present` (bool)
- `pickerfield_present` (bool)
- `active_element_category` (`contact_input` / `gem_button` /
  `option` / `opret_ny` / `other` / `none`)
- `mutation_owned_count` (int)
- `scoped_rows` (list, cap 16)
- `unique_action` (`existing_option` / `inline_opret_ny` / `none`)
- `unique_action_owned_by_picker` (bool)
- `proved_bind` (`draft_save_scoped_existing_option` / `none`)
- `missing_keys`

Each scoped row persists only: `tag`, `role`, `visible`,
`interactive`, `exact_match` (`validation` / `empty_list` /
`opret_ny` / `option` / `none`), `box` (`dx`, `dy`, `w`, `h`
relative to the contact input), `z_index`, `ownership_path`
(category tokens only), `is_mutation_owned`.

`proved_bind` is `draft_save_scoped_existing_option` only when
`gem_clicked` is true, `invoice_persisted` is false,
`unique_action=existing_option`, and
`unique_action_owned_by_picker=true`. Otherwise `UI_CHANGED`.
`inline_opret_ny` is a derived target, not a proved existing
bind. Do not click it.

Do not click **Opret ny**. Do not click **Godkend**. Do not type
into `input[name=contact]`. Do not click the `pickerfield`.

Authority: `96908DC6`, `2683CE6B`, `FD39FFE7`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Closed: `3b025f4` page-wide
draft-save dump, `e49c8f3` **Vælg kunde** rest dump, `C0721A14`,
structure compare, `28C8FBC8`, `31B0C7A6`, fiber, `E87B6AEF`,
`07600147`, `452E0773`, `51E18E60`, `A3AB03C3`, `9F777B8F`,
`8EFD0EAD`, `4A5CD1E7`, `F12B607E`, `D326FFB3`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview until CUD is live-proved
  and independent review accepts
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status` in the dump-only half
- Writing `author=independent_review` / `accept` / `purge_verified`
- Remaking the page-wide draft-save dump or overwriting
  `inspect-live-invoices-draft-save-validation.json`
- Remaking the **Vælg kunde** rest dump or closed picker, Ember,
  fiber, listener, descendant, post-click, wrapper-center,
  overlay, typed, route, structure, or customer-detail **Opret
  faktura** dumps
- Page-wide `get_by_text` / `get_by_role` counts of **Opret ny**
- Generic portal sweeps
- Typing into `input[name=contact]`
- Clicking the `pickerfield` wrapper or the overlay at `dx=242`
- Clicking **Opret ny**, **Godkend**, **Godkend og send**, **Send**
- Copying the bills Leverandør wrapper onto this field
- Guessed URLs, guessed selectors, force clicks, `evaluate` clicks,
  blind keys
- Calling CDP, fetching a script URL, or issuing a `/v2/` URL
- Persisting source, locators, URLs, slugs, names, or screenshots
- Product create, ledger, files, org, quotes convert, Duplikér
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `3b025f4`. Memory and research edits from this run are
uncommitted. Not a backstop. `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations CUD rows name preview
tools. Invoice CUD rows still name `ui_invoices_*_open`. Offline
invoice preview/execute helpers exist. `_create_draft` still
`goto /invoices/new` then `bind_kunde`. That bind is unbound.

Page-wide draft-save dump is delivered and closed. Official
shot 2 ownership has not been recorded.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. `728BD2E4` child-split applies
after invoice CUD is bound, not to this inspect. Unlimited budget
is not a reason to spawn. Parked write-family children stay parked.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/invoices.py` (ticket pair; no remap yet)
- `src/billy_mcp/browser.py` `ui_invoices_create_open`
- `src/billy_mcp/ui_writes/page_flow.py`
- `src/billy_mcp/ui_writes/invoices_draft_save_validation.py`
  (closed dump; do not overwrite its owner file)
- `src/billy_mcp/ui_writes/invoices_kunde_post_click.py` (observer
  row shape only; do not click pickerfield)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_invoices_writes.md`

New files:

- `src/billy_mcp/ui_writes/invoices_draft_save_scoped.py`
- `tests/unit/test_ui_invoices_draft_save_scoped.py`
- `tests/live/test_ui_invoices_draft_save_scoped.py`

Do not append this inspect to
`tests/live/test_ui_invoices_writes.py` or
`tests/live/test_ui_invoices_draft_save_validation.py`.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-invoices-draft-save-scoped.json`

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing unit fixture. An empty
   dump must fail `missing_keys` with the exact research section 5
   set. Run it. Capture the failure. Then implement the helper.
2. **Honesty-16 stays red.** Do not remap invoice tools in the
   dump-only half.
3. **New dump file.** Do not write to
   `inspect-live-invoices-draft-save-validation.json`.
4. **Tagged customer first.** Create `MCP-UI-INV-` + 8 hex through
   FastMCP. Confirm the name in a fresh session. Then open
   `/:org_slug/invoices/new` through `ui_invoices_create_open`.
5. **Observer before the click.** Attach to `input[name=contact]`,
   closest `.pickerfield`, any `aria-controls` target, and
   `document.body` childList. After the click, keep only
   mutation-owned or linked nodes, plus geometry immediately
   below the input. Cap 16 rows. No page-wide text counts.
6. **One Gem click.** Exact **Gem som kladde** only. No type. No
   picker click. Read-only `evaluate` for the observer is allowed.
   `evaluate` clicks are not.
7. **Ownership, not page-wide counts.** Sidebar **Opret ny** has
   ownership `nav` and large negative `dx`. Shot-2 footer has
   ownership `pickerfield` or `list` and `dy > 0` in the same
   column. Persist tokens and relative box only.
8. **Invoice must not persist.** If the page leaves create-open,
   set `invoice_persisted=true`, `proved_bind=none`, delete the
   draft, prove absence.
9. **Do not click Opret ny.** If `unique_action=inline_opret_ny`,
   record it and stop. Official shot 3 is create-new.
   `FD39FFE7` still requires existing-customer bind.
10. **CUD is a second half of the same EXECUTE.** Only if
    `proved_bind=draft_save_scoped_existing_option`. Smallest
    route means reuse that scoped option. Do not fall back to
    typed `bind_kunde`.
11. **Default stop is `UI_CHANGED`.** If `unique_action=none`,
    do not fall back to closed picker probes. Report the owner
    question from research section 5. Do not freeze invoice CUD
    as done.
12. **Vision.** Live test may write `author=live_test` and
    `reviewer_verdict=pending_review` only.
13. **No remap** until independent accept + purge of a real CUD.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_DRAFT_SAVE_SCOPED_KEYS` and `missing_keys` in
`invoices_draft_save_scoped.py` only after a unit fixture fails
because those names do not exist, or add the fixture first
against a local helper stub. Preferred shape:

- Fixture feeds `{}` or a flags-only stub.
- Helper returns the missing key list from research section 5.
- Current empty dump must fail with that exact set.
- Page-wide `opret_ny_count` is not a scoped key.
- `unique_action=inline_opret_ny` must not become
  `proved_bind=draft_save_scoped_existing_option`.

Do not edit `invoices_form.py` or remap coverage until that
fixture fails and, later, bind is proved.

Keep the closed **Vælg kunde** / picker / Ember / fiber /
listener / descendant / draft-save-validation / customer-detail
fixtures.

### 2. Smallest helper, then one live scoped inspect

- Implement the helper in `invoices_draft_save_scoped.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  customer text.
- Derive `unique_action` and `proved_bind` from the Goal rules.
  If bind fails, set `code=UI_CHANGED`.
- Isolate unit dumps so they cannot overwrite owner files.
- Live path through `create_server` `call_tool`:
  1. Login. Create tagged customer through FastMCP. Confirm in a
     fresh session.
  2. Open `ui_invoices_create_open`.
  3. Attach the observer. Confirm `contact_input_present` and
     `pickerfield_present`.
  4. One click of exact **Gem som kladde**. Set `gem_clicked`.
  5. Collect scoped rows. Set `active_element_category`,
     `mutation_owned_count`, `unique_action`, and
     `unique_action_owned_by_picker`.
  6. Set `invoice_persisted` if the page left create-open.
  7. Existing-option bind only if `unique_action=existing_option`
     and the picker owns it. Persist `proved_bind` only. Do not
     write the name. Do not click **Opret ny**.
  8. Do not **Godkend**. Do not type into the contact input. Do
     not remake picker or page-wide dumps.
  9. Write allowlisted keys to
     `~/.local/share/billy-mcp/inspect-live-invoices-draft-save-scoped.json`.
  10. Delete the tagged customer through FastMCP and prove
      absence. If a draft invoice appeared, delete it first.
- After the dump is delivered, later `ui-full` verifies the file
  and returns unless CUD half is in the same run.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the gate into memory `state.md` / `todo.md`. No iteration
  labels.

### 3. Scoped existing-option CUD only if proved

If `proved_bind=draft_save_scoped_existing_option`:

1. Add a failing product fixture that the current `_create_draft`
   path does not satisfy (it still types `bind_kunde`).
2. Implement the smallest route from the proved scoped option.
3. One disposable invoice draft CUD through FastMCP. Independent
   fresh-session read-back after create, update, and delete.
   Reverse cleanup: invoice then customer.
4. Do not remap `tool_name` until independent review accepts and
   purge is verified. Stay red in this run even if CUD persist
   works.
5. Never **Godkend**, **Send**, email, book, or pay.

If scoped capture does not bind, skip this half.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus the honesty-16
gate. Do not run live API tests. Do not claim `complete: true`.

## Done when

- Honesty-16 still length 16 and red
- Failing scoped-dump fixture existed, then the helper lists
  `missing_keys` on an empty dump
- Live dump written through FastMCP create-open plus one
  **Gem som kladde** click with a contact-owned observer
- Keys match research section 5. No URL, source, or customer
  text in the dump or git
- Either `proved_bind=draft_save_scoped_existing_option` and the
  CUD half ran, or `UI_CHANGED`, no leftover customer or invoice,
  scoped evidence recorded, invoice CUD still red and not frozen
  as done
- No remake of the page-wide draft-save dump, the **Vælg kunde**
  rest dump, or the closed picker set
- Invoice CUD rows still name `ui_invoices_*_open` unless the
  CUD half completed and independent review already accepted
  (not expected in this pass)
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice fits. No spawn.
Research section 5 is the contract. Stay red.

## Post-Mortem

Completed: TDD empty-dump fixture, scoped helper, live FastMCP
inspect. First capture used `document.body`. Independent review
FAIL under `8EBC19D5`. FIX-VERIFY removed that root, kept cap 16,
and recaptured only on contact-owned roots. Dump:
`gem_clicked=true`, `invoice_persisted=false`,
`mutation_owned_count=1`, seven pickerfield rows, no option, no
footer, `unique_action=none`, `proved_bind=none`, `UI_CHANGED`.
Honesty 16 still red. Invoice rows still name
`ui_invoices_*_open`. `complete=false`. Tagged customer deleted.

Deviations: no invoice CUD half. Bind unbound. Body observe was
removed after IR, not before first capture. Wiki no longer
teaches `document.body` as a root.

Review: FAIL on body observe, then the method fix. Invoice CUD
still unbound. Do not remap. Do not freeze as done.

Tests: honesty-16 2 passed. Scoped unit tests 12 passed. Live
narrow dump delivered. Lint required after the body removal.

Cleanup: tagged customer deleted. Temp inspect profiles purged.

Next unresolved slice: `FD39FFE7` invoice draft CUD by a
different unused normal-interface path. Do not remake this dump,
the page-wide draft-save dump, or the closed picker set.

