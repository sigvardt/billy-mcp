# 198.68 invoiceReminderAssociations ticketed delete

## Pivot

`4DBD7C3F` is binding under `9B979A05`. HEAD `29dfa03` is a clean
198.67 balanceModifiers close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory
only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-4dbd7c3f.html`,
`billy-api-docs-chunk-research-4dbd7c3f.js`,
`research-4dbd7c3f-invoiceReminderAssociations-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the balanceModifiers lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor
`#v2invoicereminderassociations`. Supports: `get by id, list,
create, update, bulk save, delete, bulk delete`. Singular delete
**is listed**. Property table is exactly three rows: `reminder`
belongs-to required, `invoice` belongs-to required, `lateFee`
belongs-to readonly. No delete-body table.

Intro `#deleting-a-record`: `DELETE /v2/invoices/:id`. The request
should not have a body. DELETE is idempotent; a missing id still
returns `200 OK`. That narrative explains the historical unauth
meta-only 200. It is **not** cleanup proof and must not keep this
row red (`4DBD7C3F`, wiki rule 3).

Unauth missing-id DELETE 200 must not override Supports delete.
Residual honesty that left this row toolless is superseded for
singular delete only. Bulk stays
`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not invent bulk tools.
Do not start `api.transactions.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_invoice_reminder_associations_*` tools. Do not finish. No live
API.

## Goal

Land one proved cohort: ticketed offline
`api.invoiceReminderAssociations.delete`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.invoiceReminderAssociations.delete` | `api_invoice_reminder_associations_delete_preview` / `_execute` | `DELETE /invoiceReminderAssociations/:id` | non-empty `id` only; no JSON body |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 4 method-closed + 2 readonly
map + 1 meta-delete = 7. Bulk 92 stay red. Implemented 545,
contract 550 (HEAD 544 / 549). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds DELETE with `json_body=None`,
canonical request `{id}`, and an encoded path. Sibling product is
`src/billy_mcp/api/bank_payment_writes.py`
(`api_bank_payments_delete_*`). Extend the existing association
module rather than adding a second register.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

Existing `tests/api/test_invoice_reminder_association_writes.py`
is 258 lines and the tickets file is 203. Do **not** grow those
with the full delete suite. Minimal edit only: rename
`test_registers_exactly_four_flat_typed_invoice_reminder_association_write_tools`
to six tools and add the two delete names. Put the rest in new
files under 250 lines.

1. New `tests/api/test_invoice_reminder_association_delete.py`,
   importing `make_server` / `call_tool` from the writes tests:
   - Isolated register count is 6. Preview fields:
     `api_invoice_reminder_associations_delete_preview` `{id}`,
     `api_invoice_reminder_associations_delete_execute`
     `{confirmation_ticket}`. `id` and `confirmation_ticket` have
     `minLength` 1. `additionalProperties` is false. No `input`
     wrapper. No nested `invoiceReminderAssociation` on delete
     preview.
   - Outer extras and empty `id` fail `ValidationError` on
     `InvoiceReminderAssociationDeletePreviewInput`. Execute extras
     fail on `WriteExecuteInput`. Preview still makes no HTTP.
   - FastMCP `call_tool` with empty `id`, extra keys, or a nested
     payload on delete preview raises FastMCP validation. No HTTP.
   - Preview binds canonical request `{"id": "assoc-1"}` and
     expected effect
     `{action: delete, resource: invoiceReminderAssociation, id: assoc-1}`.
     Preview makes no HTTP.
2. New `tests/api/test_invoice_reminder_association_delete_tickets.py`:
   - Execute DELETEs
     `https://api.billysbilling.com/v2/invoiceReminderAssociations/<urlencoded id>`
     with `request.content == b""`. Encoded id `assoc /?` becomes
     `/v2/invoiceReminderAssociations/assoc%20%2F%3F`.
   - Success fixture
     `{"meta": {"deletedRecords": {"invoiceReminderAssociations": ["assoc /?"]}}}`
     maps `changed_records={}` and
     `deleted_records={"invoiceReminderAssociations": ["assoc /?"]}`.
     Do not expose a top-level `invoiceReminderAssociation` write
     success field. Missing plural root is allowed on DELETE.
   - Tamper, wrong executor (create ticket on delete execute, and
     delete ticket on create execute), replay, expiry, missing auth
     (`token=""`), and failed write not retried. Preview and
     missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(meta_delete) == 1`, residual `== 7`. Assert
   `api.invoiceReminderAssociations.delete` is **absent** from
   `RESIDUAL_META_DELETE_IDS`. Keep `api.transactions.delete`.
   Bump every hardcoded `implemented_rows == 544` /
   `contract_tested_rows == 549` in that file to **545 / 550**.
   `complete` stays false. Live/vision counts stay 339.
4. Map the id in `OPERATION_TEST_REFERENCES` after
   `api.invoiceReminderAssociations.update`. On HEAD `tool_name` is
   empty and the row is meta-delete unqualified; the new mapping
   should fail until honesty lets the preview name through.
5. Add
   `test_invoice_reminder_associations_delete_is_ticketed_offline_and_not_live_tested`
   next to `test_bank_payments_delete_is_ticketed_offline_and_not_live_tested`.
   Assert `tool_name == api_invoice_reminder_associations_delete_preview`,
   `method_or_route == DELETE /v2/invoiceReminderAssociations/:id`,
   `request_fields == ["id"]`, `implemented` and `contract_tested`
   true, `live_tested` false,
   `qualification.live_api == out_of_scope_by_user`,
   `qualification.kind != meta_delete_unqualified`.
6. `tests/unit/test_coverage_server.py`: extend
   `INVOICE_REMINDER_ASSOCIATION_WRITE_API_TOOL_NAMES` with the two
   delete tools. Bump
   `len(INVOICE_REMINDER_ASSOCIATION_WRITE_API_TOOL_NAMES) == 4` to
   **6**. Bump `len(api_tool_names) == 313` to **315**.
   `tests/coverage/test_api_live_qualification_semantics.py`: bump
   `len(implemented) == 205` to **206**.

Run:

```text
uv run pytest tests/api/test_invoice_reminder_association_delete.py tests/api/test_invoice_reminder_association_delete_tickets.py tests/api/test_invoice_reminder_association_writes.py tests/coverage/test_coverage_inventory.py tests/unit/test_coverage_server.py tests/coverage/test_api_live_qualification_semantics.py -q --tb=line
```

Capture the first red.

## Implement

`src/billy_mcp/api/invoice_reminder_association_writes.py` (extend,
currently 179 lines):

- `InvoiceReminderAssociationDeletePreviewInput`: `id: str =
  Field(min_length=1)`, `extra=forbid`, frozen. No nested payload.
  Do not put route `id` inside `InvoiceReminderAssociationPayload`.
- Preview: `api_invoice_reminder_associations_delete_preview(id)`.
- Execute: `api_invoice_reminder_associations_delete_execute(confirmation_ticket)`.
- `_association_spec(..., method=WriteMethod.DELETE, payload=None,
  resource_id=input.id)` with expected effect
  `{action: delete, resource: invoiceReminderAssociation, id}`.
- Register the two tools next to create/update. Reuse
  `WriteProtocolService`. Do not add a second protocol or a second
  `server.py` register call.
- Collection path `/invoiceReminderAssociations`. Singular root
  `invoiceReminderAssociation`. Plural root
  `invoiceReminderAssociations`. `organization_id=None`.
- Update the register docstring from "four" to the six tools.
  Keep the file under 250 lines.

`scripts/generate_coverage_report.py`:

- Remove `api.invoiceReminderAssociations.delete` from
  `RESIDUAL_META_DELETE_IDS`. Leave `api.transactions.delete`.
- Add `OPERATION_TEST_REFERENCES` for the delete id after update,
  pointing at the new delete test files plus
  `SERVER_REGISTRY_TEST_REFERENCE`.
- Do not touch bulk helpers, the other 4 method-closed ids, or the
  readonly map.
- Keep `RESOURCE_SUPPORTS` `("invoiceReminderAssociations", True, True, True)`.
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

`live_probe.py` may still list this id as unauth 200
`METADATA_ONLY_UNQUALIFIED`. Leave that historical matrix. It is
not the contract.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  residual honesty remaining is 7. This row is a ticketed offline
  tool (`api_invoice_reminder_associations_delete_preview`). Meta
  delete remaining is `api.transactions.delete` only. Not
  completeness.
- `wiki/offline_write_probe_rules.md`: Supports singular delete
  plus path `id` and no delete body table is enough to ship the
  ticketed DELETE with no JSON body. Confirm
  `invoiceReminderAssociations` delete follows
  `bankPayments.delete`. Historical unauth 200 is not the contract.
  Update the confirmed-examples row that still says DELETE
  missing-id 200 stays meta-delete unqualified.

Keep `wiki/ui_contact_postings_late_fees_reminder_assoc_not_applicable.md`
as history. Do not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.invoiceReminderAssociations.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No create/update field-map edits except registering the two new
  tools beside them.
- Do not start `api.transactions.delete` (`7B947636`).
- Do not start readonly-map research (`A485F530`).
- Do not accept a JSON delete body, `{}`, or a nested
  `invoiceReminderAssociation` on delete preview.
- `complete` stays false.
- Saved `9B979A05` still owns the remaining 7 residual rows and
  bulk92. Saved `96908DC6` still owns overall finish.

## Independent review

After EXECUTE, review must re-parse
`#v2invoicereminderassociations` on the locked HTML/chunk and
confirm:

1. Supports still lists singular delete (and create, update, bulk
   save, bulk delete).
2. Property table is still three fields. Delete has no body table.
3. Tools exist as preview plus execute for create, update, and
   delete. No bulk tools. No UI tools.
4. Inventory `tool_name` for delete is
   `api_invoice_reminder_associations_delete_preview`.
5. Execute path is `DELETE /invoiceReminderAssociations/:id` with
   encoded id and empty body.
6. Preview input is `{id}` only. Empty id and extras fail.
7. `live_tested` is false.
8. Remaining residual honesty is 7 toolless rows.
9. No 200-meta-only green. No bulk tools. No
   `api.transactions.delete` work in this slice.

## Post-Mortem

Work completed: ticketed offline
`api.invoiceReminderAssociations.delete` in
`src/billy_mcp/api/invoice_reminder_association_writes.py`.
Preview takes a non-empty `id` with `extra=forbid`. Execute takes
`confirmation_ticket` only and sends
`DELETE /invoiceReminderAssociations/:id` with an encoded id and
no JSON body. Empty id, extras, and a nested payload fail at the
FastMCP boundary. Coverage regenerated: implemented 545, contract
550, live/vision 339, complete false. Residual honesty remaining
7.

Deviations: none from official Supports delete or the intro
no-body rule. Registry count asserts also bumped (`api` tools
315, implemented API rows 206, association write tools 6).
Delete contract tests live in
`tests/api/test_invoice_reminder_association_delete.py` and
`tests/api/test_invoice_reminder_association_delete_tickets.py`
so each test file stays under the 250-line ceiling.

Independent review: PASS. No required fixes. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
`test.sh` `2415 passed, 83 deselected`. No live UI records.
Next unresolved slice after COMMIT: saved `7B947636` ticketed
singular `transactions` delete. Residual honesty remaining 7.
Bulk 92 stay red. `complete` stays false.

