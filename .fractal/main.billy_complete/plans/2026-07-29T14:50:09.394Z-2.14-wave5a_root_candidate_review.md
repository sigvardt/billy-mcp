# 2.14 Wave-5a root candidate review and integration

## Objective

Obtain the required root-owned, authenticated Grok review for the repaired
Wave-5a candidate tips before selectively integrating any product code. This
slice keeps the inventory, live status, UI status, and `complete` flag
fail-closed. It does not implement the later catalog/daybook cohort or root
tool registration unless the review completes and the integration checks pass.

## Contract and current state

- The cited Grok brief at `.fractal/main.billy_complete/tmp/grok-research.md`
  revalidated the current official docs at ETag `hsisik4g9p3603` / MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`. It requires ticket-only execute inputs,
  singular CUD roots, no write retry, and no offline bulk inference.
- Root `5ab3efa` contains the execute-twin coverage allowance only: an exact
  `api_*_execute` companion is allowed for a manifest `api_*_preview` tool;
  it is not an inventory row and advances no coverage state.
- Candidate branch tips all contain the current root and have clean product/test
  diffs: protocol `4b2c12c` (`write_protocol.py` and its unit test), contacts
  `a86afe9` (`contact_writes.py` and its API test), and contact persons
  `341d0b1` (`contact_person_writes.py` and its API test).
- The three leaves are operator-paused after their own Grok authentication
  failure. Their committed branches and paused nodes remain intact until their
  accepted bytes are selectively merged. A root Grok reviewer is the required
  independent-review lane.

## Execution plan

1. Recheck the three sibling worktrees with their actual sibling paths and
   confirm no uncommitted product work remains. Keep the operator-paused nodes
   and their branches intact until the reviewed product/test bytes have merged;
   do not resume their unauthenticated review loops or close them early.
2. Create a childless reviewer from the current root baseline and configure it
   before launch:

   ```bash
   fractal node init wave5a_root_candidate_review \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --base=main.billy_complete --agent=grok --scope=wiki \
     --max-iters=2 --max-depth=0 --max-children=0 --max-descendants=0 \
     --step-timeout=20m
   ```

   Its `NODE.md` must limit it to an independent, cited review of the exact
   three tips above. It must inspect three-dot diffs and clean archives, verify
   typed preview/execute surfaces, response mapping, tests, redaction, and the
   no-inventory-greening boundary against the official brief. It may write only
   `wiki/wave5a_repaired_candidate_independent_review.md`; it must not alter
   product code, inventory/status, seeds, or claim live/UI/vision completion.
   A per-candidate ACCEPT/REJECT verdict with direct official-doc citations is
   required before any merge.
3. Monitor the reviewer and answer only review-scope questions. If it reports a
   required finding, keep every candidate unmerged and carry the finding into a
   focused Codex Power fix/re-review slice. Authentication failure before any
   review artifact is retried as the same independent-review task with Codex
   Power, per the routing policy. That fallback may produce a separate
   repository-review record, but it is not a Grok acceptance: keep candidate
   merges blocked until a cited Grok independent-review record exists.
4. If all three verdicts accept, verify the committed candidate artifacts from
   clean `git archive` exports, then integrate strictly one candidate at a time
   in this order: protocol, contacts, contact persons. Use a `--no-ff
   --no-commit` merge, restore the incoming child `.fractal/<child>` seed tree
   to the root version before committing, and use `merge <child>` as the merge
   commit message. This retains the labelled merge parent while shipping only
   the reviewed product/test files. Close each corresponding paused leaf only
   after its merge succeeds and the resulting root checks pass.
5. After each accepted merge, run formatter, Ruff, Pyright, focused candidate
   tests, the root non-live suite, `scripts/check_coverage.py
   --reject-false-completeness`, and repository policy checks. Do not change
   manifest implementation/test booleans or `coverage/status.json`: the tools
   are not registered on root yet.
6. If the accepted protocol merge is clean, prepare two disjoint Codex Power
   implementation leaves from that committed baseline: catalog writes owns
   only `src/billy_mcp/api/catalog_writes.py` and
   `tests/api/test_catalog_writes.py`; daybook writes owns only
   `src/billy_mcp/api/daybook_writes.py` and
   `tests/api/test_daybook_writes.py`. Both must use the frozen official brief,
   remain offline/fail-closed, and leave bulk and registration untouched.

## Acceptance criteria

- A root-authenticated Grok review record gives a cited verdict for every exact
  candidate branch tip; no leaf authentication failure is counted as review.
- Candidate integration, if approved, contains only the reviewed product/test
  changes and labelled no-fast-forward merge commits; no child seed files land
  on root.
- The execute-twin coverage rule continues to reject unmatched or unrelated
  registrations, and no inventory evidence is greened early.
- All non-live verification that is run passes. Full-mode, live, browser, UI,
  bulk, and completeness gates remain explicitly red.

## Post-Mortem

- The authenticated root Grok review at
  `.fractal/main.billy_complete/tmp/grok-review.md` accepted the repaired
  protocol, contact, and contact-person tips. The separate Codex Power
  fallback review did not gate this decision.
- Selectively integrated only the accepted product/test files, excluding every
  child seed tree, in labelled merge commits `95e03c4` (protocol), `9d40949`
  (contacts), and `b25582f` (contact persons). The three paused source leaves
  were intentionally closed after their reviewed bytes landed.
- Clean candidate archives passed their focused suites (29 protocol, 18
  contact, and 11 contact-person tests). The integrated root then passed
  `scripts/lint.sh` and `BILLY_TEST_MODE=commit scripts/test.sh` with 546
  tests passing.
- No manifest state, live/UI/vision evidence, or `coverage/status.json` was
  changed; full qualification remains inapplicable at this stage. No sensitive
  browser evidence was created or retained.
- The next unresolved coverage slice is the disjoint catalog and daybook
  ticketed-write implementation. Both leaves are launched from the verified
  root baseline; root registration and any offline evidence update remain
  blocked until their code and a new independent review are accepted.
