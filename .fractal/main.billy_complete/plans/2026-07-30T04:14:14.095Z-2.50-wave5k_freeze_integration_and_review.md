# 2.50 Wave-5k freeze integration and independent review

## Decision

Research50 is sufficient cited current-contract evidence for the pending
Wave-5k freeze delivery. The two committed child artifacts require root
integration, but the independent Grok freeze review cannot start until the
freeze page exists on a committed root baseline. No product implementation is
authorized in this iteration.

This is manager work: the independent review is deliberately separated from
the contract author and must re-check the root-integrated page and current
official documentation. The later product implementation is also a separate
Codex Power concern and remains blocked on that review's ACCEPT.

## Current iteration

1. Preserve the research50 state update and this plan only. Verify that the
   root has no Wave-5k product module, registration, or coverage-status change;
   bankPayments create, update, delete, and both bulk rows remain red.
2. Re-read the two child documents from their committed tips and confirm their
   boundaries: the freeze contract is create/update-only and the fallback page
   is explicitly non-authoritative. Do not merge child work during this phase;
   current root integration belongs to the next PREPARE pass.
3. Commit the durable research/memory/plan state through the normal commit
   step, with no production code or coverage changes.

## Next integration gate

1. In the next PREPARE pass, inspect three-dot diffs and merge the completed
   `main.billy_complete.wave5k_freeze_contract` branch using a labelled
   no-fast-forward merge. Integrate the Codex fallback review only if its
   review page retains its non-authoritative wording. Regenerate the project
   wiki index and run `wiki lint` on the root wiki.
2. Commit that root baseline before spawning a reviewer. The child must fork
   the committed root so the contract is present in its actual review target.
3. Spawn the required independent reviewer with an explicit Grok route:

   ```bash
   fractal node init wave5k_freeze_review \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --agent=grok --max-cost=320 --max-iter-cost=80 --max-iters=8 \
     --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Its sole project deliverable is
   `wiki/wave_fivek_freeze_independent_review.md`. It must independently
   compare the root freeze page with the cited official docs, verify POST/PUT
   401 and singular DELETE 405 at the locked base, review the exact four-tool
   boundary and ticket rules, and issue a cited ACCEPT or REJECT. It must not
   change production code, inventory state, coverage flags, UI/live evidence,
   or the product-ready fallback page. Its completion requirement is its own
   committed review page and report; it must not wait for parent index work.
4. Only a Grok ACCEPT opens a later explicitly routed Codex Power product leaf
   for `bank_payment_writes.py`, server registration, tests, and the two
   offline coverage rows. The product leaf must exclude singular delete and
   bulk tools.

## Constraints

- Official docs fingerprint remains `hsisik4g9p3603` /
  `c2efda0ee4cf9cf200e14910c5fc6996` / 147934 bytes; research50 records the
  primary-source fetch and no drift.
- `bankPayments` POST and PUT are only auth-gated (401); singular DELETE is
  closed (405) and overrides the optimistic Supports listing.
- Keep all live, UI, vision, bulk, and completeness rows fail-closed. The
  current honest totals remain 238 API tools and 166 offline-green API rows.

## Post-Mortem

The root independent review accepted research50 only as an offline
current-contract handoff. It confirmed that the child freeze page is content
ready but cannot become a freeze acceptance until it is present on the root
branch and independently reviewed there. Product code, registration, and
coverage flags stayed untouched.

The only confirmed repository correction was the stale bank-payment create
cleanup instruction in the API inventory: singular DELETE is 405, so the
future live-test cleanup direction now uses the documented irreversible
`isVoided` void path with independent verification. The missing backticks in
the child page's quoted 405 message are cosmetic; the meaning and method gate
are exact, and the root review may polish that quotation when it integrates the
page.

Commit-mode verification passed: 1,020 non-live tests, Ruff format and lint,
Pyright, coverage-inventory validation, repository-policy checks, and both
wiki lints. Full qualification was not run because `complete: false`, live,
UI, vision, and most inventory rows remain knowingly red. No sensitive
evidence was added to the repository.

Next gate: merge the completed freeze contract and supplemental fallback review
with root-owned wiki-index regeneration, then launch the explicitly Grok-routed
freeze reviewer from that committed root baseline. A dedicated ACCEPT remains
required before a Codex Power product leaf can add the four bank-payment tools.
