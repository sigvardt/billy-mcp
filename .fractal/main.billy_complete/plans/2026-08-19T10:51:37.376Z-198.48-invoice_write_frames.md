# 198.48 Invoice CUD four write frames

## Pivot

Design §10.4 requires four write frames: initial state,
completed form before submit, success after submit, restored
state after cleanup. Contacts, bills, and organizations already
do that. Live invoice CUD on `657cef5` writes only
`03_after_update.png` and a `author=live_test` /
`pending_review` record (`run_id=217dcc7d5a5b4bd098c22aa380ecb215`).
Independent review cannot accept that record. Product live test
captures none; its pending dir is empty.

Owner `A6A2B60C` / `31BAF1FF`: org is independently empty.
Do not remake leftover reverse-clean. Do not accept `217dcc7d`
as-is. Do not self-accept. Do not green.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn a child. Do not remake
dumps.

HEAD `657cef5` plus SYNC memory/wiki edits. Last commit isolated
invoice CUD and product DELETE 2xx watch. No
`(failed on <step>)` backstop.

Current fail: `test_ui_invoices_create_update_delete_via_call_tool`
calls `_capture` once for `03_after_update.png`. Organizations
assert four files before `write_vision_record`.

## Goal

Failing fixture, then four invoice write frames on the existing
self-contained FastMCP CUD, one live disposable run, then
independent Grok review. Stay red.

1. Offline failing fixture: live invoice CUD must capture
   `01_before.png`, `02_after_create.png`, `03_after_update.png`,
   `04_after_delete.png` and assert those files exist before
   `write_vision_record`.
2. Smallest fix: four `_capture` calls on the existing helper.
   Restored empty list after reverse-clean. No new helper.
3. One live FastMCP CUD. Unique tags. Independent **Enhedspris**
   `2,00`. Reverse-clean invoice, product, customer. Prove empty.
4. Independent Grok review of the four frames plus DOM/read-back.
   Vision is never the sole pass. Accept plus purge, or FAIL and
   stay red.

## Why no child

One family on files the root already owns:

- `tests/live/test_ui_invoices_writes.py`
- `tests/unit/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. Frame capture
must run on the root live session. Research forbids a new child.
`D68E402A` forbids a new inspect helper.

Do not touch `tests/live/test_ui_products_writes.py` this slice
unless the invoice CUD already captures `/products` empty and
tagged states as extra list shots. Product four-frame restore is
a later slice.

## TDD

`728BD2E4` / design §10.4 / `C7DBE974`: failing fixture first,
then the smallest fix.

1. Add an offline fixture that source-locks the live CUD:
   - `01_before.png` after login, before customer/product/invoice
     create (empty `/invoices` list).
   - `02_after_create.png` after create execute and observer list
     proof, before update preview.
   - `03_after_update.png` after update execute (already present).
   - `04_after_delete.png` after invoice/product/customer reverse
     clean, on a fresh empty `/invoices` list.
   - The test asserts all four files exist and are non-empty
     before `write_vision_record`, same as
     `tests/live/test_ui_organizations_writes.py`.
   - Keep `author=live_test`, `reviewer_verdict=pending_review`.
   - Keep `workflow_ref=ui.parity.invoices.update` on this
     self-contained CUD (`D71E5B82`). Do not map leftover delete.
   - CUD body still must not depend on `LEFTOVER_*` names.
   Run it. Capture the fail.
2. Smallest fix:
   - Reuse `_capture(observer, slug, frame_dir / "<name>", ...)`.
   - For `01_before` and `04_after_delete`, capture the invoices
     list without requiring the tagged name (empty list). If the
     current helper requires a name on `/invoices`, extend it
     with an empty-list path inside the same function. Do not add
     a new inspect module.
   - `02_after_create` uses the tagged customer name, unfiltered.
   - `03_after_update` stays the tagged customer after **Enhedspris**
     `2,00` independent read-back.
   - No `force`. No `evaluate`. No headed browser. No live API.
3. Offline unit/contract/safety on the touched files
   (`BILLY_TEST_MODE=commit`). Then one live FastMCP CUD.
4. Independent Grok review:
   - Four frames exist outside git under owner-only `vision-tmp`.
   - DOM and second-session **Enhedspris** `2,00` plus third-session
     absence still pass.
   - On accept: purge frames, set `author=independent_review`,
     `purge_verified=true`.
   - On reject: stay red. Do not purge until the next fix.
   - Live test must not write `accept`.

If the empty-list capture cannot show the invoices empty state,
FAIL closed with sanitized URL and heading. Do not invent a
second helper.

## Out of slice

Files, daybook, **Godkend**, emails, bulk, docs re-lock, dump
remakes, leftover reverse-clean, child spawn, live API, product
four-frame restore, archive dumps, Kunde dumps, honesty-16
`tool_name` remap, coverage greening, `node finish`.

## Success

Live invoice CUD writes the four required frames. Independent
review can run. Honesty-16 stay red. Org empty after cleanup.
Do not finish.

## Budget

No cost or time cap. This is one TDD slice plus one live CUD
plus one independent review. Do not expand.

## Post-Mortem

Completed:

- Four list frames landed, then independent review FAIL
  (`A342BBDC`): missing filled create form before **Gem som
  kladde**.
- Unit fixture now requires `02_before_submit.png` after fill
  and before `click_exact` **Gem som kladde**.
- Create execute captures that frame when
  `BILLY_VISION_FRAME_DIR` is set, after DOM proof of customer,
  product, description, and unit price.
- List shots renamed `03_after_create`, `04_after_update`,
  `05_after_delete`.

Deviations:

- Pre-submit capture lives inside `_create_draft`, not the live
  test helper, because FastMCP execute owns the page.
- Quantity is filled to 1 by `fill_priced_line`. No extra Antal
  locator was added.

Review: `tmp/grok-review.md` FAIL on four list frames. Fix is
this pre-submit capture. Stay `pending_review`. Do not accept
in this step.

Tests: lint passed. Offline invoice units passed. Live CUD
passed after refilling the line description following product
bind (`run_id=60b6d620772644f3bca9609c8ae53846`,
`author=live_test`, `pending_review`). Honesty-16 stay red.

Cleanup: live CUD reverse-cleaned the disposable triple. Frames
remain in owner-only storage until independent accept.

Next unresolved coverage slice: independent review of
`02_before_submit.png` plus CUD evidence. Then honesty-16
remap. Files and daybook stay fail-closed.

