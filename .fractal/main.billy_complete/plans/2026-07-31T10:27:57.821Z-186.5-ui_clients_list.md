# 186.5 Read-only ui_clients_list shell

## Goal

Ship the third **read-only** business UI FastMCP tool for the Billy clients
list shell frozen in research104, with offline contract tests, dual-session
live interface tests (no API token), and a non-sensitive vision record that is
purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research104).  
Discovery: `.fractal/main.billy_complete/tmp/discovery103/summary.json` (clients seeds + dual read-back; frames purged).  
Prior product patterns: tip `8a0cd6f` (`ui_products_list`), `e39a154` (`ui_invoices_list`).  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` §10 UI lane.  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be` (unchanged).

## Why this slice

- Auth lane, invoices list, and products list are producted and live-qualified.
- Research103/104 dual headless sessions both reached clients shell:
  path class `/:org_slug/clients`, h1 `Kunder`, CTA `Opret kontakt` present;
  A↔B path/h1/title match; no error shell; no plan-gate heuristic.
- No stable `data-cy` on clients (empty sample) — use invoices-style
  `create_action_visible` for exact label `Opret kontakt` (presence only).
- Maps offline `api.contacts.list` (`GET /v2/contacts`) for **list-open** parity only.
- Inventory targets still red: `ui.discovery.customers`, `ui.parity.contacts.list`.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- `ui_bank_accounts_list` product (secondary freeze; next after this if free).
- Daybooks recovery or daybooks tool.
- Contact create/edit/delete UI; clicking `Opret kontakt` or `Mere`.
- API list filter/sort/pagination UI for contacts.
- Residual 29 / bulk 92 API product or live method probes.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding `auth_status` to READY.
- New login-only `ui_*` tools.
- Inventing `api_bank_accounts_*` or greening bankLines from bank-accounts UI.
- Greening contacts get/create/update/delete/bulk UI rows.
- Greening all 339 UI rows or setting `complete: true`.
- Headed browsers, generic browser MCP tools, credential tool inputs.
- Children (file ownership is one tight cluster on root).

## Ownership

**Root Grok only. No children.** File cluster mirrors invoices/products:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_clients_list.py` (new; pattern from products/invoices) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` if tool-name map required |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only `ui.discovery.customers` + `ui.parity.contacts.list` |
| Egress | `coverage/browser_egress.yaml` — append live test path to existing `mit.billy.dk` / path-scoped host `test_references` only; do not widen hosts |
| Status/report | regenerate via existing generator if required; **do not** set `complete: true` |
| Wiki (optional durable) | `wiki/ui_clients_list_shell.md` after product (EXECUTE/COMMIT may defer to post-review) |

Do **not** spawn residual/bulk, bank-accounts, or daybooks children this iteration.

## Contract freeze (research104)

### Tool

- **Name:** `ui_clients_list`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  filter, credential, or contact fields.
- **Success:** non-PII only:
  - `path_class`: literal `/:org_slug/clients`
  - `heading`: literal `Kunder`
  - `create_action_visible`: bool for exact CTA label **`Opret kontakt`**
    (presence only; never click)
  - `shell_markers_present`: bool for ≥1 shared shell marker
    (Overblik / Fakturering / Menu / Kunder) without dumping page text
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` (error shell, wrong path, missing h1 /
  required CTA), `EGRESS_DENIED`, `BILLY_ERROR`.
  Messages must not include org slug, org name, email, password, tokens,
  contact names, or addresses.
- **Navigation:**
  1. Open MCP profile under fixed egress (`coverage/browser_egress.yaml`).
  2. If login signature present → `AUTH_REQUIRED` (callers use auth tools first).
  3. Resolve org slug from session URL or outside-git identity file only
     (`~/.local/share/billy-mcp/ui-org-identity.json` pattern).
  4. Goto `https://mit.billy.dk/{slug}/clients` (same wait policy as products).
  5. Classify path/h1/CTA/shell markers; fail closed on Upsedasse / Log ind igen.
  6. Query params on real URL OK if path class remains `/:org_slug/clients`.

### Naming map

| Layer | Name |
| --- | --- |
| UI route | `/clients` |
| UI heading | `Kunder` |
| Create CTA | `Opret kontakt` (do not click) |
| Discovery inventory id | `ui.discovery.customers` (keep id; update route evidence) |
| Parity inventory id | `ui.parity.contacts.list` |
| Offline API | `api.contacts.list` / `api_contacts_list` |
| FastMCP tool | `ui_clients_list` |

### Inventory honesty when greening (only after real tests pass)

For `ui.discovery.customers` and `ui.parity.contacts.list` only:

- `discovered` / `implemented` / `contract_tested` / `live_tested` / `vision_verified` true when earned
- `tool_name`: `ui_clients_list`
- `parity_status`: `list_shell_open_only`
- `request_fields`: `[]`
- `pagination`: `null`
- `method_or_route`: observed path class `/:org_slug/clients` (not full API filter schema)
- Evidence: research104 + dual live + vision record path
- Test refs: unit + `tests/live/test_ui_clients_list.py`
- Do **not** embed API page/pageSize/include/sort fields on greened UI rows
- API `api.contacts.list` stays `live_tested: false` with out_of_scope qualification
- `coverage/status.json` `complete` stays **false**; live/vision counts may rise from 4 → 6

## Work steps (EXECUTE order)

1. **Models** — `UiClientsListInput`, `UiClientsListSuccess` in `models.py`
   (mirror invoices success field names for CTA pattern).
2. **Browser** — protocol `UiClientsListService`, constants for path/h1/CTA,
   `BrowserRuntime.ui_clients_list()` parallel to invoices/products.
3. **Server** — register `ui_clients_list` tool with typed models; injectable service seam.
4. **Unit tests** — success, AUTH_REQUIRED on login, UI_CHANGED wrong path /
   missing h1 / missing CTA / error shell, query-param path tolerance if invoices has it,
   redaction (no org/contact leakage in messages).
5. **Coverage server / inventory map** — register tool name mapping for the two rows
   if `test_coverage_inventory.py` hard-codes green tools.
6. **Live dual-session** — new `tests/live/test_ui_clients_list.py`:
   - Skip if credential refs missing
   - Ephemeral profiles A and B under owner data root
   - Login via product auth tools only
   - Call `ui_clients_list` on each; assert path/heading/create_action_visible
   - Capture frames outside repo; write vision record; purge; `purge_verified`
   - Never set or use `BILLY_API_TOKEN`
7. **Inventory YAML** — update only the two rows when tests pass; regenerate status/report.
8. **Egress test_references** — add live clients test path next to products/invoices.
9. **Wiki** — short `wiki/ui_clients_list_shell.md` (non-PII contract) if time; else note for post-commit.
10. **Offline suite before claim** — format/lint/type/unit/contract/safety as existing scripts require.
11. **Live + vision** — run clients live test; record vision ACCEPT with purge.

## Test plan

| Layer | Command / target | Pass bar |
| --- | --- | --- |
| Unit / models | `pytest tests/test_models.py tests/unit/test_browser.py tests/unit/test_coverage_server.py -q` (or project lint/test scripts) | new clients cases green |
| Inventory honesty | coverage inventory tests | only two UI rows green; complete false; API live false |
| Live dual | `pytest tests/live/test_ui_clients_list.py -m live` (credentials required) | A↔B success + vision purge |
| Safety | existing repository policy / credential scan | no secrets/frames in repo |
| Pre-commit suite | project scripts used before `fractal commit` | green offline gates |

Do **not** run live API tests. Do not require full `BILLY_TEST_MODE=ui-full` for this slice commit, but do not claim node complete.

## Risks and fail-closed rules

| Risk | Response |
| --- | --- |
| CTA missing or renamed | `UI_CHANGED`; do not invent alternate selectors from guesswork |
| Session lands on login | `AUTH_REQUIRED`; no auto-login inside list tool |
| Error shell (Upsedasse) | `UI_CHANGED` |
| Org slug missing | fail closed via existing resolve helper; no caller slug input |
| Accidental create click | never click; only count/visibility |
| Greening wrong rows | touch only customers discovery + contacts.list parity |
| Research-only greening | forbidden — tests first |

## Success criteria (slice; node still incomplete)

- Tool `ui_clients_list` registered with Pydantic I/O.
- Offline unit/contract tests pass for the tool.
- Live dual-session test passes with independent second interface session.
- Vision record exists with non-sensitive verdict + `purge_verified`.
- Inventory rows `ui.discovery.customers` and `ui.parity.contacts.list` green only as list-shell-open.
- Frames purged; repo clean of credentials/screenshots/HAR/traces.
- `coverage/status.json` still `complete: false`.
- Branch commits pushed via normal commit step.

## Review focus (next step)

- Contract matches research104 path/h1/CTA.
- No API live claims; no bulk residual greening.
- Dual read-back independence proven.
- Vision not sole pass condition.
- Message redaction.
- Inventory honesty (`list_shell_open_only`, empty request_fields).

## Management decision

No child spawn. Root implements and qualifies this file cluster. Max children
unlimited does not justify splitting a single-tool pattern already proven twice.

## Post-Mortem

### Completed

- Implemented typed `ui_clients_list` (models, browser workflow, server registration).
- Offline unit/model/registry/inventory tests; commit-mode suite 1331 passed.
- Live dual-session test passed; vision record accept with purge_verified.
- Inventory greened only `ui.discovery.customers` and `ui.parity.contacts.list`
  as `list_shell_open_only` (empty request_fields, null pagination).
- Wiki contract page `wiki/ui_clients_list_shell.md`.
- Independent review: product ACCEPT; completeness FAIL (expected).
- FIX-VERIFY: no product code fixes required; lint + offline + live reconfirm pass.

### Deviations

- None material vs plan. Shell markers use shared Overblik/Fakturering/Menu
  (same as invoices/products), not a dedicated Kunder marker.

### Review findings

- Blocking product issues: none.
- Non-blocking: node incomplete; per-row API `out_of_scope_by_user` field still
  status-level only; bulk 92 and residual UI remain red.

### Tests

- `bash scripts/lint.sh`: pass
- `BILLY_TEST_MODE=commit bash scripts/test.sh`: 1331 passed, 5 deselected
- Live: `pytest tests/live/test_ui_clients_list.py -m live`: 1 passed
- `coverage/status.json`: complete false; live/vision 6; API live 0

### Cleanup

- No business records created (read-only shell).
- Vision frames outside repo; purge_verified true.
- No credentials or raw frames in repository.

### Next unresolved coverage slice

- `ui_bank_accounts_list` (UI-only discovery; no `api_bank_accounts_*`).
- Daybooks error-shell recovery research before any daybooks product.
- Residual clear API + bulk 92 remain offline-only red.
