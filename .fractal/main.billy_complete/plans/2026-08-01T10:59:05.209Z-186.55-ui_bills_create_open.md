# 186.55 UI bills create open shell + dual-count create


## Goal

Product research154 ACCEPT:

1. New read-only FastMCP UI tool **`ui_bills_create_open`** that opens the
   Billy bill **create form** (`Opret køb` / `/:org_slug/bills/new`) as
   form/shell open only.
2. Dual-count **exactly** `ui.parity.bills.create` (maps `api.bills.create` /
   `POST /v2/bills`) onto that tool with five UI flags true + vision.
3. Green discovery row **`ui.discovery.bills_create`** (new
   `UI_DISCOVERY_FAMILIES` entry; do **not** repurpose existing bills list
   discovery / `ui_bills_list`).

Target math (from tip `6b5a67f`): live/vision **147 → 149** (discovery +
parity); GEO NA **100** unchanged; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research154)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. |
| Soft paths | `emails` / `invoiceEmails` / nonsense soft-empty body **127** dual. |
| Create form dual | Soft `/bills/new` body **621**, h1 **Opret køb**, path `/bills/new`, soft_empty **false** dual. |
| Form chrome class | Godkend, **Gem som kladde**, Træk, Upload fil, Køb, Leverandør, Bilagsdato, Forfaldsdato, Valuta, Linje #1, Beskrivelse, Udgiftskategori, Momssats, Tilføj linje. |
| Tool gap | `ui_bills_list` is list-only (CTA text class **Opret køb** already in browser constants; never enters create as success). Dual-count create onto list is resource-identity theft. |
| Isolation | Bill create form ≠ special invoice email; ≠ Levering settings; ≠ soft `/files` (Bilag Upload filer stays separate). |
| Deferred | special.invoice_email (empty invoice list + failed disposable draft; Levering blocks weak NA); files_upload pure NA rejected; postings steal rejected; products/contacts create dual-count rejected this slice; bills get/update/delete/bulk stay red. |

Brief: `tmp/grok-research.md`. Dual: `tmp/research154_residual_dual.json`.

## Non-goals

- Greening `special.invoice_email`, `special.files_upload`, `postings.*`,
  productPrices, products.create, contacts.create, tax*, bank*,
  bills.get/update/delete/bulk_*
- Dual-counting create onto `ui_bills_list`
- Clicking Godkend / Gem som kladde / Upload fil / Træk / Tilføj linje /
  supplier pickers / any submit path
- Saving or approving any bill; creating disposable records in product path
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery + dual-count create) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research154)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_bills_create_open` |
| lane | ui |
| kind | form_open_only (read-only observe) |
| shell_kind | `bills_create` |
| success model | `UiBillsCreateOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools (API create already offline-green) |

### Success model fields (non-PII)

Mirror invoices create open / list-shell strictness (`extra=forbid`, no
org_slug/url/selector secrets):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/bills/new` (scrub SPA slug; fail closed if list path only) |
| `heading` | Literal `Opret køb` |
| `shell_kind` | Literal `bills_create` |
| `draft_save_chrome_visible` | bool — **Gem som kladde** present (must not click) |
| `line_chrome_visible` | bool — Linje / Beskrivelse / **Tilføj linje** class present |
| `shell_markers_present` | bool — aggregate signature true when form is the create shell |

Optional isolation bool (e.g. `supplier_field_chrome_visible` for Leverandør)
only if tests need it; do not require special-email fields.

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Preferred path: open `https://mit.billy.dk/{slug}/bills/new` directly
   (research154 soft route dual-proved real form).
4. Fallback: open bills list shell then observe-only click **Opret køb**
   (exact CTA already referenced by list tool constants; never click if already
   on create form). Prefer direct `/bills/new` first (same as invoices create).
5. Success when:
   - path class is create form (`/:org_slug/bills/new` — **not** list
     `/:org_slug/bills` alone)
   - h1 **Opret køb**
   - form markers dual-stable class: **Gem som kladde** + line chrome
     (Tilføj linje or Linje/Beskrivelse)
6. Never click Godkend / Gem som kladde / Upload fil / Træk / Tilføj linje /
   Mere menus / Slet / supplier or category pickers.

### Reject as success

| Failure | Code class |
| --- | --- |
| Login page | AUTH_REQUIRED |
| CAPTCHA / passkey / push | AUTH_INTERACTION_REQUIRED |
| Still on list shell only (no create form h1 Opret køb) | UI_CHANGED / shell signature miss |
| Egress deny | EGRESS_DENIED |
| Soft empty SPA chrome (body ≤132, no h1) | not a success path |
| Upsedasse / Unauthorized error page | UI_CHANGED / not success |

### CTA hard ban (tests must prove no click)

Never interact with: Godkend, Gem, Gem som kladde, Upload fil, Træk, Tilføj
linje, Slet, Mere action items that submit, supplier/autocomplete selection,
bulk actions.

Product path is **open and classify only**.

## Coverage generator contract

Mirror `apply_ui_invoices_create_open_shell_evidence` pattern exactly:

1. **Constants**: tool name, model/unit/live test reference paths, vision record
   class path `tmp/vision-records/ui_bills_create_open.json`.
2. **Discovery seed**: add `"bills_create"` to `UI_DISCOVERY_FAMILIES`; in
   `build_ui_manifest` discovery loop call
   `apply_ui_bills_create_open_shell_evidence(row)` when
   `family == "bills_create"`.
3. **Dual-count** exact API id only:
   - When `api_row["id"] == "api.bills.create"`, call apply with
     `parity_of_api_create=True` for `ui.parity.bills.create`.
4. Set five UI flags true; tool_name; parity_status=`form_open_only`; evidence
   cites research154 dual + live test ref + vision record path class.
5. **Must not** green: `api.bills.list` (stays list tool),
   get/update/delete/bulk_*, special.invoice_email, files_upload, postings,
   products.create.
6. Existing bills list discovery / `ui.parity.bills.list` remain on
   `ui_bills_list` unchanged.
7. After regenerate: live/vision **149**; implemented/contract **+2** (or
   generator actual); complete **false**; GEO NA **100**; API live_tested all
   false with `out_of_scope_by_user`.
8. Inventory tests: discovery green; create parity green; residual bill ops +
   special.invoice_email red; live/vision math 147→149.

## EXECUTE steps (root)

1. **Models** (`src/billy_mcp/models.py` + `tests/test_models.py`):
   `UiBillsCreateOpenInput` (empty forbid) +
   `UiBillsCreateOpenSuccess` fields above; schema forbids secrets.
2. **Browser** (`src/billy_mcp/browser.py`): Protocol +
   `ui_bills_create_open` implementation; signature helpers for create form;
   unit tests in `tests/unit/test_browser.py` (mirror invoices create open);
   ensure list tool still never navigates into create as success.
3. **Server** (`src/billy_mcp/server.py` + registry tests): register tool with
   typed I/O; no generic browser exposure.
4. **Live test** `tests/live/test_ui_bills_create_open.py`:
   dual ephemeral profiles; DOM assert success; second-session independent
   read-back; vision record + purge outside repo; skip if credential refs
   missing; never API token; never submit CTAs.
5. **Coverage generator** + inventory tests asserting discovery green, create
   dual-count green, residuals red, live/vision math 147→149.
6. **Regenerate** coverage manifests (`scripts/generate_coverage_report.py` or
   project regen entry).
7. **Wiki** page `ui_bills_create_open_shell` + `_index` link; non-sensitive;
   cite research154 markers class only; isolation note create form ≠ list and
   ≠ special invoice email.
8. **Offline gate**: `scripts/lint.sh`; offline `scripts/test.sh` (live gated by
   credentials — follow prior invoices create open / bills list commits).
9. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Registered `ui_bills_create_open` only; typed success/error |
| Discovery | `ui.discovery.bills_create` five flags true |
| Parity | `ui.parity.bills.create` five flags true; tool same |
| Exact id | Only `api.bills.create` dual-counted (not list/get/update/delete/bulk) |
| Isolation | list stays on `ui_bills_list`; invoice_email/files_upload/postings stay red |
| CTA | No Godkend/Gem/Upload/Tilføj/Træk/Slet in product path |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **149**; GEO NA **100**; complete **false** |
| Suite | Offline lint + test.sh green; live dual when credentials configured |

## Expected coverage math

| Metric | Before (tip 6b5a67f) | After product |
| --- | --- | --- |
| live/vision | 147 | **149** |
| NA rows | 100 | 100 unchanged |
| discovery families | 36 | **37** (`bills_create`) |
| implemented/contract | 331 | ~333 |
| complete | false | false |

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| SPA path not literally `/bills/new` | Prefer direct navigation dual-proved; scrub path class; fail closed if only list path |
| Accidental Gem som kladde / Godkend click | Explicit ban list; open-only — no click on save/approve chrome |
| Dual-count onto list tool | Generator exact `api.bills.create` only; inventory assert list tool unchanged |
| Confuse with creditor balances “Opret køb” CTA constant | Bills create path + h1 Opret køb on `/bills/new`; path class must include bills/new |
| Phone/org name in evidence | Scrub PII; store class markers only in wiki/coverage |
| Live credentials missing in CI | Live test skip pattern matching sibling bills list / invoices create tests |
| Generator over-green other bills.* | Exact `api.bills.create` match only |

## Done when (this product only)

- Tool + models + server + unit/model tests landed
- Live dual-session test present and green when credentials configured
- Coverage regenerated: discovery + bills.create green; residuals red; 149
  live/vision; complete false
- Wiki + inventory tests green; lint + offline test.sh green
- IR ACCEPT (or required fixes applied)
- Ready COMMIT via fractal commit — not node finish

## Ready for EXECUTE

Root-only product from research154. No spawn. Not finish.

## Post-Mortem

### Completed

- Root EXECUTE producted `ui_bills_create_open` (models, browser, server,
  unit, live dual, coverage generator discovery `bills_create` + exact dual-count
  `api.bills.create`, wiki shell page + `_index`).
- Live multi-h1 strict-mode fix: signature uses `.first` for heading/draft/line
  visibility on Billy forms with 2× h1.
- IR **ACCEPT** (`tmp/grok-review.md`): no required fixes; optional N1–N2 deferred.
- FIX-VERIFY: lint.sh pass; wiki lint memory+project clean; offline test.sh
  **1585 passed** / 38 deselected; coverage live/vision **149**, implemented/contract
  **333**, complete **false**; create parity + discovery green; residual bill
  get/update/delete/bulk + special.invoice_email stay red.
- Vision record purge_verified true (node tmp, outside repo frames).

### Deferred

- Optional N1: list→Opret køb fallback navigation (direct `/bills/new` only).
- Optional N2: re-measure draft/line bools after signature (hardcoded True after pass).
- special.invoice_email (empty list + failed draft; Levering blocks weak NA).
- special.files_upload dual-count policy; postings nested; multi-resource tax*/bank*/productPrices.
- bills.get/update/delete/bulk_* UI.
- Bulk 92 external-contract; annual_reports org_inaccessible.
- API live remains out_of_scope_by_user.

### Notes

- Prefer residual dual-count/NA next: special.invoice_email (need disposable draft +
  send dialog dual or stronger NA package); products.create if form dual later;
  nested postings. Complete false; do not node finish.

### Test results

| Gate | Result |
| --- | --- |
| lint.sh | pass |
| wiki lint (memory + project) | clean |
| offline test.sh | 1585 passed, 38 deselected |
| live bills create open | 1 passed (EXECUTE) |
| coverage | live/vision 149; contract/impl 333; complete false |

### Cleanup

- No disposable bill left (product open-only).
- Live profiles ephemeral; vision frames purged (purge_verified true).
