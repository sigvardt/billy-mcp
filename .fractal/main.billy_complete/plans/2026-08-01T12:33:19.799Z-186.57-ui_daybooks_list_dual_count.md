# 186.57 ui daybooks list dual-count

## Goal

Dual-count green **`ui.parity.daybooks.list`** by mapping the already qualified
read-only **`ui_daybooks_open`** Kassekladde editor shell to
**`api.daybooks.list`**, using the same empty-input `shell_open_only` contract
already live for `ui.discovery.daybooks`.

No new browser workflow. No new API/UI tools. No get/create/update/delete/bulk
greening. No nested daybookTransactions*/daybookTransactionLines*/
daybookBalanceAccounts greening. No invoice_email product. No annual product.
No bulk external-contract greening.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research156;
also worktree `tmp/grok-research.md`).  
Dual evidence (tmp, non-git):
`.fractal/main.billy_complete/tmp/research156_residual_dual.json` (+ worktree
`tmp/` copy).  
Discovery product already green: `ui_daybooks_open` (research117 / plan 186.18);
wiki `wiki/ui_daybooks_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_daybooks_open.py`, vision
record class `tmp/vision-records/ui_daybooks_open.json`.  
API offline list already green: `api.daybooks.list` → daybook reads
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–156).  
Prior dual-count precedents: invoices/products/contacts/bills/transactions/
salesTaxReturns/users/accounts/organizations `.list` via `parity_of_api_list=True`
in `scripts/generate_coverage_report.py` (accounts/organizations settings-panel
and transactions list templates are closest; daybooks uses **editor** shell at
`/:org_slug/daybooks/new`).

## Why this slice (research156)

1. Dual headless sessions research156:
   - Typed `ui_daybooks_open` dual READY + success
     (`path_class=/:org_slug/daybooks/new`, `editor_markers_present=true`,
     `shell_markers_present=true`, empty heading allowed).
   - Soft route `/daybooks/new` dual non-empty (body_len class 318); bare
     `/daybooks` dual h1 **Upsedasse!** (error shell — not success).
   - Soft seeds emails/files/postings/bankLines/organizations/users dual
     soft-empty body_len **127** (= nonsense).
   - Invoice detail links **0** dual; clients row links **0**; disposable draft
     create failed (stayed on `/invoices/new`); invoice_email deferred.
   - No API token; no Send email; ephemeral profiles purged.
2. Same inventory dual-count pattern as `ui.parity.accounts.list` /
   `ui.parity.organizations.list` / `ui.parity.transactions.list`, with
   **parity_status remaining `shell_open_only`** (editor is Billy daybook UI
   surface; not a classic multi-row list route; bare list path is Upsedasse).
3. get/create/update/delete/bulk UI parity remain red (no dual-proved detail/write
   isolation; bulk external-contract on API lane).
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research156: special.invoice_email dual/NA; daybooks
   write family; daybookTransactions*; postings steal; suppliers steal contacts;
   organizations.get / users.get.

## Non-goals

- Inventing a second daybooks UI tool or `api_kassekladde_*`.
- Greening `ui.parity.daybooks.get|create|update|delete|bulk_save|bulk_delete`.
- Greening nested daybookTransactions / daybookTransactionLines /
  daybookBalanceAccounts parity rows.
- Greening `ui.parity.special.invoice_email` or inventing weak NA.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting files.*, attachments.*, postings onto transactions.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research117 live
  qualification + research156 dual proof). Optional single live daybooks dual
  reconfirm during EXECUTE if credentials available (not required for inventory
  greening if live test already green).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes expected). Node instructions: Grok owns product on
this root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_daybooks_open_shell_evidence` with
  `parity_of_api_list: bool = False` (mirror transactions/accounts dual-count);
  when True: keep `api_row_id` from parity builder (`api.daybooks.list`), append
  research156 evidence mapping, keep `parity_status=shell_open_only`; discovery
  path keeps default False and `api_row_id=None`; revise evidence so discovery
  still says daybook* parity not greened **except** when dual-count flag True;
  wire `if api_row["id"] == "api.daybooks.list": apply_...(parity_of_api_list=True)`
  in the parity builder loop (alongside accounts/users/organizations/
  transactions/salesTaxReturns) |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.daybooks.list` → `ui_daybooks_open`; qualified shell/parity green
  count **50→51** without GEO NA; live/vision total **150→151**
  (`51 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` with geo_na **100**); assert evidence
  mentions `api.daybooks.list` + research156; assert other daybooks ops stay red
  `discovery_required`; update hard-coded `== 150` and `50 + GEO` asserts |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_daybooks_open_shell.md` — dual-count note for
  `api.daybooks.list` / shell open only; non-claims get/create/update/delete/bulk
  and nested daybook* families; bare `/daybooks` Upsedasse remains; API offline
  non-live; update desc / `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_daybooks_open`
- models / server registration
- `tests/live/test_ui_daybooks_open.py` (unless evidence string asserts need a
  light refresh — prefer inventory-only)
- unit browser tests for the daybooks shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_daybooks_open` (existing) |
| Open sequence | Kassekladde editor (research117 product) |
| Success path class | `/:org_slug/daybooks/new` |
| Success h1 | empty allowed |
| Editor markers | Opret ny kassekladde; Tilføj kassekladdelinje; Ingen postering valgt |
| shell_markers_present / editor_markers_present | true dual (research156) |
| Input | empty strict model |
| Bare `/daybooks` | dual Upsedasse — **not** success path |
| Soft aliases | kassekladde / daybook not tool success path |
| CTA / write | never Opret ny kassekladde / Tilføj kassekladdelinje / Bogfør / Ny postering |
| Parity row | `ui.parity.daybooks.list` / `api_row_id=api.daybooks.list` |
| parity_status | **`shell_open_only`** |
| API invent | never invent tools beyond offline daybook API product already present |
| Distinct shells | transactions Posteringer stays separate |
| get/create/update/delete/bulk UI | stay red |
| Nested daybook* UI | stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.daybooks.list` wiring on
   `apply_ui_daybooks_open_shell_evidence`.
2. Update inventory tests counts and assertions (51 green shells without GEO;
   151 live/vision with geo NA = 51 + 100).
3. Run coverage generator; confirm status live/vision **151**, implemented/contract
   **335** (if parity counts), complete **false**.
4. Wiki dual-count note on daybooks shell page + index desc if needed.
5. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (no live API; no
   `BILLY_TEST_MODE=ui-full` completeness claim). Optional:
   live daybooks dual reconfirm only if credentials configured.
6. Memory EXECUTE notes; leave dual JSON under tmp (non-git) for IR.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.daybooks.list` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_daybooks_open` |
| parity_status | `shell_open_only` |
| api_row_id | `api.daybooks.list` |
| Evidence | research156 (+ research117 shell) + maps api.daybooks.list; no PII |
| Other daybooks ops | stay red / discovery_required |
| Nested daybook* families | stay incomplete / red |
| Deferred specials | invoice_email remains red; files_upload stays green from 186.56 |
| Tools | No new tool registrations |
| API live | All API live_tested remain false with out_of_scope_by_user |
| Complete | `complete: false` |
| Offline tests | Inventory + non-live suite green |

## Independent review focus

- Confirm research156 dual proves daybooks tool + editor markers + bare
  `/daybooks` Upsedasse rejection (not claiming bare list as success).
- Confirm only `.list` is greened; get/create/update/delete/bulk stay red.
- Confirm `shell_open_only` honesty (editor at daybooks/new is Billy daybook
  surface; not inventing a multi-row daybooks index).
- Confirm live/vision 151 math: 51 shells + 100 GEO NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.
- Confirm invoice_email isolation deferred correctly (empty list + failed draft).

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects editor shell as daybooks.list | Same bar as accounts Kontoplan / orgs company dual-count; shell_open_only; bare Upsedasse dual documented; only list greened |
| Accidentally greening all daybooks ops | Explicit per-op id gate `api.daybooks.list` only |
| Count drift (50→51 shells; 150→151 live) | Update every hard-coded 50 / 150 assertion in inventory tests |
| Wiki still claims “does not green daybook* parity” | Edit dual-count note carefully; keep non-claims for other ops and nested families |
| Stealing transactions green | Explicit isolation: Posteringer tool and path remain separate; evidence bans dual-count steal |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **151**; complete **false**.
- Ready independent review of dual-count package 186.57.
- Not node finish.

## Post-Mortem

### Delivered

- Dual-count exact `api.daybooks.list` → existing `ui_daybooks_open`
  (`shell_open_only`; research156 / plan 186.57).
- Generator `parity_of_api_list` flag + parity-builder exact-id wiring.
- Inventory tests: qualified shells 50→51; live/vision 150→151; daybooks.list
  green asserts; other daybooks.* residual red.
- Wiki dual-count note on `ui_daybooks_open_shell` + `_index` desc.
- Coverage regenerated: live/vision **151**, implemented/contract **335**,
  complete **false**.
- IR **ACCEPT** (`tmp/grok-review.md`): no required fixes; optional N1–N2 deferred.
- FIX-VERIFY: lint.sh pass; wiki lint memory+project clean; offline test.sh
  **1586 passed** / 38 deselected; daybooks.list green; invoice_email + other
  daybooks.* residual red; bulk 92 external-contract; annual stay red.
- No new UI/API tools; no src browser/server changes.

### Deferred

- Optional N1: vision record `workflow_ref` also name `ui.parity.daybooks.list`
  on next live reconfirm.
- Optional N2: explicit nested daybookTransactions* residual family assert.
- special.invoice_email (empty list + failed draft; Levering blocks weak NA).
- daybooks get/create/update/delete/bulk; nested daybookTransactions* families.
- products/contacts create form open; postings nested.
- bulk 92 external-contract; annual_reports org_inaccessible.
- API live remains out_of_scope_by_user.

### Notes

- Prefer residual dual-count/NA next: special.invoice_email only with disposable
  draft + valid client dual; else multi-resource dual packages. Complete false;
  do not node finish.

### Test results

| Gate | Result |
| --- | --- |
| lint.sh | pass |
| wiki lint (memory + project) | clean |
| offline test.sh | 1586 passed, 38 deselected |
| coverage | live/vision 151; contract/impl 335; complete false |

### Cleanup

- No daybook writes performed.
- Research156 draft left no disposable invoice; profiles purged.
- Vision frames previously purge_verified true (node tmp).
