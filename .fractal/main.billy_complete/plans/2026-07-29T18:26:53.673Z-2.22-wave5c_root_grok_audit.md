# 2.22 Wave-5c root Grok audit

## Objective

Obtain the operator-required independent **Grok** verdict for the exact merged
Wave-5c product tree at `f235ac2` before any further API-write cohort begins.
This audit validates only the offline ticketed daybook-transaction and
daybook-transaction-line slice integrated at `51bdc22` and retained at the
current tip; it must not imply live, UI, vision, bulk, special-route, or
overall-completeness acceptance.

## Evidence and scope

- The audit contract is the current official Billy documentation cited in
  `.fractal/main.billy_complete/tmp/grok-research.md`, whose current fingerprint
  is ETag `hsisik4g9p3603` / MD5 `c2efda0ee4cf9cf200e14910c5fc6996`, plus the
  approved design sections 4.2, 5.3, 6.1, 8, and 12.
- The frozen Wave-5c contract is
  `wiki/wave_fivec_ticketed_writes_contract.md`; the existing fallback record
  in `wiki/wave_fivec_product_fallback_review.md` is explicitly non-Grok,
  offline-only evidence and cannot close this gate.
- In scope: the two Wave-5c write registrars and their focused tests, shared
  confirmation/write-protocol behaviour, root registration, generator-owned
  six-row evidence map, and generated coverage artifacts.
- Out of scope: production code changes, a Wave-5d invoice/bill implementation,
  all live calls, browser/UI activity, raw visual evidence, bulk inference, and
  any claim that `coverage/status.json` is complete.

## Delegation decision

Do not spawn a child. The operator requires the authenticated **root**
independent-review step, and that step is already pinned to `agent: grok`.
The dedicated Grok review child failed before reviewing because it was not
authenticated; duplicating the audit in another child would neither satisfy the
root-only directive nor improve independence.

## Execution and verification

1. Preserve the reviewed product tree: inspect `f235ac2` and the Wave-5c
   integration commit `51bdc22`; do not modify `src/`, `tests/`, coverage rows,
   or tool registration before the Grok verdict. The already staged memory
   correction only records the authorised review route.
2. Run the focused offline baseline from the root worktree:

   ```sh
   uv run pytest -q \
     tests/api/test_daybook_transaction_writes.py \
     tests/api/test_daybook_transaction_line_writes.py \
     tests/api/test_daybook_transaction_cross_executor.py \
     tests/unit/test_write_protocol.py \
     tests/unit/test_confirmations.py \
     tests/unit/test_coverage_server.py \
     tests/coverage/test_coverage_inventory.py
   bash .fractal/main.billy_complete/scripts/lint.sh
   bash .fractal/main.billy_complete/scripts/test.sh
   ```

   Verify the resulting server surface remains 154 `api_*` tools, exactly six
   Wave-5c rows are offline implemented/contract-tested, `live_tested` stays
   zero, and generated `complete` stays `false`.
3. In the root Grok independent-review step, compare the clean committed source
   and generated artifacts to the cited documentation and Wave-5c freeze. The
   report must independently check exact singular request roots, POST/PUT/DELETE
   paths, bodyless delete, mapped roots, typed error mapping, preview
   non-mutation, ticket-only executes, executor mismatch before consume/HTTP,
   replay/expiry/pruning, zero write retry, no secret/raw-browser evidence, and
   no premature coverage greens. It must write an explicit ACCEPT or FAIL with
   direct citations to `.fractal/main.billy_complete/tmp/grok-review.md`.
4. In Fix-Verify, reproduce each reported finding. If Grok ACCEPTs, preserve a
   durable non-sensitive Grok-review summary in
   `wiki/wave_fivec_product_independent_review.md`, retaining the exact commit,
   evidence commands, verdict, and fail-closed qualifications; update memory
   and unsave the operator directive. If it FAILs, fix only confirmed defects,
   keep all affected rows red, record the failure, and defer all next-slice work
   until a fresh root Grok re-review ACCEPTs the changed tree.

## Acceptance boundary

- The only newly closed gate is an explicit Grok product-audit verdict for
  Wave-5c at `f235ac2`; it never substitutes for live qualification or global
  completion.
- No next write cohort begins without Grok ACCEPT.
- The branch remains free of credentials, token values, screenshots, frames,
  HAR files, traces, or sensitive Billy data.


## Post-Mortem

- Completed: root Grok independent-review step ACCEPTed Wave-5c offline product at tip `f235ac2`.
- Durable record: `wiki/wave_fivec_product_independent_review.md`; scratch `tmp/grok-review.md`.
- Verification: 105 focused tests, 699 full offline suite, lint/type/coverage/policy all passed.
- Operator radio directive for Wave-5c Grok audit unsaved after ACCEPT reply.
- No production code changed. Wave-5d remains the next write cohort under Plan/Implement.
