# 142.5 Wave-5r handoff integration and freeze review

## Decision

The completed child handoffs are ready but could not be merged in this
iteration's PREPARE pass because they committed afterwards. Research74 is a
cited current-doc package, not product authority. Preserve and validate its
wiki/state artifacts now; integrate the two child branches during the next
PREPARE pass; then delegate the separate Wave-5r freeze acceptance to Grok.

No source, server registration, tests, inventory flags, coverage counts, or
live/UI/browser evidence may change in this iteration. In particular,
`api.salesTaxReturns.update` remains red until the independent freeze review
accepts the merged contract.

## Current evidence and integration queue

| Item | Status | Required handling |
| --- | --- | --- |
| Research74 | Official docs ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`; method gates POST 405, PUT 401, DELETE 405 | Keep its research page and state update; no coverage change. |
| Wave-5r freeze | `main.billy_complete.wave5r_sales_tax_returns_freeze` @ `a8590b4`; page MD5 `078aca13828b5e0d71b454c1aa2dc00f` | Merge as a no-fast-forward child handoff in next PREPARE. |
| Wave-5q product IR | `main.billy_complete.wave5q_users_product_review` @ `4b1cffb`; offline ACCEPT | Merge as a no-fast-forward child handoff in next PREPARE, then regenerate the project wiki index. |

## This iteration

1. Review the uncommitted Research74 state, plan, and wiki artifacts against
   `tmp/grok-research.md`, the approved design, and the existing coverage
   boundaries. Retain only non-sensitive cited evidence.
2. Run `wiki update` and `wiki lint` for the shared wiki so the new research
   page has generated navigation and no structural defects. Run whitespace and
   repository policy checks. Confirm the inventory remains 178 offline, 0
   live, 0 vision, 92 ambiguous-bulk red, and `complete: false`.
3. Keep the child branches unmerged during this post-PREPARE iteration. Their
   deliverables are committed and are intentionally queued for the mandatory
   next PREPARE merge; do not copy or cherry-pick child files.
4. Commit the validated research records and this plan only after the required
   non-live format, lint, type, unit, contract, coverage-policy, and safety
   checks pass.

## Next PREPARE integration and delegation

1. Merge both committed child branches with labelled no-fast-forward merges,
   review their three-dot diffs, run `wiki update` and `wiki lint`, and post an
   integration note naming the merged handoffs.
2. After the Wave-5r freeze page is on the root branch, create the independent
   reviewer as a childless Grok leaf. It owns only
   `wiki/wave_fiver_freeze_independent_review.md`, must independently fetch
   and cite the current official docs, and must issue an unambiguous ACCEPT or
   REJECT without source or coverage changes:

   ```sh
   fractal node init wave5r_sales_tax_returns_freeze_review \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --base=main.billy_complete --agent=grok \
     --max-depth=0 --max-children=0 --max-descendants=0 \
     --title='Wave-5r Sales-Tax-Returns Freeze Independent Review'
   ```

3. Do not create the Codex Power product child until that Grok verdict is
   ACCEPT. Its later ownership must be restricted to
   `sales_tax_return_writes.py`, its focused tests, server registration,
   coverage-generator evidence, generated coverage artifacts, and registry
   assertions. It must implement precisely the two tools in
   `wiki/wave_fiver_sales_tax_returns_product_ready_research.md` and advance
   offline coverage only from 178 to 179.

## Verification and fail-closed conditions

- Validate only documented `PUT /salesTaxReturns/:id` with outer
  `{id, salesTaxReturn}` and required `salesTaxReturns` response root.
- Keep create/delete closed by 405, all bulk rows red, and opaque blank-notes
  fields free of writable or reversible claims.
- Execute remains ticket-only, single-use, exact-bound, one-write/no-retry in
  the later product slice; no caller confirmation boolean or generic control
  can be introduced.
- Any non-ACCEPT freeze verdict, doc/probe drift, missing child diff evidence,
  or failed structural/policy check stops product planning and leaves the row
  red.

## Post-Mortem

- Completed: retained the cited Research74 package and its independent Grok
  research acceptance without adding product code, tools, tests, or coverage
  evidence.
- Verification: project wiki update/lint, Ruff, Pyright, coverage-policy,
  repository-policy, and 1,146 non-live/non-vision tests passed.
- Review finding: no contract defect was reported. The required freeze-root
  merge and separate freeze IR remain blockers by design, not rejected
  findings.
- Cleanup: no credentials, live writes, browser session, screenshot, HAR,
  trace, or sensitive evidence was created; coverage remains 178/178/0/0 and
  `complete: false`.
- Next unresolved coverage slice: merge the committed Wave-5r freeze and
  Wave-5q product-review wiki handoffs, then obtain a Grok freeze IR before
  creating the bounded Wave-5r product leaf.
