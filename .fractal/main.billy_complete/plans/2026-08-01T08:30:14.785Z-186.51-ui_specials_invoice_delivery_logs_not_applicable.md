# 186.51 ui specials invoice_delivery+invoice_logs not_applicable freeze


## Goal

Product research150 into a durable UI-parity package that:

1. Marks **evidence-backed `not_applicable`** on dual-proved UI parity rows for
   **two specials only** (**2 rows total**):
   - `ui.parity.special.invoice_delivery` ↔ `api.special.invoice_delivery`
   - `ui.parity.special.invoice_logs` ↔ `api.special.invoice_logs`
2. Extends the existing geo/reference NA applicator path in
   `scripts/generate_coverage_report.py` so those two **exact** special API ids
   are included (never a bare `api.special.` prefix that would green
   invoice_email / files_upload / user_*).
3. Updates inventory tests for NA count 98→**100** and live/vision baseline
   140→**142** (`42` shell/parity dual-counts + `100` NA).
4. Adds a scrubbed wiki page for the freeze; one-line cross-link from peer NA
   wiki pages (invoiceReminders / research144 join NA / geo).
5. Regenerates coverage; **complete stays false**; bulk 92 external-contract
   red unchanged; annual remains NA-rejected red; **no** new `ui_*` tools for
   these specials.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research150).  
Dual evidence (tmp, non-git): `tmp/research150_residual_dual.json`,
`tmp/research150_levering_context_dual.json` (plus research149 shell markers for
settings_invoicing body 1494).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged vs research103–149).  
Design: §10.2 — UI parity `not_applicable` only when Billy exposes **no
equivalent UI workflow**; contrast annual
(`wiki/ui_annual_reports_inaccessible.md`) where nav exists → NA rejected.  
Prior packages: 186.39–50 geo/join/meta NA and dual-counts; 186.50
invoiceReminders NA (5).

## Why this slice

1. research149 deferred specials due to settings **Levering** ambiguity.
2. research150 re-probed residual soft seeds after 186.50:
   - soft `/invoiceDeliveries` and `/invoiceLogs` (and kebab) body_len **127**,
     empty h1, dual-agree identical to nonsense `zz-research150-no-such-route`.
   - dual tools READY: invoices (Fakturaer), settings_invoicing,
     transactions, uploads, bank_recon, daybooks, etc.
3. Levering isolation dual on `/:org_slug/settings/invoicing` (SPA bare
   `/settings`, body_len **1494** dual): scrubbed context is
   **"Levering af faktura pr. e-mail"** — email invoice delivery **settings**
   text. Markers dual: levering true; e_invoice/GLN/NemHandel/Peppol **false**;
   sendt / invoice-log markers **false**.
4. Therefore Levering blocks pure NA for **`special.invoice_email` only**. It
   does **not** establish an e-invoice (`invoiceDeliveries`) workflow or an
   invoiceLogs UI workflow.
5. Official docs: `POST /v2/invoiceDeliveries` is e-invoice (receiverIdType/gln);
   `GET /v2/invoiceLogs` is async delivery/email log poll — no dedicated Billy
   UI list/shell found in dual soft paths or greened shells.
6. Soft URL landing is not a productable specials shell — do not invent UI
   tools for these two specials.
7. Reuse evidence code `GEO_UI_NO_EQUIVALENT_WORKFLOW` (same absence class as
   research144–149).
8. Explicit rejections/deferrals stay red this slice:
   - `special.invoice_email` (email Levering settings related)
   - `special.user_get` / `special.user_organizations` (org switcher / profil
     ambiguity)
   - `special.files_upload` (Bilag greened)
   - attachments pure NA (Bilag bilag/upload markers dual true)
   - taxRateDeductionComponents / taxRates (Momssatser multi-resource)
   - productPrices (`/products/new` non-empty dual)
   - bankLine*/bankPayments (recon tool greened; nested isolation weak)
   - daybook* (editor greened; list dual-count weak)
   - postings dual-count onto transactions (secondary, not this slice)
   - files (Bilag weak map)

## Non-goals

- Shipping any new FastMCP tools (`ui_invoice_deliveries_*`,
  `ui_invoice_logs_*`).
- Greening soft-empty paths as list shells.
- Dual-counting invoice_delivery or invoice_logs onto `ui_invoices_list`
  (different resource; no e-invoice/log markers on Fakturaer shell).
- Greening `special.invoice_email` / user_* / files_upload this slice.
- Dual-counting daybooks.list / taxRates / files / bankLines / postings /
  productPrices this slice.
- Re-touching invoiceReminders / invoiceReminderAssociations (already NA green).
- Greening `ui.discovery.annual_reports` or marking it `not_applicable`.
- Greening bulk (92) API tools or inventing bulk tools.
- Changing offline API specials tools (API lane separate; live_tested stays
  false + out_of_scope_by_user).
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- `complete: true` or node finish.

## Ownership

**Root product. No children** (generator + inventory tests + wiki; checker
likely untouched). Grok owns EXECUTE on this root (node instruction: only
`grok`). Independent review remains Grok.

| Area | Paths |
| --- | --- |
| NA prefixes + count | `scripts/generate_coverage_report.py` — extend
  `GEO_UI_NOT_APPLICABLE_API_PREFIXES` with **exact** ids
  `api.special.invoice_delivery` and `api.special.invoice_logs` (startwith
  match is exact for single-row specials; **do not** add `api.special.`).
  Set `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 100` (prior 98 + **2**). Add
  research150 identity set for evidence branch — prefer full-id frozenset
  e.g. `GEO_UI_NOT_APPLICABLE_RESEARCH150_SPECIAL_IDS =
  frozenset({"api.special.invoice_delivery", "api.special.invoice_logs"})`
  rather than resource=`special` (would be ambiguous for all specials). Prefer
  **same evidence code** `GEO_UI_NO_EQUIVALENT_WORKFLOW`. Update header comment
  that research150 adds specials invoice_delivery + invoice_logs (2 ops). |
| Applicator evidence text | `apply_ui_geo_reference_not_applicable_evidence` —
  branch research150 when `api_row_id` in RESEARCH150_SPECIAL_IDS:
  evidence_ref `research150_specials_invoice_delivery_logs_dual`; nonsense path
  `zz-research150-no-such-route`; dual flag e.g.
  `dual_agree_soft_empty_specials_delivery_logs`; family_label e.g.
  `special.invoice_delivery+invoice_logs`; contrast controls include
  invoices (Fakturaer), settings_invoicing (email Levering contrast),
  uploads, transactions. Evidence text must state: settings Levering is
  **"Levering af faktura pr. e-mail"** (email settings, not e-invoice/logs
  workflow); e_invoice markers dual false. Keep research138–149 branches
  intact. Order branch checks so research150 is evaluated **before** older
  catch-alls (and before any future generic special handling). |
| Wire in UI parity loop | Already calls applicator when
  `geo_ui_not_applicable_api_row(api_row_id)`; exact prefixes should wire both
  parity rows — verify in `build_ui_manifest`. Confirm resource split
  `api.special.invoice_delivery` → middle segment `special` does not break
  other branches (research150 uses full-id match, not resource name alone). |
| Qualification object | Same shape: `kind=ui_not_applicable`,
  `evidence_code=GEO_UI_NO_EQUIVALENT_WORKFLOW`,
  `not_applicable_decision=accepted`, `sessions=dual_independent_ephemeral`,
  docs etag/md5, evidence_ref, contrast_controls. |
| Row flags | Per NA row: five flags true; `parity_status=not_applicable`;
  empty `tool_name`; `vision_evidence=None`; no PII/slugs in evidence. |
| check_coverage | Prefer **no change** if existing NA validation covers
  required fields. |
| Inventory tests | `tests/coverage/test_coverage_inventory.py`:
  - NA row count uses `GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **100**.
  - research150 evidence_ref branch assertions (mirror research149).
  - live/vision `42 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` → **142** when
    ROW_COUNT=100.
  - Assert two special rows
    `api.special.invoice_delivery` / `api.special.invoice_logs` are NA green
    (via ui parity rows).
  - Keep research149 invoiceReminders asserts green.
  - Assert `api.special.invoice_email`, `files_upload`, `user_get`,
    `user_organizations` stay red (not greened by accidental `api.special.`
    prefix).
  - attachments/specials other reds stay as intended; bulk 92 + annual red. |
| Wiki | New `wiki/ui_specials_invoice_delivery_logs_not_applicable.md` (scrubbed
  dual evidence; Levering email contrast; no credentials/slugs/frames). Update
  `wiki/_index.md`. One-line peer cross-link from
  `wiki/ui_invoice_reminders_not_applicable.md` and/or related specials/NA
  pages. |
| Coverage regenerate | Run generator → refresh `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md`. implemented/contract may move
  with UI NA greening (historical pattern: 324→~326). complete=false; API
  live_tested false. |

## EXECUTE steps (ordered)

1. Patch `scripts/generate_coverage_report.py` constants + research150 branch in
   applicator (exact special ids, ROW_COUNT 100, RESEARCH150 set, evidence_ref,
   Levering email contrast in evidence prose, dual flag).
2. Patch `tests/coverage/test_coverage_inventory.py` for 100 / 142 / two-row
   specials asserts + research150 evidence_ref; assert sibling specials stay
   red; keep older package tests.
3. Add wiki freeze page + `_index` + peer cross-link(s).
4. Regenerate coverage artifacts via project generator/check scripts as used in
   186.50 (do not hand-edit YAML status).
5. Run lint + offline `test.sh` (no live API; live UI suite not required for this
   inventory-only freeze before IR, matching 186.48–186.50 pattern).
6. Leave residual dual JSON in owner tmp only (not commit).

## Acceptance

- Two `ui.parity.special.invoice_delivery` and `ui.parity.special.invoice_logs`
  rows: discovered/implemented/contract_tested/live_tested/vision_verified all
  true; `parity_status=not_applicable`; empty tool_name; evidence_code
  `GEO_UI_NO_EQUIVALENT_WORKFLOW`; evidence_ref contains research150;
  not_applicable_decision accepted.
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 100`; live/vision status 142.
- No new ui_* tools; no greening of invoice_email / files_upload / user_* specials.
- attachments / bank* / productPrices / daybooks / taxRates residual red as
  intended.
- invoiceReminders remains NA green (unchanged).
- complete=false; bulk 92 red; annual red; API live_tested false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Risks

| Risk | Mitigation |
| --- | --- |
| Accidental `api.special.` prefix greens all 6 specials | Use **exact** full-ids only; tests assert invoice_email/files_upload/user_* stay red |
| Greening invoice_email with delivery/logs | Explicit non-goal; Levering email context is the reason email stays red |
| resource=`special` middle segment confuses research150 branch | Match on full `api_row_id` frozenset, not resource name alone |
| Dual-count onto invoices by mistake | Assert empty tool_name + not_applicable; invoices shell stays invoices.list only |
| ROW_COUNT drift | Single constant drives tests + status formula |
| Settings bare path vs invoicing panel | Evidence cites settings/**invoicing** seed body 1494 + email Levering string |

## Post-mortem

- **Outcome:** Producted research150 specials GEO NA freeze for
  `invoice_delivery` + `invoice_logs` (2 ops, exact API ids). IR **ACCEPT**
  with no required fixes. Offline suite and lint green.
- **Actual live/vision:** 140 → **142** (42 shells + 100 NA).
- **Actual NA count:** 98 → **100**.
- **Actual implemented/contract:** 324 → **326**.
- **complete:** still **false** (expected).
- **Tests:** lint.sh pass; test.sh offline **1565 passed**, 35 deselected.
- **Deviations:** none vs plan. Optional IR N1–N2 deferred (test docstring
  research list stale; bare `/settings` vs invoicing panel already documented
  in research150 brief).
- **Cleanup:** no disposable Billy records (observe-only research). Profiles
  purged during research. No credentials/frames in repo.
- **Next unresolved coverage slice:** residual UI parity — special.invoice_email
  (email Levering settings), user_get/user_organizations, files_upload/Bilag
  map, taxRateDeduction/taxRates multi-resource, bank recon match isolation,
  postings dual-count onto transactions, productPrices nested, daybooks weak
  list map; bulk 92 external-contract and annual_reports org_inaccessible
  remain red.
- **Not done:** node finish; complete true; live API; invent UI tools for
  greened specials.

