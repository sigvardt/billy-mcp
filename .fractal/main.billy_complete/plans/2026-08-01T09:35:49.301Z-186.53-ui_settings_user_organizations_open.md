# 186.53 UI settings user organizations open shell + dual-count


## Goal

Product research152 ACCEPT:

1. New read-only FastMCP UI tool **`ui_settings_user_organizations_open`** that
   opens the Billy **Indstillinger → Profil group → Virksomheder** multi-org
   membership panel (shell_open_only).
2. Dual-count **exactly** `ui.parity.special.user_organizations` (maps
   `api.special.user_organizations` / `GET /v2/user/organizations`) onto that
   tool with five UI flags true + vision.
3. Green discovery row **`ui.discovery.settings_user_organizations`** (or
   generator-consistent id) for the new shell.

Target math (from tip `41ad5ee`): live/vision **143 → 145** (discovery +
parity); GEO NA **100** unchanged; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research152)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. Docs: “list your user's companies through GET /v2/user/organizations”. |
| Soft paths | `userOrganizations` / `user-organizations` / `settings/virksomheder` soft-empty == nonsense body **127** dual (SPA needs nav click). |
| Profil vs Virksomheder dual | Profil click body **523** (Fulde navn / Billede / Sprog og tema / Skift adgangskode). Virksomheder click body **298** (Virksomheder, **Opret organisation**, **Alle organisationer**, Navn, CVR list chrome). Path stays `/:org_slug/settings`. Dual agree. |
| Tool gap | No greened tool opens Virksomheder panel. Dual-count onto `ui_settings_user_open` or `ui_settings_company_open` is resource-identity theft. |
| Isolation | Distinct from `user_get` (Profil), `users.list` (Brugere), `organizations` company settings (single-org form). |
| Deferred | invoice_email (empty invoice list dual); files_upload pure NA rejected; postings→transactions steal rejected. |

Brief: `tmp/grok-research.md`. Dual: `tmp/research152_residual_dual.json`.

## Non-goals

- Greening `special.invoice_email`, `special.files_upload`, `postings.*`,
  productPrices, tax*, bank*, daybooks list dual-count
- Bulk 92 / annual_reports
- Dual-counting user_organizations onto Profil or company tools
- Clicking Opret organisation / Gem / Upload / Slet / password / Send
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery + dual-count special) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research152)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_settings_user_organizations_open` |
| lane | ui |
| kind | shell_open_only (read-only observe) |
| shell_kind | `settings_user_organizations` (or generator-consistent string) |
| success model | `UiSettingsUserOrganizationsOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no `api_settings_*` |

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open `https://mit.billy.dk/{slug}/settings`.
4. Observe-only side-nav: open **Profil** group if required, then click
   **Virksomheder** (exact label dual-proved research152).
5. Success when:
   - path class stays settings hub (`/:org_slug/settings` or bare `/settings`
     class already accepted by sibling settings tools)
   - h1 **Indstillinger**
   - panel markers present dual-stable class:
     - `Virksomheder` true
     - (`Alle organisationer` **or** org list chrome **Navn** + **CVR**)
     - Prefer also detect **Opret organisation** as list chrome (must **not**
       click it)
   - Profil-only user-edit markers must **not** be required for success
     (Fulde navn / Billede / Gem are Profil panel, not this panel)
6. Never click Opret organisation / Gem ændringer / Upload / password / Slet /
   Tilføj / Send.

### Reject as success

| Failure | Code class |
| --- | --- |
| Login page | AUTH_REQUIRED |
| CAPTCHA / passkey / push | AUTH_INTERACTION_REQUIRED |
| Wrong panel (still Profil user fields only; no Virksomheder list chrome) | UI_CHANGED / shell signature miss |
| Egress deny | EGRESS_DENIED |
| Soft URL only without nav click | not a success path (research152 soft empty) |

### CTA hard ban (tests must prove no click)

Never interact with: Opret organisation, Gem ændringer, Upload, password
submit, Slet, Tilføj, Send e-mail, bulk actions.

## Coverage generator contract

Mirror `apply_ui_settings_user_open_shell_evidence` / users open patterns:

1. **Discovery seed** `settings_user_organizations` in UI discovery families so
   `ui.discovery.settings_user_organizations` greens via
   `apply_ui_settings_user_organizations_open_shell_evidence(row)`.
2. **Dual-count** exact id only:
   - When building `ui.parity.special.user_organizations` (api_row_id
     `api.special.user_organizations`), call apply with
     `parity_of_special_user_organizations=True` (or equivalent branch that
     matches **exact** special id, not bare `api.special.`).
3. Set five UI flags true; tool_name; parity_status=`shell_open_only`; evidence
   cites research152 dual + live test ref + vision record path class.
4. **Must not** green: user_get (already green), users.*, organizations.*,
   invoice_email, files_upload, company settings rows.
5. Test references: model + unit browser + live test paths.
6. After regenerate: live/vision **145**; implemented/contract **+2** (or
   generator actual); complete **false**; GEO NA **100**; API live_tested all
   false with `out_of_scope_by_user`.

## EXECUTE steps (root)

1. **Models** (`src/billy_mcp/models.py` + `tests/test_models.py`): success
   model with `shell_kind`, path/heading markers, org-list panel marker bool
   (e.g. `user_organizations_panel_markers_present`).
2. **Browser** (`src/billy_mcp/browser.py`): implement
   `ui_settings_user_organizations_open` following `ui_settings_user_open`
   structure; reuse side-nav click helper; Virksomheder signature helpers;
   unit tests in `tests/unit/test_browser.py` (mock page where pattern exists).
3. **Server** (`src/billy_mcp/server.py` + registry tests): register tool with
   typed I/O; no generic browser exposure.
4. **Live test** `tests/live/test_ui_settings_user_organizations_open.py`:
   dual ephemeral profiles; DOM assert success; second-session independent
   read-back; vision record + purge outside repo; skip if credential refs
   missing; never API token.
5. **Coverage generator** + inventory tests asserting discovery green, special
   dual-count green, residual specials red, live/vision math 143→145.
6. **Regenerate** coverage manifests (`scripts/generate_coverage_report.py` or
   project regen entry).
7. **Wiki** page `ui_settings_user_organizations_open_shell` (or consistent
   name) + `_index` link; non-sensitive; cite research152 markers class only.
8. **Offline gate**: `scripts/lint.sh`; offline `scripts/test.sh` (no live
   required for COMMIT path if live gated by credentials in CI — follow prior
   settings_* shell commits: offline full + live when refs present in
   FIX-VERIFY).
9. Ready INDEPENDENT-REVIEW then FIX-VERIFY then COMMIT.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Tool | Registered `ui_settings_user_organizations_open` only; typed success/error |
| Discovery | `ui.discovery.settings_user_organizations` five flags true |
| Parity | `ui.parity.special.user_organizations` five flags true; tool same |
| Exact id | Only `api.special.user_organizations` dual-counted (not bare special.) |
| Isolation | user_get remains on settings_user; users on settings_users; company on company; invoice_email/files_upload/postings stay red |
| CTA | No Opret/Gem/Upload/password/Slet in product path |
| Live | Dual session DOM + second read-back + vision purge record |
| API live | All API live_tested false + out_of_scope_by_user |
| Math | live/vision **145**; GEO NA **100**; complete **false** |
| Offline | lint + offline test suite green |
| Secrets | No tokens/PII in repo/wiki/coverage |

## Independent review focus

- Resource identity: multi-org membership list ≠ Profil user ≠ company form ≠ Brugere.
- Exact special id wiring only.
- Soft empty alone is not the green path; greened tool + dual reconfirm is.
- No accidental greening of invoice_email via Levering or files_upload via Bilag.
- live/vision math: discovery +1 and parity +1 → 145; complete false.
- Never click Opret organisation in product or tests.

## Expected coverage math

| Metric | Before (tip 41ad5ee) | After product |
| --- | --- | --- |
| live/vision | 143 | **145** |
| NA rows | 100 | 100 unchanged |
| shell / dual-count greened specials path | 43 dual-count shells class | + discovery +1 special |
| implemented/contract | 327 | ~329 |
| complete | false | false |

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| SPA label drift | Fail closed UI_CHANGED; dual-proved Danish labels from research152 |
| Clicking Opret by accidental role match | Explicit ban list; click only exact Virksomheder nav |
| Phone/org name in evidence | Scrub PII; store class markers only in wiki/coverage |
| Live credentials missing in CI | Live test skip pattern matching sibling settings_* tests; still ship offline contract + inventory |
| Generator over-green bare `api.special.` | Exact id match only (same rule as user_get dual-count) |

## Done when (this product only)

- Tool + models + server + unit/model tests landed
- Live dual-session test present and green when credentials configured
- Coverage regenerated: discovery + special.user_organizations green; residuals red; 145 live/vision; complete false
- Wiki + inventory tests green; lint + offline test.sh green
- IR ACCEPT (or required fixes applied)
- Ready COMMIT via fractal commit — not node finish

## Ready for EXECUTE

Root-only product from research152. No spawn. Not finish.

## Post-Mortem

### Completed

- Producted new `ui_settings_user_organizations_open` (Profil → Virksomheder
  multi-org panel; markers Virksomheder + Alle organisationer + Opret
  organisation chrome only).
- Dual-count exact `ui.parity.special.user_organizations` /
  `api.special.user_organizations` onto that tool; discovery
  `ui.discovery.settings_user_organizations` greened.
- Models, server registration, unit/browser/server tests, live dual-session
  test + vision purge record (`purge_verified: true`).
- Wiki shell page + `_index` link; note on `ui_settings_user_open_shell` that
  Virksomheder is a separate tool.
- Regenerated coverage: live/vision **145**, implemented/contract **329**,
  GEO NA **100**, complete **false**. Residual specials invoice_email /
  files_upload stay red.

### Deviations

- None vs plan 186.53. Optional IR N1–N2 left as notes only (no product change).

### Review

- Independent Grok review (`tmp/grok-review.md`): **ACCEPT** product; no
  required fixes. Overall completeness **FAIL** (expected: bulk 92, annual,
  residual UI).

### Tests

- FIX-VERIFY: `scripts/lint.sh` green; offline `scripts/test.sh` **1571
  passed**, 36 deselected. Wiki lint clean (project + memory).
- Live dual test passed during EXECUTE (1 passed). Did not run
  `BILLY_TEST_MODE=ui-full` or `full` (complete still false; not a
  completeness claim).

### Cleanup

- No disposable Billy records created this slice (read-only shell).
- Research dual profiles purged; live vision frames outside repo and purge
  verified.

### Next unresolved coverage slice

- Prefer residual dual-count/NA: `special.invoice_email` (need invoice detail
  send dual or stronger non-send NA package), files_upload dual-count policy
  onto Bilag, nested postings isolation (no transactions steal), tax*/bank*/
  productPrices multi-resource.
- Bulk 92 external-contract and annual_reports org_inaccessible stay red.
- API live remains `out_of_scope_by_user`.
