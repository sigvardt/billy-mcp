# 186.78 ui accounts get create update delete NA + special.invoice_email NA

## Goal

Product research178 ACCEPT:

1. Freeze UI parity **`not_applicable`** for exact API ops:
   - `api.accounts.get` → `ui.parity.accounts.get`
   - `api.accounts.create` → `ui.parity.accounts.create`
   - `api.accounts.update` → `ui.parity.accounts.update`
   - `api.accounts.delete` → `ui.parity.accounts.delete`
   - `api.special.invoice_email` → `ui.parity.special.invoice_email`
2. Evidence: dual independent headless sessions prove **no equivalent** account
   get/create/update/delete chrome on Regnskab/Kontoplan settings, and **no**
   durable invoice email compose form (soft `/email`/`/send`/`/delivery` empty;
   edit shows Godkend og send only — rejected as email equivalent). research178.
3. Keep greened `ui_settings_accounting_open` (accounts.list shell) and all
   invoice list/create/get/update/delete tools unchanged. Do **not** dual-count
   list shell onto get/create/update/delete. Do **not** map invoice_email onto
   Godkend og send.
4. Do **not** green `accounts.bulk_save` / `accounts.bulk_delete` (external-contract
   bulk freeze). Do **not** green daybooks residual / users residual / invoiceLines.
5. Peer pattern: research176 products get/update/delete NA (exact ids) +
   research150 exact special ids (operation-scoped).

Target math (tip `49f4cc7` / product 186.77):
live/vision **181 → 186**; implemented/contract **365 → 370**;
`GEO_UI_NOT_APPLICABLE_ROW_COUNT` **111 → 116**; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research178)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. accounts Supports get/list/create/update/bulk save/delete/bulk delete. |
| SPA seed dual | contact+product+invoice POST 200 dual under committed egress; cleanup DELETE 200; markers gone dual. |
| Accounting panel | path `/:org_slug/settings`; Regnskab + Kontoplan dual (list already green). |
| Create CTAs | Tilføj/Opret/Ny konto **0 dual**; create_attempt null dual. |
| Ret / row get-update | absent dual. |
| Mere → Slet konto | absent dual. |
| Soft account routes | rewrite/soft; no dedicated account CRUD form dual. |
| Soft email/send/delivery | path dual; inputs_n **0**; Send **0** dual. |
| Invoice edit | Godkend og send **1 dual** — irreversible approve-send, **not** compose. |
| invoice_email form_ready | **false dual**; na_ready **true dual**. |
| Strong accounts CRUD ready | **false dual**. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research178_focus_dual.json`.  
Probe: `tmp/research178_focus_probe.py` (tmp only; not required in git).

## Non-goals

- New FastMCP UI tools for accounts get/create/update/delete or invoice_email
- Dual-count accounts.list shell onto residual CRUD
- Mapping invoice_email to Godkend og send
- accounts.bulk_* greening; daybooks residual; users residual; invoiceLines
- annual_reports greening; bulk 92 external-contract
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Changing greened list/accounting/invoice tools or egress

## Ownership

| Area | Owner |
| --- | --- |
| Generator NA freeze (exact accounts get/create/update/delete + special.invoice_email) | Root Grok EXECUTE |
| Inventory unit tests (count 116; five rows NA; list still non-NA) | Root Grok EXECUTE |
| Wiki NA pages | Root Grok EXECUTE |
| Optional dual live NA harness only if peer packages require it | Root Grok EXECUTE (products NA used inventory dual only — no new live test file required) |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research178)

### A. Generator (`scripts/generate_coverage_report.py`)

1. Add exact API row id sets (not full `api.accounts.` prefix — that would NA list/bulk):

```python
GEO_UI_NOT_APPLICABLE_RESEARCH178_ACCOUNT_IDS: frozenset[str] = frozenset(
    {
        "api.accounts.get",
        "api.accounts.create",
        "api.accounts.update",
        "api.accounts.delete",
    }
)
GEO_UI_NOT_APPLICABLE_RESEARCH178_SPECIAL_IDS: frozenset[str] = frozenset(
    {"api.special.invoice_email"}
)
```

2. Include those ids in `GEO_UI_NOT_APPLICABLE_API_PREFIXES` as exact strings
   (same technique as products.get/update/delete and special.invoice_delivery/logs).
3. Bump `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **111 → 116**.
4. Extend `apply_ui_geo_reference_not_applicable_evidence`:
   - `is_research178_accounts = api_row_id in GEO_UI_NOT_APPLICABLE_RESEARCH178_ACCOUNT_IDS`
   - `is_research178_email = api_row_id in GEO_UI_NOT_APPLICABLE_RESEARCH178_SPECIAL_IDS`
   - Branch metadata for accounts:
     - research_id = `research178`
     - evidence_ref = `research178_accounts_get_create_update_delete_dual`
     - dual_agree_flag / family_label describing accounts get/create/update/delete absence
     - contrast shells: settings_accounting (list), invoices, clients, products, daybooks
     - evidence prose: panel Regnskab/Kontoplan only; no Tilføj/Opret/Ny konto; no Ret; no Mere→Slet
   - Branch metadata for invoice_email:
     - research_id = `research178`
     - evidence_ref = `research178_special_invoice_email_dual`
     - dual_agree_flag / family_label for invoice_email compose absence
     - contrast: invoices edit (Godkend og send present but rejected as non-compose);
       soft `/email`/`/send`/`/delivery` empty dual
     - evidence prose: soft compose empty; Godkend og send not equivalent to email compose
5. Ensure apply path runs for UI parity rows whose `api_row_id` matches.
6. **Must not** change accounts.list dual-count applicator for
   `ui_settings_accounting_open`. **Must not** change invoice tool dual-counts.

### B. Flags per NA row

| Field | Value |
| --- | --- |
| `parity_status` | `not_applicable` |
| `tool_name` | empty |
| discovered / implemented / contract_tested / live_tested / vision_verified | all **true** |
| `vision_evidence` | null |
| `qualification.kind` | `ui_not_applicable` |
| `qualification.not_applicable_decision` | `accepted` |
| `qualification.sessions` | `dual_independent_ephemeral` |
| `qualification.evidence_ref` | contains `research178` |
| `evidence_code` | `GEO_UI_NO_EQUIVALENT_WORKFLOW` |

### C. Tests (`tests/coverage/test_coverage_inventory.py`)

1. Assert `GEO_UI_NOT_APPLICABLE_ROW_COUNT == 116`.
2. Assert live/vision total still `70 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` (or current formula).
3. Dedicated block: four accounts residual rows NA with research178 evidence; list
   row still shell_open_only on `ui_settings_accounting_open` (not NA).
4. Dedicated block: special.invoice_email NA with research178 evidence; not
   greened as tool; invoice list/create/get/update/delete tools unchanged.
5. Bulk accounts rows remain discovery_required / external-contract red (not NA).
6. Run offline inventory + regenerate coverage; no greening of bulk/annual.

### D. Wiki

1. `wiki/ui_accounts_get_create_update_delete_not_applicable.md` — dual absence
   of account CRUD chrome; list shell contrast; research178; GEO code.
2. `wiki/ui_special_invoice_email_not_applicable.md` — soft compose empty dual;
   Godkend-og-send rejected; research178; GEO code.
3. Refresh wiki `_index.md` via normal commit/wiki tools as needed.
4. Optional one-line note on `wiki/ui_settings_accounting_open` that list is green
   and residual CRUD is NA (if that page exists and is the natural place).

### E. Regeneration + offline verify

1. `uv run python scripts/generate_coverage_report.py` (or project’s regen path).
2. `bash scripts/lint.sh` (or node lint script).
3. `BILLY_TEST_MODE=commit bash .fractal/main.billy_complete/scripts/test.sh`
   (or equivalent offline pytest).
4. Confirm status.json: live/vision 186; implemented/contract 370; complete false;
   blocker still bulk+annual+residual discovery.

## Execution order

1. Generator frozensets + PREFIXES + count 116 + evidence branches.
2. Inventory tests for research178 five NA rows + list non-NA + count 116.
3. Wiki NA pages + index.
4. Regenerate coverage manifests/status.
5. Lint + offline tests.
6. Self IR → FIX-VERIFY → COMMIT.

## Risks / anti-patterns

| Risk | Mitigation |
| --- | --- |
| Prefix `api.accounts.` NA steals list/bulk | Use **exact** ids only |
| Dual-count list onto get | Do not touch accounting dual-count applicator |
| invoice_email mapped to Godkend og send | Explicit rejection in evidence prose |
| Weak daybooks NA greened | Out of scope this slice |
| Claiming complete | complete stays false |

## Done when

- Five UI parity rows green as NA with research178 evidence
- accounts.list still tool-green shell_open_only
- Coverage counts match target math; complete false
- Offline lint + tests pass
- No new tools; no live API; no headed browser
- Plan post-mortem filled after FIX-VERIFY

## Post-mortem

### Completed
- Generator: exact NA freeze research178 for
  `api.accounts.get|create|update|delete` + `api.special.invoice_email`
  (PREFIXES + frozensets; GEO count 111→116; evidence branches).
- Inventory tests: five NA rows, accounts.list shell preserved, bulk non-NA,
  live/vision 186, GEO 116.
- Wiki: `ui_accounts_get_create_update_delete_not_applicable.md`,
  `ui_special_invoice_email_not_applicable.md`, `_index.md` links.
- Coverage regenerated: implemented/contract **370**; live/vision **186**;
  complete **false**.
- No new FastMCP tools; no egress change; no live API.

### Deviations
- None material. Optional IR N1 (wiki untracked until commit) and N2 (generic
  geo NA reason wording) deferred as non-blocking.

### Review
- IR **ACCEPT** product (`tmp/grok-review.md`); completeness FAIL expected.
- No required fixes.

### Tests / cleanup
- lint.sh pass (wiki + memory clean).
- `BILLY_TEST_MODE=commit` offline suite: **1642 passed** / 50 deselected
  (EXECUTE + FIX-VERIFY reconfirm).
- Research dual cleanup_all_clean dual; profiles purged; api_token_used false.
- No ui-full (complete false).

### Next unresolved coverage slice (not this package)
1. daybooks get/update/delete residual after stronger dual.
2. users.get/update after users panel SPA seed dual.
3. invoiceLines embedded dual-count or NA package.
4. bulk 92 external-contract red (BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS).
5. annual_reports org_inaccessible red.
