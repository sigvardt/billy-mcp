# Plan — Wave-5k freeze delivery verification (research50)

## Goal

Reconfirm Billy official API contract, verify completed Wave-5k freeze child content, and lock the four-tool product implementer recipe without greening coverage.

## Done this research step

- Re-fetched https://www.billy.dk/api/ — ETag `hsisik4g9p3603`, MD5 `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (unchanged vs research49).
- Unauth probes on locked base: bankPayments POST/PUT 401, DELETE 405; extended payment-family and invoiceReminders (PUT 405 no-update) probes.
- Read completed freeze child page; content matches create+update-only contract.
- Wrote cited brief: `.fractal/main.billy_complete/tmp/grok-research.md`.
- Updated memory `state.md`.

## Next (not this step)

1. Merge `main.billy_complete.wave5k_freeze_contract` onto root; regenerate wiki index.
2. Independent Grok freeze review ACCEPT.
3. Codex Power product leaf: `bank_payment_writes.py` four tools → 242/168 offline.
4. Independent product review; then Wave-5l `salesTaxPayments` freeze-ready research.

## Non-goals

No coverage greens, no product code, no headed browser, no live token use, no persistent test data.

## Post-Mortem

Research50 reconfirmed the official Billy documentation fingerprint and the
bankPayments method boundary: create and update stop at the authentication
gate, while singular delete is closed with 405. Root Grok independently
accepted this as a current-contract handoff only; it did not accept the
child-only freeze page or any product work.

The review found the root freeze page absent, which is an expected integration
gate rather than a research defect. It also identified one future-live safety
description that was worth correcting now: the `api.bankPayments.create`
inventory cleanup now directs a dedicated record to be voided through the
documented irreversible `isVoided` path and independently verified, rather
than incorrectly instructing a singular delete that the API rejects with 405.
No coverage flag changed.

Commit-mode verification passed: 1,020 non-live tests, Ruff format and lint,
Pyright, coverage-inventory validation, repository-policy checks, and both
wiki lints. Research and review evidence are confined to ignored node scratch;
no credentials, browser frames, HAR files, or customer data were created.

The unresolved slice is root integration of the freeze contract and the
non-authoritative fallback page, wiki-index regeneration, then a dedicated
independent Grok freeze review. Only that ACCEPT can open the four-tool
bank-payment implementation.
