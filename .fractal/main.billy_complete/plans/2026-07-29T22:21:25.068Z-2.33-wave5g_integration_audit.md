# 2.33 Wave-5g integration and independent audit

## Decision

The completed Codex Power leaf delivered the only ready implementation slice:
six sales-tax ruleset/rule CUD inventory rows expressed as twelve ticketed
tools. Candidate `e6f96e7` was merged at root `55faa02`. Root verification
and the mandatory independent Grok audit passed; the offline product slice is
accepted at `wiki/wave_fiveg_product_independent_review.md`.

Current Grok research is already recorded in
`.fractal/main.billy_complete/tmp/grok-research.md`. It is cited,
fingerprint-checked evidence for the *following* Wave-5h attachment freeze;
it does not replace the mandatory post-merge Wave-5g product audit. Preserve
the in-progress research updates to `memory/state.md` and
`wiki/offline_write_probe_rules.md`, but do not use them to change coverage or
start a Wave-5h implementation.

Wave-5g's mandatory root independent-review route has accepted the merged
product. Wave-5h attachments may now proceed only through its own cited freeze
contract and independent review; it remains a separate, unimplemented cohort.

## Completed integration gate

1. Used only the clean committed candidate from
   `main.billy_complete.wave5g_sales_tax_product`; no uncommitted file was
   copied.
2. Reviewed `git diff main.billy_complete...main.billy_complete.wave5g_sales_tax_product`
   and verified executable artifacts from a clean `git archive` checkout. The
   permitted product delta is `sales_tax_writes.py`, registrar wiring, focused
   and cross-executor tests, generator evidence, and generated API coverage for
   exactly these six rows: `salesTaxRulesets.{create,update,delete}` and
   `salesTaxRules.{create,update,delete}`. The child seed is expected branch
   metadata, not product evidence.
3. No unexpected tool, live/UI/vision/bulk claim, coverage
   green outside those six rows, leaf-local confirmation store, generic HTTP or
   browser control, non-Billy egress, or sensitive artifact was found.
4. The candidate passed review and was merged with:

   ```sh
   git merge --no-ff -m "merge main.billy_complete.wave5g_sales_tax_product" \
     main.billy_complete.wave5g_sales_tax_product
   ```

   Refresh and lint the project wiki after the merge if merge-driver index rows
   need regeneration. Announce the material integration through the root
   outbox.

## Completed root verification

Generated coverage validation, Ruff, Pyright, the focused sales-tax tests, and
the complete non-live suite passed on the merge commit (**892** tests). The
offline-only state is: **202** `api_*` tools; **148** implemented and
contract-tested API rows; **zero** live and vision rows; unchanged bulk/UI/
special rows; `coverage/status.json` remains `complete: false`. Full mode
failed only at the deliberate incompleteness gate; it is not a qualification
pass.

Keep the root research handoff honest: it may state that Wave-5h attachments
is freeze-ready, while its three CUD rows and all later cohorts remain red.

## Independent-review result

The root independent-review route re-fetched the official API documentation
unchanged (ETag `hsisik4g9p3603`, MD5
`c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes) and issued the mandatory
offline product **ACCEPT** in `wiki/wave_fiveg_product_independent_review.md`.
It verified the exact twelve-tool surface, request roots and paths, response
mapping, confirmation binding, 202-tool registry, six-row coverage delta, and
the continuing fail-closed qualification state.

One attempted child Grok route exited before edits because CLI authentication
was unavailable. Its required Codex Power fallback was closed without a project
deliverable once the authoritative root Grok review arrived; it has no bearing
on the ACCEPT.

## Post-Mortem

- Delivered root integration `55faa02`, a clean archive review (42 focused
  tests), root lint/coverage/policy checks, and 892 passing non-live tests.
- Root Grok independently ACCEPTed the Wave-5g offline product and wrote the
  durable review record. A redundant Codex fallback was intentionally closed
  without a project deliverable after that result arrived.
- The Grok review reported no product defect to fix or reject; it explicitly
  retained every live, UI, vision, bulk, and completeness row as fail-closed.
- No completion or live/UI/vision/bulk claim is authorized by this plan.
