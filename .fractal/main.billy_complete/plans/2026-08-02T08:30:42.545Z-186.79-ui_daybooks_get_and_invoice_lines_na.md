# 186.79 ui daybooks get open + invoiceLines NA

## Goal

Product research179 ACCEPT as one root-only package:

### A. Tool — `ui_daybooks_get_open`

1. New typed FastMCP tool `ui_daybooks_get_open` (`detail_open_only`).
2. Maps **only** `api.daybooks.get` → `ui.parity.daybooks.get`.
3. Path class **`/:org_slug/daybooks/:id`** (research179 dual SPA-seeded daybook).
4. DOM: editor markers dual on id path (`Opret ny kassekladde` / `Tilføj kassekladdelinje` / `Ingen postering valgt`); Mere may be present; never click create/add-line/post/Slet/Mere-confirm.
5. Keep greened `ui_daybooks_open` (list+create on `/daybooks/new`) unchanged. Do **not** dual-count list/create onto get.

### B. NA freeze — invoiceLines non-bulk

Freeze UI parity **`not_applicable`** for exact API ops only:

| UI row | API op |
| --- | --- |
| `ui.parity.invoiceLines.get` | `api.invoiceLines.get` |
| `ui.parity.invoiceLines.list` | `api.invoiceLines.list` |
| `ui.parity.invoiceLines.create` | `api.invoiceLines.create` |
| `ui.parity.invoiceLines.update` | `api.invoiceLines.update` |
| `ui.parity.invoiceLines.delete` | `api.invoiceLines.delete` |

Evidence: research179 dual — dedicated `/invoiceLines` surfaces absent; lines only embedded on invoice edit (already maps `invoices.update`). Do **not** dual-count line ops onto `ui_invoices_update_open`. Do **not** NA bulk_*.

### Explicit DEFER (out of this plan)

- `daybooks.update` (update form false dual)
- `daybooks.delete` (Slet text only; Mere→Slet not body-proven)
- billLines (SPA seed failed `UNKNOWN_ATTRIBUTE` on `text`)
- users.get/update (panel markers weak)
- bulk 92, annual_reports, offline 29 clear API gaps

Target math (tip after 186.78 / ~370/186):

- live/vision **186 → 192** (+1 get tool + 5 invoiceLines NA)
- implemented/contract **370 → 376**
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **116 → 121**
- complete **false**

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research179)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. daybooks Supports get by id + list/create/update/delete/bulk. invoiceLines full CRUD+bulk. |
| SPA daybook seed dual | POST `/v2/daybooks` 200 dual under committed egress; DELETE 200 dual; markers cleaned dual. |
| Get surface | path `/:org_slug/daybooks/:id` dual; not Upsedasse; editor chrome dual; distinct from greened `/daybooks/new`. |
| Update | `update_ready` false dual → DEFER. |
| Delete | Slet text dual on id path; Slet button 0 dual; Mere menu not expanded dual → DEFER. |
| invoiceLines dedicated | soft/empty dual; `dedicated_list_ready` false dual. |
| invoiceLines embedded | invoice edit path dual; lineish true; inputs_n 9 dual → NA dedicated ops, not dual-count steal. |
| bill seed | 400 dual → DEFER billLines. |
| users | `/settings/users` path dual but Brugere/Ret weak → DEFER. |
| accounts + invoice_email NA | reconfirm dual (already producted 186.78). |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research179_focus_dual.json`.  
Probe: `tmp/research179_focus_probe.py` (tmp only).

## Non-goals

- daybooks.update / daybooks.delete greening or weak NA
- billLines / users residual product
- Dual-count invoiceLines onto invoices.update
- invoiceLines.bulk_* or daybooks.bulk_* greening
- Changing greened `ui_daybooks_open` list+create semantics
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Offline product of the 29 clear API write gaps

## Ownership

| Area | Owner |
| --- | --- |
| Models + browser + server for `ui_daybooks_get_open` | Root Grok EXECUTE |
| Live dual + vision for get tool (SPA seed daybook, cleanup) | Root Grok EXECUTE |
| Generator dual-count daybooks.get + invoiceLines NA frozenset | Root Grok EXECUTE |
| Inventory unit tests (count 121; five invoiceLines NA; daybooks.get tool-green) | Root Grok EXECUTE |
| Wiki shell + NA pages (scrubbed) | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research179)

### A. Tool product `ui_daybooks_get_open`

| Item | Value |
| --- | --- |
| Tool name | `ui_daybooks_get_open` |
| Input | empty / org-bound only as peers (no caller URL/path control) |
| Success | path_class `/:org_slug/daybooks/:id`; editor_markers_present; shell markers as peers |
| Errors | `AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED`, `EGRESS_DENIED`, `BILLY_ERROR` |
| Peer pattern | `ui_invoices_get_open` / `ui_daybooks_open` (headless, dual live, vision) |
| Seed | disposable daybook via SPA under egress (or harness equivalent); unique marker name; DELETE reverse cleanup; fresh-session read-back markers gone |
| Never | Opret/Tilføj/Bogfør/Slet confirm; headed window; env API token for qualification |

### B. Generator (`scripts/generate_coverage_report.py`)

1. `GEO_UI_NOT_APPLICABLE_ROW_COUNT = 121` (116 + 5 invoiceLines).
2. New frozenset `GEO_UI_NOT_APPLICABLE_RESEARCH179_INVOICE_LINE_IDS` exact:
   - `api.invoiceLines.get`
   - `api.invoiceLines.list`
   - `api.invoiceLines.create`
   - `api.invoiceLines.update`
   - `api.invoiceLines.delete`
3. Wire into existing geo-NA qualification branch (peer research176/178): evidence cites research179 dual dedicated absence + embedded-only; `parity_status=not_applicable`.
4. New `apply_ui_daybooks_get_open_shell_evidence(..., parity_of_api_get=True)` for exact `api.daybooks.get` only:
   - tool `ui_daybooks_get_open`
   - path `/:org_slug/daybooks/:id`
   - `parity_status=detail_open_only`
   - live/vision true; test refs unit+live+models+server+inventory
   - evidence research179; does **not** claim update/delete
5. Keep `apply_ui_daybooks_open_shell_evidence` for list+create only.
6. Do **not** green bulk or daybooks.update/delete.

### C. Tests

| Layer | Expectation |
| --- | --- |
| models | empty/forbidden extra inputs; success path_class literal `/:org_slug/daybooks/:id` |
| unit browser | mock path+markers success; bare `/daybooks` and `/daybooks/new` fail closed for get tool |
| unit server | tool registered; no generic controls |
| inventory | NA count 121; five invoiceLines ids NA with research179; daybooks.get green on get tool; list+create still on open tool; bulk not NA |
| live | dual sessions; SPA seed daybook; get tool succeeds dual; second-session read-back; cleanup verified; vision record under tmp; frames owner-only then purge |
| offline suite | lint + `test.sh` offline pass before COMMIT |

### D. Wiki

- Short shell page for daybooks get open (scrubbed paths/markers; research179 cite).
- Short NA page/section for invoiceLines dedicated-absent (research179; not dual-count).

### E. Egress

No egress change expected (same `mit.billy.dk` daybooks family already allowlisted). Confirm only; do not widen hosts.

## Execution order

1. **EXECUTE** models → browser get tool → server register → generator get dual-count + invoiceLines NA → inventory tests → unit tests → live dual + vision → wiki → regenerate coverage.
2. **INDEPENDENT-REVIEW** self-IR against research179 + design; no false dual-count; no bulk greening.
3. **FIX-VERIFY** required IR fixes only; lint; offline tests; coverage math 192/376, GEO 121, complete false.
4. **COMMIT** via `fractal commit` (product message); push via fractal; **not** `node finish`.

## Feasibility

- Budget: no limit.
- Blockers: none for ACCEPT slice; live needs browser keyring refs (already used in research179).
- Complexity: peer of invoices get + products/accounts NA; root-only is correct (no clean child file split).
- Residual after this plan remains incomplete by design (bulk, annual, daybooks update/delete, users, billLines, offline 29).

## Done when

- `ui_daybooks_get_open` implemented, registered, unit+live dual+vision green for daybooks.get only.
- Five invoiceLines parity rows NA with research179 evidence; bulk still red.
- Coverage: live/vision 192; implemented/contract 376; GEO NA 121; complete false; API live false `out_of_scope_by_user`.
- Offline lint/tests pass; branch ready for COMMIT.
- No children; no finish.

## Post-Mortem

### Completed

- Producted research179 ACCEPT package (root-only Grok):
  1. `ui_daybooks_get_open` (`detail_open_only`) maps **only** `api.daybooks.get`
     at path class `/:org_slug/daybooks/:id` with editor markers dual.
  2. UI NA freeze exact five ops: `api.invoiceLines.get|list|create|update|delete`
     (dedicated surfaces absent dual; embedded on invoice edit only; no dual-count
     steal onto `ui_invoices_update_open`).
- Egress: GET/POST/DELETE `/v2/daybooks` for SPA list + live seed/cleanup;
  product org id via allowlisted `GET /user/organizations` (Bearer) + slug match.
- Models, browser, server, generator, unit + inventory tests, live dual + vision
  record (`tmp/vision-records/ui_daybooks_get_open.json`, purge verified).
- Wiki: `ui_daybooks_get_open_shell.md`, `ui_invoice_lines_not_applicable.md`.

### Deviations

- Research dual auto-prefer briefly suggested get/update/delete open; product and
  research prose deferred update (false dual) and delete (Slet text only, no
  Mere→Slet body proof). Shipped get tool only + invoiceLines NA package.
- Live harness still hardcodes test org id for seed (peer pattern); product path
  resolves org dynamically (IR optional N2).

### Review findings (IR 186.79)

- Product: **ACCEPT**. Overall completeness: **FAIL** (expected).
- Required fixes: **none**.
- Optional N1–N2: non-blocking; not code-changed (N1 documented; N2 deferred).

### Test results

| Suite | Result |
| --- | --- |
| `scripts/lint.sh` | pass (FIX-VERIFY reconfirm) |
| `scripts/test.sh` offline | **1648 passed**, 51 deselected |
| Live `test_ui_daybooks_get_open` | pass (dual session; EXECUTE + IR evidence) |
| `BILLY_TEST_MODE=full` / `ui-full` | **not run** — complete still false |
| wiki lint (project + memory) | clean |

### Coverage after slice

- live/vision **192**; implemented/contract **376**; GEO NA **121**
- complete **false**
- API `live_tested` remains false (`out_of_scope_by_user`)

### Cleanup

- Live dual: disposable daybook seed when empty; DELETE reverse; profiles removed;
  vision frames outside repo purged (`purge_verified: true`).
- Research179 dual: `cleanup_all_clean` true; profiles purged; no env API token.

### Next unresolved coverage slice

1. daybooks.delete after Mere→Slet dual body proof on seeded id (do not green from Slet text alone).
2. daybooks.update after valued settings form dual on id.
3. billLines after docs-correct SPA seed body.
4. users.get/update after users panel dual.
5. Offline ticketed tools for remaining 29 clear API write gaps (readonly audit first).
6. bulk 92 external-contract freeze; annual_reports org_inaccessible red.

**Not** node finish.
