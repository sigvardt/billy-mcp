# 186.82 ui transactions create open and daybook transaction lines NA

## Goal

Product research182 ACCEPT as one root-only package (full slice, not A-only):

### A. Tool — `ui_transactions_create_open`

1. New typed FastMCP tool `ui_transactions_create_open`
   (`create_chrome_open_only`).
2. Maps **only** `api.transactions.create` →
   `ui.parity.transactions.create`.
3. Path class **`/:org_slug/transactions`** (research182 dual list shell).
4. Observe-only open Posteringer list; assert create chrome markers dual:
   h1/text **Posteringer** + button **Ny postering**.
5. Soft `/transactions/new` may show title **Postering:** with **0 inputs** —
   title shell only. Do **not** claim `form_open_only` field matrix (no name/
   amount/account flags). Product is **create chrome**, peer daybook create
   chrome open.
6. Never click Gem / Bogfør / Opret submit / void / Slet. Optional CTA click
   is allowlisted only if it stays observe-only and does not submit; default
   success path is list + CTA **presence** (research182 `create_ready` dual),
   not filled form fields after click.
7. Keep greened `ui_transactions_list` unchanged. Do **not** dual-count list
   onto create. Do **not** map get/update/delete (already NA research181).
   Do **not** map `postings.*` onto this tool.

Peer pattern: `ui_daybook_transactions_create_open` (create_chrome_open_only
text/DOM presence without write submit).

### B. NA freeze — daybookTransactionLines non-bulk (5)

| UI row | API op |
| --- | --- |
| `ui.parity.daybookTransactionLines.get` | `api.daybookTransactionLines.get` |
| `ui.parity.daybookTransactionLines.list` | `api.daybookTransactionLines.list` |
| `ui.parity.daybookTransactionLines.create` | `api.daybookTransactionLines.create` |
| `ui.parity.daybookTransactionLines.update` | `api.daybookTransactionLines.update` |
| `ui.parity.daybookTransactionLines.delete` | `api.daybookTransactionLines.delete` |

Evidence: research182 dual — daybook detail open; **Tilføj kassekladdelinje** +
**Ingen postering valgt** embedded; dedicated routes
(`daybook-transaction-lines`, `daybookTransactionLines`, id paths) soft-empty
body_len **138**; `embedded_only` / `na_non_bulk` true dual.

**Dual-count ban:** Tilføj chrome already greened as
`ui_daybook_transactions_create_open` → `daybookTransactions.create`. Do **not**
also product or green any `daybookTransactionLines.*` from that chrome. Do
**not** NA bulk_*.

### Explicit DEFER / REJECT (out of this plan)

- re-product `ui_transactions_list` or change list parity
- re-NA `transactions.get/update/delete` (already NA)
- product full posting form / field matrix after Ny postering
- `postings.*` residual product
- `salesTaxReturns.get/update` (research181 DEFER)
- bulk 92 external-contract
- annual_reports org_inaccessible
- offline clear-API tool gaps (separate lane)
- click Tilføj kassekladdelinje / Bogfør / Gem for lines

Target math (tip after 186.81 / 406/222/149):

- Product +1 + NA 5 = **6** rows greened under scoped policy
- live/vision **222 → 223**
- implemented/contract **406 → 412** (tool row + 5 NA full flags; verify generator math)
- `GEO_UI_NOT_APPLICABLE_ROW_COUNT` **149 → 154** (+5 NA; product is not GEO NA)
- complete **false**

Root-only Grok. No children. No live API. Not node finish.

## Why this slice (research182)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. transactions Supports create; daybookTransactionLines full CRUD Supports (no dedicated UI surface). |
| Dual list + CTA | `tx_list` / `tx_create` / `tx_cta_clicked` / `tx_product_create_open_ready` **true dual**. |
| Soft new shell | `/transactions/new` h1 **Postering:** dual; inputs_n **0** dual — title only. |
| Form-open nuance | After CTA, list markers remain; only filter-ish input dual — do not claim form_open_only. |
| get/update/delete | still false dual; already NA — leave alone. |
| DTL NA | dedicated soft-empty 138 dual; embedded line-add true dual; na_non_bulk true dual. |
| Cleanup | daybook + daybookTransaction DELETE 200 dual; profiles purged; `api_token_used` false. |
| Brief | `.fractal/main.billy_complete/tmp/grok-research.md` research182. |

## Non-goals / safety

- No live `BILLY_API_TOKEN` / no credentialed API qualification calls.
- No headed browser / no desktop window.
- No bulk greening; no annual greening; no stub/skip as green.
- No dual-count: list tool, daybook create tool, daybooks tools stay exclusive.
- No write submit (Gem/Bogfør/Opret/Slet confirm).
- Frames only in owner-only tmp; purge after vision; durable non-sensitive vision record only.
- API `live_tested` remains false with `out_of_scope_by_user`.

## File ownership (root EXECUTE)

| Area | Paths (expected) |
| --- | --- |
| Models | `src/billy_mcp/models.py` — `UiTransactionsCreateOpenInput/Success` (empty input; non-PII success with `shell_kind=transactions_create` or equivalent) |
| Browser | `src/billy_mcp/browser.py` — protocol + `ui_transactions_create_open`; helpers for list URL + Posteringer + Ny postering signature; never submit |
| Server | `src/billy_mcp/server.py` — register tool |
| Unit | `tests/unit/test_browser.py`, `tests/test_models.py` |
| Live | `tests/live/test_ui_transactions_create_open.py` — dual session, DOM assert, second profile, vision record |
| Coverage generator | `scripts/generate_coverage_report.py` — product shell evidence apply; `GEO_UI_NOT_APPLICABLE_RESEARCH182_DAYBOOK_TRANSACTION_LINES_IDS` frozenset (5 exact api ids); PREFIXES/count; evidence branches; egress live test ref |
| Inventory tests | `tests/coverage/test_coverage_inventory.py` — tool map, parity_status, NA exact ids, GEO count 149→154 |
| Egress | `coverage/browser_egress.yaml` regenerated (transactions host already allowlisted via list; add live test ref) |
| Manifests | regenerate via generator (no hand-edit false green) |
| Wiki | short NA evidence page for daybookTransactionLines research182 + product note; index on commit |
| Plan post-mortem | fill after FIX-VERIFY |

## Implementation steps (EXECUTE)

1. **Models + browser + server** for `ui_transactions_create_open` peer to
   daybook create chrome / transactions list.
2. **Unit + model tests** for empty input, shell_kind, non-PII success, fail-closed
   auth / wrong surface.
3. **Live dual test** — no env API token; assert Posteringer + Ny postering dual
   equal; independent session B; write vision record + purge frames.
4. **Generator NA freeze** research182 exact ids for daybookTransactionLines
   g/l/c/u/d; product apply for transactions.create; bump GEO count; inventory
   asserts.
5. **Regenerate coverage** (`uv run python scripts/generate_coverage_report.py`
   or project peer command); `check_coverage`.
6. **Offline** `bash .fractal/main.billy_complete/scripts/test.sh` (commit mode);
   lint/type via lint.sh.
7. **Do not** run ui-full or claim complete; do not finish node.

## Verification (FIX-VERIFY / IR)

- Offline tests pass; live dual create open pass when credentials present.
- Vision accept + purge_verified for create open.
- Coverage: transactions.create tool green create_chrome_open_only; DTL×5 NA full
  flags; bulk still red; annual still red; list tool unchanged; daybook create
  tool unchanged.
- IR ACCEPT package; overall completeness FAIL expected.
- Commit via `fractal commit` after FIX-VERIFY; push via fractal.

## Children

None. Root-only Grok owns product + NA + tests (file ownership clear; same pattern
as 186.78–186.81 residual packages).

## Budget / risk

- Time/cost: no limit — full package still small (1 tool + 5 NA).
- Risk: over-claiming form fields after CTA — plan forces create_chrome only.
- Risk: dual-count DTL create onto Tilføj — explicit ban.
- Risk: contact SPA seed 422 in research (type enum) — product needs no contact seed.

## Success criteria (package, not node)

- [ ] Tool registered, typed, unit + live dual + vision
- [ ] `ui.parity.transactions.create` implemented/contract/live/vision true;
      parity `create_chrome_open_only`; tool_name set
- [ ] Five DTL rows NA full flags; evidence research182
- [ ] complete false; bulk 92 red; API live false
- [ ] Offline suite green; no secrets in tree
- [ ] Plan post-mortem filled at FIX-VERIFY
- [ ] Not `fractal node finish`

## Post-mortem (fill at FIX-VERIFY)

- What shipped:
- Counts after:
- Deviations from plan:
- Follow-ups deferred:

## Post-Mortem

### Work completed
- Producted `ui_transactions_create_open` (`create_chrome_open_only` →
  `api.transactions.create`): models, browser, server registration, unit,
  live dual, vision record.
- NA freeze research182: `daybookTransactionLines` get/list/create/update/delete
  (5) via generator frozenset + PREFIXES; dual-count ban on daybook create tool.
- Regenerated coverage; wiki page
  `daybook_transaction_lines_not_applicable_research182.md`.
- IR optional N1/N2 applied in FIX-VERIFY: vision `workflow_ref` →
  `ui.parity.transactions.create`; live test renamed
  `test_dual_profiles_open_transactions_create_chrome`.

### Deviations
- Soft `/transactions/new` not producted as form_open_only (title shell only
  dual) — as planned.
- Offline `api.transactions.create` remains incomplete (pre-existing offline
  residual); not claimed by this UI package.

### Review findings
- IR product **ACCEPT**; overall completeness **FAIL** expected.
- Required fixes: none. Optional N1/N2 applied.

### Test results
- lint.sh: pass.
- offline test.sh: 1663+ passed / 54 deselected (EXECUTE); FIX-VERIFY re-run.
- live dual `test_ui_transactions_create_open`: pass; vision accept;
  purge_verified true.

### Cleanup state
- No disposable transaction seeds required for create chrome product.
- Research182 dual: daybook + daybookTransaction cleanup dual; profiles purged.
- Vision frames outside repo; purge verified.

### Next unresolved coverage slice
- Residual UI: postings.*, salesTaxReturns get/update, bank* / attachments
  residual, annual_reports org_inaccessible.
- Bulk 92 external-contract red.
- Offline clear-API gaps (e.g. `api.transactions.create` offline incomplete).
- complete remains **false**. Not node finish.
