# 110.11 Wave-5o integration and Wave-5p organization freeze

## Objective

Integrate the completed, strictly scoped Wave-5o invoice-reminder create
delivery only after root verification, obtain a fresh independent product
review, and in parallel author the next cited contract-only freeze for
organizations create and update. Preserve all incomplete/live/UI/bulk status.

## Starting evidence

- `main.billy_complete.wave5o_invoice_reminder_product` is completed at
  `81bf925`. Its accepted boundary is
  `wiki/wave_fiveo_ticketed_writes_contract.md`; the child was additionally
  authorized to add a generator override for the exact `invoiceReminders[]`
  response field with a focused regression test.
- Research67 in `.fractal/main.billy_complete/tmp/grok-research.md` is current
  Grok evidence: official HTML MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag
  `wcw4x9hqvu3603`, and 147934 bytes. It recommends only organizations
  create/update for the next offline freeze; singular DELETE is 405 and all
  bulk operations remain red.
- `wiki/wave_fivep_candidate_write_research.md` is the merged durable
  candidate record. The current research67 plan and memory update are pending
  root commit; scratch evidence is not a deliverable.

## Delegation decision

The two remaining concerns have disjoint ownership and real depth:

1. Product verification is independent from the producer and must start only
   after the root has a committed, tested merge. A Grok leaf owns the review
   record only.
2. Organizations freeze authoring is a wiki-only Codex Power task whose output
   must not overlap product source, tests, coverage, or the review record. It
   can proceed from the cited research while the product review runs.

## Execution

1. At the next PREPARE, inspect `81bf925` with
   `git diff main.billy_complete...main.billy_complete.wave5o_invoice_reminder_product`
   and a clean `git archive` checkout. Require only the invoice-reminder module,
   registration, focused tests, one coverage-generator override/test, and the
   regenerated API coverage artifacts; exclude the child seed. Verify that the
   response-field override is exactly `api.invoiceReminders.create` →
   `invoiceReminders[]`, without changing generic rows. Reject any premature
   live/UI/bulk/completeness claim.
2. If the committed bytes satisfy that boundary, merge with
   `git merge --no-ff -m "merge main.billy_complete.wave5o_invoice_reminder_product"`
   and remove the generated child seed before amending the labelled merge.
   Regenerate/lint the project wiki if the merge affects its index. Run root
   formatting, Ruff, Pyright, relevant API/coverage tests, coverage-policy and
   credential/safety checks, then the non-live suite before requesting review.
3. After the root product merge and checks, initialise the independent reviewer
   explicitly as Grok:

   ```bash
   fractal node init wave5o_product_review \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --agent=grok --max-cost=320 --max-iter-cost=80 --max-iters=8 \
     --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Its sole tracked deliverable is
   `wiki/wave_fiveo_product_independent_review.md` plus its generated index
   entry. It must independently verify the merged tool schemas, exact ticket
   binding, one POST/no retry, `invoiceReminders[]` mapping, generator output,
   coverage truth, and all cited official evidence. It may not edit source,
   tests, coverage, or accepted freeze pages. If Grok fails before edits solely
   from auth, quota, or rate limiting, launch the same review with explicit
   `--agent=codex-power`; after any edit, retain the original reviewer route.
4. In the same EXECUTE pass, initialise the contract-authoring leaf explicitly
   as Codex Power:

   ```bash
   fractal node init wave5p_organizations_freeze \
     --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
     --agent=codex-power --max-cost=320 --max-iter-cost=80 --max-iters=8 \
     --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Its NODE contract must embed research67's cited fingerprint, 401 POST/PUT
   gate, DELETE 405 boundary, exact opaque `organization` inputs, required
   `organizations` response root, high-sensitivity redaction requirement,
   shared ticket semantics, no-retry rule, and the non-delete cleanup wording.
   It owns only `wiki/wave_fivep_ticketed_writes_contract.md`, its generated
   index entry, and its own seed; no source/tests/coverage/status/product or
   freeze-review edits. Its deliverable is a freeze page only, never an
   acceptance or coverage green.
5. Once the authoring leaf commits, inspect and merge the wiki-only contract,
   stripping its generated child seed. Then launch a separate Grok freeze
   review leaf. Do not start an organizations product implementation before
   the accepted freeze page and its independent review are both on root.

## Verification and boundaries

- Product target after merge: 256 `api_*` tools and 175 implemented plus
  contract-tested API rows. Live/vision remain zero, all 92 ambiguous bulk
  rows remain red, and `complete` remains false.
- Organizations freeze must leave coverage unchanged. It excludes organization
  delete, bulk save/delete, relation-wire speculation, subscription-card
  logging/storage claims, webhooks, live work, and UI/vision work.
- The next sequential candidates remain users update-only and the narrow
  sales-tax-returns update; invoice-reminder-association deletion remains
  blocked until authenticated non-production cleanup evidence.

## Acceptance criteria

- The root product merge contains only reviewed Wave-5o deliverables and has
  clean root non-live verification.
- A fresh independent review leaf has a committed, bounded assignment against
  the root product bytes.
- A separate Codex Power leaf has a committed, cited, freeze-only assignment
  for organizations create/update, with disjoint file ownership.
- No completion, live, UI, vision, cleanup-success, or bulk claim is promoted.

## Post-Mortem

- Wave-5o product child delivered committed tip `81bf925`; root integration is
  deliberately deferred to the next PREPARE, where its clean archived bytes
  will be inspected before merge and independent product review.
- The Wave-5p organizations freeze-authoring Codex Power leaf was configured
  with exclusive wiki ownership and launched from the accepted Grok research.
- Research67 received an independent Grok ACCEPT as research. Its durable wiki
  review is on root; this is not a freeze or product acceptance.
- Root verification passed formatting, Ruff, Pyright, inventory/policy checks,
  both wiki lints, and 1,094 non-live tests. Live, UI, vision, bulk, and full
  completeness tests remain intentionally out of scope and red.
- The next unresolved slice is reviewed Wave-5o root integration, followed by
  the organizations freeze page and independent freeze review before any
  organization product work.
