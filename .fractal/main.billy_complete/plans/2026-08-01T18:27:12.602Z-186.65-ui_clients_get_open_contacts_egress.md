# 186.65 ui clients get open + contacts data-plane egress

## Goal

Product research164 ACCEPT:

1. **Scoped browser egress expansion** on `api.billysbilling.com` `path_allow`
   so the Billy SPA can complete contacts UI data-plane XHR needed for list
   rows and detail open (today blocked dual: `GET/POST /v2/contacts` →
   `net::ERR_BLOCKED_BY_CLIENT`).
2. New read-only FastMCP UI tool **`ui_clients_get_open`** that opens a **client
   (contact) detail** surface as observe-only get/open (never Gem/Slet submit).
3. Green exact **`ui.parity.contacts.get`** / `api.contacts.get` only onto
   `ui_clients_get_open` (`detail_open_only` / get-open parity).
4. Keep greened `ui_clients_list` and `ui_clients_create_open` unchanged
   (list-shell and form_open_only contracts).

Target math (from tip `9d6570a` / product 186.64 products create open):
live/vision **166 → 167** (parity get only; no new discovery seed unless
generator requires one — prefer parity-only +1); implemented/contract
**350 → 351**; complete **false**.

If EXECUTE proves durable create is required for an empty org after GET allow,
include test-only disposable create/cleanup inside the live test (not a
ticketed UI write tool). Optional `GET /v2/countries` path_allow if form seed
needs it. Do **not** green contacts.create as durable write.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research164)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. contacts resource supports get by id / list / create / update / delete. |
| Egress dual | SPA issues `GET /v2/contacts`, `POST /v2/contacts`, `GET /v2/products|invoices|accounts|countries|…`. Under frozen path_allow only auth/bootstrap/org GETs return 200. Contacts GET/POST dual **failed** with `ERR_BLOCKED_BY_CLIENT.Inspector`. |
| Strict get dual | clients/suppliers/products/invoices/bills detail_ready **false** dual (header FP rejected; empty grids under blocked GET). |
| Client Gem dual | Form fields + Gem enabled dual; list marker absent dual — consistent with blocked POST `/v2/contacts`, not merely bad form fill. |
| Tool gap | `ui_clients_list` list chrome only; `ui_clients_create_open` form open only; no get/detail tool; `ui.parity.contacts.get` still `discovery_required`. |
| Isolation | Get-open ≠ list shell ≠ create form_open. Green only **contacts.get**. Leave update/delete/bulk/invoice_email red. |
| Deferred | special.invoice_email; products.get/update; salesTaxRules NA steal; bulk 92; annual_reports Upsedasse. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research164_focus_dual.json`, `tmp/research164_deep_dual.json`,
`tmp/research164_egress_dual.json`.

## Non-goals

- Full `browser_action: allow` on `api.billysbilling.com` (keep path_allow)
- Greening products/invoices/accounts/taxRates/… egress paths this slice
  (only contacts data plane + optional countries GET)
- Greening `contacts.update` / `delete` / bulk_* / special.invoice_email
- Ticketed durable UI write tool for contacts create (API writes stay offline)
- Replacing or dual-counting onto `ui_clients_list` / `ui_clients_create_open`
- Submitting Gem/Slet/Rediger save on detail after open
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| Egress YAML + allow-matrix unit/coverage tests | Root Grok EXECUTE |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record | Root Grok EXECUTE |
| Coverage generator (contacts.get dual-count) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research164)

### A. Browser egress (`coverage/browser_egress.yaml`)

Host `api.billysbilling.com` remains `browser_action: path_allow`.

**Add** (minimum set; adjust only with EXECUTE network dual proof):

| match | methods | path | purpose |
| --- | --- | --- | --- |
| prefix | GET | `/v2/contacts` | list + get-by-id SPA reads |
| prefix | POST | `/v2/contacts` | test-harness disposable create only if org empty |
| prefix | PUT | `/v2/contacts/` | only if SPA update path required for open (prefer omit) |
| prefix | DELETE | `/v2/contacts/` | only if live cleanup requires SPA delete |
| exact or prefix | GET | `/v2/countries` | optional; only if durable seed form needs country options |

**Keep denied** (explicit regression):

- `POST /v2/invoices/`…`/emails` (invoice_email later)
- `GET /v2/products`, `GET /v2/invoices`, `GET /v2/accounts` (other slices)
- `api.billy.dk` deny
- default_action deny

Update host `evidence` / `test_references` to cite research164 + new live test.
Offline coverage inventory assertions that enumerate path_allows must expect the
new rules and still assert non-contacts data-plane paths remain absent.

### B. Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_clients_get_open` |
| lane | ui |
| kind | detail_open_only (read-only observe) |
| shell_kind | `clients_get` |
| success model | `UiClientsGetOpenSuccess` (Pydantic, `extra=forbid`) |
| error model | existing `ToolError` (`AUTH_*`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — `api.contacts.get` already offline implemented |

### C. Success model fields (non-PII)

Mirror other UI open tools (no org_slug, raw URL, selector secrets, customer PII):

| Field | Contract |
| --- | --- |
| `path_class` | `/:org_slug/clients` **or** `/:org_slug/clients/:id` class (normalize id segment to `:id` / `:contactId` if path changes — freeze exact literal in EXECUTE against dual) |
| `heading` | clients/detail heading class (e.g. retained `Kunder` or detail title class) — freeze from dual |
| `shell_kind` | Literal `clients_get` |
| `detail_open` | bool — contact detail surface open |
| `name_field_visible` | bool — name field visible on detail (preferred) |
| `detail_markers_present` | bool — aggregate: detail_ready strict signature (path changed and/or valued name + ≥ content fields, **not** header-only dialog) |
| `shell_markers_present` | bool — same aggregate used by existing open tools |

Strict **reject** header-row false positives (research162 trap): column header text
`Navn / E-mail / Telefon / …` alone is **not** detail_open.

### D. Navigation (observe-only product path)

1. Headless Playwright via `BrowserRuntime`; egress from updated
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. Open `https://mit.billy.dk/{slug}/clients`, settle until list XHR can complete
   under new egress (networkidle / bounded wait).
4. Open first **non-header** data row or id link → detail.
   - Prefer anchor `…/clients/{id}` when present.
   - Else row click excluding header rows (research164 classifier rules).
5. Success when strict detail_ready:
   - path_changed to detail **or** real detail drawer with content fields
   - `name` field visible and/or content fields ≥ 3 with Gem/Slet chrome optional
   - never treat table header dialog as success
6. Escape only after classify; **never** click Gem / Slet / submit / permanent edit.
7. Independent second-session read-back of same detail signature.

### E. Empty-org live test harness (not a product write tool)

If after GET allow the list has zero openable rows dual:

1. Live test may open create form, fill disposable marker name + minimal fields,
   click Gem **once**, wait for list marker.
2. Then run get-open on that row.
3. Cleanup: delete disposable via UI if DELETE allowed, else best-effort Slet
   flow; verify marker absent on fresh list read-back.
4. Never leave residual markers; never product a public ticketed create-execute
   tool in this slice.
5. If POST allow is unnecessary (rows already exist), omit POST/DELETE path
   rules to keep egress minimal.

### F. Coverage generator

- Apply shell evidence helper analogous to other get/open tools (if none for
  get, mirror create-open pattern with `parity_of_api_get` flag).
- Dual-count **exact** `api.contacts.get` / `ui.parity.contacts.get` only.
- Do **not** dual-count list, create, update, delete, bulk.
- Assert residuals stay red: contacts.update/delete/bulk; special.invoice_email;
  products.get; annual_reports; bulk 92 external-contract.
- live/vision 166→167; implemented/contract 350→351; complete false;
  API live_tested false + `out_of_scope_by_user`.

## Implementation checklist (EXECUTE)

1. **Egress** — edit `coverage/browser_egress.yaml` contacts path_allows; unit
   tests for allow/deny matrix; coverage inventory egress assertions.
2. **Models** — `UiClientsGetOpenSuccess` in `src/billy_mcp/models.py` +
   `tests/test_models.py`.
3. **Browser** — protocol + `BrowserRuntime.ui_clients_get_open`; strict
   detail open helpers; never submit.
4. **Server** — register FastMCP tool with typed return.
5. **Unit tests** — success path; reject header-only; reject list-without-detail;
   egress still denies non-contacts data plane samples.
6. **Live test** — `tests/live/test_ui_clients_get_open.py`: dual ephemeral
   profiles, DOM assertions, second-session read-back, vision record under
   node tmp, purge frames, no API token; optional disposable seed+cleanup.
7. **Coverage generator** + inventory tests for contacts.get green math.
8. **Wiki** — `wiki/ui_clients_get_open_shell.md` (scrubbed) + index links from
   clients list / create pages; note egress dependency.
9. **Regenerate** coverage manifests/status/report; lint + offline test.sh.
10. Leave research dual JSON in owner tmp only (not commit secrets/PII).

## Acceptance

- Egress allows contacts GET (and only the extra methods EXECUTE proves needed).
- Tool registered; offline unit + inventory tests pass.
- Live dual test passes when credentials present.
- Vision record non-sensitive + purge verified.
- Coverage: contacts.get parity green; list + create-open tools unchanged;
  residual reds as above; complete=false; bulk 92 red; annual red; API live false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Safety rails

| Risk | Mitigation |
| --- | --- |
| Over-broad API browser allow | path_allow only; unit deny products/invoices/emails samples |
| Header-row false green | strict detail_ready; unit test header rejection |
| Accidental contact mutation | never Gem/Slet in product path; live asserts no unintended marker unless harness owned |
| Harness create leftover | reverse cleanup + fresh list read-back dual |
| Dual-count steal onto list/create | generator exact `api.contacts.get` only; inventory asserts list/create tools unchanged |
| Live API leakage | unset/forbid `BILLY_API_TOKEN`; api_token_used false |
| Children / wrong agent | root-only Grok; no spawn |

## Feasibility

- Uncapped budget/time/cost. Root-only. Research dual evidence already on disk.
- Largest risk: SPA detail DOM differs after egress unlock — freeze model
  literals in EXECUTE from fresh dual, not from pre-egress false opens.
- If get-open still impossible after minimal egress (platform blocker), do **not**
  false-green; fall back to egress-only product with complete=false and document
  blocker (prefer still shipping get-open).

## Done when

- Plan producted in EXECUTE and IR ACCEPT (or required fixes landed).
- FIX-VERIFY: lint + offline tests green; coverage math 166→167 / 350→351;
  residual reds as above; complete false.
- Ready COMMIT 186.65. **Not** `fractal node finish`.

## Post-Mortem

- **Commit:** `66ac07a` (ui clients get open contacts egress).
- **live/vision:** 166 → **167**
- **implemented/contract:** 350 → **351**
- **Offline tests:** lint pass; `test.sh` **1610 passed** / 42 deselected (FIX-VERIFY reconfirm).
- **Live dual:** `tests/live/test_ui_clients_get_open.py` passed in EXECUTE; vision record
  `tmp/vision-records/ui_clients_get_open.json` purge_verified true.
- **IR:** ACCEPT (`tmp/grok-review.md`); required fixes **none**; optional N1–N3 deferred
  (supplier path_class Literal vs regex; vision assertion_ref naming; plan/wiki/live ride commit).
- **Cleanup:** product path is get/open only; live harness may seed disposable client when
  empty; residual R18665 markers deleted in EXECUTE cleanup.
- **Residuals still red:** special.invoice_email; contacts.update/delete/bulk_*;
  products.get/update/delete; bulk 92 external-contract; annual_reports org_inaccessible;
  other api_parity discovery_required rows. API live_tested false (`out_of_scope_by_user`).
  complete **false**.
- **Next unresolved coverage slice:** residual UI parity — prefer contacts.update form dual
  after get surface (Ret open only or detail_edit); special.invoice_email with durable draft
  dual once invoices data-plane egress considered; products.get if products GET egress
  producted; nested salesTaxRules isolation; no postings/bankLines steal; bulk 92 and
  annual_reports stay red until evidence.
- **Deviation from plan freeze:** detail path is `/:org_slug/contacts/:id/customer` profile
  overview (Name (Kunde) + Ret), not `/clients/:id` name-input form. Signature frozen from
  EXECUTE dual observation; success model updated accordingly.

## Done when

- Plan producted in EXECUTE and IR ACCEPT (or required fixes landed).
- FIX-VERIFY: lint + offline tests green; coverage math 166→167 / 350→351;
  residual reds as above; complete false.
- Ready COMMIT 186.65. **Not** `fractal node finish`.
