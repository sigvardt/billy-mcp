# 198.8 Invoice Kunde chevron hit target

## Goal

Prove the invoice **Kunde** chevron hit target (`A3AB03C3`). First a
failing unit fixture that the current live opener and `after_click`
payloads miss the right-edge keys. Then the smallest helpers that
record them. Then one live read-only recapture at rest. Click the
proved `input[name=contact]` at the dumped offset only if
`right_edge_same_input` is true. Wait for a visible option before
type or select. If the right-edge hit is not that input, do not
click. Stay red.

Do not repeat center-click plus type. Do not guess a separate
selector. Do not remap invoice CUD rows.

Authority: `A3AB03C3`, `51E18E60`, `96908DC6`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Another center `field.click()` plus type
- Guessed sibling, caret, or chevron selector
- Force click, `evaluate` click, Escape, Tab, Enter, ArrowDown
- Products, ledger, files, org, or bills
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Copying the bills vendor wrapper onto this field
- Treating a hidden `contactId` as a bind
- Persisting screenshots, frames, HAR, or traces in git
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `1275d00`. Memory edits from this continue are uncommitted.
Not a backstop. `complete=false`. Honesty 16 stay red. Contacts and
bills CUD rows already name preview tools. Invoice CUD rows still
name `ui_invoices_*_open`.

`51E18E60` widget dump is complete. Live opener box is `250x40` at
`(65,121)`. Center `elementFromPoint` is `INPUT` name `contact`.
`named_opener=null`. After center click and type:
`option_role_count=0`, GET `/v2/contacts` count 0.

Current dump does **not** record:

- `right_edge_offset`
- `right_edge_element_from_point`
- `right_edge_same_input`
- `appearance_token`
- `background_image_kind`
- `before_content_kind`
- `after_content_kind`
- `input_child_count`

`_click_field_once` still uses Playwright default center click.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/invoices_kunde.py`
- `src/billy_mcp/ui_writes/invoices_form_observe.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `wiki/ui_invoices_writes.md`

Parked write-family children stay parked. Unlimited budget is not
a reason to spawn.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is a failing unit
   fixture. The current live opener and `after_click` payloads fail
   `chevron_hit_missing_keys`. Run it. Capture the failure. Then
   implement.
2. **Honesty-16 stays red.** Confirm the coverage gate still
   length 16 before any other edit.
3. **Keys are flags, counts, and one integer offset.** Never store
   tag, CSS urls, pseudo content strings, or pixels in git.
4. **One right-edge sample.** `dx = w - 8`, `dy = h // 2`. Record
   that offset. Keep the existing center sample.
5. **Click gate.** `right_edge_same_input` is true only when the
   right-edge hit is `INPUT` with `name=contact`. Then one
   `field.click(position={"x": dx, "y": dy})` on the proved field.
   Wait for a visible option. Do not type until an option is
   visible. If no option, return `UI_CHANGED` and stop.
6. **If the right-edge hit is not the proved input, do not click.**
   Record the hit tokens. Stay red.
7. **Read-only first.** Open `/:org_slug/invoices/new`. Recapture
   at rest before any click. Do not save a draft.
8. **Do not remap.** Preview `tool_name` waits for a later slice
   after independent accept + purge.
9. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only. Field shot lives under
   `~/.local/share/billy-mcp/` and is purged after review.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_CHEVRON_HIT_KEYS` and `chevron_hit_missing_keys` in
`invoices_kunde.py` **only after** a unit fixture fails because
those names do not exist, or add the fixture first against a local
helper stub that is not yet imported from product code. Preferred
shape:

- Fixture copies the current live opener plus current `after_click`
  keys from `~/.local/share/billy-mcp/inspect-live-invoices-kunde*.json`
  (flags only; no tag text).
- Helper returns the missing key list:
  `right_edge_offset`, `right_edge_element_from_point`,
  `right_edge_same_input`, `appearance_token`,
  `background_image_kind`, `before_content_kind`,
  `after_content_kind`, `input_child_count`.
- Current payloads must fail with that exact set.

Do not edit observe/bind product files until that fixture fails.

Keep `widget_contract_missing_keys`,
`test_typed_contact_dump_is_not_a_kunde_bind`,
`after_type_is_existing_customer_observation`, and
`test_bind_does_not_press_extra_keys_on_unbound_contact`.

### 2. Smallest helper, then live recapture

- Implement the helper in `invoices_kunde.py`.
- Extend `_OWNERSHIP_JS` / `observe_kunde_opener` / `observe_kunde`
  with the new flags. Isolate unit dumps so they cannot overwrite
  the owner files.
- Allowlists:
  - `appearance_token`: `auto` / `none` / `textfield` / `other`
  - `background_image_kind`: `none` / `url` / `gradient` / `other`
  - `before_content_kind` / `after_content_kind`: `none` / `empty` /
    `present`
- `right_edge_same_input` is true only for `INPUT` + `name=contact`.
- Recapture a redacted field shot outside git (`present` / `bytes` /
  `box` only in the dump).
- Live path: `create_server` login, open
  `/:org_slug/invoices/new`, wait for networkidle plus a short
  field wait, dump at rest. Do not type. Do not click **Gem som
  kladde**.
- After the dump is complete, apply the click gate in section 5.
  If the click runs, wait for a visible option. Do not type. If no
  option, `UI_CHANGED` and stop. If the right-edge hit is not the
  proved input, do not click.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the click gate into memory `state.md` / `todo.md`. No
  iteration labels.

### 3. Do not write a draft

This slice is dump plus at most one evidenced right-edge click.
Do not click **Gem som kladde**. Do not persist an invoice. Do not
add PUT egress.

If observer login fails, recapture from the primary FastMCP
session and stop fail-closed.

### 4. Do not green

Honesty 16 stays red. Invoice `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. Do not call
`apply_ui_invoices_cud_preview_tool_names`.

Run honesty + new unit tests, then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline) after
the helper lands. Live recapture is the product proof, not the
offline suite.

## Done when

- Failing chevron-hit fixture existed, then the helper lists the
  missing keys on the current live payloads.
- Observe records those keys. Live recapture writes a complete
  chevron-hit dump.
- Either one evidenced right-edge click ran and waited for a
  visible option, or no click because `right_edge_same_input` was
  false.
- Honesty 16 still red. `complete=false`.
- Zero leftover tagged invoices or contacts. Field shots stay
  outside git and are purged after review if any were written.

## Budget

Unlimited. No children. One slice. Do not expand into other CUD
families this iteration.

## Post-Mortem

Work completed: failing chevron-hit fixture first, then
`REQUIRED_CHEVRON_HIT_KEYS` / `chevron_hit_missing_keys`, then
observe right-edge dump, then live read-only recapture. Offset
`{dx:242, dy:20}`. Center hit is `INPUT` name `contact`. Right-edge
hit is a nameless `DIV`. `right_edge_same_input=false`. No click.
Honesty 16 stay red. No remap.

Deviations: none. No child spawn. No type. No **Gem som kladde**.

Review findings: FAIL. Dump slice met `A3AB03C3`. Invoice CUD still
unbound. Field shot showed **Vælg kunde** plus a visible chevron.
The right-edge point is not the proved `INPUT`. No draft, no
persist, no read-back, no vision accept. Field shot purged after
review.

Tests: honesty-16 pass. Chevron unit tests pass. Live chevron dump
pass. Offline `BILLY_TEST_MODE=commit` `test.sh` 1904 passed, 62
deselected (EXECUTE and FIX-VERIFY). Did not run
`BILLY_TEST_MODE=full` or `ui-full`. `complete=false`.

Cleanup: field shot `inspect-live-invoices-kunde-field.png` purged
after review. Zero `billy-live-invoices-*` profiles. Shared chrome
profiles untouched. No invoice or contact created this slice.

Next unresolved coverage slice: stay on the proved `INPUT`. Do not
click the nameless right-edge `DIV`. Stay red until a later
observation names a real bind, then one FastMCP draft CUD with
independent read-back.

