# 190.11 Accept contacts vision then point preview

## Goal

Independent Grok accepts the live contacts create/update/delete frames,
purges them, and points the three contacts parity rows at preview tools.
Coverage stays red.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Removing contacts from `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or changing
  its length-16 assertion
- Changing contacts `parity_status` off open-only
- Changing discovery or get-open tool names (`ui.discovery.clients_create`,
  `ui.parity.contacts.get`, `ui_clients_*_open` shells)
- A second tagged create or another live CUD
- Self-approve or purge from `tests/live/test_ui_contacts_writes.py`
- BrowserRuntime-only qualification
- Live API token calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `907667c`. Last commit is 190.10 PUT persist, not a backstop.
Live FastMCP contacts CUD passed. Vision record is
`tmp/vision-records/ui_contacts_writes.json`:
`author=live_test`, `pending_review`, `purge_verified=false`,
`run_id=e459379ef00d4ff4ac8b82caea12ceee`. Frames are
`~/.local/share/billy-mcp/vision-tmp/run-3d5b151dfd5342258f8734373597f8c1/`
(`01_before` empty Kontakter, `02` name `MCP-UI-C-E25807B6`, `03`
`{tag}-U`, `04` empty again). Twenty-three other leftover `run-*`
dirs exist. Zero `billy-live-contacts-*`. `chrome-profile` stays.
Coverage `complete=false`. Honesty 16 stays red. Gate
`tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py` already
holds.

Open-shell evidence still assigns
`ui.parity.contacts.{create,update,delete}` to `ui_clients_*_open`.
Preview/execute twins already exist.

## Delegation

No children. Root owns this slice. The files sit on the mainline and
would collide if forked:

- `tests/coverage/test_vision_review_provenance.py`
- `tests/coverage/test_coverage_inventory.py`
- `scripts/generate_coverage_report.py`
- `coverage/ui_workflows_manifest.yaml` (generated)
- `tmp/vision-records/ui_contacts_writes.json` (untracked, must stay untracked)
- `.gitignore`
- `wiki/ui_contacts_writes.md`
- `wiki/review_provenance_rules.md` (only if the bind/purge sentence is missing)

Parked UI-write children stay parked. Do not continue or reset them.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is failing tests. Run
   them. Capture the failure. Then implement.
2. **Independent accept (`C7DBE974`).** EXECUTE writes the accept record.
   The live test stays `pending_review` only. Same-session IR must not
   write accept or purge.
3. **Bind the frame dir.** Record `run_id` becomes
   `3d5b151dfd5342258f8734373597f8c1`. Then purge that directory. Then
   `mark_purge_verified`. Then delete leftover `vision-tmp/run-*` dirs.
4. **Preview tool_name only on the three CUD parity rows.** Do not change
   `UI_CLIENTS_*_OPEN_TOOL_NAME` constants used by discovery/get-open.
   Override after `apply_ui_cud_parity_open_only_honesty` (honesty must
   still force implemented/live/vision false and keep open-only status).
5. **Keep honesty 16.** Gate `EE0A0F1B` stays. Open-only status plus
   preview tool is still not green.
6. **Keep `tmp/vision-records/` out of git.** Add that path to
   `.gitignore`. Do not commit the JSON or any PNG.

## EXECUTE

### 1. Failing tests (first edit)

Keep `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
as-is. Run it first and confirm it still passes (16 rows red).

Then add failing tests. Run them. Capture the failure. Do not edit
product files yet.

| Case | File | Expected today | Expected after fix |
| --- | --- | --- | --- |
| Current live_test pending record does not qualify | `tests/coverage/test_vision_review_provenance.py` | `qualifies_for_coverage_vision` is false (already) | still false until overwrite; add this as a lock |
| Independent accept + purge_verified qualifies | same, using a temp copy | no such fixture | `qualifies_for_coverage_vision` is true only for `author=independent_review` + `accept` + `purge_verified` |
| Three contacts CUD rows use preview tools | `tests/coverage/test_coverage_inventory.py` | `ui_clients_*_open` | `ui_clients_{create,update,delete}_preview` |
| Discovery/get-open stay open tools | same | `ui_clients_*_open` | unchanged |
| Honesty 16 still red | open-only gate | pass | pass |
| Live write tests still forbid accept/purge | `test_vision_review_provenance.py` | pass | pass |

The first *failing* product assertion is the preview `tool_name` lock.
The qualify lock on the current file is already false; keep it so
EXECUTE cannot "qualify" a `live_test` accept.

### 2. Smallest fix

- `scripts/generate_coverage_report.py`: after honesty, set
  `ui.parity.contacts.create/update/delete` `tool_name` to the matching
  `ui_clients_*_preview`. Leave `parity_status` open-only. Leave the
  honesty set at 16. Do not touch discovery or get-open rows.
- Update the three inventory assertions that currently require
  `ui_clients_*_open` on those CUD rows.
- Independent writer (EXECUTE, not the live test) overwrites
  `tmp/vision-records/ui_contacts_writes.json`:
  `author=independent_review`, `reviewer_verdict=accept`,
  `run_id=3d5b151dfd5342258f8734373597f8c1`, same `workflow_ref` and
  assertion refs, `purge_verified=false`.
- `purge_frame_dir` on
  `~/.local/share/billy-mcp/vision-tmp/run-3d5b151dfd5342258f8734373597f8c1`.
  Then `mark_purge_verified`. Then delete the other leftover `run-*`
  dirs. Do not touch `chrome-profile`.
- `.gitignore`: add `tmp/vision-records/`.
- `wiki/ui_contacts_writes.md`: one sentence that independent review
  accepted the four-state FastMCP CUD, purged
  `run-3d5b151dfd5342258f8734373597f8c1`, and that the three parity
  rows now name preview tools while honesty still keeps them red.
  Then `wiki update --path=wiki` and `wiki lint --path=wiki`.

Regenerate coverage so the manifest matches the generator. Run the new
unit tests. Then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline). Do not
claim completeness. Do not run another live CUD.

### 3. Do not green

Honesty 16 stays red. Contacts `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. `vision_verified` on
the three CUD rows stays false until a later slice removes them from
the honesty set.

## Done when

- Failing preview-tool and qualify tests existed, then passed.
- Durable record is `independent_review` / `accept` / `purge_verified`
  with `run_id=3d5b151dfd5342258f8734373597f8c1`.
- That frame dir is gone. Leftover `vision-tmp/run-*` dirs are gone.
- Three contacts CUD `tool_name` values are preview. Discovery/get-open
  stay open. Honesty gate still passes. Offline `test.sh` passes.
- No leftover `MCP-UI-C-*` names. No leftover live/inspect profiles.
- `tmp/vision-records/` is gitignored. No PNG/HAR/trace in git.
- No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm the accept record is not from the live test,
the named frame dir is gone, honesty 16 is still red, only the three
CUD rows changed tool names, and no secrets landed.

## Post-Mortem

Completed: durable accept record for `run-3d5b151dfd5342258f8734373597f8c1`
with purge verified. That folder and leftover `run-*` dirs are gone.
`.gitignore` covers `tmp/vision-records/`. Vision qualify tests stay.
Honesty 16 red. `complete=false`. Offline lint and test.sh run in this
step.

Deviations: EXECUTE pointed contacts CUD rows at preview tools before
the accept write. Parent `93D7A063` forbids that order. IR FAIL.
FIX-VERIFY reverted the mapping, the retained-open checker allowlist,
and the inventory preview assertions. Contacts CUD `tool_name` is
`ui_clients_*_open` again.

Review: FAIL, do not green. Same-session accept plus inverted order.
Frames cannot be re-seen. Product CUD and PUT persist from 190.10 still
stand.

Tests: lint run after revert. Targeted honesty/vision/inventory tests
plus commit-mode `test.sh`. `BILLY_TEST_MODE=full` not run: completion
requirements are not all true.

Cleanup: zero `billy-live-contacts-*`. Zero `vision-tmp/run-*`.
`chrome-profile` untouched. No leftover `MCP-UI-C-*`.

Next slice: point the three contacts parity rows at preview tools now
that the recorded verdict and proved purge exist. Do not green. Then
the other honesty-16 families.

