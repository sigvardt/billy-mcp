# 2.42 Wave-5j freeze-contract handoff research refresh

## Scope

Research handoff and gated contract integration. Reconfirm the official-doc
fingerprint and unauthenticated method gates for the nine clear singular CUD
operations on `bankLineMatches`, `bankLines`, and
`bankLineSubjectAssociations`, then integrate the independently authored,
offline-only freeze contract for review. Do not green coverage or implement
product tools in this work.

## Evidence collected

- Re-fetched https://www.billy.dk/api/ → ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (byte-identical to research41).
- Unauth probes against `https://api.billysbilling.com/v2`: POST/PUT 401 and
  DELETE missing-id 200 for all three bank-line resources; bankPayments DELETE
  405; balanceModifiers POST 405.
- Re-extracted Supports lines and property tables; reconfirmed A1 embed
  replace-vs-readonly ambiguity and opaque enums.
- Confirmed UI credentials and `BILLY_API_TOKEN` absent; no interface discovery
  or disposable records.
- At collection time the freeze page was missing and leaf
  `wave5j_freeze_contract` was active. It subsequently completed at `8272e83`;
  the contract is committed on the child branch but not yet integrated.

## Deliverable

Cited brief:
`.fractal/main.billy_complete/tmp/grok-research.md` (research42).

## Execution plan

1. Review the exact child delta at `8272e83`, ensuring its authored contract
   freezes all nine singular operations and eighteen preview/execute names
   without changing coverage or product code. Merge the completed child with a
   no-fast-forward integration commit, regenerate and lint the project wiki,
   and recheck the resulting root diff and coverage status.
2. After the contract exists on the root commit, create one focused
   `--agent=grok` leaf for an independent freeze review. It owns only
   `wiki/wave_fivej_freeze_independent_review.md` and the generated wiki index.
   Its cited review must compare the exact contract, official current Billy
   documentation, manifest rows, probe boundary, and root safety protocol, and
   deliver a durable explicit ACCEPT or REJECT without touching product code or
   coverage flags.
3. Merge and act on the review only after it finishes: ACCEPT opens a separate
   product-ready implementation plan; REJECT is repaired and independently
   re-reviewed. No product leaf, coverage green, live test, UI, or vision work
   may begin beforehand.

## Decomposition and fallback

The completed Codex Power contract leaf and one clean-context Grok review leaf
are the only separable tasks at this gate. Product implementation is dependent
on the review verdict, so parallel implementation would violate the freeze
boundary. If Grok fails before editing because of authentication, quota, or a
rate limit, retry the entire review with Codex Power as a recorded fallback;
that fallback does not satisfy the mandatory Grok review gate.

## Boundaries

No coverage green. No headed browser. No secrets. No persistent test data.
Live/UI/vision/bulk/completeness remain fail-closed.

## Post-Mortem

- Completed: current official documentation and unauthenticated method evidence
  remained unchanged, and the all-red nine-row freeze contract was merged.
- Review: the authoritative root Grok review accepted the offline contract;
  product, live, UI, vision, bulk, and completeness claims stayed closed.
- Cleanup: no credentials, browser artifacts, or persistent test data were
  introduced; the redundant clean review leaf was stopped.
- Next coverage slice: product-ready research and a separate Codex Power
  implementation for the eighteen bank-line ticketed tools, followed by an
  independent product review.
