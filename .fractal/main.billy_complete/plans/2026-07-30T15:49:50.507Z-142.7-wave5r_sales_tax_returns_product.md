# 142.7 Wave-5r salesTaxReturns Product Delivery

## Decision

Adopt the existing Research76 package rather than repeat contract research.
It is a cited Grok brief that reconfirms the current official Billy document
fingerprint and the update-only salesTaxReturns method gates after the accepted
freeze review. The product is a separable implementation slice with a stable
five-source/test/coverage file map, so it belongs to one childless Codex Power
leaf. A product review cannot run in parallel: it must inspect the leaf's
committed product result.

## Baseline to commit before delegation

1. Preserve the current uncommitted Research76 deliverables: the project wiki
   page `wiki/wave_fiver_product_implementation_research.md`, its generated
   index link, the `memory/state.md` update, and the existing Research76 plan.
   They contain only cited, non-sensitive findings; scratch remains ignored.
2. Recheck the authority boundary before committing: freeze page MD5
   `078aca13828b5e0d71b454c1aa2dc00f`; authoritative freeze review ACCEPT at
   `wiki/wave_fiver_freeze_independent_review.md`; official docs MD5
   `8b94b0135c91fd15fe54ea33e088a4be`; and method gates PUT 401, POST/DELETE
   405, empty PUT 400.
3. Run `wiki update --path=wiki`, `wiki lint --path=wiki`, the node lint script,
   and the non-live/non-vision suite. Commit the research baseline before
   spawning so the child receives the approved contract and no uncommitted
   project files.

## Delegation

Create one focused Codex Power leaf from that baseline:

```sh
fractal node init wave5r_sales_tax_returns_product \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --base=main.billy_complete \
  --agent=codex-power \
  --title='Wave-5r salesTaxReturns Product' \
  --max-depth=0 --max-children=0 --max-descendants=0
```

The child owns only these product artifacts:

- `src/billy_mcp/api/sales_tax_return_writes.py`
- `tests/api/test_sales_tax_return_writes.py`
- `src/billy_mcp/server.py`
- `tests/unit/test_coverage_server.py`
- `scripts/generate_coverage_report.py` and regenerated `coverage/` artifacts

Its `NODE.md` must state the following contract and completion requirements:

1. Implement exactly `api_sales_tax_returns_update_preview` and
   `api_sales_tax_returns_update_execute`. The wire operation is
   `PUT /salesTaxReturns/:id`, outer body `{salesTaxReturn: map}`, and the
   required success root is `salesTaxReturns`.
2. Clone the proven user-update shape: forbid unknown outer fields, preserve the
   opaque inner map, reject a mismatched optional `salesTaxReturn.id`, issue a
   single-use ticket with a five-minute-or-less TTL, and accept only the ticket
   at execution. One execute call makes one non-retried HTTP PUT.
3. Cover registration/schema shape, mutation-free preview, exact request
   encoding, response-root rejection, tamper/expiry/replay, typed HTTP errors,
   missing-token no-network behavior, and sensitive-value log redaction.
4. Register the two tools; extend the registry expectation from 262 to 264;
   add only `api.salesTaxReturns.update` to offline implementation evidence;
   regenerate coverage to 179 implemented/contract-tested and 0 live/vision,
   `complete: false`.
5. Do not implement create, singular delete, bulk, webhooks, UI, browser/auth,
   live tests, field-level settlement validation, or other inventory rows. Do
   not alter the accepted freeze/Research76 wiki pages.
6. Run formatting, Ruff, Pyright, touched contract tests, coverage generation,
   `scripts/check_coverage.py --reject-false-completeness`, and the non-live,
   non-vision suite. Commit only the owned product deliverables and finish.

## Integration and review gate

While the leaf runs, monitor its status and outbox only; do not compete for its
files. On completion, inspect the three-dot diff and independently rerun the
listed targeted checks before merging with `git merge --no-ff -m "merge
main.billy_complete.wave5r_sales_tax_returns_product"`. Rebuild and lint the
project wiki after integration.

Only a committed product that satisfies every offline condition may trigger a
new, childless Grok independent-review leaf. That review must issue a clear
ACCEPT or REJECT for the product while explicitly excluding live, UI, vision,
bulk, create/delete, cleanup qualification, and completeness. A rejection or
any drift leaves `api.salesTaxReturns.update` red.

## Non-goals

- No coverage greening from Research76, a plan, or freeze acceptance alone.
- No live organisation/API-token use, browser session, screenshots, HARs,
  traces, or sensitive test material.
- No residual clear-write, special-operation, bulk, UI, or completeness work.

## Post-Mortem

### Completed

- Committed the cited research baseline at `c9d49c5` after full local
  non-live/non-vision verification.
- Configured and started the childless Codex Power leaf
  `main.billy_complete.wave5r_sales_tax_returns_product` with exclusive product
  ownership and the exact two-tool contract.
- A separate Grok review accepted the product implementation map as research;
  it explicitly did not review product code.

### Verification and cleanup

- The root lint, type, coverage-integrity, repository-policy, wiki, and
  1,146-test non-live/non-vision checks pass.
- The root worktree contains only the non-sensitive review record, memory
  update, and this plan post-mortem. No live or browser evidence was created.

### Next unresolved coverage slice

- Await the product leaf's committed implementation. Only then inspect and
  merge its diff, regenerate coverage on root, and start a distinct Grok
  product independent review.
