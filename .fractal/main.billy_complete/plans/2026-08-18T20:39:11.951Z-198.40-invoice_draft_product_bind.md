# 198.40 invoice draft CUD with tagged product bind

## Pivot

Owner `FD39FFE7` / `65521475` / `1F1B34F8` / `56354201`:
invoice draft CUD is next. Product persist and scoped
table-item delete are live-proved. Leftover tagged products
are gone. Official `invoiceLines.product` is required.
Official docs ETag `"121myuqjdm53603"` is unchanged. Do not
re-lock. Do not requalify API contracts.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.

HEAD `50c2b2a` already has fresh `/invoices/new` Kunde bind,
required `unit_price > 0`, **Enhedspris** fill, and
`ui_invoices_*_{preview,execute}`. The live CUD test still
skips `ui_products_create_*`. `_bind_existing_product`
picks the first visible option or succeeds if **Vælg produkt**
is missing. After **Ingen produkter**, that path is
`UI_CHANGED`.

Do not remake Kunde, picker, archive, or daybook dumps
(`113F1E05`, `A337A622`, `D68E402A`). Do not click **Opret
ny**. Do not **Godkend**, send, or email.

## Goal

Prove one tagged FastMCP invoice draft CUD after a disposable
customer and product exist:

1. Offline failing fixture: create preview requires
   `product_name`. Current `InvoiceCreatePreviewInput` has
   none.
2. Offline failing fixture: `_bind_existing_product` fails
   closed when **Vælg produkt** is absent or the exact tag is
   not visible. It must not skip and must not click the first
   unrelated option.
3. Offline failing fixture: live
   `test_ui_invoices_create_update_delete_via_call_tool`
   creates and independently confirms a tagged product before
   `ui_invoices_create_preview`. Today's source never calls
   `ui_products_create_*`.
4. Smallest fix: add `product_name` to the create ticket,
   bind the exact **Vælg produkt** tag via the proved
   `[data-cy='dropdown-icon']` chevron, keep fresh
   `/invoices/new` after both seeds exist.
5. One live FastMCP CUD through `create_server.call_tool`.
   Sequence: customer, product, invoice draft create / update
   / delete. Cleanup invoice, then product (scoped
   `table-item` `delete-icon` / **Ja, slet**), then customer.
   Third session proves all three tags absent.
6. Stay red. Do not remap `ui.parity.invoices.create`. Do not
   finish.

## Why no child

One family on files the root already owns:

- `tests/unit/test_ui_invoices_writes.py`
- `tests/live/test_ui_invoices_writes.py`
- `src/billy_mcp/ui_writes/invoices.py`
- `src/billy_mcp/ui_writes/invoices_form.py`
- `src/billy_mcp/ui_writes/invoices_form_bind.py`
- `wiki/ui_invoices_writes.md`
- `wiki/ui_write_residual_table.md`

The seven write children are completed and merged. A child
would split a live proof the root must run anyway.

## TDD

`728BD2E4` / `1F1B34F8`: failing fixture first, then the
smallest fix.

1. Add an offline unit test that
   `ui_invoices_create_preview` rejects a missing or blank
   `product_name`. Today's `_create_args()` omits it, so this
   is the first fail. Run that unit test. Capture the fail.
2. Add an offline source test that `_bind_existing_product`
   returns `UI_CHANGED` when the **Vælg produkt** textbox is
   missing, and that it looks for the exact `product_name`
   rather than the first option. Today's `return None` skip
   and first-option click are the fail.
3. Add an offline source test that
   `tests/live/test_ui_invoices_writes.py` calls
   `ui_products_create_preview` / `ui_products_create_execute`
   and independently confirms the product before invoice
   preview, then deletes the product after the invoice.
   Today's source never names those tools.
4. Smallest preview fix: add `product_name` to
   `InvoiceCreatePreviewInput` and the create execute
   request. Keep `unit_price > 0` and **Gem som kladde**.
5. Smallest bind fix: open the product chevron, pick the
   exact visible tag, never **Opret ny**. Missing picker or
   missing tag is `UI_CHANGED`.
6. Smallest live fix: create customer, create product
   (`MCP-UI-PRD-` plus 8 hex, `unitPrice=1`), confirm both
   on independent sessions, then fresh `/invoices/new`
   invoice CUD. Cleanup invoice, product, customer.
7. Run `tests/unit/test_ui_invoices_writes.py` and the
   invoice-adjacent commit-mode unit set. Then one live
   FastMCP CUD. If exact product bind still misses, record
   `UI_CHANGED` and stop. Do not invent a new invoice CTA.
8. Update `wiki/ui_invoices_writes.md` and
   `wiki/ui_write_residual_table.md` in place. Stay red.

## Out of scope

- Coverage remaps or greening
- Official-docs re-lock
- Files or daybook writes
- New inspect dumps or archive probes
- New child nodes
- `node finish`
- Send, email, **Godkend**

## Done when

- Offline tests prove required `product_name`, exact product
  bind, and live-test product seed-before-invoice
- Live FastMCP draft either POSTs `/v2/invoices` with a
  non-zero amount, proves fresh-session read-back, updates,
  deletes, and restores the org, or returns `UI_CHANGED`
  without leftover records
- Honesty-16 stay red
- No headed browser, no live API, no new dumps

