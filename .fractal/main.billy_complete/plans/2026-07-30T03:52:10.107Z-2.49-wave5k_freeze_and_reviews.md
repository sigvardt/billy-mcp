# 2.49 Wave-5k freeze contract and independent handoff review

## Objective

Advance only the two prerequisite artifacts for the next offline
`bankPayments` slice: the cited freeze contract and an independent Grok review
of the product-ready handoff. No production tools, coverage greens, live calls,
or browser work are in scope.

## Established evidence and constraints

- The interrupted-but-uncommitted research49 record is retained: its plan and
  memory update cite the current official-doc fingerprint
  `hsisik4g9p3603` / `c2efda0ee4cf9cf200e14910c5fc6996` / 147934 bytes and
  real unauthenticated `bankPayments` gates (POST/PUT 401, singular DELETE
  405). It is a handoff, not implementation authority.
- `wiki/wave_fivek_freeze_authoring_authority_research_independent_review.md`
  already opens freeze-page authoring for only
  `api.bankPayments.{create,update}`. It excludes singular delete, all bulk,
  live, UI, vision, and any coverage change.
- The repo currently has 238 registered `api_*` tools and 166 offline
  implemented/contract-tested rows; all `bankPayments` CUD rows remain red.
  Credentials are unavailable, so the work remains entirely offline.

## Parallel child work

Both jobs are independently valuable and have non-overlapping deliverable
files. I will create, configure, commit, and launch the following leaf nodes
in the execution phase after checking the branch is clean enough for a child
baseline:

Historical children are intentionally left closed: none is running, and their
three-dot diffs contain only node-seed files. Restarting them would not advance
the two current prerequisite artifacts.

1. `wave5k_freeze_contract` routed explicitly with `--agent=codex-power`.
   It owns only `wiki/wave_fivek_ticketed_writes_contract.md` (do not modify
   source, tests, coverage, status, or existing wiki pages/indexes). It must
   author a Wave-5j-shaped offline contract for the two create/update rows and
   their four preview/execute tools, with client-relative `POST`/`PUT`
   `/bankPayments` paths, singular `bankPayment` root, required
   `bankPayments` response root, opaque inner maps, strict outer inputs,
   partial-update ID equality, the shared confirmation protocol, one request
   with no retry, and exact executor binding. It must document `isVoided` as
   irreversible, keep delete excluded by the exact 405 finding, preserve all
   bulk/UI/live/vision rows as red, and state that the page is not itself a
   product gate. The leaf validates prose facts against the cited accepted
   research records and runs wiki lint; it does not perform fresh web research.

2. `wave5k_product_ready_review` routed explicitly with `--agent=grok`.
   It owns only
   `wiki/wave_fivek_product_ready_research_independent_review.md` (and
   non-repository scratch evidence). It independently rechecks the official
   documentation and the locked unauthenticated method gates, then records a
   durable cited ACCEPT or REJECT of research49 as a handoff only. Its review
   must verify the exact four-tool/create-update boundary, 405 exclusion of
   delete, opaque payload discipline, 238-to-242/166-to-168 offline arithmetic,
   and no coverage or completeness claim. It must not author the freeze page,
   code, inventory changes, or browser evidence.

If the Grok node fails before editing because of authentication, quota, or a
rate limit, retry the whole review with a new `--agent=codex-power` fallback
node and preserve the fact that this does not satisfy the mandatory Grok gate.
Never switch that node after it writes a review record.

## Integration and gate checks

After each child completes, inspect its three-dot diff and its durable output;
merge only the scoped result with a labelled no-fast-forward merge. Root will
regenerate and lint the project wiki index after both merges. A contract page
or handoff review that lacks its stated sources, invents delete/bulk behavior,
or changes coverage is rejected rather than merged.

Even if both children are accepted, product implementation remains blocked on
a separate independent Grok review of the authored freeze page. The next plan
will launch that review; only an ACCEPT of both the freeze page and
product-ready handoff can open a later Codex Power product leaf.
