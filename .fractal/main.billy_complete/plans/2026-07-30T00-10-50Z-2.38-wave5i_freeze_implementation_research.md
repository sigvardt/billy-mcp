# Plan 2.38 — Wave-5i sales-tax freeze contract

## Adopted evidence

Grok's cited freeze-implementation brief is complete at
`.fractal/main.billy_complete/tmp/grok-research.md`; it rechecked the unchanged
official-doc fingerprint (`hsisik4g9p3603`,
`c2efda0ee4cf9cf200e14910c5fc6996`) and the unauthenticated 401 POST/PUT plus
idempotent-DELETE-200 trap for both collections. The authoritative drafting
gate is `wiki/wave_fivei_freeze_ready_research_independent_review.md`.

## Scope

Land only the durable, offline contract page
`wiki/wave_fivei_ticketed_writes_contract.md` for the six singular
`salesTaxAccounts` and `salesTaxMetaFields` CUD operations. Preserve the
machine inventory and coverage state: 208 registered `api_*` tools, 151
implemented/contract-tested API rows, zero live/vision evidence, 92 ambiguous
bulk rows, and `complete: false`.

## Execution

1. Draft the freeze page from the cited Grok brief, the accepted Wave-5i
   freeze-ready review, and the Wave-5g dual-resource template. Record exact
   preview/execute names, methods, client-relative paths, singular roots,
   empty additional roots, property uncertainty, the common ticket protocol,
   probe interpretation, later test obligations, and exclusions.
2. Keep both inner request roots opaque and keep bulk, file upload, live,
   browser/UI, vision, webhooks, and product implementation explicitly out of
   scope. Do not change a coverage flag or product source file.
3. Run `wiki update` and `wiki lint` on the shared wiki, inspect the complete
   diff, and run the repository lint/safety gate required before the document
   commit. Update memory only with durable current-state facts.
4. Commit the document baseline with the normal Fractal commit flow. Once that
   committed baseline exists, create a focused `--agent=grok` review child that
   owns only `wiki/wave_fivei_freeze_independent_review.md`. It must independently
   recheck current official documentation and issue an ACCEPT or REJECT for the
   freeze text only; it may not implement product code or green coverage.

## Delegation decision

The contract draft is a single, tightly coupled documentation change and stays
with the root Codex Power lane beside the newly produced research handoff. The
post-commit independent review is separable, requires fresh external-source
judgement, and is therefore delegated to Grok with exclusive ownership of its
review record.

## Acceptance boundary

This iteration accepted the Wave-5i offline freeze contract. Freeze acceptance
is not product acceptance; product-ready research, the twelve real tools and
their tests, live qualification, UI parity, vision, and overall completion
remain red.

## Post-Mortem

- Completed the durable six-row sales-tax account/meta-field ticketed-write
  contract and preserved the red inventory, coverage, live, UI, vision, and
  bulk boundaries.
- Root Grok independently re-fetched the unchanged official documentation,
  reconfirmed the unauthenticated POST/PUT 401 and missing-id DELETE 200 trap,
  and accepted the freeze at `a176887`; no contract correction was required.
- The dedicated Grok child could not authenticate before producing review
  output. Its Codex Power fallback inherited two Grok-only step overrides;
  those were corrected, then the fallback was intentionally closed when the
  authoritative root review landed. No child review artifact was merged.
- `bash .fractal/main.billy_complete/scripts/lint.sh` and
  `bash .fractal/main.billy_complete/scripts/test.sh` passed (`922 passed`).
  No browser ran and no screenshots, frames, HAR files, traces, credentials, or
  customer data were retained.
- Next unresolved coverage slice: product-ready research followed by the
  twelve real Wave-5i tools and offline suites (target 220 API tools and 157
  offline implemented/contract-tested rows), then a separate product review.
