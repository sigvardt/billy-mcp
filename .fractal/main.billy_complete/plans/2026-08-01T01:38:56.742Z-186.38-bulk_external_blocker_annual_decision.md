# 186.38 bulk external-contract blocker + annual decision

## Goal

Product research137 into a durable offline package that:

1. Records **one** machine-readable **external-contract blocker** on all **92**
   `ambiguous_bulk` API rows after the official docs/asset exhaust (still red;
   no bulk tools; no greening).
2. Records the **annual_reports** decision: honest `not_applicable` **rejected**;
   exact unresolved requirement `ANNUAL_REPORTS_ORG_INACCESSIBLE` (row stays red).
3. Updates status/report blocker text and wiki so operators stop bulk
   evidence-only loops and know the annual unlock condition.
4. Leaves a **bounded interface-parity next pointer** (geo `cities` UI parity
   `not_applicable` dual-session freeze) as the follow-on slice — **not** green
   geo NA this package unless dual-session evidence is already committed and
   review-safe without inventing UI absence.

Cited authority: `tmp/grok-research.md` (research137).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged).  
Prior shape freeze: research136 package on tip (`6ef0e69` / bookkeeping
`1ed7209`).  
Annual dual evidence (existing, non-sensitive):  
`tmp/discovery122_summary_dual_saft.json` `.dual.annual_reports` /
`.seed_path_h1.annual_reports` (Upsedasse both sessions; nav `Årsrapporter`).  
Steer: parent inbox `B148CF36`.

## Why this slice

1. Steer forbids repeating evidence-only bulk iterations after docs/asset exhaust.
2. Official bulk request/response schema remains unspecified (research137 §1–2);
   live API qualification is out of user scope → blocker, not tools.
3. Design forbids greening ambiguous bulk and forbids API `not_applicable`.
4. Design allows UI parity `not_applicable` only when no equivalent UI exists;
   annual_reports has nav + route but dual Upsedasse → NA **rejected**, stay red
   with an explicit org/platform unlock requirement.
5. Dual-count list maps exhausted; residual 29 writes still blocked; this is the
   honest convergence package for bulk + annual decisions.

## Non-goals

- Greening any bulk row (`implemented`, `contract_tested`, non-empty `tool_name`).
- Registering `api_*_bulk_*` FastMCP tools.
- Live `BILLY_API_TOKEN` / credentialed bulk probes.
- Marking `ui.discovery.annual_reports` `not_applicable` or live-green.
- Inventing `api_annual_*`.
- Greening residual 29 clear writes.
- Invent dual-count maps (vat↔salesTaxReturns, uploads↔files, etc.).
- Producting geo `not_applicable` parity greens **in this package** (next slice
  after dual-session freeze; document pointer only unless evidence already
  lands cleanly offline-only — default: **do not** green geo this package).
- `complete: true`, live/vision count changes, headed browser, children.
- Node finish.

## Ownership

**Root Grok only. No children.**

| Area | Paths |
| --- | --- |
| Bulk external-contract on inventory | `scripts/generate_coverage_report.py` `bulk_rows()` — keep shape hints; add machine-readable `qualification` object on both bulk_save and bulk_delete rows (or equivalent fields if checker rejects unknown keys — prefer optional dict if `check_coverage` allows extra keys; else encode structured JSON-ish evidence prefix + status-level fields). Required semantics: `kind=external_contract_blocker`, `blocker_code=BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`, docs etag/md5, `asset_sweep=research137`, `offline_shape=research136`, `live_api=out_of_scope_by_user`. Keep `source_kind`/`contract_status=ambiguous_bulk`, empty tool, all green flags false. Extend evidence string to cite research137 asset sweep (OpenAPI 404s / page chunk Supports-only) **without** claiming full contract. |
| Status blocker copy | `qualification_blocker()` in same generator — first clause names external-contract bulk blocker code + still-red ambiguous_bulk + API live out_of_scope + UI incomplete (do not imply bulk is green or complete). |
| Annual discovery evidence (still red) | `scripts/generate_coverage_report.py` — `apply_ui_annual_reports_inaccessible_evidence(row)` (name flexible) applied only when `family == "annual_reports"`: set `discovered=True` **only if** existing design/tests allow discovery true without live tool; **prefer** keep `discovered=false` and update `evidence` / `method_or_route` / `side_effects` / `cleanup` to record dual Upsedasse path class `/:org_slug/annual_reports`, nav `Årsrapporter`, CVR companies hint, `blocker_code=ANNUAL_REPORTS_ORG_INACCESSIBLE`, NA rejected per design L572, exact unlock requirement. **Never** set implemented/contract/live/vision true; **never** set `parity_status=not_applicable`. |
| Inventory / unit tests | `tests/coverage/test_coverage_inventory.py` — assert all 92 bulk rows carry external-contract qualification (or structured evidence markers) and remain toolless/red; assert status blocker mentions external-contract / `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`; assert annual discovery row still red and evidence contains `ANNUAL_REPORTS_ORG_INACCESSIBLE` + NA-rejected / Upsedasse dual; assert `parity_status != not_applicable` still holds (or update assertion carefully if only annual stays non-NA). Do **not** require geo NA rows. |
| Live probe | **No new network fixtures required** if research136 matrices remain. Optionally add a fail-closed constant documenting research137 asset-sweep outcome (`opens_network: false`) in `live_probe.py` only if tests need a single source of truth — skip if noise. |
| Wiki | `wiki/offline_write_probe_rules.md` — research137 external-contract freeze paragraph (docs+asset exhaust done; stop bulk evidence loops; 92 stay red). `wiki/wave5u_method_probe_contract.md` — same. New or extend: `wiki/ui_annual_reports_inaccessible.md` (scrubbed) with dual Upsedasse facts, NA rejection, unlock requirement; link from `_index` if new page. Optional pointer page for next: geo cities UI parity NA dual-session protocol (no product claim). |
| Regenerated manifests | Run `scripts/generate_coverage_report.py` → `coverage/api_v2_manifest.yaml`, `ui_workflows_manifest.yaml`, `status.json`, `report.md`. Expect: `complete: false`; bulk 92; live/vision **38**; implemented/contract **222** (unchanged). |
| Server tools | **No** new FastMCP tools. Confirm no `bulk` tool registration in `src/billy_mcp/server.py`. |

## Implementation steps (EXECUTE)

1. Extend `bulk_rows()` with research137 external-contract qualification + evidence
   citations; keep research136 shape hints and AMBIGUOUS Supports prefix.
2. Update `qualification_blocker()` string for external-contract clarity.
3. Add annual inaccessible evidence applicator; wire in `build_ui_manifest` for
   `annual_reports` only; keep all green flags false; no tool_name.
4. Add/adjust inventory tests for bulk blocker + annual decision; keep
   `test_bulk_rows_remain_ambiguous_and_toolless` invariants.
5. Wiki promotion (scrub iteration labels from durable prose).
6. Regenerate coverage manifests.
7. Offline verify: unit/coverage inventory tests for touched paths; full
   `bash .fractal/main.billy_complete/scripts/lint.sh` and
   `bash .fractal/main.billy_complete/scripts/test.sh` before COMMIT claim.
8. Confirm no secrets, no bulk tools, complete false, live/vision 38.
9. After COMMIT, unsave parent steer `B148CF36` only if package landed as
   specified (blocker + annual decision recorded).

## Acceptance criteria

- [ ] All 92 bulk rows: `source_kind=ambiguous_bulk`, empty `tool_name`,
      `implemented=false`, `contract_tested=false`, `live_tested=false`.
- [ ] All 92 bulk rows carry machine-readable external-contract marker
      (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS` / `external_contract_blocker`)
      plus docs etag/md5 and research136/137 citations.
- [ ] `coverage/status.json` blocker text names external-contract bulk freeze
      (still incomplete overall).
- [ ] `ui.discovery.annual_reports` remains non-green; evidence records NA
      rejected + `ANNUAL_REPORTS_ORG_INACCESSIBLE` + dual Upsedasse path class;
      `parity_status` is **not** `not_applicable`.
- [ ] No new bulk or annual FastMCP tools; live/vision counts stay 38.
- [ ] Wiki updated; wiki lint clean.
- [ ] Offline lint + test suite pass; no live API; no headed browser.
- [ ] `complete: false`.

## Review focus

- Did we accidentally green bulk or annual?
- Is external-contract machine-readable and test-asserted, not prose-only?
- Does annual evidence falsely claim not_applicable?
- Did check_coverage reject extra keys? (fix by encoding without schema break)
- Completeness still correctly false.

## Risks

| Risk | Mitigation |
| --- | --- |
| `check_coverage` rejects unknown `qualification` key on rows | Prefer fields already free-form (`evidence`, `side_effects`) plus status-level structured counts; or extend checker allowlist deliberately |
| Treating annual Upsedasse as NA | Explicit tests that `parity_status != not_applicable` and NA-rejected string present |
| Scope creep into live geo dual-session | Out of primary package; pointer only |
| Claiming complete after blocker | `coverage_is_complete` still false while `ambiguous_bulk` exists |

## Follow-on (not this package)

1. Dual-session freeze for geo/reference UI absence → `ui.parity.cities.*`
   `not_applicable` (and sibling geo families) with design L572 evidence.
2. Residual 29 clear writes remain offline-blocked.
3. annual_reports product only after non-Upsedasse shell on a non-prod org.
4. Bulk tools only if Billy publishes exact bulk schemas (or user re-opens live
   API bulk qualification).

## Post-mortem

_(fill after EXECUTE / IR / COMMIT)_

## Post-Mortem

### Work completed
- research137 external-contract freeze: machine-readable `qualification` on all
  92 `ambiguous_bulk` rows (`kind=external_contract_blocker`,
  `blocker_code=BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`, docs etag/md5,
  research136 shape + research137 asset-sweep evidence; empty tools; all green
  flags false).
- `qualification_blocker()` status text names external-contract freeze.
- `apply_ui_annual_reports_inaccessible_evidence`: dual Upsedasse record;
  `not_applicable` **rejected**; `ANNUAL_REPORTS_ORG_INACCESSIBLE` unlock
  requirement; row stays red (no tool).
- Inventory tests extended; wiki: offline_write_probe_rules,
  wave5u_method_probe_contract, ui_annual_reports_inaccessible + index.
- Regenerated manifests: complete=false; live/vision 38; bulk 92 red.

### Deviations
- None material. Geo UI parity `not_applicable` deferred by plan (pointer only);
  matches IR N3.

### Review findings
- Independent review **ACCEPT** (`tmp/grok-review.md`); no required product
  fixes. Optional N1–N3 deferred (inventory errors[] code; scratch evidence_ref;
  geo next slice).

### Verification
- `bash .fractal/main.billy_complete/scripts/lint.sh` pass.
- `bash .fractal/main.billy_complete/scripts/test.sh` pass: **1564 passed**, 35
  deselected (~6m).
- `scripts/check_coverage.py` via lint: pass; complete=false.
- Wiki lint (project + memory): clean.
- No `BILLY_TEST_MODE=ui-full` / full (completeness not claimed).
- No live API token; no headed browser; no frames this slice.

### Cleanup
- No disposable Billy records created.

### Next unresolved coverage slice
1. Geo/reference UI parity `not_applicable` dual-session freeze (e.g. cities)
   with design L572 evidence of no Billy screen.
2. Residual 29 clear offline writes stay blocked (405/metadata).
3. Bulk 92 remain red under external-contract until Billy publishes schemas or
   user re-opens live API bulk qualification (no more evidence-only bulk loops).
4. `ui.discovery.annual_reports` stays red until non-Upsedasse shell on a
   non-prod org.
5. Dual-count list maps exhausted under wiki freezes.
