# 110.4 Wave-5n invoice-late-fee freeze

## Objective

Turn the accepted research60 package into the bounded Wave-5n freeze page for
singular `invoiceLateFees` create and update. This is a contract-authoring
slice only: it must not add tools, register code, change inventory rows, or
claim any live, UI, vision, or bulk qualification.

## Evidence and current state

- `tmp/grok-research.md` is the required cited Grok brief. It reconfirms the
  official plain API contract is unchanged and records the current HTTP access
  fingerprint `juf598rs793603` / `f3925615c452b34694abb7f2856e845a`.
- The existing inventory rows are `api.invoiceLateFees.create` and
  `api.invoiceLateFees.update`; both remain red. `bulk_save` and `bulk_delete`
  remain ambiguous with empty tool names.
- Official and unauthenticated evidence permits only a freeze for four later
  tools: POST/PUT are auth-gated (401), while singular DELETE and probed bulk
  DELETE are 405. No delete or bulk operation is authorised.
- Wave-5m product is independently accepted offline at
  `wiki/wave_fivem_product_independent_review.md`; that opens freeze authoring.
  The active Codex fallback reviewer is redundant to that gate and does not
  block the freeze.

## Delegation decision

The freeze is a separable, contract-bound wiki deliverable. Spawn one
`codex-power` leaf named `wave5n_invoice_late_fee_freeze` with `wiki` scope and
file ownership limited to `wiki/wave_fiven_ticketed_writes_contract.md` plus
generated index entries. Its seed must repeat the research60 boundary and
explicitly prohibit code, tests, coverage, inventory, credential, and browser
edits. The running Wave-5m fallback reviewer retains sole ownership of its own
review record, so no files overlap.

Do not start a Wave-5n product child yet. Once the freeze is merged, request a
fresh Grok independent freeze review. If that reviewer fails before edits due to
authentication, quota, or rate limit, retry the same review with `codex-power`
and record that it is a non-authoritative fallback; product remains blocked
until the required Grok gate is satisfied.

## Execution sequence

1. Configure, commit, and start the Codex Power freeze leaf from the committed
   root baseline. Its page must identify the two inventory rows, four later
   tool names, POST/PUT client-relative paths, singular request and required
   plural response roots, strict opaque-map and shared-ticket boundaries,
   property constraints, and exact exclusions.
2. Monitor the fallback review and freeze leaf. Merge only completed,
   committed, reviewable work; resolve wiki index links after each merge.
3. On freeze delivery, validate from committed bytes: `wiki lint`, absence of
   `invoice_late_fee_writes.py` and its tests, unchanged invoice-late-fee
   inventory/status rows, no registered write tools, and no credentials or raw
   evidence in tracked paths.
4. Launch the independent freeze review only after the frozen page is on root.
   Treat a static fallback as supplemental, never as the mandatory Grok
   acceptance. Keep product work gated until a valid review result exists.

## Success criteria

- A cited Wave-5n freeze page is merged with a precise offline-only boundary.
- API coverage remains 172 implemented/contract-tested rows, 0 live/vision,
  92 ambiguous bulk rows, and `complete: false`.
- The next authorised action is solely an independent freeze review; no
  invoice-late-fee product implementation begins early.
