# 186.87 UI product-plane bulk parity inventory honesty

## Goal

Product research187 as one **root-only inventory honesty freeze** (no FastMCP bulk tools, **no greening**, **no UI bulk `not_applicable`**):

Align the **58 incomplete product-plane UI bulk parity rows** with design §10.2 and the research187 wall analysis so inventory carries machine-readable discovery-required qualifications instead of only a generic design-mapping evidence string.

| Subset | Count | Exact treatment |
| --- | ---: | --- |
| Product-plane UI bulk save/delete (29 resources) | **58** | Keep `discovered`/`implemented`/`contract_tested`/`live_tested`/`vision_verified` **false**; `tool_name=""`; `parity_status` stays **`discovery_required`** (do **not** set `not_applicable`); attach `qualification` block (see below); evidence cites research187 + API bulk external-contract linkage + dual bulk-chrome required before NA or product |

### Exact 29 resources (research187 §2.2)

```
accounts
attachments
bankLineMatches
bankLineSubjectAssociations
bankLines
bankPayments
billLines
bills
contacts
daybookBalanceAccounts
daybookTransactionLines
daybookTransactions
daybooks
files
invoiceLines
invoices
organizations
postings
products
salesTaxAccounts
salesTaxMetaFields
salesTaxPayments
salesTaxReturns
salesTaxRules
salesTaxRulesets
taxRateDeductionComponents
taxRates
transactions
users
```

### Exact 58 row ids

For each resource `R` above:

```
ui.parity.<R>.bulk_save
ui.parity.<R>.bulk_delete
```

### Qualification block (each of 58)

```yaml
kind: ui_bulk_parity_discovery_required
blocker_code: UI_BULK_CHROME_DUAL_REQUIRED
tools_allowed: false
docs_md5: 8b94b0135c91fd15fe54ea33e088a4be
docs_etag: wcw4x9hqvu3603
api_bulk_blocker: BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS
linked_api_bulk_ops: [api.<R>.bulk_save, api.<R>.bulk_delete]  # matching pair for that row's op
evidence_ref: research187
not_applicable_decision: deferred
not_applicable_reason: >
  Design §10.2 requires dual-session evidence that Billy exposes no equivalent
  bulk workflow (multi-select / bulk-action chrome) before UI bulk NA.
  Inferring NA from API external_contract_blocker alone is forbidden.
  Browser credential refs were unset in research187; dual bulk-chrome not run.
```

### Keep unchanged (REJECT if touched toward green)

| Family | Rule |
| --- | --- |
| API bulk ×92 | Stay `ambiguous_bulk` + `external_contract_blocker` / empty tool_name |
| Residual clear ×29 | Stay research186 honesty (`method_closed_offline` / readonly map / meta delete); no tools |
| Geo/reference UI bulk already NA (34 rows) | Stay `parity_status=not_applicable` with existing dual evidence; **do not** re-apply product-plane honesty over them |
| `ui.discovery.annual_reports` | Stay `ANNUAL_REPORTS_ORG_INACCESSIBLE` red; NA rejected |
| Already-green implemented/contract **470** and live/vision **286** | No demotion; no false green of the 58 |
| Greened non-bulk UI shells | No change |

### Optional (in-scope, no greening)

Refresh `qualification_blocker` string in `scripts/generate_coverage_report.py` so the bulk-branch text no longer says stale “residual UI parity open”. Replace with explicit:

`UI product-plane bulk ×58 discovery_required (UI_BULK_CHROME_DUAL_REQUIRED); annual_reports org_inaccessible`

Do **not** change `coverage_is_complete` logic in this package (API live_tested policy gap is a separate follow-on; residual/bulk walls still force complete false).

### Target math (tip `d1ca96a` / 186.86)

| Metric | Before | After |
| --- | --- | --- |
| live/vision | 286 | **286** |
| implemented/contract | 470 | **470** |
| UI bulk ×58 with `qualification` | 0 | **58** |
| UI incomplete (live≠true) | 59 | **59** (still red) |
| complete | false | **false** |

Root-only Grok. No children. No live API. No headed browser. Not node finish.

## Why this slice (research187)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged vs inventory. Bulk still Supports-only (46 save + 46 delete). |
| Residual unauth | Research187 reconfirm 25×405 / 2×401 / 2×200; residual honesty already landed 186.86 — do not re-plan residual tools. |
| UI bulk inventory gap | 58 product-plane UI bulk rows lack machine-readable `qualification` while API bulk (92) and residual clear (29) already carry structured blockers. |
| Design §10.2 | UI `not_applicable` only with dual evidence Billy exposes no equivalent bulk workflow. Creds unset → cannot NA honestly. |
| Completeness | Walls remain: API bulk 92, residual 29, UI bulk 58, annual 1. Honesty does not claim finish. |

## Non-goals (explicit REJECT triggers)

- Setting any of the 58 rows green (`discovered`/`implemented`/`contract_tested`/`live_tested`/`vision_verified` true).
- Setting `parity_status=not_applicable` without dual bulk-chrome evidence committed + reviewed.
- Shipping any bulk FastMCP tool or registering bulk preview tool names.
- Greening API bulk, residual clear, or annual_reports.
- Claiming `complete:true` or running ui-full as a finish gate.
- Spawning children (ownership is single-file generator + tests + wiki).
- Live API traffic or headed browser.

## Files to touch (EXECUTE)

| Path | Change |
| --- | --- |
| `scripts/generate_coverage_report.py` | Add frozenset of 29 resources / 58 ids; `ui_bulk_parity_discovery_required_qualification()`; `apply_ui_product_plane_bulk_parity_honesty(workflows)`; call from UI manifest build after other parity/NA applies; optional `qualification_blocker` string refresh |
| regenerate | Run generator so `coverage/api_v2_manifest.yaml`, `coverage/ui_workflows_manifest.yaml`, `coverage/status.json`, `coverage/report.md` (if generated) match |
| `tests/coverage/test_coverage_inventory.py` | `test_ui_product_plane_bulk_parity_honesty_rows_are_toolless_and_qualified` — assert exact 58 ids, qualification fields, parity_status discovery_required, no greening, counts 286/470, complete false; assert geo NA bulk rows still not_applicable and untouched by this freeze |
| `wiki/ui_product_plane_bulk_parity_inventory_honesty.md` | Durable honesty page (peer of residual_clear honesty); not product ACCEPT |
| `wiki/_index.md` | Link + one-line desc (or let fractal commit regenerate link block; ensure page exists) |
| `wiki/offline_write_probe_rules.md` | Optional one-line cross-link: UI product-plane bulk honesty is discovery_required, not greening |
| `.fractal/.../memory/state.md` | EXECUTE/IR notes |

## Implementation steps (EXECUTE order)

1. **Constants + qualification builder** next to residual honesty helpers in `generate_coverage_report.py`:
   - `UI_PRODUCT_PLANE_BULK_RESOURCES: frozenset[str]` (29 camelCase resource names matching inventory)
   - `UI_PRODUCT_PLANE_BULK_HONESTY_IDS` derived as `{f"ui.parity.{r}.bulk_save", f"ui.parity.{r}.bulk_delete"}` for each r
   - assert len == 58
2. **`apply_ui_product_plane_bulk_parity_honesty(workflows)`**:
   - For each matching row id: force red flags false; empty tool_name; parity_status must remain/be `discovery_required`; set qualification; append research187 evidence (do not wipe prior design §10.2 mapping text — append)
   - Skip rows already `parity_status=not_applicable` (belt-and-suspenders; frozenset should not include geo NA resources)
3. **Wire apply** into UI document build path (after NA applies so product-plane bulk that somehow became NA is not force-redded incorrectly — order: apply honesty only when row id in frozenset AND current parity_status is discovery_required)
4. **Optional blocker string** update in `qualification_blocker`
5. **Regenerate coverage** via existing script entrypoint used in prior honesty packages
6. **Unit test** as above
7. **Wiki** honesty page + cross-links
8. **Offline verify** before IR:
   - `bash scripts/lint.sh` (or project lint path)
   - commit-mode `bash .fractal/main.billy_complete/scripts/test.sh` offline inventory + unit (no live API; no ui-full)

## Test plan

| Check | Expected |
| --- | --- |
| `len(UI_PRODUCT_PLANE_BULK_HONESTY_IDS) == 58` | pass |
| Each of 58: tool_name empty; all five UI flags false; parity_status discovery_required; qualification.kind/blocker/tools_allowed | pass |
| Each of 58: linked_api / api_bulk_blocker present | pass |
| Geo/reference UI bulk already NA (e.g. accountGroups bulk_*) still not_applicable | pass |
| Residual clear honesty tests still pass | pass |
| API bulk 92 still external_contract_blocker | pass |
| status counts 470/286 complete false | pass |
| No new tools in FastMCP registry for bulk | pass |

## Review criteria (IR)

**ACCEPT package** if honesty freeze matches research187 exactly and math unchanged.

**REJECT package** if any greening, any bulk NA without dual evidence, residual/API bulk mutated toward green, or complete claimed true.

**Node completeness:** FAIL expected (external walls remain).

## Child nodes

None. Root-only Grok. Max depth/children unlimited is not a reason to spawn for a single-generator honesty patch.

## Post-mortem

_(fill after FIX-VERIFY)_

## Done when

- Plan executed; coverage regenerated; unit inventory tests pass offline
- 58 UI bulk rows carry honesty qualifications; still red
- Wiki honesty page present
- IR ACCEPT package; FIX-VERIFY clean; fractal commit lands
- Not node finish

## Post-Mortem

### Work completed

- Research187: docs fingerprint unchanged; residual unauth stable; recommended UI product-plane bulk honesty.
- Plan 186.87: root-only freeze for exact 58 UI bulk rows.
- EXECUTE: generator frozensets (29 resources / 58 ids), `ui_bulk_parity_discovery_required_qualification`, `apply_ui_product_plane_bulk_parity_honesty` at end of `build_ui_manifest`; `qualification_blocker` text refresh; coverage regenerated; unit test; wiki honesty page + residual/offline_write_probe_rules cross-links + `_index`.
- IR: **PASS (package)** / node complete **FAIL** expected (`tmp/grok-review.md`). Required fixes: **none**.

### Deviations

- None vs plan. Optional dual bulk-chrome survey not run (browser credential refs unset) — correctly out of package scope.
- Did not run `BILLY_TEST_MODE=ui-full` or `full` (complete false; not a finish claim).

### Review findings

- Package ACCEPT: 58 rows discovery_required + `UI_BULK_CHROME_DUAL_REQUIRED`; no greening; geo NA bulk untouched; residual 29 and API bulk 92 still red.
- Optional N1: commit untracked plan + wiki page at COMMIT.
- Optional N2/N3: dual bulk-chrome follow-on; scoped API live_tested completeness policy (separate).

### Test results (FIX-VERIFY)

- `bash .fractal/main.billy_complete/scripts/lint.sh` — pass (wiki + memory + coverage inventory + policy).
- `bash .fractal/main.billy_complete/scripts/test.sh` — offline **1668 passed** / 54 deselected (EXECUTE + reconfirm FIX-VERIFY).
- Coverage: implemented/contract **470**, live/vision **286**, `complete: false`.

### Cleanup state

- No disposable Billy org records created.
- No live API token traffic.
- No browser dual; no raw frames to purge.
- Uncommitted product ready for fractal commit (includes untracked plan/wiki honesty page).

### Next unresolved coverage slice

| Wall | Rows | Path |
| --- | ---: | --- |
| API bulk external-contract | 92 | Official bulk body/response schema or user re-opens live API bulk |
| Residual clear honesty (still red) | 29 | Docs Supports fix or live API proof (out of scope) |
| UI product-plane bulk chrome dual | 58 | Dual bulk-chrome survey when browser credentials available (NA only if dual-absent) |
| annual_reports | 1 | Non-Upsedasse org shell |

Not node finish. Counts stay 286/470 complete false after COMMIT.
