# 186.29 ui settings invoicing open Faktura panel


## Goal

Ship the next **read-only invoicing settings open-shell** FastMCP tool for
Billy's Indstillinger **Faktura** panel, dual-confirmed in research128 as
final path `/:org_slug/settings` (opened via seed `settings/invoicing`) with
h1 **`Indstillinger`** and invoicing panel markers (`Faktura`, `Produkter`,
and `Betalingsmetoder` or `Standard fakturalogo`; also `Fakturadesigner`,
`Betalingsvalutaer`), distinct from the company default panel and the
Regnskab accounting panel, with offline contract tests, dual-session live
interface tests (no API token), and a non-sensitive vision record that is
purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research128).  
Discovery: `.fractal/main.billy_complete/tmp/discovery128/summary.json` +
`summary_dual_invoicing.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; `dual_invoicing_seed_ok: true`;
`dual_click_ok: true`; frames purged; PII scrubbed in brief.  
Prior product tip: settings accounting 186.28 (`aaa54c4` / `7fbaf9b`)
`ui_settings_accounting_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family (invoicing seed).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–128).

**Research128 decision:** invoicing is a **SPA panel on the same hub path** as
company and accounting. Request `/:org_slug/settings/invoicing` rewrites to bare
`/:org_slug/settings` with Faktura panel content. Left-rail click on exact
text `Faktura` also opens the same panel (observe-only dual OK). Product
should prefer the **deterministic seed** `settings/invoicing` for open.
Success requires invoicing panel markers and **must reject** the company
default panel (bare hub open) and the Regnskab panel (`settings/accounting`).
Soft nested rejects include `settings/company`, `settings/faktura`,
`settings/invoice`, `settings/invoices`, `settings/vat`, `settings/users`,
`settings/list`, `settings/access-token`, `settings/beta`, etc. No official
settings API — do **not** invent `api_settings_*`. This product is **open-shell
classification only**. Write CTAs (`Gem ændringer`, `Opret betalingsmetode`,
`Upload`, `Opret*`, `Tilføj*`) are **observe-only** — never click.

## Why this slice

- UI live/vision is 30 (through settings accounting).
- Research128 dual headless sessions:
  - Seed `settings/invoicing` → path `/:org_slug/settings`, h1
    `Indstillinger`, dual path/h1/title match true.
  - Panel h2 dual-matched: `Faktura`, `Produkter`, `Standard fakturalogo`,
    `Fakturadesigner`, `Betalingsmetoder`, `Betalingsvalutaer`.
  - `invoicing_panel_success` true both sessions; company and accounting
    success false; distinct from both true.
  - Click Faktura from hub also dual-success (product optional recovery only).
  - Soft aliases soft-empty or wrong panel (reject for this tool).
  - Bare `settings` is company default — **reject** for invoicing tool.
  - `settings/accounting` is Regnskab panel — **reject** for invoicing tool
    (already greened by accounting product).
  - annual_reports remains non-success — **do not green**.
  - Other settings_* (user/vat/users/subscription/access_token/beta) deferred.
- No official Billy API v2 settings resource. Closest offline APIs
  (`api_invoices_*`, `api_products_*`, orgs) are **not** greened by this UI
  open tool and **not** invented here as `api_settings_*`.
- Target still red: `ui.discovery.settings_invoicing` only for this product.
- Hard ban: never click Gem / Opret* / Tilføj* / Upload / Opgrader /
  Opret betalingsmetode.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening other `settings_*` rows (company/accounting re-green, user, vat,
  users, subscription, access_token, beta) from this panel open.
- Producting settings write / ticketed Gem / payment-method create / logo upload.
- Inventing any `api_settings_*` or webhook tools.
- Treating bare hub open (company default) as invoicing success.
- Treating `settings/accounting` Regnskab panel as invoicing success.
- Producting annual_reports.
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings accounting open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_invoicing_open.py` (new; pattern from accounting open; invoicing panel success; hard-ban write CTAs; reject company default + accounting panel + soft aliases) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_invoicing` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_invoicing_open_shell_evidence` for
  **`ui.discovery.settings_invoicing`** only; wire family `settings_invoicing`
  in discovery apply path; ensure `build_browser_egress` retains prior lives
  (including settings company + accounting) + appends invoicing open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings accounting, company,
  inventory, integrations, addons, saft_exports, exports, vat_declarations,
  reports, transactions, daybooks, financing, bank recon, receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_invoicing_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, or children this
iteration.

## Contract freeze (research128 invoicing / Faktura open shell)

### Tool

- **Name:** `ui_settings_invoicing_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, or write payload.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf after SPA rewrite).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_invoicing`.
  - `invoicing_panel_markers_present`: bool true when required markers are
    visible — require **`Faktura`**, **`Produkter`**, and (**`Betalingsmetoder`**
    **or** **`Standard fakturalogo`**) as panel markers (h2 / visible body).
    Optional observation of `Fakturadesigner` / `Betalingsvalutaer` without
    logging invoice field values, payment accounts, or PII.
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** both classify as
    the active panel signature.
  - Must be **distinct from accounting**: accounting panel trio
    (`Regnskab` + `Køb` + `Kontoplan` as active panel h2 set) must **not**
    classify as success. Rail label `Faktura` alone is insufficient — it
    appears on the left rail of other settings panels too.
  - Optional observe-only: `save_cta_markers_present` when `Gem ændringer` text
    is visible (**no page dump**, no org names, no invoice setting values).
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path (`settings/company`,
    `settings/faktura`, `settings/invoice`, `settings/invoices`,
    `settings/vat`, `settings/users`, `settings/list`,
    `settings/access-token`, `settings/beta`, `indstillinger`, …)
  - hub open but **wrong panel** (company default; Regnskab accounting panel;
    incomplete subscription chrome; empty/other panels)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
- **Never:** click Gem ændringer / Opret* / Tilføj* / Upload / Opgrader /
  Opret betalingsmetode; invent `api_settings_*`; treat company or accounting
  open as this tool's success.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Prefer direct
   `https://mit.billy.dk/{org_slug}/settings/invoicing`
   (SPA rewrites to bare `/settings` with Faktura panel).
3. Optional recovery only: bare `/settings` then exact-text click `Faktura`
   (never click write CTAs). Prefer seed path for primary product path.
4. Do **not** accept bare `/settings` alone as success (company default).
5. Independent second session (fresh profile) re-opens the same seed path and
   asserts path / h1 Indstillinger / invoicing panel markers. **No** API
   read-back.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers |
| Accounting panel | seed `settings/accounting` / Regnskab+Køb+Kontoplan active panel |
| Soft nested paths | `settings/company`, `settings/faktura`, `settings/invoice`, `settings/invoices`, `settings/list`, `settings/vat`, `settings/users`, `settings/access-token`, `settings/beta`, … |
| Other hub panels | incomplete subscription chrome; Profil/etc. until frozen |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, settings company/accounting tool success |

### CTA hard ban (tests must prove no click)

- Gem ændringer / Gem / Save  
- Opret* / Tilføj* / Upload / file input activate  
- Opret betalingsmetode  
- Opgrader / Upgrade / Slet / Delete  

Observation of CTA **text** is allowed (and expected on invoicing panel).
**Click count for write CTAs must stay 0.** Observe-only Faktura tab click is
allowed only if used as recovery navigation inside the tool (prefer seed).

## EXECUTE steps (root)

1. **Models** — `UiSettingsInvoicingOpenInput` (empty, forbid extra),
   `UiSettingsInvoicingOpenSuccess` (`path_class`, `heading=Indstillinger`,
   `shell_kind=settings_invoicing`, `invoicing_panel_markers_present`).
2. **Browser** — `UiSettingsInvoicingOpenService` protocol +
   `BrowserRuntime.ui_settings_invoicing_open` implementing seed open +
   invoicing panel classify; soft-alias / company default / accounting panel /
   error → `UI_CHANGED`; write CTAs never clicked. Signature uses `.first` for
   multi-match rail/body labels (Faktura appears on rail of other panels).
3. **Server** — register `ui_settings_invoicing_open` with typed input/output.
4. **Offline tests** — model validation, browser unit (mock page states for
   invoicing success / company default reject / accounting panel reject /
   soft alias / login / error), server registration, coverage inventory map.
5. **Coverage generator** — `apply_ui_settings_invoicing_open_shell_evidence`
   for `ui.discovery.settings_invoicing` only; wire family; egress append
   without drop; regenerate manifests/status (**complete remains false**).
6. **Live tests** — dual-session open via seed; DOM assert path + h1
   Indstillinger + invoicing markers; independent read-back; reject company
   and accounting panels via unit (and soft live checks as feasible);
   write CTA click ban; no API token.
7. **Vision** — capture non-sensitive Indstillinger Faktura chrome frame(s)
   outside repo; Grok vision record; purge frames; store only non-sensitive
   review record with `purge_verified`.
8. **Wiki** — `wiki/ui_settings_invoicing_open_shell.md` (contract + non-claims).
9. **Lint/type/offline suite** before REVIEW; live dual + vision before
   completeness claims for this row.

## FIX-VERIFY / COMMIT gates

- Independent Grok review ACCEPT for product slice (expected overall
  completeness FAIL).
- Offline non-live suite green; live settings invoicing dual + vision
  purge_verified.
- Egress test_references retain all prior UI live paths + settings invoicing
  open (and still include settings company + accounting).
- Coverage: only `ui.discovery.settings_invoicing` newly greened for
  live/vision; complete false; API live_tested stays false with
  out_of_scope_by_user.
- No secrets, frames, HAR, traces in repo.
- `fractal commit` product + memory tip; **not** `node finish`.

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Mis-green all settings_* from hub open | Evidence helper only touches `ui.discovery.settings_invoicing` |
| Company default classified as invoicing | Require Faktura+Produkter+(Betalingsmetoder\|logo); reject company h2 pair |
| Accounting panel false success | Reject Regnskab+Køb+Kontoplan active panel as invoicing success |
| Soft alias false success | Require final path bare settings leaf + invoicing markers |
| Rail-only Faktura false positive | Require full marker set; rail label alone insufficient |
| Accidental Gem / payment-method click | Live test asserts write_cta clicks 0; browser never clicks those labels |
| Egress drop of prior lives | Generator append-only; coverage unit asserts prior refs present (incl. settings company + accounting) |
| annual accidental green | Not in evidence helper target list |
| Inventing API tools | Explicit non-goal; no `api_settings_*` registration |
| PII / invoice values in vision/logs | Scrub org/person names and field values; non-sensitive review record only |

## Done when (this product only)

- [x] `ui_settings_invoicing_open` registered with typed models
- [x] Offline unit + coverage map green
- [x] Live dual-session + vision purge_verified green for invoicing shell
- [x] `ui.discovery.settings_invoicing` live_tested + vision_verified true via generator
- [x] No other discovery rows newly greened
- [x] Wiki shell page written
- [x] Independent review ACCEPT; commit pending

## Post-mortem (fill after EXECUTE/REVIEW)

- Filled in Post-Mortem below after FIX-VERIFY.

## Post-Mortem

### Work completed
- Producted `ui_settings_invoicing_open` (models, browser, server, offline unit,
  live dual, coverage generator, egress append, wiki shell page).
- Greened only `ui.discovery.settings_invoicing` (Indstillinger Faktura panel
  via seed `settings/invoicing` → bare `/settings`; markers Faktura/Produkter/
  Betalingsmetoder or logo).
- Live dual-session + vision record with `purge_verified: true`.
- No `api_settings_*` invented; write CTAs observe-only (never clicked).

### Deviations
- Signature uses `.first` on multi-match labels (Faktura appears on rail of
  other panels); required for Playwright multi-match `is_visible` strictness.
- Optional IR nits N1–N3 left as-is (constant True after signature; no unit for
  stuck nested URL; no live click-count instrumentation).

### Review findings
- Independent review **ACCEPT** product (`tmp/grok-review.md`).
- Required fixes: none.
- Overall completeness **FAIL** (expected).

### Test results (FIX-VERIFY)
- `bash scripts/lint.sh`: pass
- `bash scripts/test.sh` (offline): **1512** passed, 29 deselected
- Live: `tests/live/test_ui_settings_invoicing_open.py`: **1 passed** (reconfirm)
- Vision: `tmp/vision-records/ui_settings_invoicing_open.json` purge_verified true
- Coverage: live/vision **31**; complete **false**
- Egress: invoicing live ref on mit + api hosts; company + accounting + prior
  lives retained

### Cleanup
- No disposable org records created (read-only open).
- Raw frames purged after vision review.

### Next unresolved coverage slice
- Remaining settings_* (user/vat/users/subscription/access_token/beta via
  click-nav freezes).
- `ui.discovery.annual_reports` stays red until non-error shell.
- Residual bulk offline API reds; API live remains `out_of_scope_by_user`.
