# 186.73 ui bills delete open

## Goal

Product research173 ACCEPT:

1. New read-only FastMCP UI tool **`ui_bills_delete_open`** that opens **bill
   delete chrome** on the edit surface frozen as `/:org_slug/bills/:id/edit`.
   Preferred stronger signature: primary **Slet** present, click Slet **once**
   to open confirm overlay (Slet button count ≥2 + **Annuller**), dismiss with
   **Annuller only**. Never second Slet / permanent delete. Never Opdater /
   Godkend / Træk / Upload / Registrer betaling / real DELETE.
2. Green exact **`ui.parity.bills.delete`** / map `api.bills.delete` onto
   `ui_bills_delete_open` (`delete_chrome_open_only` / confirm observe +
   dismiss).
3. Keep greened `ui_bills_list`, `ui_bills_create_open`, `ui_bills_get_open`, and
   `ui_bills_update_open` unchanged. Do **not** dual-count delete onto update
   or get tools. Same path class as update; **distinct assertion family**
   (Slet confirm chrome vs Opdater form).
4. **No** browser egress change this slice (POST/DELETE bills + taxRates GET +
   accounts GET already committed for SPA seed/cleanup).

Target math (tip `6bb8ffc` / product 186.72):
live/vision **172 → 173**; implemented/contract **356 → 357**; complete
**false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research173)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. bills Supports get/list/create/update/bulk save/**delete**/bulk delete. DELETE `/v2/bills/:id`. |
| SPA seed dual | contact + draft bill nested lines (accountId, taxRateId, description, amount; no paymentDate) POST 200 dual under **committed** egress. taxRates/accounts GET require `organizationId`. |
| Edit delete chrome dual | soft **`/:org_slug/bills/:id/edit`** path dual; primary Slet=1 dual; `delete_chrome_ready` **true dual**. |
| Confirm overlay dual | first Slet click dual → Slet=2 + Annuller=1 (no role=dialog); Annuller dismiss dual; still on edit path. |
| Non-destructive | after dismiss: bill SPA GET 200 dual + list marker present dual; cleanup all_clean dual. |
| Get contrast | soft `/:org_slug/bills/:id` Slet button **0 dual** (read detail alone is not delete success). |
| Residual rejects | invoices.delete: Mere body text has Slet dual but role=button Slet=0 — DEFER. products.get detail_ready **false dual**. bulk/annual/email stay red. |
| Isolation | delete_chrome ≠ update form_open (Opdater family) ≠ get detail_open ≠ list/create. Green only **bills.delete**. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research173_focus_dual.json`.  
Probe: `tmp/research173_focus_probe.py`.

## Non-goals

- Any change to `coverage/browser_egress.yaml` this slice
- Greening bills.update again (already green via `ui_bills_update_open`)
- Greening invoices.update/delete, products.get*, special.invoice_email
- bulk_* greening; annual_reports greening
- Ticketed durable UI write for bill delete (no real DELETE from UI tool path;
  no approval ticket for observe-only chrome)
- Clicking confirm / second Slet / permanent delete; Opdater; Godkend
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Dual-counting delete onto list/create/get/update tools
- Greening on body substring "slet" alone without button/Annuller chrome

## Ownership

| Area | Owner |
| --- | --- |
| Models + `BrowserRuntime.ui_bills_delete_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count bills.delete only | Root Grok EXECUTE |
| Unit tests (models + browser injectable) | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research173)

### A. Browser egress

**No change.** Reuse committed:

- exact POST `/v2/bills`
- prefix DELETE `/v2/bills`
- prefix GET `/v2/taxRates`
- prefix GET `/v2/bills`
- prefix GET `/v2/accounts` (expense account pick)
- contacts POST/DELETE (seed contact if needed)

Harness SPA token only. Never `BILLY_API_TOKEN`.

### B. Tool: `ui_bills_delete_open`

| Field | Contract |
| --- | --- |
| Name | `ui_bills_delete_open` |
| Input | empty / org-from-session only (mirror update-open: open first openable draft / marker path; no caller-provided generic browser control; optional internal seed when list empty) |
| Success model | `UiBillsDeleteOpenSuccess` with booleans only (no raw ids/PII): e.g. `edit_open`, `slet_present`, `confirm_open`, `annuller_present`, `confirm_dismissed`, `shell_markers_present`; path freeze via internal asserts; `shell_kind=bills_delete` if model carries it |
| Path class | **`/:org_slug/bills/:id/edit` only**. Not bare list, not `/new`, not read-only `/:org_slug/bills/:id` (Slet absent). |
| Errors | typed `ToolError` for auth/egress/UI_CHANGED; never generic browser control |
| Preferred flow | open edit → assert primary Slet ≥1 → click Slet once → assert confirm (Slet ≥2 and Annuller ≥1; **do not** require role=dialog) → click Annuller → success |
| Minimal acceptable | if confirm click is flaky: assert primary Slet ≥1 + Ret regning on edit path **only if** product still proves delete-distinct flags vs update (prefer confirm path; research173 dual-stable) |
| Forbidden | second Slet, permanent delete, Opdater, Godkend, Træk, Upload fil, Registrer betaling, API DELETE from tool path |

Mirror structure of `ui_bills_update_open` / `ui_clients_delete_open` (models →
browser runtime → server registry → unit tests with injectable page/context).

### C. Live dual qualification

File: `tests/live/test_ui_bills_delete_open.py`

| Rule | Requirement |
| --- | --- |
| Dual sessions | two independent ephemeral profiles; second is independent read-back |
| Auth | browser credential references only; no API token env |
| Seed | if no draft bill: SPA POST contact (supplier) + bill with nested line (account+taxRate+description+amount; omit paymentDate); unique R18673 markers |
| Open path | navigate to `/:org_slug/bills/:id/edit` (or list → edit) |
| DOM assert | path_class edit; Slet present; after first Slet: Slet≥2 + Annuller; after Annuller still on edit; bill marker still listable |
| Never | click second Slet / confirm permanent delete; Opdater; Godkend |
| Read-back | second session opens same delete chrome independently |
| Cleanup | DELETE bill → contact (and any seed deps) reverse order; fresh list read-back clean dual |
| Vision | non-sensitive record under `tmp/vision-records/ui_bills_delete_open.json`; frames outside repo; purge verified |
| api_token_used | false |

### D. Coverage generator

In `scripts/generate_coverage_report.py`:

- Constants: tool name, live test ref, vision record path, research173 cite
- `apply_ui_bills_delete_open_shell_evidence(row, parity_of_api_delete=True)`
  sets discovered/implemented/contract_tested/live_tested/vision_verified,
  parity_status=`delete_chrome_open_only` (or project-consistent delete status),
  tool_name, evidence, test_references
- Apply **only** when row maps `api.bills.delete` / `ui.parity.bills.delete`
- Do **not** apply to list/create/get/update/bulk bills rows
- Status math: live/vision 173; implemented/contract 357; complete false
- Blocker text may still mention residual UI + bulk + annual

### E. Wiki

Short shell page `wiki/ui_bills_delete_open_shell.md` (pattern from
`ui_bills_update_open_shell.md` / `ui_clients_delete_open_shell.md`): path
freeze, never confirm delete, dual-count only bills.delete, research173
pointer scrubbed of iteration noise where durable.

## File touch list (EXECUTE)

| Path | Action |
| --- | --- |
| `src/billy_mcp/models.py` | add `UiBillsDeleteOpenSuccess` (+ input if needed) |
| `src/billy_mcp/browser.py` | `ui_bills_delete_open` workflow + helpers |
| `src/billy_mcp/server.py` | register tool |
| `scripts/generate_coverage_report.py` | dual-count apply bills.delete |
| `tests/unit/test_browser.py` / models unit | injectable open + flags |
| `tests/live/test_ui_bills_delete_open.py` | dual live |
| `tests/…` inventory if asserts fixed tool set | extend |
| `wiki/ui_bills_delete_open_shell.md` | shell note |
| `coverage/*` | regenerate via generator (not hand-edit green) |
| `coverage/browser_egress.yaml` | **no change** |

## Verification (EXECUTE → FIX-VERIFY)

1. `bash .fractal/main.billy_complete/scripts/lint.sh` pass  
2. Offline: `BILLY_TEST_MODE=commit` / default offline test.sh (unit+contract) pass  
3. Live dual: `tests/live/test_ui_bills_delete_open.py` pass (when credentials present)  
4. Vision record written; frames purged  
5. `uv run python scripts/generate_coverage_report.py` → bills.delete green;
   list/create/get/update unchanged; live/vision 173; contract 357; complete false  
6. `scripts/check_coverage.py` (without require-complete) consistent  
7. No credentials/tokens in repo diff  

## IR acceptance criteria

- ACCEPT if: delete_chrome on edit path dual; confirm open+Annuller dismiss
  dual without bill loss; dual-count only bills.delete; get/list/create/update
  tools unchanged; no egress churn; no bulk greening; cleanup dual proven;
  vision record non-sensitive.
- REJECT if: dual-counts wrong rows; confirms permanent delete; greened
  products/invoice_email/update again; uses BILLY_API_TOKEN; treats read detail
  (no Slet) as success; incomplete cleanup; greened on "slet" substring alone.

## Out of slice residual (leave red)

- invoices.update/delete; products.get/update/delete
- special.invoice_email
- bulk 92 external-contract; annual_reports org_inaccessible
- API live_tested stays false (`out_of_scope_by_user`)

## Ready for EXECUTE

Root Grok implements plan 186.73 from research173. Not finish.

## Post-Mortem

- Outcome: DELIVERED ui_bills_delete_open (delete_chrome_open_only maps
  api.bills.delete / ui.parity.bills.delete). IR ACCEPT; no required fixes
  (optional N1 Escape-dismiss proof / N2 confirm under-count fallback left as
  non-blocking nits; live dual used Annuller + Slet≥2 path).
- live/vision: 172 → 173
- implemented/contract: 356 → 357
- complete: false (bulk 92 external-contract; annual_reports org_inaccessible;
  residual UI parity open)
- Tests: lint.sh pass; offline BILLY_TEST_MODE=commit 1634 passed / 48 deselected
  (EXECUTE + FIX-VERIFY re-run); live dual tests/live/test_ui_bills_delete_open.py
  pass (EXECUTE); vision record purge_verified true.
- Cleanup: SPA seed contact+bill deleted reverse-order; dual list read-back clean;
  profiles ephemeral; frames purged outside repo.
- Deviations: none material. No egress path_allow expansion (reused 186.71/72).
- Next unresolved coverage slice: prefer invoices.update form residual (path
  overlaps get) or products.get only if dual-stable; special.invoice_email if
  durable dual holds; no bulk greening; no annual green without access. Not
  node finish.

