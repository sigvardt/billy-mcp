# 186.33 ui settings access token open Adgangsnøgler panel


## Goal

Ship the next **read-only access-keys settings open-shell** FastMCP tool for
Billy's Indstillinger **Adgangsnøgler** panel, dual-confirmed in research132 as
final path `/:org_slug/settings` opened via hub open then observe-only side-nav
click **`Adgangsnøgler`**, with h1 **`Indstillinger`** and sole content h2
**`Adgangsnøgler`**, distinct from company / Regnskab / Faktura / Profil /
Momssatser / Brugere / Betas panels, with offline contract tests, dual-session
live interface tests (no API token), and a non-sensitive vision record that is
purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research132).  
Discovery: `.fractal/main.billy_complete/tmp/discovery132/summary.json` +
`summary_dual_settings_click.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; dual click path/h1/h2 match for
Adgangsnøgler; frames purged; PII scrubbed in discovery summaries.  
Prior product tip: settings users 186.32 (`79048b0`) `ui_settings_users_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family (access tokens / Adgangsnøgler).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–132).

**Research132 decision:** soft path seeds for access tokens are **not**
sufficient. Soft `settings/access-token`, `settings/access-tokens`,
`settings/api`, `settings/api-keys`, `settings/tokens`, `settings/keys`,
`settings/adgangsnoegler`, `settings/adgangsnogler` soft-empty. Hub bare
`settings` is **company default**. Product **must** open via authenticated hub
`/:org_slug/settings` then exact-text / role click of side label
**`Adgangsnøgler`** (observe-only; never write CTAs). Success requires **strict
h2 `Adgangsnøgler`** and **must reject** company, accounting, invoicing,
user/Profil, vat/Momssatser, users/Brugere, beta, and empty subscription
chrome. No official settings or access-keys CRUD API — do **not** invent
`api_settings_*`, `api_access_token_*`, or `api_api_keys_*`. Do **not** green
this row from offline API auth / OAuth token docs. This product is **open-shell
classification only**. Write CTA **`Opret adgangsnøgle`** (and bare `Opret`) is
**observe-only** — never click. Empty key list + panel h2 + create CTA is a
valid open shell in the test org.

## Why this slice

- UI live/vision is 34 (through settings users).
- Research132 dual headless sessions:
  - Click **Adgangsnøgler** from hub → path `/:org_slug/settings`, h1
    `Indstillinger`, dual path/h1/h2 match true.
  - Panel h2 dual-matched sole marker: `Adgangsnøgler` (`h2_only_adgangsnoegler`).
  - Body chrome (scrubbed): side rail labels + panel heading +
    `Opret adgangsnøgle` CTA; form_count 0; input_count 0.
  - Soft seeds listed above soft-empty — **reject**.
  - Hub default company panel — **reject**.
  - Control clicks same sessions: Betas → beta true (h2 Betas + Tidlig adgang);
    Brugere → users true; Virksomhed → company true — all dual false for
    access_token success.
  - Prior research129 loose body markers false-positived vat/beta on this
    panel; product **must** use strict h2 only (research132 clean).
  - Also dual-frozen for **later** tip (not this product): Betas.
  - subscription empty chrome and annual_reports Upsedasse stay red.
- Official docs only describe OAuth/API-token **authentication**, not UI key
  CRUD. No invent API tools.
- Target still red: `ui.discovery.settings_access_token` only for this product.
- Hard ban: never click Opret adgangsnøgle / Opret* / Gem* / Tilføj* / Slet /
  Upload / Opgrader* / Invitér* / Overdrag*.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening other `settings_*` rows (subscription, beta) or re-greening
  company/accounting/invoicing/user/vat/users from this panel open.
- Producting settings write / ticketed create-or-revoke access-key workflows.
- Inventing any `api_settings_*`, `api_access_token_*`, `api_api_keys_*`, or
  webhook tools; rewriting auth API-token offline behaviour as UI product.
- Treating bare hub open (company default) or soft access-token seeds as
  success.
- Confusing with API OAuth access-token auth docs or `auth_*` tools.
- Producting annual_reports, Abonnement empty chrome, or Betas this slice.
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings users open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_access_token_open.py` (new; pattern from users open; hub+click Adgangsnøgler; hard-ban Opret adgangsnøgle; reject company default + accounting + invoicing + user + vat + users + soft aliases) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_access_token` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_access_token_open_shell_evidence` for
  **`ui.discovery.settings_access_token`** only; wire family
  `settings_access_token` in discovery apply path; ensure
  `build_browser_egress` retains prior lives (including settings company +
  accounting + invoicing + user + vat + users) + appends access_token open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings users, vat, user,
  invoicing, accounting, company, inventory, integrations, addons,
  saft_exports, exports, vat_declarations, reports, transactions, daybooks,
  financing, bank recon, receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_access_token_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, beta product, or
children this iteration.

## Contract freeze (research132 access token / Adgangsnøgler open shell)

### Tool

- **Name:** `ui_settings_access_token_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, write payload, or secret
  token fields.
- **Success:** non-PII / non-secret only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf; SPA does **not** keep `/settings/access-token` etc.).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_access_token`.
  - `access_token_panel_markers_present`: bool true when required markers are
    visible — require h2 set includes **`Adgangsnøgler`** (strict; research132
    observed sole content h2).
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** both classify as
    the active panel signature.
  - Must be **distinct from accounting**: accounting panel trio
    (`Regnskab` + `Køb` + `Kontoplan`) must **not** classify as success.
  - Must be **distinct from invoicing**: invoicing panel
    (`Faktura` + `Produkter` + (`Betalingsmetoder` or `Standard fakturalogo`))
    must **not** classify as success.
  - Must be **distinct from user/Profil**: user panel
    (`Profil` + `Billede` + `Sprog og tema` + `Skift adgangskode`) must
    **not** classify as success.
  - Must be **distinct from vat/Momssatser**: vat panel triad
    (`Regelsæt` + `Satser for salg` + `Satser for køb`) must **not** classify
    as success.
  - Must be **distinct from users/Brugere**: users panel pair
    (`Brugere` + `Revisorer og bogholdere`) must **not** classify as success.
  - Must be **distinct from beta**: beta panel pair
    (`Betas` + `Tidlig adgang`) must **not** classify as success.
  - Optional observe-only: `create_access_key_cta_present` when
    `Opret adgangsnøgle` text is visible (**no page dump**, no secret token
    values, no person names, no emails, no org secrets).
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path or wrong panel
    (`settings/access-token` soft-empty, `settings/api`, `settings/keys`, …)
  - hub open but **wrong panel** (company default; Regnskab; Faktura; Profil;
    Momssatser; Brugere; Betas; empty Abonnement chrome)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
- **Never:** click Opret adgangsnøgle / Opret* / Gem* / Tilføj* / Upload /
  Slet / Opgrader* / Invitér* / Overdrag*; invent `api_settings_*` or
  access-key API tools; treat company, accounting, invoicing, user, vat,
  users, or beta open as this tool's success; use loose body/side-nav chrome
  labels (Momssatser/Betas in rail) as access-token success; log or return
  secret key material.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Open hub `https://mit.billy.dk/{org_slug}/settings` (company default is
   expected intermediate; **not** success).
3. Observe-only click exact side label **`Adgangsnøgler`** (prefer role
   link/button, fall back to exact text). Never click write CTAs.
4. Do **not** accept soft seed `settings/access-token` (or api/keys/tokens)
   alone as success. Do **not** accept bare hub company default.
5. Independent second session (fresh profile) re-opens hub + clicks
   Adgangsnøgler and asserts path / h1 Indstillinger / h2 Adgangsnøgler.
   **No** API read-back. **No** `BILLY_API_TOKEN`.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers / Ejere |
| Accounting panel | seed `settings/accounting` / Regnskab+Køb+Kontoplan |
| Invoicing panel | seed `settings/invoicing` / Faktura+Produkter+… |
| User/Profil panel | hub+Profil / Billede+Sprog og tema+Skift adgangskode |
| VAT/Momssatser panel | hub+Momssatser / Regelsæt+Satser for salg+Satser for køb |
| Users/Brugere panel | hub+Brugere / Brugere+Revisorer og bogholdere |
| Beta panel | hub+Betas / Betas+Tidlig adgang |
| Soft nested paths | `settings/access-token`, `settings/access-tokens`, `settings/api`, `settings/api-keys`, `settings/tokens`, `settings/keys`, `settings/adgangsnoegler`, … |
| Other hub panels | Abonnement empty chrome |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, other settings_* tool success |

### CTA hard ban (tests must prove no click)

- Opret adgangsnøgle  
- Opret* / Tilføj* / Gem ændringer / Gem / Save  
- Slet / Delete / Upload / file input activate  
- Opgrader / Invitér* / Overdrag*  

Observation of CTA **text** is allowed (and expected: `Opret adgangsnøgle`).  
**Click count for write CTAs must stay 0.** The only allowed navigation click
is the side label **`Adgangsnøgler`** (and only as open path).

### Sensitivity

- Never assert, log, or capture secret key strings if keys appear later.
- Scrub opaque long tokens, emails, person names from vision/logs/body asserts.
- Empty key list is valid open-shell success (research132 test org state).

## EXECUTE steps (root)

1. **Models** — `UiSettingsAccessTokenOpenInput` (empty, forbid extra),
   `UiSettingsAccessTokenOpenSuccess` (`path_class`, `heading=Indstillinger`,
   `shell_kind=settings_access_token`,
   `access_token_panel_markers_present`).
2. **Browser** — `UiSettingsAccessTokenOpenService` protocol +
   `BrowserRuntime.ui_settings_access_token_open` implementing hub open +
   Adgangsnøgler click + access-token panel classify; soft-alias / company
   default / accounting / invoicing / user / vat / users / beta / other panel /
   error → `UI_CHANGED`; write CTAs never clicked. Signature uses `.first` for
   multi-match rail/body labels (`Adgangsnøgler` may appear as side-nav +
   panel h2 after open).
3. **Server** — register `ui_settings_access_token_open` with typed
   input/output.
4. **Offline tests** — model validation, browser unit (mock page states for
   access-token success / company default reject / accounting reject /
   invoicing reject / user reject / vat reject / users reject / beta reject /
   soft alias / login / error), server registration, coverage inventory map.
5. **Coverage generator** —
   `apply_ui_settings_access_token_open_shell_evidence` for
   `ui.discovery.settings_access_token` only; wire family; egress append
   without drop; regenerate manifests/status (**complete remains false**).
6. **Live tests** — dual-session open via hub+click Adgangsnøgler; DOM assert
   path + h1 Indstillinger + h2 Adgangsnøgler; independent read-back; reject
   wrong panels via unit (and soft live checks as feasible); write CTA click
   ban; no API token; no secret/PII asserts.
7. **Vision** — capture non-sensitive Indstillinger Adgangsnøgler chrome
   frame(s) outside repo; Grok vision record; purge frames; store only
   non-sensitive review record with `purge_verified` (no secrets, emails,
   phones, person names, customer data dumps).
8. **Wiki** — `wiki/ui_settings_access_token_open_shell.md` (contract +
   non-claims).
9. **Lint/type/offline suite** before REVIEW; live dual + vision before
   completeness claims for this row.

## FIX-VERIFY / COMMIT gates

- Independent Grok review ACCEPT for product slice (expected overall
  completeness FAIL).
- Offline non-live suite green; live settings access_token dual + vision
  purge_verified.
- Egress test_references retain all prior UI live paths + settings access_token
  open (and still include settings company + accounting + invoicing + user +
  vat + users).
- Coverage: only `ui.discovery.settings_access_token` newly greened for
  live/vision; complete false; API live_tested stays false with
  out_of_scope_by_user.
- No secrets, frames, HAR, traces, PII values in repo.
- `fractal commit` product + memory tip; **not** `node finish`.

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Mis-green all settings_* from hub open | Evidence helper only touches `ui.discovery.settings_access_token` |
| Soft seed classified as success | Soft-empty path fails; require hub+click + h2 Adgangsnøgler |
| Company / accounting / invoicing / user / vat / users / beta false success | Explicit distinct-panel rejects |
| Rail-only Adgangsnøgler false positive | Require h2 Adgangsnøgler after click, not side-nav alone before panel load |
| Vat/beta false positive from chrome rail labels | Never use loose body markers; strict h2 only (research132) |
| Accidental Opret adgangsnøgle click | Live test asserts write_cta clicks 0; browser never clicks those labels |
| Secret key leakage in vision/logs | Scrub opaque tokens; no key-value asserts; non-sensitive review only |
| Egress drop of prior lives | Generator append-only; coverage unit asserts prior refs present (incl. settings company + accounting + invoicing + user + vat + users) |
| annual / subscription / beta accidental green | Not in evidence helper target list |
| Inventing API tools | Explicit non-goal; no `api_settings_*` / access-key API registration |
| Confusing with API OAuth auth docs | Non-goal; offline API auth separate |

## Done when (this product only)

- [x] `ui_settings_access_token_open` registered with typed models
- [x] Offline unit + coverage map green
- [x] Live dual-session + vision purge_verified green for Adgangsnøgler shell
- [x] Coverage greened only for `ui.discovery.settings_access_token`; complete false
- [x] Wiki contract page present; no secrets/frames in repo
- [x] Independent review ACCEPT (overall completeness still FAIL)
- [ ] Committed on `main.billy_complete`; **not** node finish

## Post-mortem

### Completed

- Producted `ui_settings_access_token_open` for `ui.discovery.settings_access_token` only.
- Open path: hub `/:org_slug/settings` + observe-only click **Adgangsnøgler**
  (research132; soft seeds insufficient).
- Success: path `/:org_slug/settings`, h1 `Indstillinger`, shell_kind
  `settings_access_token`, marker Adgangsnøgler.
- Distinct from company / accounting / invoicing / user / vat / users / beta panels.
- Offline: lint pass; **1545** non-live pytest passed (FIX-VERIFY reconfirm).
- Live: dual independent sessions + vision purge_verified.
- Coverage: live/vision **35**; `complete: false`.
- IR 186.33: **ACCEPT** product; no required fixes; optional N2 applied (drop
  live frame body assert for `Opret adgangsnøgle` beyond product h2 marker).
- Egress append retains company + accounting + invoicing + user + vat + users + access_token.
- Wiki: `wiki/ui_settings_access_token_open_shell.md` + `_index.md` link.

### Deviations

- None material vs plan. Soft-seed primary open intentionally rejected per
  research132 (click-nav required).

### Review findings

- Required fixes: **none**.
- Optional N1: unit matrix lacks explicit beta/vat/user wrong-panel cases —
  deferred (signature rejects; live dual covers real shell).
- Optional N2: live frame assert used `Opret adgangsnøgle` beyond product
  contract — removed in FIX-VERIFY so frame DOM checks match product markers.

### Cleanup

- No disposable Billy records created (read-only open shell).
- Vision frames purged; `purge_verified: true` on
  `.fractal/main.billy_complete/tmp/vision-records/ui_settings_access_token_open.json`.
- No API token used.

### Next unresolved coverage slice

- Remaining red UI discovery: `settings_subscription` (weak freeze),
  `settings_beta`, `annual_reports` (inaccessible Upsedasse).
- Prefer next product tip: `ui_settings_beta_open` (Betas) after dedicated freeze
  if needed; subscription stays red until non-empty shell.
- Residual ambiguous bulk API reds offline only; no live API.
- Not node finish.
