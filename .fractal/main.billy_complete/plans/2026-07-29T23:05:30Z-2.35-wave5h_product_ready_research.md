# Plan: Wave-5h product-ready research

## Goal

Produce a cited Grok research brief that freezes nothing new and opens a
bounded Codex Power product slice for singular attachment JSON ticketed writes
after Wave-5h freeze-ready ACCEPT and freeze-page authorship.

## Done

- Re-fetched https://www.billy.dk/api/ — ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (byte-identical to research34).
- Unauth probes on locked base for attachments, salesTaxAccounts,
  salesTaxMetaFields, and files JSON POST; results in
  `tmp/write-probes-research35.json`.
- Re-read `/v2/attachments` property table, binary `/files` narrative, and
  `/v2/files` all-readonly property table.
- Reconfirmed freeze-ready ACCEPT and product absence (registry 202, coverage 148).
- Replied to freeze child: permitted derived `wiki/_index.md` link row only.
- Wrote `.fractal/main.billy_complete/tmp/grok-research.md` (research35).
- Updated node memory state with product-ready handoff.

## Recommendation for next IMPLEMENT

1. Start the bounded Codex Power product leaf from the accepted freeze.
2. Product only: `attachment_writes` module, 6 tools, root register 208 `api_*`.
3. Offline suite clone of `test_sales_tax_writes.py` with `additional_plural_roots=()`.
4. Coverage 151 offline after green tests only.
5. Independent Grok product review after root merge.

## Explicit non-goals this research

- No product implementation.
- No coverage green.
- No headed browser / UI credentials.
- No persistent test data.
- No additional freeze-scope edits.

## Post-Mortem

- Completed: cited official-document refresh and the bounded six-tool Codex
  Power product handoff for attachments. The root Grok review subsequently
  accepted this handoff and the merged freeze as offline gates.
- New probe nuance: unauth JSON `POST /files` returns 400 INVALID_JSON before
  auth; still does not open a JSON create contract.
- Review finding: the handoff's stale statement that the freeze was unmerged is
  superseded by freeze merge `c14266e`; the durable review records the current
  state, so no scratch rewrite is required.
- Cleanup: no credentials, browser evidence, live records, or product files
  were created by the research/review work.
- Verification: parent formatting, Ruff, Pyright, coverage and repository-policy
  checks passed; the non-live suite passed **892** tests.
- Next unresolved coverage slice: Wave-5h attachment product implementation
  and product review, then Wave-5i sales-tax accounts + meta-fields.
