# 198.23 Organizations empty-phone restore

## Goal

Radio `23709235` supersedes the dump-era freeze on an empty
original. The dedicated test organisation has a proved phone-only
company panel (`proved_phone_only=true`, `phone_value_len=0`).
Empty is a reversible clear.

TDD first. Honesty-16 stays red. `ui.parity.organizations.update`
still names `ui_settings_company_open`. Do not remap.

Make the existing ticket pair bind exact empty phone as a
deliberate clear, keep non-empty trim, distinguish `clear` from
`set` in preview summary and expected effect, fill
`input[name=phone]`, click the exact **Gem ændringer** control,
and prove the exact input value in a fresh session.

Then through FastMCP: one tagged non-empty phone update,
independent verify, separately previewed exact empty restore,
third-session empty proof.

Authority: `96908DC6`, `23709235`, `AD8966F2`, `728BD2E4`,
`E004E7D5`, `EE0A0F1B`, `C7DBE974`, `F3F318A2`, `5018B3FA`.
Research: `.fractal/main.billy_complete/tmp/grok-research.md`
section 5. Official docs `HEAD` ETag `"121myuqjdm53603"` /
147934 bytes. Inventory lock stays `"wcw4x9hqvu3603"`. Do not
re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.organizations.update` to
  `ui_organizations_update_preview`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Inventing `ui_organizations_create_*`, `ui_annual_*`, or
  `api_settings_*`
- Calling `api.billysbilling.com` or using a live API token
- Remaking the phone dump, invoice inspects, or product
  delete-chrome
- Filling name, CVR, address, email, owners, users, tokens,
  subscription, or VAT
- Guessed URLs, guessed selectors, page-wide text read-back,
  force clicks, `evaluate` clicks
- A caller boolean as approval. Empty phone is the exact string
  `""`, not a flag
- Persisting source, locators, URLs, slugs, phone values, or
  screenshots
- Ledger, files, invoices, products, or a new contacts/bills CUD
- Docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `522491b`. Clean. Not a backstop. `complete=false`. Honesty
16 stay red. Contacts and bills CUD rows already name preview
tools. Invoice CUD stays `ui_invoices_*_open` and frozen.
Products create stays `ui_products_create_open` and frozen.

Phone dump is delivered. Do not remake it.

Current preview contract rejects empty:

- `OrganizationUpdatePreviewInput.phone` is `Field(min_length=1)`
- `phone_is_non_blank` strips and rejects blank
- FastMCP tool signature repeats `min_length=1`
- `test_preview_input_forbids_empty_extras_and_closed_fields`
  expects `{"phone": ""}` to raise
- Tool schema test expects `minLength == 1` on every preview
  field, including `phone`

`BrowserOrganizationSubmitter` fills `input[name='phone']`,
clicks substring **Gem ændringer**, and proves page text. Empty
cannot be proved that way.

`persist_allowed_from` still requires `phone_value_len > 0`.
Wiki `ui_organizations_writes.md` still says do not invent a
blank-phone preview. `23709235` overrides both.

`tests/live/test_ui_organizations_writes.py` still skips on stale
`D5136C2E`, omits `organization_id`, and uses a recording
submitter. Replace that skip. Do not set the env flag as a
bypass.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

Browser egress allows GET `/v2/organizations/` only. If persist
XHR is PUT and egress denies it, add a failing egress test first,
then PUT prefix `/v2/organizations/` only.

## Delegation

No children. Root owns this slice. `728BD2E4` child-split already
landed the seven write-family modules. Those children stay merged
and parked. Unlimited budget is not a reason to spawn.

The honesty-16 failing coverage gate already exists. Do not remake
it. This PLAN starts TDD with the new empty-phone fixture.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/organizations.py` (ticket pair)
- `src/billy_mcp/ui_writes/page_flow.py` (`_fill_named` is the
  proved phone locator)
- `src/billy_mcp/ui_writes/organizations_phone_dump.py` (exact
  **Gem ændringer** count helper; dump is closed)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `tests/unit/test_ui_organizations_writes.py`
- `wiki/ui_organizations_writes.md`

Do not grow the dump live test into a submit.

## Design choices

1. **TDD first (`23709235`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing fixture: preview
   accepts exact `""`, rejects whitespace-only, trims non-empty,
   expected effect and summary distinguish `clear` from `set`,
   and read-back compares `input[name=phone]` exactly including
   empty. Run it. Capture the failure. Then the smallest repair.
2. **Honesty-16 stays red.** Do not remap org tools.
3. **Empty is `""` only.** No `null`, omit, or clear flag. A
   caller boolean is never approval. `organization_id` stays
   non-empty. Execute still accepts only `confirmation_ticket`.
4. **Exact save control.** After fill, count exact **Gem
   ændringer**. Click only when count is 1. No substring
   `text=` locator.
5. **Exact input read-back.** Fresh session reads
   `input[name=phone].input_value()`. Persist `value_len` and a
   boolean match. Never persist the value. Page-wide text is not
   a pass.
6. **Live FastMCP after the contract is green offline.** Tagged
   set, independent verify, empty restore, third-session empty.
   Fail `CLEANUP_FAILED` if restore does not land. Fail
   `UI_CHANGED` if save refuses empty.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
8. **No remap** until independent accept + purge of the real
   update with restored empty state.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.organizations.update` still names
`ui_settings_company_open`.

Add a focused fixture in
`tests/unit/test_ui_organizations_writes.py` (or a sibling unit
file) that currently fails:

- `OrganizationUpdatePreviewInput(phone="", organization_id="org-test")`
  validates
- `phone="   "` still raises
- `phone="  +4500abcd1234  "` stores the trimmed value
- Preview FastMCP call with `phone=""` issues a ticket, does not
  submit, `canonical_request.phone == ""`,
  `expected_effect_state.phone_action == "clear"`, and summary
  names a clear
- Preview with a non-empty phone has `phone_action == "set"`
- A read-back helper compares `input[name=phone]` exactly,
  including empty, and does not call page-wide text search

Run the fixture. Capture the expected failure. Do not edit
`generate_coverage_report.py` or remap coverage.

### 2. Smallest contract repair

- Allow exact empty phone on the model and the preview tool
  schema. Keep `organization_id` `min_length=1`.
- Preserve non-empty trim. Keep `extra=forbid` and every other
  company field forbidden.
- Set `phone_action` to `clear` or `set` on
  `expected_effect_state`. Summary distinguishes the two.
- Add a family read-back that reads `input[name=phone]` on a
  fresh session. Stop using `prove_text_on_fresh_page` for this
  family.
- Click the exact **Gem ændringer** control after a count of 1.
- Reuse `_fill_named(..., "phone", value)` including `""`.
- Stop treating `persist_allowed_from` as a persist freeze.
- Update `wiki/ui_organizations_writes.md` so it no longer
  forbids a blank-phone preview.

### 3. Egress, then live FastMCP

- If the observed persist request is PUT `/v2/organizations/:id`
  and egress denies it: failing narrow egress test first, then
  allow PUT prefix `/v2/organizations/` only. No PATCH. No
  collection PUT. No token API.
- Replace the stale live skip. One FastMCP tagged non-empty
  update (`+4500` plus 8 hex). Independent input match.
- Second, separately previewed execute with `phone=""`.
- Third fresh session: `value_len=0`, `matches_empty=true`.
- Four-state capture outside git. Live test writes
  `author=live_test` / `reviewer_verdict=pending_review` only.
- Fail closed on any other field or panel.

### 4. Stay red

Do not remap. Do not finish. Do not claim complete.

## Success criteria

- Honesty-16 still length 16 and red
- Failing empty-phone fixture existed, then passed
- Preview binds `""` as `clear` and still rejects whitespace-only
- Live FastMCP set plus empty restore proved on
  `input[name=phone]` in fresh sessions
- Company phone restored to empty
- Invoice and product coverage untouched
- No children spawned
- Node stays red

## Post-Mortem

Completed: failing empty-phone fixture, then contract repair.
Preview binds `phone=""` as `clear` and still rejects whitespace-only.
Read-back is `input[name=phone]`. Exact **Gem ændringer** click.
Browser egress PUT prefix `/v2/organizations/` after a failing
allow-test. Live FastMCP tagged set, independent input match,
exact empty restore, third-session empty. Vision record is
`author=live_test` / `pending_review` only. Honesty 16 still red.
`ui.parity.organizations.update` still names
`ui_settings_company_open`. `complete=false`. No remap.

Deviations: first live login on a temp profile was `UI_CHANGED`.
Default write profile plus its existing `-readback` session worked.
No children.

Review next: independent Grok accept and purge before any remap.
Invoice and product stay frozen. Ledger last. Stay red. Do not
finish.

FIX-VERIFY: IR FAIL was empty frames. Live test now writes
`01_initial`, `02_before_submit`, `03_after_set`, `04_restored`
outside git. Recapture passed. Vision stays `pending_review`.
No accept. No remap. Honesty 16 stay red. Commit the contract.
