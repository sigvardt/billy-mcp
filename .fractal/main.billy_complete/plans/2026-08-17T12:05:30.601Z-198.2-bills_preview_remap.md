# 198.2 Point bills CUD rows at preview tools

## Goal

Point `ui.parity.bills.{create,update,delete}` `tool_name` at the
existing preview tools. Coverage stays red.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `91A2C363`,
`C7DBE974`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

198.1 already landed live FastMCP draft-bill CUD plus accept
`run_id=9920dd9476474b41aef70e6d66d24638` (`author=independent_review`,
`reviewer_verdict=accept`, `purge_verified=true`). Frame folder is
gone. This slice remaps only. Same order as contacts 190.12: accept
first, then map.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Removing bills from `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or changing
  its length-16 assertion
- Changing bills `parity_status` off open-only
- Changing discovery or get-open tool names (`ui.discovery.bills_create`,
  `ui.parity.bills.get`, `ui_bills_*_open` shells)
- Remapping the other 10 honesty families
- Another tagged create, live CUD, or vision rewrite
- Live API token calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Booking, pay, email, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `333b41d`. Last commit is 198.1 existing-option bind plus accepted
purged vision. Not a backstop. Tree is clean. `complete=false`.
Honesty 16 stay red. Bills `tool_name` stay `ui_bills_*_open`.

Open-shell evidence still assigns the three CUD rows to
`ui_bills_*_open`. Preview/execute twins already exist in
`src/billy_mcp/ui_writes/bills.py` and reuse `ConfirmationStore`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds. Contacts remap `93D7A063` stays closed.

## Delegation

No children. Root owns this slice. The files sit on the mainline and
would collide if forked:

- `tests/coverage/test_coverage_inventory.py`
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `scripts/generate_coverage_report.py`
- `scripts/check_coverage.py`
- `coverage/ui_workflows_manifest.yaml` (generated)
- `wiki/ui_bills_writes.md`

`728BD2E4` child split was for write-family implementation. Those seven
families are already merged and parked. A three-row generator override
is not a new family.

## Design choices

1. **TDD first (`728BD2E4`).** First product edit is the failing
   inventory `tool_name` lock. Run it. Capture the failure. Then
   implement.
2. **Override after honesty.** Open-shell evidence still writes
   `*_open`. Honesty must still force implemented/live/vision false
   and keep open-only status. Set preview `tool_name` only after
   `apply_ui_cud_parity_open_only_honesty`, next to
   `apply_ui_contacts_cud_preview_tool_names`.
3. **Keep honesty 16.** Gate `EE0A0F1B` stays. Open-only status plus
   preview tool is still not green.
4. **Retain update/delete open shells.** Remap leaves
   `ui_bills_update_open` and `ui_bills_delete_open` without a
   coverage row. They still have live form-open tests. Add them to
   `RETAINED_OPEN_SHELL_TOOLS`. Create-open stays on
   `ui.discovery.bills_create`.
5. **Do not touch the accept record.** It already qualifies. Mapping
   must not self-approve evidence.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions in
`tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`.
Run that file first and confirm it still passes (16 rows red).

Then edit only `tests/coverage/test_coverage_inventory.py`. Run those
assertions. Capture the failure. Do not edit product files yet.

| Case | Location | Expected today | Expected after fix |
| --- | --- | --- | --- |
| `tool_by_id` CUD map | ~1861-1883 | `ui_bills_*_open` | `ui_bills_{create,update,delete}_preview` |
| Explicit create/update/delete asserts | ~2329, 2338, 2347 | `ui_bills_*_open` | matching `*_preview` |
| Discovery create-open | ~2354 | `ui_bills_create_open` | unchanged |
| Get-open | ~2320 | `ui_bills_get_open` | unchanged |
| Honesty 16 still red | open-only gate | pass | pass |

Command for the expected failure:

`uv run pytest -q tests/coverage/test_coverage_inventory.py::test_ui_parity_and_egress_are_complete_but_visibly_red`

The first failing product assertion is the preview `tool_name` lock.

### 2. Smallest fix

- `scripts/generate_coverage_report.py`: add
  `UI_BILLS_CUD_PREVIEW_TOOL_NAMES` and
  `apply_ui_bills_cud_preview_tool_names`. Call it immediately after
  `apply_ui_contacts_cud_preview_tool_names`. Map
  `ui.parity.bills.create` → `ui_bills_create_preview`,
  `ui.parity.bills.update` → `ui_bills_update_preview`,
  `ui.parity.bills.delete` → `ui_bills_delete_preview`.
  Leave `parity_status` open-only. Leave the honesty set at 16.
  Do not change `UI_BILLS_*_OPEN_TOOL_NAME`.
- `uv run python scripts/generate_coverage_report.py` so
  `coverage/ui_workflows_manifest.yaml` matches.
- `scripts/check_coverage.py`: add `ui_bills_update_open` and
  `ui_bills_delete_open` to `RETAINED_OPEN_SHELL_TOOLS`.
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`:
  lock the retained set to the four open shells (two contacts, two
  bills).
- `wiki/ui_bills_writes.md`: say the three CUD rows now name preview
  tools and honesty still keeps them red. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.

Run the inventory tests. Then commit-mode
`bash .fractal/main.billy_complete/scripts/test.sh` (offline). Do not
claim completeness. Do not run another live CUD. Do not rewrite
`tmp/vision-records/ui_bills_writes.json`.

### 3. Do not green

Honesty 16 stays red. Bills `parity_status` stays open-only.
`coverage/status.json` stays `complete=false`. `vision_verified` on
the three CUD rows stays false until a later slice removes them from
the honesty set.

## Done when

- Failing preview-tool inventory tests existed, then passed.
- Three bills CUD `tool_name` values are preview. Discovery/get-open
  stay open. Honesty gate still passes. Offline `test.sh` passes.
- Accept record and purge stay as they are. Zero `vision-tmp/run-*`.
  Zero leftover `MCP-UI-B-*`. Zero `billy-live-bills-*`.
- No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm only the three CUD rows changed tool names,
honesty 16 is still red, the accept record was not rewritten by this
slice, retained open shells stay registered, and no secrets landed.

## Post-Mortem

Work completed:

- Honesty 16 still length 16 and red. `complete=false`.
- Inventory `tool_name` lock failed first on `ui_bills_create_open`
  versus preview, then passed after the after-honesty generator
  override.
- Three bills CUD rows name `ui_bills_{create,update,delete}_preview`.
  Discovery and get-open stay `*_open`.
- `RETAINED_OPEN_SHELL_TOOLS` now includes `ui_bills_update_open` and
  `ui_bills_delete_open`.
- Generator now emits PUT prefix `/v2/bills/` so regenerate matches
  the already-checked-in egress. Working-tree `browser_egress.yaml`
  unchanged versus HEAD.
- Official docs not re-locked.
- Accept record `9920dd9476474b41aef70e6d66d24638` untouched.

Deviations:

- No live CUD this slice. None required.

Review findings (`tmp/grok-review.md`): PASS this slice. Stay red.
No required fix. Non-blockers: stale open-shell `test_references` /
`method_or_route` on the three CUD rows; honesty flags stay false by
set membership; unique-tag create-footer path not re-run live.

Tests:

- Honesty and inventory locks: 3 passed.
- Lint.sh passed. Coverage inventory 305/345. Repository policy passed.
- Offline `BILLY_TEST_MODE=commit` `test.sh`: 1884 passed, 60
  deselected on EXECUTE and again on FIX-VERIFY.

Cleanup:

- Zero `vision-tmp/run-*`. Shared chrome profiles untouched.
- No new bills or suppliers created.

Next unresolved coverage slice:

- Remaining 10 honesty families need live FastMCP proof, then remap.
  Do not green from stubs. Do not finish.

