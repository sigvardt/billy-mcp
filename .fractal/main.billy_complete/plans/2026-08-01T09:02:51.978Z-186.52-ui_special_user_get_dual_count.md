# 186.52 ui special user_get dual-count via settings user shell

## Goal

Dual-count green **`ui.parity.special.user_get`** by mapping the already
qualified read-only **`ui_settings_user_open`** Indstillinger **Profil / user**
panel shell to **`api.special.user_get`** (`GET /v2/user` current-user
bootstrap), using the same empty-input `shell_open_only` contract already live
for `ui.discovery.settings_user`.

No new browser workflow. No new API/UI tools. No greening of
`user_organizations`, `users.get|update|bulk_*`, invoice_email, files_upload,
or postings. No annual product. No bulk external-contract greening.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research151).  
Dual evidence (tmp, non-git): `tmp/research151_residual_dual.json` (+ probe
`tmp/research151_residual_probe.py`).  
Discovery product already green: `ui_settings_user_open` (research129 /
186.30); wiki `wiki/ui_settings_user_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_settings_user_open.py`,
vision record class `tmp/vision-records/ui_settings_user_open.json`.  
API offline special already green: `api.special.user_get` →
`tests/api/test_bootstrap_reads.py` (`live_tested: false`,
`out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–151).  
Prior dual-count precedents: `users.list` → settings_users,
`accounts.list` → settings_accounting, `organizations.list` → settings_company
via `parity_of_api_list=True` in `scripts/generate_coverage_report.py`.

## Why this slice (research151)

1. Dual headless sessions research151 (ephemeral primary + secondary; no API
   token; observe-only):
   - Typed `ui_settings_user_open` dual READY + success
     (`shell_kind=settings_user`, `user_panel_markers_present=true`).
   - Soft seeds `/user`, `/users`, `/userOrganizations` dual soft-empty body_len
     **127** (= nonsense `zz-research151-no-such-route`) — no dedicated
     bootstrap-user soft route.
   - Observe-only settings hub + click **Profil** dual body_len **523**:
     markers profil/billede/sprog_tema/skift_adgangskode/email **true**;
     brugere **false**; levering/bilag/posteringer **false**.
   - Scrubbed prefix dual: Profil / Fulde navn / E-mail-adresse / Telefon /
     Billede / Skift adgangskode (current-user panel).
2. Official docs: `GET /v2/user` is the authenticated **current user** document
   (distinct from org-scoped `/v2/users` and from
   `GET /v2/user/organizations`).
3. Same inventory dual-count pattern as settings-panel dual-counts, with
   **parity_status remaining `shell_open_only`**.
4. One new green UI row only; no new tool surface.
5. Explicit research151 rejections/deferrals stay red this slice:
   - `special.user_organizations` (Virksomheder chrome under Profil only; no
     sub-panel dual open)
   - `special.invoice_email` (Levering email settings ambiguity)
   - `special.files_upload` (Bilag greened → reject pure NA)
   - postings dual-count onto transactions (resource identity: shell already
     maps `api.transactions.list`)
   - productPrices / tax* / bank* / daybooks list / attachments pure NA
   - annual_reports org_inaccessible; bulk 92 external-contract

## Non-goals

- Inventing `api_settings_*` or a second UI user/profile tool
  (`ui_user_get`).
- Greening `ui.parity.special.user_organizations` /
  `invoice_email` / `files_upload`.
- Greening `ui.parity.users.get|update|bulk_*` (users.list already dual-counted
  to Brugere).
- Dual-counting postings onto `ui_transactions_list`.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research129 live
  qualification + research151 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes). Node instructions: Grok owns product on this
root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_settings_user_open_shell_evidence` with
  `parity_of_special_user_get: bool = False` (or `parity_of_api_list` if a
  single flag name is preferred for consistency with users/accounts; either
  works if discovery path keeps default False). When True: keep `api_row_id`
  from parity builder (`api.special.user_get`), append research151 evidence
  mapping, keep `parity_status=shell_open_only`, tool
  `ui_settings_user_open`; discovery path keeps default False and
  `api_row_id=None`; wire
  `if api_row["id"] == "api.special.user_get": apply_...(parity_…=True)` in the
  parity builder loop (alongside organizations/accounts/users). Do **not**
  match bare `api.special.` (would green sibling specials). |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — add
  `ui.parity.special.user_get` → `ui_settings_user_open`; qualified shell /
  dual-count count **42→43**; live/vision total **142→143**
  (`ui_shell_green + geo_na` = `43 + GEO_UI_NOT_APPLICABLE_ROW_COUNT` with
  `GEO_UI_NOT_APPLICABLE_ROW_COUNT` still **100**); assert evidence mentions
  `api.special.user_get` + research151; assert residual specials
  `user_organizations` / `invoice_email` / `files_upload` stay red
  `discovery_required`; hard-code live total 143 where present; keep sibling
  specials delivery/logs NA asserts intact. |
| Unit coverage server tests | `tests/unit/test_coverage_server.py` only if it
  hard-codes live/vision 142 or shell count 42 — bump to 143 / 43. |
| Regenerated manifests | `coverage/ui_workflows_manifest.yaml`,
  `coverage/status.json`, `coverage/report.md` (generator only) |
| Wiki | `wiki/ui_settings_user_open_shell.md` — dual-count note for
  `api.special.user_get` / shell open only; non-claims user_organizations /
  users.get/update; soft `/user` empty observation; API offline non-live;
  update desc / `_index.md` if required |
| Memory | `.fractal/main.billy_complete/memory/state.md` |

**No changes required (reuse tip product):**

- `src/billy_mcp/browser.py` `ui_settings_user_open`
- models / server registration
- `tests/live/test_ui_settings_user_open.py`
- unit browser tests for the user settings shell

## Contract freeze (shell-open only)

| Item | Value |
| --- | --- |
| Tool | `ui_settings_user_open` (existing) |
| Open sequence | hub `/:org_slug/settings` + observe-only click **Profil** |
| Success path class | `/:org_slug/settings` |
| Success h1 | `Indstillinger` |
| shell_kind | `settings_user` |
| Panel markers | Profil / Billede / Sprog og tema / Skift adgangskode |
| Input | empty strict model |
| Soft seeds | `/user`, `/users`, `/userOrganizations` dual soft-empty = nonsense |
| CTA / write | never Gem ændringer / Upload / password submit / Opret* / Tilføj* |
| Parity row | `ui.parity.special.user_get` / `api_row_id=api.special.user_get` |
| parity_status | **`shell_open_only`** |
| API invent | never invent `api_settings_*` |
| Distinct shells | company / accounting / vat / invoicing / users settings stay separate |
| Sibling specials | user_organizations / invoice_email / files_upload stay red |

## EXECUTE steps

1. Implement generator dual-count flag + `api.special.user_get` wiring on
   `apply_ui_settings_user_open_shell_evidence`.
2. Update inventory tests counts and assertions (43 qualified shells/dual-counts;
   live/vision **143** = 43 + 100 NA).
3. Bump any hard-coded 142/42 coverage server tests if present.
4. Run coverage generator; confirm status live/vision **143**, complete
   **false**.
5. Wiki dual-count note on settings user shell page + index desc if needed.
6. Run `bash .fractal/main.billy_complete/scripts/lint.sh` and offline
   `bash .fractal/main.billy_complete/scripts/test.sh` (no live API; no
   `BILLY_TEST_MODE=ui-full` completeness claim).
7. Memory EXECUTE notes; leave dual JSON under tmp (non-git) for IR.

## Verification gates

| Gate | Pass condition |
| --- | --- |
| Row | Exactly **1** new green UI parity row: `ui.parity.special.user_get` |
| Flags | discovered/implemented/contract_tested/live_tested/vision_verified all true |
| tool_name | `ui_settings_user_open` |
| parity_status | `shell_open_only` |
| api_row_id | `api.special.user_get` |
| Evidence | research151 + maps api.special.user_get; no PII |
| Residual specials | user_organizations / invoice_email / files_upload stay red |
| Postings / deferred families | remain incomplete |
| Tools | No new tool registrations |
| API live | All API live_tested remain false with out_of_scope_by_user |
| Complete | `complete: false` |
| Offline tests | Inventory + non-live suite green |
| NA count | GEO NA still **100** (unchanged this slice) |

## Independent review focus

- Exact special id match only (`api.special.user_get`), not bare `api.special.`.
- Distinct from `api.users.list` → Brugere dual-count.
- Soft empty alone is not the green path; greened Profil tool + dual reconfirm is.
- No postings steal; no files_upload/invoice_email greening.
- live/vision math: 43 + 100 = 143; complete false.

## Expected coverage math

| Metric | Before (tip 446b287) | After product |
| --- | --- | --- |
| live/vision | 142 | **143** |
| NA rows | 100 | 100 unchanged |
| dual-count / shell parity greened | 42 | **43** |
| implemented/contract | 326 | ~327 |
| complete | false | false |

## Ready for EXECUTE

Root-only dual-count product. No spawn. Not finish.

## Post-Mortem

### Completed

- Dual-count greened `ui.parity.special.user_get` onto existing
  `ui_settings_user_open` (exact `api.special.user_get` wiring).
- Generator flag `parity_of_special_user_get`; inventory tests 42→43 shells,
  live/vision 142→143; residual specials stay red.
- Wiki dual-count note on `ui_settings_user_open_shell` + `_index` desc.
- Regenerated coverage: implemented/contract **327**, live/vision **143**,
  complete **false**, GEO NA **100** unchanged.
- No new FastMCP tools; no `src/` changes; no live API traffic.

### Deviations

- None vs plan 186.52. Optional IR N1 (settings chrome markers) left as note only.

### Review

- Independent Grok review (`tmp/grok-review.md`): **ACCEPT** product; no required
  fixes. Overall completeness **FAIL** (expected).

### Tests

- FIX-VERIFY: `scripts/lint.sh` green; offline `scripts/test.sh` **1565 passed**,
  35 deselected. Did not run `BILLY_TEST_MODE=ui-full` or `full` (complete still
  false; not a completeness claim).

### Cleanup

- No disposable Billy records created this slice (read-only dual-count inventory).
- Dual probe profiles were ephemeral and deleted during research151.

### Next unresolved coverage slice

- Prefer residual dual-count/list or isolation: `special.user_organizations`
  (Profil → Virksomheder sub-panel dual), `invoice_email` (send UI vs Levering
  settings), nested postings isolation (do not steal transactions), files_upload
  policy, tax*/bank*/productPrices multi-resource. Bulk 92 external-contract and
  annual_reports org_inaccessible stay red. API live remains out_of_scope_by_user.
