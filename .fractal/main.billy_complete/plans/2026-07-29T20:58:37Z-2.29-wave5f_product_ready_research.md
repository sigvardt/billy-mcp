# Wave-5f product-ready research

## Goal

Produce a cited Grok research brief that freezes nothing new and opens a
bounded Codex Power product slice for tax-rate and deduction-component ticketed
writes under the already-accepted Wave-5f freeze.

## Findings

- Official docs re-fetched: ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes — unchanged.
- Wave-5f freeze remains ACCEPT; product code is still absent.
- Unauth POST/PUT on `/taxRates` and `/taxRateDeductionComponents` return 401;
  missing-id DELETE returns 200 (not cleanup proof).
- Next slice: one `tax_writes.py` (12 tools), root register 190 `api_*`, offline
  suite, coverage 142 after green tests only.

## Deliverable

- `.fractal/main.billy_complete/tmp/grok-research.md` (replaced this pass)

## Out of scope

- Implementation, coverage green flips, live/UI/bulk/specials, freeze edits.
