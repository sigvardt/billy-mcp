# 2.44 Wave-5k bankPayments freeze-ready research

## Objective

Produce a cited offline freeze-ready handoff for the next singular CUD cohort
after Wave-5j: `bankPayments` create and update only.

## Findings

- Official docs fingerprint unchanged: ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (byte-identical to research43).
- Unauth probes: POST/PUT `/bankPayments` → 401; DELETE → 405
  `METHOD_NOT_ALLOWED` (overrides Supports delete).
- Authorized offline freeze: 2 inventory rows / 4 tools; delete stays red.
- After 5j product (238/166) and 5k product: registry 242 / coverage 168 offline.
- `cashSide` published debit/credit; `isVoided` primary mutable update
  (irreversible once true); associations create embed via `subjectReference`.
- No coverage greens; no UI/live; no disposable records.

## Deliverable

`.fractal/main.billy_complete/tmp/grok-research.md` (research44).

## Next step (not this RESEARCH process)

Wait for Wave-5j product merge + independent product review ACCEPT, then
Codex Power authors `wiki/wave_fivek_ticketed_writes_contract.md`.

## Execution plan

1. Preserve the current research artifacts for the root commit after their
   normal repository checks. They establish only the Wave-5k freeze-ready
   handoff: no Wave-5k freeze page, product tool, coverage green, live claim,
   or UI claim is authorised in this iteration.
2. The current `wave5j_bank_line_product` child is stopping because an external
   change rerouted its immutable RESEARCH seed from Grok to Codex Power. It has
   no committed product files. Once stopped, retain it only as an audit record;
   do not merge it or modify its seed.
3. Create a replacement focused leaf, explicitly routed with
   `--agent=codex-power`, after the root's durable Wave-5j authority is
   committed. Its completion scope is the accepted Wave-5j product only:
   `src/billy_mcp/api/bank_line_writes.py`, `src/billy_mcp/server.py`,
   `tests/api/test_bank_line_writes.py`,
   `tests/api/test_bank_line_cross_executor.py`,
   `tests/unit/test_coverage_server.py`, `coverage/api_v2_manifest.yaml`, and
   `coverage/status.json`. The leaf must use the existing accepted freeze and
   product-ready review rather than substitute fresh Codex research.
4. Require the replacement leaf to deliver exactly eighteen preview/execute
   tools for the nine frozen bank-line CUD rows, real offline tests, registry
   238, and offline coverage 166. It must preserve `complete: false`, zero
   live/UI/vision rows, all bulk rows red, opaque inner payloads, match-only
   optional child response roots, and no `bankPayments` implementation.
5. Review the finished child diff and its checks from committed bytes, then
   merge it with `--no-ff` only if it meets the frozen scope. Start a separate
   Grok product review with `--agent=grok`; Wave-5j becomes an accepted product
   only after that review accepts the root integration.
6. Only then may Codex Power author the Wave-5k freeze page from this cited
   handoff. It is limited to `bankPayments` create and update; singular delete
   remains red because the authenticated-independent method gate is not yet
   proven and the unauthenticated probe returned 405.

## Post-Mortem

- Completed: Grok produced the cited Wave-5k freeze-ready handoff and an
  independent Grok review accepted it as research only. The docs fingerprint
  remained `hsisik4g9p3603` / `c2efda0ee4cf9cf200e14910c5fc6996` / 147934
  bytes; unauthenticated bankPayments POST/PUT returned 401 and DELETE 405.
- Delivery: the original Wave-5j product child was retired without product
  files after a seed-route interruption. A replacement Codex Power leaf now
  owns only the accepted nine-row Wave-5j product and its offline tests.
- Review evidence: the preassigned inventory label
  `api_bank_payments_delete_preview` is not a registered server tool; its row
  remains unimplemented and red. The future freeze must continue to exclude it.
- Verification: root Ruff format/check, Pyright, coverage and repository-policy
  checks passed; 963 non-live tests passed. No credential, browser, live-write,
  UI, vision, bulk, or completeness claim was added.
- Next unresolved slice: finish and independently review Wave-5j product, then
  author and independently review the constrained Wave-5k freeze page.
