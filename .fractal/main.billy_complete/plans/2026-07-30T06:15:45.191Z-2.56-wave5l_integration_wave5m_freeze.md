# 2.56 Wave-5l integration and Wave-5m freeze gate

## Objective

Integrate the completed Wave-5l `salesTaxPayments` ticketed create/update
slice from `main.billy_complete.wave5l_sales_tax_payment_product` at `00aae09`,
verify it on root, and open the independently reviewed Wave-5m
`contactBalancePayments` freeze-authoring gate without making any live, UI,
vision, bulk, delete, or completeness claim.

## Contract and state

- The cited Grok brief in `.fractal/main.billy_complete/tmp/grok-research.md`
  reconfirms the official docs fingerprint `hsisik4g9p3603` /
  `c2efda0ee4cf9cf200e14910c5fc6996` and authorises only the Wave-5m freeze
  page: singular `contactBalancePayments` create + update.
- Wave-5m requires four eventual tools, opaque `contactBalancePayment` maps,
  `POST`/`PUT` only, and required `contactBalancePayments` response root.
  Singular delete is documented/probed 405; bulk remains ambiguous and empty;
  bankPayment sample fields are not transferable to contactBalancePayment.
- Wave-5l product is complete on its child at `00aae09`, but root remains
  authoritative at 242 tools / 168 offline rows until the reviewed merge.
- Root research updates are already present and must be retained: the cited
  brief reference in memory, current offline-probe rules, and the
  freeze-ready-research plan. Scrub only runtime logs and private-memory
  iteration-history phrasing before committing.

## Delegation decision

This is manager work with two independent, clean ownership boundaries after
root integration:

1. A **Grok** leaf reviews the exact merged Wave-5l product diff against the
   frozen contract and writes only its independent review record. It cannot
   green live/UI/vision/bulk/completeness and must return an explicit ACCEPT or
   defect list.
2. A **Codex Power** leaf authors only
   `wiki/wave_fivem_ticketed_writes_contract.md` from the cited research. It
   owns no product module, inventory change, review page, or coverage green.

The tasks are parallel after root has a committed integration checkpoint. No
Wave-5m product leaf starts until the new freeze page receives a separate Grok
ACCEPT. The review child is intentionally spawned only after integration so it
reviews root bytes rather than a moving child branch.

## Execute

1. Inspect `00aae09` from a clean archive and three-dot diff. Verify the seven
   allowed product paths, exact four registrations, typed flat outer schemas,
   write-ticket behavior, `salesTaxPayments` required response root, coverage
   arithmetic 168 -> 170, and red live/UI/vision/bulk state. Reject child seed
   files and any non-product artifact.
2. Merge only the accepted product bytes with a labelled no-fast-forward merge:
   run `git merge --no-ff --no-commit` for the child, remove its complete
   `.fractal/main.billy_complete.wave5l_sales_tax_payment_product/` seed from
   the merge index, then commit the merge with the required labelled message.
   Run root formatter, Ruff, Pyright, coverage/policy checks, focused
   sales-tax-payment and coverage/registry tests, then `BILLY_TEST_MODE=commit`
   non-live suite. Regenerate coverage only from its checked generator.
3. Preserve and normalize the current cited research handoff: retain the new
   research plan and probe rules, update private state in present tense without
   iteration labels, and restore the runtime-only `codex-power.err` before
   checkpointing root.
4. Commit the root checkpoint before spawning. Create a bounded `--agent=grok`
   review leaf and a disjoint bounded `--agent=codex-power` Wave-5m contract
   authoring leaf using these commands (configure their committed seeds before
   starting them):

   ```bash
   fractal node init wave5l_product_review_grok \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --base=main.billy_complete --agent=grok \
     --max-cost=320 --max-iter-cost=80 --max-iters=10 --step-timeout=30m \
     --max-depth=0 --max-children=0 --max-descendants=0
   fractal node init wave5m_contact_balance_payment_freeze_contract \
     --path=/Volumes/ssd_1/Repositories/billy-mcp \
     --base=main.billy_complete --agent=codex-power \
     --max-cost=320 --max-iter-cost=80 --max-iters=10 --step-timeout=30m \
     --max-depth=0 --max-children=0 --max-descendants=0
   ```

   Give the reviewer exclusive ownership of its review record and the contract
   author exclusive ownership of `wiki/wave_fivem_ticketed_writes_contract.md`.
   Both carry frozen evidence, fail-closed completion criteria, and no child
   descendants; report their scopes through radio after launch.

## Acceptance boundaries

- Wave-5l may be recorded as offline implemented + contract-tested only after
  root verification. It is not product-ACCEPTed until the new Grok review.
- Wave-5m research/freeze prose does not implement or test a tool and does not
  alter coverage rows. Product code awaits an independent freeze ACCEPT.
- No API token, UI credential, browser run, raw evidence, non-Billy request,
  or disposable live record is introduced by this iteration.

## Post-Mortem

- Pending execution.
