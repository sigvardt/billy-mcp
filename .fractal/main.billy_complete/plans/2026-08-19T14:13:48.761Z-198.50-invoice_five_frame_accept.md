# 198.50 Independent five-frame invoice CUD accept

## Pivot

Owner `A342BBDC` / `330C5913` / design §10.4 / §14.3: independent
Grok vision of the five invoice CUD frames, then purge, then
preview-tool remap. HEAD `004edb3` already gated screenshots.
The pending record is `tmp/vision-records/ui_invoices_writes.json`
(`run_id=60b6d620772644f3bca9609c8ae53846`, `author=live_test`,
`pending_review`). Frames still exist outside git.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Do not re-lock (`728BD2E4`). Do not spawn a child. Do not remake
dumps. Do not remake leftover reverse-clean. Do not remake the
five-frame live CUD. Do not green.

Last commit `004edb3` gated `02_before_submit.png` to live-test
mode AND pytest `/live/`. No `(failed on <step>)` backstop.
Working tree clean.

Grok already read the five frames. Visible values:

- `01_before.png`: **Ingen fakturaer**
- `02_before_submit.png`: Kunde `MCP-UI-INV-B9827C71`, product
  `MCP-UI-PRD-53A2CD86`, description `{tag} line`, **Antal** 1,
  **Enhedspris** `1,00`, **Gem som kladde** visible
- `03_after_create.png`: list **Kladde** `1 DKK`, row `1,00 DKK`
- `04_after_update.png`: list **Kladde** `2 DKK`, row `2,00 DKK`
- `05_after_delete.png`: **Ingen fakturaer** again

`04` is the resulting list, not the edit-form **Enhedspris**
input. Accept it. Fresh-session `2,00` is the live-test DOM
assertion (`B9C4FA7C`). Vision is never the sole pass.

## Goal

Independent accept and purge of run `60b6d620`, then point
invoice CUD coverage rows at preview tools. Honesty-16 stay red.

1. Offline fixture: pending `author=live_test` record does not
   qualify. `run_id` matches. Frame dir exists before purge.
2. Write `grok-review.md` naming the five frames. Then
   `write_vision_record(..., author="independent_review",
   reviewer_verdict="accept")`, `purge_frame_dir`,
   `mark_purge_verified`.
3. Remap `ui.parity.invoices.{create,update,delete}` to
   `ui_invoices_{create,update,delete}_preview`. Leave
   `parity_status` open-only. Honesty still forces implemented /
   live / vision false.
4. No live rerun. No honesty-16 membership change.

## Why no child

Six files the root already owns, plus the vision record and
wiki. Independent vision must be this Grok
(`wiki/review_provenance_rules.md`). Research forbids a new
child. `D68E402A` forbids a new inspect helper. The seven write
children stay completed and merged.

## TDD

`A342BBDC` / `C7DBE974` / design §14.3 / `728BD2E4`: lock the
pending record first, then accept and purge, then remap.

1. Add `tests/coverage/test_ui_invoices_writes_vision.py`
   modeled on `test_ui_organizations_update_preview_remap.py`:
   - Load `tmp/vision-records/ui_invoices_writes.json`.
   - If `author=live_test`: assert `pending_review`,
     `purge_verified=false`, `run_id=60b6d620...`,
     `qualifies_for_coverage_vision` is false, and
     `~/.local/share/billy-mcp/vision-tmp/run-60b6d620...`
     still exists with the five PNG names.
   - Run it on HEAD. It must pass while pending.
2. Independent review:
   - Write `.fractal/main.billy_complete/tmp/grok-review.md`
     with the five-frame visible values. Verdict accept.
   - Rewrite the vision record with
     `author=independent_review`, `reviewer_verdict=accept`,
     same `run_id` and `workflow_ref`, same assertion refs plus
     the grok-review path. `purge_verified=False`.
   - `purge_frame_dir` of that `run-*` directory.
   - `mark_purge_verified`.
   - Flip the coverage fixture to the accept branch: author,
     accept, `purge_verified`, frame dir gone,
     `qualifies_for_coverage_vision` true.
   - Live tests still must not contain `reviewer_verdict="accept"`
     or `purge_frame_dir(` (`C7DBE974`).
3. Remap:
   - Change `tests/coverage/test_coverage_inventory.py` so the
     three invoice CUD ids expect `ui_invoices_*_preview`.
     Run it. Capture the fail (HEAD still names `*_open`).
   - Add `UI_INVOICES_CUD_PREVIEW_TOOL_NAMES` and
     `apply_ui_invoices_cud_preview_tool_names` next to the
     contacts/bills/orgs maps. Call it after honesty.
   - Discovery / list / get stay on `*_open` / `*_list`.
4. Wiki + memory: residual table moves invoice CUD to
   `already_live_proved_honesty_red`. `ui_invoices_writes.md`
   records accept + purge. Stay red. Not finish.
5. Offline unit/contract/coverage on the touched files.
   Do not run live UI. Do not set `BILLY_API_TOKEN`.
   Do not remake frames. Do not green honesty-16.

## Out of scope

- Live invoice CUD rerun.
- Product accept of `b1597ed1`.
- Files, daybook, leftover probes.
- Official-docs re-lock.
- `complete: true` or `fractal node finish`.

## Done when

- Vision record is `author=independent_review`,
  `reviewer_verdict=accept`, `purge_verified=true`,
  `run_id=60b6d620772644f3bca9609c8ae53846`.
- Frame directory is gone. No PNG / HAR / trace in the repo.
- Invoice CUD coverage `tool_name` values are the preview tools.
- Honesty-16 rows stay `implemented=false`, `live_tested=false`,
  `vision_verified=false`. `complete` stays false.
- Offline coverage fixtures pass. Stay red. Not finish.

## Post-Mortem

Work completed: independent accept and purge of run
`60b6d620772644f3bca9609c8ae53846`. Invoice CUD coverage names
preview tools. Honesty-16 stay red. IR FAIL: live CUD still
called `write_vision_record` and would overwrite accept.
FIX-VERIFY switched it to `write_live_pending_unless_accepted`.
No live remake. Accept record kept.

Deviations: EXECUTE skipped the live-helper swap. Generator
egress PUT `/v2/invoices/` and GET `/v2/invoiceLines` restored
to match committed `browser_egress.yaml`.

Review: IR FAIL on the clobber hole. Required fix landed.

Tests: source-lock failed on HEAD, then passed. Lint passed.
commit-mode 2110 passed, 83 deselected. Stay red. Not finish.

Cleanup: frame dir gone. Org independently empty after
`330C5913`. No new live records.

Next unresolved: product independent accept of `b1597ed1`.
Honesty-16 stay red.

