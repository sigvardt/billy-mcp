# 190.16 Click named portal create-footer then live bills CUD

## Goal

Click the already-named vendor create footer, then run one tagged draft
bill create/update/delete through `create_server` `call_tool`. Prove the
create dump, the create-form frame, and third-session bill plus supplier
absence. Coverage stays red.

The 07:49 wrapper dump has nine `.ds-dropdown-list.ds-moved-with-portal`
nodes after type, with `has_opret` and `has_tag`. The 05:25 isolate names
the clickable child: footer `Opret "{tag}"` inside the one list whose
text is `Ingen resultater` plus that footer. Current code walks the nine
lists as rows and never binds. No selector fan-out beyond that list and
footer text.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`,
`31F6E753`, `4994ECE6`, `F3F318A2`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`
- Writing `author=independent_review` / `accept` / `purge_verified`
- Accepting stale vision `run_id=3af923d562b546d597e9f156b5c3227f`
- Live API token calls, SPA `POST /v2/contacts` seed, docs re-lock
- Seeding vendor via `ui_clients_create`
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`
- Adding a new selector family beyond the named portal list + footer

## Baseline

HEAD `d931a87`. Clean tree. Not a backstop. `complete=false`. Honesty 16
stay red. Bills `tool_name` stay `ui_bills_*_open`. Contacts remap
`93D7A063` stays closed.

`d931a87` already scoped the Leverandør wrapper and removed page-wide
tag click, `evaluate`, and the six-selector walk. Live create still
returns `UI_CHANGED` (`source=scoped_leverandor_wrapper`). Chosen bind
in `inspect-live-bills-vendor.json` is `null`.

Owner-only create dump is still a unit overwrite (`MCP-BILL-BLANK-RB`,
persist `fake-bill-id`). Ignore it as live proof.

Cited contract (research):

- Official belongs-to is `contact` / `contactId`
- Live create chrome is `input[name='vendor']` with label **Leverandør**
- Empty typeahead list text is `Ingen resultater` + footer `Opret "{tag}"`
- Date `billDate`, amount `billLines.0.inclVatAmount`
- Ticket pattern is already `UiWriteProtocol` / `ConfirmationStore`
- Browser persist is POST `/v2/bills`, PUT prefix `/v2/bills/`, DELETE
  prefix `/v2/bills/`. No PATCH. No collection PUT

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds. No extra CUD parity row is greened by open-only evidence.

## Delegation

No children. Root owns this slice. The files sit on the mainline:

- `src/billy_mcp/ui_writes/bills_form.py`
- `src/billy_mcp/ui_writes/bills_vendor.py`
- `tests/unit/test_ui_bills_writes.py`
- `tests/live/test_ui_bills_writes.py`
- `wiki/ui_bills_writes.md`

Parked write-family children stay parked. `728BD2E4` child split was
for family implementation. Those seven families are already merged. A
portal-footer click is not a new family.

## Design choices

1. **Honesty first (`728BD2E4`).** Confirm the 16-row open-only gate
   still fails those rows closed. Do not edit the gate.
2. **TDD.** First product edit is a failing unit test: nine portal
   lists, only one with `Ingen resultater` + `Opret "{tag}"`, must
   click that list's footer. A decoy list that contains only `Opret`
   must not be clicked. Page-wide exact-tag click is still not a bind.
3. **One matching list.** Pick the portal list whose snapshot has
   `has_opret` and `has_tag`. Click `Opret "{tag}"` inside it. Do not
   walk the nine lists as rows.
4. **Dump isolation.** Every unit test writes dumps under `tmp_path`.
   Live test asserts `inspect-live-bills-create-presubmit.json` has
   `unique_tag` starting `MCP-UI-B-`, `draft_cta` **Gem som kladde**,
   and a portal-footer `vendor_bind`. Assert `01b_create_form.png`
   exists and is non-empty.
5. **`31F6E753` dump before click.** No submit until that dump exists.
6. **Cleanup reverse.** Delete the draft bill, then the supplier.
   Third session proves both gone.
7. **Vision.** `author=live_test`, `reviewer_verdict=pending_review`
   only. `run_id` is the frame-dir suffix.

## Execute

1. Run honesty 16. It must stay length 16 and red.
2. Add failing unit tests for the nine-list / one-footer bind and
   `tmp_path` dumps. Run them. Capture the expected failure.
3. Smallest product change in `bills_form.py` / `bills_vendor.py` so
   those tests pass.
4. Point any remaining unit path at `tmp_path`. Live test asserts
   create dump + create-form frame + supplier absence.
5. One live FastMCP bills CUD. Never submit until the create dump
   shows tag, bound vendor, date, amount, and **Gem som kladde**.
   POST create, **Opdater** PUT, tagged **Slet**. Independent
   second-session read-back. Third-session bill and supplier absence.
6. Offline `test.sh`. Keep coverage red.

## Done when

- Honesty 16 still red, length 16
- Unit tests pick the matching portal list and click its footer
- Live FastMCP create/update/delete of one tagged draft bill passes
  with independent read-back
- Create dump is a live `MCP-UI-B-*` record, not a unit-test overwrite
- Create-form frame exists. Third session shows no bill and no supplier
- Vision record is `live_test` / `pending_review` only, new `run_id`
- `complete=false`. Bills `tool_name` still `ui_bills_*_open`

## After this slice (not now)

Independent vision accept + purge, then remap only the three bills CUD
rows.

## Post-Mortem

Work completed:

- Honesty 16 still length 16 and red. Bills `tool_name` stay
  `ui_bills_*_open`. No remap. Official docs not re-locked.
- Unit picker: nine portal lists, one empty create footer. Decoy and
  huge-ancestor lists skipped.
- Live vendor bind is `scoped:portal_footer`.
- IR FAIL was correct: leftover-footer count error is not a bind;
  date had to be observed. Date field was present and visible;
  a prior click opened date chrome and blocked fill. Fill without
  click sets `value_len` 10.
- Live create dump is now a real `MCP-UI-B-*` record with vendor,
  date, amount `1,00`, **Gem som kladde**, and
  `vendor_bind=scoped:portal_footer`.

Deviations:

- Plan asked for a finished FastMCP CUD. Persist did not fire.
  `BILLY_ERROR` / watched `[]` after **Gem som kladde**.

Review findings (`tmp/grok-review.md`): FAIL stood. Vendor bind
accepted. Date/CUD not accepted. Stale vision
`3af923d562b546d597e9f156b5c3227f` not accepted.

Tests:

- Offline `BILLY_TEST_MODE=commit` `test.sh`: 1867 passed, 60
  deselected. Lint passed. Coverage still `complete: false`.
- Live CUD still fails on persist. No bill POST.

Cleanup:

- No bill persisted. Footer create may have made a supplier. Live
  test `finally` calls `ui_clients_delete_*` for the tag. Prove
  absence on the next live run.

Next unresolved coverage slice:

- Persist `POST /v2/bills` after the proved pre-submit dump. Then
  update/delete, independent read-back, third-session bill and
  supplier absence. Stay red. No remap.

