# 198.33 classify daybook create persist chrome

## Pivot

`A337A622` stops product list, dialog, and archive probes.
Product persist stays fail-closed. `67CBACB6` is unanswered.
`113F1E05` closed invoice entry-CTA dumps.
`unique_normal_action=false`. Invoice CUD stays unbound.
`FD39FFE7` still makes those two mandatory and red. Do not
freeze them as done. Do not remake those dumps.

Files stay fail-closed on existing evidence: research185 Bilag
**Slet** count 0, no singular `DELETE /v2/files/:id`. Do not add
a Bilag helper. Do not click **Upload filer**.

`A337A622` then requires the next non-blocked owner-scope write.
That family is daybooks. Create chrome exists on
`/:org_slug/daybooks/new`. Delete chrome is **Mere** then menu
**Slet**. Bare `/daybooks` is Upsedasse and is not a read-back
path.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official persist of a **posting** is **Godkend** / **Godkend
alle**. That is not `daybooks.create`. The current
`_ledger_write` FamilyWrite is a guess: it fills `name`, clicks
**Opret ny kassekladde**, clicks primary **Slet**, and reads
back `daybooks`. Do not arm it.

## Goal

Open the existing daybook create surface through `ui_daybooks_open`
and classify persist chrome. Read-only. TDD first. Do not create
a journal. Do not post. Do not click **Godkend**.

Persist **only**:

- `path_class` (`daybooks_new` / `daybooks_id` / `other`)
- `heading_token` (`none` / `other`)
- `opret_ny_kassekladde_count` (int)
- `indstillinger_count` (int)
- `name_input_count` (int)
- `gem_count` (int)
- `godkend_count` (int)
- `godkend_alle_count` (int)
- `bogfor_count` (int)
- `tilfoej_count` (int)
- `mere_count` (int)
- `unique_persist_token` (`opret_ny_kassekladde` /
  `indstillinger` / `none`)
- `proved_bind` (`none`)
- `missing_keys`

`unique_persist_token` is the one named persist count that equals
1. Named persist counts are `opret_ny_kassekladde_count` and
`indstillinger_count` only. **Godkend**, **Godkend alle**,
**Bogfør**, **Tilføj**, **Gem**, and **Mere** never count as
`daybooks.create` persist. If both persist counts are 1, or
neither is 1, the token is `none`. `proved_bind` stays `none`.
Empty h1 is allowed (`heading_token=none`).

Prefer count-only. Do not click **Opret ny kassekladde** unless
the failing fixture first requires a classification that cannot
be done without it **and** the URL already proves a restorable
`:id`. Default is no click.

Do not click **Godkend**, **Godkend alle**, **Bogfør**, **Tilføj
kassekladdelinje**, **Gem**, or confirm **Slet**. Do not open
**Indstillinger** unless a later slice proves it is the unique
name control. Do not arm `_ledger_write`. Do not change
`LEDGER_SUBMIT_BLOCKER` into a live submit.

Authority: `96908DC6`, `A337A622`, `FD39FFE7`, `113F1E05`,
`728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock
stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping `ui.parity.daybooks.create` or
  `ui.parity.daybooks.delete` to preview tools
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or daybook
  `parity_status`
- Arming `src/billy_mcp/ui_writes/ledger.py` `_ledger_write`
- Calling `ui_daybooks_create_execute` or
  `ui_daybooks_delete_execute` as a live submit
- Clicking **Godkend**, **Bogfør**, or **Tilføj kassekladdelinje**
- Seeding with `POST /v2/daybooks` or any live API token
- Using bare `/daybooks` as read-back
- Daybook line create or Posteringer create
- Files upload, invoice picker, product dumps
- Remaking `ui_daybooks_open`, `ui_daybooks_get_open`, or
  `ui_daybooks_delete_open`
- Picture upload, force clicks, `evaluate` clicks, guessed
  selectors
- Persisting source, locators, URLs, slugs, names, or screenshots
- Live API token calls, docs re-lock, bulk92, residual29
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `6180222`. Not a backstop. Memory `state.md` is the only
dirty file (HEAD hash fold). `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations CUD rows already name
preview tools. Invoice CUD stays `ui_invoices_*_open` and
unbound. Product create stays `ui_products_create_open` and
fail-closed. Daybook preview/execute already registered.
Manifest still points honesty rows at `ui_daybooks_open` and
`ui_daybooks_delete_open`. Default ledger execute returns
`submitted=false`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

`ui_daybooks_open` already opens `/:org_slug/daybooks/new` and
rejects bare `/daybooks`. It never clicks create, add-line, or
post. research157/179/180 classified editor markers, get-open,
and Mere/Slet. They did not classify a unique persist token for
`daybooks.create`.

## Delegation

No children. Root owns this slice. `728BD2E4` child-split
already landed the seven write-family modules. Those children
stay merged and parked. Unlimited budget is not a reason to
spawn. A new helper is justified only because persist chrome is
not in the open-only shells; if independent review finds this
dump restates research157 markers with no new persist token
rule, drop the helper and keep the wiki fact, same as 198.32.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/ledger.py` (ticket pair; no arm)
- `src/billy_mcp/browser.py` `ui_daybooks_open`
- `tests/live/test_ui_daybooks_open.py`
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_ledger_writes.md`
- `wiki/ui_daybooks_open_shell.md`
- `src/billy_mcp/ui_writes/products_form_contract.py` (key-set
  and dump shape only)

New files:

- `src/billy_mcp/ui_writes/daybooks_create_contract.py`
- `tests/unit/test_ui_daybooks_create_contract.py`
- `tests/live/test_ui_daybooks_create_contract.py`

Owner dump:

`~/.local/share/billy-mcp/inspect-live-daybooks-create-contract.json`

Closed dumps the live test must not rewrite:

- `~/.local/share/billy-mcp/inspect-live-products-form-contract.json`
- `~/.local/share/billy-mcp/inspect-live-products-delete-chrome.json`
- `~/.local/share/billy-mcp/inspect-live-products-archive-list.json`
- `~/.local/share/billy-mcp/inspect-live-inventory-archive-list.json`

## Design choices

1. **P10 wins.** Do not remake product or invoice dumps. Do not
   remake daybook open-only shells.
2. **TDD first.** Honesty-16 stays 16 and red. First product edit
   is a failing unit fixture against the key set above.
3. **Official labels only.** Count exact **Opret ny kassekladde**,
   **Indstillinger**, **Gem**, **Godkend**, **Godkend alle**,
   **Bogfør**, **Tilføj kassekladdelinje**, **Mere**. No
   substring. No body sweep.
4. **Read-only.** Open `/:org_slug/daybooks/new` through the same
   path `ui_daybooks_open` uses. Classify. Fail closed on
   Upsedasse or login.
5. **Persist stays fail-closed.** A unique persist token is later
   execute evidence, not permission to submit this slice.
6. **No remap** until live FastMCP create plus Mere/Slet delete
   plus independent accept plus purge.
7. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red). Assert
`ui.parity.daybooks.create` still names `ui_daybooks_open`.

Add `REQUIRED_DAYBOOKS_CREATE_CONTRACT_KEYS` and `missing_keys`
in `daybooks_create_contract.py` only after a unit fixture fails
because those names do not exist, or add the fixture first
against a local helper stub. Preferred shape:

- Fixture feeds `{}`.
- Helper returns the missing key list from this plan.
- Current empty dump must fail with that exact set.

Also unit-test the persist rule: unique only when exactly one of
`opret_ny_kassekladde_count` and `indstillinger_count` is 1.
Unit-test that `godkend_count=1` does not become
`unique_persist_token`. Do not edit
`generate_coverage_report.py` or remap coverage. Assert
`LEDGER_SUBMIT_BLOCKER` still refuses mutation.

### 2. Smallest helper, then one live inspect

- Implement the helper in `daybooks_create_contract.py`.
- Sanitizers: allowlisted tokens only. Unknown path becomes
  `other`. Never persist source, locators, URLs, or names.
- Live test: READY via existing credential refs. No
  `BILLY_API_TOKEN`. Open `/:org_slug/daybooks/new` only.
  Classify. Write the dump outside git.
- Live test verifies the dump keys without repeating a click if
  the dump already exists from this EXECUTE. Prefer one capture.
- If a delivered dump has every required key, do not recapture.
- Snapshot the four closed dumps and assert they are
  byte-unchanged.
- Do not call `ui_daybooks_create_execute`.
- Create nothing. Fresh-session check: no tagged
  `MCP-LEDGER-` name. `proved_bind` must be `none`.

### 3. Stay red

Do not finish. Do not claim complete. If a unique persist token
exists, record it and keep execute fail-closed. If none exists,
record `unique_persist_token=none`. Either way, do not treat
this dump as a finished daybook create.

## Success criteria

- Honesty-16 still length 16 and red
- Failing dump-key fixture existed, then passed
- Live dump has every required key
- `proved_bind` is `none`
- Closed product and inventory dumps are byte-unchanged
- No **Godkend** / **Bogfør** / **Tilføj** click
- `LEDGER_SUBMIT_BLOCKER` still refuses mutation
- `ui.parity.daybooks.create` still names `ui_daybooks_open`
- No credentials, tokens, screenshots, frames, HAR, traces, or
  daybook names in git
- Branch stays red. Do not finish.

## Post-Mortem

Work completed: TDD dump-key fixture failed on missing helper, then
passed. Live inspect delivered
`inspect-live-daybooks-create-contract.json` outside git.
`path_class=daybooks_new`. Both named persist counts are 1, so
`unique_persist_token=none`. No **Godkend** click. No journal
created. Honesty-16 stayed red. Ledger execute still refuses
mutation.

Deviations: independent review (`07E53819`) failed the helper as a
redundant permanent diagnostic. research157 already recorded
**Opret ny kassekladde** on `/daybooks/new`. Page-wide
**Indstillinger** matches nav, so `unique_persist_token=none` is
not a unique persist path. FIX-VERIFY deleted
`daybooks_create_contract.py` and its unit/live tests. Wiki keeps
the dump fact.

Review findings: dump contract met. Helper dropped. Do not remake.
Do not remap. Do not arm `_ledger_write`. Stay red.

Cleanup: no journal created. Closed dumps unchanged. Helper files
gone. Ephemeral profile removed. No frames, HAR, or credentials
in git. Lint passed. Commit-mode suite 2063 passed / 82 deselected.

Next unresolved coverage slice: invoice draft CUD still unbound
(`FD39FFE7`, `113F1E05`). Product create stays fail-closed pending
`67CBACB6`. Files stay fail-closed. Daybook persist is not unique.
Do not add another daybook classify helper. Stay red.

