# 2.7 Wave 4 remaining clear API reads

## Objective

Use the fresh cited Grok freeze in
`wiki/wave_four_remaining_clear_reads_contract.md` and
`.fractal/main.billy_complete/tmp/grok-research.md` to offline-implement every
remaining clear get/list operation: 50 typed `api_*` tools across 25 resources.
This raises only `implemented` and `contract_tested` for those rows; all live,
UI, vision, write, bulk, special-route, and completeness states remain red.

## Baseline and boundaries

- The official-doc fingerprint is unchanged: ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes.
- Preserve the current cited research artifacts and root memory/wiki updates.
  Restore the session-managed `codex-power.err` before staging; retain the
  staged removal of the independent-review child's generated seed.
- Verify `git diff --check`, `wiki lint`, formatting, lint, types, non-live
  tests, coverage generation, repository-policy checks, and credential scan;
  then make a baseline `fractal commit` before any child is initialised. This
  gives every implementation leaf the same frozen contract.
- Integrate the completed wiki-only `wave3_envelope_rereview` report at the
  next preparation/integration opportunity. It confirms that the Wave-3 fixes
  already on root preserve flat FastMCP schemas and `meta.paging`.
- Do not add resource filters beyond the explicitly live-proven, non-empty
  `countryId` required on cities/states/zipcodes lists. Reject `offset`,
  invented parent IDs, unknown fields, and nested `request` input wrappers.

## Delegation decision

This is a five-way, file-disjoint implementation slice with material testing
work in each partition. The fresh Grok brief already satisfies the
research-before-Codex gate. Use the three available child slots first while
root owns shared registration, coverage, and redaction; launch the remaining
two leaves immediately as slots free. Every leaf uses `--agent=codex-power`,
has depth/children/descendants zero, and owns only the named module and focused
test file.

### First cohort

| Child | Tools | Exclusive files |
| --- | --- | --- |
| `wave4_geo_reads` | countryGroups, cities, states, zipcodes (8) | `src/billy_mcp/api/geo_reads.py`; `tests/api/test_geo_reads.py` |
| `wave4_tax_reads` | taxRates, taxRateDeductionComponents, salesTaxRulesets, salesTaxRules, salesTaxAccounts, salesTaxMetaFields, salesTaxReturns, salesTaxPayments (16) | `src/billy_mcp/api/tax_reads.py`; `tests/api/test_tax_reads.py` |
| `wave4_bank_reads` | bankPayments, bankLineMatches, bankLines, bankLineSubjectAssociations, balanceModifiers (10) | `src/billy_mcp/api/bank_reads.py`; `tests/api/test_bank_reads.py` |

### Second cohort

| Child | Tools | Exclusive files |
| --- | --- | --- |
| `wave4_balance_invoice_ext_reads` | contactBalancePayments, contactBalancePostings, invoiceLateFees, invoiceReminders, invoiceReminderAssociations (10) | `src/billy_mcp/api/balance_invoice_ext_reads.py`; `tests/api/test_balance_invoice_ext_reads.py` |
| `wave4_ledger_user_reads` | transactions, postings, users (6) | `src/billy_mcp/api/ledger_user_reads.py`; `tests/api/test_ledger_user_reads.py` |

Each leaf must implement flat Pydantic get/list inputs, opaque response models,
encoded paths, allowed query serialization, optional `meta.paging`, typed 401
mapping, unknown-key rejection, direct FastMCP registration, and focused
contract tests. Geo must additionally test the required `countryId` validation
and no other invented filters. No leaf may edit the server, coverage generator,
generated coverage files, redaction, or wiki.

After the baseline commit, initialise the first cohort sequentially, author
their `NODE.md` ownership/contract boundaries, commit each seed from its child
worktree, then start each branch:

```bash
fractal node init wave4_geo_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/geo_reads.py --scope=tests/api/test_geo_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Geo Reads'
fractal node init wave4_tax_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/tax_reads.py --scope=tests/api/test_tax_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Tax Reads'
fractal node init wave4_bank_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/bank_reads.py --scope=tests/api/test_bank_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Bank Reads'
```

When a first-cohort slot closes, initialise the corresponding second-cohort
leaf with the same base, agent, inheritance, and leaf limits:

```bash
fractal node init wave4_balance_invoice_ext_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/balance_invoice_ext_reads.py --scope=tests/api/test_balance_invoice_ext_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Balance and Invoice Extension Reads'
fractal node init wave4_ledger_user_reads --path=/Volumes/ssd_1/Repositories/billy-mcp --base=main.billy_complete --agent=codex-power --inherit=all --scope=src/billy_mcp/api/ledger_user_reads.py --scope=tests/api/test_ledger_user_reads.py --max-depth=0 --max-children=0 --max-descendants=0 --title='Wave 4 Ledger and User Reads'
```

## Root-owned integration

1. Validate each child from a clean archive before merging; merge only a real,
   passing module/test delivery with `git merge --no-ff -m "merge <child>"`.
2. Add the five registration hooks to `src/billy_mcp/server.py`; extend the
   registry test to exactly 94 `api_*` plus two `coverage_*` tools.
3. Add only the 50 cited IDs to `OFFLINE_API_IMPLEMENTATION_EVIDENCE`, include
   the new geo `countryId` metadata in the generated inventory, regenerate
   coverage, and confirm 94 offline implemented/contract-tested API rows with
   zero live/UI/vision advancement and `complete: false`.
4. Extend redaction regression coverage for user email/phone, reminder
   `downloadUrl` query tokens, and included bank identifiers without changing
   successful tool payloads.
5. Run the complete non-production/offline suite and the deliberately failing
   full-mode completeness gate. Request an independent Grok review of the
   committed integration, then repair and re-review every finding.

## Acceptance criteria for this slice

- All 50 tools are real typed reads and have focused path/query/root/paging,
  validation, and typed-error contract tests.
- `api_cities_list`, `api_states_list`, and `api_zipcodes_list` require a
  non-empty `countryId`; no other Wave-4 list gains an invented filter.
- The generated coverage state is fail-closed: 94 API rows are offline green,
  `live_tested_rows` remains zero, and `complete` remains false.
- The documentation and review records make no product-completeness claim.
