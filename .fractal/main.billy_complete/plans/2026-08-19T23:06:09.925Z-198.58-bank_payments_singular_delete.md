# 198.58 bankPayments ticketed singular delete

## Pivot

`B54A6BFC` is binding under `9B979A05`. HEAD `cc43700` is a clean
198.57 natures close, not a `(failed on <step>)` backstop. Working
tree is clean. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). `#v2bankpayments` Supports:
`get by id, list, create, update, bulk save, delete, bulk delete`.
Singular delete is listed. No delete body table. Request field is
`id` only. Response map stays `changed_records[]` plus
`meta.deletedRecords`.

Unauth 405 must not override that table. Wave-5k freeze that left
delete out on 405 is superseded.

Do not spawn. Do not remake UI dumps. Do not invent a delete JSON
root, `isVoided` substitute, bulk tools, or
`invoiceReminderAssociations` tools. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.bankPayments.delete`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.bankPayments.delete` | `api_bank_payments_delete_preview` / `_execute` | `DELETE /bankPayments/:id` | `{id}` only |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 22 method-closed + 2 readonly
map + 2 meta delete = 26. Bulk 92 stay red. Implemented 526,
contract 531 (HEAD 525 / 530). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/api/bank_payment_writes.py`,
`src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds DELETE with no JSON body.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. Extend `tests/api/test_bank_payment_writes.py`:
   - Register count becomes 6 (was 4). New preview fields:
     `api_bank_payments_delete_preview` `{id}`,
     execute `{confirmation_ticket}`.
   - Outer extras and empty `id` fail `ValidationError`.
   - Preview delete sends no HTTP. Canonical request is
     `{"id": <id>}`. Expected effect
     `{action: delete, resource: bankPayment, id: <id>}`.
   - Execute DELETEs
     `https://api.billysbilling.com/v2/bankPayments/<urlencoded id>`
     with no JSON body. Encoded id `payment /?` becomes
     `/v2/bankPayments/payment%20%2F%3F`.
   - Success fixture:
     `{"meta": {"deletedRecords": {"bankPayments": ["payment /?"]}}}`.
   - Tamper, replay, expiry, wrong execute-tool, and missing
     auth fail closed. Preview still makes no network.
2. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 22`, residual `== 26`.
   `api.bankPayments.delete` is **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. After generator regenerate,
   `implemented_rows == 526` and `contract_tested_rows == 531`.
   `complete` stays false. Live/vision counts stay 339.
3. Map `api.bankPayments.delete` in `OPERATION_TEST_REFERENCES`
   to `tests/api/test_bank_payment_writes.py` plus
   `SERVER_REGISTRY_TEST_REFERENCE`. On HEAD `tool_name` is empty
   and the row is method-closed; the new mapping should fail
   until honesty lets the preview name through.

Run:
`uv run pytest tests/api/test_bank_payment_writes.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/bank_payment_writes.py`:

- Add `BankPaymentDeletePreviewInput` (`id` min_length 1,
  extra=forbid).
- Extend `_bank_payment_specification` so DELETE is a valid
  method (today the action map is POST/PUT only). Payload is
  `None`. Resource id is required.
- Register `api_bank_payments_delete_preview` and
  `api_bank_payments_delete_execute`. Reuse
  `WriteProtocolService`. Do not add a second protocol.
- Do not send `bankPayment` on DELETE.

`src/billy_mcp/server.py`: already calls
`register_bank_payment_write_tools`. Do not add a second register.

`scripts/generate_coverage_report.py`:

- Remove `api.bankPayments.delete` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for that id to
  `tests/api/test_bank_payment_writes.py` plus
  `SERVER_REGISTRY_TEST_REFERENCE`.
- Do not touch bulk helpers, the other 22 method-closed ids,
  readonly map, or meta delete.
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 22. `api.bankPayments.delete` is a
  ticketed offline tool. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports singular delete
  with path `id` and no body is enough to ship the ticketed
  DELETE. Unauth 405 is not the contract. The writable-field-map
  clause stays for create/update bodies only.

Keep `wiki/wave_fivek_ticketed_writes_contract.md` as history.
Do not rewrite it as if delete was in Wave-5k.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.bankPayments.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- UI `bankPayments.delete` parity stays `not_applicable`
  (research183).
- Do not use `isVoided` as this row.
- `invoiceReminderAssociations` create/update wait until after
  this COMMIT.
- `complete` stays false.

## Independent review

After EXECUTE, review must re-parse `#v2bankpayments` on the
locked HTML and confirm:

1. Supports still lists singular delete.
2. Tools exist only as preview plus execute for that one op.
3. Inventory `tool_name` is `api_bank_payments_delete_preview`.
4. Execute path is `DELETE /bankPayments/:id` with no body.
5. `live_tested` is false.
6. Remaining residual honesty is 26 toolless rows.
7. No 405-based green. No invented delete body. No bulk tools.

## Post-Mortem

Work completed: ticketed offline `api.bankPayments.delete` in
`src/billy_mcp/api/bank_payment_writes.py`. Preview tool
`api_bank_payments_delete_preview` takes a non-empty `id` with
`extra=forbid`. Execute binds `DELETE /bankPayments/:id` with no
JSON body. Shared write protocol reused. Coverage regenerated:
implemented 526, contract 531, live/vision 339, complete false.

Deviations: none versus the plan. `_bank_payment_specification`
maps DELETE through the existing method dict instead of
`assert_never`, because Pyright treats that arm as unreachable
on `WriteMethod`.

Review findings: independent review PASS. Required fixes none.
Follow-up only: `live_probe.py` still lists this id in the
historical unauth 405 matrix. That is not the contract. Inventory
qualification is `live_api_deferred`. Rejected as a fail.

Test results: focused bank-payment write and inventory tests
passed. Default `test.sh` (commit mode, not live, not vision):
2158 passed, 83 deselected. `lint.sh` passed. Wiki lint clean on
project wiki and memory. `BILLY_TEST_MODE=full` was not run
because completion requirements are still false.

Cleanup state: no live records. No frames. No credentials.

Next unresolved coverage slice: ticketed
`invoiceReminderAssociations` create/update, then remaining
geo/country-group writes, readonly transaction writes, and meta
deletes. Residual honesty 26. Bulk 92. `complete` stays false.
Do not finish.

