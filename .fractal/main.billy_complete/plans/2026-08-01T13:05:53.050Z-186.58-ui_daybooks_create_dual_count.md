# 186.58 ui daybooks create dual-count

## Goal

Dual-count green **`ui.parity.daybooks.create`** by mapping the already qualified
read-only **`ui_daybooks_open`** Kassekladde editor shell to
**`api.daybooks.create`**, using a **`form_open_only`** contract on the same
empty-input path `/:org_slug/daybooks/new` already live for discovery + list.

No new browser workflow. No new API/UI tools. No get/update/delete/bulk greening.
Keep **`ui.parity.daybooks.list`** green from 186.57. No nested
daybookTransactions*/daybookTransactionLines*/daybookBalanceAccounts greening.
No invoice_email product. No annual product. No bulk external-contract greening.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research157;
also worktree `tmp/grok-research.md`).  
Dual evidence (tmp, non-git):
`.fractal/main.billy_complete/tmp/research157_residual_dual.json` (+ worktree
`tmp/` copy).  
Discovery + list already green: `ui_daybooks_open` (research117 / 156 / 186.57);
wiki `wiki/ui_daybooks_open_shell.md`.  
Live evidence already on tip: `tests/live/test_ui_daybooks_open.py`, vision
record class `tmp/vision-records/ui_daybooks_open.json`.  
API offline create already green: `api.daybooks.create` → daybook writes
(`live_tested: false`, `out_of_scope_by_user`).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged research103–157).  
Prior create dual-count precedents: `invoices.create` / `bills.create` via
`parity_of_api_create=True` → `form_open_only` in
`scripts/generate_coverage_report.py`. Daybooks reuses the **same** editor tool
as list (not a separate create tool).

## Why this slice (research157)

1. Dual headless sessions research157:
   - Typed `ui_daybooks_open` dual READY + success
     (`path_class=/:org_slug/daybooks/new`, `editor_markers_present=true`,
     `shell_markers_present=true`).
   - Soft route `/daybooks/new` dual non-empty (body_len class **318**); prefix
     includes “Opret ny kassekladde” / “Tilføj kassekladdelinje” / “Ingen
     postering valgt”.
   - Bare `/daybooks` dual h1 **Upsedasse!** (error shell — not success).
   - Soft `emails`/`files`/`postings`/`bankLines`/`daybookTransactions` dual
     soft-empty body_len **127** (= nonsense).
   - Invoice detail links **0** dual; disposable client create failed
     (chrome-only `/clients/new`); draft stayed on `/invoices/new`;
     invoice_email deferred.
   - No API token; no Send email; ephemeral profiles purged; no residual markers.
2. Create is the stronger semantic for `/daybooks/new` (create CTAs). List stays
   greened with `shell_open_only` (bare index Upsedasse; editor is Billy daybook
   surface). Both rows share one tool with different `parity_status`.
3. get/update/delete/bulk UI parity remain red (no dual-proved detail/write
   isolation; bulk external-contract on API lane).
4. One new green UI row only; no new tool surface.
5. Explicitly deferred by research157: special.invoice_email dual/NA;
   contacts/products create tools; daybookTransactions*; postings steal;
   suppliers steal contacts.

## Non-goals

- Inventing a second daybooks UI tool or `api_kassekladde_*`.
- Greening `ui.parity.daybooks.get|update|delete|bulk_save|bulk_delete`.
- Reverting or weakening `ui.parity.daybooks.list`.
- Greening nested daybookTransactions / daybookTransactionLines /
  daybookBalanceAccounts parity rows.
- Greening `ui.parity.special.invoice_email` or inventing weak NA.
- Greening `ui.discovery.annual_reports` or bulk 92 API rows.
- Dual-counting files.*, attachments.*, postings onto transactions.
- Live API traffic / `BILLY_API_TOKEN`.
- Expanding browser egress; headed browser; children.
- Setting `complete: true` or node finish.
- Re-running full live UI suite unless IR demands it (reuse research117 live
  qualification + research157 dual proof).

## Ownership

**Root product. No children** (generator + inventory tests + wiki note; no
browser/server model changes expected). Node instructions: Grok owns product on
this root. Independent review remains Grok.

| Area | Paths |
| --- | --- |
| Coverage generator | `scripts/generate_coverage_report.py` — extend
  `apply_ui_daybooks_open_shell_evidence` with
  `parity_of_api_create: bool = False` (alongside existing
  `parity_of_api_list`); mutual exclusivity per call (list OR create OR
  discovery defaults); when create True: keep `api_row_id` from parity builder
  (`api.daybooks.create`), append research157 evidence mapping, set
  `parity_status=form_open_only`, sensitivity medium (open-only create form
  class, mirror invoices/bills create); clear `api_row_id` only when **neither**
  list nor create flag; revise base evidence so “does not green … create …” is
  accurate for discovery/list paths and create path notes residual get/update/
  delete/bulk only; wire
  `if api_row["id"] == "api.daybooks.create": apply_...(parity_of_api_create=True)`
  in the parity builder loop next to daybooks.list |
| Coverage inventory tests | `tests/coverage/test_coverage_inventory.py` — bump
  greened shells/parity **51→52**; live/vision **151→152**;
  implemented/contract **335→336** where hard-coded; map
  `ui.parity.daybooks.create` → `ui_daybooks_open`; assert create green
  (`api_row_id=api.daybooks.create`, `form_open_only`, research157 in evidence);
  assert list still green; residual daybooks.get/update/delete/bulk red;
  invoice_email still red; nested daybook* residual red |
| Wiki | `wiki/ui_daybooks_open_shell.md` (+ `_index` desc if needed) — dual-count
  note for **list + create**; keep bans for get/update/delete/bulk and nested
  families; cite research157 for create |
| Coverage artifacts | regenerate `coverage/status.json` + `coverage/report.md` via
  existing generator main path |
| Live / vision | no new live test required if inventory reuses existing
  daybooks open live refs; optional evidence string refresh only |

## Implementation steps (EXECUTE)

1. Generator dual-count flag + exact-id wiring for `api.daybooks.create` only;
   preserve list dual-count.
2. Inventory test count/assert updates (51→52 shells; 151→152 live/vision).
3. Wiki dual-count note for create; discovery evidence still honest.
4. Regenerate coverage status/report.
5. Run lint + offline `test.sh` (or inventory-focused suite) before IR handoff.
6. Do **not** mark complete true; do **not** node finish.

## Verification

| Gate | Expectation |
| --- | --- |
| lint.sh | pass |
| offline test.sh | inventory + non-live suite green (count deltas only) |
| `ui.parity.daybooks.create` | five flags true; tool `ui_daybooks_open`;
  `form_open_only`; evidence cites research157 |
| `ui.parity.daybooks.list` | still green; `shell_open_only` |
| residual daybooks.* | get/update/delete/bulk stay red |
| `ui.parity.special.invoice_email` | still red |
| bulk 92 | still external-contract red |
| annual_reports | still org_inaccessible red |
| API live_tested | all false + out_of_scope_by_user |
| complete | false |
| live/vision | **152** |
| implemented/contract | **336** |

## Independent review focus

- Confirm research157 dual proves daybooks tool + create CTAs on `/daybooks/new`
  + bare `/daybooks` Upsedasse rejection.
- Confirm only `.create` is newly greened; list stays; get/update/delete/bulk stay
  red.
- Confirm `form_open_only` honesty (create surface; never submits; CTA ban).
- Confirm live/vision 152 math: 52 shells + 100 GEO NA.
- Confirm no new UI tools; no live API; no credentials in wiki/coverage.
- Confirm invoice_email isolation deferred correctly.

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects create dual-count on same tool as list | invoices/bills use separate create tools; here path is literally `/new` with create CTAs; form_open_only + CTA ban; list keeps shell_open_only |
| Accidentally greening all daybooks ops | Explicit per-op id gates `api.daybooks.list` and `api.daybooks.create` only |
| Count drift (51→52; 151→152; 335→336) | Update every hard-coded assertion in inventory tests |
| Wiki still claims “does not green create” | Edit dual-count note carefully; ban only residual ops |
| api_row_id cleared when create flag set | Fix clear condition: clear only if not list and not create |

## Done definition (this plan only)

- Generator + tests + wiki + regenerated coverage ready for IR.
- live/vision **152**; complete **false**.
- Ready independent review of dual-count package 186.58.
- Not node finish.

## Post-Mortem

### Delivered

- Dual-count exact `api.daybooks.create` → existing `ui_daybooks_open`
  (`form_open_only`; research157 / plan 186.58).
- Generator `parity_of_api_create` flag + mutual-exclusion with list flag +
  parity-builder exact-id wiring.
- Inventory tests: qualified shells 51→52; live/vision 151→152; daybooks.create
  green asserts; daybooks.list still green; residual get/update/delete/bulk red.
- Wiki dual-count note on `ui_daybooks_open_shell` + `_index` desc.
- Coverage regenerated: live/vision **152**, implemented/contract **336**,
  complete **false**.
- IR **ACCEPT** (`tmp/grok-review.md`): no required fixes; optional N1–N2 deferred.
- FIX-VERIFY: lint.sh pass; wiki lint memory+project clean; offline test.sh
  **1586 passed** / 38 deselected; daybooks.create green; invoice_email + other
  daybooks residual red; bulk 92 external-contract; annual stay red.
- No new UI/API tools; no src browser/server changes.

### Deferred

- Optional N1: vision record `workflow_ref` also name `ui.parity.daybooks.create`
  on next live reconfirm.
- Optional N2: base evidence lead-in still says “shell open only” before create
  append (cosmetic).
- special.invoice_email (client create chrome-only; draft failed; Levering blocks weak NA).
- daybooks get/update/delete/bulk; nested daybookTransactions* families.
- contacts/products create form tools.
- bulk 92 external-contract; annual_reports org_inaccessible.
- API live remains out_of_scope_by_user.

### Notes

- Prefer residual dual-count/NA next: special.invoice_email only with disposable
  client form dual + draft dual; else contacts/products create-open tools; no
  weak NA. Complete false; do not node finish.

### Test results

| Gate | Result |
| --- | --- |
| lint.sh | pass |
| wiki lint (memory + project) | clean |
| offline test.sh | 1586 passed, 38 deselected |
| coverage | live/vision 152; contract/impl 336; complete false |

### Cleanup

- No daybook writes performed.
- Research157 client/draft left no disposable markers; profiles purged.
- Vision frames previously purge_verified true (node tmp).

