# 198.62 countries ticketed create and update

## Pivot

`4DE5EE18` is binding under `9B979A05`. HEAD `1b037a6` is a clean
198.61 countryGroups close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory only
(`state.md`, `todo.md`, `decisions.md`). Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-4de5ee18.html`,
`billy-api-docs-chunk-research-4de5ee18.js`,
`research-4de5ee18-countries-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the countryGroups lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2countries`.
Supports: `get by id, list, create, update, bulk save, bulk
delete`. No singular delete. Property table is exactly six rows
with empty notes: `name` string, `hasStates` boolean,
`hasFiniteStates` boolean, `hasFiniteZipcodes` boolean, `icon`
string, `locale` belongs-to.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.countries.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_countries_*` or bulk tools. Do not fold this into
`reference_reads.py` or `geo_reads.py`. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.countries.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.countries.create` | `api_countries_create_preview` / `_execute` | `POST /countries` | nested optional `{name, hasStates, hasFiniteStates, hasFiniteZipcodes, icon, locale}` |
| `api.countries.update` | `api_countries_update_preview` / `_execute` | `PUT /countries/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 14 method-closed + 2 readonly
map + 2 meta-delete = 18. Bulk 92 stay red. Implemented 534,
contract 539 (HEAD 532 / 537). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload copy shape is
`src/billy_mcp/api/city_writes.py`. Country groups are the
just-landed twin in `src/billy_mcp/api/country_group_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_country_writes.py`, copied from country
   groups for the typed optional nested payload:
   - Register count is 4. Preview fields:
     `api_countries_create_preview` `{country}`,
     `api_countries_update_preview` `{id, country}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{name, hasStates, hasFiniteStates, hasFiniteZipcodes,
     icon, locale}`. JSON schema types: `name`, `icon`, and
     `locale` are string; the three `has*` fields are boolean.
     `additionalProperties` is false on outer and nested schemas.
     No `input` wrapper. Nested `required` is empty or absent.
   - Outer extras, empty update `id`, `localeId`, a JSON array for
     `locale` or `hasStates`, nested objects, and other inner
     extras fail `ValidationError` / FastMCP `extra_forbidden`.
     Execute extras fail. Preview still makes no HTTP.
   - Empty nested object `{"country": {}}` is valid. Partial
     payloads such as `{"name": "X"}` or `{"hasStates": true}` are
     valid. Do not require `name` or `locale`.
   - Preview create sends no HTTP. Canonical request is
     `{"country": { ...set keys only... }}`. Expected effect
     `{action: create, resource: country}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_country_write_tickets.py`, copied from
   country group ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/countries` with the
     exact nested JSON. Execute PUTs
     `/v2/countries/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/countries/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `countries` plus optional `meta.deletedRecords`. Do not
     expose a top-level `country` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 14`, residual `== 18`. Assert
   `api.countries.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk countries rows
   unspecified. Bump every hardcoded `implemented_rows == 532` /
   `contract_tested_rows == 537` in that file to **534 / 539**
   (seven pairs). `complete` stays false. Live/vision counts stay
   339.
4. Map both ids in `OPERATION_TEST_REFERENCES` to
   `tests/api/test_country_writes.py`,
   `tests/api/test_country_write_tickets.py`, plus
   `SERVER_REGISTRY_TEST_REFERENCE`, next to the existing
   `api.countries.get` / `.list` rows. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_countries_writes_are_ticketed_offline_and_not_live_tested`
   next to the country-groups inventory tests.

Run:
`uv run pytest tests/api/test_country_writes.py tests/api/test_country_write_tickets.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/country_writes.py` (new), copy
`city_writes.py` / `country_group_writes.py`:

- `CountryPayload`: `name: str | None = None`,
  `hasStates: bool | None = None`,
  `hasFiniteStates: bool | None = None`,
  `hasFiniteZipcodes: bool | None = None`,
  `icon: str | None = None`,
  `locale: str | None = None`, `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{country}`.
- Update preview input: `{id min_length=1, country}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/countries`. Singular root `country`.
  Plural root `countries`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_country_write_tools` next to
`register_country_group_write_tools`. Do not change reference
reads.

`scripts/generate_coverage_report.py`:

- Remove `api.countries.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids.
- Do not touch bulk helpers, the other 14 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` countries create/update true,
  delete false (`("countries", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 14. These two rows are ticketed
  offline tools. Residual honesty remaining 18. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional `name`, boolean `hasStates`,
  `hasFiniteStates`, `hasFiniteZipcodes`, string `icon`, and
  belongs-to `locale` as an opaque string id is enough to ship
  the ticketed POST/PUT. Reject `localeId`. Unauth 405 is not
  the contract. Empty payload is allowed.

Keep `wiki/ui_geo_cities_not_applicable.md` as history. Do not
remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.countries.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.countries.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research138).
- Do not accept `localeId` or a JSON object/array for `locale`.
- Do not require any nested field.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.

## Independent review

After EXECUTE, review must re-parse `#v2countries` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still six optional fields: `name` string,
   three booleans, `icon` string, `locale` belongs-to.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /countries` and `PUT /countries/:id`.
6. Nested payload rejects `localeId`, arrays, nested objects, and
   extra keys. Empty nested object is valid. Boolean schema types
   hold for the three `has*` fields.
7. `live_tested` is false.
8. Remaining residual honesty is 18 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.

## Post-Mortem

Work completed: ticketed offline `api.countries.create` and
`.update` in `src/billy_mcp/api/country_writes.py`. Preview
tools take a nested payload of optional string `name`, boolean
`hasStates`, `hasFiniteStates`, `hasFiniteZipcodes`, string
`icon`, and string `locale` with `extra=forbid`. Belongs-to
`locale` is an opaque string id. `localeId`, arrays, nested
objects, and extra keys fail at the FastMCP boundary. Empty
nested object is valid. Execute binds `POST /countries` and
`PUT /countries/:id` through the shared write protocol.
Coverage regenerated: implemented 534, contract 539, live/vision
339, complete false. Residual honesty remaining 18.

Deviations: none from the official field table. Ticket and
execute contract tests live in
`tests/api/test_country_write_tickets.py` so each test file
stays under the 250-line ceiling.

Review findings: independent Grok review PASS
(`.fractal/main.billy_complete/tmp/grok-review.md`). Required
fixes: none. Official `#v2countries` still lists create and
update, six optional fields, no singular delete.

Test results: lint passed. Commit-mode `test.sh` passed 2260
deselected-live/vision. Do not run `BILLY_TEST_MODE=full` or
`ui-full`; completeness is still false.

Cleanup: no live records, no frames. Next unresolved coverage
slice after COMMIT is saved `47B85E43` currencies create/update.
Do not finish.

