# 198.70 readonly-map honesty remap

## Pivot

`A485F530` is binding under `9B979A05`. HEAD `bb66a48` is a clean
transactions-delete close, not a `(failed on <step>)` backstop.
Uncommitted files are node memory only. Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md` (this
iteration). Official scratch fetch this slice:
`billy-api-docs-research-a485f530.html`,
`billy-api-docs-chunk-research-a485f530.js`,
`research-a485f530-contactBalancePostings-section.js`,
`research-a485f530-postings-section.js`,
`research-a485f530-transactions-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the transactions-delete lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`).

Supports still lists create and update on all three resources.
Every official property is readonly or immutable. First-party
bundles expose GET only. There is **no** writable request field
map. Do not ship tools. Do not invent an empty nested payload,
opaque dict, or guessed field from client models.

Unauth 405 on four rows must not stay as the contract
(`9B979A05`, wiki rule 3). Record the contradiction: Supports
lists create/update; the property table has no writable field.

Do not spawn. Do not remake UI dumps. Do not finish. No live API.

## Goal

Land one proved honesty cohort: remap four residual rows onto the
existing readonly-map freeze. Keep all six toolless and red.

| Inventory id | Current kind | After |
| --- | --- | --- |
| `api.contactBalancePostings.create` | `method_closed_offline` | `readonly_field_map_insufficient` |
| `api.contactBalancePostings.update` | `method_closed_offline` | `readonly_field_map_insufficient` |
| `api.postings.create` | `method_closed_offline` | `readonly_field_map_insufficient` |
| `api.postings.update` | `method_closed_offline` | `readonly_field_map_insufficient` |
| `api.transactions.create` | `readonly_field_map_insufficient` | unchanged kind |
| `api.transactions.update` | `readonly_field_map_insufficient` | unchanged kind |

`RESIDUAL_METHOD_CLOSED_IDS` becomes empty.
`RESIDUAL_READONLY_MAP_IDS` becomes those six ids.
`RESIDUAL_META_DELETE_IDS` stays empty. Residual honesty remaining
stays **6**.

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Bulk 92 stay red. Implemented 546, contract 551, live/vision
339 stay.

Drop `unauth_status` from the shared readonly qualification. The
historical gates differ (405 vs 401) and are not the contract.
Keep `research186` / `research191` / `research192` in evidence.
Append `research-a485f530-readonly-map` to `evidence_ref`.

## Why no child

Work is one generator freeze plus its tests and wiki. File
ownership would collide on
`scripts/generate_coverage_report.py`,
`coverage/api_v2_manifest.yaml`,
`tests/coverage/test_coverage_inventory.py`, and
`wiki/residual_clear_method_closed_inventory_honesty.md`. Seven
write children stay completed and merged. Leftover retired
descendants stay unmerged.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. In `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 0`, `len(readonly_map) == 6`. Assert
   the four former method-closed ids are in `readonly_map` and
   not in `method_closed`. Keep residual == 6, meta-delete empty,
   all six toolless, flags false, `tools_allowed=false`,
   `live_api=out_of_scope_by_user`. Counts stay 546 / 551 / 339,
   `complete is False`.
2. Same test: those four ids assert
   `kind=readonly_field_map_insufficient` and
   `blocker_code=READONLY_PROPERTY_TABLE`. Stop requiring
   `METHOD_NOT_ALLOWED` in their evidence.
3. Keep `research186` / `research191` / `research192` in
   evidence. Require `research-a485f530-readonly-map` in
   `qualification.evidence_ref`.
4. `tests/coverage/test_api_live_qualification_semantics.py`
   `test_residual_and_bulk_keep_existing_kinds` already follows
   the frozensets. After the count change it should still pass
   once the generator maps the four ids to readonly-map.

Run the honesty test. Expect fail on `len(method_closed) == 0`
(HEAD is 4) and on the four ids still `method_closed_offline`.
Capture that failure. Then change the generator.

## Implement

1. `scripts/generate_coverage_report.py`:
   - Empty `RESIDUAL_METHOD_CLOSED_IDS`.
   - Put the six create/update ids in `RESIDUAL_READONLY_MAP_IDS`.
   - Append `+research-a485f530-readonly-map` to
     `RESIDUAL_EVIDENCE_REF_CHAIN`.
   - Remove `unauth_status` from
     `readonly_field_map_qualification()`.
   - Rewrite the readonly evidence sentence so it names the
     official property table and the A485F530 exhaust, not a
     universal 401. Keep `research186` / `research191` /
     `research192` tokens the tests already require.
   - Leave `method_closed_offline_qualification` in place unused,
     or keep it for the empty set. Do not delete bulk freeze.
2. `uv run python scripts/generate_coverage_report.py`. Do not
   edit `coverage/status.json` or the API manifest by hand.
3. Wiki:
   - `wiki/residual_clear_method_closed_inventory_honesty.md`:
     remaining set is 6 readonly-map, 0 method-closed, 0
     meta-delete. Cite A485F530. No greening.
   - `wiki/offline_write_probe_rules.md`: `postings` and
     `contactBalancePostings` create/update stay red for
     `READONLY_PROPERTY_TABLE`, not for unauth 405.
4. Memory `state.md` / `todo.md` / `decisions.md`: A485F530
   honesty remap; no tools; `complete` false.

Out of scope this slice: any `api_*_create_*` / `api_*_update_*`
module, bulk92, UI, live API, OpenAPI re-probe, **Godkend**,
annual reports, coverage greening, `node finish`.

## Verify

Commit-mode coverage tests plus lint/type on the touched files.
No `ui-full`. No live API. Independent review after EXECUTE.

## Stop line

If a writable official field appears in review, stop and do not
remap. If tests force a 401 stamp onto the 405 rows, drop
`unauth_status` rather than lie. Keep `complete=false`.

## Post-Mortem

Work completed: honesty remap of six residual create/update rows
onto `readonly_field_map_insufficient` /
`READONLY_PROPERTY_TABLE`. `RESIDUAL_METHOD_CLOSED_IDS` is empty.
`RESIDUAL_READONLY_MAP_IDS` holds `contactBalancePostings` and
`postings` create/update plus `transactions` create/update. No
tools. No empty payload. `unauth_status` dropped from the shared
readonly qualification. Evidence ref appends
`research-a485f530-readonly-map`. Coverage regenerated:
implemented 546, contract 551, live/vision 339, complete false.
Residual honesty remaining 6, all readonly-map. Bulk 92 stay red.

Deviations: none from official property tables or wiki rule 3.
TDD first failed `len(method_closed) == 0` against HEAD 4. Did
not ship tools from Supports alone. Did not re-probe live API.

Independent review: PASS. Required fixes none. Docs lock still
ETag `tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`.
No production edits in FIX-VERIFY. Lint passed. Commit-mode
`test.sh` `2429 passed, 83 deselected`. No live UI records.
Advisory only: unused `method_closed_offline_qualification()`
left in place.

Next unresolved slice after COMMIT: parent `88C4B0A9` one residual
audit. If remaining red is bulk92 plus these six readonly-map
rows, do not repeat research and do not guess tools. `complete`
stays false.

