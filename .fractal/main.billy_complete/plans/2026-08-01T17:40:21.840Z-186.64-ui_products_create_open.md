# 186.64 ui products create open form_open discovery

## Goal

Product research163 ACCEPT:

1. New read-only FastMCP UI tool **`ui_products_create_open`** that opens the
   Billy **product create form** as form open only.
2. Entry path is **Lagermodul** (`/:org_slug/inventory`) + observe-only click
   **Opret produkt** (research163 dual). Catalog `/products` has no create CTA.
3. Green discovery row **`ui.discovery.products_create`** (new
   `UI_DISCOVERY_FAMILIES` entry `products_create`; do **not** repurpose
   `ui.discovery.products` list shell on `ui_products_list` or
   `ui.discovery.inventory` shell on `ui_inventory_open`).
4. Dual-count exact **`ui.parity.products.create`** / `api.products.create` only
   onto `ui_products_create_open` (`form_open_only`).

Target math (from tip `53d73bf` / product 186.63 productPrices dual-NA):
live/vision **164 → 166** (discovery + parity); implemented/contract
**348 → 350**; GEO NA **107** unchanged (productPrices package stays);
complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research163)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, recorded ETag `wcw4x9hqvu3603` unchanged. `POST /v2/products` requires name, accountId, salesTaxRulesetId; optional prices[] unitPrice/currencyId. |
| Catalog products list | `/products` dual: **no** Opret produkt; Mere → Eksportér/Importér only. Soft `/products/new` chrome-only body **190** (0 name inputs). |
| Inventory create dual | `/inventory` h1 Lagermodul; CTA **Opret produkt** opens form dual (`research163_focus_dual.json`): `name`, `account`, `salesTaxRuleset`, `unitPrice`, `currency`, `productNo`, `description`, Gem chrome; content fields **9**; `form_open_ready` **true** both sessions. Path stays inventory (in-place form). |
| API field match | Form names align with official product create + embedded price fields (not a separate productPrices shell — already NA research162). |
| Tool gap | `ui_products_list` list-only; `ui_inventory_open` shell-only and **must never** click Opret primo/produkt/status. |
| Isolation | Create form ≠ list shell ≠ import shell ≠ productPrices NA. Green only products.**create** parity, not get/update/delete/bulk. |
| Deferred | special.invoice_email (client Gem no persist dual); quotes/recurring create false opens; salesTaxRules soft-empty steal rejected; bulk 92 + annual red. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research163_focus_dual.json`.

## Non-goals

- Greening `products.get` / `update` / `delete` / bulk_* 
- Re-opening or changing productPrices dual-NA package
- Dual-counting create onto `ui_products_list` or `ui_inventory_open`
- Submitting the form (Gem / Opret submit / Save / Create) or file upload
- Clicking Opret primo / Opret status (inventory-only other creates)
- special.invoice_email, quotes.create, recurring.create, salesTaxRules isolation
- Live API traffic, headed browser, completeness, `node finish`
- Generic browser or HTTP tools
- Children / codex-power / non-grok agents

## Ownership

| Area | Owner |
| --- | --- |
| Browser tool + models + server register | Root Grok EXECUTE |
| Live dual-session test + vision record pattern | Root Grok EXECUTE |
| Coverage generator (discovery + dual-count create) | Root Grok EXECUTE |
| Inventory / unit / model tests | Root Grok EXECUTE |
| Wiki shell page + `_index` | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR per node seed) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research163)

### Tool

| Field | Value |
| --- | --- |
| tool_name | `ui_products_create_open` |
| lane | ui |
| kind | form_open_only (read-only observe) |
| shell_kind | `products_create` |
| success model | `UiProductsCreateOpenSuccess` (Pydantic) |
| error model | existing `ToolError` codes (`AUTH_REQUIRED`, `AUTH_INTERACTION_REQUIRED`, `UI_CHANGED` / project equivalent, `EGRESS_DENIED`, `BILLY_ERROR`) |
| API invent | none — no new `api_*` tools (api.products.create already offline implemented) |

### Success model fields (non-PII)

Mirror clients/suppliers create-open strictness (`extra=forbid`, no org_slug/url/selector secrets):

| Field | Contract |
| --- | --- |
| `path_class` | Literal `/:org_slug/inventory` (form opens **on inventory path**, not catalog `/products` or soft `/products/new`) |
| `heading` | Literal `Lagermodul` (inventory h1 retained while form is open) **or** documented literal that matches dual if form replaces h1 — verify in EXECUTE against dual body; prefer inventory h1 if still present |
| `shell_kind` | Literal `products_create` |
| `create_form_open` | bool — product create form surface present after CTA |
| `name_field_visible` | bool — `input[name=name]` visible |
| `account_field_present` | bool — `input[name=account]` (or equivalent) present |
| `sales_tax_ruleset_field_present` | bool — `input[name=salesTaxRuleset]` present |
| `unit_price_field_present` | bool — `input[name=unitPrice]` present (prices embed chrome; not productPrices resource tool) |
| `shell_markers_present` | bool — aggregate true when form signature holds |

If model field names need slight rename for style consistency with existing
Ui* models, keep the same boolean evidence set.

### Navigation (observe-only)

1. Headless Playwright via `BrowserRuntime`; egress from
   `coverage/browser_egress.yaml`; no `BILLY_API_TOKEN`.
2. Auth READY via existing login wait path.
3. **Required path** (research163 dual-proved): open
   `https://mit.billy.dk/{slug}/inventory`, settle, then observe-only click
   **Opret produkt** (prefer role=button / exact text; do **not** click
   Opret primo or Opret status).
4. Success when:
   - path class remains `/:org_slug/inventory` (reject soft `/products/new` alone
     as success; reject catalog `/products` without form)
   - form signature:
     - `name` field visible
     - at least two of `account` / `salesTaxRuleset` / `unitPrice` present
     - create form open true (body/form markers e.g. “Opret produkt”, Gem chrome optional)
   - shell_markers_present true
5. Close with Escape only after classify (no submit).
6. Never click Gem / submit Opret / Save / Create / file chooser / Opret primo /
   Opret status after form open; never fill and save disposable products in
   product path.

### Reject as success

| Failure | Code class |
| --- | --- |
| Soft `/products/new` chrome-only or catalog list without form | `UI_CHANGED` |
| Wrong CTA (primo/status) or inventory shell without form open | `UI_CHANGED` |
| Auth not READY / interaction required | existing auth codes |
| Egress violation | `EGRESS_DENIED` |

### Preserve existing inventory shell contract

`ui_inventory_open` stays shell_open_only:

- Still reports create CTA markers present **without** clicking them.
- Tests must continue to assert inventory open never opens product form as its
  success condition.
- New tool owns the Opret produkt click exclusively.

## Coverage generator contract

1. Add discovery family `products_create` to `UI_DISCOVERY_FAMILIES`.
2. Applicator `apply_ui_products_create_open_shell_evidence` (mirror
   clients/suppliers create open):
   - discovery row `ui.discovery.products_create`: all flags true; tool
     `ui_products_create_open`; parity_status `form_open_only`; evidence cites
     research163 + live test + vision record path.
   - parity row `ui.parity.products.create` only: same tool; form_open_only;
     dual-count `api.products.create` UI mapping.
3. **Do not** green products.get/update/delete/bulk_*.
4. **Do not** change productPrices NA applicator (research162).
5. **Do not** empty or reassign `ui_products_list` / `ui_inventory_open` /
   `ui_products_import`.
6. Live/unit test references on both greened rows.
7. After regenerate: live/vision **166**; implemented/contract **350**;
   complete **false**; bulk 92 red; annual red; API live_tested false
   (`out_of_scope_by_user`); residual products.get/update/delete +
   special.invoice_email red.

## Implementation checklist (EXECUTE)

1. **Models** — `UiProductsCreateOpenSuccess` in `src/billy_mcp/models.py` (+
   export/tests in `tests/test_models.py`).
2. **Browser** — `ui_products_create_open` on protocol + `BrowserRuntime`
   implementation; helpers for inventory product-create form signature; CTA
   click Opret produkt only; never submit.
3. **Server** — register tool on FastMCP server with typed return.
4. **Unit tests** — browser unit coverage for success path + reject soft
   `/products/new` + reject unopened inventory shell (pattern from
   suppliers/clients create).
5. **Live test** — `tests/live/test_ui_products_create_open.py`: dual ephemeral
   profiles, DOM assertions, second-session independent read-back, vision record
   under node tmp, purge frames, no API token.
6. **Coverage generator** + inventory tests asserting discovery green, create
   dual-count green, residuals red, live/vision math 164→166; list/inventory
   tools unchanged; productPrices NA intact.
7. **Wiki** — `wiki/ui_products_create_open_shell.md` (scrubbed) + index links
   from products list / inventory pages; note catalog has no create CTA.
8. **Regenerate** coverage manifests/status/report; run lint + offline test.sh.
9. Leave residual dual JSON / research brief in owner tmp only (not commit raw
   frames).

## Acceptance

- Tool registered; offline unit + inventory tests pass.
- Live dual test passes when credentials present (EXECUTE runs live suite path
  required by node seed before IR).
- Vision record non-sensitive + purge verified.
- Coverage: products_create discovery + products.create parity green; list +
  inventory shells unchanged; productPrices NA unchanged; complete=false; bulk
  92 red; annual red; API live false.
- Offline tests pass; wiki lint clean; no secrets/frames in repo.

## Safety rails

| Risk | Mitigation |
| --- | --- |
| Accidental product create submit | Never click Gem/submit; Escape after classify; live test asserts no disposable product marker |
| Breaking `ui_inventory_open` | Keep shell-only; separate tool; regression unit asserts inventory open does not require form open |
| Dual-count onto list tool | Generator exact `api.products.create` only; inventory assert list tool unchanged |
| Confuse unitPrice with productPrices resource | Comment + generator: unitPrice is create embed chrome; productPrices NA package untouched |
| Soft `/products/new` false success | Path must be inventory + form signature; soft new alone → UI_CHANGED |
| Catalog list false create | No success from `/products` without inventory form path |
| Live API leakage | Unset/forbid `BILLY_API_TOKEN` in live test; api_token_used false |
| Children / wrong agent | Root-only Grok; no spawn |


## Post-Mortem

- **Commit:** pending COMMIT step (product uncommitted at FIX-VERIFY).
- **live/vision:** 164 → **166**
- **implemented/contract:** 348 → **350**
- **Offline tests:** lint pass; `test.sh` **1604 passed** / 41 deselected (FIX-VERIFY reconfirm).
- **Live dual:** `tests/live/test_ui_products_create_open.py` passed in EXECUTE; vision record
  `tmp/vision-records/ui_products_create_open.json` purge_verified true.
- **IR:** ACCEPT (`tmp/grok-review.md`); required fixes **none**; optional N1–N3 deferred
  (signature ≥2 of 3 fields; vision re-click; plan/wiki/live ride commit).
- **Cleanup:** form_open only — no disposable product created; no org mutation.
- **Residuals still red:** special.invoice_email; products.get/update/delete/bulk_*;
  bulk 92 external-contract; annual_reports org_inaccessible; other api_parity
  discovery_required rows. API live_tested false (`out_of_scope_by_user`).
  complete **false**.
- **Next unresolved coverage slice:** residual UI parity — prefer special.invoice_email
  with durable disposable client save + draft dual; else products.get/update isolation;
  quotes/recurring create if form fields appear; nested salesTaxRules isolation; no
  postings/bankLines steal; bulk 92 and annual_reports stay red until evidence.

## Done when

- Plan producted in EXECUTE and IR ACCEPT (or required fixes landed).
- FIX-VERIFY: lint + offline tests green; coverage math 164→166 / 348→350;
  residual reds as above; complete false.
- Ready COMMIT 186.64. **Not** `fractal node finish`.
