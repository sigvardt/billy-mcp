# 198.25 Invoice Vælg kunde named control

## Goal

Prove whether `/:org_slug/invoices/new` has an exact **Vælg kunde**
named control that is not the contact `INPUT` placeholder. Official
support (12 Sep 2025) says: after **Opret faktura**, click **Vælg
kunde**, then **Opret ny** or pick an existing customer. Prior dumps
only tokenized those words as `placeholder_flags`.
`named_kunde_opener` still returns null for that placeholder.

`FD39FFE7` keeps invoice draft CUD mandatory and red. This is a new
normal-interface path. It is not a remake of the closed picker set.

TDD first. Rest inspect first. Through the real FastMCP boundary,
open the create form and persist **only**:

- `vaelg_kunde_button_count` (int)
- `vaelg_kunde_link_count` (int)
- `vaelg_kunde_other_count` (int)
- `unique_vaelg_kunde` (bool)
- `hit_is_contact_input` (bool)
- `clicked` (bool)
- `opret_ny_count` (int)
- `option_role_count` (int)
- `proved_bind` (`vaelg_kunde_existing_option` / `none`)
- `missing_keys`

`unique_vaelg_kunde` is true only when exactly one named **Vælg
kunde** control exists and `hit_is_contact_input` is false.
`proved_bind` is `vaelg_kunde_existing_option` only after that
unique control is clicked once and a unique existing option
matches a FastMCP-created `MCP-UI-INV-` + 8 hex tag. Otherwise
`UI_CHANGED`.

Rest dump needs no customer. One click only when the unique
non-input control exists. Do not save until bind is proved and a
failing product fixture exists. Never **Godkend**, **Send**, or
email.

Authority: `96908DC6`, `FD39FFE7`, `728BD2E4`, `E004E7D5`,
`EE0A0F1B`, `C7DBE974`. Closed: `C0721A14`, structure compare,
`28C8FBC8`, `31B0C7A6`, fiber, `E87B6AEF`, `07600147`,
`452E0773`, `51E18E60`, `A3AB03C3`, `9F777B8F`, `8EFD0EAD`,
`4A5CD1E7`, `F12B607E`, `D326FFB3`. Research:
`.fractal/main.billy_complete/tmp/grok-research.md` section 5.
Official docs `HEAD` ETag `"121myuqjdm53603"` / 147934 bytes.
Inventory lock stays `"wcw4x9hqvu3603"`. Do not re-lock.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping invoice `tool_name` to preview until CUD is live-proved
  and independent review accepts
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or invoice
  `parity_status` in the dump-only half
- Writing `author=independent_review` / `accept` / `purge_verified`
- Remaking closed picker, Ember, fiber, listener, descendant,
  post-click, wrapper-center, overlay, typed, route, structure, or
  customer-detail **Opret faktura** dumps
- Typing into `input[name=contact]`
- Clicking the `pickerfield` wrapper or the overlay at `dx=242`
- Copying the bills Leverandør wrapper onto this field
- Guessed URLs, guessed selectors, generic text sweeps, force
  clicks, `evaluate` clicks, blind keys
- Calling CDP, fetching a script URL, or issuing a `/v2/` URL
- Persisting source, locators, URLs, slugs, names, or screenshots
- Product create, ledger, files, org, quotes convert, Duplikér
- Live API token calls, docs re-lock, bulk92, residual29
- **Godkend**, **Send**, email, `POST /v2/invoices/:id/emails`
- Touching `chrome-profile` or `chrome-profile-readback`
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `d223c37`. Memory and research edits from this run are
uncommitted. Not a backstop. `complete=false`. Honesty 16 stay
red. Contacts, bills, and organizations CUD rows name preview
tools. Invoice CUD rows still name `ui_invoices_*_open`. Offline
invoice preview/execute helpers exist. `_create_draft` still
`goto /invoices/new` then `bind_kunde`. That bind is unbound.

Live opener dump already has `placeholder_present=true` and
`has_vaelg` / `has_kunde`. `named_opener` is null. Exact **Vælg
kunde** as `button` / `link` / other has not been counted.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds (length 16).

## Delegation

No children. Root owns this slice. `728BD2E4` child-split applies
after invoice CUD is bound, not to this inspect. Unlimited budget
is not a reason to spawn. Parked write-family children stay parked.

Reuse (do not remake):

- `src/billy_mcp/ui_writes/invoices.py` (ticket pair; no remap yet)
- `src/billy_mcp/browser.py` `ui_invoices_create_open`
- `src/billy_mcp/ui_writes/page_flow.py`
- `src/billy_mcp/ui_writes/invoices_kunde.py` (`placeholder_flags`
  stays as-is; do not treat it as a named opener)
- `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
- `wiki/ui_invoices_writes.md`

New files:

- `src/billy_mcp/ui_writes/invoices_vaelg_kunde.py`
- `tests/unit/test_ui_invoices_vaelg_kunde.py`
- `tests/live/test_ui_invoices_vaelg_kunde.py`

Do not append this inspect to `tests/live/test_ui_invoices_writes.py`.

Owner dump:
`~/.local/share/billy-mcp/inspect-live-invoices-vaelg-kunde.json`

## Design choices

1. **TDD first (`728BD2E4`).** Confirm honesty-16 still length 16
   and red. First product edit is a failing unit fixture. An empty
   dump must fail `missing_keys` with the exact research section 5
   set. Run it. Capture the failure. Then implement the helper.
2. **Honesty-16 stays red.** Do not remap invoice tools in the
   dump-only half.
3. **Rest dump first, no customer.** Open
   `/:org_slug/invoices/new` through FastMCP
   `ui_invoices_create_open`. Count exact **Vælg kunde** by role.
   No type. No picker click.
4. **Exact labels only.** Count **Vælg kunde** and later **Opret
   ny** via exact role or exact text. No substring. No body sweep.
5. **Placeholder is not the control.** If the only hit is
   `input[name=contact]`, set `hit_is_contact_input=true`,
   `unique_vaelg_kunde=false`, do not click, `UI_CHANGED`.
6. **One click only when unique and not the input.** Then count
   exact **Opret ny** and option roles. Create a tagged customer
   only if that click opened a unique existing-option path.
7. **Default stop is `UI_CHANGED`.** If the named control is
   absent or is the placeholder: do not click, do not create a
   customer, close this rest inspect. Report the owner decision.
   Do not freeze invoice CUD as done. Do not fall back to closed
   picker probes.
8. **CUD is a second half of the same EXECUTE.** Only if
   `proved_bind=vaelg_kunde_existing_option`. Smallest route
   means reuse the proved named control. Do not fall back to
   typed `bind_kunde`.
9. **Vision.** Live test may write `author=live_test` and
   `reviewer_verdict=pending_review` only.
10. **No remap** until independent accept + purge of a real CUD.

## EXECUTE

### 1. Failing tests (first edit)

Keep the honesty-16 assertions. Run:

`uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`

It must pass (length 16, red).

Add `REQUIRED_VAELG_KUNDE_KEYS` and `missing_keys` in
`invoices_vaelg_kunde.py` only after a unit fixture fails because
those names do not exist, or add the fixture first against a local
helper stub. Preferred shape:

- Fixture feeds `{}` or a flags-only stub.
- Helper returns the missing key list from research section 5.
- Current empty dump must fail with that exact set.

Do not edit `invoices_form.py` or remap coverage until that fixture
fails and, later, bind is proved.

Keep the closed picker / Ember / fiber / listener / descendant /
customer-detail fixtures.

### 2. Smallest helper, then one live rest inspect

- Implement the helper in `invoices_vaelg_kunde.py`.
- Sanitizers: allowlisted tokens only. Unknown names become
  `other` or `none`. Never persist source, locators, URLs, or
  customer text.
- Derive `unique_vaelg_kunde` and `proved_bind` from the Goal
  rule. If either fails, set `code=UI_CHANGED`.
- Isolate unit dumps so they cannot overwrite owner files.
- Live path through `create_server` `call_tool`:
  1. Login. Open `ui_invoices_create_open`.
  2. Count exact **Vælg kunde** by `button`, `link`, and other.
     Record whether the unique hit is `input[name=contact]`.
  3. If `unique_vaelg_kunde` is false: write the rest dump, stop.
     Do not click. Do not create a customer.
  4. If unique and not the input: one click. Count exact **Opret
     ny** and option roles.
  5. Existing-option bind only if a unique option exists. Then
     create `MCP-UI-INV-` + 8 hex through FastMCP, confirm in a
     fresh session, pick that option. Persist `proved_bind` only.
     Do not write the name.
  6. Do not save. Do not type into the contact input. Do not
     remake picker dumps.
  7. Write allowlisted keys to
     `~/.local/share/billy-mcp/inspect-live-invoices-vaelg-kunde.json`.
  8. If a customer was created and bind failed: delete it through
     FastMCP and prove absence.
- After the dump is delivered, later `ui-full` verifies the file
  and returns unless CUD half is in the same run.
- Refresh `wiki/ui_invoices_writes.md` with the new keys. Then
  `wiki update --path=wiki` and `wiki lint --path=wiki`.
- Fold the gate into memory `state.md` / `todo.md`. No iteration
  labels.

### 3. Named-control CUD only if proved

If `proved_bind=vaelg_kunde_existing_option`:

1. Add a failing product fixture that the current `_create_draft`
   path does not satisfy (it still types `bind_kunde`).
2. Implement the smallest route from the proved named control.
3. One disposable invoice draft CUD through FastMCP. Independent
   fresh-session read-back after create, update, and delete.
   Reverse cleanup: invoice then customer.
4. Do not remap `tool_name` until independent review accepts and
   purge is verified. Stay red in this run even if CUD persist
   works.
5. Never **Godkend**, **Send**, email, book, or pay.

If the named control is absent or is the placeholder, skip this
half.

### 4. Offline checks before COMMIT

`uv run ruff check` and the new unit file plus the honesty-16
gate. Do not run live API tests. Do not claim `complete: true`.

## Done when

- Honesty-16 still length 16 and red
- Failing **Vælg kunde** fixture existed, then the helper lists
  `missing_keys` on an empty dump
- Live rest dump written through FastMCP create-open
- Keys match research section 5. No URL, source, or customer
  text in the dump or git
- Either `proved_bind=vaelg_kunde_existing_option` and the CUD
  half ran, or `UI_CHANGED`, no leftover customer, route evidence
  recorded, invoice CUD still red and not frozen as done
- No remake of closed picker or customer-detail dumps
- Invoice CUD rows still name `ui_invoices_*_open` unless the
  CUD half completed and independent review already accepted
  (not expected in this pass)
- `complete=false`

## Feasibility

Time and cost are unlimited. One root slice fits. No spawn.
Research section 5 is the contract. Stay red.

## Post-Mortem

Completed: TDD empty-dump fixture, helper, sibling live rest
inspect. FastMCP `ui_invoices_create_open` succeeded. Rest dump:
button 0, link 0, other 0, `hit_is_contact_input=true`,
`unique_vaelg_kunde=false`, no click, `proved_bind=none`,
`UI_CHANGED`. No customer created. Honesty 16 still red. Invoice
rows still name `ui_invoices_*_open`. `complete=false`.

Dump has no URL, source, or customer text.

Deviations: no invoice CUD half. The named control is the
placeholder input, so the route closed per the Goal rule.

Review: FAIL. Dump contract met. Invoice CUD still unbound.
Required: commit helpers, no remap, no remake, stay red. Do not
freeze as done. `FD39FFE7` still binds.

Tests: honesty-16 2 passed. Vælg kunde unit tests 9 passed. Live
`test_ui_invoices_vaelg_kunde` passed. Lint passed. Offline
`test.sh` 2008 passed, 77 deselected.

Cleanup: no customer or invoice created. Temp profiles purged.

Next unresolved slice: `FD39FFE7` invoice draft CUD by a
different unused normal-interface path. Do not remake this rest
dump or the closed picker set. Product after invoice. Ledger last.

