# 186.90 UI product-plane bulk chrome dual NA empty list

## Goal

Product research190 as one **root-only** inventory package: promote the remaining **empty-list dual-absent bulk-chrome** subset of product-plane UI bulk parity honesty rows to evidenced UI `not_applicable`.

| Subset | Count | Treatment |
| --- | ---: | --- |
| Empty-list dual-absent (`/…/empty` real list shells) | **5 resources / 10 UI rows** | `parity_status=not_applicable`; empty `tool_name`; all five UI flags **true** (NA bookkeeping); qualification `ui_not_applicable` + `UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST` |
| Research188 strong dual-NA | **15 / 30** | Unchanged; keep `UI_BULK_CHROME_ABSENT_DUAL` |
| Research189 soft tool dual-NA | **9 / 18** | Unchanged; keep `UI_BULK_CHROME_ABSENT_DUAL_TOOL_PANEL` |
| API bulk | **92** | Unchanged `external_contract_blocker` |
| Residual clear | **29** | Unchanged honesty red |
| annual_reports | **1** | Unchanged org_inaccessible |

No FastMCP bulk tools. No live API. No headed browser. Not node finish. `complete` stays **false**.

After this package, **product-plane UI bulk honesty remaining = 0** (all 58 research187 ids either strong/soft/empty-list dual-NA). Completeness still blocked by API bulk 92 + residual 29 + annual_reports 1.

### Exact 5 resources (research190)

```
contacts
invoices
invoiceLines
bills
billLines
```

### Exact 10 row ids

For each resource `R` above:

```
ui.parity.<R>.bulk_save
ui.parity.<R>.bulk_delete
```

Linked API rows (stay red, empty tool):

```
api.<R>.bulk_save
api.<R>.bulk_delete
```

### Shell binding (research190 dual READY/READY, no writes)

| Shell id | Path class | H1 | Resources | Greened list tool ref (evidence only) |
| --- | --- | --- | --- | --- |
| `clients_list` | `/clients/empty` | Kontakter | contacts | `ui_clients_list` |
| `invoices_list` | `/invoices/empty` | Fakturaer | invoices, invoiceLines | `ui_invoices_list` |
| `bills_list` | `/bills/empty` | Køb | bills, billLines | `ui_bills_list` |

Notes for evidence text:

- Real empty-state shells (body_len ~290–330, empty-state copy “Ingen kontakter/fakturaer/udgifter”) ≠ nonsense soft-empty control (body_len 127, no h1).
- Dual bulk chrome absent: tables 0, tbody_rows 0, checkbox_visible 0, no bulk text / bulkish buttons.
- invoiceLines share invoices list shell; billLines share bills list shell.
- Greened list tools returned dual `UI_CHANGED` on empty org data (signature drift). **Do not** invent bulk chrome from that. **Do not** repair list-tool signatures in this package (optional later product slice).
- Create tools remain open-only; no disposable populate/submit/cleanup this package.
- Products precedent (research188): bulk NA accepted with 0 tables on non-`/empty` path; empty-list path is stronger classification, not weaker chrome evidence.

### Explicitly excluded (REJECT if touched)

| Item | Why |
| --- | --- |
| Soft 18 / strong 30 | Already producted; do not re-code or demote |
| API bulk 92 | Schema still unspecified; live API out of scope |
| Residual clear 29 | Unrelated honesty |
| annual_reports | org_inaccessible |
| List-tool UI_CHANGED repair | Separate product drift; not bulk NA |
| Disposable create/submit for non-empty list re-probe | Open-only tools; out of this inventory package |
| Any `ui_*_bulk_*` / `api_*_bulk_*` tools | Forbidden |
| `complete:true` / ui-full finish claim | Walls remain |

## Why this slice (research190)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged; bulk Supports-only 46+46 |
| Dual sessions | Independent ephemeral READY/READY; `api_token_used=false`; no writes/submits; profiles purged |
| Survey | `tmp/research190_empty_shell_dual.json` — empty_shell `bulk_chrome_absent_dual_empty_list_shell` **5**; present **0**; disagree **0** |
| Brief | `tmp/grok-research.md` research190 |
| Design §10.2 | UI bulk NA only with dual evidence no multi-select / bulk-action chrome on relevant real shells — satisfied for empty-list subset |
| STEER `1E6AF3C9` | Dual-session probes required; empty-shell 10 dual-proved (after soft/strong already dual-proved). Unsave after this package lands. |
| Baseline | After 186.89: live/vision **334**, implemented/contract **518**, UI bulk honesty remaining **10** |

## Non-goals (REJECT triggers)

- Setting any of the 10 to product tools or non-empty `tool_name`
- Using strong (`UI_BULK_CHROME_ABSENT_DUAL`) or soft-tool (`…_TOOL_PANEL`) evidence codes for empty-list rows
- Greening API bulk 92, residual clear 29, or annual_reports
- Claiming `complete:true` or running ui-full as finish
- Live API traffic; headed browser; disposable records left behind
- Demoting already-green non-bulk UI rows or research188/189 freezes
- Shipping any bulk FastMCP tools
- Spawning children (single-generator + tests + wiki ownership on root)
- “Fixing” list-tool `UI_CHANGED` as part of this package

## Target coverage math (this package alone)

| Metric | Before | After |
| --- | ---: | ---: |
| live/vision | 334 | **344** (+10) |
| implemented/contract | 518 | **528** (+10) |
| UI product-plane bulk honesty remaining | 10 | **0** |
| complete | false | **false** |

Completeness walls still open after land: API bulk **92** + residual clear **29** + annual_reports **1**.

Blocker text after land: must drop empty-shell / “×10 discovery_required” / `UI_BULK_CHROME_DUAL_REQUIRED` product-plane bulk remaining phrasing. Keep API bulk + residual + annual_reports.

## Implementation steps (EXECUTE)

### 1. Generator (`scripts/generate_coverage_report.py`)

Mirror soft/strong packages. Add after soft-tool apply (order: honesty → strong → soft → **empty_list**):

1. Constants:
   - `UI_BULK_CHROME_DUAL_NA_EMPTY_LIST_RESOURCES` (exactly 5)
   - `UI_BULK_CHROME_DUAL_NA_EMPTY_LIST_IDS` (exactly 10)
   - `UI_BULK_CHROME_DUAL_NA_EMPTY_LIST_SHELLS: dict[resource, {shell_id, path_class, tool_ref}]`
   - `UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST_EVIDENCE_CODE = "UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST"`
2. `ui_bulk_chrome_dual_na_empty_list_qualification(resource, operation)` builder:
   - `kind=ui_not_applicable`
   - `evidence_code=UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST`
   - `not_applicable_decision=accepted`
   - `sessions=dual_independent_ephemeral`
   - `evidence_ref=research190_empty_shell_dual`
   - `tools_allowed=false`
   - `empty_list_shell=true`
   - `tool_panel=false`
   - `linked_api_row_id=api.<R>.<op>`
   - shell_id / path_class / tool_ref from shells map
3. `apply_ui_product_plane_bulk_chrome_dual_na_empty_list(workflows)`:
   - invariants: len resources 5, ids 10, shells keys match, ids ⊂ honesty ids, disjoint from strong and soft sets
   - for each empty-list id: empty tool_name; all five flags True; `vision_evidence=None`; `parity_status=not_applicable`; set qualification; append evidence (research190 + evidence_code + dual empty paths + design §10.2 accepted + API bulk unchanged)
4. Honesty / remaining logic: remaining honesty = honesty_ids − strong − soft − **empty_list** → length **0**
5. Status blocker builder: when remaining product-plane bulk honesty is 0, omit “UI product-plane bulk remaining ×N … UI_BULK_CHROME_DUAL_REQUIRED”; keep API bulk + residual + annual_reports
6. Wire apply call after soft-tool in main generate path
7. Update any GEO_UI_NOT_APPLICABLE_ROW_COUNT / green-count related constants if the generator uses fixed expected green counts for UI NA bulk families (match soft package pattern; target +10 live/vision and +10 implemented/contract)

### 2. Tests (`tests/coverage/test_coverage_inventory.py`)

1. Update existing honesty / strong / soft tests:
   - `remaining_honesty` after strong+soft+empty = **0** (or honesty − all three NA sets)
   - status counts: implemented/contract **528**, live/vision **344**
   - blocker no longer contains product-plane bulk remaining ×10 / UI_BULK_CHROME_DUAL_REQUIRED for product-plane bulk (API bulk phrasing may still mention bulk schema)
2. Add `test_ui_product_plane_bulk_chrome_dual_na_empty_list_rows`:
   - 5 resources / 10 ids; subset of honesty; disjoint strong+soft
   - each row: toolless; five flags true; parity not_applicable; qual fields as above; evidence contains research190 + EMPTY_LIST code
   - linked API bulk stays red / toolless / live_tested false
   - complete false; counts 528/344
3. Keep geo bulk NA assertion intact
4. Keep annual_reports inaccessible test unchanged

### 3. Regenerate coverage

```bash
uv run python scripts/generate_coverage_report.py
```

Verify `coverage/status.json`: complete false; implemented/contract 528; live/vision 344; blocker without empty-shell 10.

### 4. Wiki

Add `wiki/ui_product_plane_bulk_chrome_dual_na_empty_list.md` (mirror soft/strong pages):

- Authority/scope: research190 empty-list dual-NA only
- Exact 5 resources / 10 rows
- Shell mapping table
- Not product ACCEPT; not API bulk green; complete false
- Completeness walls table (92 + 29 + 1)
- Generator + test references
- Cross-link honesty / strong / soft pages; update those pages’ “remaining honesty” lines to 0 product-plane bulk

### 5. Verification (EXECUTE / FIX-VERIFY)

```bash
bash .fractal/main.billy_complete/scripts/lint.sh
BILLY_TEST_MODE=commit bash .fractal/main.billy_complete/scripts/test.sh
# or at least: uv run pytest tests/coverage/test_coverage_inventory.py -q
wiki lint --path=wiki
```

Do **not** run ui-full / finish this iteration unless all completion walls clear (they will not).

### 6. Post-land radio

- Unsave STEER `1E6AF3C9` after package commits (dual probes for original 58 product-plane bulk honesty complete via strong+soft+empty-list).
- Outbox one-liner on package land.

## File ownership (root-only)

| Path | Change |
| --- | --- |
| `scripts/generate_coverage_report.py` | empty-list constants, qual builder, apply, status blocker, wire |
| `tests/coverage/test_coverage_inventory.py` | update counts + new empty-list test |
| `coverage/*` | regenerated manifests/status/report |
| `wiki/ui_product_plane_bulk_chrome_dual_na_empty_list.md` | new |
| `wiki/ui_product_plane_bulk_parity_inventory_honesty.md` | remaining honesty → 0 product-plane bulk |
| `wiki/ui_product_plane_bulk_chrome_dual_na_strong.md` | remaining walls |
| `wiki/ui_product_plane_bulk_chrome_dual_na_soft_tool.md` | remaining walls |
| `.fractal/main.billy_complete/memory/state.md` | EXECUTE note |

No children. No src/billy_mcp product tool changes.

## Acceptance criteria

1. 10 empty-list UI bulk rows: `not_applicable`, empty tool, five flags true, evidence_code `UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST`, `empty_list_shell=true`, evidence_ref `research190_empty_shell_dual`
2. Strong 30 + soft 18 unchanged codes
3. Product-plane UI bulk honesty remaining **0**
4. API bulk 92 still red / toolless / external_contract_blocker; live_tested false out_of_scope_by_user
5. status: 528 implemented/contract, 344 live/vision, complete **false**
6. inventory tests + lint + commit-mode tests pass
7. Wiki page present; no secrets; no bulk tools
8. STEER 1E6AF3C9 unsaved after commit

## Risks

| Risk | Mitigation |
| --- | --- |
| IR rejects empty-list NA without non-empty populate | Research190 + products empty-ish precedent; distinct evidence code; dual READY; real h1 empty-state ≠ nonsense. If IR REJECT, revert package only; do not invent bulk tools |
| Status blocker still mentions ×10 after empty-list | Explicitly update remaining-honesty math and blocker template |
| Hard-coded 518/334 assertions miss update | Grep tests for 518/334 and update together |
| List-tool UI_CHANGED confuses IR | Document as separate product drift; dual direct-nav is primary evidence |

## Out of scope next walls (not this plan)

- API bulk 92 schema (docs/asset exhaust already done; live API forbidden)
- Residual clear 29
- annual_reports org_inaccessible
- List-tool signature repair for empty org data

## Feasibility

- Budget: no limit
- Root-only generator/tests/wiki; same shape as 186.88/186.89
- Research evidence already dual READY/READY in `tmp/research190_empty_shell_dual.json`
- No child spawn needed

## Post-Mortem

### Completed
- Research190 dual empty-shell probe READY/READY; docs MD5/ETag unchanged
- Producted empty-list dual-NA package for 5 resources / 10 UI bulk rows
  (evidence_code UI_BULK_CHROME_ABSENT_DUAL_EMPTY_LIST; evidence_ref research190_empty_shell_dual)
- Generator: empty-list constants/apply after soft; GEO_UI_NOT_APPLICABLE_ROW_COUNT 268
- Coverage regenerated: implemented/contract **528**, live/vision **344**, complete **false**
- Product-plane UI bulk honesty remaining: **0** (strong 30 + soft 18 + empty 10)
- Wiki: ui_product_plane_bulk_chrome_dual_na_empty_list.md + honesty/strong/soft/index updates
- IR: package **PASS**, node complete **FAIL** (expected); required fixes **none**

### Deviations
- None material. List tools returned dual UI_CHANGED on empty org data during research;
  dual direct-nav empty shells used as primary evidence (documented; not repaired this slice).

### Review findings
- Independent review (tmp/grok-review.md): package PASS; no package fixes
- Rejected findings: none (no contested IR items)

### Test results
- lint.sh: pass
- BILLY_TEST_MODE=commit test.sh: **1671 passed**, 54 deselected
- Did not run ui-full/full (complete false; walls remain)

### Cleanup state
- Research dual profiles purged; no disposable UI writes/submits
- No secrets/screenshots/HAR in repo

### Next unresolved coverage slice
1. API bulk x92 (BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS; live API out of scope)
2. Residual clear honesty x29
3. annual_reports org_inaccessible x1
4. Optional: empty-org list-tool signature repair (UI_CHANGED on clients/invoices/bills list)

### STEER
- After COMMIT 186.90: unsave radio STEER 1E6AF3C9 (dual probes for original 58
  product-plane bulk honesty complete via strong+soft+empty-list).

