# 186.88 UI product-plane bulk chrome dual NA strong

## Goal

Product research188 as one **root-only** inventory package: promote the **strong dual-absent bulk-chrome** subset of product-plane UI bulk parity rows to evidenced UI `not_applicable`.

| Subset | Count | Treatment |
| --- | ---: | --- |
| Strong dual-absent (non-empty greened shells) | **15 resources / 30 UI rows** | `parity_status=not_applicable`; empty `tool_name`; all five UI flags **true** (NA bookkeeping, geo style); qualification `ui_not_applicable` + `UI_BULK_CHROME_ABSENT_DUAL` |
| Empty-shell dual-absent (contacts/invoices/bills + lines) | **5 / 10** | **Hold** research187 honesty (`discovery_required` + `UI_BULK_CHROME_DUAL_REQUIRED`) |
| Soft VAT/users/sales-tax path seeds | **9 / 18** | **Hold** honesty until tool-based dual follow-on |
| API bulk | **92** | Unchanged `external_contract_blocker` |
| Residual clear | **29** | Unchanged honesty red |
| annual_reports | **1** | Unchanged org_inaccessible |

No FastMCP bulk tools. No live API. No headed browser. Not node finish. `complete` stays **false**.

### Exact 15 resources (research188 §4.3)

```
accounts
attachments
bankLineMatches
bankLineSubjectAssociations
bankLines
bankPayments
daybookBalanceAccounts
daybookTransactionLines
daybookTransactions
daybooks
files
organizations
postings
products
transactions
```

### Exact 30 row ids

For each resource `R` above:

```
ui.parity.<R>.bulk_save
ui.parity.<R>.bulk_delete
```

Linked API rows (stay red, empty tool):

```
api.<R>.bulk_save
api.<R>.bulk_delete
```

### Explicitly excluded from this package (REJECT if NA'd)

| Resource | Why held |
| --- | --- |
| contacts, invoices, invoiceLines, bills, billLines | Dual observed `/…/empty` list shells only; bulk chrome may appear when rows exist |
| salesTaxAccounts, salesTaxMetaFields, salesTaxPayments, salesTaxReturns, salesTaxRules, salesTaxRulesets, taxRateDeductionComponents, taxRates, users | Soft path seeds (`/settings/vat`, `/settings/users`, `/vat-returns`) dual soft-empty == nonsense; real panels need `ui_settings_vat_open` / `ui_settings_users_open` / `ui_vat_declarations_list` dual |

## Why this slice (research188)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged; bulk Supports-only 46+46 |
| Dual sessions | Independent ephemeral READY/READY; org non-prod; `api_token_used=false`; no writes; profiles purged |
| Survey | `tmp/research188_bulk_chrome_dual.json` — dual-absent real shell **20**, present **0**, no real shell **9** |
| Strong subset | **15** of the 20 dual-absent map to **non-empty** path classes (not `*/empty`) |
| Design §10.2 | UI bulk NA only with dual evidence no multi-select / bulk-action chrome — satisfied for strong subset |
| Honesty 186.87 | 58 rows already machine-readable discovery_required; this package **lifts** 30 of them to NA without greening tools |

## Non-goals (REJECT triggers)

- Setting any of the 30 to product tools or non-empty `tool_name`
- NA of the held 10 empty-shell or 18 soft VAT/users rows
- Greening API bulk 92, residual clear 29, or annual_reports
- Claiming `complete:true` or running ui-full as finish
- Live API traffic; headed browser; disposable records left behind
- Demoting already-green non-bulk UI rows
- Spawning children (single-generator + tests + wiki ownership)

## Files to touch (EXECUTE)

| Path | Change |
| --- | --- |
| `scripts/generate_coverage_report.py` | Add frozensets (15 resources / 30 UI ids); `ui_bulk_chrome_absent_dual_qualification()`; `apply_ui_product_plane_bulk_chrome_dual_na_strong(workflows)` **after** `apply_ui_product_plane_bulk_parity_honesty`; update `qualification_blocker` text to remaining UI bulk ×28 (empty 10 + soft VAT/users 18) + annual |
| regenerate | Run coverage generator so manifests + `coverage/status.json` + `coverage/report.md` match |
| `tests/coverage/test_coverage_inventory.py` | New `test_ui_product_plane_bulk_chrome_dual_na_strong_*`; **update** honesty test so only remaining 28 honesty rows assert `discovery_required` (exclude the 30 NA ids); assert geo NA bulk still NA; API bulk 92 still blocked; counts 316/500 complete false |
| `wiki/ui_product_plane_bulk_chrome_dual_na_strong.md` | Durable dual-NA page (peer of geo NA + honesty); not product ACCEPT |
| `wiki/ui_product_plane_bulk_parity_inventory_honesty.md` | Note 30 rows lifted by research188; 28 remain discovery_required |
| `wiki/offline_write_probe_rules.md` | One-line cross-link if needed |
| `wiki/_index.md` | Link + desc (or fractal commit regenerate) |
| `.fractal/.../memory/state.md` | EXECUTE/IR notes |

## Implementation steps (EXECUTE order)

1. **Constants** next to research187 honesty helpers:
   - `UI_BULK_CHROME_DUAL_NA_STRONG_RESOURCES: frozenset[str]` (exact 15 camelCase names)
   - `UI_BULK_CHROME_DUAL_NA_STRONG_IDS` derived as `{f"ui.parity.{r}.bulk_save", f"ui.parity.{r}.bulk_delete"}` — assert len == 30
   - `UI_BULK_CHROME_ABSENT_DUAL_EVIDENCE_CODE = "UI_BULK_CHROME_ABSENT_DUAL"`
2. **Qualification builder** (mirror geo NA fields):
   - `kind: ui_not_applicable`
   - `evidence_code: UI_BULK_CHROME_ABSENT_DUAL`
   - `not_applicable_decision: accepted`
   - `sessions: dual_independent_ephemeral`
   - `evidence_ref: research188_bulk_chrome_dual`
   - `docs_md5` / `docs_etag`: locked fingerprint
   - `linked_api_row_id: api.<R>.bulk_*`
   - `tools_allowed: false`
   - `empty_list_shell: false`
   - shell_refs from research188 mapping table below
3. **`apply_ui_product_plane_bulk_chrome_dual_na_strong(workflows)`**:
   - For each row id in strong frozenset:
     - force `parity_status=not_applicable`
     - set five UI flags true; `tool_name=""`
     - set qualification + method_or_route + evidence citing research188 dual (no multi-select bulk chrome on non-empty greened shells; no writes; dual READY)
     - `vision_verified` true with `vision_evidence` null (DOM dual only; research took no frames — same pattern as many geo NA rows)
   - Do **not** touch held honesty rows
4. **Wire** in `build_ui_manifest` **after** `apply_ui_product_plane_bulk_parity_honesty` so NA wins over discovery_required for the 30.
5. **Blocker string** refresh: replace `UI product-plane bulk ×58 discovery_required` with remaining UI bulk ×28 discovery_required (empty-shell 10 + soft VAT/users 18); annual_reports org_inaccessible (keep API bulk / residual text).
6. **Regenerate coverage** via existing script entrypoint.
7. **Tests**:
   - Strong 30: tool empty; flags true; parity not_applicable; qualification fields; evidence_code; linked_api still red/toolless on API side
   - Honesty remaining 28: still discovery_required + UI_BULK_CHROME_DUAL_REQUIRED
   - Geo bulk NA unchanged; API bulk 92 external_contract; residual 29 red; annual red
   - status: live/vision **316**, implemented/contract **500**, complete **false**
8. **Wiki** dual-NA strong page + honesty cross-link update
9. **Offline verify**: lint + commit-mode `test.sh` (no live API; no ui-full required for package)

### Shell mapping to cite in evidence (from research188)

| Resources | Shell id | Path class (dual) |
| --- | --- | --- |
| products | products_list | `/products` |
| transactions, postings | transactions_list | `/transactions` |
| attachments, files | uploads_list | `/uploads` (file-picker “vælg alle” observe-only; no row multi-select) |
| bankLines, bankLineMatches, bankLineSubjectAssociations, bankPayments | bank_accounts_list | `/bank-accounts` |
| daybooks, daybookTransactions, daybookTransactionLines, daybookBalanceAccounts | daybooks_open | `/daybooks/<id>` (≤1 checkbox; not multi-select bulk) |
| accounts | settings_accounting | `/settings` hub |
| organizations | settings_company | `/settings` hub |

## Test plan

| Check | Expected |
| --- | --- |
| `len(UI_BULK_CHROME_DUAL_NA_STRONG_IDS) == 30` | pass |
| Each of 30: empty tool; flags all true; parity not_applicable; evidence_code UI_BULK_CHROME_ABSENT_DUAL | pass |
| Remaining product-plane bulk honesty ids == 28; discovery_required; blocker UI_BULK_CHROME_DUAL_REQUIRED | pass |
| Intersection strong ∩ held empty/soft == empty | pass |
| API bulk 92 still external_contract_blocker / empty tool | pass |
| status 500/316 complete false | pass |
| No new bulk tools in FastMCP registry | pass |

## Review criteria (IR)

**ACCEPT package** if strong 30 match research188 exactly, held 28 stay honesty red, math 500/316 complete false, no bulk tools.

**REJECT package** if empty-shell/soft rows NA'd, any API bulk greened, complete claimed true, or tools invented.

**Node completeness:** FAIL expected (walls remain).

## Child nodes

None. Root-only Grok.

## Done when

- Plan executed; coverage regenerated; inventory tests pass offline
- 30 strong UI bulk rows NA with dual evidence; 28 remain discovery_required
- Wiki dual-NA page present; honesty page notes the lift
- IR ACCEPT package; FIX-VERIFY clean; fractal commit lands
- Not node finish

## Post-mortem

### Work completed

- Research188 dual bulk-chrome survey (READY/READY, no API token, no writes) identified strong non-empty-shell dual-absent set: 15 resources / 30 UI bulk rows.
- EXECUTE: generator frozensets + `apply_ui_product_plane_bulk_chrome_dual_na_strong` after honesty; `GEO_UI_NOT_APPLICABLE_ROW_COUNT` 240; coverage regenerated; inventory tests + wiki dual-NA page.
- IR: **PASS (package)** / node complete **FAIL** expected (`tmp/grok-review.md`). Required fixes: **none**.
- FIX-VERIFY: invariants reconfirmed (30 NA + 28 honesty, 500/316, complete false); pyright cast hygiene on evidence_code filter; lint clean; commit-mode **1669 passed**; wiki lint clean on project wiki and memory.

### Deviations

- None material. Conservative slice held empty-shell 10 and soft VAT/users 18 as planned.
- Minor: `GEO_UI_NOT_APPLICABLE_ROW_COUNT` now includes research188 bulk dual-NA (name overloaded; non-blocking).

### Review findings

- Package ACCEPT; no production required fixes.
- Non-blocking: follow-on disposable-row re-probe for empty shells; tool-based dual for VAT/users panels.

### Test results

- `bash scripts/lint.sh` (via node lint.sh): pass
- `BILLY_TEST_MODE=commit bash .../scripts/test.sh`: 1669 passed, 54 deselected
- Did **not** run ui-full/full (complete false; not finish)

### Cleanup state

- Dual research profiles purged during research; no disposable Billy records created in this package.
- No raw frames/screenshots in repo.

### Next unresolved coverage slice

1. Empty-shell dual re-probe (contacts/invoices/bills + lines) after disposable row create/cleanup.
2. Tool-based dual bulk-chrome for salesTax*/taxRates*/users soft panels.
3. API bulk 92 schema / residual clear 29 / annual_reports remain open walls (complete stays false).

Not node finish.
