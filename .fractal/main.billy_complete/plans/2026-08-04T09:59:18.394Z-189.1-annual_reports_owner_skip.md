# 189.1 Annual reports owner out-of-scope skip

## Goal

Record Joakim’s owner decision (`DC3B8E96`): `ui.discovery.annual_reports` is
**out of scope** for this deployment (Årsrapporter not in Billy plan). It must
stop being a completion blocker. **No** annual-reports tool. No live API. No
bulk/residual greening. Grok-only root work.

Authority: research brief
`.fractal/main.billy_complete/tmp/grok-research.md` + parent radio `DC3B8E96`.

## Non-goals

- Invent `ui_annual_*` / `api_annual_*`
- Live browser dual reconfirm or `ui-full` suite for this slice alone
- Official docs re-lock, bulk92 unlock, residual29 unlock
- Claim `complete: true` while bulk/residual remain red
- Child nodes (single clear ownership on root)

## Current baseline (HEAD `3c0bfa7`)

| Metric | Value |
| --- | --- |
| `complete` | false |
| implemented/contract | 528 |
| UI live/vision | 344 |
| Walls | bulk92 + residual29 + annual1 |
| Only incomplete UI row | `ui.discovery.annual_reports` (`ANNUAL_REPORTS_ORG_INACCESSIBLE`) |

## Design choices

1. **Not `not_applicable`.** Prior freezes correctly rejected NA (nav Årsrapporter
   + route exist). Owner skip is plan/deployment scope, not absence of UI.
2. **Machine-readable** `qualification.kind = out_of_scope_by_user` with stable
   `scope_code = ANNUAL_REPORTS_OWNER_SKIP`, `owner_decision_ref = radio:DC3B8E96`,
   `tools_allowed = false`, prior dual refs kept under `prior_evidence_ref`.
3. **Completeness:** exclude owner-scoped UI rows from the UI live/vision
   incompleteness gate (and from `row_is_qualified` / `coverage_is_complete` UI
   all-true requirement) **without** setting `live_tested`/`vision_verified`
   true as if dual-qualified. Prefer exclude-by-kind over false live green.
4. **Flags:** keep `discovered/implemented/contract_tested/live_tested/
   vision_verified` honest (false or explicit scoped non-claim). Do not
   NA-style all-true green.
5. **Blocker text:** drop `annual_reports org_inaccessible` once annual is
   owner-scoped; bulk92 clause remains primary external wall.

## Execute steps (single root slice)

### 1. Generator (`scripts/generate_coverage_report.py`)

- Replace `apply_ui_annual_reports_inaccessible_evidence` with
  `apply_ui_annual_reports_out_of_scope_by_user` (or rename + rewrite body).
- Call site for family `annual_reports` uses the new applier.
- Qualification object minimum:
  - `kind: out_of_scope_by_user`
  - `scope_code: ANNUAL_REPORTS_OWNER_SKIP`
  - `owner_decision_ref: radio:DC3B8E96`
  - `owner_decision_date: 2026-08-04`
  - `tools_allowed: false`
  - `reason`: Årsrapporter not in owner Billy plan for this deployment
  - `prior_evidence_ref`: research191 / discovery122 (history preserved)
  - `not_applicable_decision: rejected` (nav still exists; owner skip ≠ NA)
- Evidence / method_or_route / errors strings: remove stay-red inaccessible
  framing; state owner out-of-scope; no tool; no invent api_annual_*.
- `tool_name`: null / unset.
- `parity_status`: e.g. `out_of_scope_by_user` (not `not_applicable`, not
  `discovery_required` unlock-wait).
- `cleanup`: no records; no tool.
- Update helpers:
  - `row_is_qualified` **or** `coverage_is_complete` so UI rows with
    `qualification.kind == out_of_scope_by_user` do not fail completeness UI
    gate.
  - `qualification_blocker()`: when annual is owner-scoped, do not mention
    `annual_reports org_inaccessible`. Keep bulk freeze text if bulk rows
    remain.

### 2. Regenerate coverage artifacts

```bash
uv run python scripts/generate_coverage_report.py
```

Confirm:

- `ui.discovery.annual_reports` has owner scope qual; no tool
- status blocker no longer cites annual inaccessible
- bulk92 + residual29 still red; `complete` still false
- UI incomplete count for live/vision excludes annual (or annual not blocking)

### 3. Tests (`tests/coverage/test_coverage_inventory.py`)

- Replace tests asserting `annual_reports org_inaccessible` in blocker and
  `ANNUAL_REPORTS_ORG_INACCESSIBLE` stay-red / dual inaccessible decision.
- Add / rewrite `test_annual_reports_owner_out_of_scope_by_user`:
  - `qualification.kind == out_of_scope_by_user`
  - `scope_code == ANNUAL_REPORTS_OWNER_SKIP`
  - `tools_allowed is false`
  - `parity_status != not_applicable`
  - `live_tested is not True` and `vision_verified is not True` (no false dual)
  - no `ui_annual_*` / `api_annual_*` tool_name
  - blocker string does not contain `annual_reports org_inaccessible`
- Keep inventory honesty: row still present; prior evidence refs retained.

### 4. Wiki + NODE.md

- Update `wiki/ui_annual_reports_inaccessible.md` (keep filename for links or
  retitle in front matter): supersession by owner skip; dual history retained;
  unlock-via-org no longer required for **this deployment’s** completeness.
- Update `wiki/_index.md` blurb for that page.
- Light contrast notes elsewhere only if tests/docs require (prefer minimal
  churn; full rewrite of every “Contrast with annual reports” section is
  optional if links still resolve).
- NODE.md: add qualification bullet parallel to 2026-07-31 API live out of
  scope: 2026-08-04 owner skip Årsrapporter / `ui.discovery.annual_reports`
  `out_of_scope_by_user`.

### 5. Verify (before COMMIT)

```bash
bash .fractal/main.billy_complete/scripts/lint.sh
bash .fractal/main.billy_complete/scripts/test.sh   # default offline suite
```

Do **not** run live API tests. Do not require `BILLY_TEST_MODE=ui-full` for
this slice unless already cheap and green.

### 6. Report (COMMIT / outbox)

Exact remaining blockers after slice:

- API ambiguous bulk: **92** (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`)
- API residual offline freezes: **29** (method-closed / meta-delete / readonly)
- Annual: owner out of scope (not a wall)
- `complete: false` (expected)

Unsave radio `DC3B8E96` only after product commit lands.

## Children

**None.** File ownership is clear on root (`scripts/`, `coverage/`, `tests/`,
`wiki/`, NODE.md). Spawning would only add merge tax.

## Risks

| Risk | Mitigation |
| --- | --- |
| Tests hard-code inaccessible blocker in many places | `rg annual_reports` and fix all inventory assertions |
| Accidental NA greening | Explicit reject NA; assert parity_status ≠ not_applicable |
| Accidental complete true | Assert bulk still present; complete false |
| False live/vision green | Assert flags not True for dual-qualified claim |
| Wiki link rot | Keep page path; update abstract |

## Success criteria

- Owner decision recorded machine-readably on `ui.discovery.annual_reports`
- No annual tool invented
- Annual not in status blocker as org_inaccessible
- Offline lint + test suite pass
- Walls reported: bulk92 + residual29 only (for UI/annual)
- Commits pushed via `fractal commit` on node branch

## Next step

EXECUTE this plan on root with agent grok. No research restart.

## Post-Mortem

### Completed

- Owner radio `DC3B8E96` recorded as machine-readable `out_of_scope_by_user` on
  `ui.discovery.annual_reports` (`scope_code=ANNUAL_REPORTS_OWNER_SKIP`).
- Generator: `apply_ui_annual_reports_out_of_scope_by_user` +
  `is_owner_out_of_scope` completeness exclusion (flags stay false; no tool).
- Regenerated `coverage/ui_workflows_manifest.yaml`, `status.json`, `report.md`.
- Tests updated; wiki + NODE.md supersession notes.
- Independent review: **PASS (package)**; required fixes **none**.

### Deviations

- None material. Non-blocking: wiki path still `ui_annual_reports_inaccessible.md`
  for link stability.

### Review findings

- Package PASS; node complete FAIL expected.
- No greening of bulk92 or residual29; no invent tools; not NA; no false live/vision.

### Test results (FIX-VERIFY)

- `scripts/lint.sh`: pass
- `scripts/test.sh`: 1671 passed, 54 deselected
- `BILLY_TEST_MODE=full` / `ui-full`: **not run** (completion requirements not met)
- wiki lint project + memory: clean

### Cleanup state

- No disposable Billy records created this slice.
- No raw browser frames.

### Next unresolved coverage slice

- API bulk **92** (`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`) stay red until official
  docs/schema unlock or further owner scope.
- API residual offline freezes **29** (method-closed / meta-delete / readonly).
- `complete` remains false; do not node finish.
