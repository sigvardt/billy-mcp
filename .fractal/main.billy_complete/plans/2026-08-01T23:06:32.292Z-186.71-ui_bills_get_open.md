# 186.71 ui bills get open

## Goal

Product research170 ACCEPT:

1. **Scoped browser egress expansion** on `api.billysbilling.com` `path_allow`:
   keep GET `/v2/bills` (186.69); **add** exact POST `/v2/bills`, prefix DELETE
   `/v2/bills`, and prefix GET `/v2/taxRates` so live harness can SPA-seed a
   disposable draft bill (needs taxRateId) and delete it without
   `BILLY_API_TOKEN`. Accounts GET already committed.
2. New read-only FastMCP UI tool **`ui_bills_get_open`** that opens a **bill
   detail observe surface** frozen as `/:org_slug/bills/:id` (read detail;
   research170 dual marker + chrome). Never Gem / Godkend / Opdater / Slet /
   Træk / submit. Edit surface `/bills/:id/edit` may appear after list click;
   tool may soft-navigate to read detail or accept edit chrome only as
   intermediate — final success asserts read `path_class` (or documented dual
   freeze if EXECUTE proves edit-only settle is stabler; prefer read detail).
3. Green exact **`ui.parity.bills.get`** / map `api.bills.get` onto
   `ui_bills_get_open` (`detail_open_only` / get-open parity).
4. Keep greened `ui_bills_list` and `ui_bills_create_open` unchanged. Do **not**
   dual-count get onto those tools.

Target math (tip `71e816a` / product 186.70):
live/vision **170 → 171**; implemented/contract **354 → 355**; complete
**false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research170)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. bills Support get/list/create/update/bulk. billLines: account+taxRate+description+amount (not unitPrice). |
| GET plane | Committed bills GET reconfirmed dual 200. |
| SPA seed dual | contact supplier + draft bill nested lines (accountId, taxRateId, description, amount; **no paymentDate**) POST 200 dual under TEMP POST/DELETE bills + GET taxRates. paymentDate without paymentAccount → 422 dual. |
| Detail dual | After seed, list marker dual; soft **`/:org_slug/bills/:id`** detail_ready true dual (marker + Kladde / Restbeløb / Ret køb); text-click lands `/bills/:id/edit` chrome dual. |
| products residual | list marker dual; soft detail **false dual** — REJECT greening products.get this slice. |
| Isolation | Get-open ≠ list shell ≠ create form_open ≠ special.invoice_email. Green only **bills.get**. |
| Deferred | products.get/update/delete; bills.update/delete; invoice_email; bulk 92; annual_reports. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research170_focus_dual.json`.  
Temp egress reference: `tmp/research170_egress_temp.yaml` (do not commit TEMP as-is; promote only the three path rules below).

## Non-goals

- Full `browser_action: allow` on `api.billysbilling.com`
- billLines POST/DELETE path_allow unless EXECUTE proves nested lines fail without them (prefer nested `lines` on POST bill body)
- POST `…/emails` / invoice_email path_allow
- Greening `bills.update` / `delete` / bulk_* / special.invoice_email
- Greening products.get* or dual-counting get onto list/create bills tools
- Ticketed durable UI write tool for bill create (API writes stay offline; live seed is harness-only SPA token)
- Submitting Gem / Godkend / Opdater / Slet / Træk after open
- Live API token, headed browser, bulk/annual green, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools

## Ownership

| Area | Owner |
| --- | --- |
| `coverage/browser_egress.yaml` POST/DELETE bills + GET taxRates + evidence | Root Grok EXECUTE |
| Unit allow-matrix (`tests/unit/test_browser.py`) | Root Grok EXECUTE |
| Models + `BrowserRuntime.ui_bills_get_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count bills.get | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research170)

### A. Browser egress (`coverage/browser_egress.yaml`)

Host `api.billysbilling.com` remains `browser_action: path_allow` and
`api_client_action: exclusive_allow`.

**Keep** existing auth/bootstrap + contacts GET/POST/DELETE + countries GET +
products GET/POST/DELETE + accounts GET + salesTaxRulesets GET + invoices
GET/POST/DELETE + bills GET.

**Add** (minimum dual-proven seed/cleanup set):

| match | methods | path | purpose |
| --- | --- | --- | --- |
| exact | POST | `/v2/bills` | disposable draft seed (SPA token; nested lines in body) |
| prefix | DELETE | `/v2/bills` | reverse-order cleanup after live test |
| prefix | GET | `/v2/taxRates` | pick purchase taxRateId for nested billLine seed |

**Do not add this slice** (unless EXECUTE proves hard block):

- POST/DELETE `/v2/billLines` (nested lines on bill create dual-worked)
- any `…/emails` path
- full host allow

Update host `evidence` / `purpose` / `test_references` to cite research170 +
`tests/live/test_ui_bills_get_open.py`. Deny regressions stay: unlisted
billing integrations, api.billy.dk, default deny.

### B. Tool: `ui_bills_get_open`

| Field | Contract |
| --- | --- |
| Name | `ui_bills_get_open` |
| Input | empty / org-from-session only (mirror invoices get: open first openable draft / marker path; no caller-provided generic browser control) |
| Success model | `UiBillsGetOpenSuccess` with booleans only (no raw ids/PII): e.g. `detail_open`, `path_class`, `read_chrome_present` / `edit_chrome_present`, `kladde_or_state_chrome`, `supplier_or_amount_chrome` / similar non-sensitive flags; `shell_kind=bills_get` |
| Path class | **`/:org_slug/bills/:id`** preferred (research170 dual marker proof). Accept dual-documented edit path only if read detail cannot settle; freeze one class in code + tests. Not `/new`, not bare list. |
| Errors | typed `ToolError` for auth/egress/UI_CHANGED; never generic browser control |
| Forbidden | Gem, Godkend, Opdater, Slet, Træk, Upload submit, payment actions |

Mirror structure of `ui_invoices_get_open` (models → browser runtime → server
registry → unit tests with injectable page/context).

### C. Live dual qualification

File: `tests/live/test_ui_bills_get_open.py`

| Rule | Requirement |
| --- | --- |
| Sessions | Independent dual profiles; second-interface read-back (no API token) |
| Seed | SPA token only: contact (supplier) + draft bill with nested line `accountId` + `taxRateId` + unique description marker + `amount`; omit `paymentDate` |
| Open | Session A + B both call `ui_bills_get_open`; assert dual path_class + chrome flags |
| DOM | Independent of vision: path not list/not new; chrome markers dual |
| Vision | Non-sensitive Grok vision record under owner-only tmp; purge frames after; durable review record only |
| Cleanup | DELETE bill → contact (and any product if used); fresh UI list empty of markers dual |
| Safety | `BILLY_API_TOKEN` unset; headless; no screenshots in repo |

### D. Coverage generator

- Add `apply_ui_bills_get_open_shell_evidence` (or extend pattern from invoices get).
- Wire exact dual-count for `api.bills.get` / `ui.parity.bills.get` → tool
  `ui_bills_get_open`, parity `detail_open_only`.
- Discovery row if present (`bills_get` / purchases detail) green only with
  dual live+vision refs.
- Leave bills.update/delete/bulk_* red; leave products.get red; leave
  invoice_email red.
- Regenerate manifests; `live_tested_rows` 170→171; `implemented_rows` /
  `contract_tested_rows` 354→355 if a new tool increments contract (match
  invoices get math: one UI get tool + parity row).
- API `live_tested` stays false with `out_of_scope_by_user`.
- `complete: false` (bulk 92 + annual + residual UI).

### E. Tests / wiki

| Deliverable | Notes |
| --- | --- |
| Unit egress | allow POST bills, DELETE bills, GET taxRates; deny email paths; keep invoices rules |
| Unit browser | success path + UI_CHANGED / empty list failures for `ui_bills_get_open` |
| Models/server registry | typed models + tool name registered |
| Coverage inventory | bills.get green on get tool; list/create tools unchanged |
| Wiki | `wiki/ui_bills_get_open_shell.md` + `_index` link; optional note on invoices/bills egress page |
| Offline suite | `bash .fractal/main.billy_complete/scripts/lint.sh` + offline `test.sh` before COMMIT |
| Live | dual test pass once credentials available in EXECUTE |

## EXECUTE checklist (ordered)

1. Expand `coverage/browser_egress.yaml` (POST bills, DELETE bills, GET taxRates) + generator `build_browser_egress` if duplicated.
2. Models: `UiBillsGetOpenSuccess` (+ protocol if used).
3. `BrowserRuntime.ui_bills_get_open` + path/chrome helpers.
4. Server register tool.
5. Unit tests (egress + browser + models/server).
6. Live dual test + vision purge path.
7. Coverage generator + inventory tests; regenerate coverage artifacts.
8. Wiki shell page.
9. Offline lint + test.sh; run live dual test.
10. Stop for IR (no green of deferred residuals).

## Success criteria

- [ ] research170 dual seed contract implemented in live harness (no paymentDate; amount line)
- [ ] `ui_bills_get_open` typed tool passes unit + dual live + vision record purged
- [ ] `ui.parity.bills.get` discovered/implemented/contract/live/vision true
- [ ] Egress matrix documents new paths; no false full-host allow
- [ ] Counters: live/vision 171; implemented/contract 355; complete false
- [ ] Residual red: products.get, bills update/delete, invoice_email, bulk 92, annual
- [ ] No `BILLY_API_TOKEN`; org clean after live test
- [ ] Not node finish

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Edit vs read path ambiguity | Freeze preferred `/:org_slug/bills/:id` in code; if list click lands edit, soft-goto read or assert dual chrome without write |
| taxRates pick unstable | Prefer first `appliesToPurchases=true` like research170; keep amount small draft |
| Empty org bills list | Harness seeds before open; tool errors typed if no bill openable |
| Accidental write chrome click | Never click Godkend/Slet/Gem/Opdater; unit asserts forbidden selectors not invoked |
| Coverage false dual-count | Exact row id `api.bills.get` / `ui.parity.bills.get` only |

## Post-mortem (fill at FIX-VERIFY / COMMIT)

_TBD after EXECUTE._

## References

- research170 brief: `.fractal/main.billy_complete/tmp/grok-research.md`
- dual: `.fractal/main.billy_complete/tmp/research170_focus_dual.json`
- prior pattern: plan 186.70 `ui_invoices_get_open`
- design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
- official docs: https://www.billy.dk/api/

## Post-Mortem

### Completed
- Egress: exact POST `/v2/bills`, prefix DELETE `/v2/bills`, prefix GET `/v2/taxRates` (research170 dual).
- Tool `ui_bills_get_open` (path_class `/:org_slug/bills/:id`, shell_kind `bills_get`); soft-nav from `/edit` to read path.
- Dual-count `ui.parity.bills.get` / `api.bills.get` (`detail_open_only`).
- Live dual test + vision record purge_verified; offline suite green.
- Coverage: live/vision 170→171; implemented/contract 354→355; complete false.

### Deviations
- None material vs plan. Path freeze preferred read detail (as plan recommended). Nested bill lines (no billLines path_allow) dual-worked.

### Review findings
- IR product **ACCEPT**; required fixes none; optional N1 (vision workflow_ref naming) and N2 (taxRates GET breadth) deferred.

### Tests
- lint.sh pass (FIX-VERIFY).
- Offline test.sh **1628 passed** / 46 deselected (EXECUTE; **1628 passed** / 46 deselected (FIX-VERIFY).
- Live `tests/live/test_ui_bills_get_open.py` **1 passed** (EXECUTE; no API token).
- No ui-full / no node finish (complete false).

### Cleanup
- Live harness DELETE bill then contact; UI lists free of R18671 markers dual.
- Profiles ephemeral purged; vision frames outside repo, purge_verified true.

### Next unresolved coverage slice
- Prefer residual dual-count/NA: products.get only if detail surface dual-stable; special.invoice_email if durable dual holds; bills update/delete only with write dual+restore; no bulk greening without official schema; annual stays org_inaccessible red.
