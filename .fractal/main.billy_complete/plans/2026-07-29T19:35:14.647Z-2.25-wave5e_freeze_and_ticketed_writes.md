# 2.25 Wave-5e freeze and ticketed bill writes

## Objective

Promote the cited Grok brief into the Wave-5e offline contract for the six
documented singular bill and bill-line CUD rows, retain its authoritative root
Grok freeze acceptance, and start the two independent Codex Power product
leaves. Keep all live, UI, vision, bulk, special-route, and completeness rows
red throughout this cohort.

## Contract and boundaries

- Research authority is `.fractal/main.billy_complete/tmp/grok-research.md`.
  It re-fetches `https://www.billy.dk/api/` at ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes, and freezes exactly
  `api.bills.{create,update,delete}` and
  `api.billLines.{create,update,delete}`.
- Each API row has one preview name and a registered execute twin. The exact
  paths are `/bills` and `/billLines`, with `bill` / `billLine` singular
  request roots, `PUT` updates, and bodyless deletes.
- Bill parents have no offline additional response root. Bill-line specs must
  use optional `additional_plural_roots=("bills",)`; tests must cover both
  presence and absence without fabricating a parent record.
- Preserve opaque inner payloads, strict `extra="forbid"` outer Pydantic
  models, exact executor-name ticket binding, one shared root
  `ConfirmationStore` and `WriteProtocolService`, and zero write retries.
- The completed Wave-5d Codex fallback report is intentionally not merged: it
  is non-gating and its branch would import node seed material. The authoritative
  Wave-5d Grok acceptance already lives in `wiki/wave_fived_product_independent_review.md`.

## Execution sequence

1. Add `wiki/wave_fivee_ticketed_writes_contract.md`, patterned after the
   accepted Wave-5d contract. Record all six row/tool/path/root mappings,
   bill and bill-line field limits, protocol invariants, optional parent-root
   rule, coverage target (178 `api_*`, 136 offline rows), live ambiguities,
   exclusions, and explicit fail-closed claims. Run `wiki update` and
   `wiki lint` for the project wiki.
2. The contract was committed before the reviewer and implementation leaves
   were eligible to start. The authoritative root Grok `INDEPENDENT-REVIEW`
   accepted exact tip `fc8118e`; its durable record is
   `wiki/wave_fivee_freeze_independent_review.md`. The separate child exited
   without a review artifact and is not the source of this verdict. The Codex
   fallback is supplemental and non-gating. Product implementation may begin,
   while live, UI, bulk, and completeness coverage remains closed.
3. After the accepted freeze review is merged to root, initialize two Codex
   Power leaves with explicit `--agent=codex-power` and disjoint ownership:

   - `wave5e_bill_writes`: `src/billy_mcp/api/bill_writes.py` and
     `tests/api/test_bill_writes.py`.
   - `wave5e_bill_line_writes`: `src/billy_mcp/api/bill_line_writes.py` and
     `tests/api/test_bill_line_writes.py`.

   Both leaves copy the accepted invoice patterns but use the exact bill
   roots/fields from the freeze. They must test flat FastMCP schemas, preview
   non-mutation, exact paths/bodies, update-id matching, bodyless deletes,
   typed auth/not-found results, tamper/expiry/replay/target/organisation/
   payload binding, same-module executor mismatch, and no retry. Bill-line
   fixtures additionally prove optional `bills` mapping.
4. Root exclusively owns `src/billy_mcp/server.py`,
   `tests/api/test_bill_cross_executor.py`,
   `tests/unit/test_coverage_server.py`,
   `scripts/generate_coverage_report.py`, and generated coverage artifacts.
   After clean leaf review and labelled no-fast-forward merges, register both
   six-tool sets with the existing shared protocol, add bidirectional
   bill-to-line executor-mismatch tests, add exactly six implementation
   evidence entries, regenerate coverage, and assert 178 API tools / 136
   offline API rows while live and vision remain zero and `complete` remains
   false.
5. Verify formatter, Ruff, Pyright, contract suites, coverage generation and
   policy checks, the targeted cross-executor suite, and the complete non-live
   suite. Then request a separate Grok product review of the committed root.
   It must inspect the exact six routes, field-root separation, line parent
   response mapping, ticket binding and replay boundaries, registry/evidence
   arithmetic, and fail-closed coverage. No live or browser run is authorized
   without the dedicated non-production credentials.

## Delegation decision

The freeze review is an independent current-document task and therefore belongs
to Grok. The authoritative root review accepted the frozen contract. The two
product modules are now independent and belong to separate Codex Power leaves.
Root retains shared registration, generator, coverage, and cross-module safety
tests to avoid conflicting ownership.

## Verification targets

- Freeze: project wiki lint passes and the independent Grok page says ACCEPT
  only for the offline contract.
- Product: each leaf proves its local tool contract from a clean committed
  archive before integration.
- Root: registration, cross-executor, generator/inventory, formatter, Ruff,
  Pyright, and the non-live suite pass. Full mode must continue to fail closed
  until every live/UI/bulk/special requirement has real evidence.

## Post-Mortem

### Completed

- Promoted the cited Wave-5e bill and bill-line brief into
  `wiki/wave_fivee_ticketed_writes_contract.md`, preserving six red CUD rows,
  the fixed base host, ticket binding, opaque inner payloads, and optional
  bill-line parent mapping.
- Committed the freeze at `fc8118e` and configured the required independent
  Grok freeze-review node before any product leaf could start.
- Clarified the explicit denial of the documentation-only `api.billy.dk` sample
  host and removed a redundant tool-name sentence from the contract.

### Review findings and correction

- The separate Grok child exited before adding a review artifact; its empty
  branch is not the freeze-review authority.
- The authoritative authenticated root Grok `INDEPENDENT-REVIEW` accepted
  exact tip `fc8118e`. The restored durable verdict is
  `wiki/wave_fivee_freeze_independent_review.md`; it opens only the offline
  product implementation gate.
- The Codex Power fallback audit remains supplemental. It cannot replace the
  Grok ACCEPT or accept the resulting product implementation.

### Verification and cleanup

- Project wiki update and lint passed after the contract page was added.
- Repository lint/policy checks passed and the complete non-live suite passed:
  753 tests.
- No API token, UI credential, browser session, live mutation, raw browser
  evidence, or coverage greening was used. Full mode remains intentionally
  fail-closed.

### Next unresolved coverage slice

Implement the two disjoint Codex bill and bill-line product leaves, integrate
the 178-tool / 136-offline-row cohort at root, and request its separate Grok
product review. Live, UI, vision, bulk, special-route, and completeness rows
remain red until independently qualified.
