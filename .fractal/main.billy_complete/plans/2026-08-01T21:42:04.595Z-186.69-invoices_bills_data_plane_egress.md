# 186.69 invoices bills data plane egress

## Goal

Product research168 ACCEPT: scoped browser egress expansion only.

1. Commit **GET path_allow** on `api.billysbilling.com` for Billy SPA invoices and
   bills list/data settle (today blocked dual: `GET /v2/invoices` /
   `GET /v2/bills` (+ summary) → `net::ERR_BLOCKED_BY_CLIENT` under production
   egress).
2. Mirror the 186.66 products data-plane egress product shape: ship the plane
   **without** a new get-open tool until dual detail_ready is proven.
3. Keep greened `ui_invoices_list`, `ui_invoices_create_open`, `ui_bills_list`,
   `ui_bills_create_open` unchanged (no dual-count steal onto list/create tools
   for `invoices.get` / `bills.get`).
4. Do **not** mark `ui.parity.invoices.get` / `ui.parity.bills.get` (or
   update/delete/email) implemented, contract-live, or vision-green.

Target math (tip `dd3e427` / product 186.68): live/vision **169 → 169**;
implemented/contract **353 → 353**; complete **false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research168)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. invoices/bills Support get/list/create/update/delete/bulk. |
| TEMP dual | With probe-only path_allow: `GET /v2/invoices` + `summary` and `GET /v2/bills` + `summary` **ok dual**. Empty-org list shells valid (`Ingen fakturaer` / `Ingen udgifter`). |
| detail_ready | **false dual** (no rows). Do not green get parity. |
| products residual | UI seed dual; row/modal/soft URL detail_ready **false dual** — REJECT greening products.get this slice. |
| Prerequisite | Future disposable invoice/bill seed + get-open needs this plane first (same pattern as contacts/products egress before get-open). |
| Deferred | products.get/update/delete; invoices/bills get/update/delete; special.invoice_email; bulk 92; annual_reports. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research168_focus_dual.json`, `tmp/research168_modal_dual.json`.  
Temp egress (reference only): `tmp/research168_egress_temp.yaml`.

## Non-goals

- New FastMCP UI tool (`ui_invoices_get_open`, `ui_bills_get_open`, etc.)
- Greening any `ui.parity.invoices.*` / `bills.*` get/update/delete rows
- POST/DELETE path_allow for invoices/bills (no SPA seed/cleanup plane this slice)
- `/v2/invoiceLines` / `/v2/billLines` GET unless EXECUTE live list network proves
  empty shell fires them (prefer **minimal** invoices+bills GET only)
- `POST …/emails` / invoice_email path_allow
- `integrations/billing/*` allow (stay denied)
- Full `browser_action: allow` on `api.billysbilling.com`
- products.get greening; bulk/annual green; live API; headed browser
- Children / codex-power / non-grok agents
- Completeness / `node finish`

## Ownership

| Area | Owner |
| --- | --- |
| `coverage/browser_egress.yaml` path_allow + evidence | Root Grok EXECUTE |
| Unit allow-matrix (`tests/unit/test_browser.py`) | Root Grok EXECUTE |
| Coverage inventory egress assertions | Root Grok EXECUTE |
| Live list reconfirm (invoices + bills) offline-safe checks | Root Grok EXECUTE |
| Wiki residual note (optional short page or shell update) | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root.

## Contract freeze (research168)

### A. Browser egress (`coverage/browser_egress.yaml`)

Host `api.billysbilling.com` remains `browser_action: path_allow` and
`api_client_action: exclusive_allow`.

**Keep** existing auth/bootstrap + contacts GET/POST/DELETE + countries GET +
products GET/POST/DELETE + accounts GET + salesTaxRulesets GET.

**Add** (minimum dual-proven set):

| match | methods | path | purpose |
| --- | --- | --- | --- |
| prefix | GET | `/v2/invoices` | list + summary SPA reads (`/summary` under prefix) |
| prefix | GET | `/v2/bills` | list + summary SPA reads |

**Keep denied** (explicit regression — flip only the GET allow assertions above):

- `POST /v2/invoices/…/emails` (invoice_email later)
- POST/PUT/DELETE on `/v2/invoices` and `/v2/bills` (not opened this slice)
- `GET /integrations/billing/*` (still blocked dual; not required)
- `api.billy.dk` deny
- `default_action: deny`

Update host `evidence` / `condition` / `purpose` / `test_references` to cite
research168 dual + live list tests. Do not claim get-open or live API.

### B. No new tool / no coverage green

| Item | Value |
| --- | --- |
| New tool_name | none |
| Parity rows greened | none |
| `ui.parity.invoices.get` / `bills.get` | remain discovery_required / red |
| API `live_tested` | stays false + `out_of_scope_by_user` |

### C. Tests to update

1. **`tests/unit/test_browser.py`**
   - `test_browser_policy_real_manifest_allows_contacts_data_plane` (rename or
     extend docstring to contacts/products/invoices-bills):
     - `assert policy.allows("…/v2/invoices", "GET")`
     - `assert policy.allows("…/v2/invoices/summary", "GET")` (prefix)
     - `assert policy.allows("…/v2/bills", "GET")`
     - `assert policy.allows("…/v2/bills/summary", "GET")`
     - keep `assert not policy.allows("…/v2/invoices/x/emails", "POST")`
     - keep write methods denied if policy is method-sensitive
2. **`tests/coverage/test_coverage_inventory.py`**
   - Assert path_allows include GET `/v2/invoices` and GET `/v2/bills`
   - Assert complete still false; counts 353/169 unchanged
3. **Live reconfirm (if credentials available during EXECUTE/FIX-VERIFY)**
   - `tests/live/test_ui_invoices_list.py` and `test_ui_bills_list.py` still pass
   - Optional: network observer that list visit does not fail on `/v2/invoices`
     or `/v2/bills` with ERR_BLOCKED_BY_CLIENT (no token logging)
4. **Offline suite** must pass before commit: lint + `test.sh` default offline
   mode (no live API).

### D. Wiki (minimal)

Short durable note under wiki (new page or append residual shell docs):

- invoices/bills SPA GET data-plane path_allow is open (research168)
- get/update/delete parity still red until dual detail_ready after seed
- Never describe as live API verified

## EXECUTE checklist

1. Edit `coverage/browser_egress.yaml` only for the two GET prefix rules +
   evidence text (JSON-as-YAML file; preserve formatting style of neighbors).
2. Flip unit/coverage assertions that currently require invoices/bills GET deny.
3. Run offline unit + coverage tests for browser egress.
4. Run lint.sh; run offline test.sh.
5. If live credentials present: reconfirm invoices/bills list live tests.
6. Do **not** regenerate coverage to green get rows; run coverage generator only
   if status evidence strings need refresh without changing complete/counts.
7. Wiki note scrubbed of iteration labels where durable.
8. Ready IR: confirm no false green, path_allow minimal, emails still denied.

## IR / FIX-VERIFY / COMMIT gates

- IR ACCEPT only if: GET invoices+bills allowed dual-proven; emails/writes still
  denied; no parity greening; complete false; no secrets.
- FIX-VERIFY: offline suite green; live list optional reconfirm.
- COMMIT message theme: `invoices bills data plane egress` (or fractal commit
  default for 186.69). Push via `fractal commit`.
- Not node finish.

## Residual after this slice

1. Disposable invoice/bill seed dual → `ui_invoices_get_open` /
   `ui_bills_get_open` if detail_ready dual.
2. products.get only after dual detail hydrate.
3. special.invoice_email after invoices detail + email path research.
4. bulk 92 external-contract; annual org_inaccessible.

## Post-mortem (fill after COMMIT)

- Shipped:
- Math actual:
- Deviations from plan:
- Next residual:

## Decision

**PLAN 186.69 = research168 ACCEPT: invoices+bills GET path_allow only.**  
No get-open tool. No children. complete stays false.

## Post-Mortem

### Work completed
- Committed-ready path_allow: prefix GET `/v2/invoices` and `/v2/bills` on
  `api.billysbilling.com` (covers `/summary`).
- Generator embed in `scripts/generate_coverage_report.py` kept in lockstep.
- Unit matrix + coverage inventory asserts allow GET and deny POST writes.
- Wiki: `invoices_bills_data_plane_egress.md`, auth egress note, list shell refs.
- No new UI tool; no parity greening for invoices.get / bills.get.

### Deviations
- Optional live list reconfirm (N2) not run; offline suite + research168 dual
  used as path-rule evidence.
- Optional email-GET narrow under invoice prefix (N1) left as documentation only.

### Review findings
- IR product **ACCEPT** (`tmp/grok-review.md`); required fixes: none.
- Overall completeness **FAIL** (expected).

### Tests
- lint.sh pass.
- offline test.sh **1620 passed**, 44 deselected (EXECUTE + FIX-VERIFY).
- wiki lint: wiki + memory clean.
- complete false; live/vision 169; implemented/contract 353.

### Cleanup state
- Research168 dual cleanup products clean; no durable markers left by product.
- No raw frames (no new vision product).

### Next unresolved coverage slice
1. Disposable invoice/bill seed dual → get-open if detail_ready.
2. products.get only after dual detail hydrate.
3. special.invoice_email after invoices detail.
4. bulk 92 external-contract; annual org_inaccessible.
