# 2.19 Wave-5b product review superseded by P1/P2 findings

## Objective

Independently review the integrated Wave-5b offline ticketed-write product on
root without implementing production code.

## Verdict

- Original Wave-5b offline product ACCEPT at `920ceab`: **SUPERSEDED**
- P1: an account-create ticket could execute through another executor
- P2: terminal ticket and prepared-request state was not bounded
- Required: shared-protocol repairs, full offline gates, commit, fresh Grok review
- Durable record: `wiki/wave_fiveb_product_independent_review.md`

## Checks performed

- Official docs fingerprint re-fetch (unchanged)
- The original nine-row offline check omitted invoked-executor identity
- Fallback reproduction showed a cross-module ticket execution bypass
- Bulk/UI/specials/postings/natures remain fail-closed

## Post-Mortem

The initial Grok verdict missed P1 executor binding and P2 terminal-state
retention. The fallback review correctly rejected the product and overrides the
original ACCEPT. The root candidate binds all 24 execute handlers, rejects
cross-executor calls without HTTP or consumption, and prunes expired bindings,
replay markers, and terminal request bodies while preserving live errors. It
passes the full non-live suite (650 tests), but it has no fresh independent
acceptance. Wave-5c, live, UI, vision, bulk, specials, and other writes remain
blocked or red.
