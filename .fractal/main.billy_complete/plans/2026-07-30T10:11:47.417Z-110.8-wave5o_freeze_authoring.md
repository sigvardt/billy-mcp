# 110.8 Wave-5o invoice-reminder freeze authoring

## Objective

Land a cited, fail-closed Wave-5o contract page that freezes only the future
singular `invoiceReminders` create preview/execute pair. The page is an
authority boundary, not a product delivery: it must leave source, tests,
coverage inventory/status, live, UI, vision, bulk, delete, and completeness
states unchanged and red where they are red today.

## Accepted evidence and boundaries

- `tmp/grok-research.md` research64 is the current cited official-doc brief.
  The official HTML is byte-identical to research63: ETag `wcw4x9hqvu3603`,
  MD5 `8b94b0135c91fd15fe54ea33e088a4be`, body 147934 bytes.
- The prerequisite product authority is
  `wiki/wave_fiven_product_independent_review.md`: Wave-5n singular
  invoice-late-fee create/update is **ACCEPT** offline only.
- For `/invoiceReminders`, `POST {}` reaches 401 `AUTHENTICATION_REQUIRED`;
  PUT, singular DELETE, and bulk DELETE with `ids[]` return 405. The official
  Supports list is get, list, create, bulk save, bulk delete; it omits update
  and singular delete.
- Therefore the sole frozen operation is `api.invoiceReminders.create`, with
  future tools `api_invoice_reminders_create_preview` and
  `api_invoice_reminders_create_execute`, client-relative `POST
  /invoiceReminders`, request root `invoiceReminder`, and required success root
  `invoiceReminders`.
- The inner `invoiceReminder` map remains opaque. Its documented outer
  boundaries include readonly `createdTime`, unproven `downloadUrl`, and no
  promise about `sendEmail` effects or `associations` nesting. No relation wire
  form, date/number coercion, or response extras is frozen.
- The root remains 254 `api_*` tools, 174 implemented/contract-tested API rows,
  zero live/vision rows, 92 ambiguous bulk rows, and `complete: false`.

## Delegation decision

The authority page is a focused, independently reviewable deliverable with
clear file ownership, so a `codex-power` leaf owns it. The upcoming freeze
review cannot begin before this committed page exists, and product code cannot
begin before that review accepts it; both remain parent-managed gated work.

The leaf owns only `wiki/wave_fiveo_ticketed_writes_contract.md` plus its own
node seed. It must not edit `coverage/**`, `src/**`, `tests/**`, server
registration, existing Wave-5n records, or the root wiki index. Root owns the
research64 baseline, merge reconciliation, `wiki/_index.md`, freeze review
launch, and all later implementation decisions.

After the research64 baseline is committed, launch:

```bash
fractal node init wave5o_invoice_reminder_freeze \
  --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete \
  --agent=codex-power --max-cost=320 --max-iter-cost=80 --max-iters=8 \
  --step-timeout=20m --max-depth=0 --max-children=0 --max-descendants=0
```

Configure and commit its seed before starting. Its completion condition is
observable on its own branch: the one contract page is committed, wiki lint is
clean, the page contains exactly the two authorised future tool names and one
inventory operation, and no prohibited source/coverage/test changes exist.

## Execution plan

1. Preserve and validate the pending research64 baseline: memory state,
   `wiki/offline_write_probe_rules.md`, and the research64 plan. Run the normal
   non-live lint, type, test, coverage, policy, and dependency-audit checks,
   then commit it before creating the child so its branch inherits the cited
   evidence.
2. Initialise/configure/start the Codex Power leaf. Its NODE.md must encode the
   exact research64 contract: strict `{invoiceReminder: map}` preview,
   ticket-only execute, one no-retry write, required-root-only mapping, opaque
   inner map, all field boundaries, explicit 401/405 method gates, empty-tool
   red bulk rows, no webhook, and no product/coverage edits.
3. Monitor the leaf. On completion, inspect its committed diff from a clean
   archive and merge only if it is limited to the designated contract page and
   independent checks pass. Regenerate/lint the root wiki index after merging.
4. Once the page is on root, launch an independent `grok` freeze-review leaf.
   It must recheck current official documentation, inspect the committed page,
   and write an authoritative ACCEPT or REJECT review record without editing
   product code. If Grok fails before edits due to authentication, quota, or a
   rate limit, repeat the full review with a `codex-power` leaf; never switch
   reviewers after a review page has been edited.
5. Keep Wave-5o product code blocked unless that independent freeze review is
   ACCEPT. A later product leaf may add only the two create tools, correct the
   create cleanup wording while greening the row, and must retain all red
   boundaries.

## Acceptance checks for this iteration

- Research64 provenance and the maintained offline-probe rules are committed
  without weakening any coverage state.
- The freeze authoring leaf has an explicit Codex Power route, isolated
  ownership, sufficient close-out budget, and committed configuration.
- No live request, browser action, credentials, mutable data, product tool,
  server registration, coverage green, bulk/delete/association write, webhook,
  or generic control is introduced.
- Root continues to report `complete: false` and retains the planned independent
  review gate.

## Post-Mortem

- Research64 provenance, probe-rule maintenance, and the private state update
  passed root formatting, Ruff, Pyright, coverage, repository-policy,
  non-live-test, and dependency-audit checks without changing code or coverage
  state.
- Review64 independently accepted the cited create-only authoring package as
  research, while explicitly rejecting any interpretation as a freeze, product,
  live, UI, vision, bulk, or completeness acceptance. Its durable record is
  `wiki/wave_fiveo_freeze_authoring_research_independent_review.md`.
- The scoped authoring child is active; its single-page delivery is still the
  next gate. Root introduced no product tools or coverage changes, and the
  subsequent independent freeze review remains required before implementation.
- Final root verification for this record: `lint.sh` passed Ruff, Pyright,
  coverage-inventory, and repository-policy checks; `test.sh` passed all 1,094
  non-live tests. Regenerating `wiki/_index.md` resolved its ordering drift;
  `wiki lint` now passes for both project wiki and private memory. No live,
  browser, or full-suite run was appropriate because `complete` remains false.
