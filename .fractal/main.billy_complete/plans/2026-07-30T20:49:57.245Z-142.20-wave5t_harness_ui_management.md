# 142.20 Wave-5t harness and UI/auth discovery management

## Objective

Preserve the Research89 contract reconfirmation, then actively manage the two
non-overlapping Wave-5t leaves to independently verified, mergeable states.
This iteration is infrastructure/discovery only: it must not add residual or
bulk MCP tools, advance coverage, or claim live or vision qualification.

## Evidence and boundaries

- Research89 in `.fractal/main.billy_complete/tmp/grok-research.md` reconfirms
  the official API document fingerprint (`8b94b0135c91fd15fe54ea33e088a4be`)
  and keeps residual clear 29 plus bulk 92 red.
- The only API client base is `https://api.billysbilling.com/v2`; the browser
  surface is restricted to the established Billy allowlist. The unauthenticated
  `/user/organizations` 404 is a live qualification risk, not a product
  correction or coverage result.
- UI discovery is login-only without stored credentials. Existing evidence
  supports stable name selectors and both English and Danish submit labels, but
  not post-login workflow or vision claims.
- `BILLY_API_TOKEN`, UI credentials, and an explicit non-production
  organisation are unavailable. The harness must remain no-token/no-network
  and fail closed; no disposable record, raw browser artifact, or secret may
  be written to the repository.

## Delegation decision

No additional child is appropriate. The live-gate harness and UI/auth
discovery have disjoint ownership and are already active. A third child would
duplicate their product/research boundary and complicate review. The original
Grok discovery attempt failed before edits because of authentication; the
Codex Power retry is explicitly recorded as a fallback and cannot satisfy a
future Grok audit requirement.

## Execution plan

1. Treat the current root wiki and memory edits as the Research89 handoff.
   Validate that they contain only cited, non-sensitive reconfirmation facts,
   retain the coverage truth (184 implemented/contract-tested, zero live and
   vision), and run the project wiki/memory checks before committing them.
2. Monitor `wave5t_live_gate_harness`. Require its committed diff to be limited
   to the declared harness source, runner, and focused tests; ensure its
   no-token test makes no network call and its future token path demands both
   the fixed API base and an explicit non-production organisation. Do not merge
   while it is active or if it only contains a plan/log.
3. Monitor `wave5t_ui_auth_discovery_fallback`. Require a cited discovery page
   with observed headless login/session facts, no profile or raw-evidence
   leakage, no UI coverage movement, and an explicit statement that Codex
   fallback findings are not a Grok audit. Do not merge a seed-only or failed
   branch.
4. When either leaf finishes, inspect its three-dot diff and verify executable
   artifacts from a clean `git archive`; run its focused tests plus root
   non-live checks. Merge only a scoped passing child with a labelled no-ff
   merge, run `wiki lint`, and announce the material integration.
5. After a product merge, request a Grok independent review if Grok becomes
   available. Until then, preserve the implementation as unreviewed
   infrastructure/research and keep all live/UI/vision rows red.

## Acceptance criteria

- Research89 root records are lint-clean, non-sensitive, and accurately state
  the new `/user/organizations` and login-locale observations without turning
  either into a green claim.
- Both active leaves are either progressing under their stated boundaries or
  explicitly stopped with their failure cause recorded; no unexamined exited
  child remains.
- Any merged child has a narrow reviewed diff and passing relevant tests.
- `coverage/status.json` remains incomplete with zero live and vision evidence;
  residual/bulk/UI rows remain red absent real dedicated-organisation proof.

## Post-Mortem

- Completed: promoted cited Research89 context, isolated pytest modules with
  importlib mode, obtained Review89, archive-verified the exact four-file
  harness diff, and integrated it in `7571cec`. The clean archive focused run
  reported **10 passed, 1 opt-in live skip**.
- Review disposition: Review89 accepts Research89 and the harness only as
  research/infrastructure. The OPTIONS-only observations remain explicitly
  non-qualifying, omit method-level 401/405 reproof, and exclude
  `GET /user/organizations`.
- Deviation: the original Grok UI discovery failed before edits because
  authentication was unavailable. The active Codex fallback is bounded to a
  scrubbed discovery brief and cannot satisfy the required future Grok audit.
- Verification and cleanup: no coverage flags were changed; no live token,
  browser credential, raw evidence, or disposable record was used. The merged
  child seed is removed from the root tree while its reviewed source remains in
  Git history. Root formatting, Ruff, Pyright, inventory, and repository-policy
  checks pass; the non-live suite reports **1265 passed, 1 deselected**.
- Next unresolved coverage slice: obtain dedicated non-production credentials
  and organisation identity, then qualify the residual 29 and bulk 92 with
  real method-level evidence and conduct post-login UI/vision workflows.
