# 198.63 currencies ticketed create and update

## Pivot

`47B85E43` is binding under `9B979A05`. HEAD `5e78f1d` is a clean
198.62 countries close, not a `(failed on <step>)` backstop.
Product tree matches origin. Uncommitted files are node memory only
(`state.md`, `todo.md`, `decisions.md`). Research brief:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official
scratch fetch this slice:
`billy-api-docs-research-47b85e43.html`,
`billy-api-docs-chunk-research-47b85e43.js`,
`research-47b85e43-currencies-section.js`.

Official https://www.billy.dk/api/ still matches lock ETag
`tmhc6wpdc835zt` / MD5 `053f755f52e3926b028e29325e3670d4`
(147924 bytes). Chunk
`/_next/static/chunks/pages/api-docs-2c3fd2ccd1de2fed.js` is
byte-identical to the countries lock (MD5
`7902955e4d1a56ecea2d85551f17b49c`). Anchor `#v2currencies`.
Supports: `get by id, list, create, update, bulk save, bulk
delete`. No singular delete. Property table is exactly two rows
with empty notes: `name` string, `exchangeRate` float.

Unauth POST/PUT 405 must not override that table. Residual honesty
that left these two rows toolless is superseded for create/update
only. Bulk stays `BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`. Do not
invent `api.currencies.delete`.

Do not spawn. Do not remake UI dumps. Do not invent
`ui_currencies_*` or bulk tools. Do not fold this into
`reference_reads.py`. Do not start saved `05F3200D` locales until
this commit lands. Do not finish. No live API.

## Goal

Land one proved cohort: ticketed offline `api.currencies.create`
and `.update`.

| Inventory id | Preview / execute | Path | Request |
| --- | --- | --- | --- |
| `api.currencies.create` | `api_currencies_create_preview` / `_execute` | `POST /currencies` | nested optional `{name, exchangeRate}` |
| `api.currencies.update` | `api_currencies_update_preview` / `_execute` | `PUT /currencies/:id` | non-empty `id` plus the same nested object |

`live_tested` stays false with
`qualification.live_api=out_of_scope_by_user`. `complete` stays
false. Residual honesty remaining: 12 method-closed + 2 readonly
map + 2 meta-delete = 16. Bulk 92 stay red. Implemented 536,
contract 541 (HEAD 534 / 539). Live/vision stays 339.

## Why no child

Work shares `src/billy_mcp/server.py`,
`scripts/generate_coverage_report.py`, and
`tests/coverage/test_coverage_inventory.py`. A child would fork
those. Seven write children stay completed and merged. Shared
`WriteProtocolService` already binds POST/PUT with a singular JSON
root. Typed optional nested payload copy shape is
`src/billy_mcp/api/city_writes.py`. Countries are the just-landed
twin in `src/billy_mcp/api/country_writes.py`.

## TDD

On HEAD, add failing tests first. Capture the expected failure.
Then implement.

1. New `tests/api/test_currency_writes.py`, copied from countries
   for the typed optional nested payload:
   - Register count is 4. Preview fields:
     `api_currencies_create_preview` `{currency}`,
     `api_currencies_update_preview` `{id, currency}`,
     both executes `{confirmation_ticket}`. Nested properties are
     exactly `{name, exchangeRate}`. JSON schema types: `name` is
     string; `exchangeRate` is number. `additionalProperties` is
     false on outer and nested schemas. No `input` wrapper.
     Nested `required` is empty or absent.
   - Outer extras, empty update `id`, `currencyId`, a JSON array
     for `name` or `exchangeRate`, nested objects, and other inner
     extras fail `ValidationError` / FastMCP `extra_forbidden`.
     Execute extras fail. Preview still makes no HTTP.
   - Empty nested object `{"currency": {}}` is valid. Partial
     payloads such as `{"name": "X"}` or `{"exchangeRate": 1.5}`
     are valid. Do not require `name` or `exchangeRate`.
   - Preview create sends no HTTP. Canonical request is
     `{"currency": { ...set keys only... }}`. Expected effect
     `{action: create, resource: currency}`.
   - Preview update sends no HTTP. Canonical request is the same
     nested object. Expected effect includes `id`.
   - Combined FastMCP schema: call tools through `server.call_tool`,
     not by constructing models only.
2. New `tests/api/test_currency_write_tickets.py`, copied from
   country ticket tests. Keep each file under 250 lines:
   - Execute POSTs
     `https://api.billysbilling.com/v2/currencies` with the
     exact nested JSON. Execute PUTs
     `/v2/currencies/<urlencoded id>`. Encoded id `dk /?`
     becomes `/v2/currencies/dk%20%2F%3F`.
   - Success fixture maps only declared changed root
     `currencies` plus optional `meta.deletedRecords`. Do not
     expose a top-level `currency` write success field.
   - Tamper, wrong executor (create ticket on update execute),
     replay, expiry, missing auth (`token=""`), and failed write
     not retried. Preview and missing-auth execute make no network.
3. `tests/coverage/test_coverage_inventory.py`
   `test_residual_clear_honesty_rows_are_toolless_and_qualified`:
   `len(method_closed) == 12`, residual `== 16`. Assert
   `api.currencies.create` and `.update` are **absent** from
   `RESIDUAL_METHOD_CLOSED_IDS`. Keep bulk currencies rows
   unspecified. Bump every hardcoded `implemented_rows == 534` /
   `contract_tested_rows == 539` in that file to **536 / 541**
   (seven pairs). `complete` stays false. Live/vision counts stay
   339.
4. Map both ids in `OPERATION_TEST_REFERENCES` to
   `tests/api/test_currency_writes.py`,
   `tests/api/test_currency_write_tickets.py`, plus
   `SERVER_REGISTRY_TEST_REFERENCE`, next to the existing
   `api.currencies.get` / `.list` rows. On HEAD `tool_name` is empty
   and the rows are method-closed; the new mapping should fail
   until honesty lets the preview names through.
5. Add
   `test_currencies_writes_are_ticketed_offline_and_not_live_tested`
   next to the countries inventory tests.

Run:
`uv run pytest tests/api/test_currency_writes.py tests/api/test_currency_write_tickets.py tests/coverage/test_coverage_inventory.py -q --tb=line`
and capture the first red.

## Implement

`src/billy_mcp/api/currency_writes.py` (new), copy
`country_writes.py`:

- `CurrencyPayload`: `name: str | None = None`,
  `exchangeRate: float | None = None`, `extra=forbid`, frozen.
  Do not set `strict=True`.
- Create preview input: `{currency}`.
- Update preview input: `{id min_length=1, currency}`.
- Dump with `exclude_none=True, exclude_unset=True`. Empty
  nested object dumps to `{}`.
- Register the four tools. Reuse `WriteProtocolService`. Do not
  add a second protocol.
- Collection path `/currencies`. Singular root `currency`.
  Plural root `currencies`. `organization_id=None`.

`src/billy_mcp/server.py`: import and call
`register_currency_write_tools` next to
`register_country_write_tools`. Do not change reference
reads.

`scripts/generate_coverage_report.py`:

- Remove `api.currencies.create` and `.update` from
  `RESIDUAL_METHOD_CLOSED_IDS`.
- Add `OPERATION_TEST_REFERENCES` for both ids.
- Do not touch bulk helpers, the other 12 method-closed ids,
  readonly map, or meta delete.
- Keep `RESOURCE_SUPPORTS` currencies create/update true,
  delete false (`("currencies", True, True, False)` already).
- Regenerate with `python scripts/generate_coverage_report.py`.
  Do not edit `coverage/status.json` by hand.

Wiki (durable, no iteration labels):

- `wiki/residual_clear_method_closed_inventory_honesty.md`:
  method-closed remaining is 12. These two rows are ticketed
  offline tools. Residual honesty remaining 16. Not completeness.
- `wiki/offline_write_probe_rules.md`: Supports create/update plus
  a writable field map of optional string `name` and float
  `exchangeRate` is enough to ship the ticketed POST/PUT. Reject
  `currencyId`. Unauth 405 is not the contract. Empty payload is
  allowed.

Keep `wiki/ui_currencies_locales_not_applicable.md` as history. Do
not remake dumps.

## Bounds

- No live API. No headed browser. No UI dumps. No finish.
- `api.currencies.bulk_save` / `.bulk_delete` stay
  `ambiguous_bulk`.
- No `api.currencies.delete`. Supports does not list singular
  delete.
- UI parity stays `not_applicable` (research139).
- Do not accept `currencyId` or a JSON object/array for `name`
  or `exchangeRate`.
- Do not require any nested field.
- Do not add `strict=True` on the payload.
- `complete` stays false.
- `live_probe.py` may still list these ids in the historical
  unauth 405 matrix. That is not the contract.
- Saved `05F3200D` locales create/update wait until this commit.

## Independent review

After EXECUTE, review must re-parse `#v2currencies` on the
locked HTML/chunk and confirm:

1. Supports still lists create and update, not singular delete.
2. Property table is still two optional fields: `name` string,
   `exchangeRate` float.
3. Tools exist only as preview plus execute for those two ops.
4. Inventory `tool_name` values are the two `*_preview` names.
5. Execute paths are `POST /currencies` and `PUT /currencies/:id`.
6. Nested payload rejects `currencyId`, arrays, nested objects, and
   extra keys. Empty nested object is valid. JSON schema type
   `number` holds for `exchangeRate`.
7. `live_tested` is false.
8. Remaining residual honesty is 16 toolless rows.
9. No 405-based green. No delete or bulk tools. No UI tools.

## Post-Mortem

Work completed: ticketed offline `api.currencies.create` and
`.update` in `src/billy_mcp/api/currency_writes.py`. Preview
tools take a nested payload of optional string `name` and float
`exchangeRate` with `extra=forbid`. `currencyId`, arrays, nested
objects, and extra keys fail at the FastMCP boundary. Empty
nested object is valid. Execute binds `POST /currencies` and
`PUT /currencies/:id` through the shared write protocol.
Coverage regenerated: implemented 536, contract 541, live/vision
339, complete false. Residual honesty remaining 16.

Deviations: none from the official field table. Ticket and
execute contract tests live in
`tests/api/test_currency_write_tickets.py` so each test file
stays under the 250-line ceiling.

Review findings: independent Grok review PASS
(`.fractal/main.billy_complete/tmp/grok-review.md`). Required
fixes: none. Official `#v2currencies` still lists create and
update, two optional fields, no singular delete.

Test results: lint passed. Commit-mode `test.sh` passed 2287
deselected-live/vision. Do not run `BILLY_TEST_MODE=full` or
`ui-full`; completeness is still false.

Cleanup: no live records, no frames. Next unresolved coverage
slice after COMMIT is saved `05F3200D` locales create/update.
Do not finish.

