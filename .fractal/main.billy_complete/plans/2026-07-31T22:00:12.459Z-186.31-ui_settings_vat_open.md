# 186.31 ui settings vat open Momssatser panel with dual live and vision


## Goal

Ship the next **read-only VAT settings open-shell** FastMCP tool for Billy's
Indstillinger **Momssatser** panel, dual-confirmed in research130 as final path
`/:org_slug/settings` opened via hub open then observe-only side-nav click
**`Momssatser`**, with h1 **`Indstillinger`** and VAT panel h2 triad
(`Regelsæt`, `Satser for salg`, `Satser for køb`), distinct from the company
default panel and the Regnskab / Faktura / Profil panels, with offline contract
tests, dual-session live interface tests (no API token), and a non-sensitive
vision record that is purged after review.

Cited authority: `.fractal/main.billy_complete/tmp/grok-research.md` (research130).  
Discovery: `.fractal/main.billy_complete/tmp/discovery130/summary.json` +
`summary_dual_settings_click.json`; `api_token_used: false`;
`write_cta_clicks_attempted: false`; dual click path/h1/h2 match for Momssatser;
frames purged; PII scrubbed in brief.  
Prior product tip: settings user 186.30 (`9a0b180`) `ui_settings_user_open`.  
Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
§10.3 settings route family (vat / Momssatser).  
Docs fingerprint: ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`
(unchanged since research103–130).

**Research130 decision:** remaining settings panels (vat, users, access_token,
beta; subscription weak) are **not** reachable by soft URL seeds. Soft
`settings/vat`, `settings/moms`, `settings/tax`, `settings/sales-tax`,
`settings/salestax` soft-empty. Hub bare `settings` is **company default**.
Product **must** open via authenticated hub `/:org_slug/settings` then
exact-text / role click of side label **`Momssatser`** (observe-only; never
write CTAs). Success requires the **strict h2 triad** and **must reject**
company, accounting, invoicing, user/Profil, and other settings panels. Soft
nested rejects include the vat/tax seeds above plus accounting/invoicing seeds
as wrong panels. No official settings API — do **not** invent `api_settings_*`.
Do **not** re-implement salesTax API tools in this slice. This product is
**open-shell classification only**. Write CTAs (`Opret`, `Opret regelsæt`,
`Opret sats`) are **observe-only** — never click.

## Why this slice

- UI live/vision is 32 (through settings user).
- Research130 dual headless sessions:
  - Click **Momssatser** from hub → path `/:org_slug/settings`, h1
    `Indstillinger`, dual path/h1/h2 match true.
  - Panel h2 dual-matched triad: `Regelsæt`, `Satser for salg`,
    `Satser for køb`.
  - Body chrome (scrubbed): ruleset description, `Opret regelsæt`, `Opret sats`,
    `Vis arkiveret`, side rail labels.
  - Soft seeds `settings/vat|moms|tax|sales-tax|salestax` soft-empty — **reject**.
  - Hub default company panel — **reject**.
  - Control clicks: Virksomhed → company; Regnskab → accounting (Kontoplan) —
    both dual false for vat success.
  - `beta_panel_success_candidate` can false-positive from side-nav labels in
    body — product **must not** use loose Betas markers; use h2 triad only.
  - Also dual-frozen for **later** tips (not this product): Brugere,
    Adgangsnøgler, Betas (research129).
  - Abonnement: empty h2 — **keep red**.
  - annual_reports remains non-success — **do not green**.
- No official Billy API v2 settings resource. Offline `api_sales_tax_*` tools
  already producted separately and are **not** greened or reinvented by this UI
  open tool; **not** invented here as `api_settings_*`.
- Target still red: `ui.discovery.settings_vat` only for this product.
- Hard ban: never click Opret* / Gem* / Tilføj* / Slet / Upload / Opgrader /
  Inviter*.
- API live remains out of scope (`live_tested: false` / `out_of_scope_by_user`).

## Non-goals

- Greening other `settings_*` rows (users, subscription, access_token, beta)
  or re-greening company/accounting/invoicing/user from this panel open.
- Producting settings write / ticketed Opret regelsæt / Opret sats / archive
  toggles.
- Inventing any `api_settings_*` or webhook tools; rewriting salesTax API.
- Treating bare hub open (company default) or soft `settings/vat` as VAT
  success.
- Producting annual_reports or Abonnement (not ready).
- Expanding browser egress hosts beyond existing allowlist.
- Live API traffic or `BILLY_API_TOKEN` use.
- Expanding auth tools; generic browser MCP tools.
- Setting `complete: true` or greening all remaining UI rows.
- Headed browsers; credential or file-path tool inputs.
- Children (file ownership is one tight cluster on root).
- Residual bulk API reds.

## Ownership

**Root Grok only. No children.** File cluster mirrors settings user open:

| Area | Paths |
| --- | --- |
| Models | `src/billy_mcp/models.py` |
| Browser workflow | `src/billy_mcp/browser.py` |
| Server registration | `src/billy_mcp/server.py` |
| Offline unit | `tests/unit/test_browser.py`, `tests/test_models.py`, `tests/unit/test_coverage_server.py` |
| Live | `tests/live/test_ui_settings_vat_open.py` (new; pattern from user open; hub+click Momssatser; hard-ban write CTAs; reject company default + accounting + invoicing + user + soft aliases) |
| Coverage inventory gate | `tests/coverage/test_coverage_inventory.py` tool-name map for `discovery.settings_vat` |
| Coverage generator | `scripts/generate_coverage_report.py` — add
  `apply_ui_settings_vat_open_shell_evidence` for
  **`ui.discovery.settings_vat`** only; wire family `settings_vat` in
  discovery apply path; ensure `build_browser_egress` retains prior lives
  (including settings company + accounting + invoicing + user) + appends vat open |
| Vision | reuse `src/billy_mcp/vision_evidence.py` |
| Inventory | `coverage/ui_workflows_manifest.yaml` — only via generator for that row |
| Egress | `coverage/browser_egress.yaml` — **append** live test path to existing
  hosts; **must not drop** prior UI live tests (settings user, invoicing,
  accounting, company, inventory, integrations, addons, saft_exports, exports,
  vat_declarations, reports, transactions, daybooks, financing, bank recon,
  receipt, uploads, …) |
| Status/report | regenerate via existing generator; **do not** set `complete: true` |
| Wiki | `wiki/ui_settings_vat_open_shell.md` after product |

Do **not** spawn residual/bulk, annual, multi-settings product, or children this
iteration.

## Contract freeze (research130 vat / Momssatser open shell)

### Tool

- **Name:** `ui_settings_vat_open`
- **Input:** empty strict model (`extra=forbid`) — no URL, selector, org id,
  credential, local file path, digest, destination, or write payload.
- **Success:** non-PII only:
  - `path_class`: `/:org_slug/settings` (canonical class without query; bare
    hub leaf; SPA does **not** keep `/settings/vat`).
  - `heading`: `Indstillinger` (exact).
  - `shell_kind`: literal `settings_vat`.
  - `vat_panel_markers_present`: bool true when required markers are
    visible — require h2 set includes **`Regelsæt`**, **`Satser for salg`**,
    and **`Satser for køb`** (strict triad; all three).
  - Must be **distinct from company**: company panel h2 pair
    (`Navn og adresse` + `Kontaktinformation`) must **not** both classify as
    the active panel signature.
  - Must be **distinct from accounting**: accounting panel trio
    (`Regnskab` + `Køb` + `Kontoplan`) must **not** classify as success
    (note: accounting also shows `Momsopgørelse` — not VAT panel success).
  - Must be **distinct from invoicing**: invoicing panel
    (`Faktura` + `Produkter` + (`Betalingsmetoder` or `Standard fakturalogo`))
    must **not** classify as success.
  - Must be **distinct from user/Profil**: user panel
    (`Profil` + `Billede` + `Sprog og tema` + `Skift adgangskode`) must
    **not** classify as success.
  - Optional observe-only: `create_cta_markers_present` when `Opret` /
    `Opret regelsæt` / `Opret sats` text is visible (**no page dump**, no
    rate names that look like customer PII, no org secrets).
- **Errors:** `AUTH_REQUIRED` (login signature), `AUTH_INTERACTION_REQUIRED`
  (challenge selectors), `UI_CHANGED` when any of:
  - soft aliases leaving non-hub settings path or wrong panel
    (`settings/vat` soft-empty, `settings/moms`, `settings/tax`,
    `settings/sales-tax`, `settings/salestax`, company default hub, …)
  - hub open but **wrong panel** (company default; Regnskab; Faktura;
    Profil; Brugere; Adgangsnøgler; Betas; empty Abonnement chrome)
  - Upsedasse / error shell / plan-gate as sole state / login form present
  - heading not `Indstillinger` on settings path
  - sibling shells (Lagermodul, Fordele, Eksportér data, Overblik, products)
- **Never:** click Opret* / Gem* / Tilføj* / Upload / Slet / Opgrader /
  Inviter*; invent `api_settings_*`; treat company, accounting, invoicing, or
  user open as this tool's success; use loose Betas-in-body as VAT success.

### Navigation

1. Ensure authenticated READY session via existing browser auth path.
2. Open hub `https://mit.billy.dk/{org_slug}/settings` (company default is
   expected intermediate; **not** success).
3. Observe-only click exact side label **`Momssatser`** (prefer role
   link/button, fall back to exact text). Never click write CTAs.
4. Do **not** accept soft seed `settings/vat` (or moms/tax variants) alone as
   success. Do **not** accept bare hub company default.
5. Independent second session (fresh profile) re-opens hub + clicks Momssatser
   and asserts path / h1 Indstillinger / VAT h2 triad. **No** API read-back.

### Reject as success

| Class | Examples |
| --- | --- |
| Company default | bare `settings` / company panel markers |
| Accounting panel | seed `settings/accounting` / Regnskab+Køb+Kontoplan / Momsopgørelse |
| Invoicing panel | seed `settings/invoicing` / Faktura+Produkter+… |
| User/Profil panel | hub+Profil / Billede+Sprog og tema+Skift adgangskode |
| Soft nested paths | `settings/vat`, `settings/moms`, `settings/tax`, `settings/sales-tax`, `settings/salestax`, … |
| Other hub panels | Brugere, Adgangsnøgler, Betas, Abonnement empty chrome |
| Sibling success shells | inventory Lagermodul, add-ons Fordele, integrations, exports, dashboard Overblik, annual_reports, settings company/accounting/invoicing/user tool success |

### CTA hard ban (tests must prove no click)

- Opret / Opret regelsæt / Opret sats  
- Gem ændringer / Gem / Save  
- Tilføj* / Upload / file input activate  
- Slet / Delete / Opgrader / Inviter*  

Observation of CTA **text** is allowed (and expected on Momssatser panel).  
**Click count for write CTAs must stay 0.** The only allowed navigation click
is the side label **`Momssatser`** (and only as open path).

## EXECUTE steps (root)

1. **Models** — `UiSettingsVatOpenInput` (empty, forbid extra),
   `UiSettingsVatOpenSuccess` (`path_class`, `heading=Indstillinger`,
   `shell_kind=settings_vat`, `vat_panel_markers_present`).
2. **Browser** — `UiSettingsVatOpenService` protocol +
   `BrowserRuntime.ui_settings_vat_open` implementing hub open + Momssatser
   click + VAT panel classify; soft-alias / company default / accounting /
   invoicing / user / other panel / error → `UI_CHANGED`; write CTAs never
   clicked. Signature uses `.first` for multi-match rail/body labels
   (`Momssatser` / section titles may appear more than once after open).
3. **Server** — register `ui_settings_vat_open` with typed input/output.
4. **Offline tests** — model validation, browser unit (mock page states for
   vat success / company default reject / accounting reject / invoicing
   reject / user reject / soft alias / login / error), server registration,
   coverage inventory map.
5. **Coverage generator** — `apply_ui_settings_vat_open_shell_evidence` for
   `ui.discovery.settings_vat` only; wire family; egress append without drop;
   regenerate manifests/status (**complete remains false**).
6. **Live tests** — dual-session open via hub+click Momssatser; DOM assert
   path + h1 Indstillinger + VAT h2 triad; independent read-back; reject
   company/accounting/invoicing/user via unit (and soft live checks as
   feasible); write CTA click ban; no API token.
7. **Vision** — capture non-sensitive Indstillinger Momssatser chrome frame(s)
   outside repo; Grok vision record; purge frames; store only non-sensitive
   review record with `purge_verified` (no emails, phones, person names,
   rate customer data dumps).
8. **Wiki** — `wiki/ui_settings_vat_open_shell.md` (contract + non-claims).
9. **Lint/type/offline suite** before REVIEW; live dual + vision before
   completeness claims for this row.

## FIX-VERIFY / COMMIT gates

- Independent Grok review ACCEPT for product slice (expected overall
  completeness FAIL).
- Offline non-live suite green; live settings vat dual + vision
  purge_verified.
- Egress test_references retain all prior UI live paths + settings vat open
  (and still include settings company + accounting + invoicing + user).
- Coverage: only `ui.discovery.settings_vat` newly greened for live/vision;
  complete false; API live_tested stays false with out_of_scope_by_user.
- No secrets, frames, HAR, traces, PII values in repo.
- `fractal commit` product + memory tip; **not** `node finish`.

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Mis-green all settings_* from hub open | Evidence helper only touches `ui.discovery.settings_vat` |
| Soft `settings/vat` classified as success | Soft-empty path fails triad; require hub+click + triad |
| Company / accounting / invoicing / user false success | Explicit distinct-panel rejects |
| Accounting Momsopgørelse confused with Momssatser | Require Regelsæt + Satser for salg + Satser for køb; reject Kontoplan trio |
| Soft alias false success | Require final path bare settings leaf + VAT triad after Momssatser click |
| Rail-only Momssatser false positive | Require full h2 triad, not side-nav label alone |
| Betas false positive from body rail labels | Never use beta markers for VAT success |
| Accidental Opret / Gem click | Live test asserts write_cta clicks 0; browser never clicks those labels |
| PII in vision/logs | Scrub names/emails/phones; non-sensitive review record only |
| Egress drop of prior lives | Generator append-only; coverage unit asserts prior refs present (incl. settings company + accounting + invoicing + user) |
| annual / subscription accidental green | Not in evidence helper target list |
| Inventing API tools | Explicit non-goal; no `api_settings_*` registration |
| salesTax API rework | Non-goal; offline API already separate |

## Done when (this product only)

- [x] `ui_settings_vat_open` registered with typed models
- [x] Offline unit + coverage map green
- [x] Live dual-session + vision purge_verified green for VAT Momssatser shell
- [x] Coverage greened only for `ui.discovery.settings_vat`; complete false
- [x] Wiki contract page present; no secrets/frames in repo
- [x] Independent review ACCEPT (overall completeness still FAIL)
- [ ] Committed on `main.billy_complete`; **not** node finish

## Post-mortem

### Completed

- Producted `ui_settings_vat_open` for `ui.discovery.settings_vat` only.
- Open path: hub `/:org_slug/settings` + observe-only click **Momssatser**
  (research130; soft seeds insufficient).
- Success: path `/:org_slug/settings`, h1 `Indstillinger`, shell_kind
  `settings_vat`, markers Regelsæt / Satser for salg / Satser for køb.
- Distinct from company / accounting / invoicing / user panels.
- Offline: lint pass; **1528** non-live pytest passed (FIX-VERIFY reconfirm).
- Live: dual independent sessions + vision purge_verified (FIX-VERIFY reconfirm).
- Coverage: live/vision **33**; `complete: false`.
- IR 186.31: **ACCEPT** product; no required fixes; optional N1 wording applied.
- Egress append retains company + accounting + invoicing + user + vat live refs.
- Wiki: `wiki/ui_settings_vat_open_shell.md` + `_index.md` link.

### Deviations

- None material vs plan. Soft-seed primary open intentionally rejected per
  research130 (click-nav required).

### Review findings

- Required fixes: **none**.
- Optional N1: live assert message said "user" — fixed to "VAT" in FIX-VERIFY.

### Cleanup

- No disposable Billy records created (read-only open shell).
- Vision frames purged; `purge_verified: true` on
  `tmp/vision-records/ui_settings_vat_open.json`.
- No API token used.

### Next unresolved coverage slice

- Remaining red UI discovery: `settings_users`, `settings_subscription`
  (weak freeze), `settings_access_token`, `settings_beta`,
  `annual_reports` (inaccessible Upsedasse).
- Prefer next product tip: `ui_settings_users_open` (Brugere click-nav) or
  access_token / beta.
- Residual ambiguous bulk API reds offline only; no live API.
- Not node finish.
