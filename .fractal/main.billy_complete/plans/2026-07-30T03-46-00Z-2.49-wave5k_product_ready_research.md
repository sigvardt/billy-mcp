# 2.49 Wave-5k product-ready research

## Scope

Cited offline product-ready handoff for singular `bankPayments` create and
update ticketed writes. Reconfirm official-doc fingerprint and unauthenticated
method gates. Freeze authoring remains open from research48 ACCEPT; freeze page
is still absent. Do not green coverage or implement product tools.

## Evidence collected

- Re-fetched https://www.billy.dk/api/ → ETag `hsisik4g9p3603`, MD5
  `c2efda0ee4cf9cf200e14910c5fc6996`, 147934 bytes (byte-identical to research48).
- Unauth probes against `https://api.billysbilling.com/v2`:
  - bankPayments POST/PUT **401**, DELETE **405**
  - salesTaxPayments POST/PUT **401**, DELETE **405**
  - contactBalancePayments POST/PUT **401**, DELETE **405**
- Re-extracted `/v2/bankPayments` Supports + property table and posting-payment
  sample; reconfirmed published `cashSide` debit/credit and irreversible
  `isVoided`.
- Confirmed freeze page still absent; freeze authoring authority ACCEPT present.
- Confirmed product module absent; registry 238; coverage 166 offline; 43 clear
  red singular CUD rows; bankPayments create/update/delete red.
- Confirmed UI credentials and `BILLY_API_TOKEN` absent; no interface discovery
  or disposable records. Zero webhook mentions.

## Deliverable

Cited brief:
`.fractal/main.billy_complete/tmp/grok-research.md` (research49).

## Recommended next slice

1. Codex Power authors `wiki/wave_fivek_ticketed_writes_contract.md` (create+update
   only; delete excluded on 405) under research48 authority.
2. Independent freeze review ACCEPT.
3. Independent ACCEPT of research49 product-ready handoff.
4. Codex Power product leaf: `bank_payment_writes.py`, four ticketed tools,
   offline suites, registry 242, coverage 168 offline only (delete stays red).
5. Independent product review; then Wave-5l salesTaxPayments freeze-ready.

## Boundaries

No coverage green from research. No headed browser. No secrets. No persistent
test data. Live/UI/vision/bulk/completeness remain fail-closed.

## Post-Mortem

The cited bankPayments create/update product-ready handoff was completed
offline. Root Grok independently re-fetched the unchanged documentation,
reproduced the POST/PUT 401 and DELETE 405 gates, and accepted the handoff as
implementation-ready evidence only. No code, inventory, coverage, credentials,
browser evidence, or persistent records were introduced. Root lint and the
non-live suite passed (1,020 tests). The next unresolved slice is the cited
freeze page followed by an independent freeze ACCEPT; only then can the
four-tool product leaf begin.
