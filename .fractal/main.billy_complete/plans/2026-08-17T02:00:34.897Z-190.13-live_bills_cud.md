# 190.13 Live FastMCP draft bills CUD

## Goal

Prove one tagged draft bill create, update, and delete through
`create_server` `call_tool`. Independent second-session read-back after
each step. Third session proves absence. Coverage stays red.

Authority: `96908DC6`, `728BD2E4`, `E004E7D5`, `EE0A0F1B`, `C7DBE974`,
`F3F318A2` (egress pattern). Research:
`.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
`HEAD` ETag `"121myuqjdm53603"` / 147934 bytes. Inventory lock stays
`"wcw4x9hqvu3603"`. Do not re-lock.

Contacts CUD already remapped to preview after accept+purge. Bills stay
on `ui_bills_*_open` until a later accept+purge. This slice is live
proof only.

## Non-goals

- Greening any of the 16 CUD honesty rows
- Remapping bills (or any other remaining 13) `tool_name` to preview
- Changing `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` or its length-16 assert
- Writing `author=independent_review` / `accept` / `purge_verified`
- Live API token calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Clicking **Registrer betaling**, **Godkend**, **Træk**, or **Upload**
- Re-running live contacts CUD or rewriting the contacts accept record
- Spawning children
- Claiming `complete: true`

## Baseline

HEAD `f47b9d5`. Last commit is 190.12 contacts remap. Not a backstop.
Memory files are dirty from SYNC/RESEARCH. `complete=false`. Honesty 16
stay red.

Preview/execute twins already exist in `src/billy_mcp/ui_writes/bills.py`
and reuse `ConfirmationStore`. `create_server` already passes
`BrowserRuntime` plus `-readback`, so default execute is
`BrowserBillSubmitter`. The live file still registers without a runtime
and only proves the `9F2EC46E` blocker. That is not qualification.

Current `_bill_write` uses `input[name=description]`, substring
**Gem som kladde**, and one **Slet**. Research: that will not persist a
draft. Egress already allows GET/POST/DELETE `/v2/bills` and GET
`/v2/taxRates`. There is no PUT `/v2/bills`. Official update is
`PUT /v2/bills/:id`.

Gate `tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py`
already holds.

## Delegation

No children. Root owns this slice. The files sit on the mainline and
would collide if forked:

- `tests/live/test_ui_bills_writes.py`
- `src/billy_mcp/ui_writes/bills.py`
- `src/billy_mcp/ui_writes/page_flow.py` (only if a shared helper is
  required; prefer bills-local helpers)
- `coverage/browser_egress.yaml` (only if live update is PUT)
- `wiki/ui_bills_writes.md`

`728BD2E4` child split was for write-family implementation. Those seven
families are already merged and parked. A live bills persist fix is not
a new family.

Parked children stay parked. Do not `--continue` them.

## Design choices

1. **TDD first (`728BD2E4`).** First confirm honesty 16 still passes.
   First product edit is the failing live FastMCP bills CUD test. Run
   it. Capture the failure. Then implement.
2. **Contacts pattern.** Copy
   `tests/live/test_ui_contacts_writes.py`: unique `MCP-UI-B-*` tag,
   `create_server` login, observer session, preview+execute, exact-tag
   list proof, `{tag}-U` update, delete, third-session absence. Vision
   `author=live_test`, `reviewer_verdict=pending_review` only.
3. **Bill id from the interface.** Update/delete preview require `id`.
   After create, read the id from the edit URL or list on an
   independent session. Never from a live API GET. Execute result
   currently has no id field; do not invent an API lookup.
4. **Egress after observation.** If persist is POST `/v2/bills`,
   already allowed. If it is PUT `/v2/bills/:id`, add a failing
   browser-egress test first, then allow PUT prefix `/v2/bills/` only.
   No PATCH. No collection PUT.
5. **Exact labels.** Confirm delete is the second **Slet**, not
   **Annuller**. Never substring-click a forbidden control.
6. **Keep honesty 16.** Do not remap. Do not green.

## EXECUTE

### 1. Honesty still red (no edit)

```bash
uv run pytest -q tests/coverage/test_ui_open_only_cannot_complete_cud_parity.py
```

Must pass (16 red). Do not change this file.

### 2. Failing live FastMCP test (first product edit)

Rewrite `tests/live/test_ui_bills_writes.py` to a
`create_server` `call_tool` CUD, modeled on
`tests/live/test_ui_contacts_writes.py`.

Keep the no-runtime blocker as a unit test in
`tests/unit/test_ui_bills_writes.py` (move it if it is not already
there). It is not qualification.

Live test must:

- Skip only when both credential refs are missing. Do not skip a
  submit when credentials exist.
- Unset `BILLY_ORGANIZATION_ID`.
- Login through `auth_login_start` / `auth_login_wait` until READY
  returns `organization_id`.
- Create via `ui_bills_create_preview` + `ui_bills_create_execute`.
- Prove the exact tag on an independent bills list (`/:org_slug/bills`).
- Recover `id` from that independent interface path.
- Update via `ui_bills_update_preview` (`id`, `{tag}-U`) + execute.
- Prove exact `{tag}-U` and that `{tag}` is not a prefix hit.
- Delete via `ui_bills_delete_preview` + execute.
- Third fresh session: tag absent.
- Frames outside the repo. Vision record
  `tmp/vision-records/ui_bills_writes.json` with
  `author=live_test`, `reviewer_verdict=pending_review` only.
- Cleanup leftover `MCP-UI-B-*` even on failure. Purge ephemeral
  `billy-live-bills-*` profiles. Never `chrome-profile`.

Run the live test. Capture the first real failure
(`UI_CHANGED`, persist miss, or `EGRESS_DENIED`). Do not edit product
files before that run.

### 3. Smallest persist fix

Only after the failure is recorded:

- Bind the real create-form field and **Gem som kladde** control.
  Do not assume `input[name=description]`.
- Wait for the SPA write. If that write is PUT `/v2/bills/:id`,
  first add a failing egress test, then allow PUT prefix `/v2/bills/`.
- Delete: exact confirm (**Slet** then confirm **Slet**, never
  **Annuller**).
- Prefer bills-local helpers over growing generic `FamilyWrite`.
- Fail closed on payment, approve, pull, and upload chrome.

### 4. Wiki and offline

One wiki paragraph on `wiki/ui_bills_writes.md`: the live-slot
blocker is no longer the default `create_server` path; live
qualification is FastMCP CUD; coverage stays red. Then
`wiki update --path=wiki` and `wiki lint --path=wiki`.

Offline commit-mode `bash .fractal/main.billy_complete/scripts/test.sh`.
Do not claim completeness. Do not run `ui-full`. Do not remap bills.

### 5. Do not green

Honesty 16 stays red. Bills `parity_status` stays open-only.
`tool_name` stays `ui_bills_*_open`. `coverage/status.json` stays
`complete=false`. `vision_verified` stays false.

## Done when

- Honesty 16 still passes.
- One live FastMCP draft-bill CUD passed with independent read-back
  and third-session absence.
- Vision record is `live_test` / `pending_review` only. Frames are
  outside git and not purged by this slice.
- Zero leftover `MCP-UI-B-*`. Zero leftover `billy-live-bills-*`.
- Offline `test.sh` passes. No greening. No live API. No docs re-lock.

## REVIEW

Independent Grok. Confirm the live path used `create_server`
`call_tool`, not `BrowserRuntime` as the pass proof. Confirm no
payment/send/VAT/user click. Confirm honesty 16 still red. Confirm
no secrets, frames, or HAR in git. Confirm bills `tool_name` was
not remapped.

## Post-Mortem

Work completed: independent review FAIL findings for the form contract
are fixed offline. Pre-submit dump writes tag, vendor, date, amount, and
draft CTA before click. All four fields must match. Forbidden CTAs are
refused. Create persist counts only POST 2xx and takes the id from that
write. Browser egress now allows PUT prefix `/v2/bills/` only. Wiki
execute table no longer claims the live-slot blocker. Honesty 16 stay
red. Bills `tool_name` values stay `ui_bills_*_open`. `complete` stays
false.

Deviations: live FastMCP CUD did not pass. Login reached READY. Create
execute still returns `UI_CHANGED` on vendor typeahead bind after a
contacts create. Date chrome is `dd.mm.yyyy`. Amount chrome is `1,00`.
**Opret "name"** opens **Opret leverandør** and intercepts the bill form
until **Gem**.

Review findings: all listed contract defects were confirmed and patched
except live proof, vision, and tagged-draft delete. Login `UI_CHANGED`
did not reproduce on these runs.

Test results: lint passed. Offline `test.sh` 1848 passed / 60 deselected.
Honesty gate passed. Live bills CUD failed on vendor. No `ui-full`. No
vision record.

Cleanup state: leftover `billy-live-bills-*` profiles were purged before
the live retries. Inspect-only vendor names may remain in the dedicated
org and must be deleted through contacts tools.

Next unresolved coverage slice: live vendor typeahead bind, then
**Opdater** PUT, tagged **Slet**, independent read-back, third-session
absence, `live_test` / `pending_review` vision. Do not remap. Do not
finish.

