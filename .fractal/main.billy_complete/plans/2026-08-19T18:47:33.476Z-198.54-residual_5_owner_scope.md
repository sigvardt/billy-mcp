# 198.54 Encode residual-5 owner scope

## Pivot

`FE6FA4B1` (P10) supersedes the RESEARCH wait on `AB021A02`.
Existing owner scope already decides the safety contract.
HEAD `362204e` is a clean 198.53 close, not a `(failed on
<step>)` backstop. Worktree is clean.

Research: `.fractal/main.billy_complete/tmp/grok-research.md`.
Official https://www.billy.dk/api/ (2026-08-19, ETag
`"tmhc6wpdc835zt"`, MD5 `053f755f52e3926b028e29325e3670d4`).
Inventory lock stays `wcw4x9hqvu3603`. Do not re-lock
(`728BD2E4`). Do not spawn. Do not remake dumps (`D68E402A`).
Do not arm `_ledger_write`. Do not finish.

The 11 accepted CUD rows stay `preview_execute`. This slice
does not touch them.

## Evidence gate (existing dumps only)

`FE6FA4B1`: encode Godkend rows always. Encode files and
daybooks only if prior evidence proves no uniquely verifiable
safe create/delete cycle. If a row fails that test, leave it
honesty-red and name the contradiction. Do not ask the owner
to inspect routine UI.

| Id | Prior evidence | Decision |
| --- | --- | --- |
| `ui.parity.daybookTransactions.create` | Persist is **Godkend**. `96908DC6` forbids posting | Scope. `GODKEND_HIGH_IMPACT_PROHIBITED` |
| `ui.parity.transactions.create` | **Godkend** / **Godkend alle**. Irreversible. API row is `READONLY_PROPERTY_TABLE` | Scope. `GODKEND_HIGH_IMPACT_PROHIBITED` |
| `ui.parity.files.create` | research185 dual **Slet** 0. No singular file DELETE. Execute is `PLAN_UNAVAILABLE` | Scope. `FILES_NO_UI_DELETE` |
| `ui.parity.daybooks.create` | Dump: `unique_persist_token=none`, both persist counts 1, `name_input_count=0` | Scope. `DAYBOOKS_UNIQUE_PERSIST_ABSENT` |
| `ui.parity.daybooks.delete` | **Mere** then menu **Slet**. No tagged create, so no tagged cleanup cycle | Scope. same `DAYBOOKS_UNIQUE_PERSIST_ABSENT` |

All five pass the evidence gate. EXECUTE still re-reads the
wiki pages before applying. If one contradicts, leave only
that id in `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS`.

## Goal

Machine-readable `qualification.kind=out_of_scope_by_user` on
those five UI parity rows. Model on
`apply_ui_annual_reports_out_of_scope_by_user`. Differences
from annual reports: keep `discovered=true` and
`contract_tested=true` (open chrome was observed). Force
`implemented=live_tested=vision_verified=false`.
`tools_allowed=false`. `not_applicable` rejected because nav
and routes exist. `parity_status=out_of_scope_by_user`. Leave
`vision_evidence` null. Do not green. Do not invent
`ui_annual_*`. Keep fail-closed FastMCP preview/execute tools
registered; the coverage row must not claim them.

`owner_decision_ref=radio:FE6FA4B1`. Cite `96908DC6` in
evidence for the Godkend prohibition.

`complete` stays false (92 `ambiguous_bulk` still trip
`coverage_is_complete`). Do not "fix" bulk92 this slice.

## Why no child

Generator, coverage fixtures, residual wiki, and memory are
one root-owned slice. A child would fork
`scripts/generate_coverage_report.py`. Seven write children
stay completed and merged.

## TDD

1. New fixture
   `tests/coverage/test_ui_residual_write_owner_scope.py`.
   On HEAD it must fail. Assert for each of the five:
   - `qualification.kind == out_of_scope_by_user`
   - named `scope_code` as in the table
   - `tools_allowed is False`
   - `not_applicable_decision == rejected`
   - `parity_status == out_of_scope_by_user`
   - `parity_status != not_applicable`
   - `implemented`, `live_tested`, `vision_verified` are false
   - `generator.is_owner_out_of_scope(row) is True`
   - `radio:FE6FA4B1` in evidence
   Assert `status["complete"] is False`.
   Run it. Capture the fail. Do not implement first.

2. Smallest generator change, after
   `apply_ui_cud_proved_preview_execute`:
   - New apply that sets the five rows as above.
   - Empty `UI_CUD_PARITY_OPEN_ONLY_HONESTY_IDS` (or shrink to
     any row the evidence gate left red). Allow length 0 so
     the honesty apply is a no-op.
   - Keep `test_ui_open_only_cannot_complete_cud_parity.py`
     unchanged. Open-only or `*_open`/`*_list` still cannot
     green. Empty `tool_name` is already treated as open-only
     by that gate, so scoped rows with blank tools stay safe.
   - Do not hand-edit `coverage/status.json`.
   - Do not hand-edit workflow rows except via the generator.

3. Update tests that still require the five to stay
   open-only honesty:
   - `tests/coverage/test_ui_cud_proved_honesty_promote.py`
     `test_remaining_honesty_rows_stay_open_only_and_red`
   - `tests/coverage/test_coverage_inventory.py` honesty
     branch around the five ids (keep the 11
     `preview_execute` asserts)
   Keep annual-reports tests untouched.

4. Run the generator to refresh
   `coverage/ui_workflows_manifest.yaml` and
   `coverage/status.json`. Confirm `complete` is still false.
   Confirm the 11 proved rows are still
   `preview_execute` and green.

5. Wiki `wiki/ui_write_residual_table.md`: move the five from
   honesty-red to owner-scope. Do not call them complete.
   Memory `state.md` / `todo.md` / `decisions.md`: `FE6FA4B1`
   landed. Do not finish.

6. Offline commit-mode checks only (format, lint, type, unit,
   coverage, safety). No live UI. No `BILLY_TEST_MODE=ui-full`.
   Independent Grok review in REVIEW. If review fails, fix and
   re-run the same fixtures. Do not `fractal node finish`.

## Out of scope

Live remake, dumps, `_ledger_write` submitter, official-docs
re-lock, bulk92, API live tests, product update tools, greening
the five, spawning, finish.

## Post-Mortem

Completed: TDD owner-scope for the five residual writes per
`FE6FA4B1`. Failing fixture first, then
`apply_ui_residual_write_out_of_scope_by_user`. Honesty set is
empty. 11 CUD rows stay `preview_execute`. Flags on the five
stay false. `not_applicable` rejected. Fail-closed preview/execute
tools stay registered. Wiki residual table updated.

Deviations: coverage rows kept open-shell `tool_name` values
(`ui_uploads_list`, `ui_daybooks_open`, and the other three)
instead of blanking them, so research184 dual-count and the 76-row
tool map stay intact. `tools_allowed=false` is the skip.

Review: PASS. No required fixes. Advisory: Godkend/Bogfoer
boilerplate was on files and daybook evidence. FIX-VERIFY scoped
that cite to `GODKEND_HIGH_IMPACT_PROHIBITED` only, then
regenerated.

Tests: focused 7 passed after the cite fix. Lint passed.
Commit-mode `test.sh`: 2119 passed, 83 deselected. No `ui-full`.
No live UI.

Cleanup: no new Billy records. No frames.

Next unresolved coverage slice: bulk92
(`BULK_SCHEMA_UNSPECIFIED_OFFICIAL_DOCS`) still makes
`complete=false`. API live stays `out_of_scope_by_user`. Do not
finish.

