# 190.7 Exact name match, then one contact CUD

## Goal

Prove one tagged Billy customer through FastMCP create, update, and delete.
Independent read-back after each step. A third session proves absence. Do not
green.

Authority: `96908DC6`, `A5FBD4DC`, `E1E454F4`, `E613984F`, `C7DBE974`.
Research: `.fractal/main.billy_complete/tmp/grok-research.md`. Official docs
fingerprint is unchanged. Do not re-lock it.

## Non-goals

- Greening any of the 16 CUD honesty rows
- A second tagged create
- BrowserRuntime-only write as qualification proof
- Live API calls, docs MD5 rewrite, bulk92, residual29, annual-report tools
- Invoice send/email, payments, VAT/filings, users/tokens/subscription
- Spawning children
- Claiming `complete: true`
- Self-approving vision or purging frames from the live test

## Baseline

HEAD `9c0bde8`. Exact **Ret** / **Gem** / **Mere** / **Slet kontakt** / **Slet**
are in. Leftover `MCP-UI-C-*` names are gone. Create through FastMCP already
submits. Live CUD of a new tag has not run.

The live helper `_list_has_name` does `name in body`. After rename to
`{tag}-U`, the original tag is still in the body. The current
`assert tag is False` will fail a correct update. Execute-path
`prove_text_on_fresh_page` uses Playwright `text={text}`, also substring.
Presence of `{tag}-U` is fine. Absence of `{tag}` after update is not.

Coverage `complete=false`. Honesty 16 stays red.

## Delegation

No children. A child fork would fight over
`tests/live/test_ui_contacts_writes.py` and
`src/billy_mcp/ui_writes/page_flow.py`. Root Grok implements.

## Design choices

1. **TDD first.** First product edit is a failing exact-name test. Run it.
   Capture the failure. Then implement.
2. **Exact visible name, not substring.** `MCP-UI-C-AAAA` is not present when
   the only match is `MCP-UI-C-AAAA-U`.
3. **One tag.** `MCP-UI-C-<8hex>`, then `{tag}-U` after update. No second
   create.
4. **Live qualification uses `create_server()` `call_tool`.** Vision stays
   `pending_review`. No greening.
5. **Keyring refs on.** `BILLY_ORGANIZATION_ID` unset until READY returns the
   live slug.

## EXECUTE

### 1. Failing test (first edit)

Add a focused unit test next to the existing exact-label tests, or a helper
test imported by `tests/live/test_ui_contacts_writes.py`. Fake a list body that
contains only `MCP-UI-C-AAAA-U`.

| Case | Expected today | Expected after fix |
| --- | --- | --- |
| Body has `MCP-UI-C-AAAA-U`, query `MCP-UI-C-AAAA` | True (`name in body`) | False |
| Body has `MCP-UI-C-AAAA-U`, query `MCP-UI-C-AAAA-U` | True | True |
| Body has neither | False | False |
| Playwright `text=MCP-UI-C-AAAA` on a `{tag}-U` cell | may count 1 | exact match 0 |

Do not green. Do not run live in this first edit.

### 2. Implement exact name match

Files:

- `tests/live/test_ui_contacts_writes.py` — `_list_has_name` must require an
  exact visible name (list row or `get_by_text(..., exact=True)`), not
  `name in body`.
- `src/billy_mcp/ui_writes/page_flow.py` — `prove_text_on_fresh_page` must
  not treat `{tag}-U` as a hit for `{tag}`. Use exact text, not
  `locator(f"text={text}")`.
- `src/billy_mcp/ui_writes/contacts.py` — only if the first-session post-save
  list check uses the same substring rule.
- `wiki/ui_contacts_writes.md` — exact name match after update. No greening.

Do not edit `generate_coverage_report.py` or `coverage/status.json`.

### 3. Live one-record CUD (`A5FBD4DC`)

Prefix the keyring refs. `BILLY_ORGANIZATION_ID` unset. One tag only.

Keep `tests/live/test_ui_contacts_writes.py` on `create_server()`:

1. `auth_login_start` / `auth_login_wait` through `call_tool`. Stop if READY
   has no slug or `AUTH_INTERACTION_REQUIRED`.
2. Unique tag `MCP-UI-C-<8hex>`. Preview + execute create. Independent
   observer session must see the exact tag and not a substring hit.
3. Preview + execute update to `{tag}-U`. Independent session must see
   `{tag}-U` and must not treat `{tag}` as still present.
4. Preview + execute delete. A third fresh profile proves both names absent.
5. Four-state frames in owner-only temp storage. Pending vision record only.
6. If a later step fails, delete leftover tags through `ui_clients_delete_*`.
   Delete write, readback, observer, and cleanup profiles on every exit.
   Do not touch `chrome-profile`.

If Gem stays disabled after the name fill, stop and record the blocker. Do not
invent official-API `country` / `paymentTermsDays` unless the form shows them
as required.

### 4. Green only after live pass plus independent accept

Honesty set stays 16. Do not lift `ui.parity.contacts.create|update|delete`
in this iteration unless the live CUD test passes and an independent review
writes accept plus purge. Default for this plan: keep all 16 red.

### 5. Verify

Run the new exact-name unit test, then the commit-mode suite and lint. Run the
live contacts test with the keyring refs. Do not run live API. Do not run
`ui-full` as a completeness claim.

## Done when

- Exact-name tests prove `{tag}-U` is not a hit for `{tag}`.
- One tagged customer is created, renamed, and deleted through `call_tool`.
- Independent read-back sees the exact name after create and after update.
- A third session lists neither name.
- Honesty stays 16 unless live CUD plus independent accept both landed.
- No live API, no docs re-lock, no `complete: true`.

## Post-Mortem

### Completed

- Exact-name helper and tests. `{tag}-U` is not a hit for `{tag}`.
- Live `_list_has_name` and `prove_text_on_fresh_page` use exact text.
- Delete confirm is **Ja, slet** (fallback **Slet**). Direct leftover delete
  used that label. Org has no `MCP-UI-C-*` names after cleanup.
- `_click_exact_label` no longer uses `has-text` substring.

### Deviations

- Live one-record CUD did not pass. Create through FastMCP worked earlier.
  Update **Gem** left the form open. FIX-VERIFY added input/change events and
  last-exact-Gem click. Re-run hit observer `UI_CHANGED` on login before create.
  R1 is not closed.
- No greening. No `ui-full`. No docs re-lock.

### Review

IR FAIL. R1 update save. R2 no live CUD. R3 **Ja, slet** kept. R4 `_click_named`
substring fallback remains on create.

### Tests

Exact-name and exact-label unit tests pass. Lint pass. Commit-mode suite 1829
passed, 59 deselected. Live CUD not re-proved this pass.

### Cleanup

No leftover `MCP-UI-C-*` names after the last scan. `chrome-profile` not
touched.

### Next coverage slice

Make contact update actually submit. Then one tagged FastMCP create, update,
and delete. Do not green until that proof and an independent accept.

