# 186.74 ui invoices update open

## Goal

Product research174 ACCEPT:

1. New read-only FastMCP UI tool **`ui_invoices_update_open`** that opens an
   **invoice draft edit form observe surface** frozen as
   `/:org_slug/invoices/:id/edit` (form chrome dual: Fakturanr/Dato/
   Betalingsfrist + Gem som kladde family). Never Gem / Gem som kladde /
   Godkend og send / Send / Slet / Mere→Slet / submit. Observe-only form open
   for API parity.
2. Green exact **`ui.parity.invoices.update`** / map `api.invoices.update` onto
   `ui_invoices_update_open` (`form_open_only`).
3. Keep greened `ui_invoices_list`, `ui_invoices_create_open`, and
   `ui_invoices_get_open` unchanged. Do **not** dual-count update onto those
   tools. Path class is **shared** with get (`/:org_slug/invoices/:id/edit`);
   distinction is **assertion family** (get = detail_open_only entry_date +
   contact + line; update = form_open_only with Gem som kladde + multi-label
   form freeze + inputs≥3). Same pattern as bills.update vs bills.delete on
   one path class.
4. **No** browser egress change this slice (invoices GET/POST/DELETE + products
   + contacts + accounts + salesTaxRulesets already committed for SPA
   seed/cleanup).

Target math (tip `ee5685c` / product 186.73):
live/vision **173 → 174**; implemented/contract **357 → 358**; complete
**false**.

Root-only Grok product. No children. No live API. Not node finish.

## Why this slice (research174)

| Evidence | Detail |
| --- | --- |
| Official docs | MD5 `8b94b0135c91fd15fe54ea33e088a4be`, ETag `wcw4x9hqvu3603` unchanged. invoices Supports get/list/create/**update**/bulk save/**delete**/bulk delete. PUT `/v2/invoices/:id`. |
| SPA seed dual | contact + product + draft invoice POST 200 dual under **committed** egress. Cleanup DELETE reverse dep 200 dual; all_clean dual. |
| Edit form dual | soft **`/:org_slug/invoices/:id/edit`** path dual; inputs **9 dual**; labels Fakturanr., Dato, Betalingsfrist, Valuta, Priser er, Design dual; buttons Mere=1, Gem som kladde=1, Gem=1, Godkend og send=1, Vis preview=1 dual; primary Slet button **0 dual**; update_form_ready **true dual**. |
| Get contrast | `ui_invoices_get_open` already greened on same path with detail_open_only (entry_date + contact + line). Update requires stronger form freeze (Gem som kladde + multi labels + inputs≥3). |
| products.get | soft /:id\|/edit\|/overview still nav chrome only dual; list_click stays on list — **REJECT**. |
| invoices.delete | Mere open dual; Slet text dual; Slet button 0 dual — **DEFER**. |
| invoice_email | soft /email\|/send inputs 0 dual — **DEFER**. |
| Isolation | form_open_only ≠ list shell ≠ create form ≠ get detail_open. Green only **invoices.update**. |

Brief: `.fractal/main.billy_complete/tmp/grok-research.md`.  
Dual: `tmp/research174_focus_dual.json`.  
Probe: `tmp/research174_focus_probe.py`.

## Non-goals

- Any change to `coverage/browser_egress.yaml` this slice
- Greening `invoices.delete` (Mere→Slet text dual ready for **later** separate
  tool `ui_invoices_delete_open`; do not dual-count delete with this tool)
- Greening products.get/update/delete, special.invoice_email
- bulk_* greening; annual_reports greening
- Ticketed durable UI write for invoice update (no real PUT; no approval ticket
  path for UI form open)
- Submitting Gem / Gem som kladde / Godkend og send / Send / Slet / Mere→Slet
- Live API token, headed browser, completeness, `node finish`
- Children / codex-power / non-grok agents
- Generic browser or HTTP tools
- Dual-counting update onto list/create/get tools
- Mapping invoice_email onto Godkend og send

## Ownership

| Area | Owner |
| --- | --- |
| Models + `BrowserRuntime.ui_invoices_update_open` + server register | Root Grok EXECUTE |
| Live dual test + vision record | Root Grok EXECUTE |
| Coverage generator dual-count invoices.update only | Root Grok EXECUTE |
| Unit tests (models + browser injectable) | Root Grok EXECUTE |
| Coverage inventory assertions | Root Grok EXECUTE |
| Wiki shell note | Root Grok EXECUTE |
| Independent review | Root Grok (self-IR) |
| Commit via `fractal commit` | Root after FIX-VERIFY |

No spawn. File ownership stays on root. Every child spawn would require
`--agent=grok` (none planned).

## Contract freeze (research174)

### A. Browser egress

**No change.** Reuse committed:

- prefix GET `/v2/invoices` (and summary if any)
- exact POST `/v2/invoices`
- prefix DELETE `/v2/invoices`
- products GET/POST/DELETE + accounts GET + salesTaxRulesets GET (seed product)
- contacts POST/DELETE (seed customer)

Harness SPA token only. Never `BILLY_API_TOKEN`.

### B. Tool: `ui_invoices_update_open`

| Field | Contract |
| --- | --- |
| Name | `ui_invoices_update_open` |
| Input | empty / org-from-session only (mirror get-open: open first openable draft; optional internal seed when list empty; no caller-provided generic browser control) |
| Success model | `UiInvoicesUpdateOpenSuccess` with booleans / path_class only (no raw ids/PII): e.g. `form_open`, `gem_kladde_or_save_chrome_present`, `date_or_payment_terms_chrome_present`, `contact_or_customer_chrome_present`, `inputs_present`, `shell_markers_present`; `shell_kind=invoices_update` |
| Path class | **`/:org_slug/invoices/:id/edit` only**. Not bare list, not `/new`. Soft bare `/:id` may redirect to edit (OK if form signature holds). |
| Errors | typed `ToolError` for auth/egress/UI_CHANGED; never generic browser control |
| Forbidden | Gem, Gem som kladde, Godkend, Godkend og send, Send, Send faktura, Slet, Mere→Slet, any submit |

Mirror structure of `ui_invoices_get_open` / `ui_bills_update_open` (models →
browser runtime → server registry → unit tests with injectable page/context).

**Assertion split vs get (critical):**

| Tool | Signature family |
| --- | --- |
| `ui_invoices_get_open` | detail_open_only: entry_date + contact + line chrome |
| `ui_invoices_update_open` | form_open_only: Gem som kladde (or save/approve chrome) + Fakturanr/Dato/Betalingsfrist family + inputs≥3 |

Unit tests must prove get does **not** require gem_kladde freeze and update
**does** require it.

### C. Live dual qualification

File: `tests/live/test_ui_invoices_update_open.py`

| Rule | Requirement |
| --- | --- |
| Dual sessions | two independent ephemeral profiles; second is independent read-back |
| Auth | browser credential references only; no API token env |
| Seed | if no draft invoice: SPA POST contact + product + draft invoice with line; unique R18674 markers |
| Open path | navigate to `/:org_slug/invoices/:id/edit` (or list text-click → edit) |
| DOM assert | path_class edit; form labels (Fakturanr/Dato/Betalingsfrist family) + Gem som kladde present + inputs ≥3 |
| Never | click Gem / Gem som kladde / Godkend og send / Send / Slet / Mere→Slet |
| Read-back | second session opens same path class / form chrome independently |
| Cleanup | DELETE invoice → product → contact reverse order; fresh list read-back clean dual |
| Vision | non-sensitive record under `tmp/vision-records/ui_invoices_update_open.json`; frames outside repo; purge verified |
| api_token_used | false |

### D. Coverage generator

In `scripts/generate_coverage_report.py`:

- Constants: tool name, live test ref, vision record path, research174 cite
- `apply_ui_invoices_update_open_shell_evidence(row, parity_of_api_update=True)`
  sets discovered/implemented/contract_tested/live_tested/vision_verified,
  parity_status=`form_open_only`, tool_name, evidence, test_references
- Apply **only** when row maps `api.invoices.update` / `ui.parity.invoices.update`
- Do **not** apply to list/create/get/delete/bulk invoices rows
- Status math: live/vision 174; implemented/contract 358; complete false
- Blocker text may still mention residual UI + bulk + annual

### E. Wiki

Short shell page `wiki/ui_invoices_update_open_shell.md` (pattern from
`ui_invoices_get_open_shell.md` / `ui_bills_update_open_shell.md`): path freeze,
never-submit, dual-count only invoices.update, research174 pointer scrubbed of
iteration noise where durable.

## File touch list (EXECUTE)

| Path | Action |
| --- | --- |
| `src/billy_mcp/models.py` | add `UiInvoicesUpdateOpenSuccess` (+ input if needed) |
| `src/billy_mcp/browser.py` | `ui_invoices_update_open` workflow + form signature helpers |
| `src/billy_mcp/server.py` | register tool |
| `scripts/generate_coverage_report.py` | dual-count apply invoices.update |
| `tests/unit/` / models unit | injectable open + flags; split vs get |
| `tests/live/test_ui_invoices_update_open.py` | dual live |
| `tests/…` inventory if asserts fixed tool set | extend |
| `wiki/ui_invoices_update_open_shell.md` | shell note |
| `coverage/*` | regenerate via generator (not hand-edit green) |
| `coverage/browser_egress.yaml` | **no change** |

## Verification (EXECUTE → FIX-VERIFY)

1. `bash .fractal/main.billy_complete/scripts/lint.sh` pass  
2. Offline: `BILLY_TEST_MODE=commit` / default offline test.sh (unit+contract) pass  
3. Live dual: `tests/live/test_ui_invoices_update_open.py` pass (when credentials present)  
4. Vision record written; frames purged  
5. `uv run python scripts/generate_coverage_report.py` → invoices.update green;
   list/create/get unchanged; live/vision 174; contract 358; complete false  
6. `scripts/check_coverage.py` (without require-complete) consistent  
7. No credentials/tokens in repo diff  

## IR acceptance criteria

- ACCEPT if: form_open_only on edit path dual; never-submit enforced; dual-count
  only invoices.update; get/list/create tools unchanged; assertion split vs get
  holds; no egress churn; no bulk greening; cleanup dual proven; vision record
  non-sensitive.
- REJECT if: dual-counts wrong rows; submits writes; greened delete/products/
  invoice_email; uses BILLY_API_TOKEN; collapses into get signature without
  Gem som kladde / form freeze; incomplete cleanup.

## Out of slice residual (leave red)

- ui.parity.invoices.delete (later `ui_invoices_delete_open` menu text)
- products.get/update/delete
- special.invoice_email
- bulk 92 external-contract; annual_reports org_inaccessible
- API live_tested stays false (`out_of_scope_by_user`)
- clear API write residual offline (29) not this slice

## Feasibility

- Research dual evidence present and cleanup proven.
- Pattern already shipped (bills.update, clients.update, invoices.get).
- No egress expansion risk.
- Root-only; budget unlimited; no children needed.
- Complete remains false after slice (expected).

## Ready for EXECUTE

Root Grok implements plan 186.74 from research174. Not finish.

## Post-Mortem

- Outcome: DELIVERED `ui_invoices_update_open` (form_open_only maps
  `api.invoices.update` / `ui.parity.invoices.update`). IR ACCEPT; no required
  fixes (optional N1 workflow_ref naming / N2 seed body align deferred).
- live/vision: 173 → 174
- implemented/contract: 357 → 358
- complete: false (bulk 92 external-contract; annual_reports org_inaccessible;
  residual UI parity open: invoices.delete, products.get/update/delete,
  special.invoice_email, many discovery_required rows)
- Tests: lint.sh pass; offline BILLY_TEST_MODE=commit **1638 passed** / 49
  deselected; live dual `tests/live/test_ui_invoices_update_open.py` pass
  (EXECUTE + IR evidence); vision record purge_verified true.
- Cleanup: SPA seed contact+product+invoice deleted reverse-order; dual list
  read-back clean; profiles ephemeral; frames purged outside repo.
- Deviations: none material. No egress path_allow expansion.
- Next unresolved coverage slice: prefer `ui_invoices_delete_open` (Mere→Slet
  text dual research174) if menuitem/text locator is product-safe without
  confirm; else products.get only if soft detail dual-stable; special.invoice_email
  still no durable form; no bulk greening; no annual green without access. Not
  node finish.
