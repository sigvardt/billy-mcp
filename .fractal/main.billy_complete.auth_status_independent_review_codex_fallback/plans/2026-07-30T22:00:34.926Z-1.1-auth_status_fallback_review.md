# 1.1 Auth status fallback review

## Scope and boundary

Review only the committed `cf38b5c` typed `auth_status` slice.  The sole
tracked deliverable is `wiki/auth_status_independent_review_codex_fallback.md`.
Do not change product source, tests, coverage manifests, generated coverage,
or use credentials/a live login.

## Review sequence

1. Read the approved design, the existing Wave-5t UI/auth discovery record,
   review-provenance rules, and the current official Billy API documentation.
   Record stable source references and the precise fixed-route/DOM contract.
2. Inspect the exact parent commit from a clean `git archive`, including the
   complete `browser.py`, `models.py`, `server.py`, focused tests, and egress
   policy.  Trace registration and result schemas; seek defects first.
3. Run clean-archive focused tests and static checks without credentials or
   browser evidence.  Verify that all UI coverage rows remain red.
4. Write a non-sensitive, cited fallback review page with either actionable
   defects (path/line, impact, repair) or a clear no-defect verdict.  State the
   missing Grok gate and all unmade live, E2E, read-back, vision, UI-parity, and
   completeness claims.
5. Lint the wiki, commit only the review record, and report the result (or any
   defect immediately) to the parent before signalling completion.
