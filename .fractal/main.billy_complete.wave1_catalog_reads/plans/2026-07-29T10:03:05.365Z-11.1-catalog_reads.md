# 11.1 Catalogue API reads

Deliver the catalogue read slice in exactly two product files:
`src/billy_mcp/api/catalog_reads.py` and `tests/api/test_catalog_reads.py`.
No child can be spawned because this node is a configured leaf.

1. Define strict Pydantic request and success models for the four frozen tools:
   products get/list and product-prices get/list.  Use the documented singular
   roots (`product`, `productPrice`), plural roots (`products`, `productPrices`),
   and optional `meta.paging`; retain upstream resource payloads without
   inventing undocumented product fields.
2. Add a real, dependency-injected catalogue registration function that accepts
   a `BillyHttpClient`, registers the four typed handlers, and uses only
   `/products`, `/products/{id}`, `/productPrices`, and `/productPrices/{id}`.
   List query construction will permit only page, pageSize, include,
   sortProperty, and sortDirection (ASC/DESC).
3. Build mock-transport contract tests for all request paths/query boundaries,
   singular and list-root mappings, present/absent paging, FastMCP tool names,
   and documented 401-to-`AUTH_REQUIRED` errors.  Tests will not issue live
   requests or change coverage rows.
4. Run format, Ruff, Pyright, focused pytest, then the node lint/test scripts.
   Inspect the diff to ensure only the two owned product files changed.  Report
   the four red coverage IDs (`api.products.get`, `api.products.list`,
   `api.productPrices.get`, `api.productPrices.list`) and focused test path to
   the parent; `live_tested` remains false.

## Post-Mortem

- Delivered the two owned files with four typed read tools, typed success and
  error outputs, and mock-transport contract coverage.
- No shared-interface change or contract deviation was needed; root wiring and
  coverage-status ownership remain with the parent.
- The module is ready for the parent to review and integrate; no live result is
  claimed.
