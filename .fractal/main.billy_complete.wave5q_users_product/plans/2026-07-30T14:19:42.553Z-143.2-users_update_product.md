# 143.2 Users update offline product

1. Implement the frozen update-only users write module from the organization-update pattern: strict outer validation, opaque `user` map, body/path id equality, and shared ticket protocol only.
2. Add local contract coverage for the two named tools; wire them once in the root server and update the registry expectation to 262 API tools.
3. Add the sole `api.users.update` offline evidence reference, regenerate derived coverage artifacts, then run focused and configured commit checks while preserving inherited seed/runtime changes.
4. Fold the durable implementation state and short plan post-mortem into node memory, report the result over radio, and leave the product files ready for the commit step.

## Post-mortem

The organization-update pattern and shared write protocol matched the frozen
users contract without widening the tool surface. The coverage generator made
the one permitted offline row green; lint and the complete offline commit suite
passed without live, UI, vision, bulk, or completeness claims.
