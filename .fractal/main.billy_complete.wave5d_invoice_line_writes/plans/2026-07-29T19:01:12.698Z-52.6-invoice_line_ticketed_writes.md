# 52.6 Invoice-line ticketed writes

## Scope and decision

Implement the frozen invoice-line leaf in its two exclusively owned files.
Child delegation is unavailable because all node child limits are zero; the
work is a direct adaptation of the accepted Wave-5c line leaf. Do not modify
server registration, coverage, the shared write protocol, or any invoice leaf.

## Implementation

1. Add `invoice_line_writes.py` with strict outer Pydantic preview models,
   opaque `invoiceLine` payloads, and six flat FastMCP tools.
2. Build each preview through the shared `WriteProtocolService`, binding its
   exact execute name, `POST`/`PUT`/bodyless `DELETE` shape, and the
   `invoiceLines` primary root plus optional `invoices` root.
3. Add focused tests adapted from the Wave-5c line suite: registration and
   schemas, non-mutating previews, canonical routes/bodies, ticket failures and
   same-module executor mismatch, typed error propagation, and no retries.
4. Add response fixtures for both primary-only and primary-plus-parent success
   mapping, including delete metadata, proving `invoices` is never fabricated.

## Verification and handoff

1. Run the focused invoice-line tests, then the node test and lint scripts;
   fix only the owned files if needed.
2. Review the owned diff against the frozen contract without external research.
3. Record durable state privately, commit only the two owned files, report the
   result by radio, and finish the node once all completion requirements hold.

## Post-Mortem

- Completed the six invoice-line preview/execute tools and their focused
  contract suite in the two owned files.
- A final contract pass added the missing check that an explicit update payload
  ID equals the route ID; the otherwise opaque payload remains unchanged.
- The Codex Power fallback review passes after that repair; Grok authentication
  remains unavailable, so it is not a Grok gate result.
- Final scoped evidence: 26 focused tests pass, 725 non-live tests pass, and
  lint/type/coverage-inventory/repository-policy checks pass.
- Full mode runs all 725 tests successfully and then fails only at the
  repository-wide fail-closed completeness gate; no coverage state was edited.
- Cleanup state: substantive changes are limited to the two owned files.
