# 107.1 Bank-payment ticketed writes

Implement the accepted offline-only Wave-5k slice locally. This node is a leaf
(`max_depth`, `max_children`, and `max_descendants` are zero), so no delegation
is possible or required.

1. Add `bank_payment_writes.py` following the attachment-write shape, with
   strict create/update outer Pydantic models, opaque `bankPayment` maps,
   update-body id equality, and only the four authorised preview/execute tools.
   Bind POST `/bankPayments` and PUT `/bankPayments/{id}` to the shared
   `WriteProtocolService`, using `bankPayment`/`bankPayments` roots and no
   delete or bulk surface.
2. Register that single resource registrar in `server.py` with the already
   shared client, confirmation store, and write-protocol instance; update the
   server registry assertion from 238 to 242 and enumerate the four tool names
   only if the existing test's exact-set contract requires it.
3. Add focused local contract tests adapted from attachment writes: typed flat
   schemas and strictness; id consistency and opaque payload preservation;
   no-preview HTTP; one exact POST/PUT without retry; required plural-root
   response mapping without fabricated related roots; expiry/tamper/replay and
   executor mismatch before consumption/HTTP; typed authentication errors.
4. Mark only `api.bankPayments.create` and `api.bankPayments.update` as
   implemented and contract-tested, regenerate the supported coverage evidence
   to 168 rows / 242 API tools, and assert `live_tested: false` plus
   `complete: false`. Leave delete and both bulk rows unchanged and red.
5. Run the focused tests, lint/type/coverage checks, and node non-live test
   script; review the diff for scope and generated-artifact hygiene. Record the
   durable terminal result in node memory, commit with Fractal, notify the
   parent by radio, and finish without claiming product review or completeness.

## Post-Mortem

Delivered the four approved tools and registered them through the one shared
write protocol. Focused tests cover strict flat inputs, opaque payloads,
id-equality, non-mutating previews, exact single POST/PUT execution, required
plural-root mapping, non-fabrication, confirmation failure modes, and typed
authentication errors.

The generated coverage source needed two explicit evidence entries; it also
preserves the freeze's irreversible-void cleanup wording instead of generating
a prohibited delete-cleanup claim. Generated API/status/UI artifacts now report
242 API tools and 168 offline-qualified API rows while keeping live/UI/vision/
bulk/completeness fail-closed. Focused checks, pyright, the coverage checker,
the node scripts, and the full non-live suite passed.
