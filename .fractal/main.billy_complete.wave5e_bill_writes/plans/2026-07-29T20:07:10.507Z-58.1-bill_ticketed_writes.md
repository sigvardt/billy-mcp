# 58.1 Bill ticketed writes

1. Add the bill-only write leaf by adapting the invoice module's structural
   protocol integration: flat strict outer models, opaque `bill` payload,
   update-id equality, and the six frozen preview/execute names.
2. Build each `WriteOperationSpec` with `/bills`, `bill`, `bills`, and no
   additional response roots; preserve the exact executor name in each ticket
   binding and keep delete bodyless.
3. Adapt the invoice contract suite to bills, asserting all local boundaries:
   schemas and validation, canonical preview bindings, encoded wire shapes,
   typed errors, all ticket rejection paths, executor mismatch, and one-write
   execution behaviour.
4. Run focused pytest, formatter, Ruff, and Pyright; repair only the two owned
   files. Report any required shared integration to the parent rather than
   changing server or coverage files.

## Post-Mortem

- Added the six frozen parent-bill ticketed write tools with strict flat outer
  models, opaque payloads, exact `/bills` specifications, and no response-root
  expansion.
- Added 27 local contract tests covering schemas, request/ticket binding,
  encoded execution shapes, typed failures, and non-retry/non-mutation paths.
- Verification passed: focused pytest, Ruff formatting and checks, Pyright,
  and the node test and lint scripts.
- Root-owned registration and coverage wiring remain deliberately outside this
  leaf's scope.
