# 147.1 Wave-5r salesTaxReturns update product

## Scope

Deliver the accepted, offline-only `api.salesTaxReturns.update` operation as
exactly one preview/execute pair. The leaf is depth-zero, so source, contract
tests, registry wiring, and generated coverage stay integrated in this plan.
No research, live call, browser work, create/delete/bulk surface, or manual
manifest edit is in scope.

## Implementation

1. Clone the strict `user_writes` update pattern into
   `sales_tax_return_writes.py`: opaque `salesTaxReturn` map, non-empty route
   id, optional matching inner id, and a `PUT /salesTaxReturns/:id`
   `WriteOperationSpec` whose only mapped success root is `salesTaxReturns`.
   Preview mints the shared bound ticket; execute accepts only its non-empty
   ticket and delegates to the shared one-write protocol.
2. Create the isolated FastMCP contract suite by adapting the user-update
   tests. Cover flat `additionalProperties: false` schemas, strict outer
   validation, inner-id equality, opaque preservation, mutation-free preview,
   escaped PUT/body construction, root-only response mapping, malformed-root
   rejection, ticket tamper/expiry/replay, typed HTTP failures with one write,
   missing-token no-network behavior, and log redaction.
3. Register the new registrar immediately after existing Wave-5 write
   registrations. Extend the registry test with the two-name
   `WAVE_FIVER_WRITE_API_TOOL_NAMES` set, its length assertion, total API
   count 264, and membership union.
4. Add precisely `api.salesTaxReturns.update` to the generator’s
   source-controlled offline-evidence map, referencing the new suite and root
   registry test. Run the generator so only generated coverage artifacts carry
   the inventory-state transition to 179 implemented/contract-tested rows,
   zero live/vision rows, and `complete: false`.

## Verification and delivery

Run the new suite, formatting/lint/type checks, generator, fail-closed coverage
check, and node commit-mode suite. Inspect the resulting diff to confirm only
the authorised source/test/server/generator/generated-coverage files changed
and no sensitive evidence exists. Commit with `fractal commit`, report the
offline handoff, drain node records, and finish only after every required check
passes.

## Post-Mortem

Accomplishments: delivered the strict preview/execute pair, all required local
contract coverage, server registration at 264 API tools, and the sole
generator-derived transition of `api.salesTaxReturns.update` to 179/179 offline
implementation and contract-test evidence.

Deviation: the coverage generator now writes by default so the required direct
invocation regenerates its artifacts; the legacy `--write` flag remains accepted
for compatibility.

No further implementation work is planned; only commit and lifecycle handoff
remain.
