# 32.9 Contact-person final verification

The prior backstop contains the scoped contact-person implementation and
focused tests; only the private frozen-source note is currently uncommitted.
This is a childless leaf (`max_depth`, `max_children`, and `max_descendants`
are zero), so verification remains local.

1. Inspect the six FastMCP registrations and their generated schemas, retaining
   `Field(min_length=1)` on the update-preview handler identifier. Confirm outer
   strictness, ticket-only execute inputs, and opaque nested payloads without
   adding name/email or live-only validation.
2. Run the focused contact-person suite with local doubles. It must prove
   mutation-free preview; exact POST, PUT, and bodyless DELETE shapes; ticket
   failures; 401/404 propagation; and optional deleted-record mapping.
3. Run the node lint and non-live test scripts. Preserve their actual outcomes;
   the parent owns the execute-twin coverage-checker allowance and this leaf
   makes no coverage, inventory, or live-status change.
4. If verification exposes a scoped defect, modify only
   `src/billy_mcp/api/contact_person_writes.py` or
   `tests/api/test_contact_person_writes.py`, rerun the affected checks, and
   inspect the final diff. Otherwise retain the repaired implementation.
5. Commit the private research-note refresh and any scoped correction, report
   the exact SHA plus focused/lint/non-live evidence to the parent, and preserve
   the pending root-owned independent-review and merge boundaries.
