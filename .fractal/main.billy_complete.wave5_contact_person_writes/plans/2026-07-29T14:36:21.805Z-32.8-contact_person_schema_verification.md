# 32.8 Contact-person schema verification

This childless leaf cannot delegate. The committed module already exposes the
non-empty update identifier in both its Pydantic model and FastMCP handler;
the focused suite already asserts the published `minLength` schema. Preserve
the frozen six-tool implementation unless the focused checks prove a defect.

1. Re-run the focused contact-person test suite to prove the registration,
   strict schemas, ticket-only executes, singular request paths and bodies,
   bodyless delete, ticket failures, error propagation, and optional deleted
   records behavior with local doubles only.
2. Run node lint and the non-live node test script after merging the parent's
   committed checker exemption if it becomes available. Keep product changes
   confined to the owned module and focused test, and leave coverage,
   inventory, server wiring, live qualification, UI, bulk, and special routes
   untouched.
3. Inspect the scoped diff, preserve the local frozen-source note, commit the
   delivery, report the commit and check evidence, and complete the node only
   when the required checks pass.
