# 32.7 Contact-person ticketed write verification

This childless leaf has no delegation capacity, so it will complete the
isolated module and its focused tests locally. The prior implementation is
committed in the interrupted review backstop; preserve it unless focused
verification exposes a defect.

1. Inspect the registered FastMCP schemas and existing tool calls against the
   frozen contact-person contract. Keep the six preview/execute registrations
   in the module helper, strict outer Pydantic inputs, opaque nested JSON, and
   `WriteProtocolService` as the sole ticket/client/response owner.
2. Tighten only the owned module or focused test where needed. Verify previews
   perform no write; create/update send singular `contactPerson` JSON to the
   relative path; delete sends an escaped identifier with no body; execute has
   only the confirmation ticket; and client-mapped auth/not-found plus
   invalid, expired, and replayed tickets stay typed.
3. Run the focused contact-person suite, then node lint and the non-live node
   test script. Inspect the scoped diff and commit only the module, its tests,
   and required private node state; leave server wiring, coverage/inventory,
   live status, bulk, auth, UI, and specials untouched.
