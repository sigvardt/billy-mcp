# 32.5 Contact-person ticketed writes

The leaf is childless (`max_depth`, `max_children`, and `max_descendants` are
all zero), so no delegation is possible or needed. The cited parent Grok brief
was read from the parent worktree because this node's scratch directory has no
copy; its ETag `hsisik4g9p3603` and MD5 `c2efda0ee4cf9cf200e14910c5fc6996`
match the frozen shared contract.

1. Add the isolated `contact_person_writes` module with strict outer preview
   input models, opaque JSON payloads, a shared typed helper, and exactly six
   FastMCP registrations. Build every operation through `WriteOperationSpec`
   and delegate preview/execute behavior to `WriteProtocolService`.
2. Cover registration schema and direct tool behavior with local mocked
   transports: mutation-free previews, exact POST/PUT/DELETE paths and bodies,
   bodyless delete, execute-only ticket input, escaped identifiers, mapped
   optional deleted records, and propagated 401/404 responses.
3. Exercise ticket failures using the protocol-backed tools: invalid,
   expired, replayed, and strict extra-field inputs. Do not add name/email XOR
   validation or any live/write-network test.
4. Run the focused test module, then the node lint and non-live scripts. Fix
   only the owned module or focused test file, inspect the final scoped diff,
   and leave coverage and live qualification unchanged.
