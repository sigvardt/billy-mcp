# 10.1 Reference data reads

Implement this leaf-owned reference-data slice directly; child delegation is
unavailable because all node depth, child, and descendant limits are zero.

1. Add `reference_reads.py` with strict Pydantic inputs, opaque response data,
   optional typed paging metadata, and six success models.  Register the six
   functions only through an area-level `register_reference_reads` function
   taking a `FastMCP` instance and `BillyHttpClient` instance.
2. Route singular requests to `/currencies/:id`, `/countries/:id`, and
   `/locales/:id`; route list requests to their plural paths with only the
   frozen paging/include/sort query parameters.  Return the client's
   `ToolError` unchanged, map only documented root keys, and preserve unknown
   record fields as opaque Pydantic data rather than inventing schemas.
3. Add isolated mock-transport tests covering all six request paths and
   registrations, singular/list root mapping, optional and boundary paging,
   allowed query construction, and typed 401 authentication errors.
4. Run format, lint, Pyright, focused pytest, and node scripts.  Commit only
   the two owned product files, then report coverage IDs `api.currencies.get`,
   `api.currencies.list`, `api.countries.get`, `api.countries.list`,
   `api.locales.get`, and `api.locales.list`; live testing stays false because
   no dedicated credentials are available.

## Post-Mortem

- Accomplished: added the six typed read tools, response-root and optional
  paging mapping, and mock-transport tests for requests, bounds, authentication,
  and FastMCP registration.
- Deviation: no child was created because this node is constrained to zero
  depth, children, and descendants.
- Next-iteration notes: parent integration owns `server.py` wiring and coverage
  manifest state; this module is intentionally not imported by the root server.
