# 67.1 Wave-5g sales-tax ticketed writes

Implement the already accepted, offline-only six-row sales-tax CUD slice. The
node is a leaf (`max_depth`, `max_children`, and `max_descendants` are all
zero), so no delegation is possible or required. Parent is already merged and
there is no interrupted product work; the untracked node-runtime artifacts are
out of scope and will not be staged.

1. Add `sales_tax_writes.py` by applying the established `tax_writes.py`
   ticketed-write pattern only: strict flat outer Pydantic inputs, opaque
   `salesTaxRuleset` / `salesTaxRule` payloads, id-aligned partial PUTs,
   canonical bodyless DELETEs, and literal execute-tool bindings. Define only
   the six frozen `/salesTaxRulesets` and `/salesTaxRules` specifications, with
   `salesTaxRules` as the optional additional root solely for rulesets.
2. Add focused contract tests for the twelve public FastMCP schemas and every
   create/update/delete route. Assert no preview HTTP, exactly one request for
   execute with encoded ids and no DELETE body, typed auth/not-found errors,
   no retry, ticket tamper/expiry/replay/binding failures, and declared response
   and delete-metadata mapping. Include the optional ruleset child-root and
   non-fabricated rule parent-root cases.
3. Add the cross-resource executor test through `create_server` and its shared
   confirmation store. Prove mismatched ruleset/rule executors reject before
   HTTP without consuming the ticket, then the correct executor succeeds.
4. Wire the registrar into `create_server`, extend only the Wave-5g registry
   constant/assertions to 202 `api_*` tools, and add exactly the six permitted
   CUD evidence entries. Run the coverage generator; inspect the generated
   manifest, status, and report to confirm precisely 148 implemented and
   contract-tested rows, zero live/vision rows, 92 ambiguous bulk rows, and
   `complete: false`.
5. Verify with Ruff format/check, Pyright, focused sales-tax and coverage
   tests, the coverage checker/policy checks, and the node non-live safety
   suite. Keep all live, UI, browser, credential, bulk, and completeness work
   excluded. Commit only owned source/test/coverage files, report the commit and
   checks through the outbox, then finish after the required closeout drain.

## Post-Mortem

- Accomplishments: delivered the twelve frozen ticketed sales-tax tools, root
  registrar integration, exact route/schema/ticket tests, cross-resource root
  executor protection, and the six generated offline coverage rows.
- Deviations: none; no live request, browser/UI work, credential, bulk tool, or
  completeness assertion was introduced.
- Next-iteration notes: the parent owns merge review and the independent Grok
  product audit; this leaf has no broader implementation scope.
