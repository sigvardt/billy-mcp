# 138.1 Wave-5p organizations product independent review

## Goal

Produce authoritative offline Grok product ACCEPT or FAIL for the integrated
Wave-5p singular organizations ticketed write product at root on this branch.
Deliver exactly `wiki/wave_fivep_product_independent_review.md`. No product
code, tests, coverage, credentials, browser, or other wiki edits.

## Constraints (leaf)

- Max children / depth / descendants: 0 → solo work; no spawn.
- Ownership: only the review page (+ memory / plans / node tmp).
- Research only: official docs + locked base unauth probes (no token).
- Budget: ~$320 remaining, ~20m/step; one focused EXECUTE fits.

## Baseline (orient)

| Item | Value |
| --- | --- |
| HEAD | `b4393a2` (init review node; parent product already on ancestry) |
| Parent product merge | `7bd7694` merge `main.billy_complete.wave5p_organizations_product` |
| Product commit | `fbc8996` organizations ticketed writes |
| Integration reconcile | `7a6f16d` |
| Product module | `src/billy_mcp/api/organization_writes.py` (four tools present) |
| Tests | `tests/api/test_organization_writes.py` |
| Freeze | `wiki/wave_fivep_ticketed_writes_contract.md` (ACCEPT freeze review exists) |
| Template | `wiki/wave_fiveo_product_independent_review.md` (structure) |
| Focused tests | node `scripts/test.sh`: org writes + coverage inventory + coverage server unit + `check_coverage --reject-false-completeness` + `check_repository_policy` |
| Memory | empty (create `state` on EXECUTE) |
| Interrupted work | none; clean tree; no backstop save |

## Delegation decision

No spawn. Leaf limits are 0. Work is one review page with a fixed checklist;
splitting would add ceremony without parallel gain.

## EXECUTE checklist (in order)

### 1. Cite official docs (read-only)

- `curl -sI` / body fetch of `https://www.billy.dk/api/` into node tmp only.
- Record ETag, MD5, size. Compare to freeze fingerprint if present.
- Extract Supports + properties for `/v2/organizations` (create/update expected;
  singular delete absent or not supported).

### 2. Unauth method gates (locked base only)

Base: `https://api.billysbilling.com/v2` (from `src/billy_mcp/config.py`).

Probes without credentials (JSON object bodies; no secrets; no store of
response bodies with customer data):

| Method | Path | Expect |
| --- | --- | --- |
| POST | `/organizations` | 401 |
| PUT | `/organizations/{id}` | 401 |
| DELETE | `/organizations/{id}` | 405 → delete tools excluded |

Note exact status/error codes. Purge raw bodies from tmp after notes.

### 3. Product surface inspection (root integration)

Read-only inspect:

- `src/billy_mcp/api/organization_writes.py` — exact four tools; outer models
  `extra=forbid`; opaque `organization` maps; update id path/body match;
  response root `organizations`.
- `src/billy_mcp/api/write_protocol.py` — ticket-only execute; binding; single
  use; expiry; tamper; replay; one write no retry.
- `src/billy_mcp/server.py` — registration of org write tools only (no delete).
- Redaction path for subscription-card fields (client/redaction module as used).
- Compare freeze `wiki/wave_fivep_ticketed_writes_contract.md` and freeze
  independent review ACCEPT page.

### 4. Registry and coverage honesty

Independently verify (do not edit):

| Claim | How |
| --- | --- |
| 260 `api_*` tools | count registered tool names / server inventory |
| 177 implemented+contract-tested API rows | `coverage/status.json` + report/manifest |
| Only org create/update newly green offline for this slice (other prior greens OK historically; this slice’s greens are create+update only) | grepped inventory rows for organizations |
| Create cleanup: singular DELETE unsupported | inventory cleanup text |
| Create response field `organizations[]` | inventory / tests |
| live=0, vision=0, complete=false | status.json |
| All UI red; 92 bulk ambiguous/red | status / manifest / report |

Clarify: prior waves already greened many rows; expected total offline greens is
not “only two rows green” globally. The instruction means: for organizations
CUD, only create/update may be green offline; delete must stay red; and the
counts must match 260 tools / 177 offline rows with fail-closed boundaries.

### 5. Run focused tests

```bash
bash .fractal/main.billy_complete.wave5p_product_review/scripts/test.sh
```

Expect exit 0. Separately note any non-live policy/coverage evidence the script
already exercises. Do not run live/UI/browser.

### 6. Write verdict page

Create `wiki/wave_fivep_product_independent_review.md` following Wave-5o product
review shape:

- Valid frontmatter (`name`, `title`, `desc`, `tags`, `sources`, dates)
- Explicit **ACCEPT** or **FAIL** for offline product only
- Tested root commit SHA(s)
- Scope, evidence classes (docs vs probes vs offline tests vs not-claimed)
- Method gates, four-tool surface, ticket protocol, response root, redaction,
  coverage counts, UI/bulk/live/vision exclusions
- Facts vs inferences kept distinct
- FAIL → actionable source locations; keep fail-closed coverage language

### 7. Wiki hygiene and evidence purge

```bash
wiki update --path=$WORKTREE_DIR/wiki
wiki lint --path=$WORKTREE_DIR/wiki
```

Purge node tmp probe raw evidence; keep only non-sensitive fingerprints in
memory + review page.

### 8. Memory, radio, finish path (COMMIT step owns git; finish after gates)

- Memory `state.md`: durable findings (verdict, SHAs, fingerprints, counts).
- Outbox: one-line verdict + evidence path.
- Diff must be review page only (+ regenerated wiki index if `wiki update`
  touches `_index.md` — allowed under wiki ownership for the shared wiki).
- Re-run `scripts/test.sh` before `fractal node finish` if not just passed.
- `fractal node finish --reason="Wave-5p organizations product review recorded"`.

## ACCEPT / FAIL criteria (summary)

**ACCEPT** only if all of: docs support create+update; unauth POST/PUT 401;
DELETE 405 and no delete tools; exactly the four named tools with strict outer
models, opaque maps, update id match, ticket protocol, no-retry, `organizations`
root, card redaction; registry 260; offline rows 177; org create/update green
offline with cleanup/response notes; live/vision 0; UI red; bulk 92 red;
complete false; focused tests pass.

**FAIL** if any gate fails; cite file:line and keep coverage fail-closed language.

## Out of scope

Live auth, UI, vision, bulk resolution, singular-delete cleanup product, overall
completeness, any product code change, spawning children.

## Feasibility

One solo EXECUTE. Docs fetch + three probes + static read + pytest + write page
fits budget. No blockers in memory. Parent already merged product on ancestry.

## Post-Mortem

### Accomplishments

- Independent docs re-fetch and unauth method gates (POST/PUT 401, DELETE 405).
- Root integration inspection of four organization write tools, shared ticket
  protocol, response-root mapping, redaction keys, and coverage honesty.
- Focused `scripts/test.sh` green (34 pytest + coverage + policy).
- Durable verdict written to `wiki/wave_fivep_product_independent_review.md`
  with explicit offline **ACCEPT** and fail-closed exclusions.
- Printed counts re-derived in REVIEW from `coverage/status.json`, manifests,
  and source (260/177/0/0/92/complete:false).

### Deviations

- Solo leaf as planned (child limits 0); no spawn.
- Docs access meta ETag/MD5 differs from inventory lock fingerprint while body
  size matches; treated as access-metadata churn, not a contract rewrite (same
  as freeze review and offline probe rules).
- Raw probe bodies purged from node tmp; status-only notes retained.

### Next-iteration notes

- COMMIT should stage only the review page and regenerated wiki index (plus
  allowed node memory/plan paths if the orchestrator tracks them).
- Run `fractal node finish` after commit with reason
  `Wave-5p organizations product review recorded`.
- Parent may treat Wave-5p offline create/update as product-gated; live/UI/bulk
  and singular delete remain closed.
