# 27.1 Wave 4 ledger and users offline reads — delivered

Delivered the frozen ledger/users collection slice in
`src/billy_mcp/api/ledger_user_reads.py` and
`tests/api/test_ledger_user_reads.py`, committed as `d10bb40`.

The delivery provides six strict, direct FastMCP read tools for transactions,
postings, and `/users`; opaque documented roots; optional `meta.paging`;
encoded identifiers; typed authentication errors; and an offline contract
suite. It leaves `/user` specials, root server registration, coverage evidence,
live/UI/vision qualification, and logging redaction untouched.

## Post-Mortem

- Completed the six frozen transactions, postings, and `/users` read tools in
  the two owned product files, including strict flat inputs, encoded IDs,
  opaque roots, optional paging, and direct FastMCP registrations.
- The initial synthetic encoded-ID mock assumed HTTPX preserved encoded path
  segments in `url.path`; it decodes that accessor. The mock now uses decoded
  route keys while retaining encoded full-URL assertions.
- Independent review passed the offline product contract. Its optional missing
  get-without-include fixture was added; root-owned server registration,
  evidence mapping, redaction integration, and coverage state remain outside
  this leaf.
- Focused verification covers the six paths/roots, query constraints, paging,
  typed auth errors, malformed responses, flat schemas, and structured output.
  Ruff, Pyright, and 25 focused tests pass; the non-live node suite has 300
  passing tests. Full mode is intentionally fail-closed at incomplete coverage.
  No credentials, live calls, raw evidence, generated reports, or out-of-scope
  product changes were introduced.
- The remaining coverage slice is root registration and evidence generation for
  these six tools, with live/UI/vision qualification and `complete` still red.
