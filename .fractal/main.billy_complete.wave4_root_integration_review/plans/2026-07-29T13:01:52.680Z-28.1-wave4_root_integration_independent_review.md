# 28.1 Wave-4 root integration independent review

## Goal

Produce a cited, durable independent Grok review of committed Wave-4 root
integration at `main.billy_complete` **`3f56399`**, written only to
`wiki/wave_four_root_integration_independent_review.md`. Read-only audit: no
product, coverage, test, or generated-artifact edits.

## Constraints

- Leaf node: `max_depth=0`, `max_children=0`, `max_descendants=0` — **no spawn**.
- Scope: only `wiki/wave_four_root_integration_independent_review.md` (+ wiki
  index churn from `wiki update`).
- Contract sources: https://www.billy.dk/api/ (fingerprint already
  rechecked), `wiki/wave_four_remaining_clear_reads_contract.md`,
  node `tmp/grok-research.md`, parent
  `.fractal/main.billy_complete/tmp/grok-research.md`.
- Inspect **git archive at `3f56399`**, not uncommitted noise.
- Never call live Billy API, browser, credentials, or create records.
- Parent feed claim (offline ACCEPT / product FAIL / 487 tests) is a **lead**,
  not a rubber stamp — re-derive every gate.
- Product completeness stays **incomplete** unless every parent completion
  condition is independently proven (it will not be).

## Inputs already in hand (RESEARCH)

- Docs etag `hsisik4g9p3603` / MD5 `c2efda0ee4cf9cf200e14910c5fc6996` / 147934 B
  (access 2026-07-29T12:58:20Z).
- Source scan: 94 `api_*` names; five Wave-4 modules registered in `server.py`;
  inventory 94 implemented/contract_tested, 0 live, complete false.
- Full gate-by-gate line audit and suite reproduction still required.

## Non-goals

- Implement or repair product code if discrepancies found (describe minimal
  repair only).
- Live/UI/vision/bulk/write qualification.
- Merging parent wiki pages written by other review paths; this deliverable is
  the scoped wiki page only.

## Work plan (solo EXECUTE)

### A. Contract matrix (audit module 1)

From freeze + docs, for each of 50 Wave-4 tools verify at `3f56399`:

1. Tool name registered via `server.tool`.
2. Relative path and singular/plural response roots match freeze.
3. List params: default paging/sort/include only; geo lists require
   non-empty `countryId` (cities/states/zipcodes only).
4. Inputs `extra="forbid"`; reject `offset`/unknown keys (tests + models).
5. No invented parent filters offline.

Modules: `geo_reads.py`, `tax_reads.py`, `bank_reads.py`,
`balance_invoice_ext_reads.py`, `ledger_user_reads.py` (+ matching
`tests/api/test_*.py`).

### B. Registry surface (audit module 2)

1. Enumerate tools from live FastMCP server object or registry unit test at
   `3f56399`: expect **exactly 94** `api_*` + `coverage_status` +
   `coverage_report`.
2. Confirm no write, bulk, generic HTTP, or generic browser tools exposed.
3. Confirm `api_users_*` ≠ `api_user_*` specials.

### C. Inventory / status (audit module 3)

1. YAML parse: only the documented offline-read set is green (expect 94 API
   rows including prior waves + 50 Wave-4; **not** only 50 total greens).
2. All 50 Wave-4 IDs: implemented+contract_tested true; live false.
3. No bulk/write greens; UI/vision red; `status.complete == false`.
4. Evidence map in `scripts/generate_coverage_report.py` lists the 50 IDs.

### D. Redaction / host / errors (audit module 4)

1. `redaction.py` keys: bank*, email/phone, downloadUrl.
2. `config`/`client` host lock to `https://api.billysbilling.com/v2`.
3. Typed error mapping still maps Billy auth envelopes to stable MCP codes
   (spot-check client/errors + Wave-4 contract tests for 401).

### E. Reproducible offline gates (audit module 5)

From worktree (equals product tree of `3f56399` under init commit):

1. Lint/type if available: `uv run ruff check …`, `uv run pyright` (record exact
   cmds/results).
2. Non-live suite: `BILLY_TEST_MODE=commit` →
   `uv run pytest -m "not live and not vision"` (or node
   `scripts/test.sh`). Expect parent-claimed **487** or record actual count.
3. Coverage/policy: `uv run python scripts/check_coverage.py` (non-require);
   confirm fail-closed under `--require-complete`.
4. Full mode: `BILLY_TEST_MODE=full` via node test.sh — **expect non-zero** at
   completeness gate; capture stdout/stderr excerpt.
5. No env with `BILLY_API_TOKEN` for these runs; do not set one.

### F. Verdict write-up

Write `wiki/wave_four_root_integration_independent_review.md` modeled on
`wiki/wave_three_independent_review.md` structure:

- Offline integration: **PASS** or **FAIL** (FAIL if any concrete discrepancy).
- Product completeness: **incomplete** / FAIL (explicit).
- Every discrepancy with file/line + minimal repair (do not fix).
- Exact test commands/results.
- Citations (docs fingerprint, freeze, research, commit SHA).
- Cover all five Wave-4 modules, registration, evidence/status, redaction/errors,
  offline/full posture.
- Never claim live/UI/vision/bulk/write qualification.

### G. Close-out (later steps, listed for sequence)

1. Scratch copy of full findings optional in node `tmp/grok-review.md`.
2. Radio parent verdict (high priority if FAIL offline; else clear dual verdict).
3. `wiki update` + `wiki lint` for project wiki.
4. Memory `state` page: terminal facts only (no Remaining/NEXT).
5. Scoped commit of wiki page; run node `scripts/test.sh` (commit mode) before
   finish; `fractal node finish`.

## Delegation decision

**No children.** Caps are zero; task is a single independent review with one
scoped deliverable. Spawn would violate leaf limits.

## Feasibility

- Time/cost: no limit.
- Blockers: none for offline audit. Live/UI blocked by design (no token) and
  correctly force product-completeness FAIL.
- Risk: long pytest runtime — nice the suite; bound workers if parallel.

## Success criteria (maps to Completion Requirements)

1. Wiki page exists with cited PASS/FAIL offline + incomplete product status.
2. All five audit modules covered with evidence.
3. Radio verdict posted; wiki update/lint clean; only scoped wiki committed;
   node finish after node test.sh passes.

## Order of execution

1. EXECUTE sections A–E against `3f56399` / clean worktree.
2. INDEPENDENT-REVIEW / FIX-VERIFY: assemble verdict; re-run any failed command
   once for evidence; no product patches.
3. COMMIT wiki + close.

## Post-Mortem

### Work completed

- Research: docs fingerprint rechecked; brief in node `tmp/grok-research.md`.
- Execute: static matrix audit of 50 Wave-4 tools, runtime registry count, inventory/UI scan, redaction/host/errors, offline and full gates.
- Independent review: formal report in `tmp/grok-review.md`; durable wiki record written.
- Fix-verify: no required product defects to fix; wiki update/lint; memory terminal; re-run lint/test.

### Deviations

- No children (leaf caps 0).
- No live API or browser (forbidden by node instructions).
- Parent feed offline ACCEPT treated as lead; re-derived independently.

### Review findings

- Offline Wave-4 root integration: **PASS**.
- Product completeness: **incomplete**.
- Required product rewrites: **none**.

### Test results

- `scripts/lint.sh`: expected pass.
- `BILLY_TEST_MODE=commit` / non-live pytest: **487 passed**.
- `BILLY_TEST_MODE=full`: exit 1 at completeness gate (correct fail-closed).

### Cleanup state

- No product tree edits.
- Scoped deliverable: `wiki/wave_four_root_integration_independent_review.md` (+ wiki index).
- Scratch under node `tmp/` only.

### Final evidence (completion)

- Wiki verdict record exists with cited offline PASS and product incomplete.
- Radio verdict posted (priority 8).
- Commit of scoped wiki is the remaining handoff for root merge.
