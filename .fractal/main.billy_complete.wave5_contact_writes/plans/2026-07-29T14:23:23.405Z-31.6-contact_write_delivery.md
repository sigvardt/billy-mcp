# 31.6 Contact write delivery

1. Reinspect the existing contact module and focused tests against the frozen
   ticketed-write contract, changing only the two owned product files if an
   observable mismatch remains.
2. Run the focused contact-write tests with local doubles; fix only a confirmed
   contract or test failure in the owned files.
3. Run the node lint and non-live test scripts, then inspect the final diff and
   scope before committing the contact delivery and its private research note.
4. Report the commit and checks upward, explicitly preserving red coverage and
   live qualification, then complete the terminal memory/radio drain and finish
   the node.
