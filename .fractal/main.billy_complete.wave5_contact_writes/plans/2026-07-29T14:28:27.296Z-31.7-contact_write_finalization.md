# 31.7 Contact write finalization

1. Reconcile the existing contact module and focused local-double tests with the
   frozen ticketed-write contract: exactly six registrations, ticket-only
   executes, singular `/contacts` request construction, bodyless delete, and
   no invented contact-field validation.
2. Run the focused contract test file. If it exposes an observable mismatch,
   correct only `src/billy_mcp/api/contact_writes.py` or
   `tests/api/test_contact_writes.py`, then rerun the focused evidence.
3. Run the node lint and non-live test scripts; inspect the final scope and
   diff before committing the contact delivery with its private research note.
4. Report the commit and checks upward, retain the explicit red coverage/live
   boundary, drain terminal memory/radio work, and finish without awaiting
   integration or a further review.
