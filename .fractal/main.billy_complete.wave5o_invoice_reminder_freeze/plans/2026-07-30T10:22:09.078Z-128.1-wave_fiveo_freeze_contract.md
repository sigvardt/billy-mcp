# 128.1 Wave-5o invoice-reminder freeze contract

## Outcome

Create and commit only `wiki/wave_fiveo_ticketed_writes_contract.md`: a cited,
wiki-only, fail-closed freeze for `api.invoiceReminders.create`. Preserve all
product, test, registration, inventory/status, coverage, live/UI/vision, bulk,
update, delete, association-write, webhook, and completeness gates.

## Orientation and authority

- No interrupted authored work exists. The last commit is not a failed-step
  backstop; the target page is absent; no earlier plan exists.
- Research64 is read-only scratch in the parent worktree at
  `.fractal/main.billy_complete/tmp/grok-research.md`. It is the cited Grok
  freeze-authoring brief.
- The prior Wave-5o research review is ACCEPT as research only. The prerequisite
  Wave-5n product review is now authoritative offline ACCEPT. Neither is a
  Wave-5o freeze or product acceptance.
- Research64, the two review records, the Wave-5n contract, probe rules,
  manifest/status, and approved design agree: one create row; two future tools;
  strict preview outer shape; opaque inner map; ticket-only execute; one
  non-retried write; required-root-only response mapping; POST 401; PUT,
  singular DELETE, and bulk DELETE 405; bulk rows red; `complete: false`.
- The current docs evidence is ETag `wcw4x9hqvu3603`, MD5
  `8b94b0135c91fd15fe54ea33e088a4be`, 147934 bytes. The older manifest/status
  fingerprint is access/CDN metadata drift only.
- This leaf cannot delegate: maximum child depth, children, and descendants are
  all zero. The deliverable is also a single indivisible wiki page.

## Execution

1. Author the target page with precise frontmatter and source citations for the
   official docs, research64, prior research ACCEPT, Wave-5n product ACCEPT,
   probe rules, manifest/status, Wave-5n structure reference, and approved
   design.
2. State the wiki-only authority boundary and separate mandatory Grok freeze
   review gate before any product child.
3. Freeze exactly this surface:
   `api.invoiceReminders.create` →
   `api_invoice_reminders_create_preview` /
   `api_invoice_reminders_create_execute`,
   `POST /invoiceReminders`, request root `invoiceReminder`, required success
   root `invoiceReminders`.
4. Encode the strict preview and execute shapes, shared
   `ConfirmationStore`/`WriteProtocolService`, exact ticket bindings,
   short-lived single-use semantics, one write/no retry, and no extra response
   roots.
5. Preserve the official field table as opaque/readonly/unproven boundaries,
   without defining payload, relation, encoding, association, email, download,
   or cleanup semantics.
6. Record the 401/405 gates, Supports omissions, both empty-tool red bulk rows,
   absent bulk body contract, and absent webhook. Name no excluded operation's
   tool.
7. State that only a later product child, after Grok freeze ACCEPT, may add the
   two frozen tools and correct create cleanup wording while greening.

## Verification

1. Review the page against every required source and fail closed on any factual
   conflict.
2. Use static searches to confirm exactly one frozen inventory id and exactly
   the two authorised future tool names; confirm no update, delete, bulk, or
   association tool name is authorised.
3. Confirm `git diff --name-only` contains only the target page plus this node's
   plan/memory files. Do not edit `wiki/_index.md` or any existing wiki,
   coverage, source, test, CI, or configuration file.
4. Run:
   - `wiki lint --path=/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete.wave5o_invoice_reminder_freeze/wiki`
   - this node's configured `scripts/lint.sh`
   - this node's configured non-live `scripts/test.sh`
5. Append exact check results and scope verification to a post-mortem section
   in this plan.

## Landing and finish

1. Commit the deliverable and node-owned plan/memory only, keeping runtime
   agent artifacts and immutable seed files out of the commit.
2. Verify the committed diff and clean/in-scope state.
3. Drain saved radio actions, reduce memory to terminal facts, and note that
   the contract page itself is the promoted durable finding.
4. Send one completion report with commit SHA to the parent and call
   `fractal node finish` in this same iteration.
5. If evidence or required checks conflict, do not commit or finish; report the
   precise conflict over radio.

## Post-Mortem

### Accomplishments

- Authored only `wiki/wave_fiveo_ticketed_writes_contract.md` as the shared
  deliverable.
- Re-derived the printed coverage facts from the manifest/status: 305 API rows,
  174 implemented and contract-tested, zero live, zero vision, 92 ambiguous
  bulk, and `complete: false`.
- Re-derived the reminder inventory surface: create remains red, the two bulk
  rows remain empty-tool red, and no update or singular-delete row exists.
- Static surface checks find the frozen inventory id once, each authorised
  future create tool name once, and no update/delete/bulk/association write-tool
  name.
- Reconciled the property table against Research64, including every official
  type and required/readonly/empty-note boundary.
- Kept product source, tests, registration, coverage/status, inventory,
  existing wiki pages, credentials, network/browser, live/UI/vision, and
  completeness state untouched.

### Verification

- Configured `scripts/test.sh`: exit 0 (configured no-op).
- Configured `scripts/lint.sh`: exit 0.
- Node-memory `wiki lint`: exit 0 with no issues.
- Full project `wiki lint`: exit 1 solely for the missing derived
  `wiki/_index.md` link to the new page; it reports no page-content issue.

### Deviation and handoff

The node contract forbids editing `wiki/_index.md` and assigns that derived
navigation to the parent. Radio message `4F74D684` reported the conflict.
Parent directive `41900133` explicitly resolved it: commit only this node's
owned page and seed records; the parent will regenerate the index and run full
wiki lint after merge. No next-iteration work belongs to this leaf. A separate
independent Grok freeze review remains the parent-owned gate before product
work.
