# 135.1 Wave-5p freeze independent review ACCEPT/defect

## Goal

Authoritative independent Grok freeze review of the merged Wave-5p organizations
create/update ticketed-write contract. Sole project deliverable:
`wiki/wave_fivep_freeze_independent_review.md` plus its `wiki update` index row.
Verdict must be explicit **ACCEPT** or concrete **actionable defects**. ACCEPT
authorises only a later separate Codex Power offline product leaf — not product,
live, UI, vision, cleanup, or completeness.

## Constraints

- Leaf node: `max_depth=0`, `max_children=0`, `max_descendants=0` — **no spawn**.
- Own only the review page, generated index entry, and node seed.
- Do **not** modify source, tests, coverage, status, prior contracts/reviews, or
  any other project wiki page.
- No credentials, headed browser, raw probe retention, or sensitive values.
- Unauth gates are **not** live qualification.
- Parent baseline under review: `afbe5188b029e89e7e865a6b9ee8b9a5881f51f4`.
- Freeze MD5 must stay `2742eda7bafecd619aba5fa8ad0694c5`.

## Orientation (done at PLAN)

| Fact | Value |
| --- | --- |
| Branch HEAD | `a6967e9` (init only) on top of parent `afbe5188` |
| Parent merge | Already up to date |
| Freeze page | `wiki/wave_fivep_ticketed_writes_contract.md` MD5 match |
| Candidate research | `wiki/wave_fivep_candidate_write_research.md` |
| Prior research review | `wiki/wave_fivep_freeze_ready_research_independent_review.md` (research only) |
| Template | `wiki/wave_fiveo_freeze_independent_review.md` / Wave-5n freeze reviews |
| Coverage @ baseline | `implemented_rows`/`contract_tested_rows` **175**, live **0**, vision **0**, `api_ambiguous_bulk` **92**, `complete: false` |
| Orgs inventory | get/list green; create/update red with preview tool names reserved; both bulk red empty tool; no singular delete row |
| Budget | ~$319 cost remaining; ~20m/step; fits solo one-iteration delivery |

## Decomposition decision

**Solo leaf work.** No child spawn: limits are zero; deliverable is one review
page with independent primary rechecks that must stay in one reviewer context.

## Execution plan

### 1. Primary evidence recheck (scratch only)

- Fetch `https://www.billy.dk/api/` headlessly (curl). Record HTTP status, ETag,
  Content-Length, body MD5. Compare to claimed `wcw4x9hqvu3603` / 147934 /
  `8b94b0135c91fd15fe54ea33e088a4be`.
- Parse `/v2/organizations` Supports + property table from the fetched HTML.
- Unauth JSON-object probes against `https://api.billysbilling.com/v2` (no token):
  - `POST /organizations` body `{}` → expect 401 `AUTHENTICATION_REQUIRED`
  - `PUT /organizations/<probe-id>` body `{}` → expect 401
  - singular `DELETE /organizations/<probe-id>` → expect 405 `METHOD_NOT_ALLOWED`
  - optionally bulk DELETE if freeze excludes it — record if relevant
- Store raw evidence only under node `tmp/` (gitignored). Never track secrets or
  full raw browser dumps.

### 2. Repository evidence (read-only)

- Reconfirm freeze MD5 `2742eda7…`.
- Read full freeze + candidate research + `wiki/offline_write_probe_rules.md`.
- Inventory rows for organizations create/update/bulk (+ confirm no delete row).
- `coverage/status.json` qualification and source_counts.
- Confirm no product write module/tools for organizations create/update yet
  (registry / absence of organization write registration) without greening.
- Design ticketed-write rules (preview/execute, 5-minute single-use, no retries)
  from design spec as needed.

### 3. Criteria matrix (must all be covered in the review)

Independently verify each claim:

1. Exact two inventory rows / four future tool names  
   (`api.organizations.create|update` × preview+execute).
2. Strict outer Pydantic inputs; opaque `organization` map; `extra="forbid"`.
3. Client-relative `POST /organizations` and `PUT /organizations/:id` (no `/v2`
   double-prefix).
4. Required success root `organizations` only.
5. Ticket-only execute; five-minute single-use exact operation bindings; no retries.
6. Update body-id equality when inner `organization.id` present.
7. DELETE 405 cleanup limit (no singular delete product).
8. Sensitive subscription-card redaction obligations.
9. Exclusions: delete, all bulk, generic transport, webhooks, live, UI, vision,
   completeness claims.
10. Coverage honesty: 175 offline green, 0 live, 0 vision, 92 bulk red,
    `complete: false` at review baseline; create/update still red.

### 4. Write deliverable

- Author `wiki/wave_fivep_freeze_independent_review.md` with valid frontmatter
  (`name`, `title`, `desc`, `tags`, `sources`, `created`/`updated`).
- Structure like Wave-5o freeze review: Verdict table → Reviewed baseline →
  Sources/method → Exact scope → Criteria table → Detail assertions →
  Explicit ACCEPT or defect list with actionable fixes.
- Cite primary fetch + unauth probes + repo paths/SHAs; no rubber-stamp of
  research pages without recheck.

### 5. Wiki hygiene and gates

- `wiki update --path=…/wiki` then `wiki lint --path=…/wiki`.
- Node `scripts/test.sh` and `scripts/lint.sh` must pass.
- `git diff --check` clean.
- Diff outside seed limited to review page + `_index.md` entry only.

### 6. Close-out

- Commit via `fractal commit` (or step COMMIT).
- Radio parent: verdict + commit SHA (high consequence if ACCEPT opens product).
- Memory: write `state` with durable review facts (no iteration labels).
- On full completion requirements: drain radio/memory, then
  `fractal node finish --reason=…`.

## Non-goals

- Product implementation or product ACCEPT.
- Coverage greening or status edits.
- Live authenticated calls, UI, vision.
- Editing the freeze contract or research pages.

## Feasibility

One focused review iteration is within budget and step time. Primary re-fetch +
probes + write + lint/test is sequential and fits a leaf. Risk: docs fingerprint
drift or probe mismatch → **REJECT with defects**, still a valid deliverable.
No blockers for starting EXECUTE.

## Post-Mortem

### Accomplishments

- Independently re-fetched official docs (ETag/MD5/size match claimed values).
- Re-probed organizations POST/PUT 401 and singular DELETE 405; supplemental bulk DELETE 405.
- Verified freeze MD5, two-row/four-tool map, ticket protocol, body-id equality,
  redaction duty, exclusions, and coverage honesty (175/0/0/92, complete false).
- Wrote `wiki/wave_fivep_freeze_independent_review.md` with **ACCEPT**; wiki
  update/lint pass; node test/lint scripts pass.

### Deviations

- None material. Solo leaf as planned (spawn limits 0).
- Bulk DELETE 405 recorded as supplemental evidence beyond the minimum singular
  DELETE probe; does not change freeze scope (bulk already excluded).

### Next-iteration notes

- COMMIT must land only the review page and `wiki/_index.md`.
- Radio parent with verdict ACCEPT and commit SHA, then `fractal node finish`.
- Product implementation is out of this leaf.
