# 125.1 Invoice late-fee ticketed write slice

1. Add `invoice_late_fee_writes.py` by applying the established singular-write
   protocol shape without widening it: strict preview models, opaque
   `invoiceLateFee` JSON, update-ID equality, and exactly the frozen POST/PUT
   operation specifications.
2. Add the focused offline contract suite covering tool schemas, canonical
   ticket binding, escaped paths, declared plural-root mapping, all ticket
   terminal states, typed failures, missing token, and non-retry behavior; wire
   only its four tools into `create_server`.
3. Add the two rows to the generator's offline evidence map, generate coverage,
   and retain only the authorised create/update greens. Update the generated
   count assertions and create-cleanup exception; leave both bulk rows and all
   live/UI/vision states unchanged.
4. Run focused API, coverage, and registry tests; then format, lint, Pyright,
   coverage policy, and the non-live suite. Inspect the resulting diff and
   generated counts before committing the self-contained slice.

## Post-Mortem

- Accomplishments: delivered the four frozen tools, their full focused contract
  suite, server registration, and the two authorised offline coverage greens.
- Deviations: the maintained generator is
  `scripts/generate_coverage_report.py`, so its source-controlled evidence map
  was updated; no file exists at the similarly named `src/` path. The generated
  UI-manifest cleanup drift was discarded to preserve the required untouched UI
  inventory boundary.
- Next-iteration notes: the slice is ready for the parent merge and separate
  Grok product review; this node does not claim that acceptance.
