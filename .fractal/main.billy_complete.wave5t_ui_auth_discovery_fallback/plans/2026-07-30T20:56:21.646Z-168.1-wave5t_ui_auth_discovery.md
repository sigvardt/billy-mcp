# 168.1 Wave-5t UI/auth discovery record

1. Create only `wiki/wave5t_ui_auth_discovery.md`, translating the completed
   Codex Power fallback evidence into a cited, non-sensitive shared record.
   Separate direct headless observations, existing-code findings, and bounded
   inferences; preserve the precise `AUTH_REQUIRED` versus
   `AUTH_INTERACTION_REQUIRED` distinction.
2. State the reachability boundary honestly: the allowlisted session-reuse
   check reaches the login route only, so organisation state and product-route
   parity remain unobserved. Map that evidence to a narrow future typed
   `auth_status`-first path and list the dedicated non-production organisation
   plus credential/session prerequisites for any later transition.
3. Regenerate and lint the project wiki, then audit the scoped diff to ensure
   it contains only the new page and generated index state. Do not modify
   coverage, product code, tests, browser policy, or raw evidence.
4. Run the node test script and the relevant wiki lint. Commit the completed
   research page, report the commit and blocker path to the parent, then finish
   the node with a research-only reason.

## Post-Mortem

- Completed the cited, scrubbed headless UI/auth discovery record and generated
  its wiki index entry. It records the observed login-only state as
  `AUTH_REQUIRED`, preserves the unobserved organisation/product boundary, and
  supplies the narrow `auth_status`-first prerequisite path.
- No Grok review file existed because the designated Grok leaf failed before
  edits; the authorised Codex Power fallback review passed and is explicitly
  not an independent Grok-review substitute. No review finding required a
  product, test, coverage, or policy change.
- `lint.sh` passed; the standard test suite passed 1255 tests. Full mode ran
  the same 1255 tests, then correctly rejected `complete: false`, 305
  incomplete API rows, 339 incomplete UI rows, and 92 ambiguous bulk rows.
  That gate is intentionally inapplicable to this research-only leaf, which
  must leave every UI row red.
- No raw browser artifact, session-store content, business data, or disposable
  interface record was retained. The project wiki is the canonical durable
  record; private memory has no non-duplicative terminal finding to add.
- Final evidence: scoped project diff is limited to the owned wiki record and
  generated index state; wiki lint has no actionable issues.
