# 155.3 Wave-5s-B upload release validation

## Decision

The prior run backstop committed the complete product slice and the subsequent
strict-input/header-injection repairs but did not execute its final verification
or commit phase because the requested Grok review could not authenticate. This
leaf has no child capacity, so no delegation is possible. Use the accepted
research80 brief only; do not reopen discovery or change the confirmation store,
inventory shape, JSON writes, reads, UI, or live gates.

## Work

1. Inspect the committed binary-upload service, narrow client method, exact
   registration, coverage evidence, and contract tests against the approved
   scratch brief. Stop and report any material contract mismatch rather than
   broadening the slice.
2. Run formatter, Ruff, Pyright, focused upload/client/registry/coverage tests,
   then the full non-live suite and the node `test.sh` in default commit mode.
   Make a narrowly scoped correction only if a current verification exposes a
   real defect.
3. Regenerate coverage and run ordinary and false-completeness coverage checks.
   Run `--require-complete` separately and record its required failure because
   live, UI/vision, bulk, and completeness gates remain open.
4. Run repository credential policy and dependency-audit checks. Verify the
   committed bytes from a clean archive with the relevant static/focused checks,
   then use `fractal commit` to land the reconciled memory and this plan with a
   clean tree.
5. Update private state to final delivered status, report the commit and
   remaining gates over radio, drain the saved directive, and finish only once
   the required completion checks have passed.

## Acceptance

- Exactly the two upload tool names remain registered; no `api_files_create*`
  name or generic binary-post surface exists.
- Tool-boundary strictness and CR/LF header rejection remain covered without
  HTTP; post-file stays raw-byte, fixed-path, allowlisted-header, and no-retry.
- Generated status remains 182 implemented/contract-tested API rows, 267 API
  tools, zero live/vision evidence, 92 bulk rows, and `complete: false`.
