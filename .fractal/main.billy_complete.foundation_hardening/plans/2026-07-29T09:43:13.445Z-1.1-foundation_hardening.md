# 1.1 Foundation safety hardening

1. Update `src/billy_mcp/client.py` so the fixed base version cannot be supplied
   again by a caller; reject `/v2` and `/v2/...` before any request construction
   while retaining relative-path, fixed-host, and page/pageSize validation.
2. Extend `src/billy_mcp/redaction.py` with the exact high-sensitivity
   organisation subscription-card and payment keys from the reviewed schema.
   Ensure upstream errors never return raw provider text that could echo those
   values, then exercise recursive structured-payload redaction in
   `tests/unit/test_redaction_errors.py`.
3. Add a strict typed `BrowserEgressPolicy` manifest factory in
   `src/billy_mcp/browser.py`. It will require the reviewed deny-by-default
   schema, accept only explicit browser `allow` hosts, reject absent, malformed,
   and unknown policies, and leave `BrowserRuntime`'s headless-only constructor
   unchanged.
4. Add focused unit fixtures/regressions in `tests/unit/test_client.py`,
   `tests/unit/test_redaction_errors.py`, and `tests/unit/test_browser.py`.
   The browser tests use temporary manifest fixtures and injected fakes only; no
   network request or Chromium launch is permitted.
5. Run Ruff format/check, Pyright, the owned unit suite, and repository policy;
   review the diff for no manifest, tool-registration, or coverage-state change
   before committing.

## Post-Mortem

- Accomplishments: completed the C1 fixed-base path guard, C2 structured
  subscription/payment redaction and error-text suppression, and S3 typed
  deny-by-default browser-policy loading with fixture-only tests.
- Deviation: error translation now intentionally returns a stable generic
  message rather than untrusted upstream text, preventing an upstream validation
  response from echoing sensitive payment data.
- Next-iteration notes: no additional implementation is required for this
  owned slice after final verification and commit.
