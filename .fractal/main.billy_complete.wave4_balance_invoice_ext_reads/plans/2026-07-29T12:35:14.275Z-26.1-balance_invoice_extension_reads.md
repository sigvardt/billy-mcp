# 26.1 Balance and invoice extension typed reads

Implement the frozen offline-only contact-balance and invoice-extension read
slice in the two owned product files. The leaf cannot delegate: its configured
depth, child, and descendant limits are all zero.

1. Create `balance_invoice_ext_reads.py` from the Wave-3 read-module shape:
   strict flat get/list Pydantic inputs, opaque records, optional
   `meta.paging`, root validation, encoded identifiers, and one service with
   typed get/list handlers for the five frozen collections. Register exactly
   the ten direct FastMCP tools in that module, but do not touch root server
   wiring, coverage, or redaction.
2. Create focused synthetic tests for every path and root, exact default and
   allowlisted queries, encoded IDs, absent/present paging metadata, validation
   boundaries and unknown keys, both typed 401 mappings, malformed roots, and
   flat tool schemas with structured outputs. Use mock transports only.
3. Run Ruff format/check, Pyright on the two owned files, focused pytest, and
   the node lint/non-live test scripts. Correct only failures in the owned
   product files.
4. Inspect the scoped diff for credentials and generated noise, commit only the
   two owned files, report the actual checks and commit hash over radio, record
   the delivered offline state privately, and finish without claiming live, UI,
   vision, coverage, or completeness progress.

## Post-Mortem

- Completed the two-file offline implementation: ten typed read tools for the
  five frozen collections and 24 mock-transport contract tests.
- No contract deviation or production review finding was identified. Independent
  review accepted the paths, roots, flat schemas, typed errors, and no-false-
  green posture.
- Ruff format/check and Pyright passed; focused pytest passed 24 tests; node
  lint and non-live tests passed 249 tests. The full qualification gate still
  requires the parent-owned coverage completion work.
- Product cleanup is limited to the two owned new files. Injected skills,
  runtime logs, plans, and research artifacts remain outside the product commit.
- Final evidence: scoped product commit `f5afcc7`; full mode passed all 249
  tests then failed only at the intended incomplete-coverage gate. Live, UI,
  vision, and `complete` remain red.
