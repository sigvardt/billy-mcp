# 1.1 Bill-line ticketed writes

1. Add the bill-line module by adapting only the invoice-line write structure:
   six flat preview/execute tools, opaque `billLine` payloads, strict outer
   models, `/billLines` paths, and `additional_plural_roots=("bills",)`.
2. Build the local contract suite around bill-line fields only, covering
   schemas, canonical bindings, encoded HTTP requests, optional parent mapping,
   typed failures, ticket safeguards, executor mismatch, and one-shot mutation.
3. Run the focused pytest suite and the required Ruff and Pyright checks; repair
   only the two owned files until all local checks pass.
4. Record durable leaf state, post the focused evidence to the parent, and
   commit the scoped delivery through Fractal before finishing the offline-only
   implementation slice.

## Post-Mortem

Accomplishments: implemented the six bill-line ticketed-write tools and a
28-test local contract suite, with focused pytest, Ruff, Pyright, and node
script checks passing.

Deviation: the cited scratch handoff is absent, while the accepted shared
contract contains the frozen paths, roots, and evidence boundaries needed for
this scoped implementation.

Next-iteration notes: parent integration owns server registration, coverage
evidence, and product acceptance; this leaf has no shared change to make.
