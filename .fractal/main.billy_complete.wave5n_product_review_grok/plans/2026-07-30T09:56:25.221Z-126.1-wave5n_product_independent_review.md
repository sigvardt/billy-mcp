# 126.1 Wave-5n product independent review

## Goal

Independent Grok product review of the root-integrated Wave-5n invoice-late-fee
ticketed-write implementation. Single deliverable:
`wiki/wave_fiven_product_independent_review.md` with a precise ACCEPT or REJECT.

## Constraints

- Leaf node: max children/depth/descendants all 0 — no spawn.
- Read-only on production source, tests, coverage rows, status, prior contracts.
- Only Billy network endpoints allowed (`https://www.billy.dk/api/`; API base in
  code must be `https://api.billysbilling.com/v2`).
- No credentials, headed browser, live mutation, UI/vision claims.
- Baseline: root commit `a3ad538` (contains merge `8c21d03`, product `8fab311`).
- If docs fetch fails on auth/quota/rate-limit before any review edit: outbox
  blocker and stop (Codex Power fallback). Do not switch agent route.

## State now

- Worktree clean at `0d70e1e` (init only); parent `a3ad538` already ancestor.
- Product files present on branch: `invoice_late_fee_writes.py`, tests, freeze
  contract + freeze ACCEPT, offline probe rules, coverage status.
- Memory empty; no prior plan or partial review page.
- Template: prior ACCEPT pages (e.g. `wiki/wave_fivem_product_independent_review.md`).

## Delegation decision

Solo. No spawn triggers: one atomic review deliverable, children capped at 0.

## Work plan (EXECUTE)

### 1. Evidence gather (scratch only under node `tmp/`)

1. Confirm baseline commits: `a3ad538`, `8c21d03`, `8fab311` and that product
   files match those commits.
2. Fetch official docs: `curl -sS -D headers -o tmp/billy-api-docs.html
   https://www.billy.dk/api/` — record ETag, Content-Length, MD5 of body.
3. Extract invoiceLateFees Supports / create-update evidence from the HTML
   (create/update present; no singular delete justification).
4. Optional unauth probes to Billy API only (historical evidence, not live
   qualification): POST/PUT JSON body expect 401; singular DELETE expect 405 —
   save JSON summary to node tmp, never claim as live testing.

### 2. Contract and inventory read

Read in full (or targeted) and note freeze claims:
- `wiki/wave_fiven_ticketed_writes_contract.md`
- `wiki/wave_fiven_freeze_independent_review.md`
- `wiki/offline_write_probe_rules.md`
- Design write protocol sections in
  `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
- `coverage/status.json` + invoiceLateFees rows in `coverage/api_v2_manifest.yaml`

### 3. Code and registration audit (assertions 1–5, 7–11)

Inspect:
- `src/billy_mcp/api/invoice_late_fee_writes.py`
- Server registration of the four tools (grep `invoice_late_fee` / tool names)
- Confirm no delete/bulk/webhook/generic HTTP/browser tools for this resource
- Paths: POST `/invoiceLateFees`, PUT escaped `/:id`
- Roots: request singular `invoiceLateFee`, response plural `invoiceLateFees`
- Preview strict outer / opaque inner; execute ticket-only
- Ticket binding + single-use + no-retry write
- Response mapping fail-closed on missing/malformed roots
- API base only `https://api.billysbilling.com/v2`
- Inventory: both create/update `implemented` + `contract_tested`; create cleanup
  text; update restoration-only; bulk save/delete empty-tool red
- Counts: 254 `api_*` tools; 174 implemented/contract_tested; live=0; vision=0;
  complete=false

### 4. Test audit and run (assertions 6, 12)

1. Read `tests/api/test_invoice_late_fee_writes.py` against freeze checklist
   (schema, no-mutation preview, canonical binding, path escape, tamper /
   wrong-executor / expiry / replay, no retry).
2. Run focused:
   - `uv run pytest tests/api/test_invoice_late_fee_writes.py -q`
   - coverage / registry checks used by prior reviews (discover exact commands
     from freeze page or nearby tests)
3. Run root non-live suite:
   `bash .fractal/main.billy_complete.wave5n_product_review_grok/scripts/test.sh`
4. ACCEPT requires all of these green; REJECT if any assertion fails with
   file:line or command output.

### 5. Author review wiki page

Write `wiki/wave_fiven_product_independent_review.md` following the
wave_fivem/wave_fivek shape:
- Frontmatter: name, title, desc, tags, sources, created/updated
- Verdict ACCEPT or REJECT (precise)
- Reviewed baseline `a3ad538`
- Official docs citation + fingerprint
- Checked assertions table (1–12)
- Commands/results
- Fail-closed boundaries / explicit non-accepts (live, UI/vision, bulk,
  singular delete, completeness)
- Distinguish: current official docs vs historical unauth probes vs offline
  tests vs unqualified live/UI/vision
- No secrets, HAR, frames, customer data

Then: `wiki update --path=wiki` and `wiki lint --path=wiki`.

### 6. Close out (same iteration if budget allows; else COMMIT then finish next)

1. Commit review page via `fractal commit` (COMMIT step) or mid-iteration commit
   if needed before finish.
2. Outbox verdict + evidence pointer.
3. Memory: brief `state` page (verdict + baseline only).
4. Run node test.sh; if ACCEPT path complete, `fractal node finish
   --reason="Wave-5n product review ACCEPT|REJECT"`.

## Budget fit

- Remaining ~$320, ~20m/step, leaf solo.
- Review is bounded: docs fetch + file reads + pytest + one wiki page.
- Trim if needed: skip re-running full suite only on REJECT with already-failed
  focused assertion; ACCEPT requires non-live suite pass.

## Risks / fail modes

| Risk | Handling |
| --- | --- |
| Docs fetch blocked | Outbox blocker; stop with zero edits |
| Count mismatch (254/174) | REJECT with exact counts |
| Inventory greened beyond freeze | REJECT; do not rewrite coverage |
| Tests fail | REJECT with command output |
| Temptation to soft-accept | Explicit non-accepts required in page |

## Out of scope

- Any edit to src/, tests/, coverage/, freeze contract
- Live authenticated API, UI, vision
- Wave-5o or other resources
- Spawning children

## Post-Mortem

### Accomplishments
- Independent offline product **ACCEPT** for Wave-5n invoiceLateFees create/update
  ticketed writes at baseline `a3ad538`.
- Authoritative deliverable: `wiki/wave_fiven_product_independent_review.md`
  (frontmatter, assertions 1–12, docs fingerprint, probes, commands, fail-closed
  boundaries). Project wiki update/lint clean.
- Reconfirmed official Supports (create/update; no singular delete) and unauth
  401/405 gates without claiming live qualification.
- Focused suite 33 passed; non-live pytest 1094 passed; node `test.sh` no-op exit 0.
- No production source, tests, coverage, or prior contract pages modified.

### Deviations
- Node `scripts/test.sh` is still a no-op; used project offline pytest
  (`-m "not live"`) as the real non-live suite evidence.
- Static `name="api_*"` grep under-counted tools (233); authoritative count is
  the registry test assertion of 254 via `create_server().list_tools()`.

### Next-iteration notes
- COMMIT the review page + wiki index (and optional node memory/plan).
- Outbox final verdict if needed, then `fractal node finish --reason="Wave-5n product review ACCEPT"` after completion drain.
- Parent may open Wave-5o freeze authoring only after this ACCEPT merges; do not
  green live/UI/bulk/delete from this review.
