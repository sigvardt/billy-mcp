# 140.1 Wave-5q users freeze independent review

## Goal

Authoritative independent Grok freeze review of committed
`wiki/wave_fiveq_ticketed_writes_contract.md`. Exclusive deliverable:
`wiki/wave_fiveq_freeze_independent_review.md` with unambiguous **ACCEPT** or
**REJECT**. Leaf only: no product code, tools, tests, coverage, status, or
contract edits.

## Delegation

Max child depth / children / descendants = 0. **Solo leaf.** No spawns.

## Current baseline (post-PREPARE)

| Item | State |
| --- | --- |
| Branch tip | `67cb8ba` merge parent `main.billy_complete` |
| Freeze under review | `wiki/wave_fiveq_ticketed_writes_contract.md` (present) |
| Research inputs | `wiki/wave_fiveq_users_freeze_ready_research.md`, `wiki/wave_fiveq_users_product_ready_research.md`, `wiki/wave_fiveq_users_freeze_ready_research_independent_review.md` |
| Offline rules | `wiki/offline_write_probe_rules.md` |
| Coverage | `coverage/api_v2_manifest.yaml`, `coverage/status.json` |
| Parent research scratch | `/Volumes/ssd_1/Repositories/billy-mcp/.worktrees/main.billy_complete/.fractal/main.billy_complete/tmp/grok-research.md` (read-only when present) |
| Peer pattern | e.g. `wiki/wave_fiveo_freeze_independent_review.md`, `wiki/wave_fivep_freeze_independent_review.md` |
| This review page | **missing** (write in EXECUTE) |
| Node memory | empty |
| Interrupted / dirty tree | clean; no `(failed on …)` backstop |

## Exact contract surface to verify

One inventory op, two future tools only:

| Inventory id | Preview | Execute | Method/path | Success root |
| --- | --- | --- | --- | --- |
| `api.users.update` | `api_users_update_preview` | `api_users_update_execute` | `PUT /users/:id` | `users` |

Must also confirm:

- Outer `id` + opaque `user` map; optional inner `id` must match outer when present
- Ticket-only execute; one write; no retry; no generic controls
- Method gates (unauth, JSON object body, no credentials): PUT 401; POST 405; singular DELETE 405
- Sensitivity / redaction boundary for PII and privilege flags
- Exclusions: create, delete, bulk, webhooks, live, UI, vision, cleanup, completeness
- ACCEPT authority: opens only a future offline Codex Power users-update product leaf; does **not** green coverage by itself, authorize live/UI/vision, bulk, create/delete tools, or completeness
- REJECT: name concrete correction; product remains blocked

## EXECUTE steps

1. **Fingerprint freeze and baseline**
   - MD5 of freeze body; record full commit SHA for reviewed tip (`67cb8ba` or later if re-merged).
   - Read freeze page end-to-end; extract exact table, input/request/response rules, exclusions, authority boundary.

2. **Independent official docs check (headless)**
   - Fetch `https://www.billy.dk/api/` (curl or headless; no credentials).
   - Record HTTP status, ETag, Content-Length, body MD5.
   - Parse `/v2/users` Supports and property table from HTML.
   - Compare to freeze claims and Research70 fingerprint (`wcw4x9hqvu3603` / MD5 `8b94b0135c91fd15fe54ea33e088a4be` / 147934 bytes; prior inventory-lock drift noted separately).

3. **Independent unauth method gates (no token, JSON object body)**
   - Base: `https://api.billysbilling.com/v2`
   - `PUT /users/<probe-id>` with `{}` → expect 401 with JSON error object
   - `POST /users` with `{}` → expect 405
   - `DELETE /users/<probe-id>` → expect 405
   - Do not create records; do not retain raw bodies in git; park any scratch under node `tmp/`.

4. **Cross-check cited research and rules**
   - `wiki/wave_fiveq_users_freeze_ready_research.md`
   - `wiki/wave_fiveq_users_product_ready_research.md` (context; not freeze authority)
   - Prior research IR if present
   - Parent `tmp/grok-research.md` when present (read-only)
   - `wiki/offline_write_probe_rules.md` (auth-gate vs 405 override)
   - Design ticketed-write discipline in `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
   - Peer freeze IR pattern (`wave_fiveo` / `wave_fivep`) for frontmatter and verdict table shape

5. **Coverage honesty (pre-product)**
   - Grep `users` rows in `coverage/api_v2_manifest.yaml`
   - Read `coverage/status.json` for complete/live/vision/contract_tested honesty
   - Confirm freeze does not green inventory; create/delete/bulk stay out of freeze surface

6. **Verdict and write review page**
   - Only write `wiki/wave_fiveq_freeze_independent_review.md` (+ optional node memory/plan).
   - Frontmatter: name, title, desc, tags, sources, created/updated.
   - Sections: Verdict table, Reviewed baseline, Sources and method (cited official evidence), Exact scope checks, Method gates, Sensitivity/redaction, Exclusions and non-authority, Residual notes, Final verdict line.
   - Unambiguous **ACCEPT** or **REJECT** (no soft pass).
   - If ACCEPT: state it opens only future offline Codex Power users-update product leaf after parent merge; not product, not greening, not live/UI/vision/bulk/create/delete/completeness.
   - If REJECT: name concrete correction; product blocked.

7. **Wiki hygiene**
   - `wiki update` then `wiki lint --path=…/wiki` for project wiki.
   - Fix only index/link fallout required for the new page (not contract edits).

8. **Node tests**
   - `bash …/scripts/test.sh` and lint script as required by completion.
   - Do not change product tests.

9. **Memory**
   - Short terminal-state memory: verdict, freeze MD5, docs fingerprint, probe outcomes, exclusive path. No iteration labels; no Remaining/NEXT.

## REVIEW / COMMIT / finish (later steps)

- Re-read deliverable for overclaim, secrets, raw evidence.
- `fractal commit` when COMMIT step runs; tree clean and in-scope (wiki + node memory/plans only).
- Final radio: page path, commit SHA, ACCEPT/REJECT, remaining gate (product leaf if ACCEPT).
- Drain: promote already done via review page; prune memory; drain saved radio.
- `fractal node finish` same iteration after tests pass.

## Non-goals (hard)

- Edit freeze contract, product code, registry, tests, coverage, status.
- Headed browser, credentials, live mutations, tracking secrets or raw probe dumps.
- Spawning children (depth/children = 0).
- Claiming product, coverage green, live/UI/vision, bulk, create/delete, cleanup, or completeness.

## Feasibility

- Budget: no limit.
- Work fits one iteration solo: docs fetch + three unauth probes + document compare + one wiki page.
- Blockers: none known. Parent research scratch optional if missing.
- Risk: docs fingerprint drift vs freeze; method-gate mismatch; over-scoped freeze (create/delete/bulk) → REJECT with concrete fix.

## Post-Mortem

### Accomplishments

- Independent docs fetch: HTTP 200, ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`, 147934 bytes (matches freeze/research).
- Independent unauth gates with JSON `{}`: PUT 401 `AUTHENTICATION_REQUIRED`; POST/DELETE 405.
- Wrote exclusive deliverable `wiki/wave_fiveq_freeze_independent_review.md` with **ACCEPT**.
- Freeze MD5 reconfirmed `c53717468aff0406799feca225a00728` at baseline `67cb8ba`.
- Coverage honesty re-derived: 177/177 offline, live 0, vision 0, bulk 92, complete false; update red; no users write tools in `src/`.
- `wiki lint` clean on project wiki; node lint/test exit 0; raw probes only under gitignored node `tmp/`.

### Deviations

- Parent `tmp/grok-research.md` was read from the main.billy_complete worktree path (research71), not under this node's seed path; treated as read-only context.
- No REJECT path taken; method gates and update-only surface matched freeze text.

### Next-iteration notes

- COMMIT must land wiki review page + node memory/plan only (no contract/product/coverage edits).
- Final radio must name page, commit SHA, ACCEPT, and remaining parent gates (merge + Codex Power product leaf).
- Then `fractal node finish` same iteration after tests still pass.
