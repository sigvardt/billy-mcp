# 118.1 Wave-5m contact-balance-payment product implementation

## Goal

Deliver exactly four offline ticketed tools for singular `contactBalancePayments`
create and update, then prove and register only the two authorised inventory
rows. The freeze is authoritative; no new research, live calls, browser work,
or scope expansion is needed. This is a leaf node (`max_depth`,
`max_children`, and `max_descendants` are all zero), so the bounded module,
tests, integration, and verification stay local.

## Baseline and constraints

- The merged baseline has 246 `api_*` tools and 170 implemented plus
  contract-tested API rows. The target is 250 and 172 respectively, with
  live/vision zero and `coverage/status.json` still `complete: false`.
- `wiki/wave_fivem_ticketed_writes_contract.md` authorises only
  `POST /contactBalancePayments` and `PUT /contactBalancePayments/:id`, with
  singular request root `contactBalancePayment`, required response root
  `contactBalancePayments`, opaque inner JSON, and no delete or bulk tools.
- Reuse the root-created `WriteProtocolService` and its one confirmation
  store. Do not modify the shared protocol or any frozen/review pages.

## Implementation

1. Add `contact_balance_payment_writes.py` by adapting the sales-tax-payment
   leaf only: strict flat create/update preview models, non-empty route ID,
   optional inner-ID equality, and only the four named create/update tools.
   Each preview constructs the exact contact-balance-payment specification;
   each execute accepts only a non-empty ticket and delegates to the shared
   protocol.
2. Register that registrar in `create_server` with the existing shared client
   and protocol. Update the narrowly necessary server registry expectation
   from 246 to 250 and add the four explicit Wave-5m names.
3. Create the focused contract suite by reusing the established local FastMCP
   helpers. Cover flat schemas and outer extra rejection, opaque-map
   preservation, update-ID mismatch, mutation-free preview, ticket binding,
   exact envelopes, encoded route IDs, one no-retry write, required-root-only
   mapping, typed auth/HTTP failures, empty tickets, tamper/wrong-executor/
   expiry/replay failures, and no delete or bulk registrations.

## Coverage and verification

1. Add only the two contact-balance-payment rows to the coverage generator's
   offline evidence map, including the focused suite and registry test. Set
   the resource's generated create cleanup wording to `live non-production
   cleanup strategy unqualified; singular DELETE is unsupported`; regenerate
   all coverage artefacts rather than hand-maintaining generated values.
2. Extend the seeded commit test script with the real Wave-5m validation
   commands, then run Ruff format/check, Pyright, focused product plus
   coverage/server suites, coverage generation and checker,
   repository-policy checks, and `BILLY_TEST_MODE=commit bash` on that script.
3. Inspect the generated arithmetic and tool inventory for 250/172/0/0 and
   false `complete` claims. Keep only required product, test, registry,
   generator, coverage, and node-test-script changes.

## Delivery

Commit the verified implementation, retain raw evidence outside Git, record
the terminal implementation state in private memory, and post one concise
handoff containing the tip, exact checks, coverage counts, and still-red
live/UI/vision/bulk/completeness gates. Then finish this node in the same run.

## Post-Mortem

- Added and registered the four authorised contact-balance-payment ticketed
  create/update tools without changing the shared confirmation protocol.
- Added a focused 19-case contract suite, exact root/count coverage evidence,
  and a commit-mode node gate. The generated inventory now reports 250 API
  tools and 172 offline implemented plus contract-tested rows while preserving
  live/vision zero and `complete: false`.
- Coverage generation requires its explicit `--write` flag; the node gate uses
  that deterministic command before checking the generated outputs.
- Parent integration must retain the independent Grok product review as a
  separate gate; this implementation result is not product acceptance.
