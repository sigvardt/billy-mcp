# 132.1 Delivered invoice reminder create

Delivered the accepted, create-only offline slice as one sequential leaf task.
The bounded module, test, server, and coverage work shares one protocol
boundary and required no child nodes.

1. Added `invoice_reminder_writes.py` with a strict `invoiceReminder` preview
   input, ticket-only execute input, `POST /invoiceReminders`, an empty
   additional-root tuple, and an `invoiceReminder` create effect.
2. Registered the two frozen tool names immediately after the invoice-late-fee
   registrar, extending the server registry assertion from 254 to 256 tools
   without update/delete/bulk names.
3. Added a focused contract module covering the flat typed schema, strict outer
   validation, opaque inner preservation, mutation-free preview, one exact
   POST, declared-root-only mapping, ticket rejection paths, typed errors, and
   no retry.
4. Greened only `api.invoiceReminders.create` through the deterministic
   coverage generator with the frozen cleanup wording, `invoiceReminders[]`
   response root, and new test references.
5. Passed focused checks, formatting, lint, strict typing, policy checks, and
   the complete non-live suite.

## Post-Mortem

- Accomplished: added the frozen two-tool invoice-reminder create module,
  server wiring, registry expectation, and 15 focused contract tests; API
  coverage is generated at 175 offline-qualified rows and 256 API tools.
- Verified: formatting, lint, strict typing, 1,110 non-live tests, coverage
  validation, and repository policy checks pass.
- Deviation: the coverage generator required parent authorization for its
  evidence and cleanup source entries. Review found that its shared write
  response-field text did not express the frozen `invoiceReminders[]` root;
  an authorized scoped override and regression test corrected it without
  changing the generic default or hand-editing generated artifacts.
