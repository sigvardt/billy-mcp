# 7.1 Coverage completion gate and manifest correction

Correct the red Phase 0 inventory without turning its reusable validation rules
into a permanent prohibition on future qualification. This is solo work: the
node is a leaf with zero depth, children, and descendants available, and the
generator, checker, generated artifacts, and focused tests are one tightly
coupled contract.

1. Fix the `bankLineMatches` root-key exception in the generator so it produces
   `bankLineMatch`, then regenerate both API and UI-parity artifacts. Assert the
   corrected singular value and absence of the old typo in focused tests while
   retaining the frozen 207/92/6/305 arithmetic.
2. Derive `status.complete` from the four API states, the additional UI vision
   state, and the absence of unresolved `ambiguous_bulk` rows. Keep the current
   data red and generated status false because the token and UI qualification
   evidence are unavailable; update the deterministic report to describe the
   derived state rather than hard-coding a forever-Phase-0 result.
3. Refactor `check_coverage.py` so ordinary validation permits a coherent red
   inventory but rejects a false green completeness claim. Add
   `--reject-false-completeness` for the root lint hook and
   `--require-complete` for full qualification; the latter must require a true
   generated status, every API and UI row green (including UI vision), and no
   unresolved bulk row.
4. Extend focused tests for the singular correction, dynamic status arithmetic,
   false-completeness rejection, and both CLI modes. Regenerate the checked-in
   coverage data, run the generator/checker, focused pytest, required Ruff
   format/lint commands, and repository-policy checker before the commit step.

## Post-Mortem

Delivered the singular root-key correction and regenerated the API and UI
parity artifacts without changing the 207/92/6/305 snapshot. The status builder
now derives `complete` from row qualification evidence and unresolved bulk
contracts, while the generated Phase 0 data remains red and `complete: false`.

The checker accepts the current red inventory in normal and root-lint modes,
rejects false green status/report claims, and makes full qualification fail
closed for the red API/UI rows and 92 ambiguous bulk entries. Focused coverage
tests, the parent lint hook, and the full suite all exercised the corrected
paths; the full suite then returned the expected qualification-gate failure.
