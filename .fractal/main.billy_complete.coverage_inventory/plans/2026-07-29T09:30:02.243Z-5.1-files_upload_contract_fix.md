# 5.1 Files upload coverage contract correction

Correct the parent-identified duplicate `POST /v2/files` planned-tool mapping
without changing the frozen 207/92/6/305 source arithmetic. This remains solo
work because the node is a leaf (`max_depth`, `max_children`, and
`max_descendants` are all zero); the change is narrow and has no separable
child scope.

1. Update `scripts/generate_coverage_report.py` so the clear Supports
   `api.files.create` record is explicitly a multipart alias of
   `api.special.files_upload`, carries the documented upload headers, and does
   not mint an `api_files_create_preview` JSON tool. Preserve the special row as
   the sole `api_files_upload_preview` planned family and regenerate every
   checked-in coverage artifact.
2. Strengthen `scripts/check_coverage.py` and the focused inventory test so a
   future generator change cannot restore a duplicate files-create tool or
   erase the multipart alias/header contract. Keep the existing red-only,
   duplicate-tool, source-count, and generated-output checks intact.
3. Run the generator, focused pytest, formatter, linter, and repository-policy
   checks. Inspect the regenerated API/UI parity rows and status arithmetic,
   then record the focused result for the commit and parent handoff.

## Post-Mortem

The correction preserved all source arithmetic while replacing the invented
JSON-style files create contract with a multipart alias. The generated API and
UI parity manifests now carry the same upload request fields, and the checker
plus focused tests fail if either row loses multipart headers or a separate
`api_files_create` tool returns. The validation suite completed with 305 API
rows, 339 UI rows, and `complete: false`; no live token or UI evidence was used.
