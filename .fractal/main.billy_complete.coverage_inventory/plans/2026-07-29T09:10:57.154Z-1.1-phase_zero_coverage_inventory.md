# 1.1 Phase 0 coverage inventory delivery

## Scope and contract

Implement this leaf directly because its configured child depth, child count, and
descendant count are all zero. The immutable contract is the approved design
(`docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`, especially
§§9–12 and §17) plus the cited parent brief. The fingerprint is
`hsisik4g9p3603` / `c2efda0ee4cf9cf200e14910c5fc6996`; no network research or
historical-gist contract inference is permitted.

## Delivery plan

1. Build the API inventory from the brief's 46-resource Supports matrix:
   207 clear conventional operations, 92 `ambiguous_bulk` entries, and exactly
   six documented prose routes. Give each row all manifest fields, documented
   list/paging/error details, explicit red implementation states, planned-only
   tool names where a concrete contract exists, and focused test references.
2. Build the interface inventory with red design-seed discovery rows for every
   approved route family and one red parity row for each API inventory row. No
   parity row will use `not_applicable`; every UI row will set
   `vision_verified: false` and contain no frame path or raw evidence.
3. Add the browser egress policy as deny-by-default: allow `mit.billy.dk` for
   UI/auth, conditionally allow `download.billy.dk` only for future typed file
   downloads, reserve `api.billysbilling.com` exclusively for the API client,
   and deny `api.billy.dk`. Do not add partner or marketing hosts.
4. Implement deterministic report generation and validation in the two owned
   scripts. The checker will validate manifest schemas and arithmetic, red-only
   Phase 0 status, bulk ambiguity, registered-tool coverage, absence of raw
   browser evidence, generated-report freshness, and false completeness.
5. Add focused tests that load the data through the scripts and assert the
   207/92/6 arithmetic, protected filters/paging/error codes, UI parity and
   egress policy, and negative checker rules. Generate `coverage/status.json`
   and the human-readable report, then run all required format, lint, pytest,
   policy, and node test-script checks.

## Post-Mortem

Delivered the deterministic Phase 0 freeze: 305 API rows (207 clear, 92
ambiguous bulk, six specials), 339 red UI discovery/parity rows, a
deny-by-default browser policy, generated status/report, a fail-closed checker,
and seven focused tests. The manifest files use JSON, a valid YAML subset, so
the verifier stays dependency-free and regeneration is byte-stable.

The only qualification limitation is intentional: `BILLY_API_TOKEN` is absent,
so implementation, contract, live, and vision states remain false. Bulk routes
remain tool-less and explicitly ambiguous; interface parity remains a discovery
map rather than unverified `not_applicable` claims.
