# 165.6 Wave-5t harness completion and verification

The prior backstop commit already contains the four owned harness paths, but
verification stopped before review when Grok authentication was unavailable.
This plan completes that committed implementation; it does not add product
tools, coverage rows, browser activity, or fresh contract claims. The node is a
leaf, so no delegation is possible.

1. Recheck the inherited implementation against the frozen Research89/88
   boundary. Keep the literal `https://api.billysbilling.com/v2` base, the
   static 29 residual plus 92 bulk candidates, `OPTIONS` only, no response
   bodies, no organisation-derived request shape, and the in-memory-only
   cleanup ledger. Preserve no-token as a zero-exit, no-client/no-network/no-
   evidence path.

2. Tighten fail-closed configuration and persistence ordering in
   `src/billy_mcp/live_probe.py`: treat blank token and organisation inputs as
   absent; reject a relative evidence path before requests; perform the
   non-mutating authenticated observation before creating an external evidence
   directory or file; and require a UUID-shaped Wave-5t test tag before a
   future resource can be tracked. Maintain the owner/mode/out-of-repository
   checks and safe redaction boundary.

3. Extend only the focused unit tests for the new fail-closed cases, including
   blank values, relative evidence paths, evidence non-creation until an
   authenticated observation occurs, and unique-tag enforcement. Keep live
   tests opt-in (`BILLY_LIVE_PROBE_RUN=1`) and safely skipped without all
   required credentials.

4. Run the focused unit and live-collection tests under `uv`, exercise the CLI
   with token and organisation values explicitly absent while checking its
   structured result and the absence of repository evidence, then run the node
   commit test script, lint script, and repository policy check. Do not attempt
   a live request.

5. Audit the final diff and coverage files: only the four owned paths may be
   changed; no credential, response, screenshot, HAR, trace, or coverage
   mutation may appear. Commit the leaf, send the commit/check summary to the
   outbox, and finish as infrastructure-only. The parent owns any independent
   Grok review after merge.
