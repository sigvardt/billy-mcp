# 170.1 Wave-5u safe method-observation contract

## Scope and decision

This leaf has no child capacity, so it will complete the single allowed wiki
page directly. The result is a research/infrastructure contract only: it will
not alter source, tests, coverage, scripts, dependencies, any other wiki page,
or the immutable node seed.

The parent research90 handoff is saved on radio and its checked-in relay is
`wiki/wave_fives_residual_specials_research.md`. The transient parent scratch
path named by that page is absent from both this worktree and the read-only main
checkout; the contract will report that provenance limitation explicitly rather
than reconstruct unobserved raw evidence. The fallback is therefore
non-authoritative and cannot replace the required Grok research/review gate.

## Work

1. Write `wiki/wave5u_method_probe_contract.md` with valid frontmatter and
   current official Billy documentation citation. Separate documented facts,
   reproducible research90 observations, and conservative inferences.
2. Freeze the three candidate kinds and their allowed static shapes: residual
   POST/PUT/DELETE routes (including a non-existent-id substitution), bulk-save
   `PUT /{resource}/bulk`, and bulk-delete `DELETE /{resource}?ids[]=`. State
   the JSON/no-body and content-type construction, then block each shape before
   network traffic where its non-persistence is unproven.
3. Encode the non-negotiable runner gate: exact
   `https://api.billysbilling.com/v2`, token plus explicitly matched dedicated
   non-production organisation, no caller-controlled route, owner-only
   external evidence, and a five-field sanitised evidence record. Make clear
   that `401`, `405`, empty `OPTIONS` `204`, and meta-only `200` do not qualify
   a method, mutation, contract, or coverage row.
4. Update the generated wiki index only if required, lint the wiki, run the
   supplied node test script, and inspect the three-dot diff for the one allowed
   page plus any generated index. Record the research-only fallback conclusion,
   clear the saved handoff, and finish with the mandatory Grok gate still open.

## Post-Mortem

### Accomplishments

- Delivered the cited, fail-closed Wave-5u method-observation contract and its
  generated project-wiki index entry.
- Re-derived the frozen matrix as 29 residual rows and 46 bulk resource pairs
  (92 rows), then kept every real-method candidate blocked before network.
- Verified wiki lint, repository lint, and the full configured offline test
  suite without changing source, tests, coverage, scripts, or dependencies.

### Deviation

The transient research90 parent scratch brief was absent from the fallback
checkout. The contract cites it explicitly, relies on its checked-in relay for
the reproducible observation summary, and records the missing raw artifact
instead of reconstructing it.

### Completion state

The required Grok gate remains open; this Codex Power fallback is research-only
and does not qualify an operation, a live method, or a coverage row.
