# 41.1 Account ticketed writes

1. Mirror the accepted `daybook_writes` resource-leaf pattern in a new
   `account_writes` module, exposing exactly the six preview/execute pairs for
   `accountGroups` and `accounts` through the shared `WriteProtocolService`.
2. Add offline contract tests for the twelve registrations and their schemas,
   non-mutating previews, exact POST/PUT/DELETE request construction, stable
   errors, and confirmation ticket tamper/replay/expiry/binding rejection.
3. Run the focused tests, Ruff, Pyright, and node scripts; repair only the two
   owned files. Commit the clean scoped delivery, report the evidence, and
   finish the node after its completion drain.

The work is not delegable because this node is a leaf: depth, children, and
descendants are all capped at zero. The cited offline contract and accepted
Wave-5 protocol provide the required research basis; no live or browser work is
planned.

## Post-Mortem

### Accomplishments

- Added the twelve frozen account-group and account preview/execute tools using
  the shared write protocol.
- Added 32 offline contract tests covering schemas, exact requests, typed
  errors, and confirmation-ticket failures.
- Validated the owned files with focused pytest, Ruff, Pyright, and node
  scripts.

### Deviations

None. The implementation stayed within the two owned product files and did not
attempt registration, coverage, live, UI, or bulk work.

### Next-iteration notes

No further implementation is required in this leaf; only scoped commit and
completion handoff remain.
