# 30.7 Wave-5a response-mapping delivery

## Adopted state and scope

The prior committed protocol change already carries the primary root and
`additional_plural_roots` from `WriteOperationSpec` into the prepared ticket,
and maps each present declared changed-record root. This delivery completes the
remaining documented multi-root metadata mapping without widening scope.

All child limits are zero, so this is one atomic leaf change. Product edits are
limited to `src/billy_mcp/api/write_protocol.py` and
`tests/unit/test_write_protocol.py`; research, registration, coverage, client,
confirmation, auth, UI/browser, inventory, bulk, and shared-wiki work remain
out of scope.

## Implementation

1. Pass every declared changed plural root to the deleted-record mapper, so
   `meta.deletedRecords` retains each present declared list of string IDs,
   ignores undeclared entries, omits absent declared entries, and retains the
   current typed invalid-success errors for malformed declared data.
2. Add a local MockTransport delete fixture for a products-plus-productPrices
   response, including an undeclared root to prove filtering. Keep the existing
   primary-only, missing-additional-root, malformed-additional-root, ticket,
   canonical-binding, escaped-path, locked-relative-path, no-retry, and
   delete-empty-response tests unchanged.

## Verification and delivery

Run the focused protocol unit module, then the supplied node lint and non-live
test scripts. Inspect the committed diff and clean status, report the commit
and checks through the outbox, and finish the leaf. Parent integration and
independent review are explicitly outside this delivery.
