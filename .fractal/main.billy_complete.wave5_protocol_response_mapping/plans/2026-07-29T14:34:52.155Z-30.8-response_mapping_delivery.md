# 30.8 Wave-5a response-mapping delivery

## Adopted state

The previous saved delivery already changes only the owned protocol and unit
test files. Its reviewer step stopped before edits because Grok authentication
was unavailable, so the committed bytes—not an uncommitted experiment—are the
implementation candidate. This leaf is childless, so no delegation is possible.

## Execution

1. Inspect the committed protocol diff to verify that the immutable primary and
   additional plural declarations survive preview into the prepared write, map
   every present declared changed or deleted root, and ignore absent or
   undeclared roots while retaining typed malformed-success errors.
2. Run the focused unit module using only its MockTransport fixtures. Confirm
   the products-plus-productPrices success and multi-root delete metadata
   coverage alongside the primary-only, absent-root, ticket, path, and no-retry
   regressions.
3. Run the node lint and non-live test scripts. If any check exposes a frozen
   contract gap, change only the two owned files and rerun the affected checks.
4. Inspect the committed bytes and clean scoped status, report the exact commit
   and all checks to the parent, then finish. Parent integration and independent
   review remain outside this leaf.
