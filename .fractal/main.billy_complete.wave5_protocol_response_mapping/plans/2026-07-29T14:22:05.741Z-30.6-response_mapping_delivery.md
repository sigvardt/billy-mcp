# 30.6 Wave-5a response-mapping delivery

## Adopted delivery

Commit `ac70401` already contains the owned Wave-5a protocol and unit-test
bytes. Its prior independent-review stop was an authentication failure before
any reviewer edit, not a product-test failure. This pass validates that committed
delivery against the frozen brief rather than reimplementing it.

## Scope and execution decision

All child limits are zero, so there is no delegation path. The committed design
uses `plural_root` as the required primary root and
`additional_plural_roots` as a server-known immutable declaration. Execution
maps each declared present list-of-object root, ignores undeclared and absent
additional roots, and retains delete plus primary-root deleted-record semantics.

The execution phase will:

1. Inspect the committed product diff and confirm no product file outside
   `src/billy_mcp/api/write_protocol.py` and
   `tests/unit/test_write_protocol.py` changed for this leaf.
2. Run the focused protocol unit module, including the products-plus-
   productPrices and absent-additional-root fixtures.
3. Run the supplied lint and non-live test scripts. If a contract gap or test
   regression appears, change only the two owned files and repeat the relevant
   checks; otherwise preserve the already committed product bytes.
4. Review the final tree and delivery history, report the exact product commit
   and checks through the outbox, and finish. Parent integration and an
   independent reviewer are expressly outside this leaf's delivery scope.
