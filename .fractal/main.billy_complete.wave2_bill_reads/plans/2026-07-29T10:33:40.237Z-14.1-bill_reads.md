# 14.1 Bill Read Tools

Implement the isolated Wave 2 bills read slice. This leaf has zero child-depth,
child-count, and descendant allowance, so the two tightly coupled module/test
files will be completed directly.

1. Mirror the established typed read-tool structure in a new `bill_reads.py`:
   strict request models, the exact documented bill sort enums and filters,
   opaque payload/success models, response-root validation, optional
   `meta.paging`, and only relative `/bills` GET calls through `BillyHttpClient`.
2. Register exactly `api_bills_get` and `api_bills_list` on an injected
   `FastMCP` server, without editing root server wiring or coverage state.
3. Add focused mock-transport tests for URL/query construction, singular and
   list mapping with and without paging, local rejection of unknown and
   invoice-only inputs, invalid documented enum values, both documented 401
   envelopes, and exact FastMCP registration/schema.
4. Run Ruff format/check, Pyright, the focused pytest file, node lint, and the
   non-live node test script. Fix only the two owned files if validation exposes
   a local defect.
5. Review the scoped diff, commit through Fractal, report the committed hash and
   exact test results via radio, drain durable-node state, and finish the node.

## Post-Mortem

### Accomplishments

- Delivered the two typed bill reads with exact documented query allowlists,
  optional paging mapping, and typed client failures.
- Added 22 focused mock-transport contract tests, including the documented 401
  envelopes and the complete FastMCP tool schemas.
- Verified Ruff, Pyright, node lint, and the non-live test suite without a live
  API request or coverage transition.

### Deviations

None. Server registration and coverage state remain root-owned as required.

### Next iteration notes

No further leaf work is needed. Root can wire the isolated registration and
update coverage evidence in its own scope.
