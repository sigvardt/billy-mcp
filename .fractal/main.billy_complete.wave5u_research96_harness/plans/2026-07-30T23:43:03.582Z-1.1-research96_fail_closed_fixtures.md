# 1.1 Research96 fail-closed harness fixtures

1. Extend `src/billy_mcp/live_probe.py` with strict frozen, static-only
   Research96 fixtures: exact unauthenticated outcomes for all 29 residual
   candidates, a query-only canonical bulk-delete template plus rejected JSON
   and form-body variants, and the organisation-path ambiguity record. Keep
   every fixture incapable of enabling real-method traffic or changing
   coverage/tool state.
2. Extend `tests/unit/test_live_probe.py` to assert exact membership,
   classification/status validation, immutable/adversarial rejection,
   repeated `ids[]` encoding with no body, path ambiguity semantics, and the
   existing missing-token/missing-gate no-network behavior.
3. Run the focused unit test, Ruff format/check, and Pyright for only the two
   owned project files. If clean, run the node commit-mode suite when practical
   before committing the scoped implementation.

## Post-Mortem

### Accomplishments

- Added strict frozen static fixtures for the exact Research96 residual outcome
  matrix, canonical repeated `ids[]` bulk delete form, rejected body variants,
  and organisation-path ambiguity.
- Kept the runner unchanged and OPTIONS-only; no tool, coverage, credential,
  cleanup, or live-network behavior was added.
- Focused pytest, Ruff format/check, Pyright, and the node test and lint scripts
  pass.

### Deviations

- None.

### Next-iteration notes

- No further implementation work is planned for this completed slice.
