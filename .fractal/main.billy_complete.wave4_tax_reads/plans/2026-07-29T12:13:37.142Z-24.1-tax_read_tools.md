# 24.1 Wave 4 Tax Read Tools

Scope is limited to `src/billy_mcp/api/tax_reads.py` and
`tests/api/test_tax_reads.py`. This node is a leaf (`max_depth`, `max_children`,
and `max_descendants` are zero), so implementation and verification remain
local. The frozen Wave-4 contract and Grok brief are the sole evidence sources;
do not fetch documents, probe live endpoints, or alter coverage, registration,
redaction, wiki, or generated files.

1. Build `tax_reads.py` from the Wave-3 `account_reads.py` typed-read pattern:
   strict flat request models, free-form documented sort property with only
   `ASC`/`DESC` direction, encoded IDs, opaque records, optional
   `meta.paging`, unexpected-root errors, and the locked client.
2. Implement all eight exact resource pairs—tax rates, deduction components,
   sales-tax rulesets, rules, accounts, meta fields, returns, and payments—at
   their frozen collection paths and roots. Expose only get `id`/`include` and
   default list controls; register the module-local sixteen flat MCP tools.
3. Add parameterized contract coverage for every get/list route and root,
   encoded IDs, list query construction, paging present and absent, strict
   validation (including offset and all named undeclared tax filters), both
   typed 401 envelopes, unexpected roots, and the exact registry schemas.
4. Format and run the focused pytest suite, Ruff format/check, and Pyright;
   correct only owned files. Confirm the diff contains no live, UI, coverage,
   write, bulk, credential, or browser-evidence changes.
5. Commit the two owned product files in the commit step and report the commit
   and offline verification results through the parent-visible radio outbox.

## Post-Mortem

- Delivered sixteen module-local, GET-only tax tools across the eight frozen
  resources, with strict flat request schemas, encoded IDs, opaque records,
  optional `meta.paging`, and typed Billy authentication errors.
- Added focused tests for all paths and roots, documented-only query construction,
  absent/present metadata, unknown-key rejection, malformed singular and list
  roots, authentication mapping, and the exact sixteen-tool registry schema.
- The independent review passed. Its optional list-root-error test gap was
  addressed. Root registration and evidence-map integration remain outside this
  leaf.
- Verification passes: focused pytest 163, non-live project suite 387, Ruff
  format/check, and Pyright. Full qualification ran its tests successfully and
  then correctly failed closed because coverage remains incomplete (92 ambiguous
  bulk rows and all outstanding live/UI qualification states).
- Cleanup state: no coverage, live, UI, browser, write, bulk, credential, or
  completeness claims were added. Parent registration and coverage evidence
  integration remain explicitly outside this leaf.
