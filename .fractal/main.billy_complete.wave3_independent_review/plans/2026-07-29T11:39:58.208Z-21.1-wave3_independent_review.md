# 21.1 Wave-3 independent review

## Goal

Independent Grok review of Wave-3 documented read-only API integration on
branch `main.billy_complete.wave3_independent_review` @ `a206eb0`. Sole owned
deliverable: `wiki/wave_three_independent_review.md`. No production code, test,
coverage, credential, or browser-evidence edits.

## Constraints

- Leaf node: `MAX_DEPTH=0`, `MAX_CHILDREN=0`, `MAX_DESCENDANTS=0` — no spawn.
- Scope is `wiki/`; only the report may be committed.
- Do not mark live or UI qualification complete.
- Required discrepancies go to parent radio immediately (exact row + evidence).
- If none: report and outbox both state that explicitly.
- Finish in this iteration after non-live test + lint pass.

## Spawn decision (out loud)

| Trigger | Applies? |
| --- | --- |
| Separable subtask with real depth | No — single wiki report, one reviewer |
| Parallel independent subtasks | No — one sequential cross-check |
| Clean context for independence | N/A — this node *is* the independent review |

**Decision: solo.** Spawning forbidden by caps and unnecessary for one report.

## Interrupted work

- Clean tree aside from node seed `.fractal/...` (untracked, expected).
- HEAD is Wave-3 integrate commit; not a backstop save.
- Memory empty; report not yet written.
- Inventory preview (read-only): all 22 Wave-3 get/list rows already
  `implemented=true`, `contract_tested=true`, `live_tested=false`.

## Sources (EXECUTE read set)

1. Design: `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md`
2. Official docs (live fetch): https://www.billy.dk/api/ — capture etag/MD5/size
3. Contract: `wiki/wave_three_unfiltered_reads_contract.md`
4. Related freeze docs if needed for redaction patterns:
   `wiki/phase_zero_contract.md`, prior wave contracts
5. Five Wave-3 modules + tests:
   - `src/billy_mcp/api/line_reads.py` + `tests/api/test_line_reads.py`
   - `src/billy_mcp/api/contact_person_reads.py` + `tests/api/test_contact_person_reads.py`
   - `src/billy_mcp/api/daybook_reads.py` + `tests/api/test_daybook_reads.py`
   - `src/billy_mcp/api/account_reads.py` + `tests/api/test_account_reads.py`
   - `src/billy_mcp/api/file_attachment_reads.py` + `tests/api/test_file_attachment_reads.py`
6. Registry: `src/billy_mcp/server.py`
7. Coverage: `coverage/api_v2_manifest.yaml`, `coverage/status.json`
8. Shared redaction helpers as referenced by the modules (read-only)

Scratch only under node `tmp/` if needed for doc dumps (not committed).

## 22-row matrix (must all appear in report)

| Resource | Rows |
| --- | --- |
| invoiceLines | get, list |
| billLines | get, list |
| daybookTransactionLines | get, list |
| contactPersons | get, list |
| daybooks | get, list |
| daybookBalanceAccounts | get, list |
| accounts | get, list |
| accountGroups | get, list |
| accountNatures | get, list |
| files | get, list |
| attachments | get, list |

Per row assess:

1. Official route/method vs docs + contract
2. Get/list inputs (allowlist only: page, pageSize 1–1000, include, sortProperty, sortDirection ASC|DESC; no offset; no invented parent filters)
3. Pagination and sorting
4. Response roots (singular/plural)
5. Typed success + error handling (esp. 401 mapping)
6. Server registry presence
7. Coverage evidence (`implemented` / `contract_tested` / `live_tested`)
8. Sensitive-value redaction (accounts bank fields; files `downloadUrl`; contact person email)

Verdict vocabulary per finding: **required** | **blocker** | **advisory**.
"Required" = must fix before parent trusts Wave-3 offline green; send parent radio.
"Blocker" = blocks product completeness claims beyond this wave (e.g. live/UI still red — expected, not a Wave-3 code defect unless falsely greened).
"Advisory" = nits that do not invalidate the offline contract claim.

## Report shape (`wiki/wave_three_independent_review.md`)

Frontmatter with `desc:` (and name/tags as wiki convention).

Body:

1. Revision reviewed (`a206eb0` full SHA), branch, method
2. Official source URL(s) + live docs fingerprint (etag/MD5/bytes) + access time
3. Verified facts vs inferences (explicit section)
4. Summary verdict for Wave-3 offline integration
5. Table of all 22 rows with pass/fail dimensions
6. Actionable findings classified required / blocker / advisory
7. Explicit statement on live/UI: not qualified; credentials outside scope
8. No secrets, PII, screenshots, HAR, frames, traces

## EXECUTE order

1. Fetch official docs; record fingerprint; extract Wave-3 resource TOC/Supports/query notes.
2. Read contract + design slice for Wave-3 expectations.
3. Read five modules + tests + server registry + coverage rows.
4. Cross-check each of 22 rows; note discrepancies as verified vs inferred.
5. Write the wiki report (overwrite if partial).
6. `wiki update --path=wiki` if needed for index/desc.
7. If any **required** finding: `fractal radio send` to parent (or post + parent-targeted send) with exact `api.<resource>.get|list` id and evidence; do not wait.
8. Else: outbox states no required discrepancies.
9. Seed memory `state` page with durable review outcome (no iteration labels).
10. Leave commit/test/finish for COMMIT step cadence, but ensure report is complete so COMMIT can ship.

## COMMIT / finish (same iteration)

1. Stage only `wiki/wave_three_independent_review.md` (+ wiki index if auto-updated).
2. `fractal commit` (or equivalent step protocol) — scope wiki only.
3. Run node `scripts/test.sh` and `scripts/lint.sh`; must pass.
4. Drain: memory terminal; saved radio empty; outbox final verdict.
5. `fractal node finish --reason="wiki/wave_three_independent_review.md: Wave-3 22-row independent review"`.

## Budget fit

- Time remaining ~10 min/step class, ~590s shown at plan time for node window —
  plan is single-pass review + one wiki file; no children.
- Cost: unlimited.
- Risk: docs fetch latency — use open_page/curl once into `tmp/`, then offline.

## Out of scope

- Implementing fixes (parent/codex-power owns product code).
- Live probes with real tokens.
- UI / vision qualification.
- Greening coverage beyond what inventory already claims.
- Editing any file other than the owned wiki report (and memory for self).

## Post-Mortem

### Accomplishments

- Fetched official docs; fingerprint matches freeze (etag hsisik4g9p3603, MD5 c2efda0ee4cf9cf200e14910c5fc6996, 147934 bytes).
- Reviewed all 22 Wave-3 rows against contract, five modules/tests, server registry, coverage, redaction helper.
- Delivered wiki/wave_three_independent_review.md with desc, cited sources, full 22-row table, required/blocker/advisory findings.
- Radioed required meta.paging envelope finding for api.files.list and api.attachments.list (BC8031B3).
- Non-live test.sh and lint.sh exited 0 during execute.
- Claim re-check in review step confirmed inventory flags, file model shape, and registry.

### Deviations

- Plan assumed possible zero required findings; found one tool-envelope mismatch (wire paths still correct).
- Nested FastMCP request schema for files kept advisory (parent also flagged as medium residual).

### Next-iteration notes

- COMMIT: stage only wiki report (+ wiki index); re-run test/lint; finish with reason naming the report.
- Parent/codex-power owns the meta.paging envelope fix; this leaf does not edit product code.
