# 1.1 Wave-5u fail-closed bulk-delete form matrix

## Scope and constraints

Implement only `src/billy_mcp/live_probe.py` and
`tests/unit/test_live_probe.py`. This node is a leaf (`max depth`, `max
children`, and `max descendants` are all zero), so the bounded two-file change
stays local. Preserve the literal Billy API base, existing redaction, and the
current OPTIONS-only observation loop. Do not add tools, credentials, live
flags, browser/auth work, coverage changes, or a real-method request path.

## Plan

1. Add a small immutable, typed bulk-delete form assessment that fixes the sole
   query name to `ids[]`, records the research95 empty-form validation failure
   (`INVALID_DELETE_ID_ARRAY`), and marks both empty and synthetic-id meta-only
   observations as non-qualifying / never-safe-no-op.
2. Add a default-deny real-method gate for every frozen residual and bulk
   candidate. It will represent the required non-persistence, dual-organisation,
   owner-only evidence, cleanup, and independent-read-back prerequisites while
   leaving the OPTIONS transport code unchanged and unqualified.
3. Extend focused unit coverage for the exact `ids[]` matrix invariant, every
   known empty input form being a validation error rather than a no-op, and a
   synthetic-id metadata-only success remaining unable to permit a real network
   method. Retain the existing no-network/default and redaction assertions.
4. Run the focused module tests, then the node lint and test scripts. Inspect
   the final diff and status to confirm only the two owned product files changed
   and coverage/status files remain untouched.

## Post-Mortem

- Added an immutable research95 form matrix and a literal `ids[]` candidate
  invariant without changing the OPTIONS-only runner.
- Added default-deny real-method safety fields and focused checks covering empty
  validation failures, metadata-only 200, redaction, and no-network gates.
- No scope deviation: no tools, credentials, browser/auth work, coverage/status
  changes, or real API traffic were introduced.
- Ready for the required commit; no further implementation is needed.
