# 141.1 Wave-5q users product-ready independent review

## Goal

Independent Grok **ACCEPT as research** or **REJECT** of the committed package
`wiki/wave_fiveq_users_product_ready_research.md`. Deliver only
`wiki/wave_fiveq_users_product_ready_research_independent_review.md`.

This leaf does **not** implement product, register tools, edit freeze contract,
coverage, tests, or status. Verdict is research-only; freeze independent ACCEPT
and product remain separate gates.

## Delegation decision

`max_child_depth=0`, `max_children=0`, `max_descendants=0` → **solo leaf**.
No spawn. Independent verification is this node’s job.

## Inputs (read-only)

| Source | Role |
| --- | --- |
| `wiki/wave_fiveq_users_product_ready_research.md` | Package under review |
| `wiki/wave_fiveq_ticketed_writes_contract.md` | Freeze page (MD5 check) |
| `wiki/wave_fiveq_users_freeze_ready_research.md` | Prior freeze-ready research |
| `wiki/wave_fiveq_users_freeze_ready_research_independent_review.md` | Prior research ACCEPT (freeze-ready only) |
| `wiki/offline_write_probe_rules.md` | Probe semantics |
| `coverage/api_v2_manifest.yaml` + `coverage/status.json` | Honesty / 177 offline baseline |
| `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` | Design constraints |
| `wiki/wave_fivep_product_ready_research_independent_review.md` | Page shape template |
| `.fractal/main.billy_complete/tmp/grok-research.md` | Scratch if present (may be absent in worktree) |
| https://www.billy.dk/api/ | Official docs re-fetch |
| https://api.billysbilling.com/v2 | Unauth method probes only |

## EXECUTE checklist

1. **Docs re-fetch (headless, no credentials)**
   - GET `https://www.billy.dk/api/`; record HTTP, ETag, body bytes, MD5.
   - Compare to research claim: ETag `wcw4x9hqvu3603`, MD5
     `8b94b0135c91fd15fe54ea33e088a4be`, 147934 bytes.
   - Confirm `/v2/users` Supports: update open; create + singular delete omitted;
     bulk save/delete remain inventory-only.

2. **Unauth method probes (no credentials, no records)**
   - Base `https://api.billysbilling.com/v2`.
   - POST `/users` with `{}` → expect 405 `METHOD_NOT_ALLOWED`.
   - PUT `/users/<nonempty-id>` with `{}` → expect 401 `AUTHENTICATION_REQUIRED`.
   - DELETE `/users/<nonempty-id>` → expect 405 `METHOD_NOT_ALLOWED`.
   - Optional empty-body PUT control → 400 `INVALID_REQUEST_BODY` (not method-closed).
   - Store raw probe outputs only under node `tmp/` (git-ignored); never commit.

3. **Package vs freeze / protocol**
   - Confirm prospective tools only:
     `api_users_update_preview` / `api_users_update_execute`.
   - Method/path: PUT `/users/:id`.
   - Schema: strict outer `id` + opaque `user` map; optional matching inner `id`;
     success root `users`; shared ticket protocol; one no-retry write.
   - Freeze page MD5 `c53717468aff0406799feca225a00728` matches on-disk freeze.
   - Confirm freeze independent review **not** claimed as ACCEPT by this package;
     product source barred until freeze IR ACCEPT.

4. **PII / privilege + fail-closed cleanup**
   - Research must flag users as PII/privilege-sensitive.
   - Cleanup language must not assume singular DELETE (405 closed).

5. **Coverage arithmetic + non-claims**
   - status.json: `contract_tested_rows` / `implemented_rows` = 177,
     live/vision 0, complete false, 92 bulk ambiguous.
   - Research target +1 → 178 offline after product (not greened here).
   - Confirm non-claims: no create/delete/bulk/webhook tools; no live/UI/vision;
     no complete; no product source; no freeze ACCEPT.

6. **Verdict page**
   - Write `wiki/wave_fiveq_users_product_ready_research_independent_review.md`
     with valid frontmatter, cited evidence, explicit **ACCEPT as research** or
     **REJECT**.
   - Pattern after `wave_fivep_product_ready_research_independent_review.md`.
   - Optional scratch: `.fractal/main.billy_complete.wave5q_product_ready_review/tmp/grok-review.md`
     (git-ignored; do not track secrets).

7. **Wiki hygiene**
   - `wiki update` then `wiki lint --path=wiki` (fix links/frontmatter).

8. **Node scripts**
   - `bash .fractal/main.billy_complete.wave5q_product_ready_review/scripts/test.sh`
   - `bash .fractal/main.billy_complete.wave5q_product_ready_review/scripts/lint.sh`
   - Must pass; no tracked sensitive evidence.

9. **Radio + finish (same iteration after COMMIT)**
   - Outbox: page path, commit SHA, verdict, remaining gate
     (freeze independent ACCEPT before product leaf).
   - Drain: promote nothing extra (review page is the durable deliverable) or
     one-line outbox if nothing else promotes.
   - Terminal memory; drain saved queue; `fractal node finish`.

## REJECT triggers (any one)

- Docs fingerprint material change that invalidates Supports/properties claims.
- Probe mismatch (PUT not 401 open, POST/DELETE not 405 closed).
- Package expands beyond two-tool update pair.
- Claims freeze ACCEPT, product ACCEPT, live/UI/vision, completeness, or greens coverage.
- Missing fail-closed exclusions or incorrect 177→178 arithmetic.
- Requires credentials or headed browser to “prove” claims.

## ACCEPT as research criteria (all required)

- Independent re-fetch and probes corroborate package.
- Exact two-tool users update scope only.
- Separate freeze-review gate still required before product.
- Non-claims and fail-closed language present and correct.
- Review page non-sensitive and cited.

## Out of scope this leaf

- Any `src/`, tool registration, tests under `tests/`, coverage edits, freeze
  page edits, live credentials, UI/browser automation beyond headless docs GET,
  product implementation plan authorisation.

## Feasibility

- Budget: no limit. Solo review: docs fetch + 3 probes + page write + wiki/test
  scripts in one EXECUTE pass. No children. No blockers beyond possible CDN
  drift (treat as REJECT only if Supports/gates change).

## Done when

Completion Requirements 1–5 satisfied and `fractal node finish` run in the
iteration that lands the commit.

## Post-Mortem

### Accomplishments
- Independent docs re-fetch matched research71 fingerprint (ETag/MD5/bytes).
- Unauth method gates reconfirmed: PUT 401 open for freeze path; POST/DELETE 405 closed.
- Freeze page MD5 and two-tool surface corroborated; freeze IR still separate.
- Coverage honesty 177/0/0/`complete:false` and 177→178 arithmetic checked.
- Deliverable wiki page written with explicit **ACCEPT as research** and non-claims.
- Project `wiki update`/`wiki lint` and node test/lint scripts passed.
- Parent Review71 feed note aligns; no conflict.

### Deviations
- Parent `.fractal/main.billy_complete/tmp/grok-research.md` was absent in this worktree; package wiki page + live re-fetch/probes were sufficient.
- Empty-body PUT 400 control reconfirmed; not used as a method-open signal.

### Next-iteration notes
- COMMIT the review page + wiki `_index` only (no product/coverage/freeze edits).
- Final radio must name page, commit SHA, ACCEPT as research, remaining freeze IR gate; then `fractal node finish` after test re-run.
- Do not promote probe JSON or HTML into git.
