# 134.1 Wave-5o product independent review

## Goal

Independent Grok offline product-slice review of merged Wave-5o invoice-reminder
**create** tools at parent baseline `afbe5188b029e89e7e865a6b9ee8b9a5881f51f4`
(product merge `78108f2`). Sole tracked deliverable:
`wiki/wave_fiveo_product_independent_review.md` (+ index row from `wiki update`).

**Out of scope:** any product source, tests, coverage, status, prior contracts,
other wiki pages, live/UI/vision/completeness claims, credentials, headed browser.

## State (orient)

| Fact | Value |
| --- | --- |
| HEAD | `0379cd4` (node init); baseline `afbe518` is ancestor; parent merge already up to date |
| Product file | `src/billy_mcp/api/invoice_reminder_writes.py` present (~82 LOC) |
| Tests | `tests/api/test_invoice_reminder_writes.py` present (~330 LOC) |
| Freeze preconditions | `wiki/wave_fiveo_ticketed_writes_contract.md` + ACCEPT `wiki/wave_fiveo_freeze_independent_review.md` |
| Honest counts (status.json now) | `implemented_rows`/`contract_tested_rows` 175; live 0; vision 0; `api_ambiguous_bulk` 92; `complete: false` |
| Registry target | 256 `api_*` tools (freeze had 254 pre-product; +2 create tools) |
| Deliverable page | does **not** exist yet |
| Children | max_depth/children/descendants all **0** → leaf; **no spawn** |
| Interrupted work | clean tree; no backstop `(failed on …)` commit; no prior plan |
| Budget | ~$319.7 remaining, ~20m/step; leaf review fits one EXECUTE |

## Delegation decision

Solo leaf. Triggers for children (separable depth, parallel, clean context) do
not apply under caps of 0. One review page, one baseline, no parallel ownership
split.

## Review template

Mirror structure of `wiki/wave_fiven_product_independent_review.md` and recent
product reviews (`wave_fivek`/`wave_fivem`):

- YAML front matter (`name`, `title`, `desc`, `tags`, `sources`, `created`/`updated`)
- Verdict table + explicit **ACCEPT** or actionable defects
- Reviewed baseline table (`afbe5188`, merge `78108f2`)
- Evidence classes (docs vs unauth probes vs offline tests vs live/UI)
- Official docs fingerprint re-fetch + invoiceReminders Supports/properties
- Safety checklist (two tools only; no update/delete/bulk/webhook/generic HTTP/browser;
  flat preview; ticket-only execute; mutation-free preview; one non-retried
  `POST /invoiceReminders`; ticket bind/expiry/replay/wrong-executor; map only
  `invoiceReminders`; server registration; focused tests; generator/manifest honesty)
- Fail-closed non-claims for live/UI/vision/cleanup/bulk/webhooks/completeness

## EXECUTE checklist (ordered)

1. **Docs evidence (locked host only).** Fetch `https://www.billy.dk/api/` via
   curl (no headed browser). Record ETag, body size, MD5. Confirm target
   `wcw4x9hqvu3603` / 147934 / `8b94b0135c91fd15fe54ea33e088a4be` or document
   access-metadata churn vs Supports stability (same rule as freeze review).
   Parse `/v2/invoiceReminders` Supports + property notes. Cache body under node
   `tmp/` only if needed; never commit raw HTML.

2. **Optional unauth method probes (evidence only).** Against
   `https://api.billysbilling.com/v2` with empty JSON object, no credentials:
   POST create → expect 401; PUT/DELETE singular and bulk DELETE → expect 405 per
   freeze. Do not claim live qualification. No secrets.

3. **Read freeze + product evidence (read-only).**
   - Contract: `wiki/wave_fiveo_ticketed_writes_contract.md`
   - Freeze ACCEPT: `wiki/wave_fiveo_freeze_independent_review.md`
   - Code: `invoice_reminder_writes.py`, registration in `server.py`, ticket
     protocol in `write_protocol.py` / client path lock
   - Tests: `tests/api/test_invoice_reminder_writes.py` (+ coverage unit hooks if any)
   - Diff: `git show 78108f2 --stat` and product commit contents at baseline
   - Coverage: create row greened offline; update/delete absent; bulk still red;
     status honesty 175/175/0/0/92/`complete:false`
   - Registry count: assert 256 `api_*` (read-only pytest or small Python import
     of tool names — do not edit product)

4. **Assertion matrix.** Independently verify each instruction bullet; fail-closed
   on any missing binding, nested schema, extra tools, retries, wrong response
   root, or honest-count mismatch. Verdict = **ACCEPT** only if all pass for the
   offline slice; else list concrete defects with file:symbol and fix.

5. **Write deliverable.** Create only
   `wiki/wave_fiveo_product_independent_review.md` with full citations. Run
   `wiki update --path=wiki` then `wiki lint --path=wiki`. Do not touch other
   wiki pages except generated `_index.md` row.

6. **Node gates.** Run node `scripts/test.sh` and `scripts/lint.sh`;
   `git diff --check`. Optionally focused offline pytest for the two tools as
   review evidence only (no product edits).

7. **Memory.** Write short private status in memory (what stands, no iteration
   labels): baseline, verdict, docs fingerprint, path of review page.

## REVIEW / COMMIT / finish (later steps)

- Self-check: only review page + index in project diff; no product/tests/coverage
  churn.
- `fractal commit` with clear message; report verdict + commit SHA via radio
  outbox/`--parent` as COMMIT step requires.
- Drain: memory terminal; saved radio empty; outbox verdict if not already sent;
  `fractal node finish` in same iteration once gates green.

## Budget trim

One leaf iteration is enough. No research children, no product rework, no
multi-page wiki. If ACCEPT fails on defects, still ship the review page with
actionable REJECT list (that is a complete deliverable). Do not implement fixes
here (out of ownership).

## Non-goals restated

No generic HTTP tools, no browser automation beyond forbidden, no credential
use, no greening beyond what product already committed, no completeness ACCEPT.


## Post-Mortem

### Accomplishments

- Independent offline product ACCEPT for Wave-5o invoiceReminders create at baseline
  `afbe5188` (merge `78108f2`, implement `81bf925`).
- Deliverable `wiki/wave_fiveo_product_independent_review.md` written with official docs
  fingerprint, unauth method gates, full assertion matrix, honesty counts, and
  explicit non-accepts.
- Wiki index row generated; `wiki update` / `wiki lint` clean for project wiki.
- Focused offline pytest green; no product source/tests/coverage edits.

### Deviations

- Node `scripts/test.sh` remains orchestrator no-op; verification used direct
  `uv run pytest` on the write suite and coverage unit/inventory tests instead.
- Unauth probe shell helpers needed absolute `/usr/bin/curl` under a thin PATH;
  results still recorded from successful curls.

### Notes for commit / finish

- Project commit should include only the review page and `wiki/_index.md`.
- Report verdict ACCEPT + commit SHA to parent via radio after `fractal commit`.
- Then drain memory, saved radio, and `fractal node finish`.
