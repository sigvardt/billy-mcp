# 159.2 Upload digest repair completion

## Current state

The prior run's backstop commit `dd7ec8a` contains the intended two-file
repair: `execute` hashes `file_bytes` after the final read and immediately
before `post_file`, returning `FILE_CHANGED` on mismatch. Its deterministic
patched-read regression replaces the file after identity revalidation and
asserts that the recorded HTTP request list remains empty. The present
uncommitted change is the earlier node-plan authority finding only.

This node is a leaf (`max_depth`, `max_children`, and `max_descendants` are all
zero), so no delegation is possible or needed for the narrowly coupled source
and test verification.

## Execution plan

1. Reinspect the committed two-file diff and relevant ticket/identity path for
   the exact-byte transport boundary; retain the existing implementation unless
   a concrete defect is demonstrated.
2. Run the focused upload tests and the complete required offline qualification:
   Ruff format/check, Pyright, coverage generation verification, repository
   policy checks, and the node test script.
3. Confirm every working-tree change is limited to the node plans or the two
   owned source/test paths, then preserve the node-plan record through the
   scoped completion commit.
4. In review/commit, fold private state to terminal form, state in radio that
   no shared-wiki promotion is needed because the approved handoff already
   records the durable contract, send the parent the commit and verification
   result, and signal `fractal node finish`.

## Acceptance

- A modified or replaced file buffer fails `FILE_CHANGED` before transport.
- Matching raw bytes still reach the binary client unchanged.
- Ticket one-use, path containment, symlink, no-retry, and redaction behaviors
  remain covered; coverage, live/UI/vision, and contract claims remain
  untouched.

## Verification results

- `uv run pytest tests/api/test_file_upload_writes.py`: 36 passed.
- Scoped Ruff format and lint checks passed; Pyright reported 0 errors,
  warnings, or information messages.
- `uv run python scripts/generate_coverage_report.py` regenerated the existing
  305 API / 339 UI artifact set with `complete=False`; the generated files
  produced no working-tree diff. `scripts/check_coverage.py
  --reject-false-completeness` passed.
- `scripts/check_repository_policy.py` and the node lint script passed.
- The required node test script completed successfully: 1,221 non-live,
  non-vision tests passed.
