# 129.1 Wave-5o freeze independent review

## Goal

Authoritative independent Grok **ACCEPT** or **REJECT** of the committed Wave-5o invoice-reminder freeze contract. Sole project deliverable: `wiki/wave_fiveo_freeze_independent_review.md`.

## Constraints

- Leaf node: max children/depth/descendants = 0 → **solo**, no spawn.
- Read-only on product: no edits to `src/**`, `tests/**`, `coverage/**`, `pyproject.toml`, server registration, freeze page, existing review records, or wiki index files.
- Primary source: current official docs at `https://www.billy.dk/api/` (fetch independently; record ETag/MD5/size). Distinguish current findings from inherited research.
- No headed browser, credentials, live mutations, UI discovery, raw evidence in repo.
- If official access fails **before** editing the review page (auth/quota/rate limit): report pre-edit failure to parent outbox and stop without a review page. Never hand off after editing.
- Baseline: root commit `a6a56cf` (present under branch HEAD `33ad102` init atop that baseline).

## Inputs (inspect all)

| Path | Role |
| --- | --- |
| `wiki/wave_fiveo_ticketed_writes_contract.md` | Freeze under review |
| `wiki/wave_fiveo_freeze_authoring_research_independent_review.md` | Prior research gate (not freeze ACCEPT) |
| `wiki/wave_fiveo_freeze_ready_research_independent_review.md` | Prior research gate |
| `wiki/wave_fiven_product_independent_review.md` | Prerequisite product ACCEPT context |
| `wiki/offline_write_probe_rules.md` | 401 open / 405 exclude rules |
| `coverage/api_v2_manifest.yaml` | invoiceReminders rows; create still red |
| `coverage/status.json` | 174/174 offline?, live 0, vision 0, complete false, bulk 92 |
| `docs/superpowers/specs/2026-07-28-billy-mcp-complete-design.md` | Ticketed write protocol |
| Template shape | `wiki/wave_fiven_freeze_independent_review.md` |
| Official | `https://www.billy.dk/api/` + unauth method gates on `https://api.billysbilling.com/v2` (JSON body probes only; no token) |

## Criteria to prove or reject

1. **Exact auth surface:** only `api.invoiceReminders.create` → only `api_invoice_reminders_create_preview` + `api_invoice_reminders_create_execute`.
2. **Wire freeze:** client-relative `POST /invoiceReminders`; outer `{invoiceReminder: map}`; ticket-only execute; shared confirmation/write services; exactly one non-retried write; required-root-only `invoiceReminders` response mapping.
3. **Opacity:** inner map opaque; all documented field boundaries preserved; no relation wire-form, nested association, date/number, email, or download semantics claims.
4. **Evidence gates:** `POST {}` 401 = offline create open; PUT / singular DELETE / bulk DELETE 405 = exclusions. Excluded forever in this freeze: update, delete, both bulk rows, all association writes, webhooks, live/UI/vision, cleanup qualification, coverage greening, product implementation, completeness.
5. **Root inventory honesty:** 174 implemented + contract-tested API rows; live 0; vision 0; 92 ambiguous bulk; `complete: false`; invoiceReminders create still red.

## Execution steps (EXECUTE)

1. **Fingerprint freeze page** at HEAD/`a6a56cf`: content MD5, confirm path unchanged.
2. **Fetch official docs** (curl/open_page only; not headed browser tooling for UI): record HTTP ETag, body MD5, byte length. Parse `/v2/invoiceReminders` Supports + property table. If fetch fails with auth/quota/rate limit before any review edit → radio parent and stop.
3. **Unauth method probes** (JSON object body, no token) on api host:
   - `POST /invoiceReminders` with `{}` → expect 401 AUTHENTICATION_REQUIRED
   - `PUT /invoiceReminders/...` → expect 405
   - singular `DELETE` → expect 405
   - bulk `DELETE` → expect 405
   Store only status/code summary in review (no raw secrets; no tracked raw bodies).
4. **Read freeze page fully** against criteria 1–4; note exact discrepancies if any.
5. **Read prior research/product pages + offline_write_probe_rules** only as context; do not rubber-stamp.
6. **Coverage audit** at baseline: count implemented/contract_tested; live/vision; ambiguous bulk; create row status for `api.invoiceReminders.create`; confirm no write tools registered for invoiceReminders (registry grep if present).
7. **Design doc** skim for ticketed write / no-retry / confirmation discipline alignment (non-claims for product).
8. **Write** `wiki/wave_fiveo_freeze_independent_review.md` only:
   - Valid frontmatter (mirror wave_fiven freeze review shape)
   - Authoritative ACCEPT or REJECT
   - Official-doc + root-path citations
   - Criterion-by-criterion table
   - Explicit non-claims (not product/live/UI/bulk/completeness)
   - REJECT must be actionable for parent/Codex freeze fix; do not edit freeze page
9. **Memory:** short state note of verdict + fingerprints (no iteration labels).
10. **Gates before commit:** `wiki lint --path=.../wiki`; node `lint.sh`; node `test.sh` — all must pass.
11. **Radio:** outbox verdict summary to parent (priority 5–6).
12. **COMMIT** review page only; clean tree.
13. **Drain + finish:** prune memory terminal; outbox promote-or-none line; `fractal node finish --reason=...` same iteration.

## Delegation decision

No children (caps all 0). Solo review is the right shape: one page, one verdict, independent of product implementer.

## Budget fit

- ~$320 remaining, ~20m/step, max $80/iter; work is fetch + read + one wiki page + lint/test.
- Feasible in one iteration if official access works. No product child to open on ACCEPT.

## Risks

- Official site/API unreachable → pre-edit stop + parent fallback to Codex Power full review.
- Inventory counts drift from stated 174 → REJECT on criterion 5 or document exact discrepancy if freeze is still sound but baseline numbers differ (prefer REJECT only when freeze claims are wrong; if freeze does not claim inventory totals, still verify criterion 5 as review requirement).
- Freeze claims update/delete incorrectly open → REJECT.

## Done when

- Review page committed and alone as project-wiki content change
- Lint + test pass
- Parent radio has ACCEPT/REJECT
- `fractal node finish` run

## Post-Mortem

### Accomplishments

- Independent re-fetch of https://www.billy.dk/api/ (ETag `wcw4x9hqvu3603`, MD5 `8b94b0135c91fd15fe54ea33e088a4be`, 147934 bytes).
- Independent unauth method probes: POST 401; PUT / singular DELETE / bulk DELETE 405.
- Verified freeze page MD5 `6fec5754cc76c4a07b344021cbc3c36b` at `a6a56cf` against all five required criteria.
- Wrote authoritative **ACCEPT** to `wiki/wave_fiveo_freeze_independent_review.md` with official-doc citations, root-path citations, criterion table, and explicit non-claims.
- Confirmed coverage honesty (174/174 offline, live 0, vision 0, bulk 92, complete false; create red) and freeze/src/tests/coverage untouched.
- `wiki lint`, node `lint.sh`, and `test.sh` pass.

### Deviations

- None material. Solo leaf as planned (child caps 0).
- Project `wiki/_index.md` gained only the mechanical link row for the new review page (required for wiki lint); freeze contract and other review records unchanged.
- ASCII dash normalization applied in REVIEW for Agents.md prose style (no claim changes).

### Next-iteration notes

- COMMIT the review page (and mechanical index/memory updates).
- Report ACCEPT to parent and run `fractal node finish` after clean commit.
- Do not open product child from this leaf; parent manages Codex Power product slice after freeze ACCEPT.
