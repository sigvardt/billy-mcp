# 175.1 UI authentication credentials fallback research

## Scope and constraints

Create one shared, cited research record only:
`wiki/ui_auth_credentials_login_organization_research_codex_fallback.md`.
This is a Codex Power fallback because the designated Grok runner could not
authenticate before it made edits. It is not a Grok finding and cannot satisfy
the required later Grok research, independent-review, or vision gate. This is a
leaf node (`max_depth`, `max_children`, and `max_descendants` are zero), so the
work cannot be decomposed to a child.

Do not use or disclose credentials, submit a login form, create or alter a
Billy record, make runtime browser/API probes beyond the approved documentation
fetch, retain raw evidence, modify source/tests/inventories/status, or make a
live, vision, parity, or completeness claim.

## Work plan

1. Fetch and read current official Billy documentation from direct official
   URLs before looking at repository runtime/configuration. Record only claims
   the source supports and place a direct URL beside each official claim.
2. Read committed design, browser runtime/configuration, egress policy,
   inventory/status, and existing UI-discovery/provenance records without
   executing a browser or API action. Keep repository observations separately
   labelled from official facts.
3. Write the bounded implementation brief. Preserve the sole observed login
   signature: root redirects to `https://mit.billy.dk/login`; email, password,
   remember, `button[data-cy=login-button]`, and labels `Log in` / `Log ind`.
   State that no credentials, submission, organisation selector, challenge, or
   post-login state has been observed.
4. Specify, without inventing post-login selectors or routes: secret-reference
   inputs and resolution, separate browser/TOTP handling, redaction and
   non-persistence, typed headless pre-submit checks, fail-closed error/state
   handling, expiry/reauthentication, only-observed challenge classification,
   organisation discovery/explicit selection, test seams, dedicated
   non-production-org cleanup/read-back, evidence purge, and the mandatory
   later Grok vision/research review.
5. Explicitly mark unknown post-login behaviour as a discovery blocker and all
   UI coverage red; do not infer a coverage, live, vision, parity, or
   completeness result. Update and lint the shared wiki. Inspect the exact
   diff to ensure the research page (plus unavoidable generated wiki index
   metadata) is the only project change.
6. In the close step, commit the research record, post a concise parent handoff
   that names the fallback limitation and blockers, drain radio/memory state,
   and finish the node only after the clean wiki lint is confirmed.

## Post-Mortem

### Accomplishments

- Published the cited, bounded Codex Power fallback research brief in the
  shared wiki and refreshed its generated index entry.
- Verified the record against current official Billy API documentation and the
  committed configuration, browser runtime, egress policy, inventory, design,
  and unit-test seams without using a credential or running a Billy workflow.
- Preserved the exact login observation, red UI posture, post-login discovery
  blocker, sensitive-evidence ban, and required Grok research/review and vision
  gates. Project-wiki lint and node checks pass.

### Deviations

- The designated Grok runner could not authenticate before edits, so its work
  was rerun as clearly labelled Codex Power fallback research rather than being
  attributed to Grok.

### Next-iteration notes

- No further work is assigned to this leaf. Credentialed login, post-login
  discovery, organisation selection, and any UI qualification remain blocked
  by the explicit gates in the delivered research record.
