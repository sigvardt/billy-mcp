# 153.1 Wave-5s-B files-upload Codex fallback review

## Scope and approach

This is a solo, read-only fallback review: the node is a leaf (`max_depth`,
`max_children`, and `max_descendants` are all zero), and the deliverable is one
new shared-wiki page. Do not use it as Grok evidence or as the mandatory Grok
audit.

1. Inspect baseline `5b8719b`, the Wave-5s-B and residual-specials research,
   the cited design, inventory/status, and the current client, confirmation,
   protocol, redaction, egress, and test conventions.
2. Compare each asserted upload contract detail with its preserved research:
   raw binary `POST /files`; official base host only; dual special/create alias
   ownership; no JSON-create or generic endpoint inference; exact tool-only
   ticket binding including canonical path, SHA-256, organisation, options, and
   file revalidation; and red coverage state.
3. Record only demonstrable findings in the new review page, separating
   verified facts from deliberate non-claims. State a limited handoff verdict
   and explicitly retain product, live API, UI, vision, bulk, completeness, and
   mandatory Grok-audit gates.
4. Run read-only policy/coverage and focused/full offline checks prescribed by
   the node scripts; run wiki lint after adding the page without regenerating
   the existing project index. Commit only the review plus node-private
   operational files, then finish once the checks and completion drain succeed.

## Post-Mortem

- Delivered the scoped Codex Power fallback review at
  `wiki/wave_fivesb_files_upload_research_codex_fallback_review.md` with a
  research-only ACCEPT and explicit non-claims.
- Verified the preserved contract, alias ownership, fixed-host and path guards,
  ticket conventions, red coverage, and product absence. Focused checks passed
  87 tests; the full offline suite passed 1180 tests; completeness stayed
  correctly fail-closed.
- The project wiki index deliberately remains untouched: regenerating it would
  violate the review's explicit prohibition on editing existing wiki pages.
- No further review work is required on this node.
