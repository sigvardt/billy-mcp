# 22.1 Wave-3 envelope re-review at 7965072

## Goal

Independent Grok re-review of Wave-3 tool-surface corrections at revision
`7965072`. Own deliverable only: `wiki/wave_three_envelope_rereview.md`.

Verdict must state whether these two prior findings are **closed** or name an
exact remaining defect:

1. Nested `request` input object on files/attachments/catalog tools (should be
   flat documented fields).
2. File/attachment list responses inventing top-level `paging` instead of
   preserving official optional `meta.paging`.

Also confirm corrections did not widen list filters and did not flip truthful
coverage: `live_tested=false`, UI red, bulk red, `complete=false`.

## Constraints

- Leaf node: `max_depth=0`, `max_children=0`, `max_descendants=0` — **no spawn**.
- Read-only on production code, tests, coverage, credentials, raw evidence.
- Headless / non-sensitive only. No live token use; no UI/vision completeness
  claims.
- Scope for commits: wiki only (`wiki/wave_three_envelope_rereview.md`).
- Do not merge or revive closed `wave3_independent_review` branch.

## Context (oriented)

| Item | State |
| --- | --- |
| HEAD | `7965072` — *correct Wave-3 tool contract envelopes* |
| Parent | `main.billy_complete` at same SHA (PREPARE already up to date) |
| Memory | empty |
| Wiki contracts | `wave_three_unfiltered_reads_contract.md` present |
| Correction files | `src/billy_mcp/api/file_attachment_reads.py`, `catalog_reads.py` + tests |
| Coverage | `coverage/status.json` has `complete: false`, `live_tested_rows: 0` |
| Skills | fractal/memory/radio/wiki (leaf: skip spawn) |

Primary modules to inspect:

- `src/billy_mcp/api/file_attachment_reads.py` — `api_files_*`, `api_attachments_*`
- `src/billy_mcp/api/catalog_reads.py` — `api_products_*`, `api_product_prices_*`
- `tests/api/test_file_attachment_reads.py`, `tests/api/test_catalog_reads.py`
- `wiki/wave_three_unfiltered_reads_contract.md`
- `coverage/status.json`, `coverage/api_v2_manifest.yaml` (files/attachments/products rows)
- Official docs: https://www.billy.dk/api/ (fingerprint etag/MD5/size)

## Delegation decision

**Solo.** No separable child work under zero spawn caps. One report file; one
revision; verification is the whole job.

## Work sequence (EXECUTE)

### A. Freeze evidence inputs

1. Confirm `git rev-parse HEAD` == `7965072`.
2. Fetch official Billy API docs fingerprint (etag, Content-MD5 or body MD5,
   byte size). Headless GET only; store notes in node tmp/memory, not secrets.
3. Read frozen contract `wiki/wave_three_unfiltered_reads_contract.md` for files,
   attachments, products, productPrices: list query allowlist and response
   envelope wording (`meta.paging` vs top-level `paging`).
4. Diff `7965072` vs parent of correction (`a206eb0`) for the four tool modules
   if useful for “what changed”.

### B. Verify eight flat input schemas

For tools:

- `api_files_get`, `api_files_list`
- `api_attachments_get`, `api_attachments_list`
- `api_products_get`, `api_products_list`
- `api_product_prices_get`, `api_product_prices_list`

Checks (must all pass for finding 1 closed):

1. **Source:** FastMCP registration uses flat parameters (id, include, page,
   pageSize, sortProperty, sortDirection) — not a single nested `request: {...}`
   tool argument. Compare peer modules that are already flat if needed.
2. **Runtime schema:** Load FastMCP tool list / input JSON Schema (script under
   node `tmp/`, no production edits). Confirm top-level properties are the
   documented fields; property set must **not** be solely `request`.
3. **Filters not widened:** list params limited to documented allowlist
   (page, pageSize, include, sortProperty, sortDirection ASC|DESC). Reject or
   omit offset and resource-specific filters.

### C. Verify two `meta.paging` outputs

For `api_files_list` and `api_attachments_list` only:

1. **Source:** success models keep optional `meta.paging` (not only top-level
   `paging`).
2. **Structured result:** drive offline/tool-result path (existing unit tests
   or in-process FastMCP call with mocked HTTP) and assert returned structured
   content shape includes `meta.paging` when upstream provides it, and does not
   invent a top-level `paging` field for these two tools.
3. Note catalog list models separately: if official docs place product paging
   differently, cite that; do not conflate with the files/attachments finding.

### D. Coverage non-regression

1. `coverage/status.json`: `complete` remains false; live counts remain zero.
2. Manifest rows for files/attachments/products/productPrices get+list:
   `live_tested: false`; writes/bulk/UI rows for same areas stay red/not live.
3. No claim that product is complete.

### E. Write report + radio + finish path

1. Write `wiki/wave_three_envelope_rereview.md` with:
   - frontmatter `desc:` (verdict-in-one-line)
   - revision `7965072`
   - docs URL + fingerprint
   - table/checklist for 8 tools (flat inputs) and 2 list envelopes
   - coverage non-regression note
   - explicit **CLOSED** / **OPEN** for findings 1 and 2
   - no live/UI/vision/completeness claim
2. Radio:
   - if any required defect remains → high-priority message to parent inbox with
     exact evidence (tool name, schema key, source line or schema dump path)
   - else outbox re-review **PASS**
3. Memory: terminal `state` page (facts only; no NEXT/Remaining).
4. Later COMMIT step: only wiki report; run node `scripts/test.sh` + lint before
   `fractal node finish` naming the report.

## Feasibility

| Budget | Fit |
| --- | --- |
| Time remaining ~7–8 min step / ~15 min iter / ~30 min run | Tight but leaf re-review is bounded: 2 modules + tests + schema probe + short wiki page |
| Cost | unlimited |
| Spawn | N/A (leaf) |
| Blockers | none known; BILLY_API_TOKEN must stay unused |

If FastMCP schema probe is slow, fall back to: inspect registered tool
decorators + pydantic Field signatures + pytest assertions that already dump
schemas, and cite those as primary evidence (still independent reading of
current HEAD).

## Out of scope

- Implementing fixes (parent/codex-power owns code).
- Merging other wave children or closed independent-review branch.
- Live API calls, headed browser, greening coverage.
- Editing anything under `src/`, `tests/`, `coverage/`.

## Success criteria (maps to Completion Requirements)

1. Wiki report exists, cited, covers 8 flat schemas + 2 meta.paging outputs at
   `7965072`.
2. Docs fingerprint recorded; no live/UI/vision/completeness claim.
3. Parent radio: defect with evidence OR pass announcement.
4. Only wiki report committed; non-live tests/lint pass; `node finish` with
   reason naming the report.

## Post-Mortem

### Accomplishments

- Independent re-review at HEAD `7965072` completed without production edits.
- Official docs fingerprint reconfirmed (`hsisik4g9p3603` /
  `c2efda0ee4cf9cf200e14910c5fc6996` / 147934 B).
- Finding 1 CLOSED: eight FastMCP tools expose flat inputs only (re-derived
  via `FunctionTool.parameters`).
- Finding 2 CLOSED: `api_files_list` / `api_attachments_list` preserve
  `meta.paging` in structured tool results (re-derived via `call_tool`).
- Coverage non-regression confirmed (`complete=false`, live/UI red).
- Report written to `wiki/wave_three_envelope_rereview.md`; PASS announced on
  outbox and parent inbox.

### Deviations

- Catalog list tools still flatten `meta.paging` to top-level `paging`. Documented
  as out-of-scope residual (finding 2 named only files/attachments lists).
- Node `scripts/test.sh` is a no-op; used focused module pytest (23 pass) plus
  in-process FastMCP probes for schema/result evidence.
- Leaf caps (0/0/0): no children spawned (solo work as planned).

### Next-iteration notes

- COMMIT wiki report + index only, then finish with reason naming the report.
- No further evidence regeneration required unless HEAD moves past `7965072`.
